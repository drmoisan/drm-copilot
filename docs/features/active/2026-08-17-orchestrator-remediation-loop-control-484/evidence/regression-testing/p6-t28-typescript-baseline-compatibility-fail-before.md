Timestamp: 2026-08-23T06-16
Command: `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/mcp-plan-gate-warning-projection.test.ts test/lib/validate/epic-orchestrator-state-codex-topology.test.ts test/lib/validate/orchestration-artifacts.test.ts --runInBand`
ExpectedExitCode: 1
EXIT_CODE: 1
Output Summary: The exact focused run reproduced the five deterministic compatibility failures recorded by the full-coverage safe boundary. Jest reported `3` failed suites of `3` total and `5` failed / `31` passed tests of `36` total, with `0` snapshots. The prior full-coverage result reported `3` failed / `210` passed suites and `5` failed / `2911` passed / `1` skipped tests.

Exact received-versus-expected values:

1. `test/mcp-plan-gate-warning-projection.test.ts`: expected schema keys were `["workspace_root","artifact_type","artifact_path","require_complete","require_model_routing","require_codex_model_routing","require_codex_topology","require_ready_for_execution"]`; received keys were `["workspace_root","artifact_type","artifact_path","require_complete","require_pr_creation_ready","require_model_routing","require_codex_model_routing","require_codex_topology","require_ready_for_execution"]`.
2. `test/lib/validate/epic-orchestrator-state-codex-topology.test.ts`, forced-root case: expected `Checkpoint codex_topology_receipts is missing the forced root persona receipt for epic-orchestrator.`; received `["ORCH_ROUTING_GATE_CODEX_TOPOLOGY: Checkpoint codex_topology_receipts is missing the forced root persona receipt for epic-orchestrator."]`.
3. `test/lib/validate/epic-orchestrator-state-codex-topology.test.ts`, child-evidence case: expected `Checkpoint codex_topology_receipts is missing a child topology receipt for recorded delegations.`; received `["ORCH_ROUTING_GATE_CODEX_TOPOLOGY: Checkpoint codex_topology_receipts is missing a child topology receipt for recorded delegations."]`.
4. `test/lib/validate/orchestration-artifacts.test.ts`, model-routing dispatch: expected `Checkpoint codex_model_routing_receipts must be a list when present.`; the returned array contained the exact routed diagnostic `ORCH_ROUTING_GATE_CODEX_MODEL: Checkpoint codex_model_routing_receipts must be a list when present.` plus the incomplete fixture's missing-key and incomplete-delegation structural diagnostics.
5. `test/lib/validate/orchestration-artifacts.test.ts`, ordinary topology dispatch: expected `Checkpoint codex_topology_receipts must be a list when present.`; the returned array contained the exact routed diagnostic `ORCH_ROUTING_GATE_CODEX_TOPOLOGY: Checkpoint codex_topology_receipts must be a list when present.` plus the incomplete fixture's missing-key and incomplete-delegation structural diagnostics. Jest stopped this test at the ordinary-checkpoint assertion before evaluating the subsequent epic-checkpoint assertion, whose current routed diagnostic has the same `ORCH_ROUTING_GATE_CODEX_TOPOLOGY: ` prefix.

Failure classification: one missing existing `require_pr_creation_ready` schema key, two missing `ORCH_ROUTING_GATE_CODEX_TOPOLOGY: ` prefixes in epic-topology expectations, and two incomplete-checkpoint dispatch failures caused by routed model/topology diagnostic prefixes.

Safe-boundary reconciliation: `p6-t28-typescript-jest-coverage.md` records that all three `TS-COMPAT-A` files and directly implicated non-owned production files (`src/mcp-tool-inputs.ts`, `src/lib/validate/orchestration-artifacts.ts`, and `src/lib/validate/epic-orchestrator-state-core.ts`) were byte-identical to HEAD. The owned `orchestrator-state-codex-topology.ts` diff changes only the P4-T8 invocation-local successful-resolution cache; it does not change the existing topology diagnostic prefix. No source, test, coverage, threshold, or recorded safe-boundary artifact was changed by this fail-before run.
