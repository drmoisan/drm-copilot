# 2026-07-22-mcp-promotion-tooling-defects (Remediation Plan — Cycle 1)

- **Issue:** #401
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-22T21-20
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug
- **Remediation cycle:** 1
- **Remediation inputs:** `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/remediation-inputs.2026-07-22T21-07.md`
- **Requirements source:** `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/spec.md` (AC-1..AC-14; this cycle targets AC-14 and AC-11 only)
- **Audit sources:** `feature-audit.2026-07-22T21-07.md`, `code-review.2026-07-22T21-07.md`, `policy-audit.2026-07-22T21-07.md` (same folder)
- **Branch head at cycle entry:** `9d2e7633bdb461e2c34b37a784e1f06f9628c73e` (base `main`, merge-base `a0b251d330525b8307467f4cf529c5cc3e947445`)

**Scope (binding):** Exactly R1 (Blocking, mandatory) and R2 (close AC-11). R3 is DEFERRED — no code work on the pre-existing over-500-line files `scripts/dev_tools/potential_to_issue.py` (639 lines) and `tests/scripts/dev_tools/test_potential_to_issue.py` (1076 lines); a deferral-note task in Phase 4 documents the rationale.

**Do-Not-Do list (binding, from remediation inputs):** Do not weaken, suppress, or reinterpret the 500-line rule or coverage thresholds; no `exclude` additions to coverage configuration. Do not change any MCP tool schema semantics, tool names, or the `workspace_root` required contract while extracting definitions (R1 is a pure module split). Do not modify the protected files `extensions/drm-copilot/src/lib/potential-to-issue/content.ts`, `extensions/drm-copilot/src/lib/potential-to-issue/promotion-filesystem.ts`, `extensions/drm-copilot/src/lib/prompt-mode-contract.ts`, `scripts/dev_tools/potential_to_issue_content.py`. Do not revert or alter the delivered Defect A/B behavior. R2 adds tests only — no production-code change; do not touch `potential_to_issue.py` decision branches. Do not skip toolchain stages or claim completion without single-pass green runs recorded with exit codes.

**Evidence location rule (non-overridable):** All evidence artifacts produced by this plan MUST be written under `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/evidence/<kind>/` where `<kind>` is one of `remediation-baseline`, `qa-gates`, `regression-testing`, `issue-updates`, `other`. `EVIDENCE:` below abbreviates that root. No `artifacts/` evidence path is permitted (`artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, `artifacts/evidence/` are all forbidden).

**Timestamp convention:** `<ts>` in artifact filenames denotes the ISO-8601 execution timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.

**Command working directories and shells:** TypeScript format/lint/typecheck commands run from `extensions/drm-copilot/` using `npx prettier` / `npx eslint` / `npx tsc` directly. Jest MUST be invoked through pwsh because direct node/npm/npx-jest invocations are allowlist-blocked: `pwsh: Set-Location extensions/drm-copilot; npm run test` (or `npm run test:coverage`). Python toolchain commands run from the repository root via `poetry run`. Dependencies are already installed. Line counts may be captured with `wc -l <file>` (bash) or `(Get-Content <file>).Count` (pwsh); record the command used.

### Phase 0 — Policy Reading and Remediation Baseline Capture

- [x] [P0-T1] Read policy files in the required order: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`. Write `EVIDENCE:/remediation-baseline/phase0-instructions-read.<ts>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: artifact exists with all three fields and lists all nine files.
- [x] [P0-T2] Record the branch name and HEAD commit SHA (`git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`) and confirm the head equals cycle-entry commit `9d2e7633bdb461e2c34b37a784e1f06f9628c73e`. Write `EVIDENCE:/remediation-baseline/phase0-branch-commit.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists and records branch, SHA, and an explicit match/mismatch statement against `9d2e7633`.
- [x] [P0-T3] TypeScript format baseline: from `extensions/drm-copilot/` run `npx prettier --check "src/**/*.ts" "test/**/*.ts"`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-ts-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; exit code recorded verbatim.
- [x] [P0-T4] TypeScript lint baseline: from `extensions/drm-copilot/` run `npx eslint src test`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-ts-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T5] TypeScript type-check baseline: from `extensions/drm-copilot/` run `npx tsc -p ./ --noEmit`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-ts-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T6] TypeScript test + coverage baseline: run `pwsh: Set-Location extensions/drm-copilot; npm run test:coverage`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-ts-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric line-coverage and branch-coverage percentages (expected near 96.34% lines / 89.21% branches) and the pass/fail test counts. Acceptance: artifact exists and `Output Summary:` contains numeric line % and branch % values (not placeholders).
- [x] [P0-T7] Python format baseline: from repo root run `poetry run black --check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-py-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Python lint baseline: from repo root run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-py-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T9] Python type-check baseline: from repo root run `poetry run pyright scripts/dev_tools/potential_to_issue.py scripts/dev_tools/potential_to_issue_content.py tests/scripts/dev_tools`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-py-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T10] Python test + coverage baseline: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term`. Write `EVIDENCE:/remediation-baseline/remediation-baseline-py-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric TOTAL line/branch coverage AND the per-module figures for `scripts/dev_tools/potential_to_issue.py` computed from its per-module counts (expected baseline: line 91.00% (182/200), branch 68.18% (45/66)), plus pass/fail test counts. Acceptance: artifact exists and `Output Summary:` contains the per-module branch percentage for `potential_to_issue.py` as a numeric value derived from per-module counts, not the TOTAL row.
- [x] [P0-T11] Line-count baseline for the R1 target: run `wc -l extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` (expected 504). Write `EVIDENCE:/remediation-baseline/remediation-baseline-r1-linecount.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the numeric count. Acceptance: artifact exists with the numeric line count establishing the R1 baseline.

### Phase 1 — R1: Decompose mcp-repo-automation-tool-definitions.ts Below 500 Lines

Constraint for every task in this phase: pure module split — no schema, behavior, tool-name, description, property, or `required`-array change; the array entry order of `REPO_AUTOMATION_TOOL_DEFINITIONS` is preserved exactly (21 directly-defined repo-automation tools followed by the `...DISCOVERY_TOOL_DEFINITIONS` spread).

- [x] [P1-T1] Create the new sibling module `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions-poshqc.ts` (precedent: `mcp-tool-inputs-push-down.ts` / `mcp-tool-inputs-potential-to-issue.ts`) exporting a constant `POSHQC_TOOL_DEFINITIONS: ReadonlyArray<ToolDefinition>` containing the five PoshQC tool definitions moved verbatim from `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`: `run_poshqc_format`, `run_poshqc_analyze`, `run_poshqc_test`, `run_poshqc_analyze_autofix`, `run_poshqc_suite` (currently lines ~265-369). The sibling imports `workspaceRootProperty` from `./mcp-push-down-schema-properties` and the `ToolDefinition` type via a type-only import (`import type { ToolDefinition } from "./mcp-repo-automation-tool-definitions";`) so no runtime import cycle is introduced, and carries a docblock stating the extraction rationale (500-line production-file limit). Acceptance: the new file exists, contains exactly the five PoshQC definitions with byte-identical schema content (including `required: ["workspace_root"]` in each), and is <= 500 lines.
- [x] [P1-T2] Update `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`: remove the five moved PoshQC definitions and splice `...POSHQC_TOOL_DEFINITIONS` into `REPO_AUTOMATION_TOOL_DEFINITIONS` at the exact original position (after `new_active_feature_folder`, before `resolve_policy_audit_template_asset`), and re-export the extracted constant from the original module (`export { POSHQC_TOOL_DEFINITIONS } from "./mcp-repo-automation-tool-definitions-poshqc";`) so the public import surface is unchanged. Acceptance: `REPO_AUTOMATION_TOOL_DEFINITIONS` still exposes all 21 repo-automation tools plus the discovery spread (28 entries total) in the original order with every `required` array still including `workspace_root`, and the file is <= 500 lines.
- [x] [P1-T3] Sweep for imports and tests referencing the moved definitions: run `grep -rn "run_poshqc" extensions/drm-copilot/src extensions/drm-copilot/test` and `grep -rn "mcp-repo-automation-tool-definitions" extensions/drm-copilot/src extensions/drm-copilot/test`, update any import site that must change (expected: none, since consumers import `REPO_AUTOMATION_TOOL_DEFINITIONS` from the original module), then run `npx tsc -p ./ --noEmit` from `extensions/drm-copilot/`. Record the grep findings, any updates made, and the tsc result in `EVIDENCE:/other/r1-import-sweep.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: tsc exits 0 and the artifact records the sweep result.
- [x] [P1-T4] Verify behavior invariance: run the full Jest suite via `pwsh: Set-Location extensions/drm-copilot; npm run test` and confirm all suites pass unchanged — in particular the AC-5 length-pinned `workspace_root required contract` assertions in `extensions/drm-copilot/test/mcp-repo-automation-tool-definitions.test.ts` (all 28 entries) and `extensions/drm-copilot/test/mcp-epic-validation-definitions.test.ts`. Write `EVIDENCE:/other/r1-extraction-verify.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including test counts. Acceptance: exit code 0 with zero test modifications required beyond any import-path updates recorded in P1-T3.
- [x] [P1-T5] Interim line-count check: run `wc -l extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts extensions/drm-copilot/src/mcp-repo-automation-tool-definitions-poshqc.ts`. Write `EVIDENCE:/other/r1-linecount-interim.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording both numeric counts. Acceptance: both files are <= 500 lines and the artifact records both numeric counts.

### Phase 2 — R2: Raise potential_to_issue.py Per-Module Branch Coverage to >= 75%

Constraint for every task in this phase: NO production-code change. New pytest cases go ONLY into the new file `tests/scripts/dev_tools/test_potential_to_issue_branches.py`; do not grow `tests/scripts/dev_tools/test_potential_to_issue.py` (already 1076 lines).

- [x] [P2-T1] Identify the currently-partial branches: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term-missing` and extract the partial-branch line numbers for `scripts/dev_tools/potential_to_issue.py` (baseline: 21 partial of 66 total; 45 hit). Write `EVIDENCE:/other/py-partial-branches.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the partial-branch locations to target. Acceptance: artifact enumerates the partial branches of `potential_to_issue.py` by line/arrow notation from the term-missing report.
- [x] [P2-T2] Create the new test file `tests/scripts/dev_tools/test_potential_to_issue_branches.py` containing new pytest cases that exercise the partial branches identified in P2-T1 sufficiently to raise `scripts/dev_tools/potential_to_issue.py` per-module branch coverage from 45/66 to >= 50/66 (>= 75%). Tests must use the existing fake seams (`FakeFileSystem`, `FakeGhClient`) with no network, subprocess, real-filesystem, or temporary-file usage; follow Arrange–Act–Assert with descriptive names and docstrings naming the targeted branch; the file must be <= 500 lines. No file under `scripts/dev_tools/` may be modified. Acceptance: the new file exists, is <= 500 lines, all its tests pass via `poetry run pytest tests/scripts/dev_tools/test_potential_to_issue_branches.py -q --no-cov` (exit 0), and `git status --porcelain scripts/dev_tools` is empty.
- [x] [P2-T3] Verify the per-module branch threshold: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term` and compute the `scripts/dev_tools/potential_to_issue.py` branch percentage from its per-module counts (hit branches / 66; PASS requires >= 50/66 = 75.76% >= 75%). Write `EVIDENCE:/other/py-branch-coverage-verify.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` stating the per-module numerator/denominator and percentage explicitly. Acceptance: exit code 0 and the artifact records a per-module branch percentage >= 75% computed from per-module counts (not the TOTAL row). If the threshold is not met, return to P2-T2 and add further branch-targeted cases before re-running this task.
- [x] [P2-T4] Verify zero production diff for R2: run `git diff --name-only a0b251d330525b8307467f4cf529c5cc3e947445..HEAD -- scripts/dev_tools` and confirm the only Python production change on the branch remains the pre-existing `scripts/dev_tools/potential_to_issue.py` lockstep reorder from the original cycle (no new modifications from this remediation cycle; `git status --porcelain scripts/dev_tools` empty). Write `EVIDENCE:/other/r2-no-production-change.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact confirms this remediation cycle introduced no change under `scripts/dev_tools/`.

### Phase 3 — Final QA Loop (Both Languages), Coverage Deltas, Line Counts, Scope Integrity

Restart rule (binding): within each language loop, if any step fails or changes files, fix the cause and restart that language's loop from its first step (P3-T1 for TypeScript, P3-T5 for Python) until every step passes in one uninterrupted single pass. Recorded artifacts must reflect the final clean pass. No `SKIPPED` outcome is permitted for any task in this phase.

- [x] [P3-T1] TypeScript formatting: from `extensions/drm-copilot/` run `npx prettier --check "src/**/*.ts" "test/**/*.ts"`; if it fails, run `npx prettier --write "src/**/*.ts" "test/**/*.ts"` and restart the TypeScript loop from this task. Write `EVIDENCE:/qa-gates/final-ts-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for the final clean check. Acceptance: final recorded check exits 0.
- [x] [P3-T2] TypeScript linting: from `extensions/drm-copilot/` run `npx eslint src test`. Write `EVIDENCE:/qa-gates/final-ts-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 in the same pass as P3-T1.
- [x] [P3-T3] TypeScript type-check: from `extensions/drm-copilot/` run `npx tsc -p ./ --noEmit`. Write `EVIDENCE:/qa-gates/final-ts-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 in the same pass as P3-T1..T2.
- [x] [P3-T4] TypeScript tests with coverage: run `pwsh: Set-Location extensions/drm-copilot; npm run test:coverage`. Write `EVIDENCE:/qa-gates/final-ts-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric line % and branch % and pass/fail counts. Acceptance: exit code 0 in the same pass as P3-T1..T3, with numeric coverage values recorded (not placeholders).
- [x] [P3-T5] Python formatting: from repo root run `poetry run black --check scripts/dev_tools tests/scripts/dev_tools`; if it fails, run `poetry run black scripts/dev_tools tests/scripts/dev_tools` and restart the Python loop from this task. Write `EVIDENCE:/qa-gates/final-py-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for the final clean check. Acceptance: final recorded check exits 0.
- [x] [P3-T6] Python linting: from repo root run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/qa-gates/final-py-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 in the same pass as P3-T5.
- [x] [P3-T7] Python type-check: from repo root run `poetry run pyright scripts/dev_tools/potential_to_issue.py scripts/dev_tools/potential_to_issue_content.py tests/scripts/dev_tools` (the path `tests/scripts/dev_tools` includes the new `test_potential_to_issue_branches.py`). Write `EVIDENCE:/qa-gates/final-py-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 in the same pass as P3-T5..T6.
- [x] [P3-T8] Python tests with coverage: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term`. Write `EVIDENCE:/qa-gates/final-py-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric TOTAL line/branch coverage, the per-module line and branch figures for `scripts/dev_tools/potential_to_issue.py` computed from per-module counts, and pass/fail counts. Acceptance: exit code 0 in the same pass as P3-T5..T7, with the per-module branch percentage recorded numerically.
- [x] [P3-T9] TypeScript coverage delta: compare P3-T4 results against the pre-remediation figures (96.34% lines / 89.21% branches from `evidence/qa-gates/coverage-delta-ts.2026-07-22T20-17.md` and P0-T6). Write `EVIDENCE:/qa-gates/coverage-delta-ts.<ts>.md` with `Timestamp:`, the compared commands, `EXIT_CODE:`, and an `Output Summary:` stating baseline %, post-change %, and the per-file coverage of the two R1 files (`mcp-repo-automation-tool-definitions.ts`, `mcp-repo-automation-tool-definitions-poshqc.ts`). Acceptance: artifact shows line >= 85%, branch >= 75%, and no regression versus 96.34%/89.21% beyond reporting noise attributable to the pure module split, with the comparison stated numerically.
- [x] [P3-T10] Python coverage delta (CORRECTED artifact): compare P3-T8 results against P0-T10. Write `EVIDENCE:/qa-gates/coverage-delta-py.<ts>.md` with `Timestamp:`, the compared commands, `EXIT_CODE:`, and an `Output Summary:` that states the `scripts/dev_tools/potential_to_issue.py` PER-MODULE branch percentage explicitly with its numerator/denominator (threshold check: >= 75%, i.e. >= 50/66), the per-module line percentage (>= 85%), and the TOTAL row separately labeled as repo-wide context only. The AC-11 threshold verdict MUST be computed from the per-module counts, never the TOTAL row (this corrects the evidence-accuracy defect in `coverage-delta-py.2026-07-22T20-17.md` line 15). Acceptance: artifact exists with the per-module branch percentage >= 75% stated numerically and the threshold verdict tied to per-module counts.
- [x] [P3-T11] Corrected line-count evidence over EVERY changed/new production and test file: enumerate the full branch file set with `git diff --name-only a0b251d330525b8307467f4cf529c5cc3e947445..HEAD -- '*.ts' '*.py'`, exclude non-code paths (`docs/**`), and run `wc -l` on every remaining production and test file (must include at minimum: `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`, `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions-poshqc.ts`, `extensions/drm-copilot/src/mcp-discovery-tool-definitions.ts`, `extensions/drm-copilot/src/mcp-tool-definitions.ts`, `extensions/drm-copilot/src/mcp-tool-inputs.ts`, `extensions/drm-copilot/src/mcp-tool-inputs-potential-to-issue.ts`, `extensions/drm-copilot/src/mcp-push-down-schema-properties.ts`, `extensions/drm-copilot/src/workflow-command-arguments.ts`, `extensions/drm-copilot/src/lib/potential-to-issue/promotion.ts`, all changed/new files under `extensions/drm-copilot/test/`, `scripts/dev_tools/potential_to_issue.py`, `tests/scripts/dev_tools/test_potential_to_issue.py`, `tests/scripts/dev_tools/test_potential_to_issue_branches.py`). Write `EVIDENCE:/qa-gates/linecount-all-changed.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing every file with its numeric count. Acceptance: every listed file is <= 500 lines EXCEPT exactly the two pre-existing R3-deferred files (`scripts/dev_tools/potential_to_issue.py` 639, `tests/scripts/dev_tools/test_potential_to_issue.py` 1076), which the artifact flags as `DEFERRED (R3)`.
- [x] [P3-T12] Scope-integrity check: run `git diff --name-status a0b251d330525b8307467f4cf529c5cc3e947445..HEAD` and confirm the protected files `extensions/drm-copilot/src/lib/potential-to-issue/content.ts`, `extensions/drm-copilot/src/lib/potential-to-issue/promotion-filesystem.ts`, `extensions/drm-copilot/src/lib/prompt-mode-contract.ts`, and `scripts/dev_tools/potential_to_issue_content.py` are absent from the diff, and that this remediation cycle changed only the R1 TypeScript files (new sibling + original + any import updates recorded in P1-T3) and the new R2 test file. Write `EVIDENCE:/qa-gates/scope-integrity.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming each protected file and its verified-unchanged status. Acceptance: all four protected files are verified unchanged and the remediation-cycle change set matches the R1+R2 scope exactly.

### Phase 4 — AC Re-Check and R3 Deferral Note

- [x] [P4-T1] R3 deferral note (documentation only; no code work): write `EVIDENCE:/other/r3-deferral.<ts>.md` with `Timestamp:` and the deferral rationale: `scripts/dev_tools/potential_to_issue.py` (639 lines) and `tests/scripts/dev_tools/test_potential_to_issue.py` (1076 lines) exceeded 500 lines at merge-base `a0b251d3` (634 and 1017 respectively), predate this branch, and require a parity-sensitive decomposition coordinated with `promotion.ts` byte-parity headers; the orchestrator defers them to a follow-up issue per the remediation-inputs Exit Condition allowance and the April 2026 precedent (`docs/features/archive/2026-04-05-potential-to-issue-missing-label-123/policy-audit.2026-04-05T15-30.md`). Acceptance: artifact exists and states the deferral decision, both file line counts, and the precedent citation.
- [x] [P4-T2] Re-check AC-14 in `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/spec.md`: update the AC-14 evidence citation to reference `EVIDENCE:/qa-gates/linecount-all-changed.<ts>.md` (P3-T11), the R1 final QA artifacts (P3-T1..T4, P3-T9), and the R3 deferral note (P4-T1), and set the checkbox to `[x]` only if P3-T11 shows every changed/new production and test file <= 500 lines apart from the two explicitly R3-deferred pre-existing files; otherwise leave it unchecked and record the shortfall. Acceptance: the AC-14 line in `spec.md` cites the new evidence artifacts and its checkbox state matches the P3-T11 outcome.
- [x] [P4-T3] Re-check AC-11 in `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/spec.md`: update the AC-11 evidence citation to reference `EVIDENCE:/qa-gates/coverage-delta-py.<ts>.md` (P3-T10) and the final Python QA artifacts (P3-T5..T8), and set the checkbox to `[x]` only if P3-T10 records `scripts/dev_tools/potential_to_issue.py` per-module branch coverage >= 75% (>= 50/66) and per-module line coverage >= 85% with the full Python toolchain green in a single pass; otherwise leave it unchecked and record the shortfall. Acceptance: the AC-11 line in `spec.md` cites the new evidence artifacts and its checkbox state matches the P3-T10 outcome.
