{
  "description": "V2 shared-surface enumeration, Blocking. The declared radius covers both plan paths by glob, so V1 passes, but it enumerates shared surfaces only by the glob config/**; glob coverage alone is insufficient, so both touched surfaces are reported. The second surface is a member by shared_surface_globs rather than by the literal list.",
  "input": {
    "plan_text": "### Phase 1 — Routing\n- [ ] [P1-T1] Update `config/orchestration-routing.json` and `scripts/dev_tools/validate_orchestration_artifacts.py`.\n",
    "spec_text": "",
    "feature_folder": "2026-08-07-v2-fixture-447",
    "computed_at": "2026-08-07T12-00",
    "tracked_file_count": 100,
    "radius": {
      "paths": ["config/**", "scripts/dev_tools/**"],
      "modules": ["config", "python-dev-tools"],
      "shared_surfaces": ["config/**"],
      "contracts": [],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "config": {
      "version": 1,
      "shared_surfaces": ["config/orchestration-routing.json", "poetry.lock"],
      "shared_surface_globs": ["scripts/dev_tools/validate_*.py"],
      "modules": {
        "config": ["config/**"],
        "docs": ["docs/**"],
        "python-dev-tools": ["scripts/dev_tools/**"],
        "tests": ["tests/**"]
      },
      "over_breadth_fraction": 0.25
    }
  },
  "expected": {
    "radius": {
      "paths": [
        "config/orchestration-routing.json",
        "docs/features/active/2026-08-07-v2-fixture-447/**",
        "scripts/dev_tools/validate_orchestration_artifacts.py"
      ],
      "modules": ["config", "docs", "python-dev-tools"],
      "shared_surfaces": [
        "config/orchestration-routing.json",
        "scripts/dev_tools/validate_orchestration_artifacts.py"
      ],
      "contracts": [],
      "source": "derived",
      "computed_at": "2026-08-07T12-00"
    },
    "findings": [
      {
        "rule": "V2",
        "severity": "Blocking",
        "subject": "config/orchestration-routing.json",
        "message": "Shared surface config/orchestration-routing.json is touched but is not enumerated in blast_radius.shared_surfaces."
      },
      {
        "rule": "V2",
        "severity": "Blocking",
        "subject": "scripts/dev_tools/validate_orchestration_artifacts.py",
        "message": "Shared surface scripts/dev_tools/validate_orchestration_artifacts.py is touched but is not enumerated in blast_radius.shared_surfaces."
      }
    ]
  }
}
