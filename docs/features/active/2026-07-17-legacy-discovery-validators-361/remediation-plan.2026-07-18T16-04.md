# Remediation Plan — legacy-discovery-validators (#361)

- Timestamp: 2026-07-18T16-04
- Source: `docs/features/active/2026-07-17-legacy-discovery-validators-361/remediation-inputs.2026-07-18T16-04.md`
- Scope: This is a remediation-cycle plan. It addresses exactly the two findings recorded in `remediation-inputs.2026-07-18T16-04.md` (Finding 1, Blocking; Finding 2, Non-blocking). It does not re-plan the feature and adds no unrelated phases.
- Feature folder: `docs/features/active/2026-07-17-legacy-discovery-validators-361/`
- Evidence root: `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/` (canonical `<FEATURE>/evidence/<kind>/` scheme; no `artifacts/` path is used for evidence in this plan).
- `<TIMESTAMP>` below denotes the ISO-8601 `yyyy-MM-ddTHH-mm` execution timestamp recorded at the time each task actually runs (not the remediation-cycle timestamp `2026-07-18T16-04`).

### Phase 0 — Policy Reading and Remediation Baseline Capture

- [x] [P0-T1] Read, in order, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/python.md`, and write `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/remediation-baseline/phase0-instructions-read.<TIMESTAMP>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of the four files read. Acceptance: the artifact exists and lists all four files in the stated order.
- [x] [P0-T2] Run `poetry run pytest --cov=scripts.dev_tools.schema_loading --cov-branch --cov-report=term-missing tests/scripts/dev_tools/test_schema_loading.py` against the pre-remediation state of `tests/scripts/dev_tools/test_schema_loading.py` (before any tests are added in Phase 1) and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (stating the exact per-file line and branch percentages) in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/remediation-baseline/baseline-pytest-schema-loading.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0` and the recorded branch coverage is below 75% (reproducing the 85.71% line / 71.43% branch figures reported in `remediation-inputs.2026-07-18T16-04.md`), confirming the finding before remediation.

### Phase 1 — Close Branch-Coverage Gaps in `schema_loading.py` Tests (Finding 1)

- [ ] [P1-T1] In `tests/scripts/dev_tools/test_schema_loading.py`, add a test function named `test_load_schema_file_scheme_returns_parsed_content` that, using the existing `mem_fs_path` fixture (no real temporary files, no network calls), writes a schema JSON file under `mem_fs_path`, builds a `file://` URI from that file's resolved path, calls `schema_loading.load_schema` with that URI, and asserts the returned `dict` equals the written schema content. Acceptance: the function exists in the file, uses `mem_fs_path`, contains no network call, and exercises the `file://`-scheme success path (source lines 98-100 and 103 of `scripts/dev_tools/schema_loading.py`).
- [ ] [P1-T2] In `tests/scripts/dev_tools/test_schema_loading.py`, add a test function named `test_load_schema_file_scheme_missing_raises_file_not_found` that, using the `mem_fs_path` fixture, builds a `file://` URI pointing at a path under `mem_fs_path` that is never created, and asserts `schema_loading.load_schema` raises `FileNotFoundError` for that URI. Acceptance: the function exists in the file, uses `mem_fs_path`, and exercises the `file://`-scheme not-found branch at source line 101 of `scripts/dev_tools/schema_loading.py`.
- [ ] [P1-T3] In `tests/scripts/dev_tools/test_schema_loading.py`, add a test function named `test_load_schema_relative_path_missing_raises_file_not_found` that, using the `mem_fs_path` fixture, calls `schema_loading.load_schema` with a scheme-less URI and a `base_path` under `mem_fs_path` whose resolved sibling file is never created, and asserts `FileNotFoundError` is raised. Acceptance: the function exists in the file, uses `mem_fs_path`, and exercises the scheme-less relative-path not-found branch at source line 94 of `scripts/dev_tools/schema_loading.py`.

### Phase 2 — Verify Branch-Coverage Threshold Is Met

- [ ] [P2-T1] Run `poetry run pytest --cov=scripts.dev_tools.schema_loading --cov-branch --cov-report=term-missing tests/scripts/dev_tools/test_schema_loading.py` after Phase 1's test additions and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (stating the exact numeric per-file line and branch coverage percentages for `schema_loading.py`) in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/schema-loading-branch-coverage-fix.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0`, all tests pass, and the recorded branch coverage percentage for `schema_loading.py` is `>= 75%`.

### Phase 3 — Correct the Mislabeled Coverage Metric (Finding 2)

- [ ] [P3-T1] Write `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-pytest-new-code-correction.<TIMESTAMP>.md` that: (a) references the exact numeric line and branch coverage percentages for `schema_loading.py` recorded in `P2-T1`'s artifact, stated separately (not as a blended metric); (b) names the original artifact `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-pytest-new-code.2026-07-18T10-42.md` by path and states that its "82% line coverage" figure was `coverage.py`'s blended statement+branch `percent_covered` metric (81.63% precise), not pure line coverage; and (c) states that the original artifact is left unmodified and this file is the correction. Acceptance: the new artifact exists at the stated path, states both the line and branch percentages numerically and separately, and the original `final-qc-pytest-new-code.2026-07-18T10-42.md` file is unmodified (byte-identical to its pre-remediation content).

### Phase 4 — Acceptance-Criteria Reconciliation

- [ ] [P4-T1] In `docs/features/active/2026-07-17-legacy-discovery-validators-361/spec.md`, change the Definition-of-Done checkbox for the "Toolchain pass completed ... with line coverage >= 85% and branch coverage >= 75% on all new/changed files" item (the item carrying the `**Gap (feature-review 2026-07-18T16-04):**` annotation about `schema_loading.py`'s branch coverage) from `- [ ]` to `- [x]`, and append a short note that the gap was closed by the tests added in Phase 1 of this remediation plan and verified in `P2-T1`. Perform this edit only after `P2-T1`'s artifact confirms `schema_loading.py` branch coverage `>= 75%`. Acceptance: the checkbox reads `- [x]` and the appended note cites `P2-T1`'s evidence artifact path.
- [ ] [P4-T2] In `docs/features/active/2026-07-17-legacy-discovery-validators-361/user-story.md`, change the Acceptance Criteria checkbox for the "Tests satisfy quality-tier policy (line >= 85%, branch >= 75%) ..." item (the item carrying the `**Gap (feature-review 2026-07-18T16-04):**` annotation) from `- [ ]` to `- [x]`, and append a short note that the gap was closed by the tests added in Phase 1 of this remediation plan and verified in `P2-T1`. Perform this edit only after `P4-T1` is complete and only after `P2-T1`'s artifact confirms `schema_loading.py` branch coverage `>= 75%`. Acceptance: the checkbox reads `- [x]` and the appended note cites `P2-T1`'s evidence artifact path.

### Phase 5 — Full Regression and Final QA Loop (Python)

- [ ] [P5-T1] Run `poetry run black --check scripts/dev_tools/schema_loading.py tests/scripts/dev_tools/test_schema_loading.py` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-black-remediation.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0`.
- [ ] [P5-T2] Run `poetry run ruff check scripts/dev_tools/schema_loading.py tests/scripts/dev_tools/test_schema_loading.py` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-ruff-remediation.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0` and 0 lint errors reported.
- [ ] [P5-T3] Run `poetry run pyright scripts/dev_tools/schema_loading.py tests/scripts/dev_tools/test_schema_loading.py` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-pyright-remediation.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0` and 0 type errors reported.
- [ ] [P5-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` (full test suite) and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (stating aggregate pass/fail counts and aggregate line and branch coverage percentages) in `docs/features/active/2026-07-17-legacy-discovery-validators-361/evidence/qa-gates/final-qc-pytest-full-suite-remediation.<TIMESTAMP>.md`. Acceptance: `EXIT_CODE: 0`, all tests pass, and the recorded aggregate line and branch coverage percentages are each greater than or equal to the pre-remediation aggregate figures reported in `remediation-inputs.2026-07-18T16-04.md` (88.21% line / 79.02% branch), confirming no regression.
