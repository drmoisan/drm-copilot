# 2026-06-27-fix-csharp-push-down-pack-name (Plan)

- **Issue:** #256
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-06-27T14-16
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** minor-audit (bug)
- **Requirements source:** `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/issue.md` (sole source; `## Acceptance Criteria` AC1–AC6). `spec.md` and `user-story.md` intentionally do not exist for this minor-audit folder and are not required.
- **Evidence root:** `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/<kind>/`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QC artifact tasks, and coverage-comparison tasks for the in-scope language (TypeScript) where policy requires coverage. If any required baseline artifact, QC artifact, or coverage-comparison artifact is missing or has incomplete fields, the verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path in each evidence-producing task. Do not mark evidence-backed work complete without the artifact present and field-complete (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`).

**Toolchain note (verified against `extensions/drm-copilot/package.json`):** This package uses Jest (via `node run-jest.cjs`), not Vitest, and has no `test:coverage` script. The actual commands, run from `extensions/drm-copilot/`, are:
- format: `npm run format`
- lint: `npm run lint`
- type-check: `npm run typecheck`
- test (coverage): `npm test -- --coverage` (forwards `--coverage` to Jest; `coverageProvider: v8` is set in `jest.config.cjs`)
Each task below records the exact command actually executed in its evidence artifact.

**Scope note:** Single production file changed plus one new pure helper module and one new unit-test file. The changed file is extension source TypeScript (not a bundled runtime mirror), so no `.claude` / `.codex` / `.agents` mirror sync is required for this change.

---

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read the policy files in the required order and record them in a single evidence artifact at `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/baseline/phase0-instructions-read.md`. The artifact must contain `Timestamp:`, `Policy Order:`, and an explicit list of files read, covering: `CLAUDE.md`, `.github/copilot-instructions.md`, `.github/instructions/general-code-change.instructions.md`, `.github/instructions/general-unit-test.instructions.md`, `.github/instructions/typescript-code-change.instructions.md`, `.github/instructions/typescript-unit-test.instructions.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, and `.claude/rules/typescript-suppressions.md`. Acceptance: artifact exists and lists every file above.
- [x] [P0-T2] Capture baseline formatting state. Run `npm run format -- --check` from `extensions/drm-copilot/` (use `prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` if the `--check` forward is not honored; record the exact command run). Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/baseline/baseline-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T3] Capture baseline lint state. Run `npm run lint` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/baseline/baseline-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T4] Capture baseline type-check state. Run `npm run typecheck` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/baseline/baseline-typecheck.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T5] Capture baseline test + coverage state. Run `npm test -- --coverage` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/baseline/baseline-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that includes the numeric baseline coverage headline (overall line % and branch %) and the pass/fail test count. Acceptance: artifact exists with all four fields populated and numeric line and branch coverage values recorded (no placeholders).

### Phase 1 — Constrained Small-Path Implementation (handoff)

- [x] [P1-T1] Create a new pure, vscode-free helper module at `extensions/drm-copilot/src/lib/push-down/claude-pack-name-translation.ts` exporting a single pure function (proposed `translateSelectedPackNames(packs: ReadonlyArray<string>, csharpVariant: "modern" | "legacy" | undefined): string[]`) that returns the forwarded pack list with a `csharp` entry replaced by `csharp-${csharpVariant}` and all non-C# entries (`python`, `powershell`, `typescript`, and any others) unchanged and in their original order. The function must contain no `vscode` import and no I/O. Acceptance: file exists, exports the function, and `npm run typecheck` from `extensions/drm-copilot/` passes for the new module.
- [x] [P1-T2] Add input-guard behavior to the pure function in `extensions/drm-copilot/src/lib/push-down/claude-pack-name-translation.ts`: when `packs` includes `csharp` and `csharpVariant` is `undefined`, throw a clear `Error` (fail fast, per `.claude/rules/general-code-change.md`); when `packs` does not include `csharp`, return the list unchanged regardless of `csharpVariant`. Acceptance: the function raises on the unresolved-variant case and is otherwise total.
- [x] [P1-T3] Edit `extensions/drm-copilot/src/repo-automation-command-registration-admin.ts` in `registerPushDownClaudeCustomizationsCommand` to call the pure `translateSelectedPackNames` helper after the C# variant is chosen and forward the translated pack list (instead of the raw `packs`) to `options.service.pushDownClaudeCustomizations(...)`. Keep the existing variant prompt gating (`packs.includes("csharp")`) unchanged so no variant prompt is shown when C# is not selected. Acceptance: the service call forwards `csharp-modern` / `csharp-legacy` for the C# selection and leaves non-C# names unchanged (verifies AC1, AC2, AC3, AC4).
- [x] [P1-T4] Wrap the `options.service.pushDownClaudeCustomizations(...)` invocation in `extensions/drm-copilot/src/repo-automation-command-registration-admin.ts` in a try/catch that, on a thrown failure, writes the failure to `options.output` (the existing `OutputChannel` on `RepoAutomationCommandRegistrationOptions`) using the existing `[${commandId}] ...` log convention before re-throwing so the modal still surfaces. Acceptance: a thrown service failure is appended to the output channel before being re-thrown (verifies AC5).
- [x] [P1-T5] Add a unit-test file at `extensions/drm-copilot/test/lib/push-down/claude-pack-name-translation.test.ts` (Jest, `@jest/globals`, mirroring the source path) covering AC1–AC4 on the pure function: `csharp` + `modern` -> includes `csharp-modern` and excludes literal `csharp`; `csharp` + `legacy` -> includes `csharp-legacy`; selection without `csharp` (`python`, `powershell`, `typescript`) -> returned unchanged and in order; `csharp` selected with `undefined` variant -> throws. Use Arrange–Act–Assert and one behavior per test. Acceptance: file exists; tests target the pure function with no `vscode` host dependency.
- [x] [P1-T6] Add a unit test for the output-channel logging seam (AC5) at the testable seam without the VS Code host. If the try/catch logging is extracted into a small pure/loggable helper (for example a function taking a logger interface with `appendLine`), test that a thrown failure is logged before propagation using a fake logger; otherwise add the assertion to the command-registration test that mocks `vscode` (as in `extensions/drm-copilot/test/extension-command-helpers.test.ts`) and a stub `output.appendLine`. Place the test under `extensions/drm-copilot/test/` mirroring the source path. Acceptance: a test asserts the failure is written to the output channel before being surfaced.
- [x] [P1-T7] Confirm implementation completeness against issue AC1–AC6: AC1 (`modern` -> `csharp-modern`), AC2 (`legacy` -> `csharp-legacy`), AC3 (non-C# unchanged, no variant prompt when C# absent), AC4 (no `Pack manifest is missing for pack 'csharp'`; variant-qualified manifest resolved), AC5 (service failure written to output channel before surfacing), AC6 (unit tests cover AC1–AC5 and toolchain passes with no changed-line coverage regression). Record the AC-to-task mapping in `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/other/implementation-ac-map.md` with `Timestamp:`. Acceptance: artifact exists and maps each of AC1–AC6 to the implementing task(s).

### Phase 2 — Final QC Loop

- [x] [P2-T1] Run formatting. Execute `npm run format` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/qa-gates/final-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step changes files, restart the loop from this task after re-running. Acceptance: artifact exists with all four fields; formatting reports no remaining changes.
- [x] [P2-T2] Run linting. Execute `npm run lint` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/qa-gates/final-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0` and zero lint errors.
- [x] [P2-T3] Run type checking. Execute `npm run typecheck` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/qa-gates/final-typecheck.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0` and zero type errors.
- [x] [P2-T4] Run tests with coverage. Execute `npm test -- --coverage` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/qa-gates/final-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records numeric post-change overall line % and branch % and the pass/fail test count. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0`; numeric post-change line and branch coverage recorded (no placeholders).
- [x] [P2-T5] Verify coverage thresholds and no changed-line regression. Compare the baseline coverage from `evidence/baseline/baseline-test-coverage.md` (P0-T5) against the post-change coverage from `evidence/qa-gates/final-test-coverage.md` (P2-T4). Write `docs/features/active/2026-06-27-fix-csharp-push-down-pack-name-256/evidence/qa-gates/coverage-comparison.md` with `Timestamp:`, baseline line %/branch %, post-change line %/branch %, the coverage for the changed files (`claude-pack-name-translation.ts` and the edited region of `repo-automation-command-registration-admin.ts`), and an explicit statement that overall line >= 85% and branch >= 75% and that no changed line lost coverage. Acceptance: artifact exists; thresholds met; no changed-line coverage regression. If any required value is unavailable, mark the outcome remediation-required (not PASS).
