# Definition of Done — AC Mapping (P6-T1)

- Timestamp: 2026-07-02T22-00
- Source: `spec.md` Definition of Done (AC1-AC14)

| AC | Description (summary) | Satisfying plan task(s) / test file(s) |
|---|---|---|
| AC1 | `.claude/agents/epic-orchestrator.md` exists, distinct from `orchestrator.md`, `tools:` includes `Agent(orchestrator)`; `orchestrator.md` does not delegate to itself. | `P1-T1` (`.claude/agents/epic-orchestrator.md`); `P1-T6` (confirms `orchestrator.md` carries no `Agent(orchestrator)` self-reference). |
| AC2 | `epic-plan.md` YAML-frontmatter schema documented; wave computation via longest-path layering; cycle/unresolved-`depends_on` rejection before kickoff. | `P1-T2` (`.claude/skills/epic-orchestrate/SKILL.md` documents the manifest schema and wave formula); `P3-T1`/`P3-T3` (`scripts/dev_tools/validate_epic_orchestrator_state.py` implements and `tests/scripts/dev_tools/test_validate_epic_orchestrator_state.py` tests cycle/unresolved-reference rejection). |
| AC3 | Integration-branch lifecycle (create off `main`, per-wave fetch-before-branch, PR base override, final integration-to-`main` PR) implemented and documented. | `P1-T2` (`.claude/skills/epic-orchestrate/SKILL.md` "Epic Integration Branch Lifecycle" section). |
| AC4 | `orchestrate/SKILL.md` carries S9 step 6, `epic_merge` checkpoint bullet, PR Creation Gate condition 7; standalone behavior unchanged. | `P1-T3` (S9 step 6); `P1-T4` (`epic_merge` bullet); `P1-T5` (PR Creation Gate condition 7). |
| AC5 | Fan-in merge conflict converted to a `remediation-inputs.<timestamp>.md` Blocking finding processed by unmodified R1-R5; third pass records `step9_status: "blocked_conflict_loop_limit"` / `merge_status: "blocked_conflict_loop_limit"`. | `P1-T2` (`.claude/skills/epic-orchestrate/SKILL.md` "Merge-Conflict Handling (Fan-In)" section documents the full procedure; per the plan's Open Questions/Notes, this is an agent-followed procedure with no separate code-change task). |
| AC6 | `epic-orchestrator-state.json` schema defined; `validate_epic_orchestrator_state.py` implements shape/wave-barrier validation; `"epic-orchestrator-state"` registered in the MCP tool enum and dispatched in both the Python CLI and TS port. | `P3-T1` (validator module); `P3-T2` (Python dispatch/CLI subparser); `P3-T3` (Python tests); `P4-T1` (TS port); `P4-T2` (TS dispatch); `P4-T3` (MCP enum); `P4-T4` (TS tests); `P4-T7` (cross-language string consistency). |
| AC7 | Wave barrier enforced by both the per-call `enforce-epic-wave-barrier.ps1` deterrent and the retrospective ordering check inside `validate_epic_orchestrator_state_text`, invoked at `epic-orchestrator` `SubagentStop` time via the parameterized `validate-orchestrator-output.ps1`. | `P2-T12`/`P2-T13` (deterrent hook + tests); `P3-T1`/`P3-T3` (retrospective ordering invariant + `EPIC_WAVE_BARRIER_VIOLATION` tests); `P2-T1`/`P2-T2` (parameterized `validate-orchestrator-output.ps1`); `P2-T17` (`.claude/settings.json` wires the `epic-orchestrator` `SubagentStop` matcher and the `Agent`-matcher deterrent hook). |
| AC8 | `config/orchestration-routing.json` and its byte-identical mirror carry the `epic` route (`required_agents: [orchestrator, pr-author]`, `requires_pr_gate: true`). | `P1-T7` (`config/orchestration-routing.json`); `P1-T8` (mirror `extensions/drm-copilot/resources/config/orchestration-routing.json`). |
| AC9 | `enforce-epic-worktree-removal-gate.ps1` denies `git worktree remove` unless the matching `features[]` record has `merge_status` in `{merged, worktree_removed}`. | `P2-T10` (hook); `P2-T11` (`tests/scripts/claude-hooks/enforce-epic-worktree-removal-gate.Tests.ps1`). |
| AC10 | Dependent-feature kickoff prompts cite specific upstream artifact paths (spec, plan, PR number, merge commit) for every `depends_on` entry. | `P1-T2` (`.claude/skills/epic-orchestrate/SKILL.md` "Context Handoff to Dependent Features" section carries the literal citation-line template). |
| AC11 | `enforce-epic-merge-gate.ps1` denies `gh pr merge --merge` unless epic-mode/CI-green checkpoint conditions hold; `enforce-pr-author-skill.ps1`'s `Test-EpicBaseBranchOverride` denies `gh pr create` under `epic_mode: true` unless `--base` matches `epic_context.integration_branch`. | `P2-T5`/`P2-T6` (merge gate + tests); `P2-T3`/`P2-T4` (base-branch override + tests, split across `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and the sibling `enforce-pr-author-skill.epic-base-branch.Tests.ps1`). |
| AC12 | `docs/features/epics/<epic-slug>/epic-status.md` created at epic kickoff and updated at every wave boundary and merge-status transition. | `P1-T2` (`.claude/skills/epic-orchestrate/SKILL.md` "Documentation Maintenance Boundaries" section). |
| AC13 | Every file under Bundled Mirror Parity has a byte-identical copy under `extensions/drm-copilot/resources/claude-customizations/`, verified by `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`; `packages/mcp-server/resources/claude-customizations/` verified per file with `cmp`. | `P5-T1` (copy + `cmp`); `P5-T2` (dynamic pytest parity gate); `P5-T3` (manual `packages/mcp-server` mirror `cmp` verification). |
| AC14 | All four quality toolchains pass with no coverage regression; existing tests continue to pass. | `P6-T2` (final PowerShell loop); `P6-T3` (final Python loop); `P6-T4` (final TypeScript loop); `P6-T5` (coverage-delta verification); `P6-T6` (final bundled-mirror-parity re-check). |
