## P3-T5 — README.md Inventory Verification

- Timestamp: 2026-07-09T13-00
- Commands:
  - `ls .claude/skills/`
  - `ls .claude/agents/`
  - `rg "\"command\": \"drmCopilotExtension\." extensions/drm-copilot/package.json`
  - `rg "name:\s*\"[a-zA-Z_]+\"" extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` (the file actually imported by `mcp-tools.ts`, which is imported by `mcp-server.ts`)
- EXIT_CODE: 0
- Output Summary: all skills, agents, VS Code commands, and MCP tools named in `README.md` resolve to a real source-of-truth entry. No mismatched name was found.

### Skills (README.md line 30) vs. `.claude/skills/<name>/SKILL.md`

Every skill name explicitly listed in README.md's skills paragraph resolves to an existing `.claude/skills/<name>/` directory: `orchestrate`, `epic-orchestrate`, `feature-promotion-lifecycle`, `atomic-plan-contract`, `execute-hard-lock`, `feature-review-workflow`, `pr-author`, `pr-context-artifacts`, `pr-base-branch-merge-base`, `commit-message`, `research-issue`, `acceptance-criteria-tracking`, `policy-compliance-order`, `evidence-and-timestamp-conventions`, `python-change-budget-router`, `powershell-change-budget-router`, `csharp-change-budget-router`, `python-qa-gate`, `powershell-qa-gate`, `csharp-qa-gate`, `powershell-orchestration-state-machine`, `csharp-orchestration-state-machine`, `remediation-handoff-atomic-planner`, `human-exception-runbook`, `review-feature`, `review-epic`, `review-staged`, `translate-copilot-to-claude`. Result: PASS.

Observation (not a P3-T5 failure — README names a curated subset, not a complete enumeration): `.claude/skills/` also contains `fill-feature-docs`, `invoke-csharp-engineer`, `invoke-powershell-engineer`, `invoke-python-engineer`, `make-skill-template`, `policy-audit-template-usage`, `skill-canonical-location-audit`, and `update-status`, none of which is named in README's narrative skill list. P3-T5's check scope is "every skill... currently listed in README.md," which these are not; they are recorded here for completeness only and do not affect the PASS result.

### Agents (README.md line 31) vs. `.claude/agents/<name>.md`

All 17 agent names listed in README.md resolve exactly to `.claude/agents/`, with no extras and no omissions: `orchestrator`, `epic-orchestrator`, `atomic-planner`, `atomic-executor`, `feature-review`, `epic-review`, `staged-review`, `task-researcher`, `prd-feature`, `status-updater`, `pr-author`, `commit-message`, `human-exception-runbook`, `python-typed-engineer`, `typescript-engineer`, `powershell-typed-engineer`, `csharp-typed-engineer`. Result: PASS.

### VS Code Commands (README.md lines 118-163) vs. `extensions/drm-copilot/package.json` `contributes.commands`

All 27 `drmCopilotExtension.*` command IDs listed in README.md match exactly (as a set) the 27 command IDs registered under `contributes.commands` in `extensions/drm-copilot/package.json`: `collectCommitContext`, `collectPrContext`, `newActiveFeatureFolder`, `newPotentialEntry`, `newPotentialBugEntry`, `potentialToIssue`, `linkParentChild`, `runCodexNativeConverter`, `pushDownCopilotCustomizations`, `pushDownCodexAndAgentsCustomizations`, `pushDownClaudeCustomizations`, `syncAgentsFromInstructions`, `runPoshQCSuite`, `runPoshQCFormat`, `runPoshQCAnalyze`, `runPoshQCTest`, `runPoshQCAnalyzeAutofix`, `resolvePolicyAuditTemplateAsset`, `resolveExecuteHardLockPrompt`, `resolveAtomicPlanPrompt`, `newClaudeWorktreeSession`, `newCodexWorktreeSession`, `removeSecondaryWorktrees`, `helloPython`, `helloPowerShell`, `listMcpTools`, `showSubagentTree`. Result: PASS.

### MCP Tools (README.md lines 175-194) vs. registered MCP tool surface

`README.md` lists 20 MCP tool names: `collect_commit_context`, `collect_pr_context`, `run_codex_native_converter`, `push_down_copilot_customizations`, `push_down_codex_and_agents_customizations`, `push_down_claude_customizations`, `new_potential_entry`, `new_potential_bug_entry`, `potential_to_issue`, `new_active_feature_folder`, `link_parent_child`, `run_poshqc_format`, `run_poshqc_analyze`, `run_poshqc_test`, `run_poshqc_analyze_autofix`, `run_poshqc_suite`, `resolve_policy_audit_template_asset`, `resolve_execute_hard_lock_prompt`, `resolve_atomic_plan_prompt`, `validate_orchestration_artifacts`.

Trace: `extensions/drm-copilot/src/mcp-server.ts` calls `listRepoAutomationTools()` from `mcp-tools.ts`, which returns `REPO_AUTOMATION_TOOL_DEFINITIONS` imported from `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`. That file defines exactly these 20 tool names. All 20 README-listed tool names match this actively-registered definitions file exactly, with no extras and no omissions. Result: PASS.

Observation (not a P3-T5 failure): a second, unused file `extensions/drm-copilot/src/mcp-tool-definitions.ts` also exists in the same directory and defines an 18-tool subset that is missing `link_parent_child` and `resolve_atomic_plan_prompt`. No `src/` file imports from `./mcp-tool-definitions`; it is not wired into `mcp-server.ts` and does not affect the tool surface actually exposed at runtime. This is recorded for completeness; it does not change the PASS result for README's MCP tool list, since README's claim matches the file that is actually imported and served.

### Result: PASS — every skill, agent, VS Code command, and MCP tool name listed in `README.md` matches its current source-of-truth file.
