# 2026-08-19-parallel-merge-gate-allow-branch (Plan)

- **Issue:** #492
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-19T09-39
- **Status:** Draft
- **Version:** 1.0
- **Work Mode:** minor-audit
- **Requirements source (sole AC source):** `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/issue.md` (`## Acceptance Criteria`, AC1-AC8)
- **Research (authoritative facts):** `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/research/2026-08-19T14-00-parallel-merge-gate-allow-branch-research.md`

**Mode note (minor-audit):** `spec.md`, `user-story.md`, and a top-level `research.md` are intentionally absent for this minor-audit item and are NOT required. Only the `## Acceptance Criteria` section of `issue.md` is the acceptance-criteria source. Execution must fail closed if `spec.md` or `user-story.md` unexpectedly appears in the active folder, if the `## Acceptance Criteria` section is missing from `issue.md`, if required Phase 0 artifacts are missing, or if checklist state contradicts evidence on disk.

**Fail-closed evidence rule:** Baseline and final-QC artifacts are mandatory. PowerShell is the only language in scope and repository policy requires line coverage >= 85%; therefore baseline and final-QC Pester tasks MUST run in coverage mode and record numeric coverage. If any required baseline artifact, final-QC artifact, or coverage value is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Each evidence-producing task names its exact canonical artifact path. Do not mark an evidence-backed task complete without the artifact present and complete.

**Canonical evidence roots (non-overridable):**
- Baseline: `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/baseline/`
- QA gates: `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/`

Any `artifacts/` evidence path is forbidden per `evidence-and-timestamp-conventions`. `EVIDENCE_LOCATION_OVERRIDE_REJECTED` is recorded if a non-canonical path is supplied.

**Change budget:** In scope are 2 production PowerShell mirrors of one hook (primary + byte-identical bundle copy), 1 Pester test file, and 2 documentation files (each with a byte-identical bundle mirror). The `.codex/` copies are out of scope (research Section 1.3; issue and ACs name only `.claude/hooks/enforce-epic-merge-gate.ps1`).

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read the policy files in the required order and record the read evidence to `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/baseline/phase0-instructions-read.md`. The artifact MUST contain `Timestamp:`, `Policy Order:`, and an explicit list of the files read in this order: (1) `.github/copilot-instructions.md` and `CLAUDE.md`, (2) `.github/instructions/general-code-change.instructions.md` and `.claude/rules/general-code-change.md`, (3) `.github/instructions/general-unit-test.instructions.md` and `.claude/rules/general-unit-test.md`, (4) `.github/instructions/powershell-code-change.instructions.md`, `.github/instructions/powershell-unit-test.instructions.md`, and `.claude/rules/powershell.md`, (5) `.claude/rules/quality-tiers.md`, (6) `.claude/rules/parallel-orchestration.md`. Acceptance: artifact exists with all three required headers and every listed file enumerated.
- [x] [P0-T2] Capture the PowerShell formatting baseline for the in-scope files by running `mcp__drm-copilot__run_poshqc_format` (check-only baseline) against `.claude/hooks/enforce-epic-merge-gate.ps1`, `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-epic-merge-gate.ps1`, and `tests/scripts/claude-hooks/enforce-epic-merge-gate.Tests.ps1`. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/baseline/baseline-poshqc-format.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (state whether any file would be reformatted). Acceptance: artifact exists with all four fields.
- [x] [P0-T3] Capture the PSScriptAnalyzer baseline by running `mcp__drm-copilot__run_poshqc_analyze` against the same three files. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/baseline/baseline-poshqc-analyze.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (record analyzer finding count by severity). Acceptance: artifact exists with all four fields.
- [x] [P0-T4] Capture the Pester baseline WITH coverage by running `mcp__drm-copilot__run_poshqc_test` over `tests/scripts/claude-hooks/enforce-epic-merge-gate.Tests.ps1` with coverage enabled on `.claude/hooks/enforce-epic-merge-gate.ps1` (config `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`). Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/baseline/baseline-pester-coverage.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` with numeric headline values: tests passed/failed count and baseline line-coverage percent for the hook file. Acceptance: artifact exists with all four fields and a numeric baseline line-coverage percent.

---

### Phase 1 — Constrained Small-Path Implementation (placeholder)

This phase is a single delegated handoff to the small-path implementation engineer. The engineer performs the edits below. No implementation is performed during planning. All edits must keep `.claude/hooks/enforce-epic-merge-gate.ps1` under 500 lines and preserve the dot-source guard and entrypoint (AC6).

- [x] [P1-T1] Delegated implementation handoff: apply the exact change specification in tasks P1-T2 through P1-T10 to the in-scope files only. Acceptance: every sub-task's acceptance criterion below is met and the change budget is not exceeded.
- [x] [P1-T2] In `.claude/hooks/enforce-epic-merge-gate.ps1`, add script-scoped path variable `$script:ParallelCheckpointPath = 'artifacts/orchestration/parallel-orchestrator-state.json'` and a new read-seam function `Get-ParallelOrchestratorCheckpointContent` following the exact `CmdletBinding`/`OutputType([string])`/`Test-Path -PathType Leaf` guard + `Get-Content -Raw` pattern of `Get-ChildOrchestratorCheckpointContent` and `Get-EpicOrchestratorCheckpointContent`, reading `$script:ParallelCheckpointPath` and returning `$null` when the file is absent. Acceptance: the new variable and function exist and mirror the existing seam pattern; the function returns `$null` when the file does not exist. (AC1, AC3, AC6)
- [x] [P1-T3] In `.claude/hooks/enforce-epic-merge-gate.ps1`, broaden `Get-EpicMergeGateCommandPrNumber` (or add a broadened extractor it delegates to) so it captures the PR number regardless of flag order, ADDITIVELY: `gh pr merge 410 --merge` still yields `410`, `gh pr merge --merge 410` now yields `410`, and bare `gh pr merge --merge` still yields `$null`. Recommended: after confirming `gh pr merge` is present, extract the first standalone run of digits preceded by whitespace and not preceded by `-` or a word character. Acceptance: the three stated inputs produce `410`, `410`, and `$null` respectively, and the existing epic-path outcomes remain byte-identical for the forms the epic path uses. (AC2)
- [x] [P1-T4] In `.claude/hooks/enforce-epic-merge-gate.ps1`, add decision helper `Test-ParallelCheckpointAllowsMerge -Checkpoint <parsed> -CommandPrNumber <Nullable[int]>` mirroring the `CmdletBinding`/`OutputType([bool])`/`[AllowNull()]` pattern of `Test-EpicCheckpointAllowsMerge`, with semantics: return `$false` if `$Checkpoint` is `$null`; return `$false` unless top-level `route_id` equals exactly `'parallel'`; return `$false` when `$CommandPrNumber` is `$null`; locate the `items[]` entry whose `pr_number` equals `$CommandPrNumber` (parsed via `[int]::TryParse`, mirroring epic-path numeric handling) and return `$false` if none matches; return `$true` only when that matched item's `merge_status` equals exactly `'ci_green'`, `$false` for any other value. All property access is guarded by `PSObject.Properties.Name` membership checks so missing keys do not throw. Acceptance: the helper returns the specified boolean for each documented case and never throws on missing keys. (AC1, AC2, AC3)
- [x] [P1-T5] In `.claude/hooks/enforce-epic-merge-gate.ps1`, wire the parallel branch into `Invoke-EpicMergeGateDecision` as a THIRD allow-branch, AFTER the child-feature and epic-integration branches and BEFORE the final deny (order: child -> epic -> parallel -> deny). It reads the parallel checkpoint via `Get-ParallelOrchestratorCheckpointContent` piped through `ConvertFrom-EpicMergeGateJson`, then calls `Test-ParallelCheckpointAllowsMerge` with the extracted command PR number. The existing child and epic branches and their code are left unchanged. Acceptance: the parallel branch is present in the specified position; `Test-ChildCheckpointAllowsEpicMerge` and `Test-EpicCheckpointAllowsMerge` are not modified. (AC1, AC4)
- [x] [P1-T6] In `.claude/hooks/enforce-epic-merge-gate.ps1`, minimally extend the deny-reason string to mention the parallel condition as a third accepted path (append a parallel clause to the existing message), keeping the literal token `EPIC_MERGE_GATE_BLOCKED` intact so existing `-Match 'EPIC_MERGE_GATE_BLOCKED'` assertions continue to pass. Acceptance: the deny reason still contains `EPIC_MERGE_GATE_BLOCKED` and now names the parallel allow-path. (AC3, AC8)
- [x] [P1-T7] In `.claude/hooks/enforce-epic-merge-gate.ps1`, update the `.DESCRIPTION` comment block to document the third (parallel) allow-branch (`route_id == "parallel"`, target item `merge_status == "ci_green"`, matching `pr_number` when a PR number is named). Acceptance: the doc comment lists three allow-branches; file remains under 500 lines. (AC6, AC8)
- [x] [P1-T8] Copy `.claude/hooks/enforce-epic-merge-gate.ps1` verbatim to its mandatory mirror `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-epic-merge-gate.ps1` so the two files are byte-identical. Acceptance: the two files are byte-identical (the Python parity test in P2-T6 will confirm). (AC1-AC4)
- [x] [P1-T9] Update `.claude/skills/parallel-orchestrate/SKILL.md` "F7 dependency" note (around lines 327-333) minimally: replace the statement that the parallel per-item merge "is denied until F7 scopes or extends that gate's allow conditions" with a statement that the merge gate now authorizes a parallel per-item merge when the parallel-orchestrator checkpoint has `route_id == "parallel"`, the target item's `merge_status == "ci_green"`, and (when a PR number is named) it matches the item's `pr_number`. Copy the edited file verbatim to its mirror `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md` so the two are byte-identical. Acceptance: both files reflect the landed behavior and are byte-identical. (AC8)
- [x] [P1-T10] Update `.claude/rules/parallel-orchestration.md` "Enforcement" section by adding a single concise sentence documenting that `.claude/hooks/enforce-epic-merge-gate.ps1` carries a parallel allow-branch authorizing per-item `gh pr merge --merge` from the parallel-orchestrator checkpoint (`route_id == "parallel"`, item `merge_status == "ci_green"`, matching `pr_number`). Copy the edited file verbatim to its mirror `extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md` so the two are byte-identical. Acceptance: the sentence is present, minimal, consistent with landed behavior, and both files are byte-identical. Note: `.claude/rules/parallel-orchestration.md` is a policy rule; this addition documents landed enforcement per AC8 and is authorized by the exact change specification for this issue. (AC8)
- [x] [P1-T11] Extend `tests/scripts/claude-hooks/enforce-epic-merge-gate.Tests.ps1` with Pester tests that mock the read seam `Get-ParallelOrchestratorCheckpointContent` (no temporary files), covering: (a) parallel allow — `route_id "parallel"`, item `pr_number 501` + `merge_status "ci_green"`, command `gh pr merge --merge 501` -> allow; (b) parallel deny — matching item but `merge_status` not `ci_green` (e.g. `pr_open`) -> deny with `EPIC_MERGE_GATE_BLOCKED`; (c) parallel deny — command PR number matches no item -> deny; (d) parallel deny — `route_id` not `"parallel"` -> deny; (e) parallel deny — parallel checkpoint `$null` with child and epic also `$null` -> deny (fail closed); (f) parallel deny — parallel checkpoint malformed JSON -> deny (fail closed); (g) parallel deny — bare `gh pr merge --merge` (no PR number) with only a parallel checkpoint present -> deny; (h) regression — existing child-feature and epic-integration allow/deny Contexts still behave identically; (i) PR-number extractor direct unit tests for `gh pr merge 410 --merge` -> 410, `gh pr merge --merge 410` -> 410, `gh pr merge --merge` -> `$null`; (j) a real `Test-Path`/`Get-Content` read-seam test for `Get-ParallelOrchestratorCheckpointContent` mirroring the existing read-seam tests; (k) direct branch-coverage tests for `Test-ParallelCheckpointAllowsMerge` (null checkpoint, wrong route_id, null CommandPrNumber, no matching item, non-numeric pr_number, matching item + ci_green -> true). Acceptance: all listed cases exist as `It` blocks and the suite is green in Phase 2. (AC1-AC7)

---

### Phase 2 — Final QC Loop

Run the full PowerShell toolchain in order and restart from the first step if any step changes files or fails. All QA artifacts are written under `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/`. Each artifact MUST contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.

- [x] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` over the in-scope PowerShell files (`.claude/hooks/enforce-epic-merge-gate.ps1`, its bundle mirror, and `tests/scripts/claude-hooks/enforce-epic-merge-gate.Tests.ps1`). Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-poshqc-format.md`. If formatting changes any file, restart the loop at P2-T1. Acceptance: format step reports no pending changes and the artifact records `EXIT_CODE:` and `Output Summary:`.
- [x] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` over the same files. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-poshqc-analyze.md`. If analysis fails or autofix changes any file, restart the loop at P2-T1. Acceptance: zero analyzer errors and the artifact records `EXIT_CODE:` and `Output Summary:` (finding counts by severity).
- [x] [P2-T3] Run `mcp__drm-copilot__run_poshqc_test` WITH coverage over `tests/scripts/claude-hooks/enforce-epic-merge-gate.Tests.ps1` with coverage enabled on `.claude/hooks/enforce-epic-merge-gate.ps1` (config `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`). Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-pester-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including numeric post-change line-coverage percent and tests passed/failed counts. If any test fails, fix and restart the loop at P2-T1. Acceptance: all tests pass and the artifact records a numeric post-change line-coverage percent. (AC7)
- [x] [P2-T4] Compute and record the coverage delta and threshold verification. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-coverage-delta.md` reporting baseline line coverage (from P0-T4), post-change line coverage (from P2-T3), and changed-lines coverage; confirm line coverage >= 85% and no regression on changed lines. Acceptance: artifact records all three numeric values and states PASS only if line coverage >= 85% with no changed-line regression; otherwise the outcome is remediation-required. (AC7)
- [x] [P2-T5] Verify the 500-line limit and structural invariants: confirm `.claude/hooks/enforce-epic-merge-gate.ps1` is under 500 lines and retains the dot-source guard and entrypoint. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-file-size-invariants.md` recording the line count and guard/entrypoint presence. Acceptance: line count < 500 and both invariants present. (AC6)
- [x] [P2-T6] Run the byte-identity parity Python test: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`. Write `docs/features/active/2026-08-19-parallel-merge-gate-allow-branch-492/evidence/qa-gates/qc-pushdown-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (pass/fail and count). Acceptance: the test passes, confirming the bundle mirrors of the hook, the skill file, and the rules file are byte-identical to their `.claude/**` primaries. (AC1-AC4, AC8)
