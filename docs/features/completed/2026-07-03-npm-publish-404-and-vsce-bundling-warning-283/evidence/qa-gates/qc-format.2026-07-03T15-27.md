# QC — Post-Change Format

Timestamp: 2026-07-03T15-27
Command: `npm --prefix extensions/drm-copilot run format`
EXIT_CODE: 0

Output Summary: Prettier ran `--write` against `src/**/*.ts`, `test/**/*.ts`, `*.json`, and `*.cjs`, including the new `esbuild-extension.cjs` and the modified `esbuild-mcp-server.cjs` and `package.json`. All files reported `(unchanged)`; zero files were reformatted. No loop restart required.
