# Remediation Inputs: enforce-model-selection-routing (Issue #305)

**Generated:** 2026-07-04T14-34
**Feature Folder:** `docs/features/active/2026-07-04-enforce-model-selection-routing-305`
**Base Branch:** `main` @ `f530d0e3ae7c5d0974b72cf0956e862dd94041c5`
**Head Branch:** `bug/enforce-model-selection-routing` @ `3f62485b1fc59f21b42c7bc5c40ea9422533ff6a`
**Source artifacts:**
- `policy-audit.2026-07-04T14-34.md`
- `code-review.2026-07-04T14-34.md`
- `feature-audit.2026-07-04T14-34.md`

## Summary

Two BLOCKING policy findings must be remediated before PR readiness. All 14 acceptance criteria pass; the Python and PowerShell scopes are green with coverage above threshold; bundle parity, formula reuse, foreign-schema prohibition, backward compatibility, and evidence-location rules all pass. The blockers are branch-level policy violations, not acceptance-criteria failures.

## Blocking Findings

### BLOCKING-1: File exceeds the 500-line hard limit

- **Severity:** Blocking
- **File:** `extensions/drm-copilot/src/repo-automation-service.ts`
- **Location:** whole file — currently 502 lines
- **Policy:** `.claude/rules/general-code-change.md` — "No production code, test code, or reusable script file may exceed 500 lines."
- **Detail:** Baseline (`f530d0e…`) length was 497 lines. This feature added +5 lines to thread the optional `requireModelRouting` parameter through the `RepoAutomationService` interface and `DefaultRepoAutomationService` implementation (diff hunks at lines ~143 and ~447–469), pushing the file to 502 lines and crossing the hard limit.
- **Evidence:** `wc -l` = 502; `git show f530d0e3ae7c5d0974b72cf0956e862dd94041c5:extensions/drm-copilot/src/repo-automation-service.ts | wc -l` = 497; `git diff` shows `+5/-0`.
- **Required remediation:** Extract a cohesive helper (for example, the request-shaping block that assembles the validation-service call arguments, ~lines 448–469) into a sibling module and import it, bringing `repo-automation-service.ts` under 500 lines without behavior change. Re-run `npm run typecheck` and `npm run test` and confirm `wc -l` ≤ 500.

### BLOCKING-2: TypeScript coverage artifact absent (coverage verification not satisfiable)

- **Severity:** Blocking
- **Scope:** `extensions/drm-copilot` TypeScript project (changed production files include `src/lib/validate/orchestrator-state-core.ts` +113, `src/repo-automation-service.ts`, `src/lib/validate/orchestration-artifacts.ts`, `src/lib/validate/validate-orchestration-service-call.ts`, `src/mcp-*.ts`).
- **Policy:** Coverage verification is mandatory for every language with changed files (feature-review Coverage Verification; `quality-tiers.md` uniform 85% line / 75% branch; workflow remediation trigger "coverage artifact absent for any language that has changed files").
- **Detail:** No TypeScript coverage artifact exists. Expected `coverage/lcov.info` (or `extensions/drm-copilot/coverage/`) is absent. The extension `package.json` defines only `test` and `test:unit` (both `node run-jest.cjs`); there is no `test:coverage` script and no Jest `collectCoverage`/`coverageThreshold`/lcov reporter configuration. The TS QA evidence (`evidence/qa-gates/typescript-qa.md`) reports format/lint/type/test results only — no coverage numbers. Consequently, changed-line coverage on the new TS logic cannot be measured or verified against the thresholds.
- **Evidence:** artifact search returned ABSENT for `coverage/lcov.info` and `extensions/drm-copilot/coverage/*`; `grep coverage extensions/drm-copilot/package.json` returned no coverage script; `evidence/qa-gates/typescript-qa.md` contains no coverage section.
- **Required remediation:** Wire a Jest coverage script (e.g., `test:coverage` with `--coverage --coverageReporters=lcov`) and a coverage configuration for the extension, run it, and produce the coverage artifact. Confirm ≥85% line and ≥75% branch on the changed TypeScript code (and no regression on changed lines), and record the numeric result in the feature evidence. If any changed TS file falls below threshold, add tests to close the gap.

## Non-Blocking Observations (no remediation required)

- `scripts/dev_tools/validate_orchestrator_state.py` is exactly 500 lines — compliant (does not exceed), consistent with the spec's decision to house new logic in the delegate.
- Repo-root `coverage.xml` is a pre-existing tracked Pester/JaCoCo report regenerated by this feature's PowerShell run; it is outside the prohibited `artifacts/**` evidence paths and does not violate the Evidence Location Invariant.

## Verification Gate for Remediation Closure

Remediation is complete when all of the following hold:
1. `wc -l extensions/drm-copilot/src/repo-automation-service.ts` ≤ 500.
2. A TypeScript coverage artifact exists and shows ≥85% line / ≥75% branch on changed TS code with no regression on changed lines.
3. `npm run format && npm run lint && npm run typecheck && npm run test` (plus the new coverage run) all pass.
4. Bundle parity for any newly touched `.claude/**` files (if the extraction touches none, unaffected) remains byte-identical.
5. Python and PowerShell toolchains remain green (no regression from the extraction).
