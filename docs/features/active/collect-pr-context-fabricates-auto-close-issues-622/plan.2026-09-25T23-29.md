# collect-pr-context-fabricates-auto-close-issues (Plan)

- **Issue:** #622
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-26T00-00
- **Status:** Draft
- **Version:** 0.2 (revised for spec v0.3: merge-order independence, D9-D14)

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

- **Work Mode:** full-bug (from `issue.md` metadata `- Work Mode: full-bug`)
- **Requirements source:** `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/spec.md` (version 0.3; `## Acceptance Criteria`, 27 items; decisions D1-D14; section `## Composition with #588`). `issue.md` and `research/research.2026-09-25T23-33.md` are context only.
- **Branch:** `bug/collect-pr-context-fabricates-auto-close-issues-622`
- **Sibling:** #588, branch `bug/pr-context-gh-detection-false-negative-588`. No task in this plan depends on #588's merge, branch, or plan text (spec D9, D14).

**Evidence location rule:** Every artifact is written under `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/<kind>/` with `<kind>` one of `baseline`, `regression-testing`, `qa-gates`, `other`. Coverage artifacts go where spec AC 25 and the spec's Test Strategy coverage bullets place them (see interpretation 9): baseline coverage artifacts go to `evidence/baseline/`, and final and delta coverage artifacts go to `evidence/qa-gates/`. Every artifact file name carries the fixed stamp `2026-09-25T23-29`; the `Timestamp:` field inside records the actual execution time in `yyyy-MM-ddTHH-mm` form. Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; an artifact whose command is expected to exit non-zero also carries `ExpectedExitCode:`. A loop iteration that re-runs a gate overwrites the same artifact and records `Loop iteration: <n>`. An artifact that carries `ExpectedExitCode:` records only commands with that expected exit code, because the PR-context evidence parser keeps the last `EXIT_CODE:` line and the first `ExpectedExitCode:` line of a file.

**Working directories:** Commands labelled TS run from `extensions/drm-copilot`. Python, `git`, `grep`, `wc`, and `awk` commands run from the repository root. `<scope-anchor-sha>` is the 40-character SHA recorded by [P0-T5]; substitute it literally. This plan integrates `origin/main` exactly once, by merge, in [P0-T3]; no later task fetches, merges, or rebases. If the orchestrator nevertheless directs a further `git merge --no-edit origin/main` after [P0-T5], re-run [P0-T2] (branch detection) and the [P0-T5] derivation before the next task that uses `<scope-anchor-sha>`, append the new values and their time to the [P0-T2] and [P0-T5] artifacts, and use the new values from then on. The branch is never rebased and never force-pushed (spec D13). No scope check uses the local `main` ref, and no test added or changed by this plan reads any remote ref; only [P0-T1] through [P0-T5] read `origin/...` refs.

**Branch symbol:** `<branch>` in task text means the value `M588` or `N588` recorded on the `Branch:` line of the [P0-T2] artifact. A task written as "N588: ... M588: ..." runs only the part for the recorded branch; both parts are complete instructions and neither stops execution.

**Commit rule:** Every commit task stages and commits named paths only: `git add -- <paths>` followed by `git commit -m "<message>" -- <paths>`, where `<paths>` is the exact list in the task. The commit message ends with the attribution trailer lines required by the executing session's instructions. If a hook denies staging or committing, stop and report the denial text to the orchestrator; do not work around it. Every push, including the one in [P0-T3], is the fast-forward push `git push origin HEAD:bug/collect-pr-context-fabricates-auto-close-issues-622`; the executor's local branch name may differ from the remote branch name and is not assumed. No push uses `--force` or `--force-with-lease`. If a push is rejected as non-fast-forward, stop and report the rejection text to the orchestrator. The commit log is `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/commit-log.2026-09-25T23-29.md`, one line per commit (`<sha> <subject>`). Before staging, append the line for the previous commit made by this plan (if not yet logged). Check porcelain after the push. The line for [P11-T29] is reported in the completion message and not written to the file.

## Composition with #588 (branch detection, spec D9-D13)

#622 executes to completion whether or not #588 has merged. [P0-T2] runs `git fetch origin main` and then tests `origin/main`'s `scripts/dev_tools/pr_context/render_pr_helpers.py` for the #588-owned literal `None (GitHub CLI unavailable; closing issues not verified)` with `git grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" origin/main -- scripts/dev_tools/pr_context/render_pr_helpers.py`:

- **M588 (#588 merged):** the command exits 0 and prints a count of at least 1. #588's `gh_available` / `ghAvailable` builder parameter, its call-site arguments, its empty-list unavailable branch, its `render-pr-helpers.ts` Jest threshold entry, and its test files already exist on the base. #622 reuses the parameter (it does not declare it a second time), adds only its annotation, not-open, and `pending_primary_excluded` logic, preserves #588's call-site arguments, and updates #588's H3 assertions in both runtimes ([P6-T11], [P7-T3]).
- **N588 (#588 not merged; the expected branch):** the command exits 1 with no output (`git grep` exits 1 on zero matches; this is the N588 outcome, not a failure). #622 adds the keyword-only `gh_available: bool = True` / optional `ghAvailable?: boolean` (default `true`) builder parameter and uses it only for the non-empty annotation branch; the empty-list path keeps the pre-existing PASS / non-PASS fallbacks. #622 does not write the #588 literal into production code, does not touch gh detection, and does not create or edit `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`, `extensions/drm-copilot/src/lib/executable-resolver.ts`, or `extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`. Both collector call sites pass the collector-local availability value that exists today (`collector.py` lines 144/149, call at line 260; `collector-core.ts` lines 133/138, call at line 238).
- **Any other exit code** (for example 128 when `origin/main` cannot be resolved) is an execution error, not a branch: stop and report the output.

Neither branch stops execution. The orchestrator verified on 2026-09-26 that `origin/main` at `ae8d2ce3` (contained in this worktree's HEAD) does not contain #588, so N588 is expected; [P0-T2] re-checks at execution time.

**Integration (spec D13):** after detection, [P0-T3] runs `git merge-base --is-ancestor origin/main HEAD`; when it exits 1, it runs `git merge --no-edit origin/main`. The branch is never rebased and never force-pushed. [P0-T5] derives `<scope-anchor-sha>` after the merge.

Line numbers below were re-derived against this worktree's tree (`origin/main` at `ae8d2ce3`, an N588 base). A merge in [P0-T3] can shift them, and in M588 #588's edits shift every line in `build_issues_to_autoclose_section` / `buildIssuesToAutocloseSection` and at both collector call sites, so every citation into those regions is also identified by function name and is located by name at execution time. [P0-T12] re-measures every file this plan edits after the merge.

## Spec interpretations and derived obligations (recorded, resolved in-plan)

1. **D7 contingency taken for TypeScript, in both branches.** `render-pr-helpers.ts` is 481 lines on this tree. In N588, #622's builder edits (the `ghAvailable` parameter, `pendingPrimaryExcluded`, the annotation branch, the not-open branch, the imported constants, and their TSDoc) add at least 20 lines, which exceeds 500; in M588, #588's own builder edit leaves no room below 500. Per D7, `buildCloseCandidatesSection` and `buildIssuesToAutocloseSection` move into the new `extensions/drm-copilot/src/lib/pr-context/autoclose.ts` and are re-exported from `render-pr-helpers.ts`, so every existing import (including `render.ts`, `index.ts`, `collector-output.ts`, `render-pr-helpers.test.ts`, and in M588 #588's tests) still resolves. The Python builders stay in `render_pr_helpers.py` (291 lines on this tree; ceiling 345 in both branches).
2. **AC 22 over pre-existing oversize test files.** Three test files the spec requires this item to change already exceed 500 lines on this tree: `tests/scripts/dev_tools/test_collect_pr_context.py` (654), `tests/scripts/dev_tools/test_collect_pr_context_part4.py` (1026), and `tests/scripts/dev_tools/test_render.py` (570). AC 22 requires every changed file to be at or below 500 lines, so this plan relocates whole tests out of them:
   - `test_narrative_mentions_excluded_from_autoclose_section` (test_collect_pr_context.py lines 479-654) and `test_pass_readiness_autoclose_section` (part4 lines 687-856) move into the new `tests/scripts/dev_tools/pr_context/test_autoclose_collector.py`. Both must change anyway (item 3).
   - `test_collector_reports_unparseable_evidence_without_claiming_completion` (part4 lines 500-685) and `test_non_pass_readiness_fallback` (part4 lines 858-1026) move into the new `tests/scripts/dev_tools/test_collect_pr_context_part5.py`, verbatim except that every occurrence of the substring `origin/` in the moved lines is replaced with `upstream/` (four occurrences, at part4 lines 525, 622, 883, 980). The replaced values are stub return values that no assertion reads; the substitution keeps the moved lines inside AC 23's added-line scan.
   - Class `TestResolveFeatureDir` (test_render.py lines 433-570) moves verbatim into the new `tests/scripts/dev_tools/test_render_resolve_feature_dir.py`. Those lines contain none of AC 23's forbidden tokens (verified by search on this tree).
   Test-count preservation is checked by [P2-T6] and [P6-T12].
3. **Existing tests that pin behavior D3 changes but the spec does not list.** Both relocated autoclose tests use a gh double whose `classify_entity` returns `None` for the metadata primary `#46`, so D3 would exclude `#46` and both tests would fail. In their new home the double classifies `#46` as an issue whose state is `open`; all original assertions are kept. `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` answers `gh api repos/.../issues/<n>` with `{ number }` only (lines 104-107), so `issueDetails` reports state `(unknown)` and D3 would exclude the primary `#42` asserted at line 195; the handler gains `state: "open"`. The docstring and comment of `test_build_close_candidates_section_combines_author_and_ref` (test_render.py lines 418-430) describe the removed merge; they are rewritten and its assertions tightened.
4. **Fetch-once cache scope.** AC 14 requires at most one `issue_details` call per issue number per run. A closed pending primary that is also cited in prose would otherwise be fetched once by pending selection and again by the digest loop. The selection function therefore returns the details for **every** ref it fetched (kept and state-excluded), a superset of the spec's "kept refs"; the digest loop reuses any cached entry. Excluded numbers still never appear in the autoclose section.
5. **Toolchain command forms.** The spec lists write-mode commands (`poetry run black .`, `npm run format`) and the TS test command `node run-jest.cjs --coverage` (spec lines 511-515, and AC 26). The gates below run the check-mode equivalents (`poetry run black --check .`, `npx prettier --check` over the `npm run format` globs), whose exit code distinguishes a clean tree from drift; the write-mode command runs only as the documented repair step. The TS test gate is the spec's command with explicit reporters added, `node run-jest.cjs --coverage --coverageReporters=text --coverageReporters=text-summary --coverageReporters=lcov` (the form #588's plan uses), so the per-file `text` rows, the text-summary totals, and `coverage/lcov.info` read by [P9-T6] are all produced. The Python gate runs the full suite with coverage flags, deselecting `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts`, which fails locally on gitignored batch-budget state regardless of this change (open issue #510) and runs in CI on a fresh checkout. `dependency-cruiser` is not configured (no `.dependency-cruiser*` file in the tree); the architecture-boundary stage is a recorded presence check, not a skip.
6. **AC 21 path.** AC 21 lists `extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts`; the spec's D11 and `## Composition with #588` name `extensions/drm-copilot/src/lib/executable-resolver.ts` as #588's file. [P10-T5] checks both paths in both branches.
7. **No dependency on #588's plan.** No task reads #588's plan or branch. [P0-T2] detects the branch from `origin/main` directly; in M588 the merged tree is authoritative for #588's code and tests, and in N588 no #588 artifact exists on the base.
8. **H3 test titles (M588 only).** #588's H3 tests are updated only in M588, and they keep their names for traceability to AC 19; only their assertions change. [P6-T11] and [P7-T3] locate them with the fixed commands stated there. In N588, #622 edits no #588 test; #588's revised plan carries its own H3 (spec D14).
9. **Coverage evidence location.** Spec AC 25 and the spec's Test Strategy coverage bullets (spec lines 508-512) place baseline coverage evidence under `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/` and final and delta coverage evidence under `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/`. Both are canonical evidence kinds, and `git check-ignore` reports neither as ignored. [P0-T25] and [P9-T7] assert with `git ls-files` that the coverage artifacts are committed, and [P11-T25] checks AC 25 against them.
10. **Builder-test file ownership (spec D11).** The four #622 builder tests live only in #622-owned new files, identical in both branches: Python in the new `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py`; TypeScript folded into the new `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts` under `describe("buildIssuesToAutocloseSection")`, because `autoclose.ts` is where the TypeScript builders live after the D7 move and the combined file stays within its 460-line ceiling. The precedence test covers the five D12 rows only; no #622 test asserts the (gh unavailable, empty list) body.
11. **Jest threshold for `render-pr-helpers.ts` (AC 24).** This tree's `jest.config.cjs` has no `./src/lib/pr-context/render-pr-helpers.ts` entry. [P4-T6] adds it only when the pre-edit count of that key is 0 (the N588 case, because #622 changes that file); in M588 #588's entry exists and is not duplicated.

## File-size budget (500-line cap)

"This tree" counts were measured in the planning session on this worktree's tree (`origin/main` at `ae8d2ce3`, an N588 base). They are the N588 starting counts. In M588, #588's merge is expected (spec `## Composition with #588`) to have changed some of these files (for example `render_pr_helpers.py`, `render-pr-helpers.ts`, `collector.py`, `collector-core.ts`, `jest.config.cjs`, and `render-pr-helpers.test.ts`) and to have created `test_render_pr_helpers.py`; [P0-T12] re-measures every listed file after the [P0-T3] merge, in both branches, and its counts replace this column. The "#622 ceiling" column is binding in both branches and is re-verified in [P10-T7]. The "Written in" column names the branch or branches in which this plan writes the file.

| File | This tree (N588) | #622 ceiling | Change | Written in |
|---|---|---|---|---|
| `scripts/dev_tools/pr_context/models.py` | 198 | 225 | edit | both |
| `scripts/dev_tools/pr_context/feature_docs.py` | 349 | 360 | edit | both |
| `scripts/dev_tools/pr_context/render_feature_excerpts.py` | 256 | 262 | edit | both |
| `scripts/dev_tools/pr_context/render_pr_helpers.py` | 291 | 345 | edit | both |
| `scripts/dev_tools/pr_context/autoclose.py` | new | 200 | create | both |
| `scripts/dev_tools/pr_context/collector.py` | 474 | [P0-T12] count (net zero or negative) | edit | both |
| `extensions/drm-copilot/src/lib/pr-context/models.ts` | 312 | 350 | edit | both |
| `extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts` | 322 | 328 | edit | both |
| `extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts` | 448 | 455 | edit | both |
| `extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts` | 481 | 440 | edit (builders move out) | both |
| `extensions/drm-copilot/src/lib/pr-context/autoclose.ts` | new | 340 | create | both |
| `extensions/drm-copilot/src/lib/pr-context/collector-core.ts` | 475 | 420 | edit | both |
| `extensions/drm-copilot/jest.config.cjs` | 290 | 330 | edit | both |
| `tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py` | new | 150 | create | both |
| `tests/scripts/dev_tools/pr_context/test_autoclose.py` | new | 260 | create | both |
| `tests/scripts/dev_tools/pr_context/test_autoclose_collector.py` | new | 460 | create | both |
| `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` | new | 200 | create | both |
| `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` | absent (exists only in M588) | [P0-T12] count + 10 | edit (H3 only) | M588 only |
| `tests/scripts/dev_tools/test_collect_pr_context.py` | 654 | 480 | edit (relocation) | both |
| `tests/scripts/dev_tools/test_collect_pr_context_part2.py` | 432 | 432 | edit | both |
| `tests/scripts/dev_tools/test_collect_pr_context_part4.py` | 1026 | 497 | edit (relocation) | both |
| `tests/scripts/dev_tools/test_collect_pr_context_part5.py` | new | 400 | create | both |
| `tests/scripts/dev_tools/test_render.py` | 570 | 435 | edit (relocation) | both |
| `tests/scripts/dev_tools/test_render_resolve_feature_dir.py` | new | 160 | create | both |
| `tests/scripts/dev_tools/test_feature_docs.py` | 432 | 436 | edit | both |
| `extensions/drm-copilot/test/lib/pr-context/issue-reference-pattern.test.ts` | new | 150 | create | both |
| `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts` | new | 460 | create | both |
| `extensions/drm-copilot/test/lib/pr-context/collector-core-autoclose.test.ts` | new | 420 | create | both |
| `extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` | 262 | 345 | edit (D2 test; H3 in M588) | both |
| `extensions/drm-copilot/test/lib/pr-context/feature-docs.test.ts` | 314 | 316 | edit | both |
| `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` | 417 | [P0-T12] count + 2 (one line becomes three) | edit | both |

The table has 31 rows: 30 are written in N588 and all 31 in M588. "The budget rows for `<branch>`" below means the rows whose "Written in" value is `both`, plus the `M588 only` row when `<branch>` is `M588`.

Not modified (cited for context): `scripts/dev_tools/pr_context/render.py`, `scripts/dev_tools/pr_context/collector_documents.py`, `scripts/dev_tools/pr_context/github.py` (549, over the cap before this change), `extensions/drm-copilot/src/lib/pr-context/render.ts`, `extensions/drm-copilot/src/lib/pr-context/index.ts`, `extensions/drm-copilot/src/lib/pr-context/collector-output.ts` (494), `extensions/drm-copilot/src/lib/pr-context/gh-client-core.ts`, `extensions/drm-copilot/src/lib/pr-context/gh-client-details.ts`, `extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, `scripts/dev_tools/pr_context/render.py,cover`. Never created or edited in either branch: `extensions/drm-copilot/src/lib/executable-resolver.ts` and `extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts`; in N588 additionally `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`.

## Constraints and assumptions

- Linux CI (depth-1 checkout): no new or modified test may depend on remote refs (including `origin/main`), gitignored state, a real `gh`, network access, spawned processes, temporary files, or Windows drive roots or paths. Python tests use the in-memory `mem_fs_path` fixture (`tests/conftest.py` line 146) and in-process doubles patched into `scripts.dev_tools.pr_context.collector`; TS tests use `TreeFileSystem` (`extensions/drm-copilot/test/lib/pr-context/tree-file-system.ts`), a scripted `CommandRunner`, and `jest.spyOn(GhClient.prototype, ...)`. No new test string contains `origin/`; stub upstream refs use `upstream/...`.
- Python strict Pyright covers `tests` (`pyproject.toml` lines 141-151). The private `_extract_issue_references` is obtained in tests as `vars(render_feature_excerpts)["_extract_issue_references"]` bound to a variable annotated `Callable[[str], list[str]]`, so no suppression comment is needed.
- `hypothesis` and `fast-check` are not dependencies and none are added; boundary cases use `pytest.mark.parametrize` / `it.each` matrices. `quality-tiers.yml` is absent at the repository root.
- Python batch budget: `.claude/hooks/enforce-python-batch-budget.ps1` caps each batch at 3 production and 3 test `.py` files. This plan writes 6 production `.py` files in both branches and 11 test `.py` files in N588 (12 in M588, which adds the H3 edit of `test_render_pr_helpers.py`), so it schedules explicit resets ([P2-T4], [P3-T1], [P3-T5], [P6-T1], [P6-T5], [P6-T9]). A reset runs `ls -1 .claude/state` in this worktree; for each listed `python-batch-budget.<id>.json`, read it, then run `rm -f -- .claude/state/<that exact name>` with the Bash tool (literal name, no glob). Use the PowerShell tool only when the executing agent has one. When `ls` reports that the directory does not exist or lists no such file, nothing is deleted and the record states `none`. The reset then appends `Timestamp:`, the deleted file names, and the files listed in them to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/python-batch-budget.2026-09-25T23-29.md`. If a Python write is denied at any other point, apply the same reset, record it, and retry.
- Success-case output literals asserted below were observed in recorded successful runs cited by #588's plan: Prettier check `All matched files use Prettier code style!`; Prettier write `(unchanged)` per file; Black check `would be left unchanged`; Black write `left unchanged` / `reformatted`; Ruff `All checks passed!`; Pyright `0 errors, 0 warnings, 0 informations`; Jest `Tests:` line and the `text` reporter header `File | % Stmts | % Branch | % Funcs | % Lines | Uncovered Line #s`; coverage.py JSON `files[...].summary.covered_lines` / `num_statements` / `covered_branches` / `num_branches` and file-level `missing_lines`. The coverage.py terminal `Cover` cell is a combined ratio, so branch percentages are read only from the JSON report.
- No comment, docstring, TSDoc block, or string literal that this plan writes under `scripts/dev_tools/pr_context/` or `extensions/drm-copilot/src/lib/pr-context/` contains the token `ABC-123` or any text matched by the [P10-T1] / [P10-T4] scans. Describe rejected tokens in words (for example "letter-prefixed tracker keys") without an example key or the old pattern. [P10-T1] and [P10-T4] scan every file in those two directories. Those scans are fixed-string searches (`git grep --untracked -n -F`) for three literals: `[A-Z][A-Z0-9]+-`, `ABC-123`, and `Detected issue references (classified)`. They do not evaluate `[A-Z][A-Z0-9]+-` as a regular expression, so hyphenated prose such as a timestamp-format name or a character-encoding name is not a hit; each of the three literals is a hit wherever it occurs, including inside a quoted copy of the old regular expression in a comment or TSDoc line. [P1-T3], [P3-T8], and [P4-T7] run the same fixed-string scan, with the same three patterns, over the files their phases wrote, so an early pass implies the Phase 10 pass for those files. A hit in a file this plan does not write (for example a file changed only by the [P0-T3] merge) is not repaired by this plan: stop and report the hit to the orchestrator.

## Named tests introduced or changed by this plan

Python, `tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py`:

- `test_extractors_reject_non_bare_number_tokens`: parametrized over the extractor (ids `feature_docs`, `render_pr_helpers`, `render_feature_excerpts`) and 13 inputs, each expecting `[]`: `#ISO-8601`, `#CR-1`, `ISO-8601`, `CR-1`, `UTF-8`, `SHA-256`, `AC-12`, `#12abc`, `#12_`, `abc#12`, `#`, `#١٢`, and `""`. 39 cases.
- `test_extractors_accept_bare_number_tokens`: parametrized over the same 3 extractors and 6 inputs: `#468` -> `["#468"]`, `(#660)` -> `["#660"]`, `#12-3` -> `["#12"]`, `#12é` -> `["#12"]`, `#7 and #7` -> `["#7"]`, `line one\n#0 starts line two` -> `["#0"]`. 18 cases.
- `test_issue_reference_pattern_matches_typescript_literal`: reads tracked `extensions/drm-copilot/src/lib/pr-context/models.ts`; asserts `f"/{ISSUE_REFERENCE_PATTERN.pattern}/u"` is in the source and `ISSUE_REFERENCE_PATTERN.flags & re.ASCII` is non-zero.
- `test_autoclose_literals_match_typescript_source`: asserts `f'"{AUTOCLOSE_UNVERIFIED_ANNOTATION}"'` and `f'"{AUTOCLOSE_PENDING_NOT_OPEN_TEXT}"'` are in the same source.

Python, `tests/scripts/dev_tools/pr_context/test_autoclose_collector.py` (collector runs through `collect_and_write` with `mem_fs_path`; feature `2026-09-25-autoclose-fixture-622`; `spec.md`, `issue.md` containing `- Issue: #622`, and `feature-audit.2026-01-01T00-00.md` containing `Readiness: PASS`):

- C1 `test_collector_excludes_scraped_tokens_from_autoclose_when_gh_unavailable` (spec prose `Timestamps use #ISO-8601 and ISO-8601; finding #CR-1 (CR-1); see #468.`; gh unavailable).
- C2 `test_collector_excludes_prose_cited_closed_issue_from_autoclose` (spec cites `#468`; `468` -> issue, state `closed`; `622` -> issue, `open`).
- C3 `test_collector_excludes_prose_cited_open_out_of_scope_issue_from_autoclose` (spec cites `#584`; `584` and `622` -> issue, `open`).
- C4 `test_collector_excludes_closed_pending_primary_without_printing_it`: parametrized over `622` classified `issue`/state `closed`, `issue`/`(unknown)`, `pull`, and `None`. 4 cases.
- C5 `test_collector_keeps_open_pending_primary`: parametrized over state `open` and `OPEN`. 2 cases.
- C6 `test_collector_fetches_each_issue_once` (spec cites `#468` closed issue, `#584` open issue, `#7` pull; `622` open issue).
- C7 `test_narrative_mentions_excluded_from_autoclose_section` (relocated; `issue.md` primary `#46`, spec prose `Narrative mentions #40 #42 #43 in notes.`; `46` -> issue `open`; others `None`; original assertions kept).
- C8 `test_pass_readiness_autoclose_section` (relocated; same fixture; original assertions kept).

Python, `tests/scripts/dev_tools/pr_context/test_autoclose.py`: `test_select_pending_primary_keeps_open_issue`, `test_select_pending_primary_keeps_uppercase_open_issue`, `test_select_pending_primary_excludes_closed_issue`, `test_select_pending_primary_excludes_unknown_state`, `test_select_pending_primary_excludes_pull_request`, `test_select_pending_primary_excludes_unclassified_ref`, `test_select_pending_primary_keeps_all_refs_when_gh_unavailable`, `test_select_pending_primary_fetches_each_issue_at_most_once`, `test_select_pending_primary_reports_mixed_outcomes`, `test_classify_references_routes_issue_pull_and_invalid`, `test_classify_references_adds_raw_refs_when_gh_unavailable`, `test_classify_references_prefixes_unprefixed_refs`.

Python, `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` (new #622-owned file, identical in both branches; spec D11): `test_build_issues_to_autoclose_section_appends_unverified_annotation_when_gh_unavailable`, `test_build_issues_to_autoclose_section_omits_annotation_when_gh_available`, `test_build_issues_to_autoclose_section_renders_not_open_text_when_pending_excluded`, `test_build_issues_to_autoclose_section_fallback_precedence` (5 cases, table below).

Python, M588 only, in #588's `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`: updated H3 `test_build_issues_to_autoclose_section_lists_pending_refs_when_gh_unavailable` ([P6-T11]). In N588 that file does not exist and is not created.

Composed fallback precedence rows (both runtimes, both branches; exactly the five spec D12 rows; id, gh available, verified, pending, readiness, excluded -> body):

| id | gh | verified | pending | readiness | excluded | body |
|---|---|---|---|---|---|---|
| `available-non-empty` | true | `[]` | `["#7"]` | `["PASS"]` | false | `- #7` |
| `available-empty-excluded` | true | `[]` | `[]` | `["PASS"]` | true | `None (deterministic pending issue is not an open issue)` |
| `available-empty-pass` | true | `[]` | `[]` | `["PASS"]` | false | `None (no verified closing issues and no deterministic pending issue)` |
| `available-empty-non-pass` | true | `[]` | `[]` | `[]` | false | `None (no verified closing issues and readiness not PASS)` |
| `unavailable-non-empty` | false | `[]` | `["#7"]` | `["PASS"]` | false | `- #7` + newline + the annotation |

Each row asserts exact equality of the whole result with the section header joined to the expected body by a newline. No #622 test asserts the (gh unavailable, empty list) body, whose text differs between M588 and N588 and is owned by #588 (spec D12).

Python, changed existing tests: `test_build_close_candidates_section_lists_referenced_issues_as_detected_only` (replaces `test_build_close_candidates_section_promotes_referenced_issues_to_auto_close`); `test_extract_issue_references_filters_and_deduplicates` (expects `["#12"]`); `test_gather_feature_excerpts_reads_active_docs` (expects `["#77"]`); `test_extract_issue_references_ignores_jira` in `test_render.py` (renamed from `test_extract_issue_references_finds_jira`, expects `[]`); `test_build_close_candidates_section_combines_author_and_ref` (docstring rewritten, assertions tightened); `test_extract_issue_references_ignores_jira` and `test_extract_issue_references_mixed` in `test_feature_docs.py`; `test_gather_feature_excerpts_extracts_issue_refs` (asserts `"ABC-456" not in`); the `FakeGh.classify_entity` double in `test_collect_pr_context_part2.py` (drops `ABC-10`).

TypeScript, `extensions/drm-copilot/test/lib/pr-context/issue-reference-pattern.test.ts`: `describe("extractIssueReferences bare-number contract")` with `it.each` over the three exports (`feature-docs-parsers`, `render-pr-helpers`, `render-feature-excerpts`) crossed with the same 13 rejection inputs (`rejects %s from %s`, 39 cases) and 6 acceptance inputs (`accepts %s from %s`, 18 cases).

TypeScript, `extensions/drm-copilot/test/lib/pr-context/collector-core-autoclose.test.ts` (`collectAndWrite` with `TreeFileSystem`, scripted runner, fixed clock, `jest.spyOn(GhClient.prototype, "classifyEntity" | "issueDetails" | "prDetails")`, `afterEach(() => { jest.restoreAllMocks(); })`), `describe("collector autoclose derivation")`:

- T1 `excludes scraped tokens from autoclose when gh is unavailable`
- T2 `excludes a prose-cited closed issue from autoclose`
- T3 `excludes a prose-cited open out-of-scope issue from autoclose`
- T4 `it.each` `excludes a closed pending primary without printing it (%s)` over `closed`, `(unknown)`, `pull`, `null`. 4 cases.
- T5 `it.each` `keeps an open pending primary (%s)` over `open`, `OPEN`. 2 cases.
- T6 `fetches each issue once`

TypeScript, `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts`: `describe("selectPendingPrimary")`: `keeps an open issue`, `keeps an OPEN issue`, `excludes a closed issue`, `excludes an (unknown) state`, `excludes a pull request`, `excludes an unclassified ref`, `keeps every ref when gh is unavailable`, `fetches each issue at most once`, `reports mixed outcomes`; `describe("classifyReferences")`: `routes issue, pull, and invalid refs`, `adds raw refs when gh is unavailable`, `prefixes an unprefixed ref`; and the four #622 builder tests (spec D11, identical in both branches), `describe("buildIssuesToAutocloseSection")`: `appends the unverified annotation when gh is unavailable`, `omits the annotation when gh is available`, `renders the not-open text when the pending primary is excluded`, `it.each` `applies the composed fallback precedence: %s` (the 5 rows above).

TypeScript, `extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts`: `keeps referenced issues out of author auto-close` (replaces `merges author-asserted and referenced into author auto-close`), both branches; M588 only: updated #588 H3 `lists pending refs unchanged when gh is unavailable` or `lists pending refs and omits the empty-list unavailable body when gh is unavailable`, whichever title #588's merged file carries ([P7-T3]).

TypeScript, `extensions/drm-copilot/test/lib/pr-context/feature-docs.test.ts`: `ignores JIRA-style references` (renamed from `extracts JIRA-style references`, expects `[]`); `extracts issue references from the combined doc text` (asserts `not.toContain("ABC-456")`).

---

### Phase 0 — #588 Detection, Integration by Merge, Scope Anchor, Policy Reads, and Baselines

- [ ] [P0-T1] Confirm a clean starting tree and a fast-forwardable remote branch. Run `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, `git status --porcelain`, `git fetch origin bug/collect-pr-context-fabricates-auto-close-issues-622`, and `git merge-base --is-ancestor origin/bug/collect-pr-context-fabricates-auto-close-issues-622 HEAD`. Acceptance: `git status --porcelain` prints nothing, or prints exactly the single line `?? docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/` left by an earlier interrupted run (the artifacts of [P0-T2] through [P0-T4] overwrite it); the fetch exits 0; the ancestor check exits 0, so the remote branch is contained in HEAD and every later push is a fast-forward. The local branch name is recorded, not asserted (it may differ from the remote branch name). If the porcelain output contains anything else, or the ancestor check exits non-zero, stop and report to the orchestrator (the orchestrator commits the plan before execution). The outputs are recorded by [P0-T4]. Do not write this task's checkbox until [P0-T3] acceptance has passed, so the plan file is unmodified during the merge.
- [ ] [P0-T2] Detect the branch (spec D9). Run `git fetch origin main`, `git rev-parse origin/main`, and `git grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" origin/main -- scripts/dev_tools/pr_context/render_pr_helpers.py`. Decision rule: the `git grep` exits 0 and prints `origin/main:scripts/dev_tools/pr_context/render_pr_helpers.py:<n>` with `<n>` at least 1 -> branch `M588`; it exits 1 with no output -> branch `N588` (zero matches; this is the expected outcome on a base without #588 and is not a failure); any other exit code -> stop and report the output (an execution error, not a branch). Write two artifacts. (a) `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/588-detection.2026-09-25T23-29.md` with `Timestamp:`, `Command:` and `EXIT_CODE:` for the fetch and the `rev-parse` only (both expected 0), a line `Branch: M588` or `Branch: N588`, a line `Base: <the 40-character origin/main SHA>`, and `Output Summary:` quoting the `git grep` command, its exit code, its output (or `(no output)`), and the decision rule applied. (b) `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/588-detection-grep.2026-09-25T23-29.md` with `Timestamp:`, `Command:` (the `git grep` only), `EXIT_CODE:`, and `Output Summary:`; in N588 it also carries `ExpectedExitCode: 1`, and in M588 it carries no `ExpectedExitCode:` line. Acceptance: artifact (a) has exactly one `Branch:` line with value `M588` or `N588` and one `Base:` line; artifact (b) records `EXIT_CODE: 1` and `ExpectedExitCode: 1` in N588, or `EXIT_CODE: 0` and a count of at least 1 in M588. Neither branch stops execution. This task is the only branch detection; every later branch-conditional task reads the `Branch:` line of artifact (a). Do not write this task's checkbox until [P0-T3] acceptance has passed.
- [ ] [P0-T3] Integrate `origin/main` by merge (spec D13; never rebase, never force-push). Precondition: `git status --porcelain --untracked-files=no` prints nothing (if it prints anything, stop and report). Run `git merge-base --is-ancestor origin/main HEAD` and write it alone to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/integration-precheck.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (stating `merge needed` for exit 1 or `merge not needed` for exit 0), plus `ExpectedExitCode: 1` when it exited 1 (no `ExpectedExitCode:` line when it exited 0); any other exit code stops execution with a report. When it exited 1, run `git merge --no-edit origin/main` (the merge commit keeps git's default message; it is logged in the commit log by the next commit task). If the merge reports conflicts, run `git status --porcelain`, record every line beginning with `UU`, `AA`, `DU`, `UD`, `AU`, or `UA`, run `git merge --abort`, and stop and report (a conflict is an execution error, not a branch). Then run `git merge-base --is-ancestor origin/main HEAD`, `git rev-parse HEAD`, and `git push origin HEAD:bug/collect-pr-context-fabricates-auto-close-issues-622`. Acceptance: the merge, when run, exits 0; the second ancestor check exits 0; the push exits 0 (a fast-forward; when no merge was needed and HEAD equals the remote branch, git prints `Everything up-to-date` and exits 0). After acceptance passes, check off [P0-T1], [P0-T2], and [P0-T3] together.
- [ ] [P0-T4] Record the integration and the branch pre-state. N588: run `git ls-files --cached --others -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts`; acceptance for this part: it exits 0 and prints nothing (none of the #588-only files exists on the merged base). M588: run `grep -c -F -e "gh_available: bool = True" scripts/dev_tools/pr_context/render_pr_helpers.py`, `grep -c -F -e "ghAvailable?: boolean" extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts`, `git ls-files -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`, and `grep -n -F -e "GitHub CLI unavailable" tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts`; acceptance for this part: the first prints `1`, the second prints at least `1`, the third prints the path, and the fourth exits 0 (its hit lines are the input to [P6-T11] and [P7-T3]). In M588, a parameter grep that prints `0` means #588 merged without the parameter form the spec's coordination contract states (spec `## Assumptions, Constraints, Dependencies`); stop and report that contract breach to the orchestrator. Then write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/integration.2026-09-25T23-29.md` with `Timestamp:`, `Command:` (every command from [P0-T1], every [P0-T3] command except the precheck, and this task's branch commands), `EXIT_CODE:` (one per command, each 0), and `Output Summary:` stating the local branch name, `Branch:` from [P0-T2], the pre-merge and post-merge `git rev-parse HEAD` values, whether a merge ran, the push output line, and this task's branch-command outputs. Acceptance: the artifact exists with all four fields and every recorded `EXIT_CODE:` is 0. This artifact and the two [P0-T2] artifacts are the primary evidence for AC 19 and AC 20.
- [ ] [P0-T5] Record the scope anchor. Run `git merge-base HEAD origin/main`, `git rev-parse origin/main`, `git merge-base --is-ancestor <scope-anchor-sha> HEAD`, `git diff --name-only <scope-anchor-sha> HEAD`, and the companion `git status --porcelain`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/scope-anchor.2026-09-25T23-29.md` with the four fields; `Output Summary:` records the 40-character `<scope-anchor-sha>`, the `origin/main` SHA, the full name-only path list, and the full porcelain output. Acceptance: `<scope-anchor-sha>` is 40 hexadecimal characters and equals the `origin/main` SHA; the ancestor check exits 0; every path printed by `git diff --name-only <scope-anchor-sha> HEAD` begins with `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/`; and every path in the porcelain output begins with the same prefix (the porcelain companion covers untracked evidence files the name-only diff cannot list). If any path falls outside the feature folder, stop and report before Phase 1.
- [ ] [P0-T6] Read `CLAUDE.md` in full (policy-order position 1). Acceptance: listed first under `Files Read:` in the [P0-T11] artifact.
- [ ] [P0-T7] Read `.claude/rules/general-code-change.md` in full (position 2). Acceptance: listed second in the [P0-T11] artifact.
- [ ] [P0-T8] Read `.claude/rules/general-unit-test.md` in full (position 3). Acceptance: listed third in the [P0-T11] artifact.
- [ ] [P0-T9] Read `.claude/rules/python.md` and `.claude/rules/python-suppressions.md` in full (position 4, Python). Acceptance: listed fourth and fifth in the [P0-T11] artifact.
- [ ] [P0-T10] Read `.claude/rules/typescript.md` and `.claude/rules/typescript-suppressions.md` in full (position 4, TypeScript). Acceptance: listed sixth and seventh in the [P0-T11] artifact.
- [ ] [P0-T11] Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order: CLAUDE.md -> general-code-change -> general-unit-test -> Python rules -> TypeScript rules`, and `Files Read:` listing the seven files from [P0-T6]-[P0-T10] in that order. Acceptance: the artifact exists with the three fields and seven entries.
- [ ] [P0-T12] Record post-merge file sizes and region locations. Run `wc -l scripts/dev_tools/pr_context/models.py scripts/dev_tools/pr_context/feature_docs.py scripts/dev_tools/pr_context/render_feature_excerpts.py scripts/dev_tools/pr_context/render_pr_helpers.py scripts/dev_tools/pr_context/collector.py extensions/drm-copilot/src/lib/pr-context/models.ts extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts extensions/drm-copilot/src/lib/pr-context/collector-core.ts extensions/drm-copilot/jest.config.cjs tests/scripts/dev_tools/test_collect_pr_context.py tests/scripts/dev_tools/test_collect_pr_context_part2.py tests/scripts/dev_tools/test_collect_pr_context_part4.py tests/scripts/dev_tools/test_render.py tests/scripts/dev_tools/test_feature_docs.py extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts extensions/drm-copilot/test/lib/pr-context/feature-docs.test.ts extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` (19 paths, both branches), and in M588 only also `wc -l tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` (in N588 that file does not exist and is not measured), then `grep -n -E -e "def (build_issues_to_autoclose_section|build_close_candidates_section|extract_issue_references|collect_and_write)" scripts/dev_tools/pr_context/render_pr_helpers.py scripts/dev_tools/pr_context/collector.py` and `grep -n -E -e "function (buildIssuesToAutocloseSection|buildCloseCandidatesSection|extractIssueReferences|classifyReferences|classifyOne|formatRef|collectPrContext)" extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts extensions/drm-copilot/src/lib/pr-context/collector-core.ts`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/file-sizes.2026-09-25T23-29.md` with the four fields. Acceptance: the first `wc -l` prints 19 file rows plus `total`, and in M588 the second prints one row; one location line per function; `Output Summary:` records `Branch:` from [P0-T2]; in N588 it names any count that differs from the "This tree (N588)" column of the budget table; in both branches it names any count already above its "#622 ceiling"; and it restates the binding "#622 ceiling" for `collector.py` as its recorded count, for `collector-core.test.ts` as its recorded count plus 2, and in M588 for `test_render_pr_helpers.py` as its recorded count plus 10. A count already above its "#622 ceiling" stops execution with a report before Phase 1, except for the five rows whose ceiling this plan reaches by removing code: `extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts`, `extensions/drm-copilot/src/lib/pr-context/collector-core.ts`, `tests/scripts/dev_tools/test_collect_pr_context.py`, `tests/scripts/dev_tools/test_collect_pr_context_part4.py`, and `tests/scripts/dev_tools/test_render.py`. For those five, `Output Summary:` records the count, the ceiling, and the difference, and no stop applies here. Their ceilings are enforced by the tasks that edit them ([P2-T3], [P2-T5], [P4-T4], [P4-T5], [P6-T4], [P6-T6]) and by [P10-T7].
- [ ] [P0-T13] Record test-collection counts for the three files this plan splits. Run `poetry run pytest --collect-only -q tests/scripts/dev_tools/test_collect_pr_context.py`, the same for `tests/scripts/dev_tools/test_collect_pr_context_part4.py`, and the same for `tests/scripts/dev_tools/test_render.py`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/test-collection-counts.2026-09-25T23-29.md` with the four fields. Acceptance: each command exits 0 and `Output Summary:` records each file's `N tests collected` count as `N_cpc`, `N_p4`, and `N_render`.
- [ ] [P0-T14] Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/python-batch-budget.2026-09-25T23-29.md` recording `Timestamp:`, `Branch:` from [P0-T2], the planned Python writes for that branch (6 production files; 11 test files in N588 or 12 in M588, as listed in the budget rows for `<branch>`), the cap (3 production, 3 test), and the scheduled resets ([P2-T4], [P3-T1], [P3-T5], [P6-T1], [P6-T5], [P6-T9]). Acceptance: the artifact exists with those entries.
- [ ] [P0-T15] Python baseline formatting. Run `poetry run black --check .`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/py-black.2026-09-25T23-29.md` with the four fields. Acceptance: `Output Summary:` records the exit code and the Black summary line (on a clean tree it contains `would be left unchanged`). Check mode only; no source is written. Stop condition: a non-zero exit is pre-existing drift; stop and report the listed files before Phase 1, because [P8-T1] requires exit 0 on the whole tree.
- [ ] [P0-T16] Python baseline linting. Run `poetry run ruff check .`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/py-ruff.2026-09-25T23-29.md` with the four fields. Acceptance: `Output Summary:` records the exit code and either `All checks passed!` or the finding count. No `--fix` flag is passed. Stop condition: any finding stops execution with a report before Phase 1.
- [ ] [P0-T17] Python baseline type check. Run `poetry run pyright`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/py-pyright.2026-09-25T23-29.md` with the four fields. Acceptance: `Output Summary:` records the exit code and the summary line (`0 errors, 0 warnings, 0 informations` on a clean run). Stop condition: any error stops execution with a report before Phase 1.
- [ ] [P0-T18] Python baseline tests with coverage. Run `poetry run pytest --cov=scripts.dev_tools.pr_context --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/python-coverage-baseline.json --deselect tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` (full suite). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/py-pytest-coverage.2026-09-25T23-29.md` with the four fields. Acceptance: the result line reports `1 deselected`; `Output Summary:` cites #510 as the deselection reason and records the result line, the `TOTAL` row, and the term-missing rows for `models.py`, `feature_docs.py`, `render_feature_excerpts.py`, `render_pr_helpers.py`, and `collector.py`; the JSON file exists. Stop condition: any failed test stops execution with the failing node IDs reported before Phase 1.
- [ ] [P0-T19] Python baseline per-file coverage. Read `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/python-coverage-baseline.json`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/python-coverage-baseline.2026-09-25T23-29.md` with `Timestamp:`, `Command:` (the [P0-T18] command), `EXIT_CODE:` (from [P0-T18]), and `Output Summary:` recording, for each of the five files in [P0-T18], `covered_lines`/`num_statements` and `covered_branches`/`num_branches` from `files["<path key>"]["summary"]` and the two derived percentages to two decimals. The path key uses the host separator (for example `scripts\\dev_tools\\pr_context\\collector.py` on Windows). Acceptance: five rows, each with raw counts and percentages; any file below 85.00% line or 75.00% branch is named explicitly.
- [ ] [P0-T20] TS baseline formatting. Precondition: from `extensions/drm-copilot` run `test -f node_modules/jest/bin/jest.js` and `test -f node_modules/prettier/package.json`; if either exits non-zero, run `npm ci`, record its `added ... packages` line, and repeat both checks, which must then exit 0. Run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/ts-prettier.2026-09-25T23-29.md` with the four fields. Acceptance: the artifact records the exit code and either `All matched files use Prettier code style!` or the unformatted-file list. Check mode only. Stop condition: a non-zero exit stops execution with a report before Phase 1.
- [ ] [P0-T21] TS baseline linting. Run `npm run lint` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/ts-eslint.2026-09-25T23-29.md` with the four fields. Acceptance: the artifact records the exit code and the error and warning counts (0 and 0 when ESLint prints no problem lines). Stop condition: any error stops execution with a report.
- [ ] [P0-T22] TS baseline type check. Run `npm run typecheck` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/ts-tsc.2026-09-25T23-29.md` with the four fields. Acceptance: the artifact records the exit code and the count of `error TS` lines. Stop condition: a non-zero count stops execution with a report.
- [ ] [P0-T23] TS baseline architecture-boundary stage. Run `git ls-files -- "*.dependency-cruiser*"` from the repository root. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/ts-dependency-cruiser.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0`, empty output, and `Output Summary:` stating `dependency-cruiser not configured; architecture-boundary stage has no tool to run`. If the output is non-empty, treat a listed path as a configuration only when its file name begins with `.dependency-cruiser`, run it, and require zero violations.
- [ ] [P0-T24] TS baseline tests with coverage. Run `node run-jest.cjs --coverage --coverageReporters=text --coverageReporters=text-summary --coverageReporters=lcov` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/ts-jest-coverage.2026-09-25T23-29.md` with the four fields, and write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/typescript-coverage-baseline.2026-09-25T23-29.md` with the same four fields. Acceptance: both `Output Summary:` fields record the `Test Suites:` and `Tests:` lines verbatim, the text-summary `Lines` and `Branches` percentages, and the `text` rows (`% Stmts | % Branch | % Funcs | % Lines | Uncovered Line #s`) for `models.ts`, `feature-docs-parsers.ts`, `render-feature-excerpts.ts`, `render-pr-helpers.ts`, and `collector-core.ts`; any of these five below 85 `% Lines` or 75 `% Branch` is named explicitly. Stop condition: a non-zero exit or any failed test stops execution with a report before Phase 1.
- [ ] [P0-T25] Commit and push Phase 0 evidence. Paths: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Message subject: `docs(bug): record phase 0 baselines for #622`. Acceptance: the commit exits 0; `git status --porcelain -- docs/features/active/collect-pr-context-fabricates-auto-close-issues-622` prints nothing; the push exits 0; this commit's commit-log line is appended by the next commit task before staging, per the Commit rule; `git ls-files -- docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline` lists `python-coverage-baseline.json`, `python-coverage-baseline.2026-09-25T23-29.md`, and `typescript-coverage-baseline.2026-09-25T23-29.md`.

### Phase 1 — Shared Constants (No Behavior Change)

- [ ] [P1-T1] Edit `scripts/dev_tools/pr_context/models.py`: after `CONVENTIONAL_TYPES` (currently lines 13-24) add, each with a one-line comment naming issue #622 and D1/D4/D5: `ISSUE_REFERENCE_PATTERN: re.Pattern[str] = re.compile(r"(?<!\w)#\d+(?!\w)", re.ASCII)`; `AUTOCLOSE_UNVERIFIED_ANNOTATION`, whose value is exactly `Unverified: the issues listed above come from feature metadata only and were not checked against GitHub (GitHub CLI unavailable).` written as a parenthesized implicit concatenation of two string literals so each line stays within 88 characters; and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT = "None (deterministic pending issue is not an open issue)"`. No other line changes. Acceptance: `grep -c -F -e 're.compile(r"(?<!\w)#\d+(?!\w)", re.ASCII)' scripts/dev_tools/pr_context/models.py` prints `1`; the file is at most 225 lines.
- [ ] [P1-T2] Edit `extensions/drm-copilot/src/lib/pr-context/models.ts`: after `CONVENTIONAL_TYPES` / `ConventionalType` (currently lines 29-44) add three exported constants with TSDoc naming issue #622: `export const ISSUE_REFERENCE_PATTERN = /(?<!\w)#\d+(?!\w)/u;` (TSDoc states that callers must build a fresh global copy with `new RegExp(ISSUE_REFERENCE_PATTERN, "gu")` and must not rely on `lastIndex`), `AUTOCLOSE_UNVERIFIED_ANNOTATION` (the same text as [P1-T1], as one contiguous double-quoted string literal), and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` (the same text as [P1-T1], one contiguous double-quoted literal). Update the module TSDoc `Responsibilities` list to name the three constants. Acceptance: `grep -c -F -e "export const ISSUE_REFERENCE_PATTERN = /(?<!\w)#\d+(?!\w)/u;" extensions/drm-copilot/src/lib/pr-context/models.ts` prints `1`; the file is at most 350 lines.
- [ ] [P1-T3] Verify the constants change no behavior and type-check. Run `poetry run pyright scripts/dev_tools/pr_context/models.py`, `poetry run pytest tests/scripts/dev_tools/test_collect_pr_context.py -q`, `npm run typecheck` (TS), and `node run-jest.cjs test/lib/pr-context/models.test.ts` (TS). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/phase1-constants-check.2026-09-25T23-29.md` with the four fields per command. Then, from the repository root, run the early scan of the two files Phase 1 wrote, with the same fixed-string mode and the same three patterns as [P10-T1] and [P10-T4]: `git grep --untracked -n -F -e "[A-Z][A-Z0-9]+-" -e "ABC-123" -e "Detected issue references (classified)" -- "scripts/dev_tools/pr_context/models.py" "extensions/drm-copilot/src/lib/pr-context/models.ts"`. Record the scan's command, exit code, and output (or `(no output)`) as text inside the same artifact's `Output Summary:`, not as an additional `EXIT_CODE:` line. Acceptance: all four exit 0; Pyright prints `0 errors, 0 warnings, 0 informations`; the pytest result line contains no `failed` count; the early scan exits 1 with no output. On a scan hit, edit the comment or TSDoc text of the constants in the named file so that none of the three literals remains, and re-run this task.
- [ ] [P1-T4] Commit and push Phase 1. Paths: `scripts/dev_tools/pr_context/models.py`, `extensions/drm-copilot/src/lib/pr-context/models.ts`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `feat(pr-context): add shared issue-reference and autoclose literals (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- scripts/dev_tools/pr_context/models.py extensions/drm-copilot/src/lib/pr-context/models.ts` prints nothing.

### Phase 2 — Fail-First Regression Tests and Autoclose Test Relocation

- [ ] [P2-T1] [expect-fail] Create `tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py` with the four Python tests listed under "Named tests" for this file. Import `extract_issue_references` from `scripts.dev_tools.pr_context.feature_docs` and from `scripts.dev_tools.pr_context.render_pr_helpers`; obtain the private extractor as `vars(render_feature_excerpts)["_extract_issue_references"]` bound to a module-level variable annotated `Callable[[str], list[str]]`; import `ISSUE_REFERENCE_PATTERN`, `AUTOCLOSE_UNVERIFIED_ANNOTATION`, and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` from `scripts.dev_tools.pr_context.models`. The parity tests read the tracked TS file at `Path(__file__).resolve().parents[4] / "extensions/drm-copilot/src/lib/pr-context/models.ts"` (the test lives one directory deeper than `test_pr_context_freshness.py`, which uses `parents[3]`). Every test has a docstring and Arrange-Act-Assert structure; no fixture beyond `pytest.mark.parametrize`. Acceptance: the file is at most 150 lines, LF-terminated, and its pre-fix result is recorded by [P2-T9].
- [ ] [P2-T2] [expect-fail] Create `tests/scripts/dev_tools/pr_context/test_autoclose_collector.py` with tests C1-C8 as listed under "Named tests". Shared module-level infrastructure: a `_StubGit` class (methods and return shapes of `StubGit` in `tests/scripts/dev_tools/test_pr_context_integration.py` lines 22-71, with `branch_name` returning `feature/autoclose-fixture`, `upstream` returning `upstream/feature/autoclose-fixture`, and `status_short` returning `## feature/autoclose-fixture`); a factory `_gh_double(*, available: bool, classifications: dict[str, str | None], states: dict[str, str], issue_detail_calls: list[str])` returning a fresh class per call whose `ensure_available` raises `RuntimeError("offline")` when not available, whose `classify_entity` returns `classifications.get(number)`, whose `issue_details` appends `number` to `issue_detail_calls` and returns an `IssueDetails` with `state=states.get(number, "(unknown)")`, and which also provides `current_pr` (returns `None`), `pr_details`, `ci_status`, `status_message`, and `available`; and a helper `_run_collector(monkeypatch, mem_fs_path, *, spec_text, gh_cls, primary="#622") -> str` that writes `spec.md` (given text), `issue.md` (`- Issue: ` followed by `primary` and a newline), and `feature-audit.2026-01-01T00-00.md` (`Readiness: PASS\n`) under `docs/features/active/2026-09-25-autoclose-fixture-622/`, creates `docs/features/potential/promoted`, patches `scripts.dev_tools.pr_context.collector.write_output`, `.GitClient`, `.GhClient`, `.build_pr_context` (returning a `PRContextResult` whose text is `===== Changed files (name-status) =====` followed by `M\tdocs/features/active/2026-09-25-autoclose-fixture-622/spec.md`, empty reference lists, and `resolved_base="main"`), and `._scoping_doc_changes` (returns `[]`), runs `collect_and_write`, and returns the summary text. Section helpers: `_section(summary, title)` returns the text from `===== <title> =====` up to the next line starting with `=====`; `_lines_after(summary, label)` returns the lines after `label` up to the first blank line. Assertions per test: C1 — the `Issues to autoclose (verified or pending)` section and the lines after both `Auto-close issues (verified from GitHub PR metadata):` and `Auto-close issues (author asserted):` contain none of `ISO-8601`, `CR-1`, `#468`; in the autoclose section the line after `- #622` equals `AUTOCLOSE_UNVERIFIED_ANNOTATION`; the `Referenced issues (classified)` section contains `#468` and `NOTE: Unverified (GitHub unavailable)`. C2 — `#468` absent from the autoclose section and both `Auto-close issues` label blocks. C3 — `#584` absent from those three places and present in the lines after `Referenced issues (detected):`. C4 — the autoclose section's last non-empty line equals `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` and `#622` is absent from the section. C5 — the section contains `- #622` and does not contain `Unverified:`. C6 — `sorted(issue_detail_calls) == ["468", "584", "622"]`. C7 — the original assertions of `test_narrative_mentions_excluded_from_autoclose_section` (test_collect_pr_context.py lines 646-654) with `#46` classified `issue`/`open` and `#40`, `#42`, `#43` classified `None`. C8 — the original assertions of `test_pass_readiness_autoclose_section` (part4 lines 853-855) with the same double. No string in the file contains `origin/`. Acceptance: the file is at most 460 lines, LF-terminated, and its pre-fix result is recorded by [P2-T9].
- [ ] [P2-T3] Edit `tests/scripts/dev_tools/test_collect_pr_context.py`: delete `test_narrative_mentions_excluded_from_autoclose_section` (currently lines 479-654) and the two blank lines before it; remove any import that becomes unused (Ruff reports it in [P8-T2]). No other change in this task. Acceptance: `grep -c -F -e "def test_narrative_mentions_excluded_from_autoclose_section" tests/scripts/dev_tools/test_collect_pr_context.py` prints `0` and the file is at most 480 lines.
- [ ] [P2-T4] Python batch-budget reset (test files [P2-T1]-[P2-T3] fill the test cap). Apply the reset procedure from "Constraints and assumptions" and append the record. Acceptance: the reset record exists in the budget artifact.
- [ ] [P2-T5] Split `tests/scripts/dev_tools/test_collect_pr_context_part4.py`. Create `tests/scripts/dev_tools/test_collect_pr_context_part5.py` with a module docstring naming issue #622 and the 500-line cap as the reason for the split, the import block of part4 lines 1-17 reduced to the names the moved tests use, then `test_collector_reports_unparseable_evidence_without_claiming_completion` (part4 lines 500-685) and `test_non_pass_readiness_fallback` (part4 lines 858-1026) copied verbatim except that each `origin/` substring becomes `upstream/`. In part4, delete lines 498-1026 (the two blank lines at 498-499 and the three tests `test_collector_reports_unparseable_evidence_without_claiming_completion`, `test_pass_readiness_autoclose_section`, and `test_non_pass_readiness_fallback`), so the file ends at line 497; `test_pass_readiness_autoclose_section` already lives in `test_autoclose_collector.py` as C8. Acceptance: part4 is at most 497 lines; part5 is at most 400 lines; `grep -c -F -e "origin/" tests/scripts/dev_tools/test_collect_pr_context_part5.py` prints `0`; `grep -c -E -e "^def test_" tests/scripts/dev_tools/test_collect_pr_context_part5.py` prints `2`.
- [ ] [P2-T6] Verify the Python relocation preserved every test. Run `poetry run pytest --collect-only -q tests/scripts/dev_tools/test_collect_pr_context.py tests/scripts/dev_tools/test_collect_pr_context_part4.py tests/scripts/dev_tools/test_collect_pr_context_part5.py`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/relocation-counts-p2.2026-09-25T23-29.md` with the four fields. Acceptance: exit 0 and the collected count equals `N_cpc + N_p4 - 2` using the [P0-T13] values (the two relocated autoclose tests now live in `test_autoclose_collector.py` as C7 and C8).
- [ ] [P2-T7] [expect-fail] Create `extensions/drm-copilot/test/lib/pr-context/issue-reference-pattern.test.ts` with the matrices listed under "Named tests", importing `extractIssueReferences` from `../../../src/lib/pr-context/feature-docs-parsers`, `../../../src/lib/pr-context/render-pr-helpers`, and `../../../src/lib/pr-context/render-feature-excerpts` under distinct local names. Acceptance: the [P2-T10] output contains no `Test suite failed to run` line for this file; the file is at most 150 lines, LF-terminated, and its pre-fix result is recorded by [P2-T10].
- [ ] [P2-T8] [expect-fail] Create `extensions/drm-copilot/test/lib/pr-context/collector-core-autoclose.test.ts` with T1-T6 as listed under "Named tests". Infrastructure: a `ScriptRunner` honoring the throw-on-nonzero contract (as in `collector-core.test.ts` lines 23-36); a gh handler answering `auth status` with `ok("Logged in")` or `fail("offline")` by scenario, `repo view --json nameWithOwner` with `{"nameWithOwner": "owner/repo"}`, `pr view` with `fail("no pull request")`, `run list` with `[{"status":"success"}]`, other gh argv with `{}`; a git handler as in `collector-core.test.ts` lines 118-155 except that the `@{u}` branch returns `upstream/feature/docs`, and the `diff --name-status` and `diff --numstat` branches return `M\tdocs/features/active/2026-09-25-autoclose-fixture-622/spec.md` and `1\t0\tdocs/features/active/2026-09-25-autoclose-fixture-622/spec.md`; a seeded `TreeFileSystem` with `${ROOT}/.git`, `${ROOT}/docs/features/potential/promoted`, and feature `2026-09-25-autoclose-fixture-622` containing `spec.md` (scenario text), `issue.md` (`- Issue: #622\n`), and `feature-audit.2026-01-01T00-00.md` (`Readiness: PASS\n`); `jest.spyOn(GhClient.prototype, "classifyEntity")`, `"issueDetails"` (records each `numberRef` and returns an `IssueDetails` whose `state` comes from the scenario map, default `(unknown)`), and `"prDetails"` (returns a fixed `PullRequestDetails`); `collectAndWrite` from `../../../src/lib/pr-context/collector-output` with `whichGh: () => "/usr/bin/gh"`, `clock: () => new Date(Date.UTC(2026, 8, 25, 23, 29, 0))`, summary and appendix paths under `/repo/artifacts/`, and `log: () => undefined`; assertions read `result.summaryText` with section helpers equivalent to [P2-T2] and mirror C1-C6. `AUTOCLOSE_UNVERIFIED_ANNOTATION` and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` are imported from `../../../src/lib/pr-context/models`. No string contains `origin/`, and no test uses `setTimeout`, `Date.now`, or `child_process`. Acceptance: the [P2-T10] output contains no `Test suite failed to run` line for this file; the file is at most 420 lines, LF-terminated, and its pre-fix result is recorded by [P2-T10].
- [ ] [P2-T9] [expect-fail] Run the Python fail-first tests on pre-fix production code: `poetry run pytest tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py tests/scripts/dev_tools/pr_context/test_autoclose_collector.py -v`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-fail-first.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`. Acceptance: `EXIT_CODE: 1`; the result line reports `37 failed, 34 passed`; the failed node IDs are exactly: the `test_extractors_reject_non_bare_number_tokens` cases for inputs `#ISO-8601`, `#CR-1`, `ISO-8601`, `CR-1`, `UTF-8`, `SHA-256`, `AC-12`, `#12abc`, `#12_`, and `#١٢` under each of the three extractors (30), plus C1, C2, C3, and the four C4 cases (7); every `test_extractors_accept_bare_number_tokens` case, the reject cases for `abc#12`, `#`, and `""`, both parity tests, both C5 cases, C6, C7, and C8 pass. No failure is a collection or import error.
- [ ] [P2-T10] [expect-fail] Run the TS fail-first tests on pre-fix production code: `node run-jest.cjs test/lib/pr-context/issue-reference-pattern.test.ts test/lib/pr-context/collector-core-autoclose.test.ts --verbose` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/ts-fail-first.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`. Acceptance: `EXIT_CODE: 1`; the `Tests:` line contains `34 failed, 33 passed, 67 total`; the failures are the rejection cases for `#ISO-8601`, `#CR-1`, `ISO-8601`, `CR-1`, `UTF-8`, `SHA-256`, `AC-12`, `#12abc`, and `#12_` under each of the three exports (27; the JavaScript `\d` class is ASCII-only, so `#١٢` already passes), plus T1, T2, T3, and the four T4 cases (7); the output contains no `Test suite failed to run` line for either file (ts-jest runs with `isolatedModules: true` per `tsconfig.jest.json`, so a type error is not reported here; `npm run typecheck` uses `tsconfig.json`, whose `include` is `src/**/*.ts` only, so no gate in this plan type-checks TypeScript test files).
- [ ] [P2-T11] Commit and push Phase 2. Paths: `tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py`, `tests/scripts/dev_tools/pr_context/test_autoclose_collector.py`, `tests/scripts/dev_tools/test_collect_pr_context.py`, `tests/scripts/dev_tools/test_collect_pr_context_part4.py`, `tests/scripts/dev_tools/test_collect_pr_context_part5.py`, `extensions/drm-copilot/test/lib/pr-context/issue-reference-pattern.test.ts`, `extensions/drm-copilot/test/lib/pr-context/collector-core-autoclose.test.ts`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `test(pr-context): add fail-first autoclose regression tests (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- tests extensions/drm-copilot/test` prints nothing.

### Phase 3 — Python Fix

- [ ] [P3-T1] Python batch-budget reset before the production batch. Apply the reset procedure and append the record. Acceptance: the reset record exists.
- [ ] [P3-T2] Edit `extract_issue_references` in `scripts/dev_tools/pr_context/feature_docs.py` (currently lines 34-44): import `ISSUE_REFERENCE_PATTERN` from `.models` (extend the existing `from .models import ...` line); replace the `re.findall(...)` call with `ISSUE_REFERENCE_PATTERN.findall(text)`; add a Google-style docstring whose first line is `Extract bare-number issue references (for example #123) in encounter order.` and which states that only `#` followed by ASCII digits, not preceded or followed by a word character, is returned. Keep the name, signature, ordered de-duplication, and the intent comment requirement for the loop. Acceptance: `grep -c -F -e "ISSUE_REFERENCE_PATTERN" scripts/dev_tools/pr_context/feature_docs.py` prints at least `2`; `grep -c -F -e "bare-number" scripts/dev_tools/pr_context/feature_docs.py` prints at least `1`; the file is at most 360 lines.
- [ ] [P3-T3] Edit `_extract_issue_references` in `scripts/dev_tools/pr_context/render_feature_excerpts.py` (currently lines 25-36) the same way as [P3-T2], with the docstring first line `Extract bare-number issue references (for example #123) from freeform text.` Acceptance: `ISSUE_REFERENCE_PATTERN` count at least `2`, `bare-number` count at least `1`, file at most 262 lines.
- [ ] [P3-T4] Edit `scripts/dev_tools/pr_context/render_pr_helpers.py`. (a) `extract_issue_references` (currently lines 100-111): the same change as [P3-T2], docstring first line `Extract bare-number issue references (for example #123) in encounter order.` (b) `build_close_candidates_section` (currently lines 200-225): `author_auto_close = sorted(set(author_asserted))` and `referenced_only = sorted(set(referenced) - set(verified) - set(author_asserted))`, with a decision comment citing D2; the rendered headers and fallback strings are unchanged. (c) `build_issues_to_autoclose_section` (located by name; on this tree lines 228-272). Both branches: add keyword-only `pending_primary_excluded: bool = False`; import `AUTOCLOSE_UNVERIFIED_ANNOTATION` and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` from `.models`; when the ordered list is non-empty, the body is the unchanged bulleted list, followed by `"\n" + AUTOCLOSE_UNVERIFIED_ANNOTATION` only when `gh_available` is false; document both keyword parameters in `Args:`. N588: add the keyword-only parameter `gh_available: bool = True` (same name, default, and meaning as #588's, spec D10) and read it only in the non-empty annotation branch; the empty-list precedence is `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` when `pending_primary_excluded`, then the unchanged PASS text, then the unchanged non-PASS text; do not add the #588 literal and do not add any empty-list branch keyed on `gh_available`. M588: #588's `gh_available: bool = True` already exists; reuse it and do not declare it again; the empty-list precedence is #588's unchanged unavailable text first, then `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` when `pending_primary_excluded`, then the unchanged PASS text, then the unchanged non-PASS text. In both branches add a decision-logic comment stating the precedence order and why. Acceptance (both branches): `grep -c -F -e "gh_available: bool = True" scripts/dev_tools/pr_context/render_pr_helpers.py` prints `1`; `grep -c -F -e "pending_primary_excluded" scripts/dev_tools/pr_context/render_pr_helpers.py` prints at least `3`; `grep -c -F -e "ABC-123" scripts/dev_tools/pr_context/render_pr_helpers.py` prints `0`; the strings `None (no verified closing issues and no deterministic pending issue)` and `None (no verified closing issues and readiness not PASS)` each still occur at least once (`grep -c -F`); the file is at most 345 lines. Acceptance (branch-specific): `grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" scripts/dev_tools/pr_context/render_pr_helpers.py` prints `0` in N588 and at least `1` in M588.
- [ ] [P3-T5] Python batch-budget reset before the second production batch. Apply the reset procedure and append the record. Acceptance: the reset record exists.
- [ ] [P3-T6] Create `scripts/dev_tools/pr_context/autoclose.py` with a module docstring naming issue #622 and D3/D5/D7/D8, and: `ReferenceClassifier` (`typing.Protocol` with `classify_entity(self, number: str) -> str | None`); `PendingIssueClient` (`Protocol` extending it with `issue_details(self, number: str) -> IssueDetails`); `@dataclass(frozen=True) class PendingPrimarySelection` with `kept: list[str]`, `excluded: bool`, `fetched_details: dict[str, IssueDetails]` and a class docstring; `classify_references(*, gh: ReferenceClassifier, gh_available: bool, feature_issue_refs: Sequence[str], branch_refs: Sequence[str], path_refs: Sequence[str], referenced_issues: set[str], referenced_prs: set[str], invalid_refs: set[str]) -> None`, the loop moved from `collector.py` lines 194-221 with identical routing (feature refs first, then branch plus path refs; `issue` -> issues, `pull` -> PRs, anything else -> invalid; gh unavailable -> every ref into issues) and the D8 defensive `#` prefix kept; and `select_pending_primary(*, gh: PendingIssueClient, gh_available: bool, pending_primary: Sequence[str]) -> PendingPrimarySelection`: when gh is unavailable, keep every ref, `excluded=False`, empty details; otherwise, for each ref in order, exclude it when `classify_entity(normalize_reference(ref)) != "issue"` (no `issue_details` call), else call `gh.issue_details(normalize_reference(ref))` once (so the recorded argument is the bare number, for example `"622"`, as C6 expects), store the result in `fetched_details[ref]`, and keep the ref only when `details.state.lower() == "open"`; `excluded` is true when any ref was excluded. Every function and class has a Google-style docstring (purpose, args, returns, side effects: `classify_references` mutates the three sets); every loop and branch has an intent or decision comment. Acceptance: the file is at most 200 lines; it imports only from the standard library and `.models`.
- [ ] [P3-T7] Edit `collect_and_write` in `scripts/dev_tools/pr_context/collector.py` (located by name; the line numbers below are this tree's and can shift after the [P0-T3] merge, and in M588 #588's call-site edit shifts them). (a) Replace the `if gh_available: ... else: ...` classification block (currently lines 194-221) with one `classify_references(...)` call. (b) Delete the block `if referenced_issues: author_asserted = sorted(set(author_asserted + referenced_issues)); author_reason = "Detected issue references (classified)"` (currently lines 239-241); `author_asserted` stays `[]` and `author_reason` stays `"None (author has not asserted autoclose issues)"`. (c) After the existing PASS-readiness pending loop (currently lines 245-252) and `readiness_signals` computation, call `select_pending_primary(gh=gh, gh_available=gh_available, pending_primary=pending_primary)` and pass `pending_primary=selection.kept`, `gh_available=gh_available`, and `pending_primary_excluded=selection.excluded` to `build_issues_to_autoclose_section` (the call is at line 260 on this tree). The `gh_available` argument is the collector-local boolean set at lines 144/149: in N588 this task adds the argument; in M588 #588's existing argument is preserved, not duplicated, when the section build moves after pending verification. (d) Replace the issue fetch (currently lines 266-270) with `issues_to_fetch = sorted(set(verified + referenced_issues + selection.kept))` and, when gh is available, append `selection.fetched_details[ref]` when present, otherwise `gh.issue_details(ref.lstrip("#"))`, with an intent comment stating that each number is fetched at most once per run. Import `classify_references` and `select_pending_primary` from `.autoclose`. Acceptance: `grep -c -F -e "Detected issue references (classified)" scripts/dev_tools/pr_context/collector.py` prints `0`; `grep -c -F -e '"extract_issue_references",' scripts/dev_tools/pr_context/collector.py` prints `1`; the file's line count is at most the [P0-T12] count for `collector.py`.
- [ ] [P3-T8] Quick Python check. Run `poetry run pyright scripts/dev_tools/pr_context` and `poetry run ruff check scripts/dev_tools/pr_context`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/phase3-python-check.2026-09-25T23-29.md` with the four fields per command. Then, from the repository root, run the early scan of the Python directory Phase 1 and Phase 3 wrote, with the same fixed-string mode, the same three patterns, and the same Python pathspec as [P10-T1] and [P10-T4]: `git grep --untracked -n -F -e "[A-Z][A-Z0-9]+-" -e "ABC-123" -e "Detected issue references (classified)" -- "scripts/dev_tools/pr_context/*.py"`. Record the scan's command, exit code, and output (or `(no output)`) as text inside the same artifact's `Output Summary:`, not as an additional `EXIT_CODE:` line. Acceptance: both exit 0; Pyright prints `0 errors, 0 warnings, 0 informations`; Ruff prints `All checks passed!`; the early scan exits 1 with no output. On failure, including a scan hit in a file Phase 1 or Phase 3 wrote, fix the Phase 3 files (or the Phase 1 comment in `models.py`) and re-run; a hit in a file this plan does not write follows the stop-and-report rule in "Constraints and assumptions".
- [ ] [P3-T9] Commit and push Phase 3. Paths: `scripts/dev_tools/pr_context/feature_docs.py`, `scripts/dev_tools/pr_context/render_feature_excerpts.py`, `scripts/dev_tools/pr_context/render_pr_helpers.py`, `scripts/dev_tools/pr_context/autoclose.py`, `scripts/dev_tools/pr_context/collector.py`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `fix(pr-context): stop publishing scraped refs as autoclose targets in Python (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- scripts/dev_tools/pr_context` prints nothing.

### Phase 4 — TypeScript Fix

- [ ] [P4-T1] Edit `extractIssueReferences` in `extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts` (currently lines 69-93): import `ISSUE_REFERENCE_PATTERN` from `./models` (extend the `import { splitLines } from "./models";` line); replace the regex literal with `new RegExp(ISSUE_REFERENCE_PATTERN, "gu")`; rewrite the TSDoc (including the line that currently reads `` `#\d+` / `[A-Z][A-Z0-9]+-\d+` matches. ``) to `Extract bare-number issue references (for example #123) in encounter order.` plus a sentence stating that only `#` followed by ASCII digits, not preceded or followed by a word character, is returned. Update the module TSDoc if it mentions JIRA tokens. Acceptance: `grep -c -F -e "ISSUE_REFERENCE_PATTERN" extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts` prints at least `2`; `bare-number` count at least `1`; file at most 328 lines.
- [ ] [P4-T2] Edit `extractIssueReferences` in `extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts` (currently lines 61-83) the same way (add `ISSUE_REFERENCE_PATTERN` to the existing `./models` import block). Acceptance: `ISSUE_REFERENCE_PATTERN` count at least `2`, `bare-number` count at least `1`, file at most 455 lines.
- [ ] [P4-T3] Create `extensions/drm-copilot/src/lib/pr-context/autoclose.ts` with a module TSDoc naming issue #622, D2, D3, D4, D5, D7, and D8, and exporting: `ClassifyReferencesOptions` (as `collector-core.ts` lines 368-378, with `gh: Pick<GhClient, "classifyEntity">` via `import { type GhClient } from "./gh-client-core"`); `classifyReferences` (moved verbatim from `collector-core.ts` lines 380-430) with private `classifyOne` and `formatRef` (lines 432-454; D8 prefix kept), unchanged except that `classifyOne`'s `gh` parameter is typed `Pick<GhClient, "classifyEntity">`; `PendingPrimarySelection` (`readonly kept: string[]; readonly excluded: boolean; readonly fetchedDetails: ReadonlyMap<string, IssueDetails>`); `selectPendingPrimary(options: { gh: Pick<GhClient, "classifyEntity" | "issueDetails">; ghAvailable: boolean; pendingPrimary: readonly string[] }): PendingPrimarySelection` with the [P3-T6] semantics (the fetch is `gh.issueDetails(normalizeReference(ref))`, with `normalizeReference` imported from `./models`; keep when `state.toLowerCase() === "open"`); `buildCloseCandidatesSection` moved from `render-pr-helpers.ts` (currently lines 305-340) with the D2 change (`authorAutoClose = [...new Set(authorAsserted)].sort(compareCodePoint)`, using the file-local private `compareCodePoint` described below, so no import is added for it; `referencedOnly` = `referenced` minus `verified` minus `authorAsserted`, de-duplicated, code-point sorted); and `buildIssuesToAutocloseSection` moved from `render-pr-helpers.ts` (located by name; on this tree lines 342-379), adding `pendingPrimaryExcluded?: boolean` (default `false`) and importing `AUTOCLOSE_UNVERIFIED_ANNOTATION` and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` from `./models`, with the branch-specific [P3-T4](c) body rules. N588: add the optional parameter `ghAvailable?: boolean` with the destructure default `ghAvailable = true` (spec D10) and read it only in the non-empty annotation branch; do not add the #588 literal or an empty-list branch keyed on `ghAvailable`. M588: move #588's existing `ghAvailable?: boolean`, its destructure default `ghAvailable = true`, and its empty-list unavailable branch unchanged; do not declare the parameter a second time. A private `compareCodePoint` is copied from `render-pr-helpers.ts` lines 451-460. `autoclose.ts` imports only from `./models` and (type-only) `./gh-client-core`, so no import cycle is created. Every export has TSDoc; the builder TSDoc documents both optional parameters. Acceptance (both branches): the file is at most 340 lines; `grep -c -F -e "pendingPrimaryExcluded" extensions/drm-copilot/src/lib/pr-context/autoclose.ts` prints at least `3`; `grep -c -F -e "ghAvailable = true" extensions/drm-copilot/src/lib/pr-context/autoclose.ts` prints `1`; `grep -c -F -e "ghAvailable?: boolean" extensions/drm-copilot/src/lib/pr-context/autoclose.ts` prints `1`. Acceptance (branch-specific): `grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" extensions/drm-copilot/src/lib/pr-context/autoclose.ts extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts extensions/drm-copilot/src/lib/pr-context/models.ts` prints `0` for every file in N588, and in M588 prints at least `1` for at least one of the three files (the literal travels with #588's builder body, or stays wherever #588 defined it).
- [ ] [P4-T4] Edit `extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts`: apply the [P4-T1] change to `extractIssueReferences` (currently lines 154-176, TSDoc first line `Extract bare-number issue references (for example #123) in encounter order.`); delete `buildCloseCandidatesSection` and `buildIssuesToAutocloseSection` and add `export { buildCloseCandidatesSection, buildIssuesToAutocloseSection } from "./autoclose";` with a comment stating that the builders moved under D7 and the re-export keeps every existing import path valid; remove imports that become unused; update the module TSDoc accordingly. Acceptance: the file is at most 440 lines; `grep -c -F -e "ABC-123" extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts` prints `0`; `grep -c -F -e 'from "./autoclose"' extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts` prints `1`.
- [ ] [P4-T5] Edit `collectPrContext` in `extensions/drm-copilot/src/lib/pr-context/collector-core.ts` with the [P3-T7] changes: keep the `classifyReferences({...})` call (currently lines 184-193) but import it from `./autoclose`; delete the local `ClassifyReferencesOptions`, `classifyReferences`, `classifyOne`, and `formatRef` (currently lines 368-454); delete the promotion block (currently lines 213-217) and make `authorAsserted` and `authorReason` `const`; after the pending loop and `readinessSignals`, call `selectPendingPrimary` and pass `pendingPrimary: selection.kept`, `ghAvailable`, and `pendingPrimaryExcluded: selection.excluded` to `buildIssuesToAutocloseSection` (imported from `./autoclose`; the call is at lines 238-242 on this tree). `ghAvailable` is the collector-local value set at lines 133/138: in N588 this task adds the property; in M588 #588's existing property is preserved, not duplicated; `issuesToFetch = sortedSet([...verified, ...referencedIssues, ...selection.kept])` with `selection.fetchedDetails.get(ref) ?? gh.issueDetails(...)`. Update the function TSDoc. Acceptance: the file is at most 420 lines; `grep -c -F -e "Detected issue references (classified)" extensions/drm-copilot/src/lib/pr-context/collector-core.ts` prints `0`; `grep -c -E -e "^function (classifyOne|formatRef|classifyReferences)" extensions/drm-copilot/src/lib/pr-context/collector-core.ts` prints `0`.
- [ ] [P4-T6] Edit `extensions/drm-copilot/jest.config.cjs`. First run `grep -c -F -e '"./src/lib/pr-context/render-pr-helpers.ts"' extensions/drm-copilot/jest.config.cjs` and record its output in the [P4-T7] artifact (on this tree it prints `0`, the expected N588 value; in M588 #588's entry makes it print `1`). Add `coverageThreshold` entries `{ lines: 85, branches: 75 }` for `"./src/lib/pr-context/autoclose.ts"`, `"./src/lib/pr-context/models.ts"`, `"./src/lib/pr-context/feature-docs-parsers.ts"`, and `"./src/lib/pr-context/render-feature-excerpts.ts"`, and also for `"./src/lib/pr-context/render-pr-helpers.ts"` only when the recorded count is `0` (this plan changes that file in both branches, so it must be gated; when the count is `1` the existing entry is kept and not duplicated), preceded by a comment naming issue #622 and noting that the map has no `global` key; do not add or duplicate an entry for `collector-core.ts` (present on this tree at lines 33-36). Acceptance: for each of the six keys `./src/lib/pr-context/autoclose.ts`, `./src/lib/pr-context/models.ts`, `./src/lib/pr-context/feature-docs-parsers.ts`, `./src/lib/pr-context/render-feature-excerpts.ts`, `./src/lib/pr-context/collector-core.ts`, `./src/lib/pr-context/render-pr-helpers.ts`, `grep -c -F -e '"<key>"' extensions/drm-copilot/jest.config.cjs` (with the key substituted) prints `1`; the file is at most 330 lines.
- [ ] [P4-T7] Quick TS check. Run `npm run typecheck` and `npm run lint` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/phase4-typescript-check.2026-09-25T23-29.md` with the four fields per command, and record in `Output Summary:` the pre-edit `render-pr-helpers.ts` key count from [P4-T6] and whether [P4-T6] added that entry. Then, from the repository root (not from `extensions/drm-copilot`, because `git grep` resolves pathspecs against the working directory), run the early scan of the TypeScript directory Phase 1 and Phase 4 wrote, with the same fixed-string mode, the same three patterns, and the same TypeScript pathspec as [P10-T1] and [P10-T4]: `git grep --untracked -n -F -e "[A-Z][A-Z0-9]+-" -e "ABC-123" -e "Detected issue references (classified)" -- "extensions/drm-copilot/src/lib/pr-context/*.ts"`. Record the scan's command, exit code, and output (or `(no output)`) as text inside the same artifact's `Output Summary:`, not as an additional `EXIT_CODE:` line. Acceptance: both commands exit 0 with zero `error TS` lines and zero ESLint problem lines; the early scan exits 1 with no output. On failure, including a scan hit in a file Phase 1 or Phase 4 wrote, fix the Phase 4 files (or the Phase 1 TSDoc in `models.ts`) and re-run; a hit in a file this plan does not write follows the stop-and-report rule in "Constraints and assumptions".
- [ ] [P4-T8] Commit and push Phase 4. Paths: `extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts`, `extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts`, `extensions/drm-copilot/src/lib/pr-context/autoclose.ts`, `extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts`, `extensions/drm-copilot/src/lib/pr-context/collector-core.ts`, `extensions/drm-copilot/jest.config.cjs`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `fix(pr-context): stop publishing scraped refs as autoclose targets in TypeScript (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- extensions/drm-copilot/src extensions/drm-copilot/jest.config.cjs` prints nothing.

### Phase 5 — Pass-After Verification

- [ ] [P5-T1] Re-run `poetry run pytest tests/scripts/dev_tools/pr_context/test_issue_reference_pattern.py tests/scripts/dev_tools/pr_context/test_autoclose_collector.py -v`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-pass-after.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0`; result line `71 passed`; every node ID that failed in [P2-T9] is listed as `PASSED`.
- [ ] [P5-T2] Re-run `node run-jest.cjs test/lib/pr-context/issue-reference-pattern.test.ts test/lib/pr-context/collector-core-autoclose.test.ts --verbose` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/ts-pass-after.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0`; the `Tests:` line contains `67 passed, 67 total`; T1-T6 appear as passed.
- [ ] [P5-T3] Commit and push Phase 5 evidence. Paths: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `docs(bug): record pass-after evidence for #622`. Acceptance: commit and push exit 0.

### Phase 6 — Python Unit Tests and Existing-Test Updates

- [ ] [P6-T1] Python batch-budget reset. Apply the reset procedure and append the record. Acceptance: the reset record exists.
- [ ] [P6-T2] Create `tests/scripts/dev_tools/pr_context/test_autoclose.py` with the 12 tests listed under "Named tests", using an in-file fake implementing `PendingIssueClient` (classification map, state map, recorded `issue_details` calls). `test_select_pending_primary_fetches_each_issue_at_most_once` uses pending `["#5", "#6"]` with `#5` an open issue and `#6` classified `pull`, and asserts that the recorded `issue_details` calls equal `["5"]` and that `fetched_details` has exactly the key `#5`. `test_select_pending_primary_reports_mixed_outcomes` uses pending `["#5", "#6", "#7"]` (open issue, closed issue, pull) and asserts `kept == ["#5"]` and `excluded is True`. `test_classify_references_prefixes_unprefixed_refs` passes a ref without `#` and asserts the `#`-prefixed form lands in the target set. Acceptance: the file is at most 260 lines, every test has a docstring and Arrange-Act-Assert structure.
- [ ] [P6-T3] Create `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` (new #622-owned file, identical in both branches; spec D11) with a module docstring naming issue #622 and D4, D5, D10-D12, and the four builder tests listed under "Named tests". Import `build_issues_to_autoclose_section` from `scripts.dev_tools.pr_context.render_pr_helpers` and `section`, `AUTOCLOSE_UNVERIFIED_ANNOTATION`, and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` from `scripts.dev_tools.pr_context.models`. Every call passes `verified`, `pending_primary`, `readiness_signals`, `gh_available`, and `pending_primary_excluded` explicitly by keyword. The annotation test (`pending_primary=["#7"]`, `readiness_signals=["PASS"]`, `gh_available=False`) asserts that the line after `- #7` equals `Unverified: the issues listed above come from feature metadata only and were not checked against GitHub (GitHub CLI unavailable).` and does not start with `- `. The omits-annotation test (same inputs with `gh_available=True`) asserts exact equality with `"\n".join([section("Issues to autoclose (verified or pending)"), "- #7"])`. The not-open test (`pending_primary=[]`, `readiness_signals=["PASS"]`, `gh_available=True`, `pending_primary_excluded=True`) asserts exact equality with `"\n".join([section("Issues to autoclose (verified or pending)"), AUTOCLOSE_PENDING_NOT_OPEN_TEXT])`. The precedence test is parametrized over exactly the five table rows with those ids and asserts `result == "\n".join([section("Issues to autoclose (verified or pending)"), expected_body])`. No test in the file passes `gh_available=False` together with an empty ordered list. Acceptance: the file is at most 200 lines, LF-terminated; `grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` prints `0`; every test has a docstring and Arrange-Act-Assert structure.
- [ ] [P6-T4] Edit `tests/scripts/dev_tools/test_collect_pr_context.py`: in `test_extract_issue_references_filters_and_deduplicates` (currently lines 161-164) assert `refs == ["#12"]`; in `test_gather_feature_excerpts_reads_active_docs` keep the plan text at line 276 and change line 310 to `assert excerpt.issue_refs == ["#77"]`; replace `test_build_close_candidates_section_promotes_referenced_issues_to_auto_close` (currently lines 375-386) with `test_build_close_candidates_section_lists_referenced_issues_as_detected_only`, which calls the builder with `verified=[]`, `author_asserted=[]`, `referenced=["#3"]`, and asserts the line after `Auto-close issues (author asserted):` equals `None (author has not asserted autoclose issues)` and the line after `Referenced issues (detected):` equals `- #3`. Acceptance: the file is at most 480 lines; `grep -c -F -e "test_build_close_candidates_section_lists_referenced_issues_as_detected_only" tests/scripts/dev_tools/test_collect_pr_context.py` prints `1` and `grep -c -F -e "test_build_close_candidates_section_promotes_referenced_issues_to_auto_close" tests/scripts/dev_tools/test_collect_pr_context.py` prints `0`.
- [ ] [P6-T5] Python batch-budget reset. Apply the reset procedure and append the record. Acceptance: the reset record exists.
- [ ] [P6-T6] Edit `tests/scripts/dev_tools/test_render.py`: rename `test_extract_issue_references_finds_jira` (currently lines 162-167) to `test_extract_issue_references_ignores_jira` with docstring `extract_issue_references ignores JIRA-style tokens.` and assertion `result == []`; in `test_build_close_candidates_section_combines_author_and_ref` (currently lines 418-430) replace the docstring with `build_close_candidates_section keeps referenced issues out of author auto-close.` and the comment with one stating that author auto-close lists `author_asserted` only, then assert the line after `Auto-close issues (author asserted):` equals `- #1` and the line after `Referenced issues (detected):` equals `- #2`; delete class `TestResolveFeatureDir` (currently lines 433-570) and the blank lines before it, and remove imports that become unused. Acceptance: the file is at most 435 lines; `grep -c -F -e "class TestResolveFeatureDir" tests/scripts/dev_tools/test_render.py` prints `0`.
- [ ] [P6-T7] Create `tests/scripts/dev_tools/test_render_resolve_feature_dir.py` with a module docstring naming issue #622 and the 500-line cap as the reason for the split, the imports class `TestResolveFeatureDir` needs, and the class copied verbatim from the pre-[P6-T6] `test_render.py` lines 433-570. Acceptance: the file is at most 160 lines; `grep -c -F -e "class TestResolveFeatureDir" tests/scripts/dev_tools/test_render_resolve_feature_dir.py` prints `1`.
- [ ] [P6-T8] Edit `tests/scripts/dev_tools/test_feature_docs.py`: rename `test_extract_issue_references_jira` (currently lines 75-78) to `test_extract_issue_references_ignores_jira` asserting `result == []`; in `test_extract_issue_references_mixed` (lines 80-83) assert `result == ["#42"]`; in `test_gather_feature_excerpts_extracts_issue_refs` (lines 176-193) replace the final assertion with `assert "ABC-456" not in excerpts[0].issue_refs`. Acceptance: the file is at most 436 lines.
- [ ] [P6-T9] Python batch-budget reset. Apply the reset procedure and append the record. Acceptance: the reset record exists.
- [ ] [P6-T10] Edit `tests/scripts/dev_tools/test_collect_pr_context_part2.py` line 245: change `{"1", "ABC-10"}` to `{"1"}`. No other change. Acceptance: `grep -c -F -e "ABC-10" tests/scripts/dev_tools/test_collect_pr_context_part2.py` prints `1` (the branch-name stub at line 217 remains) and the file is at most 432 lines.
- [ ] [P6-T11] #588's Python H3 (spec D11, `## Composition with #588`). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/h3-update-python.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (one per command, each 0), a `Branch:` line, and `Output Summary:`. N588: run `git ls-files --cached --others -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`; edit no file. Acceptance (N588): the command exits 0 and prints nothing, and the artifact states `N588: no #588 test file exists; #622 edits no #588 test`. M588: run `grep -n -F -e "GitHub CLI unavailable" tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` and `grep -n -F -e "def test_build_issues_to_autoclose_section_lists_pending_refs_when_gh_unavailable" tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`. The second command searches for the literal `def test_build_issues_to_autoclose_section_lists_pending_refs_when_gh_unavailable`, which exists only on an M588 base. Update-set rule (fixed; the executor does not choose): when the second command prints exactly one line, the update set is that one test; when it prints nothing, the update set is every test block (block boundaries as defined in [P10-T11]) that contains a line printed by the first command and whose text contains both `gh_available=False` and a `pending_primary=[` value other than `pending_primary=[]`; an empty update set is a breach of the spec's coordination contract and stops execution with a report. For each test in the update set, replace each assertion statement that contains a line printed by the first command with three assertions on the test's own result variable: the bullet line for the test's pending ref (`- #7` in the spec) is present; the line after that bullet equals `AUTOCLOSE_UNVERIFIED_ANNOTATION`; and `"None (GitHub CLI unavailable; closing issues not verified)"` is not in the result. For a test in the update set that contains no line printed by the first command, append the three assertions as the last statements of that test. Add `AUTOCLOSE_UNVERIFIED_ANNOTATION` to the file's existing `from scripts.dev_tools.pr_context.models import ...` statement, or add that import statement when none exists. Keep the test names and inputs; change no other test and no other line. Acceptance (M588): both commands exit 0 or the second exits 1 with the fallback set recorded; the artifact records both outputs, the update set, and each updated test's line span before and after the edit; `grep -c -F -e "AUTOCLOSE_UNVERIFIED_ANNOTATION" tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` prints at least `2`; the file is at most its [P0-T12] count plus 10 lines. Record the second command's exit code in `Output Summary:` rather than as an `EXIT_CODE:` line when it exits 1.
- [ ] [P6-T12] Verify the `test_render.py` relocation preserved every test. Run `poetry run pytest --collect-only -q tests/scripts/dev_tools/test_render.py tests/scripts/dev_tools/test_render_resolve_feature_dir.py`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/relocation-counts-p6.2026-09-25T23-29.md` with the four fields. Acceptance: exit 0 and the collected count equals `N_render` from [P0-T13].
- [ ] [P6-T13] Run the Python unit and updated tests: `poetry run pytest tests/scripts/dev_tools/pr_context tests/scripts/dev_tools/test_collect_pr_context.py tests/scripts/dev_tools/test_collect_pr_context_part2.py tests/scripts/dev_tools/test_collect_pr_context_part4.py tests/scripts/dev_tools/test_collect_pr_context_part5.py tests/scripts/dev_tools/test_render.py tests/scripts/dev_tools/test_render_resolve_feature_dir.py tests/scripts/dev_tools/test_feature_docs.py tests/scripts/dev_tools/test_pr_context_integration.py -v` (in M588 the `tests/scripts/dev_tools/pr_context` directory argument also collects #588's `test_render_pr_helpers.py`). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-unit-tests.2026-09-25T23-29.md` with the four fields and a `Branch:` line. Acceptance: `EXIT_CODE: 0`; the result line contains no `failed` count; each test named under "Named tests" for Python outside the M588-only entry appears as `PASSED`, including all five `test_build_issues_to_autoclose_section_fallback_precedence` cases in `test_autoclose_builder.py`; in M588 additionally every test node in `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`, including each test in the [P6-T11] update set, appears as `PASSED`. In M588, a failure in a #588 test outside the [P6-T11] update set conflicts with spec AC 19 ("#588's other builder tests pass unmodified"); stop and report it to the orchestrator rather than editing that test.
- [ ] [P6-T14] Commit and push Phase 6. Paths: `tests/scripts/dev_tools/pr_context/test_autoclose.py`, `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py`, `tests/scripts/dev_tools/test_collect_pr_context.py`, `tests/scripts/dev_tools/test_render.py`, `tests/scripts/dev_tools/test_render_resolve_feature_dir.py`, `tests/scripts/dev_tools/test_feature_docs.py`, `tests/scripts/dev_tools/test_collect_pr_context_part2.py`, in M588 only also `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `test(pr-context): cover autoclose selection and invert JIRA extraction tests (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- tests` prints nothing.

### Phase 7 — TypeScript Unit Tests and Existing-Test Updates

- [ ] [P7-T1] Create `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts` (new #622-owned file, identical in both branches) with the 12 selection and classification tests listed under "Named tests", using an in-file fake object satisfying `Pick<GhClient, "classifyEntity" | "issueDetails">` with the same scenarios as [P6-T2], plus the four builder tests listed under "Named tests" in `describe("buildIssuesToAutocloseSection")`, importing `buildIssuesToAutocloseSection` from `../../../src/lib/pr-context/autoclose` and `section`, `AUTOCLOSE_UNVERIFIED_ANNOTATION`, and `AUTOCLOSE_PENDING_NOT_OPEN_TEXT` from `../../../src/lib/pr-context/models`, with the [P6-T3] inputs and assertions (every call passes `ghAvailable` and `pendingPrimaryExcluded` explicitly; the precedence `it.each` covers exactly the five table rows; no test passes `ghAvailable: false` with an empty ordered list). Acceptance: the file is at most 460 lines, LF-terminated, and uses no `origin/` string; `grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts` prints `0`.
- [ ] [P7-T2] Edit `extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` (both branches): replace `merges author-asserted and referenced into author auto-close` (currently lines 193-203) with `keeps referenced issues out of author auto-close` (same inputs; asserts the line after `Auto-close issues (author asserted):` is `- #1` and the line after `Referenced issues (detected):` is `- #2`). No other change in this task; in particular the existing `describe("buildIssuesToAutocloseSection")` tests (lines 206-241 on this tree) are unchanged. Acceptance: `grep -c -F -e "keeps referenced issues out of author auto-close" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` prints `1`; `grep -c -F -e "merges author-asserted and referenced into author auto-close" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` prints `0`; the file is at most 345 lines.
- [ ] [P7-T3] #588's TypeScript H3 (spec D11, `## Composition with #588`). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/h3-update-typescript.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, a `Branch:` line, and `Output Summary:`. N588: run `grep -n -F -e "GitHub CLI unavailable" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts`; edit no file. Acceptance (N588): the command exits 1 with no output (the artifact carries `ExpectedExitCode: 1`), and the artifact states `N588: #588's H1-H4 are absent; #622 edits no #588 test`. M588: run `grep -n -F -e "GitHub CLI unavailable" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` and `grep -n -F -e "lists pending refs unchanged when gh is unavailable" -e "lists pending refs and omits the empty-list unavailable body when gh is unavailable" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` (the two `-e` patterns cover either title #588 may use for its H3). Update-set rule (fixed): when the second command prints one or more lines, the update set is the `it(` block that starts at each printed line; when it prints nothing, the update set is every `it(` / `it.each(` block (block boundaries as defined in [P10-T11]) that contains a line printed by the first command and whose text contains both `ghAvailable: false` and a `pendingPrimary: [` value other than `pendingPrimary: []`; an empty update set stops execution with a report. For each block in the set, replace each `expect(...)` statement that contains a line printed by the first command with three expectations on the block's own result variable: the bullet line for its pending ref is present; the line after that bullet equals `AUTOCLOSE_UNVERIFIED_ANNOTATION`; and the result does not contain `None (GitHub CLI unavailable; closing issues not verified)`. For a test in the update set that contains no line printed by the first command, append the three expectations as the last statements of that test. Import `AUTOCLOSE_UNVERIFIED_ANNOTATION` from `../../../src/lib/pr-context/models`. Keep titles and inputs; change no other test. Acceptance (M588): the artifact records both command outputs (the second command's exit code in `Output Summary:` when it exits 1), the update set, and each updated block's line span before and after; `grep -c -F -e "AUTOCLOSE_UNVERIFIED_ANNOTATION" extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` prints at least `2`; the file is at most 345 lines; the M588 artifact carries no `ExpectedExitCode:` line.
- [ ] [P7-T4] Edit `extensions/drm-copilot/test/lib/pr-context/feature-docs.test.ts` (both branches): rename `extracts JIRA-style references` (currently lines 83-88) to `ignores JIRA-style references` expecting `[]`; in `extracts issue references from the combined doc text` (lines 221-233) change line 232 to `expect(excerpts[0]!.issueRefs).not.toContain("ABC-456");`. Acceptance: the file is at most 316 lines.
- [ ] [P7-T5] Edit `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`: in `ghHandler`, replace the line `return okResult(JSON.stringify({ number: Number(number) }));` (currently line 106, 8-space indent) with the three-line form Prettier produces at its default 80-column width (the file has no Prettier config; the one-line form would be 83 characters): `return okResult(` / `JSON.stringify({ number: Number(number), state: "open" }),` (indented two further spaces, trailing comma) / `);`. Update the comment above it (currently line 102) to say that numeric refs classify as open issues so D3 keeps the PASS-readiness primary, keeping the comment on one line. No other change. The line is located inside `function ghHandler(`, not by its number. Before the edit, run `awk '/^function ghHandler\(/{f=1} /^function gitHandler\(/{f=0} f && index($0, "return okResult(JSON.stringify({ number: Number(number) }));") {print NR}' extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` and `grep -c -F -e "return okResult(JSON.stringify({ number: Number(number) }));" extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, and record both outputs in the [P7-T6] artifact. On this tree they print `106` and `2`. If the awk command does not print exactly one line number, stop and report. Edit only that line. The identical line inside `richGh` (line 283 on this tree) is not edited. Acceptance: the line count equals the [P0-T12] count for this file plus 2; `grep -c -F -e 'JSON.stringify({ number: Number(number), state: "open" }),' extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` prints `1`; `grep -c -F -e "return okResult(JSON.stringify({ number: Number(number) }));" extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, re-run after the edit, prints the pre-edit count minus 1 (`1` on this tree).
- [ ] [P7-T6] Run the TS unit and updated tests: `node run-jest.cjs test/lib/pr-context/autoclose.test.ts test/lib/pr-context/render-pr-helpers.test.ts test/lib/pr-context/feature-docs.test.ts test/lib/pr-context/collector-core.test.ts test/lib/pr-context/collector-core-autoclose.test.ts test/lib/pr-context/issue-reference-pattern.test.ts test/lib/pr-context/collector-integration.test.ts test/lib/pr-context/render.test.ts test/lib/pr-context/render-feature-excerpts.test.ts --verbose` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/ts-unit-tests.2026-09-25T23-29.md` with the four fields and a `Branch:` line; `Output Summary:` also quotes the two [P7-T5] pre-edit commands (the `awk` locator and the `grep -c`) with their outputs, as text and not as additional `EXIT_CODE:` lines. Acceptance: `EXIT_CODE: 0`; the `Tests:` line contains no `failed` count; every TS title under "Named tests" outside the M588-only entry appears as passed, including all five `applies the composed fallback precedence` cases in `autoclose.test.ts`; in M588 additionally every test in `render-pr-helpers.test.ts` and `collector-core.test.ts` (which include #588's tests) passes, including each block in the [P7-T3] update set. In M588, a failure in a #588 test outside the [P7-T3] update set conflicts with spec AC 19; stop and report it rather than editing that test.
- [ ] [P7-T7] Commit and push Phase 7. Paths: `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts`, `extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts`, `extensions/drm-copilot/test/lib/pr-context/feature-docs.test.ts`, `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `test(pr-context): cover TypeScript autoclose selection and builders (#622)`. Acceptance: commit and push exit 0; `git status --porcelain -- extensions/drm-copilot/test` prints nothing.

### Phase 8 — Python Final QA Loop

Loop rule: run [P8-T1] through [P8-T4] in order. If any step fails or changes any file, fix the cause and restart at [P8-T1], incrementing `Loop iteration:` in every Phase 8 artifact. The loop is complete only when one iteration passes all four steps with no file changed. Any file changed by a fix is committed by [P9-T7] and must be a file in the file-size budget table or a mirrored test file added under the [P8-T5]/[P9-T5] coverage rule.

- [ ] [P8-T1] Python formatting gate. Run `poetry run black --check .`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/py-black.2026-09-25T23-29.md` with the four fields and `Loop iteration:`. Acceptance: `EXIT_CODE: 0` and the output contains `would be left unchanged` with no `would reformat` line. On failure, run `poetry run black .`, record the `reformatted` lines it prints (a clean write run prints only `left unchanged`), and restart the loop.
- [ ] [P8-T2] Python lint gate. Run `poetry run ruff check .`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/py-ruff.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0` and the output is `All checks passed!`. Any suppression must match `.claude/rules/python-suppressions.md`.
- [ ] [P8-T3] Python type-check gate. Run `poetry run pyright`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/py-pyright.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0` and the summary line `0 errors, 0 warnings, 0 informations`.
- [ ] [P8-T4] Python test and coverage gate. Run `poetry run pytest --cov=scripts.dev_tools.pr_context --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-final.json --deselect tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` (full suite, same selection as [P0-T18]). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/py-pytest-coverage.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0`; the result line contains no `failed` count; the result line reports `1 deselected`; `Output Summary:` cites #510 for the deselection and records the result line, the `TOTAL` row, and the term-missing rows for `models.py`, `feature_docs.py`, `render_feature_excerpts.py`, `render_pr_helpers.py`, `autoclose.py`, and `collector.py`; the JSON file exists.
- [ ] [P8-T5] Python per-file coverage. Read `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-final.json`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-final.2026-09-25T23-29.md` with `Timestamp:`, `Command:` (the [P8-T4] command), `EXIT_CODE:`, and `Output Summary:` recording, for each of the six files in [P8-T4], raw `covered_lines`/`num_statements` and `covered_branches`/`num_branches` from `files["<path key>"]["summary"]` and the two percentages to two decimals. Acceptance: every file is at least 85.00% line and 75.00% branch. If any file is below a floor, add tests for its `files["<path key>"]["missing_lines"]` / `["missing_branches"]` to the mirrored test file (subject to the batch budget) and restart the Phase 8 loop. For `render_pr_helpers.py` the target is `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` in both branches; `tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` is never created in N588 and receives no coverage additions in M588.
- [ ] [P8-T6] Python coverage comparison and changed-line coverage. Run `git diff -U0 <scope-anchor-sha> -- scripts/dev_tools/pr_context/models.py scripts/dev_tools/pr_context/feature_docs.py scripts/dev_tools/pr_context/render_feature_excerpts.py scripts/dev_tools/pr_context/render_pr_helpers.py scripts/dev_tools/pr_context/collector.py`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-delta.2026-09-25T23-29.md` with the four fields; `Output Summary:` records baseline ([P0-T19]) versus final ([P8-T5]) line and branch percentages per pre-existing file, `autoclose.py` as the new-code figure, and, for every added line number on the `+` side of each hunk, whether it appears in the file-level list `files["<path key>"]["missing_lines"]` of the final JSON (not `summary.missing_lines`, which is a count). Acceptance: no per-file percentage decreases for files that received no relocated-out code. For `collector.py` (its classification loop moves to `autoclose.py`), the artifact records baseline and final values and names the relocation, and the file must be at least 85.00% line and 75.00% branch. No added executable line is in `missing_lines`; `autoclose.py` is at least 85.00% line and 75.00% branch.

### Phase 9 — TypeScript Final QA Loop

Loop rule: run [P9-T1] through [P9-T5] in order. If any step fails or changes any file, fix the cause and restart at [P9-T1], incrementing `Loop iteration:` in every Phase 9 artifact. The loop is complete only when one iteration passes all five steps with no file changed.

- [ ] [P9-T1] TS formatting gate. Run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ts-prettier.2026-09-25T23-29.md` with the four fields and `Loop iteration:`. Acceptance: `EXIT_CODE: 0` and the output contains `All matched files use Prettier code style!`. On failure, run `npm run format` from `extensions/drm-copilot`, record the output lines that do not end in `(unchanged)`, and restart the loop.
- [ ] [P9-T2] TS lint gate. Run `npm run lint` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ts-eslint.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0` and zero problem lines. Any suppression must match `.claude/rules/typescript-suppressions.md`.
- [ ] [P9-T3] TS type-check gate. Run `npm run typecheck` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ts-tsc.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0` and zero `error TS` lines.
- [ ] [P9-T4] TS architecture-boundary stage. Run `git ls-files -- "*.dependency-cruiser*"` from the repository root. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ts-dependency-cruiser.2026-09-25T23-29.md` with the four fields. Acceptance: as [P0-T23].
- [ ] [P9-T5] TS test and coverage gate. Run `node run-jest.cjs --coverage --coverageReporters=text --coverageReporters=text-summary --coverageReporters=lcov` from `extensions/drm-copilot`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ts-jest-coverage.2026-09-25T23-29.md` with the four fields, and write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/typescript-coverage-final.2026-09-25T23-29.md` with the same four fields. Acceptance: `EXIT_CODE: 0` (this also proves every per-file `coverageThreshold` entry, including the entries added by [P4-T6] (five in N588, four in M588), is met); both `Output Summary:` fields record the `Test Suites:` and `Tests:` lines, neither containing a `failed` count, the text-summary `Lines` and `Branches` totals, and the `text` rows for `autoclose.ts`, `models.ts`, `feature-docs-parsers.ts`, `render-feature-excerpts.ts`, `render-pr-helpers.ts`, and `collector-core.ts`, each showing `% Lines` >= 85 and `% Branch` >= 75. If the run fails only on a per-file threshold, add tests to that file's mirrored test file for the lines in its `Uncovered Line #s` cell and restart the loop.
- [ ] [P9-T6] TS coverage comparison and changed-line coverage. Run `git diff -U0 <scope-anchor-sha> -- extensions/drm-copilot/src/lib/pr-context/models.ts extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts extensions/drm-copilot/src/lib/pr-context/collector-core.ts`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/typescript-coverage-delta.2026-09-25T23-29.md` with the four fields; `Output Summary:` records baseline ([P0-T24]) versus final ([P9-T5]) totals and per-file rows for the five pre-existing files, the `autoclose.ts` row as the new-code figure, and, for every added line number on the `+` side of the hunks, its `DA:<line>,<hits>` entry under the matching `SF:` record of `extensions/drm-copilot/coverage/lcov.info`. Acceptance: the text-summary `Lines` and `Branches` totals do not decrease; no per-file percentage decreases for files that received no relocated-out code. For `collector-core.ts` (its `classifyReferences`, `classifyOne`, and `formatRef` move to `autoclose.ts`) and `render-pr-helpers.ts` (its two builders move to `autoclose.ts`), the artifact records baseline and final values and names the relocation, and each file must be at least 85 `% Lines` and 75 `% Branch`. Every added line that has a `DA` entry has hits > 0; `autoclose.ts` is at least 85 `% Lines` and 75 `% Branch`.
- [ ] [P9-T7] Commit and push the Phase 8 and Phase 9 evidence and any loop fixes. Paths: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`, plus every source or test path changed by a Phase 8 or Phase 9 fix (named individually in the commit command and in the commit-log line). Subject: `docs(bug): record final QA evidence for #622`. Acceptance: commit and push exit 0; `git status --porcelain` prints nothing; `git ls-files -- docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates` lists `python-coverage-final.json`, `python-coverage-final.2026-09-25T23-29.md`, `python-coverage-delta.2026-09-25T23-29.md`, `typescript-coverage-final.2026-09-25T23-29.md`, and `typescript-coverage-delta.2026-09-25T23-29.md`.

### Phase 10 — Scope, Size, Hermeticity, and Documentation Verification

- [ ] [P10-T1] AC 1 check. Run `git grep --untracked -n -F -e "[A-Z][A-Z0-9]+-" -- "scripts/dev_tools/pr_context/*.py" "extensions/drm-copilot/src/lib/pr-context/*.ts"`, then `grep -c -F -e 're.compile(r"(?<!\w)#\d+(?!\w)", re.ASCII)' scripts/dev_tools/pr_context/models.py` and `grep -c -F -e "export const ISSUE_REFERENCE_PATTERN = /(?<!\w)#\d+(?!\w)/u;" extensions/drm-copilot/src/lib/pr-context/models.ts`. Write the `git grep` command to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac01-pattern-absent.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and `Output Summary:`, and write the two `grep -c` commands to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac01-pattern-defined.2026-09-25T23-29.md` with the four fields per command and no `ExpectedExitCode:`. Acceptance: the `git grep` exits 1 with no output; the two counts print `1`.
- [ ] [P10-T2] AC 2 check. For each of `scripts/dev_tools/pr_context/feature_docs.py`, `scripts/dev_tools/pr_context/render_pr_helpers.py`, `scripts/dev_tools/pr_context/render_feature_excerpts.py`, `extensions/drm-copilot/src/lib/pr-context/feature-docs-parsers.ts`, `extensions/drm-copilot/src/lib/pr-context/render-pr-helpers.ts`, and `extensions/drm-copilot/src/lib/pr-context/render-feature-excerpts.ts`, run `grep -c -F -e "ISSUE_REFERENCE_PATTERN" <file>` (six commands with the path substituted); then run `grep -c -F -e '"extract_issue_references",' scripts/dev_tools/pr_context/collector.py` and `grep -c -F -e "extractIssueReferences," extensions/drm-copilot/src/lib/pr-context/index.ts`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac02-extractors.2026-09-25T23-29.md` with the four fields. Acceptance: each of the six counts is at least `2`; the two export counts each print `1`; the extractor names and signatures are exercised by the passing [P6-T13] and [P7-T6] runs.
- [ ] [P10-T3] AC 7 check. Run `git grep -n -F -e '"ABC-99"]' -e '"ABC-123"]' -e '"PROJECT-100"]' -e '"XYZ-456"]' -e '"XYZ-456",' -e '"ABC-456" in excerpts' -e 'assert "ABC-456" in result' -e 'assert "XYZ-789" in result' -e 'issueRefs).toContain("ABC-456")' -e '"ABC-10"}' -- tests/scripts/dev_tools extensions/drm-copilot/test/lib/pr-context`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac07-jira-assertions.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`. Acceptance: `EXIT_CODE: 1` with no output (every former positive JIRA assertion is gone), and `Output Summary:` names the inverted tests passing in [P6-T13] and [P7-T6]. The command can fail: on the pre-change tree it prints exactly 10 lines (test_collect_pr_context.py 164 and 310, test_render.py 166 and 167, test_feature_docs.py 78, 83, and 193, test_collect_pr_context_part2.py 245, feature-docs.test.ts 86 and 232), verified in the planning session.
- [ ] [P10-T4] AC 8 and AC 27 checks. Run `git grep --untracked -n -F -e "Detected issue references (classified)" -- "scripts/dev_tools/pr_context/*.py" "extensions/drm-copilot/src/lib/pr-context/*.ts"` and `git grep --untracked -n -F -e "ABC-123" -- "scripts/dev_tools/pr_context/*.py" "extensions/drm-copilot/src/lib/pr-context/*.ts"`, then for each of the six extractor files listed in [P10-T2] run `grep -c -F -e "bare-number" <file>`, then `grep -c -F -e "pending_primary_excluded" scripts/dev_tools/pr_context/render_pr_helpers.py` and `grep -c -F -e "pendingPrimaryExcluded" extensions/drm-copilot/src/lib/pr-context/autoclose.ts`. Write the two `git grep` commands to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac08-ac27-absent.2026-09-25T23-29.md` with `Timestamp:`, `Command:` and `EXIT_CODE:` per command, one `ExpectedExitCode: 1`, and `Output Summary:`; write the eight `grep -c` commands to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac08-ac27-counts.2026-09-25T23-29.md` with the four fields per command and no `ExpectedExitCode:`. Acceptance: both `git grep` commands exit 1 with no output; each `bare-number` count is at least `1`; the two parameter counts are at least `3`.
- [ ] [P10-T5] AC 21 excluded-path check. Run `git diff --exit-code --name-only <scope-anchor-sha> -- scripts/dev_tools/pr_context/github.py "extensions/drm-copilot/src/lib/pr-context/gh-client-*.ts" extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts .claude/skills/pr-author/SKILL.md .agents/skills/pr-author/SKILL.md .github/agents/pr-author.agent.md .github/prompts/generate-pr.prompt.md extensions/drm-copilot/resources` and the companion `git status --porcelain -- scripts/dev_tools/pr_context/github.py "extensions/drm-copilot/src/lib/pr-context/gh-client-*.ts" extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts .claude/skills/pr-author/SKILL.md .agents/skills/pr-author/SKILL.md .github/agents/pr-author.agent.md .github/prompts/generate-pr.prompt.md extensions/drm-copilot/resources` (the same paths). In N588 also run `git ls-files --cached --others -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/executable-resolver.ts`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac21-excluded-paths.2026-09-25T23-29.md` with the four fields and a `Branch:` line. Acceptance: the diff exits 0 with no output and the porcelain command prints nothing; in N588 the `git ls-files` command exits 0 and prints nothing (neither `test_render_pr_helpers.py` nor either `executable-resolver.ts` path was created).
- [ ] [P10-T6] AC 21 full changed-file inventory. Run `git diff --name-only <scope-anchor-sha> HEAD` and the companion `git status --porcelain`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac21-changed-files.2026-09-25T23-29.md` with the four fields. Acceptance: every path in the porcelain output is under `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/` (Phase 10 artifacts are committed by [P10-T12]), and every path printed by the diff is either under `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/` or is one of the budget rows for `<branch>` in the file-size budget table (30 files in N588, 31 in M588); any other path fails this task unless it is a mirrored test file changed by the [P8-T5] or [P9-T5] coverage rule, in which case the artifact names it and the rule that added it.
- [ ] [P10-T7] AC 22 line caps. Run `wc -l` over every file in the budget rows for `<branch>` (30 paths in N588, 31 in M588, as listed in the file-size budget table) plus `extensions/drm-copilot/src/lib/pr-context/collector-output.ts` (named by AC 22; not modified). Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac22-line-caps.2026-09-25T23-29.md` with the four fields and a `Branch:` line. Acceptance: `EXIT_CODE: 0`, 31 file rows in N588 or 32 in M588, every count at most 500 and at most its "#622 ceiling" (for `collector.py`, at most the [P0-T12] count; for `collector-core.test.ts`, at most the [P0-T12] count plus 2; in M588 for `test_render_pr_helpers.py`, at most the [P0-T12] count plus 10); plus a `wc -l` row for any file named under the [P10-T6] exception, also at most 500; `collector-output.ts` at most 500.
- [ ] [P10-T8] AC 23 hermeticity scan of added test lines. Run `git diff -U0 <scope-anchor-sha> HEAD -- tests extensions/drm-copilot/test | grep -n -E -e '^\+(.*[^A-Za-z])?[A-Za-z]:[\\/]' -e '^\+.*(tmp_path|tempfile|mkdtemp|os\.tmpdir|origin/|child_process|spawnSync|execSync)'` (the first pattern is the ERE form of the spec's drive-root lookbehind; `mkdtemp` also matches `mkdtempSync`). Because the pipeline also exits 1 on empty input, run the companion `git diff -U0 <scope-anchor-sha> HEAD -- tests extensions/drm-copilot/test | grep -c -E -e '^\+[^+]'` and `git status --porcelain -- tests extensions/drm-copilot/test`. Write the scan to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac23-hermeticity-scan.2026-09-25T23-29.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and `Output Summary:`, and write the count and porcelain companions to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac23-hermeticity-companions.2026-09-25T23-29.md` with the four fields per command and no `ExpectedExitCode:`. Acceptance: the scan exits 1 with no output; the companion count exits 0 and prints at least `1`; the porcelain output is empty.
- [ ] [P10-T9] AC 24 threshold entries. For each of the six keys listed in [P4-T6], run `grep -n -A 3 -F -e '"<key>"' extensions/drm-copilot/jest.config.cjs` with the key substituted. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac24-jest-thresholds.2026-09-25T23-29.md` with the four fields. Acceptance: each command exits 0 with exactly one key match followed by `lines: 85` and `branches: 75`.
- [ ] [P10-T10] AC 20 end-state check (order-independent; the same commands run in both branches). Run `grep -c -F -e "gh_available: bool = True" scripts/dev_tools/pr_context/render_pr_helpers.py`, `grep -c -F -e "ghAvailable?: boolean" extensions/drm-copilot/src/lib/pr-context/autoclose.ts`, `grep -c -F -e "ghAvailable = true" extensions/drm-copilot/src/lib/pr-context/autoclose.ts`, and `git merge-base --is-ancestor <scope-anchor-sha> HEAD`, and write them to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac20-parameter.2026-09-25T23-29.md` with the four fields per command, a `Branch:` line, and no `ExpectedExitCode:`. Then run `grep -c -F -e "None (GitHub CLI unavailable; closing issues not verified)" scripts/dev_tools/pr_context/render_pr_helpers.py` and, in N588 only, also `git grep --untracked -n -F -e "None (GitHub CLI unavailable; closing issues not verified)" -- scripts/dev_tools/pr_context extensions/drm-copilot/src`, and write them to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac20-empty-unavailable-body.2026-09-25T23-29.md` with `Timestamp:`, `Command:` and `EXIT_CODE:` per command, `Output Summary:`, and, in N588 only, one `ExpectedExitCode: 1`. Every command in this artifact has the same expected exit code: in N588 both commands are expected to exit 1; in M588 the artifact holds only the `grep -c` command, expected to exit 0, and carries no `ExpectedExitCode:` line. Acceptance (both branches): the three counts each print `1` and the ancestor check exits 0 (one keyword-only `gh_available` / optional `ghAvailable` parameter with default true, whichever item introduced it). Acceptance (branch-specific): N588 — the literal count prints `0` and exits 1, and the `git grep --untracked` command exits 1 with no output (the literal appears in no Python or TypeScript production file), so the builder's (gh unavailable, empty list) body is the pre-existing PASS / non-PASS fallback, because the N588 empty-list path reads no availability value ([P3-T4](c), [P4-T3]); M588 — the literal count prints at least `1` and exits 0, so the body is #588's `None (GitHub CLI unavailable; closing issues not verified)`. `Output Summary:` of `ac20-parameter` cites `Branch:` and `Base:` from the [P0-T2] artifact and the collector-level evidence that both call sites pass availability: C1 and T1 (gh unavailable through the collector, annotation appended) passed in [P5-T1] and [P5-T2].
- [ ] [P10-T11] AC 19 check (branch-conditional). Block boundaries used here and in [P6-T11] and [P7-T3]: Python — start at the first line of the contiguous decorator group directly above a column-0 `def test_` line (or at the `def` line when there is none), and end before the first later non-blank line whose first character, at column 0, is a letter, `_`, `@`, or `#`; a column-0 `)` that closes a wrapped signature does not end the block. TypeScript — start at a line beginning `  it(` or `  it.each(` and end at, and include, the first later line that is exactly `  });` (two spaces). Remove trailing empty or whitespace-only lines before comparing. N588: run `git diff --name-status <scope-anchor-sha> HEAD -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, the companion `git status --porcelain -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/src/lib/executable-resolver.ts extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, and `git ls-files --cached --others -- tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py extensions/drm-copilot/src/lib/executable-resolver.ts`; write them to `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac19-588-file-set.2026-09-25T23-29.md` with the four fields per command and a `Branch:` line. Acceptance (N588): all three commands exit 0 and print nothing (no #588-only file is created or edited), and the [P6-T11] and [P7-T3] artifacts record `N588` with no file edited. M588: enumerate the comparison set mechanically — every Python block in `git show <scope-anchor-sha>:tests/scripts/dev_tools/pr_context/test_render_pr_helpers.py` except the [P6-T11] update set, and every TypeScript block in `git show <scope-anchor-sha>:extensions/drm-copilot/test/lib/pr-context/render-pr-helpers.test.ts` except `merges author-asserted and referenced into author auto-close` (replaced by [P7-T2]) and the [P7-T3] update set — and compare each with the block of the same test name in the working file. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac19-unmodified-588-tests.2026-09-25T23-29.md` with the four fields, a `Branch:` line, the enumerated set, and each block's line span in both versions. Acceptance (M588): every block in the comparison set is byte-identical between the two versions, and each test in the [P6-T11] and [P7-T3] update sets passed in [P6-T13] and [P7-T6].
- [ ] [P10-T12] Commit and push the Phase 10 evidence. Paths: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `docs(bug): record scope and hermeticity evidence for #622`. Acceptance: commit and push exit 0; `git status --porcelain` prints nothing.

### Phase 11 — Acceptance-Criteria Check-Off

Each task below changes exactly one `- [ ]` to `- [x]` in the `## Acceptance Criteria` section of `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/spec.md`, identified by its ordinal position N (1-27) in that list, and only when every named evidence artifact exists and meets its acceptance condition. A task whose evidence is missing or failing stays unchecked.

- [ ] [P11-T1] Check off AC 1 (shared pattern definitions). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac01-pattern-absent.2026-09-25T23-29.md` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac01-pattern-defined.2026-09-25T23-29.md` passed.
- [ ] [P11-T2] Check off AC 2 (extractors use the pattern; names and exports kept). Evidence: the [P10-T2] artifact passed.
- [ ] [P11-T3] Check off AC 3 (Python rejection matrix; fails pre-fix). Evidence: the `#ISO-8601` and `#CR-1` cases failed in `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-fail-first.2026-09-25T23-29.md` and all 39 cases passed in `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-pass-after.2026-09-25T23-29.md`.
- [ ] [P11-T4] Check off AC 4 (Python acceptance matrix). Evidence: all 18 `test_extractors_accept_bare_number_tokens` cases passed in the [P5-T1] artifact.
- [ ] [P11-T5] Check off AC 5 (TS matrices). Evidence: all 57 `issue-reference-pattern.test.ts` cases passed in `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/ts-pass-after.2026-09-25T23-29.md`.
- [ ] [P11-T6] Check off AC 6 (literal parity). Evidence: `test_issue_reference_pattern_matches_typescript_literal` and `test_autoclose_literals_match_typescript_source` passed in the [P5-T1] artifact.
- [ ] [P11-T7] Check off AC 7 (JIRA assertions inverted). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac07-jira-assertions.2026-09-25T23-29.md` passed; the inverted tests passed in `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/py-unit-tests.2026-09-25T23-29.md` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/regression-testing/ts-unit-tests.2026-09-25T23-29.md`.
- [ ] [P11-T8] Check off AC 8 (promotion removed). Evidence: the first command of `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac08-ac27-absent.2026-09-25T23-29.md` exited 1 with no output.
- [ ] [P11-T9] Check off AC 9 (close-candidates tests renamed). Evidence: `test_build_close_candidates_section_lists_referenced_issues_as_detected_only` passed in the [P6-T13] artifact and `keeps referenced issues out of author auto-close` passed in the [P7-T6] artifact.
- [ ] [P11-T10] Check off AC 10 (scraped tokens, gh unavailable). Evidence: C1 and T1 failed in the [P2-T9] and [P2-T10] artifacts and passed in the [P5-T1] and [P5-T2] artifacts.
- [ ] [P11-T11] Check off AC 11 (prose-cited closed issue). Evidence: C2 and T2, same four artifacts as [P11-T10].
- [ ] [P11-T12] Check off AC 12 (prose-cited open out-of-scope issue). Evidence: C3 and T3, same four artifacts.
- [ ] [P11-T13] Check off AC 13 (pending primary kept or excluded). Evidence: the four C4 and four T4 cases failed pre-fix and passed after; both C5 and both T5 cases passed in the [P5-T1] and [P5-T2] artifacts.
- [ ] [P11-T14] Check off AC 14 (fetch once). Evidence: C6 and T6 passed in the [P5-T1] and [P5-T2] artifacts.
- [ ] [P11-T15] Check off AC 15 (autoclose unit matrices). Evidence: the 12 `test_autoclose.py` tests passed in the [P6-T13] artifact and the 12 selection and classification tests in `autoclose.test.ts` passed in the [P7-T6] artifact.
- [ ] [P11-T16] Check off AC 16 (annotation line, in #622's own builder test files). Evidence: `test_build_issues_to_autoclose_section_appends_unverified_annotation_when_gh_unavailable` in `tests/scripts/dev_tools/pr_context/test_autoclose_builder.py` passed ([P6-T13]) and `appends the unverified annotation when gh is unavailable` in `extensions/drm-copilot/test/lib/pr-context/autoclose.test.ts` passed ([P7-T6]).
- [ ] [P11-T17] Check off AC 17 (no annotation when gh available, in #622's own builder test files). Evidence: `test_build_issues_to_autoclose_section_omits_annotation_when_gh_available` in `test_autoclose_builder.py` ([P6-T13]) and `omits the annotation when gh is available` in `autoclose.test.ts` ([P7-T6]) passed.
- [ ] [P11-T18] Check off AC 18 (composed precedence, exactly the five D12 rows). Evidence: all five `test_build_issues_to_autoclose_section_fallback_precedence` cases in `test_autoclose_builder.py` ([P6-T13]) and all five `applies the composed fallback precedence` cases in `autoclose.test.ts` ([P7-T6]) passed; the [P6-T3] and [P7-T1] acceptance counts of the #588 literal in those two files are `0`, so no #622 test asserts the (gh unavailable, empty list) row.
- [ ] [P11-T19] Check off AC 19 (branch-conditional #588 test handling). Evidence: the `Branch:` line of `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/588-detection.2026-09-25T23-29.md`. N588: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac19-588-file-set.2026-09-25T23-29.md` passed, and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/h3-update-python.2026-09-25T23-29.md` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/other/h3-update-typescript.2026-09-25T23-29.md` record `N588` with no file edited. M588: the [P6-T11] and [P7-T3] update sets passed in the [P6-T13] and [P7-T6] artifacts, and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac19-unmodified-588-tests.2026-09-25T23-29.md` passed.
- [ ] [P11-T20] Check off AC 20 (availability parameter and call sites; branch-dependent empty-unavailable body). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/baseline/588-detection.2026-09-25T23-29.md` records the branch; `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac20-parameter.2026-09-25T23-29.md` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac20-empty-unavailable-body.2026-09-25T23-29.md` passed with the values [P10-T10] requires for that branch.
- [ ] [P11-T21] Check off AC 21 (scope). Evidence: the [P10-T5] and [P10-T6] artifacts passed.
- [ ] [P11-T22] Check off AC 22 (500-line cap). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac22-line-caps.2026-09-25T23-29.md` passed.
- [ ] [P11-T23] Check off AC 23 (hermetic tests). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac23-hermeticity-scan.2026-09-25T23-29.md` and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac23-hermeticity-companions.2026-09-25T23-29.md` passed.
- [ ] [P11-T24] Check off AC 24 (Jest thresholds). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac24-jest-thresholds.2026-09-25T23-29.md` passed and [P9-T5] exited 0.
- [ ] [P11-T25] Check off AC 25 (coverage both runtimes). Evidence: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-final.2026-09-25T23-29.md`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-delta.2026-09-25T23-29.md`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/typescript-coverage-final.2026-09-25T23-29.md`, and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/typescript-coverage-delta.2026-09-25T23-29.md` passed, and `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/python-coverage-final.json` is committed. These are the locations spec AC 25 names (interpretation 9).
- [ ] [P11-T26] Check off AC 26 (toolchain single pass). Evidence: the final `Loop iteration:` of every Phase 8 artifact ([P8-T1]-[P8-T4]) is the same number and all passed with no file changed; the same holds for every Phase 9 artifact ([P9-T1]-[P9-T5]).
- [ ] [P11-T27] Check off AC 27 (docstrings and TSDoc). Evidence: the `ABC-123` command in the [P10-T4] `ac08-ac27-absent` artifact exited 1; the six `bare-number` counts and the two parameter counts in the [P10-T4] `ac08-ac27-counts` artifact passed; and the [P10-T1] `ac01-pattern-absent` artifact exited 1.
- [ ] [P11-T28] Count the acceptance-criteria checkboxes. Run `awk '/^## Acceptance Criteria/{f=1;next} /^## /{f=0} f&&/^- \[x\]/{x++} f&&/^- \[ \]/{u++} END{print "checked=" x+0, "unchecked=" u+0}' docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/spec.md`. Write `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence/qa-gates/ac-checkoff-count.2026-09-25T23-29.md` with the four fields. Acceptance: `EXIT_CODE: 0` and the output is exactly `checked=27 unchecked=0` (before Phase 11 the same command prints `checked=0 unchecked=27`).
- [ ] [P11-T29] Commit and push the check-offs. Paths: `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/spec.md`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/evidence`, `docs/features/active/collect-pr-context-fabricates-auto-close-issues-622/plan.2026-09-25T23-29.md`. Subject: `docs(bug): check off #622 acceptance criteria`. Acceptance: commit and push exit 0; `git status --porcelain` prints nothing.
