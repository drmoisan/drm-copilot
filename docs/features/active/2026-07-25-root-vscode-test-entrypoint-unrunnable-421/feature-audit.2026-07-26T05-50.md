# Feature Audit: Root vscode-test Entry-Point Repair (#421)

---

**Audit Date:** 2026-07-26
**Feature Folder:** `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421`
**Base Branch:** `origin/main`
**Head Branch:** `bug/vscode-test-integration-entrypoint`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

---

## Scope and Baseline

- **Base branch:** `origin/main` (commit `fb483b8468204e4385b5583c3b3ec4c0a987eede`)
- **Head branch/commit:** `bug/vscode-test-integration-entrypoint` (commit `852075346e7068435fb2c9d9744e9892fb789260`; `origin` head verified identical via `git ls-remote`)
- **Merge base:** `fb483b8468204e4385b5583c3b3ec4c0a987eede` (base commit is the merge base; the branch is a fast-forward descendant)
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by the reviewer 2026-07-26 — the artifact pair was missing at review start; refreshed with `python -m scripts.dev_tools.pr_context.collector --base origin/main --head HEAD`)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/**` (baseline/, regression-testing/, qa-gates/, other/)
  - Additional evidence: reviewer-executed toolchain re-runs (all exit 0) and reviewer-dispatched CI run 30189725327 at head `85207534` (conclusion `success`, 16/16 jobs)
- **Feature folder used:** `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421`
- **Requirements source:** `spec.md` (only source)
- **Work mode resolution note:** Explicit marker `- Work Mode: full-bug` present in `issue.md` (line 12). Per the full-bug rule, `spec.md` is the sole AC source; `user-story.md` is correctly absent from the folder.
- **Scope note:** The audit scope is the full branch diff `fb483b84..85207534`. The caller attempted no scope narrowing. Two contextual facts supplied by the caller (dot-directory worktree / #414 Condition 3; sibling orchestration owning dependencies/lockfile) were independently verified rather than accepted as conclusions: `jest.config.cjs` and `run-jest.cjs` are unchanged on the branch, and `package.json` `devDependencies`/`dependencies`/`overrides` plus `package-lock.json` are byte-identical to base.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/spec.md` — only source (checkbox-based, `## Acceptance Criteria` section, 11 items, all currently `[x]` as checked off by the executor)

### Acceptance criteria

1. AC1 — Scripts block corrected: the root `package.json` `scripts` block defines `"test": "node run-jest.cjs"`, defines `"pretest": "npm run compile && npm run lint"`, and no longer contains `test:integration` or `compile:integration-tests`; the `format` and `format:check` glob lists no longer contain `".vscode-test.mjs"`. Evidence: the committed `package.json` diff, which touches the `scripts` block only.
2. AC2 — No root npm script references `vscode-test`: a search of the root `package.json` `scripts` values for the string `vscode-test` returns zero matches. Evidence: grep output recorded in `evidence/qa-gates/`, plus the regression guard in AC3.
3. AC3 — Regression guard present: `tests/unit/vscode-test-removal.test.ts` exists, asserts that no root npm script value invokes `vscode-test` and that `.vscode-test.mjs`/`.vscode-test.js`/`.vscode-test.cjs`/`.vscode-test.json`/`tsconfig.vscode-test.json` are absent from the repository root, and requires no change to `jest.config.cjs` or `run-jest.cjs` to be discovered (its path matches the existing `tests/unit/**/*.test.ts` `testMatch`).
4. AC4 — Regression guard executed: the guard test is listed as an executed, passing suite in the root jest run output — locally via the path-independent invocation, and on CI in the new workflow's run log. Evidence: run logs stored in `evidence/qa-gates/`.
5. AC5 — CI wiring follows convention: a new reusable workflow `_<name>.yml` exists under `.github/workflows/` declaring both `on: workflow_call:` and `on: workflow_dispatch:`, runs `npm ci` and root `npm test` at the repository root, and is referenced from `ci.yml` via `uses:`; `.github/workflows/README.md` lists it in the per-stage dispatch table.
6. AC6 — Root `npm test` ends in a defined, passing state, verified on CI: a green run of the new workflow against the branch head shows root `npm test` completing successfully (pretest compile + lint, then the full root jest suite). This run also satisfies the `modified-workflow-needs-green-run` policy for the workflow diff. Evidence: workflow run URL and log excerpt stored in `evidence/qa-gates/`. (Per the Test Strategy verification constraint, a local-worktree `npm test` pass is explicitly NOT required.)
7. AC7 — Local path-independent verification recorded: the root jest suite (including the guard test) passes locally via a rootDir-free invocation (documented #414 Condition 3 workaround) or from a checkout path without a dot-directory component. Evidence: command and output stored in `evidence/qa-gates/`.
8. AC8 — No silent coverage reduction, with proof: evidence records that (a) the removed scripts executed zero tests (baseline stack trace showing exit inside `loadDefaultConfigFile`), (b) no CI workflow previously ran the root TypeScript toolchain, and (c) the new workflow now runs the root jest suite on every `ci.yml` trigger — i.e., coverage strictly increases. All previously executing suites unchanged. Evidence: baseline reference plus new-run log in `evidence/baseline/` and `evidence/qa-gates/`.
9. AC9 — Boundaries respected: the change set contains no modifications to `run-jest.cjs`, `jest.config.cjs`, `.claude/rules/**`, `.agents/skills/**`, or `extensions/drm-copilot/resources/claude-customizations/**`, and no changes to `devDependencies` or `package-lock.json`. Evidence: `git diff --name-only` inventory recorded in `evidence/qa-gates/`.
10. AC10 — Full seven-stage toolchain passes at the repository root in a single pass (format:check, lint, typecheck, architecture [n/a with rationale], unit tests via the path-independent invocation, contract checks [n/a with rationale], integration tests [n/a with rationale]), with the loop restarted from stage 1 after any failure or auto-fix. Evidence: stage-by-stage results in `evidence/qa-gates/`.
11. AC11 — Scope decision documented: the spec's Scope Decision section records the selected option (a), the decisive evidence including the orchestrator's `git log --all` correction to the research artifact and the `2f67b888` prior art, the rejected option (b) with reasons, the strongest counterargument and why it does not prevail, and the deliberate exclusion of devDependency/lockfile changes as a follow-up.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | AC1 — Scripts block corrected | PASS | `package.json` diff vs `fb483b84`: `"test": "node run-jest.cjs"`, `"pretest": "npm run compile && npm run lint"`, `test:integration` and `compile:integration-tests` absent, `.vscode-test.mjs` removed from both glob lists. Per-key comparison: only `scripts` changed among ten top-level keys. | `git diff fb483b84..HEAD -- package.json`; reviewer per-key `node -e` comparison | Reviewer verified independently, not only from executor evidence. |
| 2 | AC2 — No script references `vscode-test` | PASS | Base scripts contained `test:integration`/`test` = `vscode-test`; current scripts contain zero occurrences (guard test 1 also enforces this and passes on both CI legs). | `node -e` scripts dump; guard suite run (`--testPathPatterns "vscode-test-removal"` → 2/2 pass) | Executor evidence: `evidence/qa-gates/scripts-block-verification.2026-07-26T05-16.md`. |
| 3 | AC3 — Regression guard present | PASS | `tests/unit/vscode-test-removal.test.ts` (78 lines) asserts both invariants over the exact five dead file names; `jest.config.cjs`/`run-jest.cjs` unchanged vs base; discovery proven via `--listTests` naming the file's absolute path. | `git diff --name-only fb483b84..HEAD -- jest.config.cjs run-jest.cjs` (empty); `node run-jest.cjs --listTests --testMatch "**/tests/unit/**/*.test.ts"` | File content reviewed directly by the reviewer. |
| 4 | AC4 — Regression guard executed | PASS | Local: 170/170 suites incl. guard (reviewer re-run, exit 0); name-scoped run `Test Suites: 1 passed`. CI: `PASS tests/unit/vscode-test-removal.test.ts` present in both OS job logs of head-matching run 30189725327 (and of prior run 30189336124). | `node run-jest.cjs --coverage --testMatch ...`; `gh run view --job 89760673003 --log`; `gh run view --job 89760673041 --log` | Both the local and CI halves independently reproduced by the reviewer. |
| 5 | AC5 — CI wiring follows convention | PASS | `_root-typescript-tests.yml` declares `on: workflow_call:` and `on: workflow_dispatch:`, runs `npm ci` then `npm test` at the root; `ci.yml` adds `root-typescript-tests:` via `uses: ./.github/workflows/_root-typescript-tests.yml`; README dispatch table lists the new row (seven → eight workflows). | `git diff fb483b84..HEAD -- .github/workflows/`; file inspection | Mirrors `_drm-copilot-extension-tests.yml` (same action majors, node 20, npm cache on root lockfile). |
| 6 | AC6 — Root `npm test` passing on CI at branch head | PASS | Reviewer-dispatched CI run **30189725327** at head SHA `852075346e7068435f...` (exact current branch head, local == origin) concluded `success`; both `root-typescript-tests` legs green; logs show pretest (compile + lint) then jest, 170 suites / 2038 tests. This satisfies `modified-workflow-needs-green-run` with a head-matching `workflow_dispatch` run. | `gh workflow run ci.yml --ref bug/vscode-test-integration-entrypoint`; `gh run view 30189725327 --json status,conclusion,headSha`; job log greps | The executor's recorded run 30189336124 was at prior head `df874e81` (delta to head is docs-only); the reviewer's fresh run closes the strict head-match requirement. Run URL: https://github.com/drmoisan/drm-copilot/actions/runs/30189725327 |
| 7 | AC7 — Local path-independent verification recorded | PASS | `evidence/qa-gates/guard-test-local-run.2026-07-26T05-22.md` and `final-test-coverage-root.2026-07-26T05-29.md` record the rootDir-free invocation with exit 0; reviewer re-ran the same command 2026-07-26 with identical results (170/2038, exit 0). Suite-set parity with CI (identical counts) confirms the workaround selects the intended suite set. | `node run-jest.cjs --coverage --testMatch "**/tests/unit/**/*.test.ts" --testMatch "**/extensions/drm-copilot/test/**/*.test.ts"` | The #414 Condition 3 constraint was verified as genuinely pre-existing: the affected files are unchanged on this branch. |
| 8 | AC8 — No silent coverage reduction, with proof | PASS | (a) Fail-before artifacts show both removed scripts exiting non-zero inside `loadDefaultConfigFile` (zero tests ever executed); (b) `evidence/baseline/baseline-ci-inventory.2026-07-26T05-11.md` records no prior CI execution of the root TS toolchain (consistent with spec's grep verification); (c) run 30189725327 shows the root jest suite now executing on `ci.yml`. Coverage numerically unchanged (97.01%/89.07%) with +1 suite/+2 tests; all pre-existing suites still pass (2038/2038). | Reviewer coverage re-run; `gh run view 30189725327 --json jobs` | Coverage strictly increases in CI-execution terms; no previously executing suite changed state. |
| 9 | AC9 — Boundaries respected | PASS | Filtered diff over all forbidden paths (`run-jest.cjs`, `jest.config.cjs`, `.claude/rules/**`, `.agents/skills/**`, `extensions/drm-copilot/resources/claude-customizations/**`, `package-lock.json`) is empty; per-key `package.json` comparison shows `devDependencies`/`dependencies`/`overrides` byte-identical to base. | `git diff --name-only fb483b84..85207534 -- <forbidden paths>` (empty); per-key comparison | Executor inventory `evidence/qa-gates/boundary-inventory.2026-07-26T05-33.md` corroborated at the final head. |
| 10 | AC10 — Seven-stage toolchain single pass | PASS | Reviewer single-pass re-run: format:check exit 0, lint exit 0, typecheck exit 0, unit tests + coverage exit 0. Stages 4/6/7 N/A with rationale recorded in `evidence/qa-gates/final-stages-4-6-7-na-root.2026-07-26T05-28.md` (no root TS architecture gate configured; no root contract surface; no root integration suite post-change — extension integration-style jest tests run under stage 5 and in their own CI job). No stage failed or auto-fixed files, so no loop restart was required. | `npm run format:check`; `npm run lint`; `npm run typecheck`; jest coverage invocation | Executor's clean-pass record: `evidence/qa-gates/final-qa-clean-pass.2026-07-26T05-34.md`. |
| 11 | AC11 — Scope decision documented | PASS | `spec.md` Scope Decision section contains: selected option (a) with six numbered decisive-evidence items including the `git log --all` correction and `2f67b888` prior art; rejected option (b) with reasons; the strongest counterargument (`test:integration` name expectation) and why it does not prevail; devDependency/lockfile exclusion recorded in Scope & Non-Goals and Rollout & Follow-up. | Direct read of `spec.md` §Scope Decision, §Scope & Non-Goals, §Rollout & Follow-up | Documentation-completeness criterion; verified by inspection. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 11 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. When authoring the PR, cite head-matching CI run 30189725327 (https://github.com/drmoisan/drm-copilot/actions/runs/30189725327) alongside the executor's recorded run 30189336124, since the in-repo `evidence/qa-gates/ci-green-run-root-typescript-tests.2026-07-26T05-40.md` predates the final docs commit.
2. After merge, hand the deferred devDependency cleanup (`@vscode/test-cli`, `@vscode/test-electron`, `@types/mocha` + lockfile regeneration) to the sibling dependency/lockfile workstream as recorded in `spec.md` Rollout & Follow-up.

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 11 criteria evaluated **PASS**. All 11 were already checked off (`[x]`) in `spec.md` by the executor with per-AC evidence (`evidence/other/ac-verification.2026-07-26T05-44.md`); the reviewer independently re-verified each and confirms the check-offs are correct. **No source-file checkbox change was made by this audit** because no unchecked PASS item remained.
- No criterion is PARTIAL, FAIL, or UNVERIFIED, so none is required to be unchecked.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/spec.md`
- Total AC items: 11
- Checked off (delivered): 11
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/spec.md` | 11 | 11 | 0 | Checkbox-backed; sole authoritative source under `full-bug` work mode |
