# 2026-07-25-npm-audit-brace-expansion-dos-414 (Plan)

- **Issue:** #414
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-25T21-00
- **Status:** Ready for Preflight
- **Version:** 0.3
- **Work Mode:** full-bug (AC source: `spec.md` only, per `.claude/skills/acceptance-criteria-tracking/SKILL.md`)
- **Branch:** `bug/npm-audit-brace-expansion` (base: `main`)
- **Spec:** `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/spec.md`
- **Research:** `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/research/2026-07-25T10-45-brace-expansion-ghsa-mh99-remediation-research.md`

## Plan Conventions

- **Evidence root (non-overridable):** every evidence artifact in this plan is written under `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/evidence/<kind>/` where `<kind>` is one of `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`. Paths under `artifacts/` (for example `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`) are forbidden for evidence output. Tasks below abbreviate the evidence root as `<EVID>` = `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/evidence`.
- **Artifact schema:** every command-step artifact records `Timestamp:` (ISO-8601 `yyyy-MM-ddTHH-mm`), `Command:`, `EXIT_CODE:`, and `Output Summary:`. Coverage-bearing test artifacts record numeric line and branch coverage percentages in `Output Summary:`, never placeholders. `<ts>` in artifact filenames denotes the execution-time ISO-8601 timestamp.
- **Working directories:** "root" = the repository root; "extension" = `extensions/drm-copilot`; "mcp-server" = `packages/mcp-server`. `node_modules` is absent in all three roots at plan time; `npm audit` reads only the lockfile, but every lint/typecheck/test/build command requires a prior install task.
- **Prohibited:** `npm audit fix --force` (its `fixAvailable` suggestion is a breaking major downgrade of jest; the exact suggested version drifts with the live advisory database and must not be pinned in reasoning). No edits to `packages/mcp-server/**`, `.github/workflows/**`, branch-protection configuration, `.claude/hooks/validate-orchestrator-output.ps1`, `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/compute_complexity_floor.py`, or `.claude/lib/model-routing/ModelRouting.psm1`.
- **Lockfile invariant:** both regenerated lockfiles must remain `"lockfileVersion": 3`. Local toolchain is Node v24.14.0 / npm 11.9.0; CI is Node 20.
- **Fail-closed evidence rule:** if any required baseline artifact, final-QA artifact, or coverage-comparison artifact is missing or has incomplete schema fields, the corresponding task stays unchecked and the plan outcome is remediation-required, never PASS.
- **QA loop rule (Phases 4–5):** if any QA step fails or changes files, remediate and restart that phase's loop from its first toolchain step (P4-T2 for root, P5-T2 for extension), re-recording each rerun in a new timestamped artifact, until all steps pass in a single clean pass.
- **Phase 0 gate-baseline escalation rule:** if any Phase 0 gate baseline (P0-T14, P0-T15, P0-T16, P0-T18, P0-T19, P0-T20) records a non-zero exit code, execution stops and the condition is escalated to the planner before Phase 1; the executor must not relax any gate acceptance on its own authority. **Escalation record (2026-07-25):** the Phase 0 gate baseline was executed. P0-T14 returned exit 1 (Condition A — root `npm run format:check` flags two committed fixtures, `tests/fixtures/discovery_schemas/v1/runtime-characterization-scenario.invalid.json` and `tests/fixtures/discovery_schemas/v1/unspecified-behavior-record.invalid.json`, which are byte-identical to `origin/main`; the gate is already red on `main` itself). P0-T11 and P0-T13 returned exit 1 (Condition B — the jest coverage scripts report `No tests found` because this worktree's path contains the `.claude` dot-directory and jest renders the preceding separator as a backslash that the glob matcher consumes as an escape; this is an artifact of the worktree location, not a repository defect). Both conditions were escalated to and dispositioned by the orchestrator on 2026-07-25. The dispositioned acceptance for the affected Phase 4/5 gates — P4-T2 (root `format:check`), P4-T5/P5-T5 (coverage capture), and P4-T7/P5-T6 (coverage comparison) — is baseline parity ("introduced no new failure") rather than absolute `EXIT_CODE: 0`. This disposition applies ONLY to the gates named in this sentence; every other gate retains absolute `EXIT_CODE: 0`. Both conditions are reported as separate defects (P6-T6), not fixed within the #414 four-file change set.

### Phase 0 — Policy Reads and Fail-Before Baseline

Policy reads follow the order defined in `.claude/skills/policy-compliance-order/SKILL.md`. TypeScript is the in-scope language (both affected roots are TypeScript projects and the lockfile refresh moves the TypeScript toolchain's own dependencies: jest, c8, minimatch, glob, mocha). Baseline audits run BEFORE any manifest edit; they are the fail-before evidence for this bug. Gate baselines (P0-T14 through P0-T20) run after their root's install task (P0-T10 for root tasks, P0-T12 for extension tasks) and before any manifest edit; they establish the pre-edit state that Phases 4–5 are compared against.

- [x] [P0-T1] Read `CLAUDE.md` (repository standing instructions, policy order position 1).
  - Acceptance: file read in this session; listed in the P0-T5 artifact.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md` (policy order position 2).
  - Acceptance: file read in this session; listed in the P0-T5 artifact.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` (policy order position 3).
  - Acceptance: file read in this session; listed in the P0-T5 artifact.
- [x] [P0-T4] Read `.claude/rules/typescript.md` and `.claude/rules/typescript-suppressions.md` (language-specific policy for the in-scope TypeScript roots).
  - Acceptance: both files read in this session; listed in the P0-T5 artifact.
- [x] [P0-T5] Write the policy-read evidence artifact `<EVID>/other/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the ordered list from P0-T1 through P0-T4), and the explicit list of files read.
  - Acceptance: artifact exists at the stated path with all three fields populated.
- [x] [P0-T6] Record the branch/commit baseline: run `git rev-parse HEAD` and `git status --porcelain` in the repository root; write `<EVID>/baseline/git-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (head SHA and clean/dirty state).
  - Acceptance: artifact exists; head SHA recorded; branch is `bug/npm-audit-brace-expansion`.
- [x] [P0-T7] [expect-fail] Fail-before audit, root: run `npm audit --audit-level=moderate` in the repository root BEFORE any edit; write `<EVID>/baseline/npm-audit-fail-before-root.<ts>.md` with the full schema fields.
  - Acceptance: artifact records `EXIT_CODE:` non-zero with 22 high-severity findings attributable to GHSA-mh99-v99m-4gvg (flagged nodes `node_modules/brace-expansion@5.0.7` and `node_modules/minimatch/node_modules/brace-expansion@2.1.2`).
- [x] [P0-T8] [expect-fail] Fail-before audit, extension: run `npm audit --audit-level=moderate` in `extensions/drm-copilot` BEFORE any edit; write `<EVID>/baseline/npm-audit-fail-before-extension.<ts>.md` with the full schema fields.
  - Acceptance: artifact records `EXIT_CODE:` non-zero with 20 high-severity findings attributable to GHSA-mh99-v99m-4gvg (flagged nodes `node_modules/brace-expansion@5.0.7` and `node_modules/glob/node_modules/brace-expansion@2.1.2`).
- [x] [P0-T9] Fail-before-window audit, mcp-server (passing control): run `npm audit --audit-level=moderate` in `packages/mcp-server`; write `<EVID>/baseline/npm-audit-baseline-mcp-server.<ts>.md` with the full schema fields.
  - Acceptance: artifact records `EXIT_CODE: 0` and `found 0 vulnerabilities`, establishing the one-passing-root baseline.
- [x] [P0-T10] Baseline install, root: run `npm ci` in the repository root (from the unmodified committed lockfile); write `<EVID>/baseline/npm-ci-root.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`; `node_modules` present at root.
- [x] [P0-T11] Baseline coverage, root: run BOTH invocations in the repository root and record each in `<EVID>/baseline/test-unit-coverage-root.<ts>.md` with the full schema fields: (a) the plan's stated script `npm run test:unit:coverage` with its actual exit code; (b) a rootDir-free jest invocation of the same binary and config that avoids the `.claude` glob-escape artifact and produces numeric coverage. The artifact must document the worktree-path artifact as the reason invocation (a) reports `No tests found`, explicitly noting it is an artifact of running under a path containing the `.claude` dot-directory and not a repository defect or a #414 regression.
  - Acceptance: numeric baseline line and branch coverage captured from invocation (b) and recorded in `Output Summary:` (never placeholders), meeting >=85% line / >=75% branch; `EXIT_CODE:` recorded truthfully for both invocations; the worktree-path artifact explanation is present.
- [x] [P0-T12] Baseline install, extension: run `npm ci` in `extensions/drm-copilot` (from the unmodified committed lockfile); write `<EVID>/baseline/npm-ci-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`; `node_modules` present in the extension root.
- [x] [P0-T13] Baseline coverage, extension: run BOTH invocations in `extensions/drm-copilot` and record each in `<EVID>/baseline/test-coverage-extension.<ts>.md` with the full schema fields: (a) the plan's stated script `npm run test:coverage` with its actual exit code; (b) a rootDir-free jest invocation of the same binary and config that avoids the `.claude` glob-escape artifact and produces numeric coverage. The artifact must document the worktree-path artifact as the reason invocation (a) reports `No tests found`, explicitly noting it is an artifact of running under a path containing the `.claude` dot-directory and not a repository defect or a #414 regression.
  - Acceptance: numeric baseline line and branch coverage captured from invocation (b) and recorded in `Output Summary:` (never placeholders), meeting >=85% line / >=75% branch; `EXIT_CODE:` recorded truthfully for both invocations; the worktree-path artifact explanation is present.
- [x] [P0-T14] Baseline format check, root: run `npm run format:check` in the repository root BEFORE any manifest edit; write `<EVID>/baseline/format-check-root-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code, establishing the pre-edit state of this gate.
- [x] [P0-T15] Baseline lint, root: run `npm run lint` in the repository root BEFORE any manifest edit; write `<EVID>/baseline/lint-root-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code and error/warning counts.
- [x] [P0-T16] Baseline typecheck, root: run `npm run typecheck` in the repository root BEFORE any manifest edit; write `<EVID>/baseline/typecheck-root-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code and diagnostic count.
- [x] [P0-T17] [expect-fail] Baseline root integration-test command: run `npm run test:integration` in the repository root BEFORE any manifest edit; write `<EVID>/baseline/test-integration-root-baseline.<ts>.md` with the full schema fields and the verbatim command output.
  - Acceptance: `EXIT_CODE: 1` with the verbatim `@vscode/test-cli` configuration error recorded, establishing that this failure is a pre-existing repository condition (no `.vscode-test.{json,js,cjs,mjs}` config exists in the repository or any parent directory, and `tsconfig.vscode-test.json` is absent) and is not caused by the override change.
- [x] [P0-T18] Baseline lint, extension: run `npm run lint` in `extensions/drm-copilot` BEFORE any manifest edit; write `<EVID>/baseline/lint-extension-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code and error/warning counts.
- [x] [P0-T19] Baseline typecheck, extension: run `npm run typecheck` in `extensions/drm-copilot` BEFORE any manifest edit; write `<EVID>/baseline/typecheck-extension-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code and diagnostic count.
- [x] [P0-T20] Baseline compile, extension: run `npm run compile` in `extensions/drm-copilot` BEFORE any manifest edit; write `<EVID>/baseline/compile-extension-baseline.<ts>.md` with the full schema fields.
  - Acceptance: artifact exists with the recorded exit code.

### Phase 1 — Root Remediation (Manifest Edit, Lockfile Regeneration, Audit Verification)

- [x] [P1-T1] Edit `package.json` (repository root) `overrides` block only: add unscoped entries `"brace-expansion": "^5.0.8"` and `"minimatch": "^10.2.5"`; delete the superseded `"c8": { "brace-expansion": "^5.0.7" }` block; leave every other overrides entry and all other manifest content unchanged (target block per research section 3).
  - Acceptance: `git diff package.json` shows exactly the two added entries and the removed `c8` block; JSON remains valid.
- [x] [P1-T2] Regenerate the root lockfile: run `npm install` in the repository root. If any stale flagged node remains in `package-lock.json` afterward (any `brace-expansion` at `2.1.2` or `5.0.7`, or any `minimatch` at `9.0.9`), delete the root `node_modules` directory and re-run `npm install` (do NOT use `npm audit fix --force`). Write `<EVID>/qa-gates/npm-install-root.<ts>.md` with the full schema fields, noting whether the delete-and-reinstall fallback was needed.
  - Acceptance: `EXIT_CODE: 0`; `package-lock.json` modified; `"lockfileVersion": 3` preserved.
- [x] [P1-T3] Pass-after audit, root: run `npm audit --audit-level=moderate` in the repository root; write `<EVID>/regression-testing/npm-audit-pass-after-root.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0` (satisfies spec AC "npm audit exits 0 in the repository root").

### Phase 2 — Extension Remediation (Manifest Edit, Lockfile Regeneration, Audit Verification)

- [x] [P2-T1] Edit `extensions/drm-copilot/package.json` `overrides` block only: add unscoped entries `"brace-expansion": "^5.0.8"` and `"minimatch": "^10.2.5"`; delete the superseded `"c8": { "brace-expansion": "^5.0.7" }` block; leave every other overrides entry and all other manifest content unchanged (target block per research section 3).
  - Acceptance: `git diff extensions/drm-copilot/package.json` shows exactly the two added entries and the removed `c8` block; JSON remains valid.
- [x] [P2-T2] Regenerate the extension lockfile: run `npm install` in `extensions/drm-copilot`. If any stale flagged node remains in `extensions/drm-copilot/package-lock.json` afterward (any `brace-expansion` at `2.1.2` or `5.0.7`, or any `minimatch` at `9.0.9`), delete `extensions/drm-copilot/node_modules` and re-run `npm install` (do NOT use `npm audit fix --force`). Write `<EVID>/qa-gates/npm-install-extension.<ts>.md` with the full schema fields, noting whether the fallback was needed.
  - Acceptance: `EXIT_CODE: 0`; `extensions/drm-copilot/package-lock.json` modified; `"lockfileVersion": 3` preserved.
- [x] [P2-T3] Pass-after audit, extension: run `npm audit --audit-level=moderate` in `extensions/drm-copilot`; write `<EVID>/regression-testing/npm-audit-pass-after-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0` (satisfies spec AC "npm audit exits 0 in extensions/drm-copilot").

### Phase 3 — Lockfile, Manifest, and Change-Set Assertions

- [x] [P3-T1] Verify the negative lockfile conditions in BOTH `package-lock.json` and `extensions/drm-copilot/package-lock.json` by grep: (a) no `node_modules/brace-expansion` node (hoisted or nested) resolves to a version `<=5.0.7` — specifically zero `brace-expansion` nodes at `2.1.2` or `5.0.7`; (b) no `minimatch` node resolves to any `9.x` version; (c) exactly one hoisted `node_modules/brace-expansion` node per lockfile, at `5.0.8` (or a higher `5.x` patch). Write `<EVID>/other/lockfile-assertions.<ts>.md` recording the exact grep commands, their `EXIT_CODE:` values, and matched/zero-match results, plus `Timestamp:` and `Output Summary:`.
  - Acceptance: all three assertions hold in both lockfiles; artifact records the evidence.
- [x] [P3-T2] Verify the manifest end-state in BOTH `package.json` and `extensions/drm-copilot/package.json`: each `overrides` block contains unscoped `"brace-expansion": "^5.0.8"` and `"minimatch": "^10.2.5"`, and neither manifest contains a `"c8"` overrides entry. Write `<EVID>/other/manifest-assertions.<ts>.md` with `Timestamp:`, `Command:` (the grep/inspection commands), `EXIT_CODE:`, and `Output Summary:`.
  - Acceptance: both conditions hold in both manifests (satisfies the spec AC on override end-state).
- [x] [P3-T3] Verify change-set exactness and mcp-server isolation against the uncommitted working tree. At this point the committed range `main...HEAD` contains only excluded `docs/features/` paths (planning artifacts committed at branch creation), so `git diff --name-only main...HEAD` cannot confirm the four-file dependency change set and MUST NOT be used here; the working-tree form is used instead, and the committed-range form of this assertion is performed post-commit in P6-T2. Run `git status --porcelain` and `git diff --name-only` in the repository root and assert (a) no reported path starts with `packages/mcp-server`; (b) after excluding paths under `docs/features/` and `artifacts/orchestration/` (feature documentation and orchestration checkpoint), the remaining reported set is exactly the four files `package.json`, `package-lock.json`, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`. Write `<EVID>/other/change-set-assertion.<ts>.md` with the full schema fields and the complete reported file list.
  - Acceptance: both assertions hold against the working tree; artifact records the full file list.
- [x] [P3-T4] Post-change regression-guard audit, mcp-server: run `npm audit --audit-level=moderate` in `packages/mcp-server`; write `<EVID>/regression-testing/npm-audit-post-change-mcp-server.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0` and `found 0 vulnerabilities` (satisfies the spec AC that mcp-server continues to pass).

### Phase 4 — Final QA Loop, Repository Root

Unconditional full toolchain against the regenerated dependency tree (format → lint → type-check → tests). This exercises the forced `minimatch` 9→10 path through jest, test-exclude, and glob; mocha's `minimatch` call site is not reachable through any runnable gate here and is verified directly in P6-T4. Apply the QA loop rule from Plan Conventions: on any failure or file change, remediate and restart from P4-T2 (re-running P4-T1 first if dependency state changed).

- [x] [P4-T1] Final install, root: run `npm ci` in the repository root against the regenerated `package-lock.json` (proves manifest/lockfile sync exactly as the CI gate's `npm ci` step does). Write `<EVID>/qa-gates/final-npm-ci-root.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P4-T2] Run `npm run format:check` in the repository root; write `<EVID>/qa-gates/final-format-check-root.<ts>.md` with the full schema fields. `EXIT_CODE: SKIPPED` is not a valid outcome.
  - Acceptance: `EXIT_CODE: 0`, OR the post-change output is identical to the P0-T14 pre-edit baseline — the same two fixture files flagged and no additional file — proving the change introduced no new formatting failure. The artifact must name the two fixtures (`tests/fixtures/discovery_schemas/v1/runtime-characterization-scenario.invalid.json`, `tests/fixtures/discovery_schemas/v1/unspecified-behavior-record.invalid.json`) and record that they are byte-identical to `origin/main` (verified: `git diff --name-only origin/main -- tests/fixtures/discovery_schemas/` is empty), so the condition is pre-existing on `main` and out of scope for #414.
- [x] [P4-T3] Run `npm run lint` in the repository root; write `<EVID>/qa-gates/final-lint-root.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P4-T4] Run `npm run typecheck` in the repository root; write `<EVID>/qa-gates/final-typecheck-root.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P4-T5] Post-change coverage, root: run BOTH invocations in the repository root and record each in `<EVID>/qa-gates/final-test-unit-coverage-root.<ts>.md` with the full schema fields: (a) the plan's stated script `npm run test:unit:coverage` with its actual exit code; (b) a rootDir-free jest invocation of the same binary and config that avoids the `.claude` glob-escape artifact and produces numeric coverage. The artifact must document the worktree-path artifact as the reason invocation (a) reports `No tests found`, explicitly noting it is an artifact of running under a path containing the `.claude` dot-directory and not a repository defect or a #414 regression.
  - Acceptance: numeric post-change line and branch coverage captured from invocation (b) and recorded in `Output Summary:` (never placeholders), meeting >=85% line / >=75% branch; `EXIT_CODE:` recorded truthfully for both invocations; the worktree-path artifact explanation is present.
- [x] [P4-T6] Run `npm run test:integration` in the repository root and compare against the P0-T17 pre-edit baseline. Write `<EVID>/qa-gates/final-test-integration-root.<ts>.md` with the full schema fields and the verbatim output. This command is not a runnable gate in this repository (root defines `test:integration` as `vscode-test`, but no `.vscode-test.{json,js,cjs,mjs}` config exists, so `@vscode/test-cli` exits 1 before starting any runner; the condition is environment-independent and also holds on CI runners, and no workflow in `.github/workflows/**` invokes root `test:integration`). `EXIT_CODE: SKIPPED` is not a valid outcome.
  - Acceptance: `EXIT_CODE: 0`, or the post-change output is the same `@vscode/test-cli` configuration error with the same exit code as the P0-T17 baseline, proving this change introduced no new failure. The artifact must state that the pre-existing missing-`vscode-test`-config condition is a separate defect to be reported as a potential issue, out of scope for #414.
- [x] [P4-T7] Coverage no-regression comparison, root: compare the numeric line and branch coverage from invocation (b) in `<EVID>/qa-gates/final-test-unit-coverage-root.<ts>.md` (P4-T5) against the invocation-(b) baseline in `<EVID>/baseline/test-unit-coverage-root.<ts>.md` (P0-T11); write `<EVID>/qa-gates/coverage-comparison-root.<ts>.md` recording `Timestamp:`, both baseline and post-change numeric values, and the delta. Because #414 changes no source file, the expected delta is zero.
  - Acceptance: post-change invocation-(b) line and branch coverage are each >= the invocation-(b) baseline (no regression; thresholds >=85% line / >=75% branch remain satisfied).

### Phase 5 — Final QA Loop, Extension and mcp-server Install Check

Unconditional full toolchain for `extensions/drm-copilot` (the extension defines no `format:check` script, and no format gate in either root covers `package.json`; P4-T2 runs the root check as a no-regression gate on unrelated files). Apply the QA loop rule: on any failure or file change, remediate and restart from P5-T2.

- [x] [P5-T1] Final install, extension: run `npm ci` in `extensions/drm-copilot` against the regenerated lockfile; write `<EVID>/qa-gates/final-npm-ci-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P5-T2] Run `npm run lint` in `extensions/drm-copilot`; write `<EVID>/qa-gates/final-lint-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P5-T3] Run `npm run typecheck` in `extensions/drm-copilot`; write `<EVID>/qa-gates/final-typecheck-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P5-T4] Run `npm run compile` in `extensions/drm-copilot`; write `<EVID>/qa-gates/final-compile-extension.<ts>.md` with the full schema fields.
  - Acceptance: `EXIT_CODE: 0`.
- [x] [P5-T5] Post-change coverage, extension: run BOTH invocations in `extensions/drm-copilot` and record each in `<EVID>/qa-gates/final-test-coverage-extension.<ts>.md` with the full schema fields: (a) the plan's stated script `npm run test:coverage` with its actual exit code; (b) a rootDir-free jest invocation of the same binary and config that avoids the `.claude` glob-escape artifact and produces numeric coverage. The artifact must document the worktree-path artifact as the reason invocation (a) reports `No tests found`, explicitly noting it is an artifact of running under a path containing the `.claude` dot-directory and not a repository defect or a #414 regression.
  - Acceptance: numeric post-change line and branch coverage captured from invocation (b) and recorded in `Output Summary:` (never placeholders), meeting >=85% line / >=75% branch; `EXIT_CODE:` recorded truthfully for both invocations; the worktree-path artifact explanation is present.
- [x] [P5-T6] Coverage no-regression comparison, extension: compare the numeric values from invocation (b) in `<EVID>/qa-gates/final-test-coverage-extension.<ts>.md` (P5-T5) against the invocation-(b) baseline in `<EVID>/baseline/test-coverage-extension.<ts>.md` (P0-T13); write `<EVID>/qa-gates/coverage-comparison-extension.<ts>.md` recording `Timestamp:`, both numeric value sets, and the delta. Because #414 changes no source file, the expected delta is zero.
  - Acceptance: post-change invocation-(b) line and branch coverage are each >= the invocation-(b) baseline (no regression; thresholds >=85% line / >=75% branch remain satisfied).
- [x] [P5-T7] mcp-server untouched-lockfile install check: run `npm ci` then `npm run build` in `packages/mcp-server` (no test script exists in this root); write `<EVID>/qa-gates/mcp-server-install-build.<ts>.md` with the full schema fields covering both commands.
  - Acceptance: both commands exit 0; no file under `packages/mcp-server` is modified by the check (`git status --porcelain packages/mcp-server` remains empty for committed files).

### Phase 6 — CI Verification, Acceptance-Criteria Tracking, Closeout

- [ ] [P6-T1] Commit the four changed dependency files (`package.json`, `package-lock.json`, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`) together with the feature evidence and plan updates on `bug/npm-audit-brace-expansion`, and push the branch to `origin`. Write `<EVID>/other/branch-head.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and the pushed head SHA in `Output Summary:`.
  - Acceptance: push exits 0; head SHA recorded.
- [ ] [P6-T2] Committed change-set verification: after the P6-T1 push, run `git diff --name-only main...HEAD` in the repository root and assert (a) no listed path starts with `packages/mcp-server`; (b) after excluding paths under `docs/features/` and `artifacts/orchestration/`, the remaining set is exactly the four files `package.json`, `package-lock.json`, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`. Write `<EVID>/other/committed-change-set-assertion.<ts>.md` with the full schema fields and the complete diff file list.
  - Acceptance: both assertions hold on the pushed head (satisfies the spec ACs on the four-file change set and untouched `packages/mcp-server`, which are worded against `git diff --name-only main...HEAD`).
- [ ] [P6-T3] Trigger and verify the `NPM Audit Gate` workflow on the pushed head SHA. Neither audit-gate entry point fires on a feature-branch push: `.github/workflows/npm-audit-gate.yml` triggers on `schedule`, `workflow_dispatch`, and `pull_request` restricted to `main`/`development`, and `.github/workflows/ci.yml` (the only other caller of `_npm-audit-gate.yml`) restricts both `push` and `pull_request` to `main`/`development`. Because this plan opens no pull request, the run must be dispatched explicitly. Run `gh workflow run npm-audit-gate.yml --ref bug/npm-audit-brace-expansion`, then identify the resulting run with `gh run list --workflow npm-audit-gate.yml --branch bug/npm-audit-brace-expansion --limit 5`, wait for it to complete, and confirm via `gh run view <run-id> --json headSha,jobs,url` that all three matrix legs — `npm audit (.)`, `npm audit (extensions/drm-copilot)`, `npm audit (packages/mcp-server)` (reported as `npm-audit / npm audit (<manifest>)` in the jobs JSON because the gate is invoked through the reusable-workflow caller) — conclude `success`. Write `<EVID>/qa-gates/npm-audit-gate-ci.<ts>.md` with the full schema fields, the dispatch command, the run URL, the run's `headSha`, and the per-job conclusions.
  - Acceptance: the run's `headSha` equals the head SHA recorded in P6-T1, and all three legs conclude `success` (satisfies the spec AC "All three `NPM Audit Gate` jobs succeed on the branch head SHA, verified via `gh run view <id> --json jobs`"). This dispatched run is early evidence; the orchestrator additionally verifies the gate on the pull-request head at its S9 CI gate, where `ci.yml` reports the legs as `NPM Audit Gate / npm audit (<manifest>)`.
- [ ] [P6-T4] Substitute verification of mocha's forced `minimatch` consumption path (required because root `test:integration` cannot execute and no workflow in this repository invokes it, so mocha's `minimatch` call site is otherwise unexercised). Against the regenerated root tree, resolve `minimatch` from mocha's own resolution root and exercise a brace-containing pattern with the options mocha passes, for example:
    `node -e "const path=require('node:path');const mochaDir=path.dirname(require.resolve('mocha/package.json'));const p=require.resolve('minimatch',{paths:[mochaDir]});const {minimatch}=require(p);console.log(require(path.join(path.dirname(p),'..','package.json')).version, minimatch('a/b.ts','**/*.{ts,js}',{dot:true,windowsPathsNoEscape:true}));"`
  Write `<EVID>/regression-testing/mocha-minimatch-brace-path.<ts>.md` with the full schema fields and the verbatim output.
  - Acceptance: `EXIT_CODE: 0`; the resolved `minimatch` version is `10.x`; the brace-containing pattern returns `true` without a `TypeError`. This is the direct negative control for the brace-expansion-only failure mode (`minimatch@9.0.9` would throw here), and it is the plan's evidence that the forced 9->10 bump is safe for the mocha consumer.
- [ ] [P6-T5] Check off each satisfied acceptance criterion in `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/spec.md` (`## Acceptance Criteria`, the sole AC source for full-bug mode) per `.claude/skills/acceptance-criteria-tracking/SKILL.md`: one at a time, evidence-before-check-off, text preserved, unmet items left unchecked. Write the AC Status Summary block to `<EVID>/other/ac-status-summary.<ts>.md` (source, total, checked, remaining).
  - Acceptance: every AC with passing evidence from Phases 1–6 is `[x]` in `spec.md`; the summary artifact exists and matches the file state.
- [ ] [P6-T6] Record the pre-existing conditions for separate filing: write `<EVID>/other/preexisting-defects-for-filing.<ts>.md` documenting (1) the root `format:check` fixture-formatting failure on `main` (Prettier flags `tests/fixtures/discovery_schemas/v1/runtime-characterization-scenario.invalid.json` and `tests/fixtures/discovery_schemas/v1/unspecified-behavior-record.invalid.json`, byte-identical to `origin/main`); (2) the missing root `.vscode-test.{json,js,cjs,mjs}` configuration making `test:integration` unrunnable; (3) the jest `.claude`-path glob-escape artifact affecting worktrees under dot-directories (jest coverage scripts report `No tests found` under such paths). For each condition, record its evidence path, its verified pre-existing status, and a note that it is out of scope for #414.
  - Acceptance: the artifact exists listing all three conditions with evidence paths, verified pre-existing status, and out-of-scope notes.
