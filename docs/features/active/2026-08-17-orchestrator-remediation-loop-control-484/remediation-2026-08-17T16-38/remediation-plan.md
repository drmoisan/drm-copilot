# Issue #484 P6-T26 and P6-T28 Performance Remediation Plan

- **Issue:** #484
- **Owner:** atomic-planner-c4
- **Last Updated:** 2026-08-23T05-35
- **Status:** Revision Prepared for Post-Revision Preflight
- **Authoritative Input:** `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/remediation-2026-08-17T16-38/remediation-inputs.md`

The original plan currently has `151/184` tasks complete. P6-T26 and P6-T27 are checked, P6-T28 is the first unchecked task, and Phase 4 is limited to the reproduced P6-T28 performance blocker. All exact P6-T26 and P6-T28 benchmark contracts elsewhere in this plan remain unchanged, and no completed original-plan task is replayed.

The completed Python remediation remains unchanged. Phase 4 adds only the newly reproduced original P6-T28 TypeScript performance blocker: p95 `0.3468000000000018 ms` against baseline p95 `0.23170000000000357 ms`, ratio `1.496763055675427`, with a confirmation ratio of `1.6849374190763644`. The bounded TypeScript behavior scope is `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-topology.ts`, `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`, `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-topology.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts`; the separate mechanical identity batch may update only the fixed expected bundle-digest literal in `tests/scripts/dev_tools/test_mcp_bundle_parity.py`. The recorded former P4-T17 full-coverage gate produced `3` failed suites and `5` failed tests while `210` suites and `2911` tests passed, exposing stale baseline expectations in exactly `extensions/drm-copilot/test/mcp-plan-gate-warning-projection.test.ts`, `extensions/drm-copilot/test/lib/validate/epic-orchestrator-state-codex-topology.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts`; a separate `0`-production/`3`-test compatibility batch may update only six stale expected-value occurrences across five failing tests to current HEAD production behavior plus blank-line-only compaction required to bring the existing `508`-line orchestration-artifacts test within the `500`-line policy. The live planning snapshot contains `71` topology receipts over `10` unique resolver inputs and `71` model-routing receipts over `5` unique resolver inputs. Phase 4 must confirm that multiplicity through fail-before profiling before implementing local, per-validation, successful-result-only resolver caches. Original P6-T28 remains unchecked until a separately routed independent executor reruns its exact command and records a ratio at or below `1.10`.

The checkpoint is live orchestration state and may legitimately append routing receipts between phases. Every benchmark must therefore record the checkpoint SHA-256, byte size, topology receipt total/unique counts, and model-routing receipt total/unique counts at execution time; this plan does not require checkpoint identity to remain unchanged. The historical pre-rebase baseline artifacts remain provenance only. Phase 0 recaptures the current post-rebase Python baseline before any production edit. If the Phase 0 fail-before benchmark already has a ratio at or below `1.10`, execution stops automatically before Phase 2, records a no-candidate result, and returns control to the orchestrator without a production change.

P2-T16 and P2-T17 are complete and must not be replayed. At the current restored-candidate boundary, Black and Ruff are clean, full Pyright reports `0` errors and `0` warnings, the focused receipt-validator suite reports `28` passed, and the full coverage-enabled suite reports `4469` passed, `5` skipped, and `0` failed with `91.778389%` repository line coverage and `83.730887%` repository branch coverage. The exact fastest two-validator candidate still failed at p50 `0.5461000837385654 ms`, p95 `0.6163000361993909 ms`, and ratio `1.214384253583986`. Five read-only zero-cost-validator p95 rounds were `0.5628999788314104`, `0.5464999703690410`, `0.5341999931260943`, `0.5469000898301601`, and `0.5874999333173037 ms`; their median `0.5469000898301601 ms` leaves only `0.011349935084581375 ms` below the unchanged maximum accepted p95 `0.5582500249147415 ms`, and two rounds failed even with both existing scoped validators replaced in-process by zero-cost functions. The evidence at `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-two-validator-scope-lower-bound.md` therefore authorizes exactly two additional production hot paths and their two tests before another full Phase 3 restart: skip `validate_route_membership` evaluation for non-strict validation, and cache successful duplicate `compute_complexity_floor` results per complexity-validation invocation. The current two-receipt-validator candidate, all completed compatibility projections, generated surfaces, host boundaries, tests, assertions, and receipt semantics remain in scope and unchanged.

### Phase 0 — Policy and Remediation Baseline

- [x] [P0-T1] Read `AGENTS.md`, `.agents/skills/general-code-change/SKILL.md`, `.agents/skills/general-unit-test/SKILL.md`, `.agents/skills/quality-tiers/SKILL.md`, `.agents/skills/python/SKILL.md`, and `.agents/skills/python-suppressions/SKILL.md` in that order; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and the explicit files read, and accept only when no policy file was modified.
- [x] [P0-T2] Reconcile the pre-rebase artifacts `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-black.txt`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-ruff.txt`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-pyright.txt`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-pytest-coverage.txt`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-coverage-analysis.txt`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-coverage-analysis.json`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/python-validator-performance.txt`, and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/python-validator-performance.txt`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/pre-rebase-provenance.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` classifying their Black, Ruff, Pyright, Pytest, coverage-policy, repository coverage, and affected-module values as `HISTORICAL PRE-REBASE ONLY`, while preserving p95 `0.507500022649765 ms` as the approved historical comparison value. Record the post-rebase intake anchors HEAD `25c5cad63bc8bfd8bd0e6d90a3bdb68674747cde`, `origin/main` and merge base `bee15c0660d382ed74c642d2e028fd136051046f`, the clean tracked worktree observed at intake, original plan state `149/184`, and first unchecked task `[P6-T26]`; also record that main's delta from pre-rebase merge base `768e485ddf3b48b16aa7588a72709e17568ee5f5` added `23` Python files and modified `27` Python files, so none of the historical command counts or coverage values is a current baseline.
- [x] [P0-T3] Run `poetry run python --version`, `poetry --version`, `node --version`, and `npm --version`; inspect `packages/mcp-server/package.json`, `packages/mcp-server/package-lock.json`, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`, `.codex/config.toml`, and `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/config.toml`; then snapshot `artifacts/orchestration/orchestrator-state.json` with SHA-256, byte size, topology receipt total/unique counts using the P2-T4 key, and model-routing receipt total/unique counts using the P2-T5 key. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/post-rebase-identity.md` with `Timestamp:`, each `Command:`, each `EXIT_CODE:`, and `Output Summary:`; accept only Python `3.13.12`, Poetry `2.3.2`, Node `24.14.0`, npm `11.9.0`, current local MCP/extension/config identity `1.0.27`, and live receipt totals no lower than the independently validated pre-planner observation of topology `42` total/`2` unique and model routing `42` total/`5` unique. Record the captured live totals as the Phase 0 receipt floor for P1-T1, and classify immutable published `@danmoisan/drm-copilot-mcp@1.0.24` only as the intentional negative `EXTERNAL_RUNTIME` fixture rather than a positive local identity.
- [x] [P0-T4] Run `poetry run black --check scripts tests` before any production edit and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-black.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` with the current post-rebase result and file count; accept when the command completes and the result is recorded without substituting a pre-rebase count.
- [x] [P0-T5] Run `poetry run ruff check scripts tests` before any production edit and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-ruff.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` with the current post-rebase diagnostic count; accept when the command completes and the result is recorded without substituting a pre-rebase count.
- [x] [P0-T6] Run `poetry run pyright` before any production edit and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-pyright.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` with current post-rebase error and warning counts; accept when the command completes and the live counts are recorded as the comparison baseline without claiming a nonzero result is clean.
- [x] [P0-T7] Set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/.coverage`, then run `poetry run pytest -o "addopts=" --cov=src --cov=scripts/dev_tools --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage.json` before any production edit; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-pytest-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, current passed/failed/skipped counts, numeric repository line/branch coverage, and numeric line/branch coverage for `scripts/dev_tools/_orchestrator_state_codex_topology.py`, `scripts/dev_tools/_orchestrator_state_codex_model_routing.py`, and `scripts/dev_tools/analyze_coverage_policy.py`; accept only when all numeric current post-rebase values are available.
- [x] [P0-T8] Run `poetry run python -m scripts.dev_tools.analyze_coverage_policy --mode baseline --language python --coverage-json docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage.json --base-ref HEAD --working-tree --coverage-config pyproject.toml --repo-line-min 85 --repo-branch-min 75 --new-symbol-min 90 --require-configured-changed-files --require-no-changed-line-regression --output docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage-analysis.json`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage-analysis.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, numeric repository line/branch coverage, per-new-symbol values, configured changed-file values, changed-line values, and the current post-rebase baseline verdict, accepting the captured verdict as baseline evidence without replacing it with the historical analyzer result.
- [x] [P0-T9] Run the existing analyzer test command `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py` before any production edit and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/coverage-analyzer-tests.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; accept only exit code `0` and record the current test counts.
- [x] [P0-T10] Set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/.focused-coverage`, then run `poetry run pytest -o "addopts=" tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py tests/scripts/dev_tools/test_analyze_coverage_policy.py --cov=scripts.dev_tools._orchestrator_state_codex_topology --cov=scripts.dev_tools._orchestrator_state_codex_model_routing --cov=scripts.dev_tools.analyze_coverage_policy --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/focused-python-coverage.json`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/focused-python-tests-coverage.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, passed/failed counts, and numeric per-module line/branch coverage for all three affected production modules, and accept only exit code `0`.
- [x] [P0-T11] Immediately before the exact performance command, resnapshot `artifacts/orchestration/orchestrator-state.json` with SHA-256, byte size, topology receipt total/unique counts, and model-routing receipt total/unique counts, then run the unchanged exact P6-T26 command `poetry run python -c "from pathlib import Path; from timeit import repeat; from scripts.dev_tools.validate_orchestrator_state import validate_orchestrator_state_text as v; s=Path('artifacts/orchestration/orchestrator-state.json').read_text(encoding='utf-8'); [v(s) for _ in range(100)]; a=sorted(x*1000 for x in repeat(lambda:v(s),number=1,repeat=100)); print({'samples':len(a),'p50_ms':a[49],'p95_ms':a[94]})"`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/p6-t26-exact-benchmark.md` with `Timestamp:`, each exact `Command:`, each `EXIT_CODE:`, `Output Summary:`, checkpoint SHA-256/bytes/receipt counts, exactly `100` timed samples after `100` warm-ups, numeric baseline p95 `0.507500022649765 ms`, numeric post p50/p95, and ratio. If the ratio is above `1.10`, accept it only as the expected fail-before baseline and continue to Phase 1; if the ratio is at or below `1.10`, stop automatically before production implementation, write a no-candidate result to the same artifact, leave original P6-T26 unchecked, and return control to the orchestrator.

### Phase 1 — Hot-Path Diagnosis

- [x] [P1-T1] Run `poetry run python -c "import cProfile,hashlib,json,pstats; from collections import Counter; from pathlib import Path; from scripts.dev_tools.validate_orchestrator_state import validate_orchestrator_state_text as v; b=Path('artifacts/orchestration/orchestrator-state.json').read_bytes(); s=b.decode('utf-8'); d=json.loads(s); t=d['codex_topology_receipts']; m=d['codex_model_routing_receipts']; tk=lambda r:(tuple(r['languages']),r['production_file_count'],r['test_file_count'],r['execution_context'],r['cross_cutting'],r['root_persona']); mk=lambda r:(str(r['logical_agent']),str(r['complexity_band']),str(r['execution_context']),str(r['orchestration_complexity_ceiling'])); p=cProfile.Profile(); p.enable(); [v(s) for _ in range(1000)]; p.disable(); print({'checkpoint_sha256':hashlib.sha256(b).hexdigest().upper(),'checkpoint_bytes':len(b),'topology_total':len(t),'topology_unique':len(Counter(tk(r) for r in t)),'model_total':len(m),'model_unique':len(Counter(mk(r) for r in m))}); pstats.Stats(p).strip_dirs().sort_stats('cumulative').print_stats('resolve_codex')"`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-hot-path-diagnosis.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, checkpoint SHA-256/bytes, cumulative timings, total/unique counts, and both `resolve_codex_topology` and `resolve_codex_deployment` profiler call paths. Accept only exit code `0`, a structurally valid checkpoint, live topology/model totals each at least its corresponding Phase 0 total recorded by P0-T3, and live topology/model unique counts each at least its corresponding P0-T3 snapshot count (currently `3`/`5`); additional unique keys are permitted only when structurally valid and total/unique receipt counts remain monotonic. A different SHA-256 or larger byte size is permitted only under those monotonic conditions, while any total or unique count below the Phase 0 snapshot stops remediation-required as checkpoint-receipt truncation.

### Phase 2 — Minimal Optimization and Regression Coverage

- [x] [P2-T1] Add `test_duplicate_topology_inputs_resolve_once_per_validation` to `tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py`; use `monkeypatch` and in-memory duplicate valid receipts with distinct phases to assert `validate_codex_topology_receipts` preserves an empty error list while calling the module-local `resolve_codex_topology` exactly once for the shared immutable input tuple, with no timing assertion, file I/O, temporary file, sleep, external process, suppression, or dependency.
- [x] [P2-T2] Add `test_duplicate_codex_model_inputs_resolve_once_per_validation` to `tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py`; use `monkeypatch` and in-memory duplicate valid receipts with distinct phases to assert `validate_codex_model_routing_receipts` preserves an empty error list while calling the module-local `resolve_codex_deployment` exactly once for the shared immutable input tuple, with no timing assertion, file I/O, temporary file, sleep, external process, suppression, or dependency.
- [x] [P2-T3] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py::test_duplicate_topology_inputs_resolve_once_per_validation tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py::test_duplicate_codex_model_inputs_resolve_once_per_validation` before production edits and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-cache-fail-before.md` with `Timestamp:`, `Command:`, the nonzero `EXIT_CODE:`, and `Output Summary:` proving both current validators resolve duplicate inputs more than once; do not commit this failing intermediate state.
- [x] [P2-T4] Update only `scripts/dev_tools/_orchestrator_state_codex_topology.py` so `validate_codex_topology_receipts` creates a local per-invocation dictionary keyed by `(tuple(languages), production_file_count, test_file_count, execution_context, cross_cutting, root_persona)` and reuses only successful `resolve_codex_topology` results for duplicate valid inputs; preserve per-receipt structural checks, phase checks, resolved-field comparisons, ordered index-specific diagnostics, resolver `ValueError` behavior, public signatures, and cross-invocation freshness, and accept only when the file remains at or below `500` lines.
- [x] [P2-T5] Update only `scripts/dev_tools/_orchestrator_state_codex_model_routing.py` so `validate_codex_model_routing_receipts` creates a local per-invocation dictionary keyed by the exact normalized `(logical_agent, complexity_band, execution_context, orchestration_complexity_ceiling)` strings and reuses only successful `resolve_codex_deployment` results for duplicate valid inputs; preserve every per-receipt required-field/model comparison, monotonic-ceiling and transition check, ordered index-specific diagnostic, resolver `ValueError` behavior, public signature, and cross-invocation freshness, and accept only when the file remains at or below `500` lines.
- [x] [P2-T6] Compact only `scripts/dev_tools/analyze_coverage_policy.py` behavior-neutrally from its current `503` lines to at most `500` lines without changing analyzer behavior, public interfaces, diagnostics, dependencies, suppressions, thresholds, or tests; run the existing exact analyzer test command `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-coverage-analyzer-compaction.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, before/after line counts, and changed paths, accepting only exit code `0`, the production file at or below `500` lines, and no analyzer test modification required for changed behavior.
- [x] [P2-T7] Set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/.coverage`, then run `poetry run pytest -o "addopts=" tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py tests/scripts/dev_tools/test_analyze_coverage_policy.py --cov=scripts.dev_tools._orchestrator_state_codex_topology --cov=scripts.dev_tools._orchestrator_state_codex_model_routing --cov=scripts.dev_tools.analyze_coverage_policy --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-focused-coverage.json`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-focused-regression.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, numeric baseline/post per-module line and branch coverage, and changed-line coverage for P2-T4 through P2-T6; accept only exit code `0`, all focused tests passing, every changed line covered, changed-code coverage at or above `90%`, and no per-module coverage regression from P0-T10.
- [x] [P2-T8] Apply type-only corrections solely within already in-scope `tests/scripts/dev_tools/test_analyze_coverage_policy.py` for the `10` baseline Pyright errors reported at lines `288-290`, `307`, `382-383`, `410`, and `417-418`, limited to unknown iteration types, object indexing, and the invariant list argument. Do not add a suppression, weaken or remove an assertion, alter fixtures, expected values, runtime behavior, coverage behavior, analyzer behavior, dependencies, benchmarks, or any unrelated code. Run `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py`, then run the exact focused command `poetry run pyright scripts/dev_tools/_orchestrator_state_codex_topology.py scripts/dev_tools/_orchestrator_state_codex_model_routing.py scripts/dev_tools/analyze_coverage_policy.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py tests/scripts/dev_tools/test_analyze_coverage_policy.py`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-analyzer-test-typing.md` with `Timestamp:`, both exact `Command:` values, both `EXIT_CODE:` values, `Output Summary:`, the corrected diagnostic locations, and exact changed paths. Accept only when the existing analyzer tests remain green, focused Pyright reports `0` errors and `0` warnings, the code diff is confined to type-only corrections in the named test file, and no suppression or behavior/assertion weakening is present; this completed focused result does not replace the full-repository zero-error gates in P2-T17, P2-T25, and P3-T3.
- [x] [P2-T9] Reconcile the failed P3-T4 evidence without editing production, test, generated, checkpoint, or index state: inspect `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-pytest-coverage.md`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage.json`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-pytest-coverage.md`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-coverage.json`, `scripts/dev_tools/generate_orchestration_customization_surfaces.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, and `scripts/dev_tools/synchronize_customization_bundles.py`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-p3-t4-failure-reconciliation.md` with `Timestamp:`, each inspected `Command:`, each `EXIT_CODE:`, and `Output Summary:` recording the exact P0/P3 counts and coverage values from the introduction, the exact `13`/`14`/`3` failure partition, and the ownership map. Accept only when the map records that `generate_orchestration_customization_surfaces.py` owns `.github/agents/orchestrator.agent.md`, `.github/prompts/orchestrate-csharp-work.prompt.md`, `.claude/agents/orchestrator.md`, and their declared generated family; `generate_codex_agent_variants.py` owns `.codex/agents/feature-reviewer.toml`, `.codex/agents/orchestrator.toml`, generated deployment profiles, and Codex bundle copies; `synchronize_customization_bundles.py` owns mapped extension mirrors; and `.github/agents/python-orchestrator.agent.md`, `.github/agents/powershell-orchestrator.agent.md`, and `.github/agents/csharp-orchestrator.agent.md` remain distinct canonical root surfaces whose language-specific handoffs must be preserved while their extension mirrors remain byte-identical.
- [x] [P2-T10] Correct one Python batch limited to production file `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py` and test files `tests/scripts/dev_tools/test_parallel_drift_parity.py`, `tests/scripts/dev_tools/test_parallel_mutation_parity.py`, and `tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py`: derive the highest current-generation cohort occupied by every pinned/running item from the validated durable assignments, bind that value to the required keyword-only `highest_pinned_cohort` parameter at every `parallel_mutation_protocol.recolor_unstarted` call, and preserve the public API, stable item order, pinned assignments, deterministic diagnostics, input immutability, and existing assertion strength. Run the exact `14`-node command `poetry run pytest -o "addopts=" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_lifecycle_requirements_bind_to_executed_shared_cases" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[declared-files-remain-quiescent]" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[observed-file-escapes-declared-radius]" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[later-started-conflicting-peer-is-halted]" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[unstarted-subgraph-recolors-after-halt]" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[multiple-halts-requeue-in-item-order]" "tests/scripts/dev_tools/test_parallel_drift_parity.py::test_python_authorities_reproduce_every_expected_output[persisted-observed-radius-resolves-drift]" "tests/scripts/dev_tools/test_parallel_mutation_parity.py::test_python_authority_reproduces_each_fixture_decision[pinned-item-excluded-from-recolor]" "tests/scripts/dev_tools/test_parallel_mutation_parity.py::test_python_authority_reproduces_each_fixture_decision[pinned-item-illegally-recomputed]" "tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py::test_unresolved_drift_quiesces_admission_and_completion" "tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py::test_recolor_pins_every_running_item" "tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py::test_recolor_changes_only_the_unstarted_subgraph" "tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py::test_persisted_requeue_order_is_ascending" "tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py::test_errors_are_deterministic_and_input_is_immutable"`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-parallel-recolor-integration.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, changed paths, and the bound value derivation. Accept only exit code `0`, all `14` nodes passing, the `1` production/`3` test Python batch cap satisfied, every required binding present, and no public API, assertion, behavior, dependency, suppression, or unrelated change.
- [x] [P2-T11] Correct only `tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py` so the three existing `require_ready_for_execution=True` acceptance cases supply complete valid in-memory `ParallelCodexReadinessEvidence` bound to the same prepared planner state and canonical routing/launch identities; call `validate_parallel_planner_state_text` with that evidence, preserve concurrency values `1`, `4`, and `32`, and do not relax, bypass, mock away, or change the production ready gate or any assertion. Run the exact `3`-node command `poetry run pytest -o "addopts=" "tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py::test_invariant_p2_accepts_in_range_concurrency_under_the_ready_gate[1]" "tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py::test_invariant_p2_accepts_in_range_concurrency_under_the_ready_gate[4]" "tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py::test_invariant_p2_accepts_in_range_concurrency_under_the_ready_gate[32]"`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-parallel-ready-gate-fixtures.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, and changed paths. Accept only exit code `0`, all `3` nodes passing through the unchanged ready gate, a `0` production/`1` test Python batch, and no behavior, assertion, dependency, suppression, temporary-file, external-process, or unrelated change.
- [x] [P2-T12] Regenerate the generic and Codex orchestration surfaces only through their recorded owners in this exact order: `poetry run python -m scripts.dev_tools.generate_orchestration_customization_surfaces`, `poetry run python -m scripts.dev_tools.generate_codex_agent_variants`, then `poetry run python -m scripts.dev_tools.synchronize_customization_bundles`; do not manually edit any output owned by those commands. Run `poetry run pytest tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_synchronize_customization_bundles.py`, then run the three corresponding `--check` commands in the same order. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-generated-customization-regeneration.md` with `Timestamp:`, every exact `Command:`, every `EXIT_CODE:`, `Output Summary:`, canonical source hashes, and exact mechanically generated root/bundle paths. Accept only when all commands exit `0`, `.codex/agents/feature-reviewer.toml`, `.codex/agents/orchestrator.toml`, `.github/agents/orchestrator.agent.md`, `.github/prompts/orchestrate-csharp-work.prompt.md`, `.claude/agents/orchestrator.md`, and their owner-declared root/extension outputs are current and deterministic, and no generated copy was treated as an independent manual implementation file.
- [x] [P2-T13] Synchronize the three language-specific GitHub orchestrators in one bounded ownership batch: update only canonical roots `.github/agents/python-orchestrator.agent.md`, `.github/agents/powershell-orchestrator.agent.md`, and `.github/agents/csharp-orchestrator.agent.md` with the current direct extension-command contract (`drmCopilotExtension.newPotentialEntry`, `drmCopilotExtension.newPotentialBugEntry`, `drmCopilotExtension.potentialToIssue`, and `drmCopilotExtension.newActiveFeatureFolder` with explicit work modes and issue number), preserving every language-specific lifecycle, handoff, budget, state-machine, and minor-audit clause; update Python production file `scripts/dev_tools/synchronize_customization_bundles.py` and test file `tests/scripts/dev_tools/test_synchronize_customization_bundles.py` so those roots canonically map to `extensions/drm-copilot/resources/customizations/.github/agents/python-orchestrator.agent.md`, `extensions/drm-copilot/resources/customizations/.github/agents/powershell-orchestrator.agent.md`, and `extensions/drm-copilot/resources/customizations/.github/agents/csharp-orchestrator.agent.md`, extending the existing exact mapping-inventory assertion without relaxing exact-set membership. Run `poetry run python -m scripts.dev_tools.synchronize_customization_bundles`, `poetry run python -m scripts.dev_tools.synchronize_customization_bundles --check`, and `poetry run pytest tests/scripts/dev_tools/test_synchronize_customization_bundles.py`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-language-orchestrator-sync.md` with `Timestamp:`, each exact `Command:`, each `EXIT_CODE:`, `Output Summary:`, changed canonical roots, generated mirrors, and root/mirror SHA-256 pairs. Accept only when all commands exit `0`, every pair is byte-identical, the Python batch is `1` production/`1` test, the three extension files are mechanical synchronizer outputs rather than manual implementation files, and no language-specific behavior, assertion, dependency, suppression, or unrelated surface changes.
- [x] [P2-T14] Correct the `13` failed customization contracts in one deterministic compatibility-projection batch. Add behavior-neutral exact wording to canonical `.agents/skills/feature-review/SKILL.md` so its existing automatic remediation handoff includes `Then automatically delegate remediation planning to the atomic-planner agent`, and to `.agents/skills/orchestrator-workflow/SKILL.md` so its existing delegation and placeholder guards include ``Treat `spawn_agent` availability as the mechanical availability signal`` and ``Do not persist placeholder lifecycle values such as `NONE`, `TBD``` without removing the stronger current rules. Update only Python production files `scripts/dev_tools/generate_codex_agent_variants.py` and `scripts/dev_tools/generate_orchestration_customization_surfaces.py`: the Codex reviewer/orchestrator projection must add `Canonical migration source:` and `Use the following repo-local skills as the canonical workflow source` while retaining all canonical source content; the applicable GitHub prompt projections must include the canonical Small Path/`minor-audit` lifecycle and explicit `## Acceptance Criteria` section rule; only `.github/agents/orchestrator.agent.md` may receive `drmCopilotExtension.newPotentialEntry`, `drmCopilotExtension.newPotentialBugEntry`, `drmCopilotExtension.potentialToIssue`, and `drmCopilotExtension.newActiveFeatureFolder` with explicit work modes and issue-number semantics; and only `.claude/agents/orchestrator.md` may receive `delegation_receipts.promotion.potential_entry`, `delegation_receipts.promotion.issue`, and `delegation_receipts.promotion.feature_folder`. Do not place extension command identifiers or raw host receipt namespaces in Codex/MCP-only payloads, edit any test assertion, or manually edit an owned generated output. For the `2` production/`0` test Python batch, with canonical Markdown inputs and mechanically generated outputs excluded from independent Python implementation-file counts, run one clean `poetry run black scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` → `poetry run ruff check scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` → `poetry run pyright scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` → `poetry run pytest tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` pass, restarting that local sequence at Black if any stage fails or Black mutates files; then regenerate in exact order with `poetry run python -m scripts.dev_tools.generate_orchestration_customization_surfaces`, `poetry run python -m scripts.dev_tools.generate_codex_agent_variants`, and `poetry run python -m scripts.dev_tools.synchronize_customization_bundles`, followed by the three corresponding `--check` commands in the same order. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-customization-compatibility-projection.md` with `Timestamp:`, every exact `Command:`, every `EXIT_CODE:`, `Output Summary:`, canonical source hashes, changed Python/canonical Markdown paths, mechanically generated paths, and host-boundary checks. Accept only when every command exits `0`, both generator scripts and both unchanged generator tests remain at or below `500` lines, the Python cap is satisfied, no assertion/suppression/dependency/public behavior is weakened, generated outputs are deterministic owner products, and no host-specific identifier crosses its authorized projection.
- [x] [P2-T15] Preserve completed P2-T14 and the failed safe-suite evidence at `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-customization-contract-regression.md`, then correct only Python production files `scripts/dev_tools/generate_orchestration_customization_surfaces.py` and `scripts/dev_tools/generate_codex_agent_variants.py` in one `2` production/`0` test batch. Project every unchanged `WRAPPER_REQUIRED_FRAGMENTS` literal from `tests/scripts/dev_tools/test_codex_full_migration_inventory.py` into generated `.codex/agents/feature-review.toml` and `.codex/agents/task-researcher.toml` through their owners, including the appropriate `.github/agents/feature-review.agent.md` and `.github/agents/task-researcher.agent.md` canonical migration source, without editing that test or manually editing either generated TOML. Project exact `stop and report blocked state` wording into the generated Codex orchestrator contract, and project the exact guard phrase ``Do not create or edit `${feature-folder}/issue.md`, `${feature-folder}/spec.md`, `${feature-folder}/user-story.md`, or `plan*.md` until`` into the base and routed Codex orchestrator family, without changing the stronger canonical lifecycle rules. Preserve P2-T14 host isolation, direct-command/receipt ownership, canonical source content, output inventory, public generator interfaces, assertions, dependencies, suppressions, and unrelated projections; compact only unchanged generator implementation structure as necessary so both generator files and both unchanged generator test files remain at or below `500` lines. Run one clean `poetry run black scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` -> `poetry run ruff check scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` -> `poetry run pyright scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` -> `poetry run pytest tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` sequence, restarting at Black if any stage fails or Black mutates files; regenerate only through owners in exact order with `poetry run python -m scripts.dev_tools.generate_orchestration_customization_surfaces`, `poetry run python -m scripts.dev_tools.generate_codex_agent_variants`, and `poetry run python -m scripts.dev_tools.synchronize_customization_bundles`, then run the three corresponding `--check` commands in the same order. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-customization-contract-projection-repair.md` with `Timestamp:`, every exact `Command:`, every `EXIT_CODE:`, `Output Summary:`, exact changed generator paths, mechanically generated paths, all four Python line counts, strict-wrapper/orchestrator literal checks, host-boundary checks, and root/mirror SHA-256 pairs. Accept only when every command exits `0`, all three exact failed contracts are present in their owner-generated projections, both generators and their unchanged tests are at or below `500` lines, generated outputs are deterministic and byte-identical to their mirrors, no assertion or literal contract was changed or weakened, and no host-specific identifier crosses its P2-T14-authorized projection.
- [x] [P2-T16] Preserve the failed P2-T15 evidence at `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-customization-contract-regression.md`, then avoid the promotion-command PreToolUse false positive by running the safe file-level superset `poetry run pytest -o "addopts=" tests/scripts/dev_tools/test_codex_agent_wrapper_contracts.py tests/scripts/dev_tools/test_codex_full_migration_inventory.py tests/scripts/dev_tools/test_codex_handoff_contract_parity.py tests/scripts/dev_tools/test_codex_orchestration_contracts.py tests/scripts/dev_tools/test_csharp_orchestration_contracts.py tests/scripts/dev_tools/test_minor_audit_acceptance_criteria_contracts.py tests/scripts/dev_tools/test_orchestration_guardrail_contracts.py tests/scripts/dev_tools/test_orchestrator_direct_command_contracts.py`; do not use temporary files, raw command fragments as pytest selectors, selection bypasses, or assertion changes. Then run `poetry run python -m scripts.dev_tools.generate_orchestration_customization_surfaces --check`, `poetry run python -m scripts.dev_tools.generate_codex_agent_variants --check`, and `poetry run python -m scripts.dev_tools.synchronize_customization_bundles --check`. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-customization-contract-regression-pass-after.md` with `Timestamp:`, every exact `Command:`, every `EXIT_CODE:`, `Output Summary:`, full file-level totals, owner-check results, and an explicit PASS row for each of the `13` exact nodes: `test_feature_reviewer_wrapper_preserves_codex_remediation_handoff`, `test_orchestrator_wrapper_preserves_codex_mandatory_delegation_contract`, `test_codex_agent_contracts_follow_expected_wrapper_or_native_patterns`, `test_feature_review_remediation_handoff_is_strict_in_skill_and_agent`, `test_codex_orchestrator_agent_requires_mandatory_specialist_handoffs`, `test_orchestrate_csharp_work_prompt_requires_minor_audit_lifecycle`, `test_minor_audit_contract_files_require_explicit_acceptance_section`, `test_codex_orchestrators_enforce_checkpoint_and_lifecycle_guardrails`, `test_claude_orchestrator_documents_promotion_receipt_namespace`, `test_root_orchestrators_use_direct_potential_entry_commands`, `test_root_orchestrators_use_direct_potential_to_issue_commands`, `test_root_orchestrators_use_direct_new_active_feature_folder_commands`, and `test_mirrored_orchestrator_agents_match_root_direct_command_contracts`. Accept only when the file-level suite exits `0` with all `41` nodes passing, all owner checks exit `0`, all `13` named nodes explicitly pass, root/mirror equality holds, and no generated output or contract assertion was weakened or manually diverged.
- [x] [P2-T17] Run full-repository `poetry run pyright` after P2-T10 through P2-T16 and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-full-pyright.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, error count, warning count, and changed Python paths. Accept only exit code `0`, `0` errors, `0` warnings, no suppression, and no error-floor comparison; Phase 3 must not start until this full-repository gate is clean.

The additional hot-path correction is one bounded Python ownership scope of exactly `2` production files and `2` test files: `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/_orchestrator_state_complexity.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py`, and `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py`. P2-T18 through P2-T28 must not edit any other production or test path, must not alter `artifacts/orchestration/orchestrator-state.json`, and must preserve the completed P2-T1 through P2-T17 candidate. Any failure or mutation after P2-T23 restarts the ordered local Black → Ruff → full-repository Pyright → focused coverage → full coverage/policy → read-only profile sequence at P2-T23.

- [x] [P2-T18] Add only `test_non_strict_route_membership_skips_validation` and `test_strict_route_membership_invokes_validation_and_preserves_diagnostics` to `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py`. Use `monkeypatch` against the module-local `validate_route_membership` with in-memory checkpoint JSON: the non-strict test must assert zero calls and absence of the monkeypatched sentinel diagnostic, while the strict test must assert exactly one call and the unchanged ordered sentinel diagnostics in the returned error list. Preserve the existing real-matrix `test_strict_route_membership_rejects_unknown_route`, every completion diagnostic, all assertion strength, and the test file's public fixtures; do not use timing assertions, file I/O, temporary files, sleeps, external processes, suppressions, dependencies, or production edits in this task.
- [x] [P2-T19] Add only `test_duplicate_complexity_signals_compute_floor_once_and_preserve_index_diagnostics`, `test_complexity_floor_cache_is_fresh_per_validation`, and `test_invalid_complexity_signals_do_not_compute_floor_and_preserve_index_diagnostics` to `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py`. Use `monkeypatch` against the module-local `compute_complexity_floor` and in-memory assessments to prove that duplicate valid assessments with the exact same ordered `signals_present` tuple compute once while retaining ordered `#0` and `#1` floor diagnostics, two separate `_validate_complexity_assessments` invocations compute once each, and invalid non-string signal shapes never call the floor function while retaining each record's exact index-specific shape diagnostic. Preserve all existing band, floor, signal-shape, ordering, rationale, public-validator, and monotonic `band >= floor` assertions; do not use timing assertions, file I/O, temporary files, sleeps, external processes, suppressions, dependencies, or production edits in this task.
- [x] [P2-T20] [expect-fail] Before either production edit, run `poetry run pytest -o "addopts=" "tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py::test_non_strict_route_membership_skips_validation" "tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py::test_duplicate_complexity_signals_compute_floor_once_and_preserve_index_diagnostics"` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-fail-before.md` with `Timestamp:`, the exact `Command:`, `ExpectedExitCode: 1`, observed `EXIT_CODE:`, and `Output Summary:`. Accept only observed exit code `1` with exactly both named nodes failing for the intended reasons that non-strict validation invoked route membership and duplicate valid signal tuples invoked `compute_complexity_floor` more than once; stop if either node passes before production correction or fails for another reason, and do not stage or commit the failing intermediate state.
- [x] [P2-T21] Update only `scripts/dev_tools/validate_orchestrator_state.py` so `validate_orchestrator_state_text` evaluates `validate_route_membership(state_map)` only inside the existing `if strict_route_membership:` branch and extends errors with the returned diagnostics there. Preserve the default `strict_route_membership=False`, strict-mode call count and diagnostic order/content, `require_complete` behavior, every other validator invocation, public signatures, exception behavior, checkpoint input, receipt handling, and the existing current two-validator cache candidate; do not alter an assertion, threshold, suppression, dependency, or unrelated path, and accept only when the production file remains at or below `500` physical lines.
- [x] [P2-T22] Update only `scripts/dev_tools/_orchestrator_state_complexity.py` so `_validate_complexity_assessments` creates a local per-invocation cache of successful `compute_complexity_floor` results keyed by the exact ordered tuple of already validated `signals_present` strings and reuses a cached result only for a later valid assessment with the same tuple. Insert into the cache only after `compute_complexity_floor` returns successfully; invalid signal shapes must neither call nor populate it, separate validator invocations must use fresh caches, and exceptions must retain current propagation behavior. Preserve every record's ordered index-specific band, floor, signal-shape, ordering, and rationale diagnostics; monotonic `band >= floor` semantics; public signatures; dependencies; and the existing current two-validator cache candidate. Do not change a threshold, assertion, suppression, checkpoint/receipt rule, benchmark input, or unrelated path, and accept only when the production file remains at or below `500` physical lines.
- [x] [P2-T23] Run `poetry run black scripts/dev_tools/validate_orchestrator_state.py scripts/dev_tools/_orchestrator_state_complexity.py tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-black.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, exact changed paths, and all four physical line counts. Accept only exit code `0`, all four paths at or below `500` lines, and a final mutation-free Black rerun; if Black changes any file, clear P2-T23 through P2-T28 and restart this local sequence at P2-T23 after the mutation.
- [x] [P2-T24] After a mutation-free P2-T23, run `poetry run ruff check scripts/dev_tools/validate_orchestrator_state.py scripts/dev_tools/_orchestrator_state_complexity.py tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-ruff.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, and `Output Summary:`. Accept only exit code `0` with zero diagnostics and no suppression; otherwise clear P2-T23 through P2-T28, correct only the bounded four-file ownership scope, and restart at P2-T23.
- [x] [P2-T25] After P2-T24, run full-repository `poetry run pyright` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-pyright.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, error count, warning count, and explicit confirmation that both added production paths and both added test paths were included. Accept only exit code `0`, `0` errors, `0` warnings, and no suppression or narrowed file selection; otherwise clear P2-T23 through P2-T28, correct only the bounded four-file ownership scope, and restart at P2-T23.
- [x] [P2-T26] After P2-T25, set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/.additional-hot-paths-coverage`, then run `poetry run pytest -o "addopts=" tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py --cov=scripts.dev_tools.validate_orchestrator_state --cov=scripts.dev_tools._orchestrator_state_complexity --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-focused-coverage.json`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-focused-coverage.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, passed/failed counts, numeric line/branch coverage for both production modules, and explicit results for all five P2-T18/P2-T19 nodes. Accept only exit code `0`, all tests in both files passing, each new node passing, all changed executable lines in the two production files covered, and no assertion, diagnostic, public behavior, benchmark, receipt, threshold, or suppression weakening; otherwise clear P2-T23 through P2-T28 and restart at P2-T23.
- [x] [P2-T27] After P2-T26, set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/.expanded-full-coverage`, run `poetry run pytest -o "addopts=" --cov=src --cov=scripts/dev_tools --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-expanded-full-coverage.json`, and then run `poetry run python -m scripts.dev_tools.analyze_coverage_policy --mode final --language python --coverage-json docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-expanded-full-coverage.json --baseline-python-analysis docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage-analysis.json --base-ref HEAD --working-tree --coverage-config pyproject.toml --repo-line-min 85 --repo-branch-min 75 --new-symbol-min 90 --require-configured-changed-files --require-no-changed-line-regression --output docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-expanded-full-coverage-analysis.json`. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-expanded-full-coverage.md` and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-expanded-full-coverage-analysis.md`, each with `Timestamp:`, its exact `Command:`, `EXIT_CODE:`, and `Output Summary:`. Accept only when both commands exit `0`, the full suite has zero failures and collects every P2-T18/P2-T19 test, repository line coverage is at least `85%`, repository branch coverage is at least `75%`, every new production symbol is at least `90%`, configured changed-file and changed-line gates pass without regression, all changed executable lines in the two added production paths are covered, both added test files are collected without being counted as application coverage, and the existing topology/model-routing changed-line and module-coverage gates remain passing without threshold, assertion, input, receipt, or suppression weakening; otherwise clear P2-T23 through P2-T28 and restart at P2-T23.
- [x] [P2-T28] After P2-T27, run the read-only profile command `poetry run python -c "import cProfile,hashlib,json,pstats; from collections import Counter; from pathlib import Path; from scripts.dev_tools.validate_orchestrator_state import validate_orchestrator_state_text as v; b=Path('artifacts/orchestration/orchestrator-state.json').read_bytes(); s=b.decode('utf-8'); d=json.loads(s); a=d.get('complexity_assessments',[]); k=[tuple(x['signals_present']) for x in a if isinstance(x,dict) and isinstance(x.get('signals_present'),list) and all(isinstance(y,str) for y in x['signals_present'])]; t=d['codex_topology_receipts']; m=d['codex_model_routing_receipts']; tk=lambda r:(tuple(r['languages']),r['production_file_count'],r['test_file_count'],r['execution_context'],r['cross_cutting'],r['root_persona']); mk=lambda r:(str(r['logical_agent']),str(r['complexity_band']),str(r['execution_context']),str(r['orchestration_complexity_ceiling'])); p=cProfile.Profile(); p.enable(); [v(s) for _ in range(1000)]; p.disable(); print({'validation_errors':v(s),'checkpoint_sha256':hashlib.sha256(b).hexdigest().upper(),'checkpoint_bytes':len(b),'complexity_total':len(a),'complexity_unique_valid_signal_tuples':len(set(k)),'topology_total':len(t),'topology_unique':len(Counter(tk(r) for r in t)),'model_total':len(m),'model_unique':len(Counter(mk(r) for r in m))}); pstats.Stats(p).strip_dirs().sort_stats('cumulative').print_stats('_validate_complexity_assessments|compute_complexity_floor|validate_route_membership|load_routing_matrix')"` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t26-additional-hot-paths-profile.md` with `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, the checkpoint SHA-256/byte size/receipt counts, complexity total/unique tuple counts, and profiler call counts. Accept only exit code `0`, a structurally valid unchanged checkpoint, topology/model totals no lower than the P0-T3 floor, monotonic valid unique counts, zero non-strict calls to `validate_route_membership` and its route-membership matrix load, and no more successful `compute_complexity_floor` calls per validation than the number of unique valid ordered signal tuples; this task has no timing assertion and must not edit the checkpoint, index, production, tests, benchmark, evidence thresholds, receipts, or generated/customization surfaces. Then leave all P3-T1 through P3-T6 tasks unchecked and begin the mandatory full Phase 3 restart.

### Phase 3 — Exact Benchmark and Python QA

Global restart rule: P3-T1 through P3-T6 form one uninterrupted Black → Ruff → full-repository Pyright → existing analyzer tests plus full coverage-enabled Pytest and coverage-policy analysis → exact benchmark → final verification pass. The production/test scope throughout this phase includes every Python file changed by P2-T4 through P2-T28, including `scripts/dev_tools/analyze_coverage_policy.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, `scripts/dev_tools/generate_orchestration_customization_surfaces.py`, `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/_orchestrator_state_complexity.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py`, and all other applicable tests already named by this plan. The exact analyzer test command is `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py`. Any failed stage, formatter mutation, generator/synchronizer drift, or corrective production/test/generated-surface edit clears all P3-T1 through P3-T6 checkoffs and restarts at P3-T1. P3 compares against the live post-rebase Phase 0 results and does not present any pre-rebase command count or coverage value as current. The P2-T27 full-suite result is a pre-Phase-3 regression gate only and does not replace or shorten P3-T1 through P3-T6.

- [x] [P3-T1] Run `poetry run black scripts/dev_tools/_orchestrator_state_codex_topology.py scripts/dev_tools/_orchestrator_state_codex_model_routing.py scripts/dev_tools/validate_orchestrator_state.py scripts/dev_tools/_orchestrator_state_complexity.py scripts/dev_tools/analyze_coverage_policy.py scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py scripts/dev_tools/synchronize_customization_bundles.py scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py tests/scripts/dev_tools/test_analyze_coverage_policy.py tests/scripts/dev_tools/test_parallel_drift_parity.py tests/scripts/dev_tools/test_parallel_mutation_parity.py tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py tests/scripts/dev_tools/test_synchronize_customization_bundles.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-black.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; accept only exit code `0`, all `21` named Python files at or below `500` physical lines, and a mutation-free pass, then clear all Phase 3 checkoffs and restart at P3-T1 if Black changed any file.
- [x] [P3-T2] After a mutation-free P3-T1, run `poetry run ruff check scripts/dev_tools/_orchestrator_state_codex_topology.py scripts/dev_tools/_orchestrator_state_codex_model_routing.py scripts/dev_tools/validate_orchestrator_state.py scripts/dev_tools/_orchestrator_state_complexity.py scripts/dev_tools/analyze_coverage_policy.py scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py scripts/dev_tools/synchronize_customization_bundles.py scripts/dev_tools/generate_codex_agent_variants.py scripts/dev_tools/generate_orchestration_customization_surfaces.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py tests/scripts/dev_tools/test_analyze_coverage_policy.py tests/scripts/dev_tools/test_parallel_drift_parity.py tests/scripts/dev_tools/test_parallel_mutation_parity.py tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py tests/scripts/dev_tools/test_synchronize_customization_bundles.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-ruff.md` containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; accept only exit code `0` and zero diagnostics across all `21` named Python files.
- [x] [P3-T3] After P3-T2, run full-repository `poetry run pyright` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-pyright.md` containing `Timestamp:`, the exact `Command:`, `EXIT_CODE:`, `Output Summary:`, and explicit inclusion of `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/_orchestrator_state_complexity.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py`, and `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py`; accept only exit code `0`, zero errors, and zero warnings, without narrowing the command to focused files or accepting the P0-T6 error floor.
- [x] [P3-T4] After P3-T3, run the exact analyzer command `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-coverage-analyzer-tests.md`; then set `COVERAGE_FILE` to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/.coverage` and run `poetry run pytest -o "addopts=" --cov=src --cov=scripts/dev_tools --cov-branch --cov-report=term-missing --cov-report=json:docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-coverage.json`, writing `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-pytest-coverage.md`; finally run `poetry run python -m scripts.dev_tools.analyze_coverage_policy --mode final --language python --coverage-json docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-coverage.json --baseline-python-analysis docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/remediation-baseline/python-coverage-analysis.json --base-ref HEAD --working-tree --coverage-config pyproject.toml --repo-line-min 85 --repo-branch-min 75 --new-symbol-min 90 --require-configured-changed-files --require-no-changed-line-regression --output docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-coverage-analysis.json`, writing `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-python-coverage-analysis.md`; each artifact must contain `Timestamp:`, exact `Command:`, `EXIT_CODE:`, and `Output Summary:`. Accept only when all three commands exit `0`, the full coverage-enabled suite has zero failures before P3-T5, repository line coverage is at least `85%`, repository branch coverage is at least `75%`, changed-code coverage is at least `90%`, every new symbol is at least `90%`, and configured changed-file and changed-line gates pass without regression. `scripts/dev_tools/_orchestrator_state_codex_topology.py`, `scripts/dev_tools/_orchestrator_state_codex_model_routing.py`, `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/_orchestrator_state_complexity.py`, `scripts/dev_tools/analyze_coverage_policy.py`, `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py`, `scripts/dev_tools/synchronize_customization_bundles.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, and `scripts/dev_tools/generate_orchestration_customization_surfaces.py` must have numeric line/branch coverage without module regression from applicable P0/P2 evidence; all changed executable lines must remain covered; both `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py` and `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py` must be collected with every P2-T18/P2-T19 node passing; and the changed generator code must remain exercised by `tests/scripts/dev_tools/test_generate_codex_agent_variants.py` and `tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py` without any assertion change.
- [x] [P3-T5] After P3-T4 proves `scripts/dev_tools/analyze_coverage_policy.py` with `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py`, immediately snapshot `artifacts/orchestration/orchestrator-state.json` with SHA-256, byte size, topology receipt total/unique counts, and model-routing receipt total/unique counts, then run the unchanged exact P6-T26 command `poetry run python -c "from pathlib import Path; from timeit import repeat; from scripts.dev_tools.validate_orchestrator_state import validate_orchestrator_state_text as v; s=Path('artifacts/orchestration/orchestrator-state.json').read_text(encoding='utf-8'); [v(s) for _ in range(100)]; a=sorted(x*1000 for x in repeat(lambda:v(s),number=1,repeat=100)); print({'samples':len(a),'p50_ms':a[49],'p95_ms':a[94]})"`; write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-remediation-exact-benchmark.md` with `Timestamp:`, each exact `Command:`, each `EXIT_CODE:`, `Output Summary:`, checkpoint SHA-256/bytes/receipt counts, exactly `100` timed samples after `100` warm-ups, numeric baseline p95 `0.507500022649765 ms`, numeric post p50/p95, and ratio. Accept only exit code `0`, a structurally valid checkpoint, topology/model totals each at least its P0-T3 floor, topology/model unique counts each at least its P0-T3 snapshot count (currently `3`/`5`), any additional unique keys structurally valid with total/unique receipt counts monotonic, and ratio at or below `1.10`; otherwise stop remediation-required and restart from P3-T1 after correction.
- [x] [P3-T6] Run `git diff --check`; rerun `poetry run pytest tests/scripts/dev_tools/test_analyze_coverage_policy.py`; run `poetry run python -m scripts.dev_tools.generate_orchestration_customization_surfaces --check`, `poetry run python -m scripts.dev_tools.generate_codex_agent_variants --check`, and `poetry run python -m scripts.dev_tools.synchronize_customization_bundles --check`; count lines in `scripts/dev_tools/_orchestrator_state_codex_topology.py`, `scripts/dev_tools/_orchestrator_state_codex_model_routing.py`, `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/_orchestrator_state_complexity.py`, `scripts/dev_tools/analyze_coverage_policy.py`, `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py`, `scripts/dev_tools/synchronize_customization_bundles.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, `scripts/dev_tools/generate_orchestration_customization_surfaces.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_codex_topology.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_codex_model_routing.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_completion.py`, `tests/scripts/dev_tools/test_validate_orchestrator_state_complexity.py`, `tests/scripts/dev_tools/test_analyze_coverage_policy.py`, `tests/scripts/dev_tools/test_parallel_drift_parity.py`, `tests/scripts/dev_tools/test_parallel_mutation_parity.py`, `tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py`, `tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py`, `tests/scripts/dev_tools/test_synchronize_customization_bundles.py`, `tests/scripts/dev_tools/test_generate_codex_agent_variants.py`, and `tests/scripts/dev_tools/test_generate_orchestration_customization_surfaces.py`; verify SHA-256 byte identity for each canonical-root/generated-mirror pair from P2-T12 through P2-T16; and inspect `git status --short` plus `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/plan.2026-08-17T07-06.md`. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t26-remediation-final-verification.md` with `Timestamp:`, each exact `Command:`, each `EXIT_CODE:`, `Output Summary:`, exact changed canonical production/test paths and mechanically generated paths, all `21` Python line counts, generator/synchronizer checks and root/mirror hashes, P2-T28 read-only profile evidence, P3-T1 through P3-T5 clean-pass evidence paths, benchmark ratio, and the checkpoint SHA-256/byte size/topology/model receipt counts from P0-T11, P1-T1, P2-T28, and P3-T5. Accept only when all `21` named Python production/test files are at or below `500` physical lines, all commands pass without mutation or drift, non-strict validation skips route-membership evaluation, strict route-membership diagnostics remain unchanged, the complexity cache is successful-result-only and per-invocation with exact ordered signal-tuple keys, every index-specific complexity diagnostic and monotonic ceiling rule remains unchanged, generated mirrors are byte-identical to their owners and were not manually implemented, receipt totals never decrease from the P0-T3 floor, any checkpoint identity difference is explained by monotonic receipt appends rather than remediation edits, no dependency/suppression/policy/benchmark/input/threshold/receipt/assertion change or weakening is attributed to this remediation, original P6-T26 remains unchecked for separately routed independent executor verification, and no failing intermediate state was staged or committed.

### Phase 4 — P6-T28 TypeScript Performance Remediation

This phase retains exactly four TypeScript performance files: production batch `TS-PROD-A` is `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-topology.ts` plus `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`; performance-test batch `TS-TEST-A` is `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-topology.test.ts` plus `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts`. Separate compatibility-test batch `TS-COMPAT-A` is `0` production files and exactly `3` test files: `extensions/drm-copilot/test/mcp-plan-gate-warning-projection.test.ts`, `extensions/drm-copilot/test/lib/validate/epic-orchestrator-state-codex-topology.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts`. `TS-COMPAT-A` may change only six stale expected-value occurrences across five failing tests so they assert existing HEAD production behavior without weaker operators or fixtures, plus blank-line-only compaction in the existing `508`-line orchestration-artifacts test to satisfy the `500`-line limit; it authorizes no production edit. Separate mechanical identity batch `PY-IDENTITY-A` is `0` production files and `1` test file, `tests/scripts/dev_tools/test_mcp_bundle_parity.py`, and may change only its fixed expected embedded-bundle digest literal after an approved provisional build; its exact equality assertion and all surrounding behavior remain unchanged. Read the already completed TypeScript policy evidence from original P0-T5 and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/phase0-instructions-read.md` before P4-T1. Do not add dependencies, suppressions, test-only production APIs, timing assertions, global or cross-invocation caches, new production modules, or edits to checkpoint/schema/routing-policy/capability/public-signature surfaces. P4-T1 through P4-T4 are read-only fail-before and diagnosis gates and must complete before either owned TypeScript performance batch changes.

Global restart rule: P4-T11 and P4-T12 reconcile the approved provisional source bundle with the one permitted fixed digest literal in `PY-IDENTITY-A`. P4-T13 through P4-T18 form the evidence-backed `TS-COMPAT-A` correction and focused-pass batch. The former P4-T13 through P4-T25 gates are renumbered P4-T19 through P4-T31 and must then complete as one ordered clean pass: Black → Prettier → Ruff → ESLint → full-repository Pyright → source-config `tsc --noEmit` for `TS-PROD-A` → extended focused Jest compiling and executing `TS-TEST-A` and `TS-COMPAT-A` through the repository ts-jest configuration → full coverage-enabled Jest compiling and executing `TS-TEST-A` and `TS-COMPAT-A` through the same repository ts-jest configuration → coverage-policy analysis → approved source/package/packed bundle builds → focused bundle-parity Pytest → extended runtime-contract Jest → exact P6-T27 compile → exact P6-T28 benchmark → final verification. For the bounded second-candidate correction, preserve checked historical P4-T10 and P4-T13 through P4-T17; execute P4-T8, P4-T9, P4-T11, P4-T12, then P4-T18 through P4-T31, skipping checked tasks without replay. Only an explicit subsequent correction to a `TS-COMPAT-A` file clears P4-T13 through P4-T31 and restarts at P4-T13; a P4-T18 focused-test failure without such a correction preserves P4-T13 through P4-T17 and clears P4-T18 through P4-T31. Any later formatter or approved build that changes `MCP_BUNDLE_SHA256` clears P4-T11, P4-T12, and P4-T18 through P4-T31 and restarts identity reconciliation at P4-T11 without replaying P4-T13 through P4-T17. Any other command failure, formatter mutation, generated-output drift, assertion/threshold change, or parity failure clears P4-T19 through P4-T31 and restarts at P4-T19 after the bounded correction; if that correction changes `TS-COMPAT-A`, clear P4-T13 through P4-T31 and restart at P4-T13, and if it can change the source bundle digest, clear P4-T11, P4-T12, and P4-T18 through P4-T31 and restart at P4-T11 without replaying P4-T13 through P4-T17. A P4-T30 ratio above `1.10` stops remediation-required, permits correction only within `TS-PROD-A` and `TS-TEST-A`, and then restarts at P4-T11 while preserving P4-T13 through P4-T17. No task may check original P6-T28, stage, commit, push, edit the checkpoint, or proceed to original P7 while this phase is incomplete.

- [x] [P4-T1] Before any Phase 4 source or test edit, save the current process value of `npm_config_node_options`; set exactly `$env:npm_config_node_options="--import=data:text/javascript,process.chdir('C:/Users/DanMoisan/repos/drm-copilot-wt/2026-08-17T07-01/extensions/drm-copilot')"`; run the unchanged original P6-T27 command `npm --prefix extensions/drm-copilot exec -- tsc --project tsconfig.json --outDir ../../docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/typescript-benchmark-build --sourceMap false`; and in a `finally` block restore the saved process value or run `Remove-Item Env:npm_config_node_options -ErrorAction SilentlyContinue` when it was initially unset. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-fail-before-compile.md`; acceptance: the artifact contains `Timestamp:`, `Environment:` with the exact process-local assignment, the unchanged exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `Environment Restore:` proving the prior value was restored or the variable was unset, the unchanged compiled path `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/typescript-benchmark-build/lib/validate/orchestrator-state-core.js`, and its SHA-256 and byte size, with no source, test, configuration, lockfile, package-manifest, or persistent user/machine environment mutation.
- [x] [P4-T2] [expect-fail] Using the P4-T1 output and unchanged input `artifacts/orchestration/orchestrator-state.json`, re-run the exact original P6-T28 command `node -e "const fs=require('node:fs');const {performance}=require('node:perf_hooks');const {validateOrchestratorStateText:v}=require('./docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/typescript-benchmark-build/lib/validate/orchestrator-state-core.js');const s=fs.readFileSync('artifacts/orchestration/orchestrator-state.json','utf8');for(let i=0;i<100;i++)v(s);const a=[];for(let i=0;i<100;i++){const t=performance.now();v(s);a.push(performance.now()-t)}a.sort((x,y)=>x-y);console.log(JSON.stringify({samples:100,p50_ms:a[49],p95_ms:a[94]}))"` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-fail-before-benchmark.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `ExpectedExitCode: 0`, `EXIT_CODE: 0`, `Output Summary:`, checkpoint SHA-256 and byte size, topology receipt total/unique counts, model-routing receipt total/unique counts, exactly `100` warm-ups and `100` timed samples, baseline p95 `0.23170000000000357 ms`, numeric p50/p95 and ratio, and a semantic `EXPECTED THRESHOLD FAIL` above `1.10` consistent with the reproduced P6-T28 evidence, without changing the benchmark input, baseline, sample counts, percentile index, or threshold.
- [x] [P4-T3] Run the same P6-T28 JavaScript body and input under Node's built-in CPU profiler with `node --cpu-prof --cpu-prof-dir=docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing --cpu-prof-name=p6-t28-typescript-fail-before.cpuprofile -e "const fs=require('node:fs');const {performance}=require('node:perf_hooks');const {validateOrchestratorStateText:v}=require('./docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/typescript-benchmark-build/lib/validate/orchestrator-state-core.js');const s=fs.readFileSync('artifacts/orchestration/orchestrator-state.json','utf8');for(let i=0;i<100;i++)v(s);const a=[];for(let i=0;i<100;i++){const t=performance.now();v(s);a.push(performance.now()-t)}a.sort((x,y)=>x-y);console.log(JSON.stringify({samples:100,p50_ms:a[49],p95_ms:a[94]}))"` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-cpu-profile.md`; acceptance: both artifacts contain the required evidence fields or deterministic profile metadata, use the exact P4-T1 compiled core and P6-T28 input/body with `100` warm-ups and `100` samples, and record the compiled-core SHA-256 plus checkpoint SHA-256/byte size/topology total and unique counts/model-routing total and unique counts without a source/test edit. If the checkpoint hash changed after P4-T2 solely because structurally valid routing receipts were appended, rerun P4-T2 against the new bytes before profiling so P4-T2 and P4-T3 use one exact checkpoint hash and record monotonic receipt counts.
- [x] [P4-T4] Parse `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-fail-before.cpuprofile` with a read-only `node -e` command that reports the top `30` JavaScript frames by sample count, then run a second read-only `node -e` command over the exact checkpoint bytes and hash shared by P4-T2/P4-T3 that reports topology total/unique keys for `(languages, production_file_count, test_file_count, execution_context, cross_cutting, root_persona)` and model-routing total/unique keys for `(logical_agent, complexity_band, execution_context, orchestration_complexity_ceiling)`; write both exact commands and results to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-hot-path-analysis.md`; acceptance: the artifact contains `Timestamp:`, each `Command:`, each `EXIT_CODE: 0`, `Output Summary:`, CPU samples attributed to the repeated receipt-validation/resolver path directly or through documented JIT inlining, exact live totals and unique-key counts from that shared checkpoint hash, counts no lower than the planning snapshot floors of topology `71/10` and model routing `71/5`, `total > unique` for both receipt families, at least one duplicated exact resolver key for each validator, and a scope lock to per-invocation successful-result caching in `TS-PROD-A`; additional structurally valid routing receipts are permitted, while any truncated count, missing duplicate, or contradictory profile stops remediation-required without broadening the plan.
- [x] [P4-T5] In `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-topology.test.ts`, add one deterministic Jest test that creates duplicate valid topology receipts with distinct phases, spies on the external `codex-topology-resolver` export, and asserts `validateCodexTopologyReceipts` returns the unchanged ordered result while resolving the shared exact input tuple once; acceptance: the test uses in-memory data, restores the spy, contains no timing assertion, file/network/process access, temporary file, suppression, assertion weakening, or production seam, and leaves the test file at or below `500` physical lines.
- [x] [P4-T6] In `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts`, add deterministic duplicate-input cases that assert `validateCodexModelRoutingReceipts` preserves valid output, ordered index-specific resolved-field diagnostics, ceiling monotonicity, and ceiling-transition behavior when phases differ but the exact normalized resolver tuple repeats; acceptance: the cases use only in-memory data, contain no timing/global-state/file/network/process assertion or production seam, do not weaken existing assertions, and leave the test file at or below `500` physical lines.
- [x] [P4-T7] [expect-fail] Before editing `TS-PROD-A`, run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/lib/validate/orchestrator-state-codex-topology.test.ts test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts --runInBand` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-cache-fail-before.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, the nonzero `EXIT_CODE:`, `ExpectedExitCode:` equal to that observed nonzero code, and `Output Summary:` proving only the new duplicate topology resolution-count assertion fails while the new model-routing semantic cases and all pre-existing nodes pass, and no failing intermediate state is staged or committed.
- [x] [P4-T8] Update only `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-topology.ts` and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-topology.test.ts` so `validateCodexTopologyReceipts` replaces the per-receipt `JSON.stringify` key, serialized-key `Map`, `flatMap`, eager index-prefix construction, and empty per-receipt diagnostic arrays with one fresh invocation-local successful-resolution entry list and a shared ordered diagnostic collector. Each entry must store a detached copy of the already validated exact `languages` values, the exact primitive `(production_file_count, test_file_count, execution_context, cross_cutting, root_persona)` values, and the successful `resolveCodexTopology` result; lookup must compare the last successful entry first and then prior entries in insertion order, compare `languages` by length and element value plus every primitive by strict equality, and use no JSON serialization, joined/delimited string, hash, object-identity shortcut, global state, or cross-invocation state. Every receipt must still independently check object shape, every required key, non-blank phase, every resolver-input type/value, and every resolved field; the valid path may avoid tuple/key/prefix/empty-error allocation only by directly checking all of those invariants, while any missing, invalid, exceptional, or mismatched path must fall back to the existing ordered index-specific diagnostics and exact resolver exception text. Insert an entry only after successful resolution; never cache an input-validation error or thrown resolution. Strengthen the existing deterministic duplicate test with separately allocated but value-equal `languages` arrays, prove one resolution per exact input within one invocation and a fresh resolution in a second invocation, add a mixed-valid-key case proving distinct tuples do not alias, and add repeated invalid semantic resolver inputs proving both indices invoke resolution and retain exact ordered exception diagnostics. Acceptance: all new assertions are in-memory and non-timing, the external resolver spy is restored, strict/non-strict gates and every public export/signature remain unchanged, no dependency/suppression/new module is added, no assertion is weakened, and both files remain at or below `500` physical lines.
- [x] [P4-T9] Update only `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts` and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts` so `validateCodexModelRoutingReceipts` replaces the per-receipt resolver-input tuple, `JSON.stringify` key, serialized-key `Map`, eager index-prefix construction, empty error array, and `ReceiptValidationResult` allocation with one fresh invocation-local successful-resolution entry list and a shared ordered diagnostic collector. Normalize each receipt's `(logical_agent, complexity_band, execution_context, orchestration_complexity_ceiling)` exactly once with the existing `pythonStr` semantics; each entry must store those four normalized strings plus the successful `resolveCodexDeployment` result; lookup must compare the last successful entry first and then prior entries in insertion order using exact string equality, with no serialization, joined/delimited key, hash, global state, or cross-invocation state. Every receipt must still independently check object shape, every required key, non-blank phase, every resolved field, ceiling monotonicity, and the full ceiling-transition contract in the existing order; the valid path may avoid tuple/key/prefix/empty-error/result allocation only by directly checking all of those invariants, while an invalid, exceptional, transition-failing, or resolved-field-mismatching path must emit the same ordered index-specific diagnostic text. Insert an entry only after successful resolution; never cache invalid normalized inputs or thrown resolutions. Extend the deterministic coverage file with alternating repeated valid tuples that prove exact-entry reuse does not alias distinct normalized tuples while retaining their expected resolved values and with repeated heterogeneous invalid raw inputs that normalize to the same invalid tuple and still emit both exact ordered index-specific resolver diagnostics; retain the existing repeated-input resolved-field and unchanged-ceiling transition assertions. Acceptance: all new assertions are in-memory and non-timing, ceiling state advances exactly as before only from a successful resolution, runtime capability/model-unavailable and routing-policy behavior remain unchanged, all public exports/signatures and diagnostic order/text remain unchanged, only behavior-neutral compaction needed to keep the existing production file within `500` physical lines is permitted, no dependency/suppression/new module is added, no assertion is weakened, and both files remain at or below `500` physical lines.
- [x] [P4-T10] Run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/lib/validate/orchestrator-state-codex-topology.test.ts test/lib/validate/orchestrator-state-codex-model-routing.test.ts test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts test/lib/validate/orchestrator-state-core.test.ts test/lib/validate/orchestrator-state-core.model-routing.test.ts test/lib/validate/orchestrator-state-core.completion.test.ts test/lib/validate/orchestration-artifacts-remediation-v2.test.ts --runInBand` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-focused-pass.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, exact suite/test counts, the P4-T5 once-per-input assertion, all P4-T6 semantic cases, and unchanged strict/non-strict, schema-parity, diagnostic-order, remediation, completion, topology, and model-routing results.
- [x] [P4-T11] Run the approved provisional source-bundle command `npm --prefix extensions/drm-copilot run bundle:mcp-server` after the P4-T10 behavior pass and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-provisional-source-mcp-build.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the exact emitted form `MCP_BUNDLE_SHA256=sha256:<64 lowercase hexadecimal characters>`, and SHA-256/byte size for `extensions/drm-copilot/out/mcp-server.js`; the captured P4-T11 digest is the complete emitted `sha256:<64hex>` value, and the generated output was produced only by the approved command with no manual bundle edit.
- [x] [P4-T12] In mechanical identity batch `PY-IDENTITY-A`, on the initial pass replace only the fixed expected string literal `sha256:72937c91d2cf0ad6809dd1c970ffa38f06eae4248c73222aa45f47266d63b0f4` in `tests/scripts/dev_tools/test_mcp_bundle_parity.py` with the exact complete P4-T11 `sha256:<64hex>` value, without adding a prefix; on any P4-T11 restart, replace only the currently installed fixed digest literal with the newly captured complete P4-T11 value. Write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-bundle-digest-reconciliation.md`; acceptance: the artifact records `Timestamp:`, the complete P4-T11 digest, old and new literals, and `Output Summary:`; `git diff -- tests/scripts/dev_tools/test_mcp_bundle_parity.py` proves exactly one fixed digest literal changed from HEAD; the existing `embedded_digests[0] == (...)` identity assertion, its structure, fixtures, and all other bytes remain unchanged; no production file, dependency, suppression, or assertion weakening is introduced; and the file remains at or below `500` physical lines.
- [x] [P4-T13] [expect-fail] Before editing `TS-COMPAT-A`, run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/mcp-plan-gate-warning-projection.test.ts test/lib/validate/epic-orchestrator-state-codex-topology.test.ts test/lib/validate/orchestration-artifacts.test.ts --runInBand` and reconcile it with the recorded safe-boundary artifact `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-jest-coverage.md` in `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-baseline-compatibility-fail-before.md`; acceptance: the new artifact contains `Timestamp:`, the exact focused `Command:`, `ExpectedExitCode: 1`, `EXIT_CODE: 1`, and `Output Summary:` with exact focused suite/test counts, exact received-versus-expected values for all five failures, and consistency with the full-coverage result of `3` failed/`210` passed suites and `5` failed/`2911` passed/`1` skipped tests. It also records the source artifact's proof that all three `TS-COMPAT-A` files and their directly implicated non-owned production files were byte-identical to HEAD while the owned topology diff changed only the P4-T8 local cache, and classifies the failures as one missing `require_pr_creation_ready` schema key, two missing `ORCH_ROUTING_GATE_CODEX_TOPOLOGY:` prefixes, and two incomplete-checkpoint dispatch expectations. Do not rerun full coverage, edit a test or production file, weaken an assertion, or alter the source failure artifact in this task.
- [x] [P4-T14] In `extensions/drm-copilot/test/mcp-plan-gate-warning-projection.test.ts`, add only existing HEAD schema key `require_pr_creation_ready` to `EXPECTED_SCHEMA_KEYS` at its catalog order between `require_complete` and `require_model_routing`; acceptance: both schema definitions still use exact ordered-array equality, the expected key count grows by exactly one, no key is removed or reordered, no fixture/operator/comment/production file changes, no dependency or suppression is added, and the file remains at or below `500` physical lines.
- [x] [P4-T15] In `extensions/drm-copilot/test/lib/validate/epic-orchestrator-state-codex-topology.test.ts`, update only the two stale expected topology diagnostic string literals to include the existing HEAD prefix `ORCH_ROUTING_GATE_CODEX_TOPOLOGY: `; acceptance: the diagnostic bodies, fixtures, test names, assertion operators, and all other bytes remain unchanged, both assertions remain exact and stronger than substring-only matching, no production file/dependency/suppression changes, and the file remains at or below `500` physical lines.
- [x] [P4-T16] In `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts`, replace exactly three stale string-literal occurrences in the two incomplete-checkpoint dispatch tests—one model-routing expectation and both the ordinary and epic topology expectations—with the exact structural diagnostics already returned by current HEAD production behavior and captured by P4-T13; acceptance: each case retains its existing fixture, validation flags, assertion operator, and dispatch purpose, none of the three string-literal expectations is deleted or broadened, all other assertions remain unchanged, and no production file/dependency/suppression changes.
- [x] [P4-T17] Remove only enough blank-only lines from `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts` to reduce its post-P4-T16 physical line count from the pre-correction `508`-line baseline to at most `500`; acceptance: `git diff --word-diff=porcelain -- extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts` proves the only non-whitespace token changes are all three P4-T16 expected string-literal occurrences, no test name/fixture/operator/comment/code token changes, and the resulting physical line count is at or below `500`.
- [x] [P4-T18] Run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/mcp-plan-gate-warning-projection.test.ts test/lib/validate/epic-orchestrator-state-codex-topology.test.ts test/lib/validate/orchestration-artifacts.test.ts --runInBand` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/p6-t28-typescript-baseline-compatibility-focused-pass.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with exact suite/test counts; all five formerly failing tests and all six corrected expected-value occurrences pass against current HEAD production behavior; every pre-existing node in the three suites passes; the diff remains confined to the six corrected expected-value occurrences plus P4-T17 blank-line-only compaction; and no production, dependency, suppression, threshold, or assertion-operator change occurred. Any failure clears P4-T18 through P4-T31 and restarts at P4-T18 unless a bounded correction changes `TS-COMPAT-A`; only then clear P4-T13 through P4-T31 and restart at P4-T13.
- [x] [P4-T19] Run `poetry run black tests/scripts/dev_tools/test_mcp_bundle_parity.py`, then run `npm --prefix extensions/drm-copilot run format`, in that order; write one command-step artifact per command to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-python-black.md` and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-prettier.md`; acceptance: both artifacts contain `Timestamp:`, exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:`, formatting covers `PY-IDENTITY-A`, `TS-PROD-A`, `TS-TEST-A`, and all three `TS-COMPAT-A` files, produces no changes outside those eight owned paths, and both commands pass again without mutation after any formatter change; every owned file remains at or below `500` physical lines. Any mutation affecting a `TS-COMPAT-A` file clears P4-T13 through P4-T31 and restarts at P4-T13; every other mutation clears P4-T19 through P4-T31 and restarts at P4-T19; if it changes the subsequently emitted `MCP_BUNDLE_SHA256`, the P4-T25 comparison instead clears P4-T11, P4-T12, and P4-T18 through P4-T31, preserves P4-T13 through P4-T17, and restarts at P4-T11.
- [x] [P4-T20] After a mutation-free P4-T19, run `poetry run ruff check tests/scripts/dev_tools/test_mcp_bundle_parity.py`, then run `npm --prefix extensions/drm-copilot run lint`, in that order; write one command-step artifact per command to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-python-ruff.md` and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-eslint.md`; acceptance: both artifacts contain the required evidence fields; the two commands collectively cover all six test files and both production files: Ruff covers PY-IDENTITY-A; ESLint covers TS-PROD-A, TS-TEST-A, and TS-COMPAT-A. Both exit `0` with zero diagnostics and no suppression, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after a bounded correction.
- [x] [P4-T21] After P4-T20, run full-repository `poetry run pyright`, then run source-config `npm --prefix extensions/drm-copilot run typecheck`, in that order; write one command-step artifact per command to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-python-pyright.md` and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-typecheck.md`; acceptance: both artifacts contain `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; Pyright exits `0` with `0` errors and `0` warnings without narrowing its repository scope; the source configuration covers both `TS-PROD-A` files; and both commands exit `0`, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after a bounded correction.
- [x] [P4-T22] Run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/lib/validate/orchestrator-state-codex-topology.test.ts test/lib/validate/orchestrator-state-codex-model-routing.test.ts test/lib/validate/orchestrator-state-codex-model-routing-coverage.test.ts test/lib/validate/orchestrator-state-core.test.ts test/lib/validate/orchestrator-state-core.model-routing.test.ts test/lib/validate/orchestrator-state-core.completion.test.ts test/lib/validate/orchestration-artifacts-remediation-v2.test.ts test/mcp-plan-gate-warning-projection.test.ts test/lib/validate/epic-orchestrator-state-codex-topology.test.ts test/lib/validate/orchestration-artifacts.test.ts --runInBand` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-focused-jest.md`; acceptance: the repository ts-jest configuration compiles and executes every named suite, including all `TS-TEST-A` and `TS-COMPAT-A` files; the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with exact suite/test counts and zero failures; every P4-T5/P4-T6 node, all five formerly failing compatibility tests and all six P4-T14 through P4-T16 corrected expected-value occurrences, and all pre-existing strict/non-strict, schema-parity, diagnostic-order, remediation, completion, topology, model-routing, and dispatch nodes pass in band, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after correction within the existing owned batch.
- [x] [P4-T23] Run `npm --prefix extensions/drm-copilot run test:unit -- --coverage --coverageDirectory ../../docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage --coverageReporters=lcov --coverageReporters=text --coverageReporters=text-summary --coverageReporters=json-summary --coverageReporters=json` and rewrite `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-jest-coverage.md` with the passing result; acceptance: the repository ts-jest configuration compiles and executes the full Jest suite, including all `TS-TEST-A` and `TS-COMPAT-A` files; the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, zero failed suites and zero failed tests, exact suite/test counts proving all three `TS-COMPAT-A` suites pass, numeric repository line/branch/function/statement coverage, and numeric line/branch coverage for both `TS-PROD-A` modules with all changed executable lines covered, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after correction, or at P4-T13 when a compatibility expectation requires correction.
- [x] [P4-T24] Run `poetry run python -m scripts.dev_tools.analyze_coverage_policy --mode final --language typescript --coverage-json docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage/coverage-final.json --coverage-summary docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage/coverage-summary.json --lcov docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage/lcov.info --baseline-typescript-analysis docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/baseline/typescript-coverage-analysis.json --base-ref HEAD --working-tree --coverage-config extensions/drm-copilot/jest.config.cjs --repo-line-min 85 --repo-branch-min 75 --new-symbol-min 90 --require-configured-changed-files --require-no-changed-line-regression --output docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage-analysis.json` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-typescript-coverage-analysis.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, numeric baseline/post repository line and branch coverage, per-new-symbol coverage, configured changed-file results, and changed-line deltas, and passes repository line `85%`, branch `75%`, new-symbol/changed-code `90%`, configured changed-file, and no-regression gates without changing any threshold or coverage configuration, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after correction.
- [x] [P4-T25] Run `npm --prefix extensions/drm-copilot run bundle:mcp-server` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-source-mcp-build.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the complete emitted `MCP_BUNDLE_SHA256=sha256:<64hex>` value, and SHA-256/byte size of `extensions/drm-copilot/out/mcp-server.js`; the output is generated only by the approved build command; and that complete emitted value exactly equals both the complete P4-T11 value and the fixed literal installed by P4-T12. Any digest difference clears P4-T11, P4-T12, and P4-T18 through P4-T31, preserves P4-T13 through P4-T17, and restarts at P4-T11 without changing the identity assertion.
- [x] [P4-T26] Run `npm --prefix packages/mcp-server run prepack` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-package-prepack.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with resource count and routing-policy SHA-256, and proves exact source/package resource parity without manual edits or routing-policy mutation.
- [x] [P4-T27] Run `npm --prefix packages/mcp-server run build` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-built-mcp-build.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, SHA-256/byte sizes for `extensions/drm-copilot/out/mcp-server.js` and `packages/mcp-server/out/mcp-server.js`, exact byte identity enforced by the package build without manual output edits, and the same complete `sha256:<64hex>` value as P4-T11, the P4-T12 literal, and P4-T25; any digest difference clears P4-T11, P4-T12, and P4-T18 through P4-T31, preserves P4-T13 through P4-T17, and restarts at P4-T11.
- [x] [P4-T28] Run `npm pack ./packages/mcp-server --pack-destination docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing`, which must select exactly `@danmoisan/drm-copilot-mcp@1.0.27` and create exactly `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/danmoisan-drm-copilot-mcp-1.0.27.tgz`; then run the separate bounded idempotent cleanup command `& { $trimChars = [char[]]@([System.IO.Path]::DirectorySeparatorChar, [System.IO.Path]::AltDirectorySeparatorChar); $comparison = [System.StringComparison]::OrdinalIgnoreCase; $workspaceOutput = @(& git rev-parse --show-toplevel); if ($LASTEXITCODE -ne 0 -or $workspaceOutput.Count -ne 1 -or [string]::IsNullOrWhiteSpace([string]$workspaceOutput[0])) { throw 'Unable to resolve exactly one workspace root.' }; $workspaceRoot = [System.IO.Path]::GetFullPath(([string]$workspaceOutput[0]).Trim()).TrimEnd($trimChars); $expectedWorkspaceRoot = [System.IO.Path]::GetFullPath('C:/Users/DanMoisan/repos/drm-copilot-wt/2026-08-17T07-01').TrimEnd($trimChars); if (-not [string]::Equals($workspaceRoot, $expectedWorkspaceRoot, $comparison) -or [string]::Equals($workspaceRoot, [System.IO.Path]::GetPathRoot($workspaceRoot).TrimEnd($trimChars), $comparison)) { throw 'Resolved workspace root is unexpected or broad.' }; $evidenceDirectory = [System.IO.Path]::GetFullPath((Join-Path -Path $workspaceRoot -ChildPath 'docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing')).TrimEnd($trimChars); $workspacePrefix = $workspaceRoot + [System.IO.Path]::DirectorySeparatorChar; if (-not $evidenceDirectory.StartsWith($workspacePrefix, $comparison)) { throw 'Evidence directory is outside the workspace.' }; $evidenceItem = Get-Item -LiteralPath $evidenceDirectory -Force -ErrorAction Stop; if (-not $evidenceItem.PSIsContainer -or $null -ne $evidenceItem.LinkType -or (($evidenceItem.Attributes -band [System.IO.FileAttributes]::ReparsePoint) -ne 0)) { throw 'Evidence directory must be a real in-workspace directory, not a link or reparse point.' }; $target = [System.IO.Path]::GetFullPath((Join-Path -Path $evidenceDirectory -ChildPath 'packed-mcp')).TrimEnd($trimChars); $expectedTarget = [System.IO.Path]::GetFullPath((Join-Path -Path $evidenceDirectory -ChildPath 'packed-mcp')).TrimEnd($trimChars); $targetParent = [System.IO.Path]::GetDirectoryName($target).TrimEnd($trimChars); if (-not [string]::Equals($target, $expectedTarget, $comparison) -or -not [string]::Equals($targetParent, $evidenceDirectory, $comparison) -or [string]::Equals($target, $workspaceRoot, $comparison) -or [string]::Equals($target, $evidenceDirectory, $comparison)) { throw 'Cleanup target identity or parent is invalid.' }; $targetItem = $null; try { $targetItem = Get-Item -LiteralPath $target -Force -ErrorAction Stop } catch [System.Management.Automation.ItemNotFoundException] { $targetItem = $null }; if ($null -ne $targetItem) { if (-not $targetItem.PSIsContainer -or $null -ne $targetItem.LinkType -or (($targetItem.Attributes -band [System.IO.FileAttributes]::ReparsePoint) -ne 0)) { throw 'Refusing to remove a non-directory, symbolic link, junction, mount point, or other reparse point.' }; Remove-Item -LiteralPath $target -Recurse -Force -ErrorAction Stop }; $remaining = $null; try { $remaining = Get-Item -LiteralPath $target -Force -ErrorAction Stop } catch [System.Management.Automation.ItemNotFoundException] { $remaining = $null }; if ($null -ne $remaining) { throw 'Exact packed-mcp target still exists after cleanup.' } }` before the packed smoke command; then run `node packages/mcp-server/test/mcp-json-rpc-smoke.cjs --tarball docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/danmoisan-drm-copilot-mcp-1.0.27.tgz --mode packed --extract-to docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/packed-mcp --require-pr-creation-ready`; then run the focused parity test `poetry run pytest tests/scripts/dev_tools/test_mcp_bundle_parity.py::test_local_candidate_source_built_and_packed_parity`; and write one command-step artifact per command to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-mcp-pack.md`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-packed-mcp-cleanup.md`, `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-packed-mcp-smoke.md`, and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-mcp-bundle-parity.md`; acceptance: all four artifacts contain `Timestamp:`, their exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; the pack artifact proves the exact explicit package spec selected `@danmoisan/drm-copilot-mcp@1.0.27` and produced only the exact feature-local archive `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/danmoisan-drm-copilot-mcp-1.0.27.tgz`; the cleanup artifact records the resolved workspace root, feature regression-testing directory, exact `packed-mcp` target, case-insensitive Windows-safe inside-workspace and exact target-parent/equality checks, whether the exact target was absent or removed, non-following `Get-Item -LiteralPath` directory/link/reparse-point inspection before any deletion when it existed, the exact `Remove-Item -LiteralPath` target when deletion occurred, and proof that absence succeeds without deletion and that the exact target is absent before smoke; cleanup rejects every non-directory, symbolic link, junction, mount point, or other `ReparsePoint`, and no cleanup uses a glob, unresolved variable, broad root, inexact parent, or out-of-workspace target; the archive and smoke extraction exist only at the feature-local archive and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing/packed-mcp` paths inside the current worktree, with no package archive, extraction, cleanup, or destination write outside the workspace; the packed local `1.0.27` candidate validates PR-creation readiness; source, built, and packed bundles are byte-identical and all expose the same complete `sha256:<64hex>` value as P4-T11, the P4-T12 literal, P4-T25, and P4-T27; the exact `embedded_digests[0] == (...)` identity assertion passes unchanged except for its fixed expected literal; validator outputs, capability/catalog identity, runtime package identity, and routing-policy digest are equal; and immutable published `1.0.24` remains only the unchanged negative external-runtime fixture. Any digest or parity difference clears P4-T11, P4-T12, and P4-T18 through P4-T31, preserves P4-T13 through P4-T17, and restarts at P4-T11; any other test failure clears P4-T19 through P4-T31 and restarts at P4-T19 after correction.
- [x] [P4-T29] Run `npm --prefix extensions/drm-copilot run test:unit -- --runTestsByPath test/mcp-server-validator-capabilities.test.ts test/mcp-validator-catalog-parity.test.ts test/repo-automation-orchestration-validation.test.ts test/lib/validate/build-validate-orchestration-service-call-input.test.ts test/lib/validate/validate-orchestration-service-call.test.ts test/mcp-plan-gate-warning-projection.test.ts test/lib/validate/epic-orchestrator-state-codex-topology.test.ts test/lib/validate/orchestration-artifacts.test.ts --runInBand` and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-runtime-capability-routing-contracts.md`; acceptance: the artifact contains `Timestamp:`, the exact `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with exact suite/test counts; every named suite passes; and schema, source/service-call dispatch, validator catalog, package/runtime capability, routing-policy, diagnostic-order, strict/non-strict behavior, and all five formerly failing compatibility tests and all six corrected expected-value occurrences remain exact, otherwise clear P4-T19 through P4-T31 and restart at P4-T19 after correction, or at P4-T13 when a compatibility expectation requires correction.
- [ ] [P4-T30] Save the current process value of `npm_config_node_options`; set exactly `$env:npm_config_node_options="--import=data:text/javascript,process.chdir('C:/Users/DanMoisan/repos/drm-copilot-wt/2026-08-17T07-01/extensions/drm-copilot')"`; run the unchanged original P6-T27 command `npm --prefix extensions/drm-copilot exec -- tsc --project tsconfig.json --outDir ../../docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/typescript-benchmark-build --sourceMap false`; and in a `finally` block restore the saved process value or run `Remove-Item Env:npm_config_node_options -ErrorAction SilentlyContinue` when it was initially unset. After restoration, immediately run the exact original P6-T28 benchmark command from P4-T2 against that compiled core and unchanged `artifacts/orchestration/orchestrator-state.json`. Write one command-step artifact per command to `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-remediation-compile.md` and `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-remediation-exact-benchmark.md`; acceptance: the compile artifact contains `Timestamp:`, `Environment:` with the exact process-local assignment, the unchanged exact `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `Environment Restore:` proving the prior value was restored or the variable was unset, and the unchanged build path; the benchmark artifact contains `Timestamp:`, its exact unchanged `Command:`, `EXIT_CODE: 0`, and `Output Summary:` and records checkpoint and compiled-core SHA-256/byte sizes, topology receipt total/unique counts, model-routing receipt total/unique counts, exactly `100` warm-ups and `100` timed samples, baseline p95 `0.23170000000000357 ms`, numeric post p50/p95 using sorted index `[94]`, and ratio at or below `1.10`; receipt counts must be no lower than the live P4-T4 counts, `total > unique` must still hold for both families, and any additional keys must be structurally valid. Do not change the input, compile command/build path, baseline, sample counts, percentile index, threshold, validator flags, or process-startup exclusion; do not persist a user/machine environment change; any failure, receipt truncation, or ratio above `1.10` stops remediation-required and restarts at P4-T11 only after a bounded P4-T8/P4-T9 correction while preserving P4-T13 through P4-T17.
- [ ] [P4-T31] Run `git diff --check`; count physical lines in every `TS-PROD-A`, `TS-TEST-A`, `TS-COMPAT-A`, and `PY-IDENTITY-A` file; inspect `git diff --` for those eight files, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`, `packages/mcp-server/package.json`, `packages/mcp-server/package-lock.json`, `config/orchestration-routing.json`, and `artifacts/orchestration/orchestrator-state.json`; calculate SHA-256/byte size for the source, built, and extracted packed MCP bundles; inspect original `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/plan.2026-08-17T07-06.md` at P6-T27 and P6-T28; verify the process `npm_config_node_options` value has been restored or remains unset as captured by P4-T30; and write `docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/qa-gates/p6-t28-remediation-final-verification.md`; acceptance: the artifact contains `Timestamp:`, every exact `Command:`, every `EXIT_CODE: 0`, `Output Summary:`, final changed-path inventory, all eight line counts at or below `500`, evidence links for P4-T1 through P4-T30, and the exact final benchmark ratio; proves only the bounded local successful-resolution caches and their two `TS-TEST-A` tests changed performance behavior, `TS-COMPAT-A` changed only six stale expected-value occurrences across five formerly failing tests to assert current HEAD production behavior plus P4-T17 blank-line-only compaction without weaker assertions, and `PY-IDENTITY-A` changed exactly one fixed expected digest literal while preserving the exact `embedded_digests[0] == (...)` identity assertion; and proves public signatures, production schema, receipt validation, phase/index diagnostics and order, ceiling monotonicity/transitions, strict/non-strict semantics, package/runtime capabilities, routing policy, dependencies, suppressions, all other assertions, coverage thresholds, benchmark input/baseline/sample counts/p95 index/threshold, checkpoint, and original P6-T27/P6-T28 task state remain unchanged. Source, built, and packed bundle bytes must be identical and use the same complete `sha256:<64hex>` value recorded by P4-T11, installed by P4-T12, and reproduced by P4-T25/P4-T27/P4-T28; generated bundles were produced only by approved commands; no failing intermediate state is staged or committed; identity reconciliation P4-T11/P4-T12 remains current; and P4-T19 through P4-T31 completed in one clean pass after the P4-T13 through P4-T18 compatibility batch.
