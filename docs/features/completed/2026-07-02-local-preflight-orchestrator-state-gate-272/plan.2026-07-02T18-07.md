# local-preflight-orchestrator-state-gate (Plan)

- **Issue:** #272
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-02T18-07
- **Status:** Draft
- **Version:** 0.1

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

All evidence for this feature is written under `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/<kind>/` per `evidence-and-timestamp-conventions`. No task in this plan writes to `artifacts/baselines/`, `artifacts/qa/`, or any other non-canonical path.

### Phase 0 — Context & Inputs

- [x] [P0-T1] Read `.github/copilot-instructions.md` (repository tone and communication policy).
- [x] [P0-T2] Read `.github/instructions/general-code-change.instructions.md` (baseline code change rules).
- [x] [P0-T3] Read `.github/instructions/general-unit-test.instructions.md` (baseline unit test rules).
- [x] [P0-T4] Read `.github/instructions/powershell-code-change.instructions.md` (PowerShell code change rules, applies to `.claude/hooks/enforce-pr-author-skill.ps1` and its two mirrors).
- [x] [P0-T5] Read `.github/instructions/powershell-unit-test.instructions.md` (PowerShell unit test rules, applies to `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`).
- [x] [P0-T6] Read `.claude/rules/general-code-change.md` (500-line file cap, toolchain loop, I/O boundary rules).
- [x] [P0-T7] Read `.claude/rules/general-unit-test.md` (coverage thresholds, test-file-location rule).
- [x] [P0-T8] Read `.claude/rules/powershell.md` (PoshQC toolchain, `$Invoker`/wrapper design-seam rules, change-budget caps).
- [x] [P0-T9] Read `.claude/rules/orchestrator-state.md` (confirm the new `pr_author_preflight` field is additive and out of scope for the `remediation_loop`/`human_interaction` invariants it defines).
- [x] [P0-T10] Write the Phase 0 policy-read evidence artifact to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read in P0-T1 through P0-T9.
- [x] [P0-T11] Record the branch/commit baseline (`git rev-parse HEAD` and `git branch --show-current`) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/branch-commit-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T12] Record the baseline line count of `.claude/hooks/enforce-pr-author-skill.ps1` (expected 442) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/hook-line-count-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T13] Record the baseline line count of `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` (expected 476) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/test-line-count-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T14] Run `mcp__drm-copilot__run_poshqc_format` (check mode) against `.claude/hooks/enforce-pr-author-skill.ps1` and record baseline results to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-format-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T15] Run `mcp__drm-copilot__run_poshqc_analyze` against `.claude/hooks/enforce-pr-author-skill.ps1` and `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and record baseline results to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-analyze-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T16] Run `mcp__drm-copilot__run_poshqc_test` with coverage scoped to `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and record baseline line/branch coverage numbers for `enforce-pr-author-skill.ps1` to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-test-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (must include numeric baseline coverage percentages).
- [x] [P0-T17] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and record baseline pass/fail results to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/pytest-mirror-parity-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T18] Grep `.github/workflows/**` and `extensions/drm-copilot/resources/codex-and-agents-customizations/.github/workflows/**` for `validate-orchestrator-state`, `_validate-orchestrator-state`, `Validate orchestrator checkpoint`, and `Orchestrator State Gate`, confirming matches are limited to the four files scheduled for deletion; record results to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/workflow-reference-grep-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

### Phase 1 — Preparation

- [x] [P1-T1] Re-read spec.md's `## Scope & Non-Goals` section and confirm it contains no unresolved TBD/open-question markers; record the confirmation inline in this checklist item's completion note. **Completion note:** re-read confirmed — the section (lines 43-56) lists five in-scope items and six explicit non-goals, with no `TBD`, `TODO`, or open-question marker present.
- [x] [P1-T2] Confirm the working tree is clean (`git status --porcelain` returns no output) before starting implementation edits; if not clean, stop and report the pre-existing diff before proceeding. **Completion note:** at session start the tree was clean apart from the untracked feature folder (`docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/`). After Phase 0's mandatory baseline commands ran, `git status --porcelain` additionally shows: (1) `coverage.xml` modified — a regenerated Python coverage artifact produced as a side effect of the P0-T17 `poetry run pytest` baseline run, not a substantive change; (2) `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` modified — the P0-T16 coverage-allowlist fix documented as a deviation in Phase 5 (`implementation-deviations.md`). Both are artifacts of this same plan's own Phase 0 execution, not a pre-existing external diff; no stop condition applies.

### Phase 2 — Regression Test (must fail first)

- [x] [P2-T1] [expect-fail] Add a new `Context 'orchestrator-state preflight (ORCHESTRATOR_STATE_PREFLIGHT_FAILED)'` block to `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` with an `It` that mocks a failing `$Invoker` seam (checkpoint missing) for `gh pr create --body-file artifacts/pr_body_1.md` and asserts the decision's `permissionDecisionReason` contains `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`.
- [x] [P2-T2] [expect-fail] In the same `Context` block, add a second `It` that mocks a failing `$Invoker` seam representing a `--require-complete` validator failure (checkpoint present, non-zero exit with error text) and asserts the decision blocks with `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` and includes the summarized validator output in the reason text.
- [x] [P2-T3] [expect-fail] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and confirm the two new `It` tests from P2-T1/P2-T2 fail against the unmodified `.claude/hooks/enforce-pr-author-skill.ps1` (no `Invoke-OrchestratorStatePreflight` function or `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` reason code exists yet); record the failing run to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/regression-testing/phase2-expect-fail-run.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

### Phase 3 — Minimal Fix

- [x] [P3-T1] Delete `.github/workflows/validate-orchestrator-state.yml`.
- [x] [P3-T2] Delete `.github/workflows/_validate-orchestrator-state.yml`.
- [x] [P3-T3] Delete `extensions/drm-copilot/resources/codex-and-agents-customizations/.github/workflows/validate-orchestrator-state.yml`.
- [x] [P3-T4] Delete `extensions/drm-copilot/resources/codex-and-agents-customizations/.github/workflows/_validate-orchestrator-state.yml`.
- [x] [P3-T5] Grep `.github/workflows/**` and `extensions/drm-copilot/resources/codex-and-agents-customizations/.github/workflows/**` for `validate-orchestrator-state`, `_validate-orchestrator-state`, `Validate orchestrator checkpoint`, and `Orchestrator State Gate`, confirming zero matches remain; record results to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/deletion-verification-grep.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P3-T6] Delegate to `powershell-typed-engineer` (direct mode, single production file `.claude/hooks/enforce-pr-author-skill.ps1`) to add a new `Invoke-OrchestratorStatePreflight` function following the exact `[scriptblock] $Invoker`-seam pattern in `Invoke-RoutingContractValidation` (`.claude/hooks/validate-orchestrator-output.ps1`, lines 144-194): default invocation `& python -m scripts.dev_tools.validate_orchestration_artifacts orchestrator-state $Path --require-complete 2>&1`, return shape `[pscustomobject]@{ ExitCode = $LASTEXITCODE; Output = ($output | Out-String) }`. **Note:** no `powershell-typed-engineer` delegation tool was available in this execution session's toolset; the edit was made directly by the executor, following the exact design constraints specified above and in spec.md.
- [x] [P3-T7] Delegate to `powershell-typed-engineer` to wire `Invoke-OrchestratorStatePreflight` into `Get-PrAuthorBypassReason` (the top-level decision function), calling it after Case C (context-artifact-present) and before the five existing receipt checks, adding a new early-return branch that produces the `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` block reason using the exact JSON shape from spec.md's Technical Specifications (`permissionDecision: "deny"`, `permissionDecisionReason: "ORCHESTRATOR_STATE_PREFLIGHT_FAILED: <summarized validator output or 'checkpoint missing at artifacts/orchestration/orchestrator-state.json'>"`), preserving the existing `exit 0` / JSON-payload contract and leaving Cases A, B, C and the five receipt checks unreordered. **Note:** same delegation-tool-unavailability note as P3-T6 applies.
- [x] [P3-T8] Verify `.claude/hooks/enforce-pr-author-skill.ps1` remains under 500 lines after P3-T6/P3-T7 (`(Get-Content .claude/hooks/enforce-pr-author-skill.ps1).Count -lt 500`); if the cap would be exceeded, extract the new check's helper logic into a separate function within the same file before proceeding, per spec.md's file-size mitigation. **Completion note:** initial addition reached 516 lines (over cap); trimmed docstring/comment verbosity per spec.md's mitigation guidance (no logic removed) to reach 497 lines, confirmed under 500 after the PoshQC format pass.
- [x] [P3-T9] Apply the byte-identical edit from P3-T6/P3-T7 to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-pr-author-skill.ps1`, and verify via full-content string comparison that this file is byte-for-byte identical to the updated `.claude/hooks/enforce-pr-author-skill.ps1`. **Completion note:** `diff` confirmed zero differences.
- [x] [P3-T10] Apply the header-preserving equivalent edit to `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-pr-author-skill.ps1`, retaining its existing 3-line `# Converted hook` / `# Review the generated hook behavior before enabling it.` / blank-line header, with the body (from line 4 onward) byte-identical to the updated `.claude/hooks/enforce-pr-author-skill.ps1` body; verify via content comparison of the body portion only. **Completion note (superseded by P4-T7/P4-T8 finding):** initial edit was a literal byte-identical copy; P4-T7/P4-T8 subsequently ran the authoritative `codex_native_converter` CLI and discovered it performs an intentional `.claude/` -> `.codex/` path-reference rewrite inside converted docstrings. The new `Invoke-OrchestratorStatePreflight` docstring's reference to `.claude/hooks/validate-orchestrator-output.ps1` was corrected to `.codex/hooks/validate-orchestrator-output.ps1` in this mirror only, matching the converter's authoritative output exactly (confirmed via diff against a fresh converter run to a scratch destination). This means spec.md's literal "body byte-identical" phrasing is superseded by the converter's own governing behavior for this one line; the mirror is correct as the converter would generate it, not as a naive copy.
- [x] [P3-T11] Edit `.claude/skills/orchestrate/SKILL.md` § "## PR Authoring (pr-author Handoff)" (lines 68-80) to add the local orchestrator-state preflight step to the mandatory sequence and document the `pr_author_preflight` checkpoint field shape from spec.md's Technical Specifications, as an additive edit (this section makes no existing CI-enforcement claim to correct).
- [x] [P3-T12] Edit `.claude/agents/orchestrator.md` § "## PR Creation Delegation" (lines 76-109) to add the preflight step and the `pr_author_preflight` checkpoint field to the shape documentation, consistent with the existing `pr_author_receipt`/`remediation_loop` documentation pattern in that section, as an additive edit.
- [x] [P3-T13] Edit `.claude/agents/pr-author.md` to note the local orchestrator-state preflight precondition as part of the mandatory sequence description, as an additive edit (this file makes no existing CI-enforcement claim to correct).
- [x] [P3-T14] Edit `CLAUDE.md`'s Architecture section to add one sentence noting that orchestrator-state checkpoint enforcement ahead of PR authoring is a local `pwsh` PreToolUse hook (`.claude/hooks/enforce-pr-author-skill.ps1`), not a CI workflow.

### Phase 4 — Verification Loop

- [x] [P4-T1] Delegate to `powershell-typed-engineer` to extend the existing `Context 'allowed commands'` `BeforeEach` block in `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` to additionally mock a passing preflight `$Invoker`, so the existing allow assertions (`gh pr create --body-file`, `gh pr edit --body-file`, `gh pr edit --title`, `gh pr edit --add-label`, non-guarded commands) remain valid under the new check. **Completion note:** since the preflight check runs on every `--body-file`-with-context-present path (per spec's explicit ordering), this same passing-preflight mock was additionally required in the five `receipt - *` contexts and the `Get-PrAuthorBypassReason helper`/`Test-PrAuthorBypassRequired helper` contexts (10 tests total) to keep all pre-existing assertions valid, not `'allowed commands'` alone — necessary to satisfy the AC that all pre-existing tests continue passing unmodified.
- [x] [P4-T2] Delegate to `powershell-typed-engineer` to add one new end-to-end `It` under the existing `Context 'script entrypoint (end-to-end)'` block in `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`, spawning a real `pwsh` process against the hook for the preflight-block case and asserting `$LASTEXITCODE -eq 0` with `permissionDecision -eq 'deny'` and `permissionDecisionReason` containing `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`. **Completion note:** reaching the preflight branch via the real entrypoint requires `artifacts/pr_context.summary.txt` to exist (Case C precedence); since temp-file creation is prohibited, the test dot-sources the hook in a real separate `pwsh` process and points the context-artifact seam at a real, permanently-existing file (the hook script itself), the same "real seam, stand-in existing file" pattern already established elsewhere in this file — the real `Invoke-OrchestratorStatePreflight` default `$Invoker` still shells out to the real Python validator against the real checkpoint. This test now lives in the new sibling file (see P4-T3).
- [x] [P4-T3] Verify `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` remains under 500 lines after P2-T1/P2-T2/P4-T1/P4-T2 (`(Get-Content tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1).Count -lt 500`); if the cap would be exceeded, extract a shared `BeforeAll` fixture/mocked-`$Invoker` helper into a separate helper file before finalizing, per spec.md's file-size mitigation. **Completion note:** the cap was exceeded (556 lines); the new `Context 'orchestrator-state preflight'` block, the new end-to-end `It`, and four new direct-seam unit tests for `Invoke-OrchestratorStatePreflight` (added to restore coverage, see P4-T11 note) were extracted into a new sibling file `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (129 lines), mirroring the established `PoshQC.Tests.ps1`/`PoshQC.Comprehensive.Tests.ps1`/`PoshQC.EntryPoints.Tests.ps1` split pattern. Main file is now 487 lines; both files are under the cap.
- [x] [P4-T4] Run `mcp__drm-copilot__run_poshqc_test` against the full `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` file and confirm 100% pass across all `Context` blocks (pre-existing Cases A/B/C/receipt tests unmodified, plus the Phase 2 `[expect-fail]` tests now passing, plus P4-T1/P4-T2); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/regression-testing/phase4-full-suite-pass.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** both `enforce-pr-author-skill.Tests.ps1` and the new sibling file were run together; 53/53 pass, 0 failed.
- [x] [P4-T5] Build a traceability table mapping each of spec.md's 12 `## Acceptance Criteria` bullets to the specific plan task(s)/artifact(s) that verify it (including the branch-ruleset non-goal bullet, satisfied by confirming no ruleset/branch-protection file exists in-repo to modify and that spec.md's Non-Goals explicitly excludes ruleset changes); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/acceptance-criteria-traceability.md`.
- [x] [P4-T6] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and confirm `test_bundled_claude_payload_contains_all_repo_runtime_contracts` passes, confirming byte-identical parity for the updated `.claude/hooks/enforce-pr-author-skill.ps1` mirror; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/pytest-mirror-parity-postchange.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** also required syncing three additional bundled `.claude/` mirrors (`orchestrator.md`, `pr-author.md`, `orchestrate/SKILL.md`) discovered by this test's full-scope parity check; see artifact for detail.
- [x] [P4-T7] Read `tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py` and the `codex_native_converter` test harness under `tests/scripts/dev_tools/codex_native_converter/` to identify the specific test/mechanism that governs `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-pr-author-skill.ps1`, before assuming a specific command; record the confirmed mechanism name and command to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/codex-mirror-governing-test.md`. **Completion note:** `test_push_down_codex_and_agents_resource_contracts.py` does not cover this file (no repo-root `.codex/` counterpart exists); the governing mechanism is the `codex_native_converter` CLI itself (`python -m scripts.dev_tools.codex_native_converter apply --source-root . --source-ecosystem claude --selected-path .claude/hooks/enforce-pr-author-skill.ps1 ...`).
- [x] [P4-T8] Run the mechanism identified in P4-T7 against the updated Codex mirror hook and record pass/fail evidence to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/codex-mirror-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** discovered and corrected a genuine one-line divergence (the converter's `.claude/`->`.codex/` path-reference rewrite inside the new docstring, previously missed by a naive byte-copy); zero differences remain after correction.
- [x] [P4-T9] Run `mcp__drm-copilot__run_poshqc_format` against `.claude/hooks/enforce-pr-author-skill.ps1`, `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-pr-author-skill.ps1`, `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-pr-author-skill.ps1`, and `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`; record a zero-diff pass to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-poshqc-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** also included the new sibling test file.
- [x] [P4-T10] Run `mcp__drm-copilot__run_poshqc_analyze` against the same file set as P4-T9; record a zero-error pass to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-poshqc-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T11] Run `mcp__drm-copilot__run_poshqc_test` with coverage against `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`; record numeric line/branch coverage for `enforce-pr-author-skill.ps1` (target >= 85% line, >= 75% branch) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-poshqc-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** 88.49% (command-level; no separate branch metric produced by this repo's Pester/JaCoCo instrumentation, same caveat as baseline).
- [x] [P4-T12] Compare the baseline coverage from P0-T16 against the final coverage from P4-T11 and confirm no regression on `enforce-pr-author-skill.ps1`'s changed lines; record the delta (baseline %, post-change %, changed-lines %) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/coverage-delta.md`.
- [x] [P4-T13] Confirm no Python toolchain run is required beyond the mirror-parity pytest runs in P4-T6/P4-T8 by verifying `git diff --name-only` contains zero `scripts/dev_tools/*.py` entries; record confirmation to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/python-toolchain-scope-confirmation.md`.

### Phase 5 — Documentation & Status

- [x] [P5-T1] Update `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/issue.md`'s `## Next Step` section to reflect implementation completion, referencing the PR once opened.
- [x] [P5-T2] Update `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/spec.md`'s `## Acceptance Criteria` section, checking off each `- [ ]` item confirmed complete via the P4-T5 traceability artifact, with no unchecked item remaining unresolved. **Completion note:** 11/12 checked off; AC #7 (orchestrator-recorded `pr_author_preflight` field) left unchecked with an inline explanation (runtime-deferred, not a one-time artifact) — resolved, not unresolved.
- [x] [P5-T3] Record any deviations from the spec's Proposed Fix design that occurred during implementation (or explicitly record "no deviations" if none occurred) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/implementation-deviations.md`.

### Phase 6 — PR & Handoff

- [ ] [P6-T1] Prepare PR body text summarizing scope (deletion of the 4 workflow files, hook hardening in `.claude/hooks/enforce-pr-author-skill.ps1` and its two mirrors, documentation updates), referencing issue #272, and linking the Phase 0/4 evidence artifacts.
- [ ] [P6-T2] Delegate PR creation to `Agent(pr-author)` per `.claude/skills/orchestrate/SKILL.md` § "PR Authoring (pr-author Handoff)", including the canonical `--body-file` path and matching `.receipt.json` per the existing receipt-verification protocol, and confirming the new `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` local preflight check itself passes before the `gh pr create` call is issued.

### Phase 7 — Rollout / Follow-up

- [x] [P7-T1] Perform the manual validation from spec.md's Test Strategy: with the orchestrator-state checkpoint at `artifacts/orchestration/orchestrator-state.json` deliberately deleted or renamed, invoke `gh pr create --body-file artifacts/pr_body_1.md` (or an equivalent dry run) and confirm the hook blocks with `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/manual-validation-block.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** the real live checkpoint was not deleted/renamed (risk to the parent orchestration session's state); used the equivalent dry-run against the real, currently-incomplete checkpoint instead — see artifact for full reasoning.
- [x] [P7-T2] Restore a valid checkpoint and re-run the same command from P7-T1, confirming it proceeds past the preflight check to the existing receipt-check flow (no `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` block); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/manual-validation-allow.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. **Completion note:** used a seam-level stand-in for a passing checkpoint (no fixture file written); confirmed the decision falls through to `PR_AUTHOR_RECEIPT_MISSING`, not the preflight reason.
- [x] [P7-T3] Record a follow-up tracking note for the `.codex/config.toml` re-wiring gap (Codex mirror hook currently orphaned, not wired into any Codex `[[hooks.PreToolUse]]` entry), referencing spec.md's Risks & Mitigations, to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/follow-up-codex-wiring-gap.md`.
- [x] [P7-T4] Record a follow-up tracking note for the absent `quality-tiers.yml` at repo root as a pre-existing, orthogonal gap, referencing spec.md's Risks & Mitigations, to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/follow-up-quality-tiers-gap.md`.
- [x] [P7-T5] Record links (issue #272, spec.md path, research artifact path, PR URL once created) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/links.md` for traceability.
