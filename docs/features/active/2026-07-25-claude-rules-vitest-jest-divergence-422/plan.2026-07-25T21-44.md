# 2026-07-25-claude-rules-vitest-jest-divergence (Plan)

- **Issue:** #422
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-25T21-44
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug (per `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/issue.md`; `spec.md` is required and present; `user-story.md` is intentionally absent and is NOT a blocker)

## Requirements Sources

- `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/spec.md` — authoritative requirements; its `## Acceptance Criteria` section (14 items) is the AC source for this full-bug plan.
- `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/issue.md` — defect record.
- `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/research/2026-07-25T22-15-claude-rules-vitest-jest-divergence-research.md` — authoritative scope adjudication.

## Evidence Location (Non-Overridable)

All evidence artifacts produced by this plan MUST be written under `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/<kind>/` with `<kind>` one of `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`. No `artifacts/`-rooted evidence path is permitted. Every command-step evidence artifact MUST contain `Timestamp:` (format `yyyy-MM-ddTHH-mm`), `Command:`, `EXIT_CODE:`, and `Output Summary:`. Coverage-bearing test artifacts MUST record numeric line and branch coverage headline values, not placeholders.

## Hard Constraints (Executor MUST honor)

1. Do NOT edit any file under `.github/instructions/` (canonical policy surface, protected by `CLAUDE.md`).
2. Do NOT edit root `package.json`, `jest.config.cjs`, `run-jest.cjs`, `tsconfig*.json`, or `.vscode-test.*`. A sibling orchestration owns these files. If the fix appears to require modifying any of them, STOP and report the conflict; do not edit.
3. Do NOT perform a Vitest migration. Explicit non-goal: the instructions are corrected to describe the Jest toolchain the repository actually runs.
4. Do NOT edit `extensions/drm-copilot/resources/customizations/.github/agents/expert-react-frontend-engineer.agent.md` or `extensions/drm-copilot/resources/customizations/.github/instructions/github-actions-ci-cd-best-practices.instructions.md` (research verdict: NOT-A-DEFECT), nor their `.github/` originals.
5. Do NOT fix the `.dependency-cruiser.cjs` reference at `.claude/rules/typescript.md:57`. It is report-only in this feature (see P3-T4).
6. Do NOT edit historical artifacts under `docs/features/completed/**` or other `docs/features/active/**` folders; they are frozen records.
7. Content parity between each repo-root mirror and its bundled copy is mandatory. The two parity tests compare `Path.read_text(encoding="utf-8")` results (UTF-8 text equality after universal-newline translation), so a line-ending-only divergence would pass the test but is still prohibited: the paired edits MUST be textually and byte-wise identical. There is NO regeneration script; each repo-root edit MUST be mirrored by hand into its bundled copy within the same task.
8. Final-QC command tasks are unconditional. `EXIT_CODE: SKIPPED` is not a valid completion outcome for any command-bearing task in this plan.

### Phase 0 — Policy Reads and Baseline Capture

- [ ] [P0-T1] Read `CLAUDE.md` (repo root). Acceptance: file read in full; content acknowledged in the Phase 0 evidence artifact written at P0-T6.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md`. Acceptance: file read in full; listed in the P0-T6 artifact.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md`. Acceptance: file read in full; listed in the P0-T6 artifact.
- [ ] [P0-T4] Read `.claude/rules/python.md` and `.claude/rules/python-suppressions.md` (Python is a changed language: new pytest module). Acceptance: both files read in full; listed in the P0-T6 artifact.
- [ ] [P0-T5] Read `.claude/rules/typescript.md` and `.claude/rules/typescript-suppressions.md` (the corrected content describes the TypeScript toolchain; the pre-fix text of `.claude/rules/typescript.md` is also an edit target). Acceptance: both files read in full; listed in the P0-T6 artifact.
- [ ] [P0-T6] Write the policy-read evidence artifact `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the P0-T1..P0-T5 order), and the explicit list of files read. Acceptance: artifact exists with all three fields populated.
- [ ] [P0-T7] Record the branch/commit baseline: run `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, and `git status --porcelain`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-git-state.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch name, commit SHA, dirty/clean state). Acceptance: artifact exists with all fields populated.
- [ ] [P0-T8] Capture the Python formatting baseline: run `poetry run black --check .`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-python-black.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all fields populated.
- [ ] [P0-T9] Capture the Python linting baseline: run `poetry run ruff check .`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-python-ruff.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all fields populated.
- [ ] [P0-T10] Capture the Python type-check baseline: run `poetry run pyright`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-python-pyright.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error/warning counts). Acceptance: artifact exists with all fields populated.
- [ ] [P0-T11] Capture the Python test-and-coverage baseline: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-python-pytest-coverage.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and NUMERIC line-coverage and branch-coverage percentages (policy floors: line >= 85%, branch >= 75%). Placeholders are invalid. Acceptance: artifact exists with numeric coverage values recorded.
- [ ] [P0-T12] Capture the `.claude/**` parity baseline: run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts"`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-parity-claude.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; baseline result recorded (expected: pass).
- [ ] [P0-T13] Capture the `.agents/**` parity baseline: run `poetry run pytest "tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts"`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/baseline/baseline-parity-codex-agents.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; baseline result recorded (expected: pass).

### Phase 1 — Fail-Before Regression Test

- [ ] [P1-T1] Read the structural precedent `tests/scripts/dev_tools/test_codex_orchestration_contracts.py` (live-tree UTF-8 Markdown reads with required/forbidden substring assertions, module-level `REPO_ROOT = Path(__file__).resolve().parents[3]`), then author the new pytest module `tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py` following that form, asserting all three properties: (a) framework-token absence — for each of the six repo-root mirror files (`.claude/rules/typescript.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/general-code-change.md`, `.claude/agents/atomic-executor.md`, `.agents/skills/general-unit-test/SKILL.md`, `.agents/skills/general-code-change/SKILL.md`) the text contains no `vitest` token (case-insensitive) and no `vi.` API token (regex `\bvi\.[a-zA-Z]`); bundled copies need no separate assertions because the two parity tests transitively extend the guarantee; (b) command resolution — every backtick-wrapped `npm run <script>` token in `.claude/rules/typescript.md` resolves to a key in the `scripts` block of root `package.json`; (c) semantic anchors — the Testing toolchain line of `.claude/rules/typescript.md` names `npm run test:unit` and the coverage line names `npm run test:unit:coverage` (necessary because `npm run test` resolves to `vscode-test` yet is semantically wrong for unit tests). Acceptance: the module exists at `tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`, reads files with `read_text(encoding="utf-8")`, writes nothing, and contains assertions for all three properties.
- [ ] [P1-T2] Make the new module toolchain-clean before evidence capture: run `poetry run black tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`, `poetry run ruff check tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`, and `poetry run pyright tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`. Acceptance: all three commands exit 0 (after any reformat, re-run all three until a clean pass).
- [ ] [P1-T3] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py` against the pre-fix tree and capture the FAILING output as fail-before evidence at `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/regression-testing/fail-before.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero expected), `Output Summary:` naming the failing assertions (token hits at the known lines; `npm run test:coverage` unresolvable; Testing line names `npm run test`). Acceptance: artifact exists, records a non-zero exit code, and predates any Phase 2 mirror edit.

### Phase 2 — Paired Mirror Corrections (repo-root + bundled copy in the same task)

- [ ] [P2-T1] Correct pair 1: edit `.claude/rules/typescript.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/rules/typescript.md` byte-identically — line 16: "**Testing — Jest**: All TypeScript unit tests must use Jest. Command: `npm run test:unit`" (NOT `npm run test`, which maps to `vscode-test`, the integration runner); line 42: use **Jest** as the test framework; line 47: `jest.spyOn`/`jest.mock` for mocking and `jest.resetAllMocks()` in `afterEach`; line 51: coverage command `npm run test:unit:coverage` (no `test:coverage` script exists) and remove or replace the stale clause "(the script is wired in Prompt B1 alongside the Vitest dependency)" with accurate text; line 73: Jest fake timers (`jest.useFakeTimers()`). Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t1.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the corrections, the parity test passes, and the artifact exists.
- [ ] [P2-T2] Correct pair 2: edit `.claude/rules/general-unit-test.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-unit-test.md` byte-identically — line 40: `vitest.config.ts` → `jest.config.cjs` in the permitted coverage-exclude list; line 105: `vi.useFakeTimers()` for Vitest → `jest.useFakeTimers()` for Jest in the fake-timer facility parenthetical. Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t2.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the corrections, the parity test passes, and the artifact exists.
- [ ] [P2-T3] Correct pair 3: edit `.claude/rules/general-code-change.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-code-change.md` byte-identically — line 39: the unit-test-stage example list `(e.g., Pytest, Vitest, MSTest, Pester)` enumerates this repository's actual per-language runners; the TypeScript slot must read Jest: `(e.g., Pytest, Jest, MSTest, Pester)`. Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t3.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the correction, the parity test passes, and the artifact exists.
- [ ] [P2-T4] Correct pair 4: edit `.claude/agents/atomic-executor.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/agents/atomic-executor.md` byte-identically — line 18: allowlist entry `"Bash(npx vitest *)"` → `"Bash(npx jest *)"`; line 79: TypeScript toolchain command list `npx vitest` → `npx jest` (precedents: hook regex `npx (prettier|eslint|tsc|jest)` in `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1:67`; canonical `.github/agents/typescript-engineer.agent.md`). Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t4.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the corrections, the parity test passes, and the artifact exists.
- [ ] [P2-T5] Correct pair 5: edit `.agents/skills/general-unit-test/SKILL.md` and `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-unit-test/SKILL.md` byte-identically — line 45: `vitest.config.ts` → `jest.config.cjs`; line 110: `vi.useFakeTimers()` for Vitest → `jest.useFakeTimers()` for Jest — the same corrections as P2-T2, in lockstep. Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t5.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the corrections, the parity test passes, and the artifact exists.
- [ ] [P2-T6] Correct pair 6: edit `.agents/skills/general-code-change/SKILL.md` and `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-code-change/SKILL.md` byte-identically — line 44: `(e.g., Pytest, Vitest, MSTest, Pester)` → `(e.g., Pytest, Jest, MSTest, Pester)` — the same correction as P2-T3, in lockstep. Then run `poetry run pytest "tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts"` and record it in `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/parity-after-p2-t6.<yyyy-MM-ddTHH-mm>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: both files carry the correction, the parity test passes, and the artifact exists.

### Phase 3 — Pass-After Verification and Non-Goal Checks

- [ ] [P3-T1] Run `poetry run pytest tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py` against the fixed tree; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/regression-testing/pass-after.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (0 required), `Output Summary:`. Acceptance: exit code 0 and artifact exists.
- [ ] [P3-T2] Verify the newly allowlisted command form. First, confirm by static inspection that (a) root `package.json` declares `"jest": "^30.4.2"` under `devDependencies`, (b) `jest.config.cjs` exists at the repo root, and (c) `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1:67` recognizes the command family `npx (prettier|eslint|tsc|jest)`. Second, attempt one confirmation invocation `npx jest --listTests` at the repo root (bare `npx jest` bypasses the `run-jest.cjs` `--testPathPattern` to `--testPathPatterns` alias rewrite, so use Jest 30 flag names only). If `node_modules/` is absent at both `./` and `extensions/drm-copilot/`, or `npx` cannot resolve Jest without a registry fetch, record the invocation as `EXIT_CODE: NOT RUN` with that reason. This skip branch is explicitly authorized by this task text and is a valid, non-blocking completion outcome; it does not require a plan revision and does not permit any ad-hoc edit. Write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/npx-jest-resolution.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording all three static-inspection results and the invocation outcome. Acceptance: artifact exists recording the three static checks and the invocation outcome (executed or `NOT RUN` with reason).
- [ ] [P3-T3] Verify non-goals: run `git status --porcelain` and `git diff --name-only` and confirm NO changes under `.github/instructions/`, and NO changes to root `package.json`, `jest.config.cjs`, `run-jest.cjs`, `tsconfig*.json`, `.vscode-test.*`, `extensions/drm-copilot/resources/customizations/.github/agents/expert-react-frontend-engineer.agent.md`, or `extensions/drm-copilot/resources/customizations/.github/instructions/github-actions-ci-cd-best-practices.instructions.md`; confirm the changed-file set is exactly the twelve mirror files plus the new test module plus feature-folder artifacts; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/non-goal-verification.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the full changed-file list). Acceptance: artifact exists and the changed-file list contains no out-of-scope path.
- [ ] [P3-T4] Record the report-only adjacent finding for separate filing: write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/other/adjacent-finding-dependency-cruiser.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:` and a note that `.claude/rules/typescript.md:57` names `.dependency-cruiser.cjs`, which does not exist anywhere in the repository; this defect is unrelated to the Vitest/Jest divergence, was NOT fixed in this feature, and is recorded for separate filing. Acceptance: artifact exists and no edit was made to `.claude/rules/typescript.md` line 57 content.

### Phase 4 — Acceptance Criteria Check-off (source: `spec.md` `## Acceptance Criteria`)

All tasks in this phase verify one AC item from `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/spec.md` against on-disk state/evidence and then check the corresponding checkbox in that file.

- [ ] [P4-T1] Verify AC 1 (`.claude/rules/typescript.md` corrected to Jest at lines 16, 42, 47, 51, 73 with `jest.spyOn`/`jest.mock`/`jest.resetAllMocks` and `jest.useFakeTimers()`; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited (P2-T1 edits, P0/P3 parity evidence).
- [ ] [P4-T2] Verify AC 2 (line 16 names `npm run test:unit`, not `npm run test`) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T3] Verify AC 3 (line 51 names `npm run test:unit:coverage`; stale "Prompt B1" clause removed or replaced) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T4] Verify AC 4 (`.claude/rules/general-unit-test.md` lines 40, 105 corrected; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T5] Verify AC 5 (`.claude/rules/general-code-change.md` line 39 names Jest; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T6] Verify AC 6 (`.claude/agents/atomic-executor.md` line 18 allowlists `Bash(npx jest *)`, line 79 names `npx jest`; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T7] Verify AC 7 (`.agents/skills/general-unit-test/SKILL.md` lines 45, 110 corrected; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T8] Verify AC 8 (`.agents/skills/general-code-change/SKILL.md` line 44 corrected; bundled copy byte-identical) and check it off in `spec.md`. Acceptance: checkbox checked with verification basis cited.
- [ ] [P4-T9] Verify AC 9 (both parity tests pass) against the `[P2-T4]` `.claude`-parity artifact and the `[P2-T6]` `.agents`-parity artifact, which record the last parity run after the final mirror edit in each family, and check it off in `spec.md`. Acceptance: checkbox checked with both evidence paths cited.
- [ ] [P4-T10] Verify AC 10 (new regression test exists with all required assertions and passes against the fixed tree) and check it off in `spec.md`. Acceptance: checkbox checked with evidence path cited (P3-T1 artifact).
- [ ] [P4-T11] Verify AC 11 (fail-before evidence recorded) against `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/regression-testing/fail-before.<yyyy-MM-ddTHH-mm>.md` and check it off in `spec.md`. Acceptance: checkbox checked with evidence path cited.
- [ ] [P4-T12] Verify AC 12 (non-goals respected) against the P3-T3 artifact and check it off in `spec.md`. Acceptance: checkbox checked with evidence path cited.
- [ ] [P4-T13] Verify AC 13 (`.dependency-cruiser.cjs` finding not folded in and recorded for separate filing) against the P3-T4 artifact and check it off in `spec.md`. Acceptance: checkbox checked with evidence path cited.

### Phase 5 — Final QA Loop (Python) and Final Verification

Loop rule: if any step in P5-T1..P5-T4 fails or changes files, restart from P5-T1 and repeat until all four steps pass cleanly in a single pass. All command tasks in this phase are unconditional; `SKIPPED` is not a valid completion outcome.

- [ ] [P5-T1] Final-QC formatting: run `poetry run black .`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/final-python-black.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (files changed count; a clean pass reports 0 files reformatted). Acceptance: artifact exists; final recorded pass changed no files.
- [ ] [P5-T2] Final-QC linting: run `poetry run ruff check .`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/final-python-ruff.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; exit code 0.
- [ ] [P5-T3] Final-QC type checking: run `poetry run pyright`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/final-python-pyright.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (0 errors required). Acceptance: artifact exists; exit code 0.
- [ ] [P5-T4] Final-QC tests with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/final-python-pytest-coverage.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and NUMERIC post-change line-coverage and branch-coverage percentages. Placeholders are invalid. Acceptance: artifact exists; exit code 0; numeric coverage values recorded.
- [ ] [P5-T5] Coverage delta verification: compare the P0-T11 baseline coverage values against the P5-T4 post-change values; confirm line coverage >= 85%, branch coverage >= 75%, and no regression attributable to this change (expected delta: none — only test code and Markdown changed; the new module is test code and is excluded from the coverage denominator per policy; record new/changed-code coverage as not-applicable with that rationale). Write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/coverage-delta.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, baseline numeric values, post-change numeric values, the new-code rationale, and a PASS/FAIL verdict. If required numeric values are unavailable or thresholds are not met, the outcome is remediation-required, not PASS. Acceptance: artifact exists with both numeric value sets and an explicit verdict.
- [ ] [P5-T6] Final explicit verification of the two parity tests plus the new regression module: run `poetry run pytest "tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts" "tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts" tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`; write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/final-parity-and-regression.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (0 required), `Output Summary:` (per-test pass counts). Acceptance: artifact exists; exit code 0; all listed tests pass.

### Phase 6 — End-State Verification and Post-QA Acceptance Criteria Check-off

- [ ] [P6-T1] Re-verify the end-state changed-file set after the final QA loop (necessary because `[P5-T1]` runs `poetry run black .` in repo-wide write mode after the `[P3-T3]` verification): run `git status --porcelain` and `git diff --name-only`; confirm the changed-file set still contains no path under `.github/instructions/`, no root `package.json`, `jest.config.cjs`, `run-jest.cjs`, `tsconfig*.json`, or `.vscode-test.*`, and no Python file outside `tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py`. Write `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/qa-gates/end-state-changed-files.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the full changed-file list and a diff against the `[P3-T3]` recorded set). Acceptance: artifact exists and the end-state changed-file list contains no out-of-scope path.
- [ ] [P6-T2] Verify AC 14 (full toolchain pass for the changed test module: format, lint, type-check, test) against the `[P5-T1]`..`[P5-T4]` QA-gate artifacts and check it off in `spec.md`. Acceptance: checkbox checked with all four evidence paths cited.
