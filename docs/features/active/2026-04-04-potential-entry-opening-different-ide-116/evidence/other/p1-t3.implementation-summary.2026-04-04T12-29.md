Timestamp: 2026-04-04T12-29
Scope: Phase 1 (`P1-T1` through `P1-T3`) only.

Modified Files:
- `tests/scripts/dev_tools/test_new_potential_bug_entry.py` — supports issue acceptance criterion 3 with targeted regression coverage for normal CLI resolution, Insiders-aware CLI resolution, `--reuse-window`, and no-CLI fallback.
- `tests/scripts/dev_tools/test_new_active_feature_folder.py` — supports issue acceptance criterion 3 with targeted regression coverage for normal CLI resolution, Insiders-aware CLI resolution, `--reuse-window`, and no-CLI fallback.
- `scripts/dev_tools/new_potential_bug_entry.py` — supports issue acceptance criterion 3 directly and supports issue acceptance criterion 1 indirectly by aligning the root launcher behavior with the intended Windows launch contract.
- `scripts/dev_tools/new_active_feature_folder_io.py` — supports issue acceptance criterion 3 directly and supports issue acceptance criterion 2 indirectly by aligning the root launcher behavior with the intended Windows launch contract.
- `extensions/drm-copilot/resources/scripts/dev_tools/new_potential_bug_entry.py` — supports issue acceptance criterion 1 by updating the bundled potential-bug launcher contract to prefer the matching CLI and pass `--reuse-window`; also supports issue acceptance criterion 3.
- `extensions/drm-copilot/resources/scripts/dev_tools/new_active_feature_folder_io.py` — supports issue acceptance criterion 2 by updating the bundled active-feature launcher contract to prefer the matching CLI and pass `--reuse-window`; also supports issue acceptance criterion 3.

Additional Helper File Status:
- No additional helper file was required or introduced in Phase 1.

`code-insiders` Preference Logic:
- Added in both launcher implementations (`new_potential_bug_entry.py` and `new_active_feature_folder_io.py`) in both the root mirror and bundled extension copy.
- Detection signals checked in deterministic order: `TERM_PROGRAM_VERSION`, `VSCODE_GIT_ASKPASS_MAIN`, `TERM_PROGRAM`, and `VSCODE_IPC_HOOK_CLI`.
- Resolution order is session-aware: Insiders session prefers `code-insiders` then falls back to `code`; non-Insiders session prefers `code` then falls back to `code-insiders`.

`--reuse-window` Confirmation:
- Present in the potential-bug launcher path in both `scripts/dev_tools/new_potential_bug_entry.py` and `extensions/drm-copilot/resources/scripts/dev_tools/new_potential_bug_entry.py`.
- Present in the active-feature launcher path in both `scripts/dev_tools/new_active_feature_folder_io.py` and `extensions/drm-copilot/resources/scripts/dev_tools/new_active_feature_folder_io.py`.

Evidence Used for Issue Acceptance Criterion 1:
- Code evidence: `extensions/drm-copilot/resources/scripts/dev_tools/new_potential_bug_entry.py` now resolves the VS Code CLI with Insiders-aware ordering and invokes it with `--reuse-window`.
- Regression evidence: `docs/features/active/2026-04-04-potential-entry-opening-different-ide-116/evidence/regression-testing/p1-t2.targeted-pytest.2026-04-04T12-29.md`.
- Live Windows verification evidence: none recorded in Phase 1.

Evidence Used for Issue Acceptance Criterion 2:
- Code evidence: `extensions/drm-copilot/resources/scripts/dev_tools/new_active_feature_folder_io.py` now resolves the VS Code CLI with Insiders-aware ordering and invokes it with `--reuse-window`.
- Regression evidence: `docs/features/active/2026-04-04-potential-entry-opening-different-ide-116/evidence/regression-testing/p1-t2.targeted-pytest.2026-04-04T12-29.md`.
- Live Windows verification evidence: none recorded in Phase 1.

Originating-Window Reuse Status:
- `drmCopilotExtension.newPotentialBugEntry`: UNVERIFIED — deterministic live Windows verification was not executed in Phase 1, so originating-window reuse cannot be claimed as observed.
- `drmCopilotExtension.newActiveFeatureFolder`: UNVERIFIED — deterministic live Windows verification was not executed in Phase 1, so originating-window reuse cannot be claimed as observed.

Control-Path Comparison Against `drmCopilotExtension.newPotentialEntry`:
- The PowerShell-backed control path already prefers `code-insiders` when the session indicates Insiders and passes `--reuse-window`.
- Phase 1 aligns the two Python launcher paths with that same contract instead of changing extension-host command registration or result plumbing.
- This keeps the fix at the existing launcher seam named in `issue.md` and the approved plan.

Verification Status:
- Acceptance Criterion 1: UNVERIFIED / remediation required.
  - Reason: no explicit live Windows verification evidence shows that `drmCopilotExtension.newPotentialBugEntry` reused the originating VS Code or VS Code Insiders window after the fix.
- Acceptance Criterion 2: UNVERIFIED / remediation required.
  - Reason: no explicit live Windows verification evidence shows that `drmCopilotExtension.newActiveFeatureFolder` reused the originating VS Code or VS Code Insiders window after the fix.
- Acceptance Criterion 3: VERIFIED by targeted regression coverage.
  - Evidence: `docs/features/active/2026-04-04-potential-entry-opening-different-ide-116/evidence/regression-testing/p1-t1.red-pytest.2026-04-04T12-26.md` and `docs/features/active/2026-04-04-potential-entry-opening-different-ide-116/evidence/regression-testing/p1-t2.targeted-pytest.2026-04-04T12-29.md`.
