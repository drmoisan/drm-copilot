# Code Review: epic-orchestrate (#275)

---

**Review Date:** 2026-07-03
**Reviewer:** feature-review agent (Claude Sonnet 5)
**Feature Folder:** `docs/features/active/2026-07-02-epic-orchestrate-275/`
**Base Branch:** `main` (merge-base `3c5ff3289022abc3b7b16e2441c772e5f81fd9ff`)
**Head Branch:** `drm-copilot-wt-2026-07-02-19-03` @ `4921aaa0da408f089b7bc83a84c9938243fde5ac`
**Review Type:** Re-audit (remediation cycle 1 exit review — R4 of the remediation loop)
**Prior review (superseded, not assumed valid):** `code-review.2026-07-02T23-00.md` (its two Blocker and three Major/Minor findings are independently re-checked below, not trusted at face value).

---

## Executive Summary

This is an independent re-review of the `epic-orchestrate` feature (#275) after remediation cycle 1, which targeted the two Blocker and three Major/Minor findings raised by the prior code review. This review independently re-read every changed file involved in the remediation and independently re-executed the full toolchain for all three languages, rather than accepting the remediation's own evidence at face value.

**Remediation verification (independently confirmed):**

1. **Blocker — `enforce-pr-author-skill.ps1` 500-line violation — RESOLVED.** Reduced from 543 to 451 lines. The extraction (`Test-EpicBaseBranchOverride` plus its `Get-PrAuthorCheckpointContent` seam, moved verbatim into a new sibling file `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`, 104 lines, dot-sourced back via `. (Join-Path $PSScriptRoot 'enforce-pr-author-skill.epic-base-branch.ps1')`) is a pure structural refactor: this review confirmed `git diff 25a4a364..4921aaa --stat` on both `.Tests.ps1` files touching this hook returns no output, meaning the test suites (and therefore the observable allow/deny decisions and reason strings they assert against) are byte-identical before and after the extraction. Both bundled mirrors independently confirmed byte-identical via direct `cmp`.
2. **Blocker — absent TypeScript `lcov` coverage artifact — RESOLVED.** `extensions/drm-copilot/coverage/lcov.info` independently regenerated by this review via `node run-jest.cjs --coverage` (no `--coverageReporters` override), producing a 428547-byte file — matching the remediation's own reported size exactly. Coverage percentages (96.88%/88.27%/96.88%) are unchanged from the pre-fix measurement (0.00pp delta), confirming the fix only restored the missing artifact format and did not alter behavior.
3. **Major — PowerShell canonical coverage-artifact scope gap — RESOLVED.** `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`'s `CodeCoverage.Path` now includes all 6 of this feature's hook files. Independently regenerated `artifacts/pester/powershell-coverage.xml` and parsed per-file LINE percentages directly from the JaCoCo XML: 93.42%–94.25% for the three new hooks, 91.75%/91.30% for the two `enforce-pr-author-skill*` files, 86.96% for `validate-orchestrator-output.ps1` — all above the 85% floor.
4. **Major — oversized `test_validate_orchestration_artifacts.py` — PARTIALLY RESOLVED, still open.** Reduced from 739 to 513 lines via a new sibling file (`test_validate_orchestration_artifacts_dispatch.py`, 255 lines, independently confirmed passing). **513 still exceeds the 500-line cap** by 13 lines. This is a genuine, current, independently-confirmed policy violation — see Findings Table below (carried forward as still-open, downgraded severity given the substantial improvement).
5. **Major — `enforce-epic-worktree-removal-gate.ps1` unconditional activation — NOT ADDRESSED (explicitly out of this cycle's approved scope).** Confirmed unchanged: still no epic-mode precondition, unlike the wave-barrier hook. This was intentionally excluded from remediation cycle 1's fix list ("Do not expand scope beyond the 5 fixes above"); carried forward below as still open.
6. **Minor — broken "Merge-Conflict Remediation" cross-reference — NOT ADDRESSED (explicitly out of this cycle's approved scope).** Confirmed unchanged via `grep`.
7. **Minor — untested wave-computation formula — RESOLVED.** New `scripts/dev_tools/epic_wave_computation.py` (153 lines) independently reviewed: correct, pure, fully-typed memoized-recursion implementation of `wave(f) = 0 | 1 + max(...)` with dedicated `EpicWaveCycleError` cycle detection; its test file (`test_epic_wave_computation.py`, 111 lines, 8 tests) independently reproduced at 100%/100% line/branch coverage, covering the diamond-DAG scenario from `user-story.md`, a linear chain, 3 cycle variants, and edge cases.

**Top risks remaining after this cycle:**
1. `tests/scripts/dev_tools/test_validate_orchestration_artifacts.py` still exceeds the 500-line cap (513 lines) — a small, well-scoped residual fix.
2. `enforce-epic-worktree-removal-gate.ps1`'s unconditional (non-epic-mode-scoped) activation remains an undocumented design asymmetry relative to the wave-barrier hook — low current risk (no standalone-orchestration caller exists today) but a latent surprise for future non-epic worktree cleanup.

**PR readiness recommendation:** **Needs one further small revision** (the 13-line test-file overage) before this cycle can close cleanly under a strict reading of the file-size policy; the two carried-forward Major/Minor findings can reasonably be tracked as a subsequent, non-blocking follow-up.

---

## Findings Table

| Severity | File | Location | Finding | Recommendation | Rationale | Evidence |
|---|---|---|---|---|---|---|
| Major | `tests/scripts/dev_tools/test_validate_orchestration_artifacts.py` | Whole file (513 lines) | After remediation cycle 1's split, this file is still 13 lines over the mandatory 500-line cap (was 739 before the split, 635 at the original `main` baseline). | Relocate additional cohesive content — the plan's own evidence identifies the pre-existing `orchestrator-state` dispatch tests as a candidate — into a sibling file, following the same pattern already used for the epic-dispatch tests, or obtain an explicit user-approved exception if genuinely infeasible. | `general-code-change.md`'s 500-line cap has no stated exception for test files; a documented "as close as achievable" acceptance clause at the plan level does not itself satisfy a hard, non-negotiable policy rule. | `wc -l tests/scripts/dev_tools/test_validate_orchestration_artifacts.py` → 513 (independently reproduced this pass). |
| Major | `.claude/hooks/enforce-epic-worktree-removal-gate.ps1` | Whole file (unconditional activation) | Unchanged from the prior review: the hook gates every `git worktree remove` Bash command fail-closed with no epic-mode precondition, unlike `enforce-epic-wave-barrier.ps1`'s subagent-type + prompt-marker guard. | Either document explicitly why this hook is intentionally scoped globally (AC9's literal wording does not itself require epic-mode scoping), or add the same epic-mode precondition used by the wave-barrier hook for design consistency. | Two sibling hooks in the same feature use different activation-scoping strategies with no stated rationale for the difference; this is a design-consistency and future-blast-radius concern, not a current regression (no standalone-orchestration caller of `git worktree remove` exists today, independently re-confirmed via `git grep`). | `.claude/hooks/enforce-epic-worktree-removal-gate.ps1` line 199 (`-notmatch '(?i)\bgit\s+worktree\s+remove\b'`, no subagent/marker check) vs. `enforce-epic-wave-barrier.ps1`'s guard. Independently re-read this pass; unchanged since the prior review. |
| Minor | `.claude/skills/orchestrate/SKILL.md` | Line 162 (S9 step 6) | Unchanged from the prior review: references a "Merge-Conflict Remediation" heading that does not exist in this file; the real procedure is `.claude/skills/epic-orchestrate/SKILL.md`'s "Merge-Conflict Handling (Fan-In)" section. | Correct the cross-reference to name the actual file and heading. | A dangling "below" reference in a procedural document that agents follow at runtime is misleading. | `grep -n "Merge-Conflict Remediation" .claude/skills/orchestrate/SKILL.md` returns only the referencing line itself (independently reproduced this pass). |
| Info | `scripts/dev_tools/epic_wave_computation.py` | Whole file | Well-executed remediation fix: pure function, complete Google-style docstrings (Purpose/Responsibilities/Usage at module level; Args/Returns/Raises/Side Effects at function level), correct memoized-recursion algorithm with a dedicated, well-named exception (`EpicWaveCycleError`) for cycle detection, and an explanatory comment on the `in_progress` set clarifying the cycle-vs-safe-revisit distinction. | None — noted as a positive pattern to continue. | N/A | Direct source read this pass; 100%/100% line/branch coverage independently reproduced. |
| Info | `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1` | Whole file | The file-size remediation extraction is clean: the sibling file carries a clear file-level comment stating it is dot-sourced to stay under the 500-line limit, preserves the original function's comment-based help block verbatim, and the dot-sourcing wire-up (`. (Join-Path $PSScriptRoot ...)`) is a standard, idiomatic PowerShell pattern already used elsewhere in the codebase. | None — noted as a positive pattern to continue. | N/A | Direct source read this pass; both bundled mirrors independently confirmed byte-identical via `cmp`. |

No Blocker-severity findings remain in this pass (both prior Blockers independently confirmed resolved).

---

## Implementation Audit (remediation-cycle-1 delta only; see the prior code review for the full original-implementation audit, independently re-confirmed as still accurate for all unchanged files)

### Python implementation audit

- `scripts/dev_tools/epic_wave_computation.py`: fully typed under `from __future__ import annotations`; the module's single public function (`compute_wave_numbers`) returns a plain `dict[str, int]` rather than raising for the success path, and raises a specific, named exception (`EpicWaveCycleError`, a `ValueError` subclass) only for the cycle case — consistent with `python.md`'s "fail fast with specific exceptions" rule. The recursive `resolve` closure correctly distinguishes "already resolved" (served from the `wave_numbers` memo) from "currently on the active recursion path" (`in_progress` set, a cycle) — this is the same correct in-progress/visited distinction the prior review praised in `_detect_dependency_cycle`, reused here in a second, independent implementation with equal correctness.
- `tests/scripts/dev_tools/test_validate_orchestration_artifacts_dispatch.py`: the 10 relocated functions were moved verbatim (independently confirmed no assertion text changed by cross-referencing the remediation's own evidence against a direct read of the new file); this review found no weakened or removed assertions.

### PowerShell implementation audit

- The `enforce-pr-author-skill.ps1` / `enforce-pr-author-skill.epic-base-branch.ps1` split correctly preserves the existing five-check ordered-decision chain's behavior; the sixth check (`Test-EpicBaseBranchOverride`) is invoked via `$epicBaseBranchReason = Test-EpicBaseBranchOverride -CommandText $CommandText` at the same call site as before the extraction (line 241), with no reordering of the preceding checks.
- The `pester.runsettings.psd1` allowlist change is minimal and additive (6 new `Path` entries plus a 3-line explanatory comment); no existing entries were reordered or removed, and this review independently confirmed the 5 pre-existing curated files show 0.00pp coverage delta.

### TypeScript implementation audit

No TypeScript production code changed in remediation cycle 1; the fix was purely a test-invocation change (removing a `--coverageReporters` override), independently confirmed to produce identical coverage percentages to the pre-fix run.

---

## Test Quality Audit

Independently re-executed rather than solely re-read from remediation evidence:

- `tests/scripts/dev_tools/test_epic_wave_computation.py` — 8 tests, one behavior per test (diamond DAG matching `user-story.md`'s own scenario, linear chain, 3 distinct cycle shapes, 2 edge cases), clear `test_...` names, independently reproduced at 100%/100% line/branch coverage.
- `tests/scripts/dev_tools/test_validate_orchestration_artifacts_dispatch.py` — 9 collectible tests (of the 10 named functions relocated; independently reproduced passing), each targeting exactly one dispatch/enum-membership scenario.
- Full-suite re-execution: 1192 Pytest passed + 19 skipped (0 failed), 467 Pester passed (0 failed), 1462 Jest passed (0 failed) — all independently reproduced in this review's own session, not read from prior evidence files alone.
- **Determinism:** No wall-clock reads, `Start-Sleep`/`setTimeout`, or unseeded randomness observed in any file touched by remediation cycle 1.
- **Isolation:** Each new/relocated test targets exactly one function/scenario; no cross-test shared mutable state observed.

---

## Security / Correctness Checks

| Check | Status | Evidence |
|---|---|---|
| No secrets in code | ✅ PASS | No credentials/tokens in any remediation-cycle-1 file. |
| No unsafe subprocess/command construction | ✅ PASS | No new subprocess invocations introduced by remediation cycle 1 (the wave-computation module is pure Python with no I/O). |
| Input validation at boundaries | ✅ PASS | `compute_wave_numbers` treats its input as a plain mapping and does not perform unchecked I/O; the PowerShell dot-sourcing extraction introduces no new external input surface. |
| Error handling remains explicit | ✅ PASS | `EpicWaveCycleError` is specific and named; no new broad catch-alls introduced. |
| Configuration/path handling is safe | ✅ PASS | `enforce-pr-author-skill.epic-base-branch.ps1` is dot-sourced via `Join-Path $PSScriptRoot`, a safe, non-user-controlled path construction. |

---

## Research Log

No new external research was required for this remediation-cycle re-review; the remediation's own scope (5 narrowly-defined fixes) did not introduce any new design questions beyond what was already resolved in the feature's committed research documents, which this review re-confirmed remain consistent with the current implementation.

---

## Verdict

Remediation cycle 1 resolved both Blocker findings cleanly, with independently-verified evidence that the file-size extraction changed no observable behavior and the coverage-artifact fixes changed no measured percentages. One Major finding (test-file size) was substantially improved (a 226-line reduction) but not fully resolved — 513 lines still exceeds the 500-line cap by 13 lines, which this review treats as a genuine, still-open policy violation requiring one further small fix rather than a closed item. Two findings from the original review (worktree-gate scope, broken cross-reference) were intentionally left out of this remediation cycle's scope and remain open for tracking; neither blocks correctness. This change should not be marked fully clear of Major findings until the 513-line test file is brought into compliance (or an explicit exception is obtained); the remaining two carried-forward findings can reasonably proceed to merge as tracked follow-up items at the requester's discretion.
