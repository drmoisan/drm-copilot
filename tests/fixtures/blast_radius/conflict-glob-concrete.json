{
  "description": "Glob x concrete overlap: the ** glob of one radius matches the concrete path of the other by the shared fnmatch subset. The pair detail is rendered with its two entries in ordinal order, so the asterisk sorts before the letter c.",
  "input": {
    "radius_a": {
      "paths": ["scripts/dev_tools/**"],
      "modules": [],
      "shared_surfaces": [],
      "contracts": [],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "radius_b": {
      "paths": ["scripts/dev_tools/compute_blast_radius.py"],
      "modules": [],
      "shared_surfaces": [],
      "contracts": [],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "config": {
      "version": 1,
      "shared_surfaces": ["config/orchestration-routing.json", "poetry.lock"],
      "shared_surface_globs": ["scripts/dev_tools/validate_*.py"],
      "modules": {
        "config": ["config/**"],
        "docs": ["docs/**"],
        "python-dev-tools": ["scripts/dev_tools/**"],
        "tests": ["tests/**"]
      },
      "over_breadth_fraction": 0.25
    }
  },
  "expected": {
    "conflict": true,
    "reasons": [
      {
        "kind": "path_overlap",
        "detail": "scripts/dev_tools/** ~ scripts/dev_tools/compute_blast_radius.py"
      }
    ]
  }
}
