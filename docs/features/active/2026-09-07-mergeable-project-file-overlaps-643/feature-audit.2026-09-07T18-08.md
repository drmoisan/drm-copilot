# Feature Audit: mergeable-project-file-overlaps (Issue #643)

**Audit Date:** 2026-09-07
**Timestamp clock:** UTC (`2026-09-07T18-08` = 2026-09-07 18:08Z)
**Feature Folder:** `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643`
**Base Branch:** `main`
**Head Branch:** `feature/mergeable-project-file-overlaps-643`
**Work Mode:** `full-feature`
**Audit Type:** Initial acceptance review

---

## Scope and Baseline

- **Base branch:** `main` (commit `ef83a2e0e26fc8486d6b9be259bf195f0a8e29c9`)
- **Head branch/commit:** `feature/mergeable-project-file-overlaps-643` (commit `515bf1547dcebf029238d3c04ac764ea0462876d`)
- **Merge base:** `ef83a2e0e26fc8486d6b9be259bf195f0a8e29c9`, resolved by merge-base ancestry per `.claude/skills/pr-base-branch-merge-base/SKILL.md`. The branch was rebased onto `origin/main` at that commit before this review; the pre-rebase merge base was `c3ffb080`.
- **Range audited:** `ef83a2e0..515bf154` — 166 files, +10,860 / −334, 11 commits
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (generated 2026-09-07 17:57:40 UTC, head SHA `515bf154`, base `origin/main @ ef83a2e0`)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/` — 44 artifacts across `baseline/` (14), `qa-gates/` (26), `other/` (5), `issue-updates/` (1)
  - Coverage artifacts (parsed, not regenerated): `artifacts/python/lcov.info`, `extensions/drm-copilot/coverage/lcov.info`, `artifacts/pester/powershell-coverage.xml`
  - Plan of record: `plan.2026-09-07T08-13.md` (118 tasks, all checked)
  - Companion artifacts from this review: `policy-audit.2026-09-07T18-08.md`, `code-review.2026-09-07T18-08.md`
- **Feature folder used:** `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643`. Selection rule: the only active folder whose suffix matches the issue number in the branch name, and the only folder with changed scoping documents in the branch diff. Not a versioned feature — documents live at the feature root, so no `v1/`/`v2/` scope selection applies and artifacts are written to the feature root.
- **Requirements source:** `spec.md` and `user-story.md` (multiple files)
- **Work mode resolution note:** The marker is explicit. `issue.md` line 10 reads `- Work Mode: full-feature`. Per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, `full-feature` resolves to `spec.md` **and** `user-story.md`. No fail-closed default was needed. `issue.md` also carries a `## Acceptance Criteria (early draft)` section with 15 unchecked boxes; under `full-feature` that section is **not** an authoritative AC source and its unchecked state is not an acceptance gap. It is addressed in the Notes below.
- **Scope note:** The audit is feature-vs-base over the full branch diff. No caller instruction attempted to narrow it; see `## Rejected Scope Narrowing` in the companion policy audit, which records "None." PR context was regenerated by the caller at the current head, so no refresh was required. The reviewer verified currency directly: the artifacts name head `515bf154` and base `ef83a2e0`, matching the audited range, and `git status --porcelain --untracked-files=all` is empty.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**

- `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` — primary (51 checkbox items across three sections)
- `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` — co-primary (11 checkbox items under `## Acceptance Criteria`)

Counts were taken with the canonical counter, `.claude/lib/requirements/GeneratedDocumentCounters.psm1`,
invoked as `Get-NamedSectionCheckboxCount -Document <raw> -Heading 'Acceptance Criteria'`:
`spec.md` → 39, `user-story.md` → 11. The counter scopes to the named section only. Direct checkbox
counts over the whole files give `spec.md` 51 and `user-story.md` 11, the difference being `spec.md`'s
two further checkbox sections, `## Definition of Done` (7) and `## Seeded Test Conditions (from
potential)` (5): 39 + 7 + 5 = 51. All three `spec.md` sections are treated as AC per the caller's
instruction and per the executor's own accounting in
`evidence/issue-updates/ac-status-summary.2026-09-07T19-46.md`. **Total in scope: 62.**

### From `spec.md` — `## Acceptance Criteria` (S1–S39)

1. **S1** Both `config/blast-radius.json` and `extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json` carry `mergeable_paths` with exactly the entries `**/*.csproj`, `**/packages.config`, `**/app.config`, `**/*.vbproj`, `**/*.props`, byte-equal between the copies.
2. **S2** The key is optional and fail-closed: for a config omitting `mergeable_paths` and for a config carrying an empty list, `conflicts` and `Test-BlastRadiusConflict` return results identical to the pre-change behavior.
3. **S3** `config_mergeable_paths` and `Get-ConfigMergeablePath` return an empty sequence for an absent key and reject a non-list value or a blank entry, matching the existing string-list reader shape.
4. **S4** A pair of radii whose only path overlap is a `.csproj` produces no `path_overlap` reason and no conflict, while both radii still list that `.csproj` in `paths`.
5. **S5** A declared glob entry that would contain a mergeable path (for example `Proj/**`) still contends, because the matcher applies glob containment to concrete entries only.
6. **S6** A pattern beginning with `**/` also matches the corresponding root-level file inside the mergeable matcher, without any change to `_glob_to_regex_text` or its PowerShell mirror.
7. **S7** The exclusion is applied only inside the contention relation: `derive_blast_radius`, `normalize_declared_radius`, `radius_from_observed_paths`, `validate_blast_radius`, and `detect_escaped_paths` read no `mergeable_paths` key, and V1/V2/V3 findings are identical with and without the key.
8. **S8** Drift recomputation inherits the exclusion through the single Python production call site of `conflicts`: `recompute_conflicts_with_observed` reports no new pair for a `.csproj`-only observed overlap.
9. **S9** `.claude/lib/blast-radius/BlastRadiusConflict.psm1` exists with `Get-ConfigMergeablePath`, `Test-MergeablePath`, `Get-NonMergeablePathEntry`, and the relocated `Get-SmallestPathOverlap` and `Get-SmallestCommonEntry`; `Test-BlastRadiusConflict` imports it and filters both path arrays before the overlap computation.
10. **S10** The three PowerShell procedural consumers of the relation inherit the exclusion without code change, and the prose in each states that the relation now reads `mergeable_paths`.
11. **S11** `mergeable_paths` is appended to `CARRIED_KEYS` in `claude-blast-radius-derive-core.ts` and emitted at the corresponding fixed position of the derived destination document, and is omitted when the source document lacks it.
12. **S12** `.claude/rules/parallel-orchestration.md` documents the mechanically-mergeable path class under the Blast-Radius Contention Doctrine beside `mandate_reads`, stating all three constraints.
13. **S13** `.claude/rules/parallel-orchestration.md` updates the key-partition sentence so `mergeable_paths` is named alongside `version`, `over_breadth_fraction`, and `mandate_reads` as byte-equal across the two copies.
14. **S14** `claude-blast-radius-derive-manifests.ts` exists and splits the manifest suffixes into `MODULE_MANIFEST_SUFFIXES` and `NON_MODULE_MANIFEST_SUFFIXES` (`.csproj`, `.fsproj`, `.vbproj`, `.sln`, `.slnx`), re-exported from `claude-blast-radius-derive-core.ts` so existing import paths keep working.
15. **S15** For a fixture in which every project directory of a multi-project .NET layout carries a `.csproj`, the derived destination module map is exactly `{ "config": ["config/**"] }`.
16. **S16** A nested `.sln` or `.slnx` yields no module, and a mixed layout containing a non-.NET manifest directory still yields that directory's module alongside `config`.
17. **S17** `assembleModules` and `PAYLOAD_MODULES` are unchanged, the assembled map is non-empty for every derived layout, and the forbidden-glob guard still receives a non-vacuous input.
18. **S18** `ProjectFileMergeGrammar.psm1`, `ProjectFileMerge.psm1`, and `Resolve-MergeableConflict.ps1` exist, are mirrored under the published resources, and are enumerated in `pack-manifests/core.json`.
19. **S19** A conflicted-file fixture for each MSBuild item type `Compile`, `Analyzer`, `None`, `Content`, `EmbeddedResource`, in both the single-line and paired forms, resolves to the keyed union with ours entries in ours order followed by theirs-only entries in theirs order.
20. **S20** A `packages.config` fixture with disjoint `id` values resolves to the union, and one with the same `id` at differing versions resolves to the higher `[System.Version]` and records `{key, ours, theirs, chosen}`.
21. **S21** An `app.config` fixture unions `dependentAssembly` blocks keyed on `assemblyIdentity name`, and for the same assembly at differing `newVersion` selects the higher version and sets the `oldVersion` upper bound to the chosen `newVersion`.
22. **S22** An unparseable version on either side, and the same key at the same version with differing attributes, both escalate rather than resolving.
23. **S23** Both `merge`-style and `diff3`/`zdiff3`-style conflict hunks are parsed, and a hunk containing any line outside the grammar escalates the file.
24. **S24** The never-drop post-condition holds: the merged file's unit-key set equals the union of the `:2:` and `:3:` key sets, every key present in `:1:` and both sides survives, and a violation escalates without writing.
25. **S25** The merged file preserves the original BOM, encoding, and per-line terminators, and no XML DOM reserialization occurs.
26. **S26** A conflict set containing any path outside `mergeable_paths` produces `{"result": "escalate"}` with that path in `escalate_paths`, writes no file, and leaves the worktree unmodified.
27. **S27** `Resolve-MergeableConflict.ps1` prints exactly one JSON object on stdout with `result`, `resolved`, and `escalate_paths`, classifies paths using the committed `mergeable_paths` and the shared blast-radius matcher, and neither stages nor commits.
28. **S28** `.claude/skills/parallel-orchestrate/SKILL.md` documents, as the new first step of `## Per-Item Merge-Conflict Handling`, the `dotnet tool restore` / `dotnet csharpier check` / `dotnet build` sequence with path arguments before re-confirming CI, and that a failing check reverts with `git reset --hard HEAD~1` and escalates with the tool output as the finding.
29. **S29** The same skill documents that an `escalate` result makes the parent run `git merge --abort` and fall through to the existing child re-delegation with a Blocking finding that includes `escalate_paths`.
30. **S30** `.claude/agents/parallel-orchestrator.md` and its mirror carry exactly the four scoped allowlist entries required by the step.
31. **S31** An item record carrying `mergeable_conflicts_resolved` with the documented shape produces zero validator errors, and an item record omitting it also produces zero errors.
32. **S32** `.claude/rules/parallel-orchestration.md` declares `mergeable_conflicts_resolved` optional, additive, and tolerated-not-validated, and the two "add no field" sentences are narrowed to permit a field the rule file declares; invariant 9 is unchanged.
33. **S33** `docs/features/templates/parallel/parallel-status.md` gains a `## Mergeable Conflicts Resolved` section, and the skill states that the resolution is cited in the projection, the resolution commit message body, the PR body, and an evidence artifact.
34. **S34** For a set of items whose only path overlaps are `.csproj` files, the contention relation yields no edge for any pair and the computed edge list is empty, and `compute_cohorts` places every item key in a single cohort.
35. **S35** `.claude/hooks/enforce-parallel-cohort-barrier.ps1`, the `PARALLEL_COHORT_BARRIER_VIOLATION` code and its semantics, the hook's tests, and the cohort-barrier validator tests are unmodified.
36. **S36** The key-partition registries are extended in both copies: `BYTE_EQUAL_KEYS` and `$script:ClassOneKeys` each gain `mergeable_paths`, which propagates to the byte-equal and exhaustiveness cases in both languages.
37. **S37** A shape case for `mergeable_paths` is added beside the `mandate_reads` case in `BlastRadius.TruthTable.Tests.ps1`, and a non-vacuity assertion for the committed list is added in the new Python test module rather than in `test_blast_radius_config_parity.py`.
38. **S38** Every file created or touched by this feature is under 500 lines, achieved by the named relocations and splits.
39. **S39** The seven-stage toolchain passes in a single pass for Python, TypeScript, and PowerShell, with line coverage >= 85% and branch coverage >= 75% for the languages whose tooling measures it, and no test creates a temporary file.

### From `spec.md` — `## Definition of Done` (D1–D7)

1. **D1** Acceptance criteria documented and mapped to tests or demos
2. **D2** Behavior matches acceptance criteria in all documented environments
3. **D3** Tests updated/added (unit/integration as applicable)
4. **D4** Edge cases and error handling covered by tests
5. **D5** Docs updated (README, docs/features/active/... links)
6. **D6** Telemetry/logging added or updated (if applicable)
7. **D7** Toolchain pass completed (format → lint → type-check → test)

### From `spec.md` — `## Seeded Test Conditions (from potential)` (T1–T5)

1. **T1** Unit coverage: `mergeable_paths` present/absent; `.csproj` overlap yields no edge; path remains in `paths`; V1/V2/V3 unaffected.
2. **T2** Module derivation fixture with nine `.csproj` directories.
3. **T3** Merge-step fixtures per item type; non-mergeable path escalates.
4. **T4** Key-partition parity (Python, TypeScript, Pester).
5. **T5** Zero-edge single-cohort run whose only overlaps are `.csproj` files.

### From `user-story.md` — `## Acceptance Criteria` (U1–U11)

1. **U1** For a set of items whose only shared paths are project files matching the published `mergeable_paths` list, the operator observes an empty conflict-edge list in the run's projection.
2. **U2** Those items are placed in a single cohort and are launched together, rather than being serialized across cohorts.
3. **U3** Each item's declared blast radius still lists the project file it touches, so the operator can see the write and drift detection still reports an undeclared write.
4. **U4** Two items in the same assembly, touching different files, produce no `module_overlap` edge; the destination module map the operator receives from push-down names no per-assembly module.
5. **U5** The operator receives the behavior through a normal push-down: no manual edit to the consumer repository's `config/blast-radius.json` or cohort-barrier hook is required, and the behavior survives the next push-down.
6. **U6** When a pull request cannot merge because of a conflict confined to project files, the orchestrator resolves it without re-delegating the item, and every entry present on either side is present in the merged file.
7. **U7** When the same package or binding-redirect entry appears on both sides at differing versions, the higher version is selected and the choice is reported to the operator.
8. **U8** When the conflict set contains any path outside the mergeable list, or any line the merge grammar does not recognise, the orchestrator escalates as it does today and names the paths that caused the escalation.
9. **U9** After a resolution the orchestrator runs the formatter check and the analyzer build before re-confirming CI, and reverts the resolution and escalates if either fails.
10. **U10** The resolution is cited where the operator reads run outcomes: the `## Mergeable Conflicts Resolved` section of the run status projection, the pull request body, the resolution commit message, and an evidence artifact under the run's evidence folder.
11. **U11** A run in which no project-file conflict occurs, or a destination whose truth table lacks the `mergeable_paths` key, behaves exactly as it does today; the operator sees no change in cohorts or escalations.

---

## Acceptance Criteria Evaluation

### `spec.md` — `## Acceptance Criteria`

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| S1 | `mergeable_paths` in both truth tables, five entries, byte-equal | PASS | Both files carry the five-entry list; reviewer diffed the key block in both copies and it is identical. `mergeable_paths` is registered in `BYTE_EQUAL_KEYS` (`blast_radius_parity_test_support.py:112`) and `$script:ClassOneKeys` (`BlastRadius.KeyPartition.Tests.ps1:33`) | `diff <(grep -A7 '"mergeable_paths"' config/blast-radius.json) <(grep -A7 '"mergeable_paths"' extensions/.../config/blast-radius.json)` → identical; `pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py`; `Invoke-Pester tests/scripts/claude-lib/blast-radius` | Class-1 byte-equal gate now covers the key in both languages |
| S2 | Optional and fail-closed | PASS | `test_conflicts_absent_key_and_empty_list_produce_identical_results` plus the mirrored Pester case | `pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` → 17 passed; `Invoke-Pester .../BlastRadiusConflict.Tests.ps1` | Reader returns an empty tuple for an absent key, so an empty list and an absent key are the same code path |
| S3 | Reader shape and rejection behavior | PASS | Four reader cases: sorted return, absent key, non-list value rejected, blank entry rejected. Implementation delegates to the existing `config_string_list`, so the shape cannot drift from the other string-list readers | `pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py::test_config_mergeable_paths_rejects_a_non_list_value` etc. | |
| S4 | `.csproj`-only overlap yields no edge; path stays in `paths` | PASS | `tests/fixtures/blast_radius/conflict-mergeable-csproj-no-edge.json` is auto-discovered by both parity suites (glob + corpus-floor + on-disk-equality guards), plus `test_conflicts_yields_no_edge_for_a_csproj_only_overlap` and `test_conflicts_leaves_the_csproj_in_both_radii_paths` | `pytest tests/scripts/dev_tools/test_blast_radius_parity.py`; `Invoke-Pester .../BlastRadius.Parity.Tests.ps1` → passed | Reviewer confirmed auto-discovery by reading `FIXTURE_PATHS` and the `MINIMUM_FIXTURE_COUNT = 30` guard, so the fixture is consumed without either suite being edited |
| S5 | A declared glob still contends | PASS | `tests/fixtures/blast_radius/conflict-mergeable-glob-still-contends.json` in both parity suites; `test_conflicts_still_contends_for_a_declared_glob_entry`. Implementation: `if is_glob_entry(entry): return False` after the exact-match step | Same parity commands as S4 | The matcher docstring states the reason: pattern subsumption is not a comparison this vocabulary supports |
| S6 | `**/`-anchored pattern matches a root-level file | PASS | `test_matches_mergeable_path_matches_a_root_level_file_for_a_double_star_prefix` and the mirrored Pester case. Implementation strips `_ANY_DEPTH_PREFIX` in a third step local to this matcher | `pytest .../test_blast_radius_mergeable_paths.py` | `_blast_radius_glob.py` and `BlastRadiusGlob.psm1` are absent from the branch diff, so the "without any change to `_glob_to_regex_text`" clause holds |
| S7 | Exclusion confined to the contention relation | PASS | Three isolation tests: `test_validate_blast_radius_findings_are_identical_with_and_without_the_key`, `test_derive_blast_radius_keeps_a_cited_csproj_in_paths`, `test_detect_escaped_paths_is_unaffected_by_the_key`. Diff inspection confirms the only Python application point is inside `conflicts` | `pytest .../test_blast_radius_mergeable_paths.py`; `git diff ef83a2e0..515bf154 -- scripts/dev_tools/` | The filter is applied to copies; neither radius object is rewritten |
| S8 | Drift recomputation inherits the exclusion | PASS | `test_recompute_conflicts_reports_no_pair_for_a_csproj_only_observed_overlap` added to `test_parallel_drift_detection_conflicts.py`, asserting `pairs == ()` | `pytest tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py` → passed | `parallel_drift_detection.py` needed no change; it inherits through the single call site |
| S9 | `BlastRadiusConflict.psm1` shape and application point | PASS | Module exports exactly the five named functions; `BlastRadius.psm1` imports it and filters both arrays through `Get-NonMergeablePathEntry` immediately before `Get-SmallestPathOverlap` | `grep -n "Export-ModuleMember" -A6 .claude/lib/blast-radius/BlastRadiusConflict.psm1`; `Invoke-Pester tests/scripts/claude-lib/blast-radius` → 488 passed | The facade's own `Export-ModuleMember` list is unchanged at six functions, so the relocation broke no consumer |
| S10 | Three PowerShell consumers inherit without code change; prose updated | PASS | `parallel-planner.md` gains the sentence "reads that table's optional `mergeable_paths` list and contributes no `path_overlap` edge for a path matching it, while the path stays in the declared radius." `parallel-plan/SKILL.md` (+6) and `parallel-add/SKILL.md` (+4/−2) carry the corresponding prose. No procedural code changed in any of the three | `git diff ef83a2e0..515bf154 -- .claude/agents/parallel-planner.md .claude/skills/parallel-plan .claude/skills/parallel-add`; SHA-256 mirror check | All three mirrors byte-equal |
| S11 | `CARRIED_KEYS` carriage, fixed position, omitted when absent | PASS | `CARRIED_KEYS` gains `mergeable_paths` as a sixth appended entry; the assembly literal emits it at `source[CARRIED_KEYS[5]]` between `mandate_reads` and `modules`; `JSON.stringify` drops the `undefined` when the source lacks it | `npx jest test/lib/push-down/blast-radius-derive-mergeable.test.ts test/lib/push-down/blast-radius-derive-core.test.ts test/lib/push-down/blast-radius-derive.test.ts` → passed | The array is appended, not inserted, so no existing positional index shifted |
| S12 | Doctrine documented with all three constraints | PASS | `.claude/rules/parallel-orchestration.md` gains "### Mechanically-mergeable path class (issue #643)" with the three numbered constraints: the planner remains obliged to enumerate a genuine write; the path stays in the declared radius so the audits still see it; `detect_escaped_paths` remains the backstop at execution time | `git diff ef83a2e0..515bf154 -- .claude/rules/parallel-orchestration.md`; SHA-256 mirror check | Placed beside `mandate_reads` under the Blast-Radius Contention Doctrine as required |
| S13 | Key-partition sentence updated | PASS | The sentence now reads "Only `version`, `over_breadth_fraction`, `mandate_reads`, and `mergeable_paths` are byte-equal across the two copies." | Same diff command as S12 | Matches the extended Class-1 registries in both languages (S36) |
| S14 | Manifest family split, re-exported | PASS | `MODULE_MANIFEST_SUFFIXES = []`, `NON_MODULE_MANIFEST_SUFFIXES = ['.csproj','.fsproj','.vbproj','.sln','.slnx']`, `MANIFEST_SUFFIXES` is their concatenation; the core re-exports eight values and two types | `npx jest test/lib/push-down/blast-radius-derive-manifests.test.ts` → passed; `npm run typecheck` → 0 | The AC's parenthetical lists the five for the non-module family, which is what was implemented |
| S15 | Nine-project layout derives exactly `{ "config": ["config/**"] }` | PASS | `dotNetSolution(9)` builds nine sibling `.csproj` directories plus the root; `expect(deriveModules(dotNetSolution(9))).toEqual({ config: ["config/**"] })` and `expect(classification.modulePaths).toEqual([])` with `structureObserved === true` | `npx jest test/lib/push-down/blast-radius-derive-manifests.test.ts` → passed | The structure signal suppresses the top-level fallback, so only the payload floor remains |
| S16 | Nested solution yields no module; mixed layout keeps the non-.NET module | PASS | "yields no module for a nested solution file" and the mixed-layout case asserting `Object.keys(modules).sort() === ["config","tools"]` | Same command as S15 | |
| S17 | `assembleModules` / `PAYLOAD_MODULES` unchanged; guard non-vacuous | PASS | Neither symbol appears as changed in the core diff; the retained guard cases pass with the `.csproj`-derived cases re-pointed at a non-.NET manifest | `npx jest test/lib/push-down/blast-radius-derive-core.test.ts` → passed | Core branch coverage rose 95.83% → 97.50% after the split |
| S18 | Three merge files exist, mirrored, and enumerated in `core.json` | PASS | All three exist under `.claude/lib/project-file-merge/`; SHA-256 confirms byte-equal mirrors; `pack-manifests/core.json` gains all four new PowerShell paths (three merge files plus `BlastRadiusConflict.psm1`) | `Invoke-Pester tests/scripts/claude-lib/ClaudeLibModuleConvention.Tests.ps1` → 6 passed; `sha256sum` over all 12 mirror pairs → all match | Convention suite covers auto-discovery, strict mode, error-action guard, and the 500-line cap |
| S19 | Keyed union for 5 item types × 2 forms | PASS | 10 conflicted/expected fixture pairs under `tests/fixtures/project_file_merge/` (`compile-`, `analyzer-`, `none-`, `content-`, `embeddedresource-`, each `-single-line` and `-paired`); `Merge-ConflictedText` emits ours units in ours order then theirs-only units in theirs order | `Invoke-Pester tests/scripts/claude-lib/project-file-merge` → passed (part of the 488) | |
| S20 | `packages.config` union and higher-version selection | PASS | `packages-disjoint.*` and `packages-version-differs.*` fixtures; `$resolution.Add(...)` records `{key, ours, theirs, chosen}` | Same command as S19 | |
| S21 | `app.config` union and `oldVersion` upper-bound retarget | PASS | `appconfig-disjoint.*` and `appconfig-newversion-differs.*`; expected output shows `oldVersion="0.0.0.0-4.0.2.0"` alongside `newVersion="4.0.2.0"` | Same command as S19 | Criterion satisfied as written. A related gap outside this criterion's wording — the single-version `oldVersion="a"` form is not retargeted and is not fixtured — is recorded as finding CR-2 in `code-review.2026-09-07T18-08.md` |
| S22 | Unparseable version and same-key-differing-attributes escalate | PASS | `packages-unparseable-version.conflicted.config` and `same-key-different-attributes.conflicted.csproj`; escalation reasons name the key and the versions | Same command as S19 | |
| S23 | Both hunk styles parsed; non-grammar line escalates | PASS | `diff3-compile.*` fixtures; `non-grammar-line.conflicted.csproj`; `Get-ConflictHunk` handles the `\|\|\|\|\|\|\|` base section and returns `$null` for an unterminated hunk | Same command as S19 | |
| S24 | Never-drop post-condition | PASS | `Test-NeverDropPostCondition` compares against `:1:`/`:2:`/`:3:` stage texts supplied through a mocked `Invoke-GitExe`; `script-never-drop.{base,ours,theirs}.csproj` fixtures; a violation returns before any write | Same command as S19 | The write loop runs only after every path has passed, so no partial write is reachable |
| S25 | BOM, encoding, and terminators preserved; no XML reserialization | PASS | `bom-compile.*` and `crlf-compile.*` fixtures; `.gitattributes` exempts `tests/fixtures/project_file_merge/crlf-*.csproj` from `text=auto eol=lf` with an in-file comment explaining that conversion would make the CRLF case pass locally and fail on every fresh checkout including CI | Same command as S19; `git diff ef83a2e0..515bf154 -- .gitattributes` | No XML API is used anywhere in the merge library; lines are matched as opaque text |
| S26 | Non-mergeable path in the set escalates, writes nothing | PASS | Classification runs over the whole conflicted set before any file is read; `Resolve-MergeableConflict.Tests.ps1` asserts `result = escalate`, the offending path in `escalate_paths`, and no write | `Invoke-Pester tests/scripts/claude-lib/project-file-merge/Resolve-MergeableConflict.Tests.ps1` → passed | |
| S27 | Single JSON object; shared matcher; no staging or commit | PASS | `ConvertTo-Json -Depth 6 -Compress` on an `[ordered]` record with exactly `result`, `resolved`, `escalate_paths`; classification uses `Get-ConfigMergeablePath` + `Test-MergeablePath` from `BlastRadiusConflict.psm1`; no `git add` or `git commit` appears in the script | Same command as S26; `grep -n "git " .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` | Exits 0 for both verdicts; a non-zero exit is reserved for an unexpected error |
| S28 | Skill documents the .NET verification sequence and the revert | PASS | `parallel-orchestrate/SKILL.md` step (c) names `dotnet tool restore --tool-manifest <worktree_path>/.config/dotnet-tools.json`, `dotnet csharpier check <worktree_path>`, and `dotnet build <worktree_path>/<solution>`, "each with path arguments rather than `cd`", and the revert `git -C <worktree_path> reset --hard HEAD~1` escalating with the tool output as the finding | `git diff ef83a2e0..515bf154 -- .claude/skills/parallel-orchestrate/SKILL.md`; SHA-256 mirror check | Steps (a)–(d) are placed above the existing numbered escalation list, which is what "new first step" requires |
| S29 | `escalate` → `merge --abort` → existing re-delegation with `escalate_paths` | PASS | Step (b): "A `result` of `escalate` makes the parent run `git -C <worktree_path> merge --abort` and continue with step 1 below, including `escalate_paths` in the Blocking finding." | Same command as S28 | |
| S30 | Exactly four scoped allowlist entries | PASS | `parallel-orchestrator.md` `tools:` gains exactly `Bash(pwsh -NoProfile -File .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1*)`, `Bash(dotnet tool restore*)`, `Bash(dotnet csharpier check *)`, `Bash(dotnet build *)` — four entries, no more | `git diff ef83a2e0..515bf154 -- .claude/agents/parallel-orchestrator.md`; SHA-256 mirror check | Surrounding prose states the entries are scoped to that step and to no other |
| S31 | Validator tolerance in both runtimes | PASS | `test_validate_parallel_orchestrator_state_mergeable.py`: one carrying case, one omitting case, both asserting zero errors. `parallel-orchestrator-state-core.test.ts:419` `describe("optional mergeable_conflicts_resolved item field")` with the same two cases | `pytest tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_mergeable.py`; `npx jest test/lib/validate/parallel-orchestrator-state-core.test.ts` → both passed | |
| S32 | Rule declares the field; two "add no field" sentences narrowed; invariant 9 unchanged | PASS | The rule states the field is "additive and tolerated-not-validated" with the documented entry keys and closes "Invariant 9 ... and invariant 15 ... are unchanged by this field." Both agent and skill sentences now read "add no field to it that `.claude/rules/parallel-orchestration.md` does not declare" | `git diff ef83a2e0..515bf154 -- .claude/rules/parallel-orchestration.md .claude/agents/parallel-orchestrator.md .claude/skills/parallel-orchestrate/SKILL.md` | All three mirrors byte-equal |
| S33 | Template section and the four citation sites | PASS | `parallel-status.md` gains `## Mergeable Conflicts Resolved` with the eight-column table. The skill names the projection, the resolution commit message body, the PR body, and `docs/features/parallel/<slug>/evidence/other/mergeable-conflicts.<yyyy-MM-ddTHH-mm>.md` carrying `Timestamp`, `Command`, `EXIT_CODE`, and the script's JSON output verbatim | `git diff ef83a2e0..515bf154 -- docs/features/templates/parallel/parallel-status.md .claude/skills/parallel-orchestrate/SKILL.md` | The named evidence path is canonical (`evidence/other/`) |
| S34 | Empty edge list and a single cohort | PASS | `test_csproj_only_overlaps_yield_no_edge_for_any_pair` calls the real `conflicts` per pair; `test_csproj_only_overlaps_place_every_item_in_a_single_cohort` calls `compute_cohorts`; `test_cohort_fixture_for_mergeable_only_overlaps_exists_in_the_shared_corpus` ties the committed fixture to the computed result. `tests/fixtures/parallel_cohorts/cohorts_mergeable_only_overlaps.json` is auto-discovered by `test_parallel_cohort_bash_parity.py`, which exercises `.claude/lib/bash/compute-cohorts.sh` | `pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py tests/scripts/dev_tools/test_parallel_cohort_bash_parity.py` → passed | Reviewer verified the bash-parity suite's `FIXTURE_DIR` glob and `MINIMUM_FIXTURE_COUNT = 20` guard, which is why the empty-edge case is satisfied without that file appearing in the diff |
| S35 | Cohort-barrier hook and its tests unmodified | PASS | Negative claim verified by search. **SearchScope:** the full 166-path name-status listing of `ef83a2e0..515bf154`. **SearchPatterns:** `enforce-parallel-cohort-barrier`, `cohort_barrier`, `cohort-barrier`, `PARALLEL_COHORT_BARRIER_VIOLATION`. **SearchResult:** none | `git diff --name-only ef83a2e0..515bf154 \| grep -i -e cohort.barrier -e cohort_barrier` → no output | Independently corroborated by `evidence/qa-gates/phase7-unmodified-surfaces.2026-09-07T18-16.md` |
| S36 | Both key-partition registries extended | PASS | `BYTE_EQUAL_KEYS` gains `"mergeable_paths"` at `blast_radius_parity_test_support.py:112` with an explanatory comment; `$script:ClassOneKeys` gains it at `BlastRadius.KeyPartition.Tests.ps1:33`. The Pester exhaustiveness loop compares each Class-1 key's JSON across both copies with `-cne` | `pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py`; `Invoke-Pester .../BlastRadius.KeyPartition.Tests.ps1` → passed | |
| S37 | Truth-table shape case and non-vacuity assertion placement | PASS | `BlastRadius.TruthTable.Tests.ps1` +24 lines beside the `mandate_reads` case. `test_committed_mergeable_paths_is_a_non_empty_list_in_both_copies` and `test_committed_mergeable_paths_carries_the_five_default_entries` live in the new `test_blast_radius_mergeable_paths.py`, not in `test_blast_radius_config_parity.py` | `Invoke-Pester .../BlastRadius.TruthTable.Tests.ps1`; `pytest .../test_blast_radius_mergeable_paths.py` → passed | Placement respects the 500-line ceiling on `test_blast_radius_config_parity.py` |
| S38 | Every created or touched file under 500 lines | PASS | Reviewer re-ran an independent scan over every changed `.py`/`.ps1`/`.psm1`/`.psd1`/`.ts`/`.cjs`/`.json` path outside `tests/fixtures/`: **zero files over 500**. Largest production file `BlastRadius.psm1` 438; largest test file `BlastRadius.Conflict.Tests.ps1` 490. The named splits were all taken: `BlastRadiusConflict.psm1` 290, `claude-blast-radius-derive-manifests.ts` 200, `_blast_radius_mergeable.py` 160 | Reviewer scan (see Appendix B of the policy audit) | The executor's `file-size-compliance.2026-09-07T18-22.md` predates the final PowerShell fix and lists `Resolve-MergeableConflict.ps1` at 226 where the final tree has 229; the reviewer's re-scan against the audited range supersedes it and reaches the same conclusion (finding CR-3) |
| S39 | Seven-stage toolchain passes; coverage thresholds met; no temporary files | PASS | Reviewer-verified: `black --check` 0, `ruff check` 0, `pyright` 0 errors, `prettier --check` 0, `eslint` 0, `tsc --noEmit` 0, targeted Python 166 passed, targeted Jest 192 passed, Pester 488 + 6 passed. Coverage re-parsed from the artifacts: Python 92.90% / 85.52%, TypeScript 96.88% / 90.45%, PowerShell 94.85% line (no branch counter). Every new production file at or above 85% line. Temp-file grep over the added test files returned only comments asserting that none is created | See Appendix B of `policy-audit.2026-09-07T18-08.md` | The single Python failure is the pre-existing C4-exempt `test_bundled_claude_payload_contains_all_repo_runtime_contracts` (issue #510, gitignored `.claude/state/`), recorded identically in the `[P0-T7]` baseline. The criterion's `evidence/qa/` path is non-canonical; the executor recorded `EVIDENCE_LOCATION_OVERRIDE_REJECTED` and wrote to `evidence/qa-gates/`, which the reviewer confirms is correct handling |

### `spec.md` — `## Definition of Done`

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| D1 | AC documented and mapped to tests or demos | PASS | Every one of S1–S39 names its verifying test, fixture, or reviewed file inline. `evidence/issue-updates/ac-status-summary.2026-09-07T19-46.md` supplies a per-item evidence path | Reviewer read-through of `spec.md` and the AC status summary | |
| D2 | Behavior matches AC in all documented environments | PASS | Verified on the rebased head in this worktree across all three runtimes. `evidence/qa-gates/post-rebase-qa.2026-09-07T17-56.md` records the full loop on the rebased head | Reviewer re-ran the targeted suites; see S39 | The one documented environment gap is that the parent-side merge step has not yet run against a live consumer repository; recorded as risk 3 in the code review, not as an AC failure |
| D3 | Tests updated/added | PASS | 8 new test suites, 4 extended, 47 new fixtures across three runtimes | `git diff --name-status ef83a2e0..515bf154 -- tests/ extensions/drm-copilot/test/` | |
| D4 | Edge cases and error handling covered | PASS | The escalation corpus outnumbers the resolution corpus: unparseable version, same-key-differing-attributes, non-grammar line, unterminated hunk, invalid UTF-8, non-mergeable path in the set, absent key, empty list, non-list value, blank entry, root-level anchored match, glob-still-contends | `Invoke-Pester tests/scripts/claude-lib/project-file-merge`; `pytest .../test_blast_radius_mergeable_paths.py` | The invalid-UTF-8 fixture was added specifically because the coverage gate flagged the untested `DecoderFallbackException` branch |
| D5 | Docs updated | PASS | `.claude/rules/parallel-orchestration.md` (+68/−8), three skills, two agents, the parallel-status template, and all published mirrors | `git diff --stat ef83a2e0..515bf154 -- .claude/ docs/features/templates/` | |
| D6 | Telemetry/logging added or updated (if applicable) | PASS | The feature's telemetry is the `mergeable_conflicts_resolved` checkpoint field, the `## Mergeable Conflicts Resolved` projection section, the resolution commit message body, and the per-resolution evidence artifact. All four are specified and the field is validator-tolerated in both runtimes | See S31, S32, S33 | No runtime logging framework applies to a pure scheduling filter |
| D7 | Toolchain pass completed | PASS | Same evidence as S39, plus `evidence/qa-gates/final-qa-loop-outcome.2026-09-07T19-32.md` recording restart counts (Python 2, TypeScript 2, PowerShell 3) and an explicit ordering statement | Reviewer verified the ordering claim: the only commit after the post-rebase QA run adds one documentation file, and the working tree is clean | |

### `spec.md` — `## Seeded Test Conditions (from potential)`

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| T1 | Present/absent; no edge; path remains; V1/V2/V3 unaffected | PASS | Covered by S2, S4, S7 | `pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` → 17 passed | |
| T2 | Module derivation fixture with nine `.csproj` directories | PASS | `dotNetSolution(9)` in `blast-radius-derive-manifests.test.ts`, used by two cases | `npx jest test/lib/push-down/blast-radius-derive-manifests.test.ts` → passed | The spec deliberately restated this as a construction rule plus an exact expected map rather than a count; the implementation satisfies both the spec wording and the seeded figure |
| T3 | Merge-step fixtures per item type; non-mergeable path escalates | PASS | Covered by S19 and S26 | `Invoke-Pester tests/scripts/claude-lib/project-file-merge` → passed | |
| T4 | Key-partition parity (Python, TypeScript, Pester) | PASS | Python `BYTE_EQUAL_KEYS`, Pester `$script:ClassOneKeys`, TypeScript `CARRIED_KEYS` with key-order assertions | `pytest .../test_blast_radius_config_parity.py`; `Invoke-Pester .../BlastRadius.KeyPartition.Tests.ps1`; `npx jest test/lib/push-down/blast-radius-derive-mergeable.test.ts` → all passed | |
| T5 | Zero-edge single-cohort run | PASS | Covered by S34 | `pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py` → passed | |

### `user-story.md` — `## Acceptance Criteria`

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| U1 | Operator observes an empty conflict-edge list | PASS | `test_csproj_only_overlaps_yield_no_edge_for_any_pair` asserts the relation returns no conflict for every pair; the committed corpus fixture carries `"conflict_edges": []` and the projection renders `conflict_edges[]` | `pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py` → passed | Verified at the relation and fixture level. The end-to-end projection rendering is existing, unmodified behavior |
| U2 | Items land in a single cohort and launch together | PASS | `compute_cohorts` places all four keys in one cohort; the corpus fixture's `expected_cohorts` is a single four-member list and is consumed by the bash-parity suite against `compute-cohorts.sh` | `pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py tests/scripts/dev_tools/test_parallel_cohort_bash_parity.py` → passed | |
| U3 | Declared radius still lists the project file; drift still reports | PASS | `test_conflicts_leaves_the_csproj_in_both_radii_paths`, `test_derive_blast_radius_keeps_a_cited_csproj_in_paths`, `test_detect_escaped_paths_is_unaffected_by_the_key` | `pytest .../test_blast_radius_mergeable_paths.py` → passed | This is constraint 3 of the doctrine: `detect_escaped_paths` remains the backstop at execution time |
| U4 | No `module_overlap` edge; module map names no per-assembly module | PASS | `deriveModules(dotNetSolution(9))` is exactly `{ "config": ["config/**"] }`, so a .NET solution produces no per-assembly module and therefore no module-overlap edge can be derived from one | `npx jest test/lib/push-down/blast-radius-derive-manifests.test.ts` → passed | |
| U5 | Delivered through a normal push-down; survives the next push-down | PASS | `mergeable_paths` is in `CARRIED_KEYS` so push-down emits it into the destination truth table; the four new PowerShell files are in `pack-manifests/core.json` so the pack carries them; the cohort-barrier hook is unmodified, so no manual consumer edit is implied | `npx jest test/lib/push-down/claude-config-carriage.test.ts`; `Invoke-Pester .../ProjectFileMerge.Manifest.Tests.ps1` → passed | This is the criterion that the whole "publish, do not patch the consumer" constraint rests on, and both halves (config key and library files) are gated |
| U6 | Parent resolves without re-delegating; no entry lost | PASS | Skill steps (a)–(d) define parent-side resolution ahead of the existing re-delegation path; the never-drop post-condition (S24) enforces "every entry present on either side is present in the merged file" and escalates without writing on violation | `Invoke-Pester tests/scripts/claude-lib/project-file-merge` → passed | |
| U7 | Higher version selected and reported | PASS | `Compare-UnitVersion` ranks with `[System.Version]`; the choice is recorded as `{key, ours, theirs, chosen}` in `version_resolutions`, which surfaces in the checkpoint field, the projection section, the commit body, the PR body, and the evidence artifact | Same command as U6; see S20, S21, S33 | |
| U8 | Escalate on any non-mergeable path or unrecognized line, naming the paths | PASS | Two independent escalation gates: path classification before any read (S26) and grammar refusal per hunk (S23). Both return `escalate_paths` | Same command as U6 | |
| U9 | Formatter check and analyzer build before re-confirming CI; revert on failure | PASS | Skill step (c) specifies `dotnet csharpier check` and `dotnet build` after the resolution commit and before step (d)'s re-entry at the durable `gh pr checks` confirmation, with `git reset --hard HEAD~1` on failure. The agent allowlist carries exactly the commands required (S30) | `git diff ef83a2e0..515bf154 -- .claude/skills/parallel-orchestrate/SKILL.md .claude/agents/parallel-orchestrator.md` | Documented contract; not executable in this repository, which contains no C# project |
| U10 | Resolution cited in all four operator-facing places | PASS | Projection section in the template; the skill names the commit message body, the PR body, and `docs/features/parallel/<slug>/evidence/other/mergeable-conflicts.<yyyy-MM-ddTHH-mm>.md` with `Timestamp`, `Command`, `EXIT_CODE`, and the JSON output | See S33 | The named evidence path uses the canonical `evidence/other/` kind |
| U11 | No change when no conflict occurs or the key is absent | PASS | Absent-key and empty-list equivalence asserted in both Python and Pester; the parity corpus is unchanged for every pre-existing fixture, which is the direct proof that no other pair's verdict moved | `pytest .../test_blast_radius_mergeable_paths.py`; `Invoke-Pester tests/scripts/claude-lib/blast-radius` → 488 passed | The fail-closed key is itself the rollout control, so a destination that has not received the published table is unaffected |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**

- **PASS:** 62 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. On the first live parent-side resolution in a consumer repository, confirm that
   `Resolve-MergeableConflict.ps1` emits a single parseable JSON object and that the `dotnet
   csharpier check` / `dotnet build` gate behaves as documented, and capture the run as
   `docs/features/parallel/<slug>/evidence/other/mergeable-conflicts.<timestamp>.md` per S33.
2. Consider the two Minor code-review follow-ups before that first live run: CR-1 (separate stderr in
   `Invoke-GitExe`) and CR-2 (single-version `oldVersion` form in the `app.config` retarget). Neither
   blocks this PR; both are cheapest to address before the step has production history.
3. On a future rebase, regenerate the two scope-shaped evidence gates against the new merge base so
   they no longer cite `c3ffb080` (CR-3).

---

## Acceptance Criteria Check-off

Per `.claude/skills/acceptance-criteria-tracking/SKILL.md`:

- Criteria evaluated as **PASS** may be checked off in the authoritative source files if they are
  checkbox-represented and not already checked.
- Criteria evaluated as **PARTIAL**, **FAIL**, or **UNVERIFIED** must remain unchecked.
- Criterion text is never modified; only `- [ ]` becomes `- [x]`.
- No criterion is added to a source file by an executor or a reviewer.

### Reviewer check-off record

**Already checked by the executor before this review began: 62 of 62.** The executor checked each
item off as the corresponding plan task passed verification, per the "check off as soon as the task
passes, do not defer to the end" timing rule, and recorded a per-item evidence path in
`evidence/issue-updates/ac-status-summary.2026-09-07T19-46.md`. State observed at the start of this
audit, measured directly on the source files:

| Source file | `- [x]` items | `- [ ]` items | Reviewer action |
|---|---|---|---|
| `spec.md` | 51 | 0 | None required — all 51 verified PASS, all already checked |
| `user-story.md` | 11 | 0 | None required — all 11 verified PASS, all already checked |
| `issue.md` | 0 | 15 | None taken — not an authoritative source under `full-feature`; left untouched |

**Verified independently by the reviewer: 62 of 62.** Every item in the
`## Acceptance Criteria Evaluation` tables above was evaluated against evidence the reviewer
inspected or commands the reviewer ran, not against the executor's assertion that it passed. The
executor's check-off was treated as a claim to be tested, and the reviewer's verification method for
each item is recorded in that table's Evidence and Verification command(s) columns. All 62 evaluate
**PASS**.

**Checkbox transitions performed by this review: none.** Because the intersection of "evaluated PASS"
and "not already checked" is empty for both authoritative source files, there was no `- [ ]` → `- [x]`
transition to make. No item evaluated PARTIAL, FAIL, or UNVERIFIED, so no item had to be left
unchecked or un-checked. Neither `spec.md` nor `user-story.md` was opened for writing; both are
byte-unchanged by this review, and `git status --porcelain --untracked-files=all` shows only the three
review artifacts as new files.

**Newly checked-off items in this review: none.**

**`issue.md` deliberately left unmodified.** Its 15 unchecked boxes sit under
`## Acceptance Criteria (early draft)`, `## Test Conditions to Consider`, and `## Next Step`. Work
mode is `full-feature`, so the skill resolves the authoritative sources to `spec.md` and
`user-story.md` only, and checking off a non-authoritative draft section would assert a delivery
record the review did not evaluate. See the note at the end of this section for two draft items that
would be incorrect to check as written.

### Acceptance Criteria Status

- Source: `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` and `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md`
- Total AC items: 62
- Checked off (delivered): 62
- Remaining (unchecked): 0
- Items remaining: None.

Per-source breakdown, in the same block form:

```
### Acceptance Criteria Status
- Source: docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md
- Total AC items: 51
- Checked off (delivered): 51
- Remaining (unchecked): 0
- Items remaining: none

### Acceptance Criteria Status
- Source: docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md
- Total AC items: 11
- Checked off (delivered): 11
- Remaining (unchecked): 0
- Items remaining: none
```

The heading above uses the exact form required by
`.claude/skills/acceptance-criteria-tracking/SKILL.md`. The bundled feature-audit template labels the
same block `### AC Status Summary`; where the two differ, the skill is authoritative.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 51 | 51 | 0 | Checkbox-backed. 39 under `## Acceptance Criteria` (S1–S39), 7 under `## Definition of Done` (D1–D7), 5 under `## Seeded Test Conditions (from potential)` (T1–T5). Canonical counter returns 39 for the named `Acceptance Criteria` section |
| `user-story.md` | 11 | 11 | 0 | Checkbox-backed, all under `## Acceptance Criteria` (U1–U11). Canonical counter returns 11 |
| `issue.md` | 15 | 0 | 15 | **Not authoritative** for `full-feature`. Section is titled `## Acceptance Criteria (early draft)` and a further 5 items sit under `## Test Conditions to Consider` and 2 under `## Next Step`. Left unchecked deliberately; see the note below |

**Note on `issue.md`.** Work mode is `full-feature`, so `.claude/skills/acceptance-criteria-tracking/SKILL.md`
resolves the AC sources to `spec.md` and `user-story.md` only. The reviewer did not check off, edit,
or reformat any `issue.md` checkbox. The draft items are superseded rather than unmet: each was
carried forward into a more precisely worded `spec.md` criterion that this audit evaluates as PASS.
Two of the draft items are also stale in substance and would be wrong to check as written — the
`## Next Step` items ("Promote to GitHub issue", "Create the feature folder") were completed by the
promotion itself, and the draft AC line asserting that "the TypeScript parity port" treats a
`mergeable_paths` match as contributing no `path_overlap` edge describes a contention relation that
TypeScript does not implement; the authoritative `spec.md` criterion S11 correctly scopes the
TypeScript surface to key carriage at push-down.
