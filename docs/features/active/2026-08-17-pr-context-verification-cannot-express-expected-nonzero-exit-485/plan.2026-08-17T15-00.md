# 2026-08-17-pr-context-verification-cannot-express-expected-nonzero-exit (Plan)

- **Issue:** #485
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-17T20-25
- **Status:** Revised after preflight iteration 5 (revision 5); ready for re-preflight
- **Version:** 1.5
- **Work Mode:** full-bug (persisted marker `- Work Mode: full-bug` in `issue.md`)
- **Requirements source:** `docs/features/active/2026-08-17-pr-context-verification-cannot-express-expected-nonzero-exit-485/spec.md` (AC1-AC25). `user-story.md` is intentionally absent under `full-bug` and its absence is not a blocker.
- **Research source:** `docs/features/active/2026-08-17-pr-context-verification-cannot-express-expected-nonzero-exit-485/research/2026-08-17T16-10-expected-nonzero-exit-research.md`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

## Plan Conventions

- `${FEATURE}` = `docs/features/active/2026-08-17-pr-context-verification-cannot-express-expected-nonzero-exit-485`
- `${EXT}` = `extensions/drm-copilot`
- `<TS>` = the ISO-8601 `yyyy-MM-ddTHH-mm` capture timestamp of the run that produced the artifact.
- Every evidence artifact path resolves under `${FEATURE}/evidence/<kind>/`. No `artifacts/`-rooted evidence path is valid in this plan.
- Every command-step evidence artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- Python commands run from the root of THIS worktree (the repository root of the checkout named in the delegation prompt, not any sibling checkout). TypeScript commands run from `${EXT}` inside this worktree. See SC9 for the cross-checkout `sys.path` hazard this distinction guards against.
- Verified toolchain command forms (research §9.4, re-verified against `pyproject.toml` and `${EXT}/package.json` during planning):
  - Python: `poetry run black .`, `poetry run ruff check .`, `poetry run pyright`, `poetry run pytest --cov --cov-branch --cov-report=term-missing`
  - TypeScript (from `${EXT}`): `npm run format`, `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:coverage`
  - `pyproject.toml:116` `addopts` carries no `--cov`, so `--cov --cov-branch` are load-bearing (constraint C3). A pytest run without them measures nothing. The same `addopts` DOES carry `--cov-report=lcov:artifacts/python/lcov.info`, so every coverage run also writes an LCOV report from which separate line (`LH/LF`) and branch (`BRH/BRF`) percentages can be derived.
  - The Phase 0 formatter baselines use the NON-MUTATING check forms `poetry run black --check .` and, from `${EXT}`, `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`. The mutating write forms `poetry run black .` and `npm run format` are used ONLY in the Phase 8 final QC loop, where rewriting files is the intended behavior.
  - `${EXT}/package.json:212` defines `test:coverage` as `node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary`, and `${EXT}/jest.config.cjs:18` sets `coverageReporters: ["lcov", "text-summary"]`. `text-summary` prints only the four overall totals, so any task needing PER-FILE rows must append `--coverageReporters=text` (or read `${EXT}/coverage/lcov.info`).

## Standing Constraints (bind every phase)

- **SC1 — Additive invariant (spec Invariant A).** An artifact carrying no `ExpectedExitCode` line must produce a byte-identical Verification row. Proved in two layers: Layer 1 hermetic exhaustive assertion (Phase 3, Phase 4) and Layer 2 corpus differential recorded as evidence (Phase 7).
- **SC2 — Parity invariant (spec Invariant C7).** Both parser legs land in the same change set. Phases 2-4 are not separately releasable.
- **SC3 — Deferred defect.** The pre-existing duplicate-`EXIT_CODE` precedence divergence (Python last-wins at `scripts/dev_tools/pr_context/verification_evidence.py:108`; TypeScript first-wins at `${EXT}/src/lib/pr-context/verification-evidence.ts:110-113`; ~156 of 968 corpus artifacts affected) is OUT OF SCOPE. No task in this plan changes `EXIT_CODE` precedence in either runtime. The `EXIT_CODE` assignment statement in each parse loop is untouched.
- **SC4 — No reuse of the executor QC path.** `scripts/dev_tools/atomic_executor/qc_runner_expectations.py` and `scripts/dev_tools/atomic_executor/pytest_expectations.py` are not imported, copied, or adapted (spec Invariant G, AC18).
- **SC5 — 500-line file limit.** `.claude/rules/general-code-change.md`. `scripts/dev_tools/pr_context/collector.py` (619 lines), `tests/scripts/dev_tools/test_collect_pr_context_part4.py` (1026 lines, verified during planning), and `tests/scripts/dev_tools/test_collect_pr_context.py` (654 lines, verified at preflight iteration 3) are ALREADY over the limit before this change; that overage is pre-existing and is not created by this change. `collector.py` growth is capped at 5 added lines (AC20) and BOTH over-limit test modules receive zero changed lines (AC20, AC25), which is what the `git diff --numstat` gates at [P5-T6] and [P8-T11] measure.
- **SC6 — No temporary files in tests.** `.claude/rules/general-unit-test.md`. Python tests use `mem_fs_path` (`tests/conftest.py:145-172`), never `tmp_path`. TypeScript tests use the in-file `SeededFileSystem` and `${EXT}/test/lib/pr-context/tree-file-system.ts`. The Layer 2 corpus differential is a THROWAWAY SCRIPT, not a test; it is never committed and is deleted before the final QC loop.
- **SC7 — Evidence-artifact authoring prohibition.** No evidence artifact produced by this plan may contain a PARSEABLE `ExpectedExitCode` key line — that is, a line whose text before the first colon strips to exactly `ExpectedExitCode` (`verification_evidence.py:105-107`), equivalently a line matching `^[[:space:]]*ExpectedExitCode[[:space:]]*:`. Naming the token inside prose or inside backticks is NOT a violation, because the parser never reads such a line as a key; [P0-T6] and [P7-T6] are required to name the token in prose, and the SC7 gates at [P7-T5] and [P9-T5] are anchored to the key form so they do not falsely match that prose. Reason: this feature's own `evidence/other/**` and `evidence/qa-gates/**` artifacts are inside the corpus discovered by `CANONICAL_GLOBS`, so an artifact carrying the new key would produce a rendered-row difference and would falsify the AC9 zero-diff assertion against its own run. Gates whose acceptance condition is a non-zero exit record the observed code faithfully in `EXIT_CODE:` and explain the expectation in prose inside `Output Summary:`.
- **SC8 — Coverage.** Line >= 85% and branch >= 75% for both Python and TypeScript, uniform across tiers. Baseline and final-QC test steps both run in coverage mode and record numeric values.
- **SC9 — Cross-checkout `sys.path` hazard.** Any Python script invoked from a path OUTSIDE this worktree (for example a scratchpad script) MUST bootstrap `sys.path` to the working directory with `sys.path.insert(0, str(Path.cwd()))` before any repository import, and MUST assert that each imported repository module's `__file__` resolves under `Path.cwd()`, failing loudly otherwise. Reason: a script's own directory becomes `sys.path[0]`, so the working directory is never on the path, and the inherited Poetry virtualenv (`VIRTUAL_ENV` = `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-17T07-04\.venv`) places a DIFFERENT checkout's repository root on `sys.path`; a bare import therefore binds to that checkout's unmodified code and any differential built on it is unfalsifiable. `poetry run pytest` is EXEMPT because `tests/conftest.py:57-61` already resolves the repository root from the conftest's own location and inserts it at `sys.path[0]`. The TypeScript legs are unaffected: Jest `rootDir` is this worktree's `${EXT}` and their imports are relative.
- **SC10 — Node dependency resolution hazard.** This worktree contains no `${EXT}/node_modules`, so Node resolves the extension's tool binaries by walking up to `C:\Users\DanMoisan\repos\drm-copilot\node_modules`, a DIFFERENT checkout (verified at preflight: `require.resolve('jest/bin/jest')` and `require.resolve('eslint')` both resolve there). That ancestor tree is incomplete for this package: `npm run lint` fails with `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@eslint/js'`, so [P0-T12], [P8-T6], [P8-T10], and AC24 cannot pass until the extension's own dependencies are installed. `${EXT}/package-lock.json` pins `@eslint/js@10.0.1`, and `${EXT}/node_modules` is gitignored (`.gitignore:3`), so `npm ci` from `${EXT}` remediates without polluting any `git diff --numstat` gate. Jest test DISCOVERY is unaffected — `rootDir` defaults to `${EXT}` in this worktree and test imports are relative — so the Layer 2 TypeScript differential of [P7-T3] remains falsifiable; this hazard is tool AVAILABILITY, not corpus scope.

---

### Phase 0 — Policy Compliance Reading and Baseline Capture

- [ ] [P0-T1] Read the policy files in the `policy-compliance-order` sequence — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`, `.claude/rules/quality-tiers.md` — and write `${FEATURE}/evidence/baseline/phase0-instructions-read.md` carrying `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists and names every file above in the stated order.
- [ ] [P0-T2] Read `${FEATURE}/spec.md`, `${FEATURE}/research/2026-08-17T16-10-expected-nonzero-exit-research.md`, and `${FEATURE}/issue.md` in full, then write `${FEATURE}/evidence/baseline/ac-inventory.<TS>.md` listing AC1 through AC25 verbatim with the plan task ID that discharges each. Acceptance: the artifact lists exactly 25 criteria and every criterion maps to at least one `[P#-T#]` in this plan.
- [ ] [P0-T3] Record the baseline git reference used by every `git diff main` acceptance gate: run `git merge-base HEAD main` and `git rev-parse HEAD`, and write `${FEATURE}/evidence/baseline/git-baseline-ref.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming both SHAs. Acceptance: the artifact exists and both SHAs are recorded.
- [ ] [P0-T4] Capture the pre-change file-size census. Run `(Get-Content <path>).Count` for `scripts/dev_tools/pr_context/verification_evidence.py`, `scripts/dev_tools/pr_context/collector.py`, `${EXT}/src/lib/pr-context/verification-evidence.ts`, `${EXT}/src/lib/pr-context/collector-output.ts`, `${EXT}/test/lib/pr-context/verification-evidence.test.ts`, `${EXT}/test/lib/pr-context/collector-output.test.ts`, and `tests/scripts/dev_tools/test_collect_pr_context_part4.py`. Write `${FEATURE}/evidence/baseline/file-size-census.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` giving each count, and an explicit statement that `collector.py` and `test_collect_pr_context_part4.py` are ALREADY over the 500-line limit before this change and that the overage is pre-existing, not created here. Acceptance: the artifact exists, records seven counts, and carries the pre-existing-overage statement.
- [ ] [P0-T5] Re-verify assumption A1 (clean namespace, AC11 pre-change leg). Run `git grep --untracked -n -E "ExpectedExitCode|expected_exit|expectedExit|expected_nonzero|EXPECTED_EXIT" -- docs/features` and write `${FEATURE}/evidence/baseline/namespace-clean-grep.<TS>.md` with the four required fields. `--untracked` is required because the evidence artifacts this gate searches are newly created and untracked when the gate runs, so a plain `git grep` would not see them and the gate could not fail; `--untracked` searches tracked and untracked files both, so it is correct whether or not the feature documents have already been committed. Acceptance: every match is under `${FEATURE}/`; a match anywhere else halts the plan for spec amendment.
- [ ] [P0-T6] Record SC7 as a durable decision at `${FEATURE}/evidence/other/evidence-authoring-constraint.<TS>.md`, stating that no evidence artifact produced by this plan may contain an `ExpectedExitCode:` line, and giving the reason (this feature's own evidence files are inside the `CANONICAL_GLOBS` corpus that AC9 asserts zero differences over). Acceptance: the artifact exists and states the prohibition and the reason.
- [ ] [P0-T7] Baseline Python formatting. Run the NON-MUTATING check form `poetry run black --check .` and write `${FEATURE}/evidence/baseline/py-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming the count of files that would be reformatted and the count left unchanged. The `--check` form is load-bearing: a baseline measures, it does not write, and a mutating baseline run could pollute the `git diff --numstat` gates at [P5-T6], [P6-T8], [P7-T8], [P7-T12], and [P8-T11]. Acceptance: the artifact exists and records the exit code and the "would be left unchanged" counts; no file is modified by this task.
- [ ] [P0-T8] Baseline Python linting. Run `poetry run ruff check .` and write `${FEATURE}/evidence/baseline/py-ruff.<TS>.md` with the four required fields; `Output Summary:` records the diagnostic count. Acceptance: the artifact exists with a numeric diagnostic count.
- [ ] [P0-T9] Baseline Python type checking. Run `poetry run pyright` and write `${FEATURE}/evidence/baseline/py-pyright.<TS>.md` with the four required fields; `Output Summary:` records the error and warning counts. Acceptance: the artifact exists with numeric error and warning counts.
- [ ] [P0-T10] Baseline Python tests with coverage. Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `${FEATURE}/evidence/baseline/py-pytest-coverage.<TS>.md` with the four required fields. `Output Summary:` MUST carry numeric values for: passed/failed/skipped counts, overall line coverage percent, overall branch coverage percent, and the per-file line and branch coverage of `scripts/dev_tools/pr_context/verification_evidence.py` and `scripts/dev_tools/pr_context/collector.py`. Note that the `term-missing` table's `Cover` column is a COMBINED statement-plus-branch metric and is neither the line nor the branch percentage; derive the separate line and branch percentages (overall and per file) from the LCOV report that the `addopts` at `pyproject.toml:116` already writes to `artifacts/python/lcov.info` (`LH/LF` for lines, `BRH/BRF` for branches), or equivalently from `poetry run coverage json -o -` against `artifacts/.coverage`. Record the derivation source in `Command:`. Acceptance: all six numeric values are present; no placeholder such as `UNVERIFIED` is permitted.
- [ ] [P0-T11] Install the extension's Node dependencies into THIS worktree, then baseline TypeScript formatting. First, from `${EXT}`, run `npm ci`, then confirm `${EXT}/node_modules` exists inside this worktree and that `node -e "console.log(require.resolve('eslint'))"` resolves under this worktree (SC10); a path under any other checkout halts the plan. Then run the NON-MUTATING check form `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` (the same glob set the `format` script writes over) and write `${FEATURE}/evidence/baseline/ts-format.<TS>.md` with the four required fields; `Command:` records both the `npm ci` and the `npx prettier --check` invocations, and `Output Summary:` records the resolved `eslint` path, whether Prettier reported `All matched files use Prettier code style!`, and the list of any file it would rewrite. The `--check` form is load-bearing for the same reason as [P0-T7]: a baseline must not write files that the later `git diff --numstat` gates measure; `${EXT}/node_modules` is gitignored so the install does not affect those gates. Acceptance: `${EXT}/node_modules` exists inside this worktree, `eslint` resolves under this worktree, the artifact exists and records the check result, and no tracked file is modified by this task.
- [ ] [P0-T12] Baseline TypeScript linting. From `${EXT}`, run `npm run lint` and write `${FEATURE}/evidence/baseline/ts-lint.<TS>.md` with the four required fields; `Output Summary:` records the error and warning counts. Acceptance: the artifact exists with numeric counts.
- [ ] [P0-T13] Baseline TypeScript type checking. From `${EXT}`, run `npm run typecheck` and write `${FEATURE}/evidence/baseline/ts-typecheck.<TS>.md` with the four required fields; `Output Summary:` records the `tsc` error count. Acceptance: the artifact exists with a numeric error count.
- [ ] [P0-T14] Baseline TypeScript unit tests. From `${EXT}`, run `npm run test:unit` and write `${FEATURE}/evidence/baseline/ts-test-unit.<TS>.md` with the four required fields; `Output Summary:` records suite and test pass/fail counts. Acceptance: the artifact exists with numeric counts.
- [ ] [P0-T15] Baseline TypeScript coverage. From `${EXT}`, run `npm run test:coverage -- --coverageReporters=text --coverageReporters=text-summary --coverageReporters=lcov` and write `${FEATURE}/evidence/baseline/ts-test-coverage.<TS>.md` with the four required fields, recording the exact invocation in `Command:`. The added `text` reporter is load-bearing: the script's configured reporters are `lcov` and `text-summary` only, and `text-summary` prints no per-file rows. `Output Summary:` MUST carry numeric overall line coverage percent and overall branch coverage percent from the text-summary block, plus the per-file line and branch coverage of `src/lib/pr-context/verification-evidence.ts` and `src/lib/pr-context/collector-output.ts` read from the `text` table or, equivalently, from the `LH/LF` and `BRH/BRF` records for those files in `${EXT}/coverage/lcov.info` (state which source was used). Acceptance: all numeric values are present; no placeholder is permitted.
- [ ] [P0-T16] Record the baseline set of in-repo construction sites of `VerificationEvidenceRecord` as object literals. Run `git grep -n "VerificationEvidenceRecord" -- ${EXT}/src ${EXT}/test scripts/dev_tools tests` and write `${FEATURE}/evidence/baseline/record-construction-sites.<TS>.md` with the four required fields; `Output Summary:` enumerates each file and line that constructs the record directly rather than through the parser. The grep returns EVERY mention of the identifier — type annotations, imports, and comments as well as object-literal construction sites — so the enumeration requires manual filtering of the raw output down to the construction sites; record both the raw match count and the filtered construction-site count. `npm run typecheck` at [P4-T2]/[P4-T8] is the independent cross-check that the filtered list is complete. Acceptance: the artifact records both counts and enumerates every construction site, which is the checklist Phase 4 must exhaust (risk R8).

---

### Phase 1 — Failing-First Regression Capture

Each task in this phase is tagged `[expect-fail]`. The regression fixtures deliberately use only symbols that exist today, so each failure is a behavior failure at an assertion rather than a compile or import error, which makes the fail-before evidence unambiguous.

- [ ] [P1-T1] [expect-fail] Create `tests/scripts/dev_tools/pr_context/__init__.py` (empty package marker) and `tests/scripts/dev_tools/pr_context/test_verification_evidence.py` containing exactly one test, `test_observed_equal_to_nonzero_expectation_passes`, which calls `parse_verification_evidence_markdown` with inline markdown carrying `Timestamp:`, `Command:`, `EXIT_CODE: 1`, and `ExpectedExitCode: 1`, and asserts `normalized_result == "pass"` and `exit_code == 1`. Acceptance: both files exist; the test references no symbol that does not exist today.
- [ ] [P1-T2] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/pr_context/test_verification_evidence.py` and record the failing run at `${FEATURE}/evidence/regression-testing/py-parser-fail-before.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` quoting the assertion failure showing `fail` where `pass` was expected. Acceptance: `EXIT_CODE:` is non-zero and the summary quotes the assertion diff.
- [ ] [P1-T3] [expect-fail] Add one test to the existing `describe("parseVerificationEvidenceMarkdown")` block in `${EXT}/test/lib/pr-context/verification-evidence.test.ts`, named `normalizes to pass when the observed code equals a non-zero expectation`, using the same inline markdown shape and asserting `normalizedResult === "pass"` and `exitCode === 1`. Acceptance: the test is added, references only symbols that exist today, and no existing test body is edited.
- [ ] [P1-T4] [expect-fail] From `${EXT}`, run `npm run test:unit -- test/lib/pr-context/verification-evidence.test.ts` and record the failing run at `${FEATURE}/evidence/regression-testing/ts-parser-fail-before.<TS>.md` with the four required fields; `Output Summary:` quotes the failing expectation. Acceptance: `EXIT_CODE:` is non-zero and the summary quotes the failure.
- [ ] [P1-T5] [expect-fail] Create the new Python collector-level sibling module `tests/scripts/dev_tools/test_collect_pr_context_expected_exit.py` (a NEW file; `tests/scripts/dev_tools/test_collect_pr_context_part4.py` receives zero changed lines per SC5) containing one test that seeds a `mem_fs_path` evidence file with `EXIT_CODE: 1` and `ExpectedExitCode: 1`, renders the verification section, and asserts the rendered body contains the line `  - Expected EXIT_CODE: 1` between the `EXIT_CODE` and `Normalized result` lines. Acceptance: the file exists, uses `mem_fs_path` and never `tmp_path`, and is under 500 lines.
- [ ] [P1-T6] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_collect_pr_context_expected_exit.py` and record the failing run at `${FEATURE}/evidence/regression-testing/py-renderer-fail-before.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE:` is non-zero and the summary shows the missing expectation line.
- [ ] [P1-T7] [expect-fail] Add one test to `describe("renderVerificationEvidenceSection")` at `${EXT}/test/lib/pr-context/collector-output.test.ts:268`, asserting the same conditional line for an evidence file carrying `EXIT_CODE: 1` and `ExpectedExitCode: 1`, using `TreeFileSystem`. Acceptance: the test is added and no existing test body in that describe block is edited.
- [ ] [P1-T8] [expect-fail] From `${EXT}`, run `npm run test:unit -- test/lib/pr-context/collector-output.test.ts` and record the failing run at `${FEATURE}/evidence/regression-testing/ts-renderer-fail-before.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE:` is non-zero and the summary shows the missing expectation line.

---

### Phase 2 — Python Parser Fix

- [ ] [P2-T1] In `scripts/dev_tools/pr_context/verification_evidence.py`, add the optional-field constant `EXPECTED_EXIT_CODE_FIELD: str = "ExpectedExitCode"` on a new line adjacent to `REQUIRED_FIELDS`. Do NOT introduce a combined accept-list: Invariant D is verified by a diff gate ([P7-T8], AC12) that rejects ANY changed line containing the token `REQUIRED_FIELDS`, so both a `(*REQUIRED_FIELDS, ...)` line and any edit to the membership test would fail it. Leave `REQUIRED_FIELDS` at line 22 and the membership test `if key in REQUIRED_FIELDS:` at line 107 both byte-identical, including the `tuple[str, str, str]` annotation. Acceptance: no changed (`+` or `-`) line in `git diff` for this file contains `REQUIRED_FIELDS`.
- [ ] [P2-T2] Append `expected_exit_code: int = 0` as the LAST field of the frozen dataclass `VerificationEvidenceRecord` (currently `verification_evidence.py:49-54`) and document it in the `Attributes:` block of the class docstring. Acceptance: the field is last, carries the default `0`, and existing keyword construction elsewhere in the repository still type-checks.
- [ ] [P2-T3] Extract the normalization into a module-level pure helper `def normalize_result(exit_code: int, expected_exit_code: int) -> NormalizedResult` returning `"pass"` when the two arguments are equal and `"fail"` otherwise, with a docstring declaring `Side Effects: None.`. Acceptance: the helper exists, takes exactly two integer parameters, and performs no I/O.
- [ ] [P2-T4] Extend the parse loop at `verification_evidence.py:102-108` by appending an `elif` branch immediately after the existing `if key in REQUIRED_FIELDS:` block, leaving that `if` line and its body byte-identical: `elif key == EXPECTED_EXIT_CODE_FIELD and key not in parsed:` followed by `parsed[key] = value.strip()`. The `key not in parsed` guard gives the optional key FIRST-WINS precedence; the untouched `if` branch leaves `EXIT_CODE` on its existing last-wins precedence (SC3). Acceptance: a duplicated `ExpectedExitCode:` resolves to the first occurrence while a duplicated `EXIT_CODE:` still resolves to the last, the diff touches no other precedence behavior, and no changed line contains `REQUIRED_FIELDS`.
- [ ] [P2-T5] Parse the expectation with the same strictness as `EXIT_CODE`: absent key yields `0`; a present value is parsed with `int()` inside the existing `try/except ValueError` discipline; a present non-integer value (including an empty value) returns an `unparseable` record. Acceptance: `ExpectedExitCode: banana` and a bare `ExpectedExitCode:` both yield `unparseable`.
- [ ] [P2-T6] Set `expected_exit_code=0` explicitly on EVERY `unparseable` return path (the missing-required-field branch at 114-122, the non-integer-`EXIT_CODE` branch at 124-134, and the new non-integer-expectation branch), alongside the existing `exit_code=None` (Invariant E). Acceptance: every `unparseable` record in this module carries `exit_code=None` and `expected_exit_code=0`.
- [ ] [P2-T7] Replace the expression at `verification_evidence.py:136` with a call to `normalize_result(exit_code, expected_exit_code)` and pass `expected_exit_code` into the success-path record construction. Acceptance: the literal comparison `exit_code == 0` no longer appears in the module, and `normalize_result` is the sole normalization site.
- [ ] [P2-T8] Verify the module introduces no logging and no new import: `git grep -n -E "^import logging|^from logging|logging\.|print\(" -- scripts/dev_tools/pr_context/verification_evidence.py` returns no match, and the `Side Effects: None.` contract at the `parse_verification_evidence_markdown` docstring is intact. Record the result at `${FEATURE}/evidence/qa-gates/py-no-logging-gate.<TS>.md` with the four required fields; the grep exits `1` on zero matches, which is the passing condition, and `Output Summary:` states that explicitly. Acceptance: zero matches and the artifact records the exit code faithfully.
- [ ] [P2-T9] Re-run the Phase 1 Python parser regression: `poetry run pytest tests/scripts/dev_tools/pr_context/test_verification_evidence.py`. Record the passing run at `${FEATURE}/evidence/regression-testing/py-parser-pass-after.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and the previously failing test passes.

---

### Phase 3 — Python Parser Test Suite

All tests in this phase live in `tests/scripts/dev_tools/pr_context/test_verification_evidence.py`, use inline markdown strings, use `mem_fs_path` for the file-reading function, and never use `tmp_path`. Test names below are the contract fixed by `spec.md`; a rename requires a spec amendment.

- [ ] [P3-T1] Add the parametrized eleven-shape fixture table from `spec.md` "Unit tests (pytest)" as a single parametrized test, with each case carrying an explicit shape identifier `shape-01` through `shape-11` in that order, so the table is textually diffable against the TypeScript transcription (AC8). Shapes 01-05 and 07-11 each carry exactly ONE `EXIT_CODE:` line, so the deferred divergence of SC3 cannot confound them. Shape-06 is the duplicated-`EXIT_CODE` case (`spec.md` shape 6, research §6.2 case 6) and therefore carries TWO `EXIT_CODE:` lines by definition; its expected record is RUNTIME-SPECIFIC and pins this runtime's existing precedence — Python is last-wins, so the Python case asserts the SECOND value. Shape-06 is EXCLUDED from the AC8 cross-runtime agreement assertion, and the exclusion is attributable to the deferred duplicate-`EXIT_CODE` defect of SC3, not to this change. Acceptance: eleven parametrized cases exist, in order, with matching shape identifiers; shape-06 asserts last-wins and is annotated in-file as runtime-specific and AC8-excluded.
- [ ] [P3-T2] Add `test_absent_expectation_records_match_pre_change_shapes`, asserting that for shapes 1 through 8 the parsed record equals the pre-change record computed inline from `("pass" if exit_code == 0 else "fail")`, and that `expected_exit_code == 0` in each (AC1). Acceptance: the test exists under that exact name and covers all eight shapes.
- [ ] [P3-T3] Add `test_normalize_result_with_default_expectation_matches_pre_change_expression`, parametrized over the integer range -8..8 plus at least four large-magnitude values, asserting `normalize_result(observed, 0) == ("pass" if observed == 0 else "fail")` (AC3, Layer 1). Acceptance: the test exists under that exact name, exercises at least 21 observed values, and touches no filesystem.
- [ ] [P3-T4] Add `test_observed_equal_to_nonzero_expectation_passes` covering `EXIT_CODE: 1` / `ExpectedExitCode: 1`, a large equal pair, and a negative equal pair, asserting `pass` and retention of the observed `exit_code` (AC4). This supersedes the single-assertion regression stub created at P1-T1. Acceptance: the test exists under that exact name and covers all three pairs.
- [ ] [P3-T5] Add `test_observed_differing_from_nonzero_expectation_fails` covering `EXIT_CODE: 2` / `ExpectedExitCode: 1` and `EXIT_CODE: 0` / `ExpectedExitCode: 1`, asserting `fail` in both (AC5). Acceptance: the test exists under that exact name and covers both directions.
- [ ] [P3-T6] Add `test_non_integer_expectation_is_unparseable_and_clears_fields` covering `ExpectedExitCode: banana` and a bare `ExpectedExitCode:`, asserting `normalized_result == "unparseable"`, `exit_code is None`, and `expected_exit_code == 0` (AC6, Invariant E). Acceptance: the test exists under that exact name and asserts all three fields for both inputs.
- [ ] [P3-T7] Add `test_duplicate_expectation_key_takes_first_occurrence`, using markdown with two `ExpectedExitCode:` lines carrying different values, asserting the first wins (AC7). Acceptance: the test exists under that exact name and pins first-wins.
- [ ] [P3-T8] Add `test_skipped_exit_code_remains_unparseable`, covering `EXIT_CODE: SKIPPED` both with and without an expectation line, asserting `unparseable` in both cases (AC14, Invariant F). Acceptance: the test exists under that exact name and covers both cases.
- [ ] [P3-T9] Add `test_unrecognized_rows_are_ignored`, covering an artifact carrying `Output Summary:` and a wrong-cased `expectedexitcode:` row, asserting the record is identical to the same artifact without those rows (AC15). Acceptance: the test exists under that exact name and asserts record equality.
- [ ] [P3-T10] Add a test asserting that a value containing colons after the first is preserved intact, and a test asserting that `parse_verification_evidence_file` still propagates `OSError` unchanged, both exercised through `mem_fs_path`. Acceptance: both tests exist and neither uses `tmp_path`.
- [ ] [P3-T11] Verify `tests/scripts/dev_tools/pr_context/test_verification_evidence.py` is at most 500 lines via `(Get-Content tests/scripts/dev_tools/pr_context/test_verification_evidence.py).Count`, and run `poetry run pytest tests/scripts/dev_tools/pr_context/ --cov=scripts.dev_tools.pr_context.verification_evidence --cov-branch --cov-report=term-missing`. Record both at `${FEATURE}/evidence/qa-gates/py-parser-module-coverage.<TS>.md` with the four required fields; `Output Summary:` carries the line count and the module's numeric line and branch coverage. Acceptance: the file is <= 500 lines and the module reports no uncovered added or changed line in the `Missing` column. If the file approaches 500 lines, split it into a sibling module rather than exceeding the limit.

---

### Phase 4 — TypeScript Parser Fix and Test Suite

This phase completes the parity pair opened in Phase 2 (SC2). It must land in the same change set as Phase 2.

- [ ] [P4-T1] In `${EXT}/src/lib/pr-context/verification-evidence.ts`, add the optional-field constant `EXPECTED_EXIT_CODE_FIELD` on a new line adjacent to `REQUIRED_FIELDS`. Do NOT introduce a combined accept-list derived from `REQUIRED_FIELDS`, for the same Invariant D diff-gate reason recorded in [P2-T1]. Leave the exported `REQUIRED_FIELDS` at line 24 and the membership test at line 111 both byte-identical. Acceptance: no changed (`+` or `-`) line in `git diff` for this file contains `REQUIRED_FIELDS`.
- [ ] [P4-T2] Add `readonly expectedExitCode: number;` to the `VerificationEvidenceRecord` interface at lines 37-44, with a doc comment stating it defaults to `0` when the key is absent and on every `unparseable` path. Acceptance: the member exists and `npm run typecheck` identifies every construction site that must be updated.
- [ ] [P4-T3] Add the exported pure helper `normalizeResult(exitCode: number, expectedExitCode: number): NormalizedResult` returning `"pass"` on equality and `"fail"` otherwise, mirroring the Python helper of P2-T3. Acceptance: the helper exists, takes exactly two numeric parameters, and performs no I/O.
- [ ] [P4-T4] Extend the parse loop at `verification-evidence.ts:103-116` by appending a NEW, SEPARATE `if` block immediately after the existing `if ((REQUIRED_FIELDS as readonly string[]).includes(key) && !parsed.has(key)) { ... }` block, leaving every line of that existing block byte-identical: `if (key === EXPECTED_EXIT_CODE_FIELD && !parsed.has(key)) { parsed.set(key, value); }`. The separate `if` form is prescribed rather than `else if` because `else if` rewrites the existing block's closing `}` line into `} else if (...) {`, which is not a pure addition; a standalone `if` is. The two conditions are mutually exclusive because `EXPECTED_EXIT_CODE_FIELD` is not a member of `REQUIRED_FIELDS`, so the separate form is semantically identical to `else if`, and it mirrors the pure-addition form of the Python `elif` at [P2-T4]. The `!parsed.has(key)` guard gives the optional key first-wins, mirroring the required-key behavior, and the `EXIT_CODE` branch is untouched (SC3). Acceptance: a duplicated `ExpectedExitCode:` resolves to the first occurrence; the `git diff` hunk covering the parse loop contains no deleted (`-`) line; and no changed line contains `REQUIRED_FIELDS`.
- [ ] [P4-T5] Parse the expectation through the existing `parseIntegerStrict` helper (lines 196-201): absent key yields `0`; a present non-integer value (including an empty value) returns an `unparseable` record. Acceptance: `ExpectedExitCode: banana` and a bare `ExpectedExitCode:` both yield `unparseable`.
- [ ] [P4-T6] Set `expectedExitCode: 0` explicitly on every `unparseable` return path (the missing-field branch at 123-132, the non-integer-`EXIT_CODE` branch at 134-144, and the new non-integer-expectation branch), alongside the existing `exitCode: null` (Invariant E). Acceptance: every `unparseable` record in this module carries `exitCode: null` and `expectedExitCode: 0`.
- [ ] [P4-T7] Replace the expression at `verification-evidence.ts:146` with a call to `normalizeResult(exitCode, expectedExitCode)` and pass `expectedExitCode` into the success-path record construction. Acceptance: the literal comparison `exitCode === 0` no longer appears in the module.
- [ ] [P4-T8] Update every in-repo construction site of `VerificationEvidenceRecord` enumerated in the P0-T16 baseline artifact so each supplies `expectedExitCode` (risk R8). Acceptance: `npm run typecheck` from `${EXT}` reports zero errors, and every site listed in the baseline artifact is accounted for.
- [ ] [P4-T9] Transcribe the eleven-shape fixture table into `${EXT}/test/lib/pr-context/verification-evidence.test.ts` inside the existing `describe("parseVerificationEvidenceMarkdown")` block, using shape identifiers `shape-01` through `shape-11` in the same order as P3-T1 so the two tables are diffable by eye (AC8). Shape-06 carries two `EXIT_CODE:` lines and its expected record is runtime-specific: TypeScript is first-wins, so the TypeScript case asserts the FIRST value, deliberately differing from the Python case of P3-T1. Shape-06 is EXCLUDED from the AC8 cross-runtime agreement assertion per SC3. Acceptance: eleven cases exist in matching order with matching identifiers; shape-06 asserts first-wins and is annotated in-file as runtime-specific and AC8-excluded.
- [ ] [P4-T10] Add the TypeScript counterparts of the named Python tests, using the exact test names fixed by `spec.md`: `defaults the expectation to zero and matches pre-change records` (AC2), `normalizeResult with a zero expectation matches the pre-change expression` (AC3), `normalizes to pass when the observed code equals a non-zero expectation` (AC4, superseding the P1-T3 stub), `normalizes to fail when the observed code differs from a non-zero expectation` (AC5), `reports unparseable for a non-integer expectation` (AC6), `takes the first occurrence of a duplicated expectation key` (AC7), `reports unparseable for EXIT_CODE SKIPPED` (AC14), and `ignores rows outside the accept-list` (AC15). All use inline markdown and the in-file `SeededFileSystem`; none adds disk access. Acceptance: all eight tests exist under those exact names and pass.
- [ ] [P4-T11] Verify `${EXT}/src/lib/pr-context/verification-evidence.ts` and `${EXT}/test/lib/pr-context/verification-evidence.test.ts` are each at most 500 lines, then from `${EXT}` run `npm run test:unit -- test/lib/pr-context/verification-evidence.test.ts`. Record both at `${FEATURE}/evidence/qa-gates/ts-parser-module-check.<TS>.md` with the four required fields; `Output Summary:` carries both line counts and the test pass count. Acceptance: both files are <= 500 lines and the suite passes.
- [ ] [P4-T12] Re-run both Phase 1 parser regressions to confirm the parity pair is closed: `poetry run pytest tests/scripts/dev_tools/pr_context/test_verification_evidence.py` and, from `${EXT}`, `npm run test:unit -- test/lib/pr-context/verification-evidence.test.ts`. Record at `${FEATURE}/evidence/regression-testing/parser-parity-pass-after.<TS>.md` with the four required fields for each command. Additionally record in that same artifact the shape-by-shape AC8 cross-runtime agreement result for shapes 01-05 and 07-11, and the explicit statement that shape-06 is excluded because it is the duplicated-`EXIT_CODE` case whose per-runtime precedence divergence is deferred by SC3. Acceptance: both commands exit `0`, the ten in-scope shapes agree across runtimes, and the shape-06 exclusion statement is present.

---

### Phase 5 — Renderers and Collector-Level Tests

- [ ] [P5-T1] In `scripts/dev_tools/pr_context/collector.py`, emit `  - Expected EXIT_CODE: <int>` between the `EXIT_CODE` line and the `Normalized result` line inside the row loop at lines 155-165, and only when `record.expected_exit_code` is non-zero. Implement it as a per-record local list spliced into the existing `lines.extend([...])` call (a bounded three-line addition after Black formatting: one local for the expectation, one local for the conditional row list, one splat entry) rather than as a new statement block, so the net addition stays well inside the AC20 cap of 5 added lines. The pre-existing 619-line overage of this file is recorded at P0-T4 and is not created by this change; extraction of the file is out of scope per `spec.md`. Acceptance: the conditional line renders correctly and `git diff --numstat <baseline-ref> -- scripts/dev_tools/pr_context/collector.py` shows at most 5 added lines.
- [ ] [P5-T2] In `${EXT}/src/lib/pr-context/collector-output.ts`, make the equivalent conditional insertion in the `lines.push(...)` call at lines 116-123 (the enclosing `for` loop spans 115-124; the `push` call itself is 116-123), emitting the identical string `  - Expected EXIT_CODE: <int>` in the identical position and only when `record.expectedExitCode` is non-zero. Acceptance: the rendered row is string-equal to the Python renderer's output for the same record, and the file remains at most 500 lines (it had 51 lines of headroom at baseline).
- [ ] [P5-T3] Complete `tests/scripts/dev_tools/test_collect_pr_context_expected_exit.py` (created at P1-T5) with exactly two cases: the conditional line appears when the expectation is non-zero, and it is absent both when the key is omitted and when the key is written explicitly as `0` (AC16). Acceptance: both cases exist, use `mem_fs_path`, and the module is at most 500 lines.
- [ ] [P5-T4] Complete the additions to `describe("renderVerificationEvidenceSection")` in `${EXT}/test/lib/pr-context/collector-output.test.ts` with the same two cases (AC16). Acceptance: both cases exist, use `TreeFileSystem`, and the file is at most 500 lines.
- [ ] [P5-T5] Verify the collector filters and section placement are untouched (Invariant C, AC13): `git diff <baseline-ref> -- scripts/dev_tools/pr_context/collector.py ${EXT}/src/lib/pr-context/collector-output.ts` shows no changed line inside the `{"pass", "fail"}` filter at `collector.py:147-149` or the equivalent at `collector-output.ts:98-101`, no change to the fallback string `No canonical verification evidence parsed`, and no change to the sort key or the section headings at `collector.py:513-514` / `collector-output.ts:236-237`. Record at `${FEATURE}/evidence/qa-gates/renderer-boundary-gate.<TS>.md` with the four required fields. Acceptance: the artifact records zero changed lines in every named region.
- [ ] [P5-T6] Verify AC20 and AC25 mechanically: `git diff --numstat <baseline-ref> -- scripts/dev_tools/pr_context/collector.py tests/scripts/dev_tools/test_collect_pr_context_part4.py tests/scripts/dev_tools/test_collect_pr_context.py`. Record at `${FEATURE}/evidence/qa-gates/file-growth-and-existing-tests-gate.<TS>.md` with the four required fields. Acceptance: `collector.py` shows at most 5 added lines; `test_collect_pr_context_part4.py` and `test_collect_pr_context.py` each emit NO `--numstat` row, which is how `git diff --numstat` represents an unmodified path — a literal `0	0` row is never emitted for an unchanged file, so absence of a row is the passing signal and presence of any row for either file is a failure.
- [ ] [P5-T7] Run both renderer regressions to confirm pass-after: `poetry run pytest tests/scripts/dev_tools/test_collect_pr_context_expected_exit.py tests/scripts/dev_tools/test_collect_pr_context.py tests/scripts/dev_tools/test_collect_pr_context_part4.py` and, from `${EXT}`, `npm run test:unit -- test/lib/pr-context/collector-output.test.ts`. Record at `${FEATURE}/evidence/regression-testing/renderer-pass-after.<TS>.md` with the four required fields per command. Acceptance: both commands exit `0`, including the three pre-existing collector-level Python tests named in `spec.md` "Regression tests to add or update", which must pass without edits to their bodies.

---

### Phase 6 — Evidence-Schema Documentation and Mirrors

- [ ] [P6-T1] Add the optional key to the machine-checkable schema block of `.claude/skills/evidence-and-timestamp-conventions/SKILL.md` (the `Timestamp` / `Command` / `EXIT_CODE` block in the "Evidence Artifact Schema" section), documenting: the exact PascalCase spelling `ExpectedExitCode`, the integer value form, the default of `0` when absent, that a present non-integer value makes the whole artifact `unparseable` and therefore removes the row from the PR body, that the first occurrence wins when the key is duplicated, and the one-gate-per-artifact guidance for the per-file limitation. Acceptance: the block documents all six points.
- [ ] [P6-T2] Apply the semantically identical edit to `.github/skills/evidence-and-timestamp-conventions/SKILL.md`, matching that file's own schema block and surrounding style. Acceptance: the key and all six points appear in that file.
- [ ] [P6-T3] Apply the semantically identical edit to `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`. Acceptance: the key and all six points appear in that file.
- [ ] [P6-T4] Propagate the byte-identical content of each canonical file into its bundled mirror: `.claude/...` into `${EXT}/resources/claude-customizations/.claude/skills/evidence-and-timestamp-conventions/SKILL.md`, `.github/...` into `${EXT}/resources/customizations/.github/skills/evidence-and-timestamp-conventions/SKILL.md`, and `.agents/...` into `${EXT}/resources/codex-and-agents-customizations/.agents/skills/evidence-and-timestamp-conventions/SKILL.md`. The push-down contract tests assert byte identity between each canonical tree and its bundled root, so the mirror must match its canonical source exactly. Acceptance: each mirror is byte-identical to its canonical source.
- [ ] [P6-T5] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py` and record at `${FEATURE}/evidence/qa-gates/push-down-contract-tests.<TS>.md` with the four required fields (AC23, risk R6). Acceptance: `EXIT_CODE: 0`.
- [ ] [P6-T6] Run `poetry run pytest tests/scripts/dev_tools/ -k push_down` to catch any copilot-side push-down contract test not named in P6-T5, and record at `${FEATURE}/evidence/qa-gates/push-down-contract-tests-all.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and the summary names every push-down contract test that ran.
- [ ] [P6-T7] Verify the six-copy fan-out: `git grep -c "ExpectedExitCode" -- "*evidence-and-timestamp-conventions/SKILL.md"` reports a match in exactly six files. Record at `${FEATURE}/evidence/qa-gates/docs-six-copy-gate.<TS>.md` with the four required fields (AC23). Acceptance: exactly six files are reported.
- [ ] [P6-T8] Verify `.claude/skills/atomic-plan-contract/SKILL.md` and its fan-out are NOT modified, since the new key is optional and the "MUST include" field lists there do not change: `git diff --numstat <baseline-ref> -- "*atomic-plan-contract/SKILL.md"` shows no output. Record at `${FEATURE}/evidence/qa-gates/atomic-plan-contract-untouched.<TS>.md` with the four required fields. Acceptance: zero changed files.

---

### Phase 7 — Additive and Parity Proof, Policy Gates

The Layer 2 corpus differential is a throwaway comparison script, not a committed test (risk R7, SC6). It is written outside the repository working tree, run once per runtime, and its absence from the repository is verified before the final QC loop.

- [ ] [P7-T1] Write the Python leg of the throwaway corpus differential script to the session scratchpad at `<scratchpad>/corpus_differential.py` (outside the repository tree, so no repository file is created or deleted). A scratchpad script sets `sys.path[0]` to its own directory, NOT to the working directory, and the active Poetry virtualenv contributes a DIFFERENT checkout's repository root to `sys.path` (verified at preflight: `VIRTUAL_ENV` resolves to `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-17T07-04\.venv`, whose site-packages entry places that checkout's root on the path), so a bare `import scripts...` binds silently to the UNCHANGED parser and renderer in another checkout and makes this gate unfalsifiable. The script MUST therefore begin, before any repository import, with `sys.path.insert(0, str(Path.cwd()))`, and MUST then hard-assert that `verification_evidence.__file__` and `collector.__file__` both resolve under `Path.cwd()`, printing the two resolved paths as its first output lines and exiting non-zero with a `WRONG_CHECKOUT` message if either does not. It enumerates every artifact discovered by `CANONICAL_GLOBS` under `docs/features/active/*/evidence/{qa-gates,regression-testing,other}/**/*.md`, computes for each the post-change record via `parse_verification_evidence_markdown` and the rendered six-line row via the post-change renderer, computes the pre-change reference row inline from `("pass" if exit_code == 0 else "fail")` with no expectation line, and reports the artifact count and the rendered-row diff count. Acceptance: the script exists in the scratchpad only, is not under the repository working tree, performs the `sys.path` bootstrap and the resolved-module assertion, prints both resolved module paths, and reports both counts.
- [ ] [P7-T2] Run the Python leg from the root of THIS worktree as `poetry run python <scratchpad>/corpus_differential.py` and capture its output, recording that exact invocation for the `Command:` field of [P7-T6]. Read the two resolved module paths the script prints first and confirm both are under this worktree; a path under any other checkout invalidates the run entirely and the diff count must not be read. Acceptance: both resolved module paths are under this worktree, the run completes, and it reports `0` rendered-row differences over the discovered artifact set. A non-zero diff count halts the plan; it means Invariant A is broken (AC9).
- [ ] [P7-T3] Write and run the TypeScript leg as a temporary Jest module at `${EXT}/test/lib/pr-context/corpus-differential.tmp.test.ts`, invoked from `${EXT}` with `npm run test:unit -- test/lib/pr-context/corpus-differential.tmp.test.ts`. It performs the same enumeration and comparison over the same artifact set using the real filesystem adapter, and reports the artifact count and the rendered-row diff count. This module is a throwaway harness, is never left in the repository, and is removed at P7-T6. Acceptance: the run reports `0` rendered-row differences (AC9).
- [ ] [P7-T4] Run the cross-runtime comparison: for each artifact containing EXACTLY ONE line-anchored `EXIT_CODE:`, compare the Python and TypeScript rendered rows string-for-string. Artifacts containing two or more `EXIT_CODE:` lines are EXCLUDED and counted separately; the exclusion is attributable to the deferred duplicate-`EXIT_CODE` defect of SC3 and is not a defect of this change (AC10, AC17, risk R3). The Python side of this comparison uses the same bootstrapped script as [P7-T1]; if its resolved-module assertion did not pass, this comparison is invalid and must not be recorded. Acceptance: `0` differences across the single-`EXIT_CODE` set, and the excluded count is captured.
- [ ] [P7-T5] Re-run the AC11 namespace grep post-change: `git grep --untracked -n -E "ExpectedExitCode|expected_exit|expectedExit|expected_nonzero|EXPECTED_EXIT" -- docs/features`. `--untracked` is load-bearing: the evidence artifacts this gate searches are newly created and untracked when the gate runs, so a plain `git grep` would not see them and the gate would pass vacuously; `--untracked` searches tracked and untracked files both, so it is correct whether or not the feature documents have already been committed. Then verify SC7 separately with the anchored key-form search `git grep --untracked -n -E "^[[:space:]]*ExpectedExitCode[[:space:]]*:" -- "${FEATURE}/evidence"`. Acceptance: every match of the first command is under `${FEATURE}/`, and the second command returns zero matches.
- [ ] [P7-T6] Write `${FEATURE}/evidence/other/additive-corpus-parity.<TS>.md` carrying `Timestamp:`, `Command:` (each of the four runs of P7-T2 through P7-T5), `EXIT_CODE:` per run, and an `Output Summary:` recording: the discovered artifact count, the Python-leg diff count, the TypeScript-leg diff count, the cross-runtime diff count over single-`EXIT_CODE` artifacts, the excluded multi-`EXIT_CODE` artifact count with the written statement attributing the exclusion to the deferred duplicate-`EXIT_CODE` defect, the two resolved module paths reported by the Python leg (proving the differential exercised THIS worktree's post-change parser and renderer), and the AC11 grep result (AC9, AC10, AC11, AC17). This artifact MUST NOT contain an `ExpectedExitCode:` line (SC7). Acceptance: all six values, the two resolved module paths, and the attribution statement are present.
- [ ] [P7-T7] Delete the throwaway comparison harnesses: remove `${EXT}/test/lib/pr-context/corpus-differential.tmp.test.ts`, then confirm repository-wide that no harness remains — `git status --porcelain` (unscoped) and `git ls-files --others --exclude-standard` return no path matching `*corpus-differential*` or `*.tmp.test.ts`. Confirm separately that the Python leg written at [P7-T1] lives only in the session scratchpad and was never created under the repository working tree. Record at `${FEATURE}/evidence/qa-gates/throwaway-script-removed.<TS>.md` with the four required fields. Acceptance: zero matching paths anywhere in the repository working tree, and the scratchpad-only location of the Python leg is stated.
- [ ] [P7-T8] Verify Invariant D (AC12): `git diff <baseline-ref> -- scripts/dev_tools/pr_context/verification_evidence.py ${EXT}/src/lib/pr-context/verification-evidence.ts` contains no changed line matching `REQUIRED_FIELDS`, and `git grep -n "REQUIRED_FIELDS" -- scripts/dev_tools/pr_context ${EXT}/src` shows the same three members in both files. Record at `${FEATURE}/evidence/qa-gates/required-fields-unchanged-gate.<TS>.md` with the four required fields. Acceptance: no changed `REQUIRED_FIELDS` line and identical three-member sets.
- [ ] [P7-T9] Verify Invariant C (AC13): `git grep -n "unparseable" -- scripts/dev_tools/pr_context/verification_evidence.py ${EXT}/src/lib/pr-context/verification-evidence.ts` shows the result vocabulary still limited to the three members `pass`, `fail`, and `unparseable` with no fourth member — spelled `Literal["pass", "fail", "unparseable"]` at `verification_evidence.py:29` and `"pass" | "fail" | "unparseable"` at `verification-evidence.ts:34`. Record at `${FEATURE}/evidence/qa-gates/result-vocabulary-closed-gate.<TS>.md` with the four required fields. Acceptance: both declarations carry exactly those three members and no fourth.
- [ ] [P7-T10] Verify Invariant G (AC18): `git grep -n -E "qc_runner_expectations|pytest_expectations" -- scripts/dev_tools/pr_context ${EXT}/src/lib/pr-context`. The command exits `1` with no output on success, which is the passing condition; record the observed exit code faithfully and state the passing condition in prose in `Output Summary:` (SC7 forbids using the new key in this artifact). Record at `${FEATURE}/evidence/qa-gates/no-executor-import-edge-gate.<TS>.md` with the four required fields. Acceptance: zero matches, and the artifact records `EXIT_CODE: 1` with the prose explanation.
- [ ] [P7-T11] Post-change file-size census (AC19): run `(Get-Content <path>).Count` for `scripts/dev_tools/pr_context/verification_evidence.py`, `${EXT}/src/lib/pr-context/verification-evidence.ts`, `${EXT}/src/lib/pr-context/collector-output.ts`, `tests/scripts/dev_tools/pr_context/test_verification_evidence.py`, `tests/scripts/dev_tools/test_collect_pr_context_expected_exit.py`, `${EXT}/test/lib/pr-context/verification-evidence.test.ts`, and `${EXT}/test/lib/pr-context/collector-output.test.ts`. Record at `${FEATURE}/evidence/qa-gates/file-size-census-post-change.<TS>.md` with the four required fields, and restate that the `collector.py` overage recorded at P0-T4 is pre-existing and capped at 5 added lines by P5-T6. Acceptance: every listed file is <= 500 lines.
- [ ] [P7-T12] Verify the coverage configuration is untouched (AC21 second clause): `git diff <baseline-ref> -- pyproject.toml` shows no change to `[tool.coverage.run]` or `[tool.coverage.report]`, and no `omit` or `exclude` entry was added for any production file. Record at `${FEATURE}/evidence/qa-gates/coverage-config-unchanged-gate.<TS>.md` with the four required fields. Acceptance: zero changed lines in either coverage section.

---

### Phase 8 — Final QC Loop

Run the full toolchain for each language in order: formatting, then linting, then type checking, then testing. If any step fails OR modifies any file, restart the loop from the first step of that language and re-capture every artifact for the restarted pass. Every task below is unconditional: the stated command is executed and its result recorded. `EXIT_CODE: SKIPPED` is not a valid outcome for any task in this phase.

- [ ] [P8-T1] Run `poetry run black .` and record at `${FEATURE}/evidence/qa-gates/final-py-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming the reformatted-file count. Acceptance: `EXIT_CODE: 0` and zero files reformatted; if any file is reformatted, restart at P8-T1.
- [ ] [P8-T2] Run `poetry run ruff check .` and record at `${FEATURE}/evidence/qa-gates/final-py-ruff.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and zero diagnostics; if files were auto-fixed, restart at P8-T1.
- [ ] [P8-T3] Run `poetry run pyright` and record at `${FEATURE}/evidence/qa-gates/final-py-pyright.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and zero errors; on failure, fix and restart at P8-T1.
- [ ] [P8-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and record at `${FEATURE}/evidence/qa-gates/final-py-pytest-coverage.<TS>.md` with the four required fields. `Output Summary:` MUST carry numeric passed/failed/skipped counts, overall line coverage percent, overall branch coverage percent, and the per-file line and branch coverage of `scripts/dev_tools/pr_context/verification_evidence.py` and `scripts/dev_tools/pr_context/collector.py`, plus the `Missing` column contents for `verification_evidence.py`. Note that the `term-missing` table's `Cover` column is a COMBINED statement-plus-branch metric and is neither the line nor the branch percentage; derive the separate line and branch percentages (overall and per file) from the LCOV report that the `addopts` at `pyproject.toml:116` already writes to `artifacts/python/lcov.info` (`LH/LF` for lines, `BRH/BRF` for branches), or equivalently from `poetry run coverage json -o -` against `artifacts/.coverage`. Record the derivation source in `Command:`. The `Missing` column is read from the `term-missing` table itself. Acceptance: `EXIT_CODE: 0`, overall line coverage >= 85%, overall branch coverage >= 75%, and no uncovered added or changed line in `verification_evidence.py` (AC21). No placeholder value is permitted.
- [ ] [P8-T5] From `${EXT}`, run `npm run format` and record at `${FEATURE}/evidence/qa-gates/final-ts-format.<TS>.md` with the four required fields; `Output Summary:` names the count of files Prettier rewrote. Acceptance: `EXIT_CODE: 0` and zero files rewritten; if any file is rewritten, restart at P8-T5.
- [ ] [P8-T6] From `${EXT}`, run `npm run lint` and record at `${FEATURE}/evidence/qa-gates/final-ts-lint.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and zero errors and zero warnings; on failure, fix and restart at P8-T5.
- [ ] [P8-T7] From `${EXT}`, run `npm run typecheck` and record at `${FEATURE}/evidence/qa-gates/final-ts-typecheck.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and zero `tsc` errors; on failure, fix and restart at P8-T5.
- [ ] [P8-T8] From `${EXT}`, run `npm run test:unit` and record at `${FEATURE}/evidence/qa-gates/final-ts-test-unit.<TS>.md` with the four required fields; `Output Summary:` carries suite and test pass/fail counts. Acceptance: `EXIT_CODE: 0` and zero failures; on failure, fix and restart at P8-T5.
- [ ] [P8-T9] From `${EXT}`, run `npm run test:coverage -- --coverageReporters=text --coverageReporters=text-summary --coverageReporters=lcov` and record at `${FEATURE}/evidence/qa-gates/final-ts-test-coverage.<TS>.md` with the four required fields, recording the exact invocation in `Command:`. The added `text` reporter is load-bearing (the script's configured reporters produce no per-file rows). `Output Summary:` MUST carry numeric overall line coverage percent, overall branch coverage percent, the per-file line and branch coverage of `src/lib/pr-context/verification-evidence.ts` and `src/lib/pr-context/collector-output.ts`, and the `Uncovered Line #s` entry for `verification-evidence.ts` from the `text` table (or the equivalent uncovered-line set derived from `${EXT}/coverage/lcov.info`). Acceptance: `EXIT_CODE: 0`, overall line coverage >= 85%, overall branch coverage >= 75%, and no uncovered added or changed line in `verification-evidence.ts` (AC22). No placeholder value is permitted.
- [ ] [P8-T10] Record the single-clean-pass attestation for AC24 at `${FEATURE}/evidence/qa-gates/final-qc-single-clean-pass.<TS>.md`, listing each of the eight commands from P8-T1 through P8-T8 in execution order with its `EXIT_CODE:` from the FINAL uninterrupted pass, and stating that no file was modified by either formatter during that pass. Acceptance: all eight exit codes are `0` and the no-modification statement is present.
- [ ] [P8-T11] Confirm AC25 after the final pass: `git diff --numstat <baseline-ref> -- tests/scripts/dev_tools/test_collect_pr_context.py tests/scripts/dev_tools/test_collect_pr_context_part4.py` shows zero changed lines, and the nine pre-existing tests in `${EXT}/test/lib/pr-context/verification-evidence.test.ts` and the four pre-existing tests in `describe("renderVerificationEvidenceSection")` pass with no edits to their bodies. Record at `${FEATURE}/evidence/qa-gates/existing-tests-unmodified-final.<TS>.md` with the four required fields. Acceptance: zero changed lines in the two Python modules and thirteen pre-existing TypeScript tests passing unedited.

---

### Phase 9 — Coverage Delta, Acceptance Criteria, and Closeout

- [ ] [P9-T1] Write the coverage delta and threshold verification at `${FEATURE}/evidence/qa-gates/coverage-delta.<TS>.md`, reporting for BOTH languages: baseline overall line and branch coverage (from `${FEATURE}/evidence/baseline/py-pytest-coverage.<TS>.md` and `${FEATURE}/evidence/baseline/ts-test-coverage.<TS>.md`), post-change overall line and branch coverage (from `${FEATURE}/evidence/qa-gates/final-py-pytest-coverage.<TS>.md` and `${FEATURE}/evidence/qa-gates/final-ts-test-coverage.<TS>.md`), the delta for each, and new/changed-code coverage stated as the covered fraction of the added or changed lines in `scripts/dev_tools/pr_context/verification_evidence.py`, `scripts/dev_tools/pr_context/collector.py`, `${EXT}/src/lib/pr-context/verification-evidence.ts`, and `${EXT}/src/lib/pr-context/collector-output.ts`. Acceptance: every value is numeric; overall line >= 85% and branch >= 75% in both languages; new/changed-code coverage is 100% for the four production files; and no metric regressed against baseline. A missing or non-numeric value makes the outcome remediation-required, never PASS.
- [ ] [P9-T2] Check off delivered acceptance criteria in `${FEATURE}/spec.md` per the `acceptance-criteria-tracking` skill. Under `full-bug`, `spec.md` is the SOLE acceptance-criteria source; `user-story.md` is intentionally absent and must not be created. Change only `- [ ]` to `- [x]` for each of AC1 through AC25 that is backed by a named evidence artifact from this plan; leave any unmet criterion unchecked and document the gap. AC8 is checked off on the basis of its own named verification method — both transcribed fixture tables passing with matching shape identifiers in matching order — together with the shape-by-shape cross-runtime comparison for shapes 01-05 and 07-11 recorded at [P4-T12]. Record in `${FEATURE}/evidence/other/ac-status-summary.<TS>.md` (the artifact [P9-T3] writes) that AC8's prose clause "for each of the eleven shapes ... agree" does not hold for shape-06, because `spec.md`'s own shape table row 6 defines that shape as pinning each runtime's EXISTING duplicate-`EXIT_CODE` precedence, whose convergence SC3 defers; the divergence is pre-existing and is not introduced by this change. Acceptance: every checked criterion names its backing evidence artifact in `${FEATURE}/evidence/other/ac-status-summary.<TS>.md`, and no criterion text is altered.
- [ ] [P9-T3] Write the AC status summary at `${FEATURE}/evidence/other/ac-status-summary.<TS>.md` in the format required by the `acceptance-criteria-tracking` skill: source file, total AC items, checked-off count, remaining count, and the text of any remaining item. Acceptance: the artifact exists, names `${FEATURE}/spec.md` as the source, and reports a total of 25.
- [ ] [P9-T4] Verify the evidence-location invariant across everything this plan produced: `git status --porcelain` and a directory listing confirm every artifact created by this plan lives under `${FEATURE}/evidence/{baseline,regression-testing,qa-gates,other}/`, and that no artifact was written to `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/coverage/`, `artifacts/regression-testing/`, `artifacts/post-change/`, or `artifacts/evidence/`. Record at `${FEATURE}/evidence/qa-gates/evidence-location-gate.<TS>.md` with the four required fields. Acceptance: zero artifacts outside the canonical scheme.
- [ ] [P9-T5] Verify SC7 held across the whole evidence set: `git grep --untracked -n -E "^[[:space:]]*ExpectedExitCode[[:space:]]*:" -- "${FEATURE}/evidence"` returns no match. The `--untracked` flag is load-bearing: the evidence artifacts this gate searches are newly created and untracked when the gate runs, so a plain `git grep` would not see them and the gate could not fail; `--untracked` searches tracked and untracked files both, so it is correct whether or not the feature documents have already been committed. The pattern is anchored to the parseable key form (the text before the first colon stripping to exactly `ExpectedExitCode`, per `verification_evidence.py:105-107`) because [P0-T6] and [P7-T6] are required to name the token in prose and a bare-token search would falsely match them. Record at `${FEATURE}/evidence/qa-gates/evidence-key-prohibition-gate.<TS>.md` with the four required fields; the grep exits `1` on zero matches, which is the passing condition, stated in prose in `Output Summary:`. Acceptance: zero matches, and the artifact records both the `--untracked` flag and the anchored pattern.
- [ ] [P9-T6] Update `${FEATURE}/spec.md` "Rollout & Follow-up" and `${FEATURE}/issue.md` with the delivered outcome, the recorded corpus counts from P7-T6, and an explicit restatement that the duplicate-`EXIT_CODE` precedence divergence remains unfixed and is to be promoted separately via the potential-to-issue path. Mirror any GitHub issue update to `${FEATURE}/evidence/issue-updates/issue-485.<TS>.md` per the evidence conventions. BEFORE editing `${FEATURE}/spec.md` in this task, copy it verbatim to `<scratchpad>/spec.before-p9t6.md`; the snapshot is taken AFTER the [P9-T2] check-offs so that those checkbox flips fall outside the comparison below. In the same edit, resolve the pre-existing internal inconsistency in `${FEATURE}/spec.md` about shape 6: correct the sentence at `spec.md:177` (Invariant B mechanism 1) and the sentence at `spec.md:399` (Test Strategy) so that neither asserts the exactly-one-`EXIT_CODE:` property of all eleven shapes; each corrected sentence must scope that property to shapes 01-05 and 07-11 and must name shape 06 as the deliberate two-`EXIT_CODE:` exception whose expected record is runtime-specific because SC3 defers convergence of the duplicate-`EXIT_CODE` precedence. The `spec.md:177` sentence continues with the trailing clause "so the deferred duplicate-`EXIT_CODE` divergence cannot confound the comparison"; that clause is FALSE when read over all eleven shapes and must be either scoped to the same ten shapes or replaced, not carried over unchanged. The correction is confined to those two sentences: it must not alter the text of any acceptance criterion and must not change any row of the eleven-shape table at `spec.md:401-413`. Verify that confinement with `git diff --no-index -- <scratchpad>/spec.before-p9t6.md ${FEATURE}/spec.md`. A plain `git diff` CANNOT be used here: `${FEATURE}/spec.md` is UNTRACKED at authoring time and, once the feature folder is committed, a diff against the merge-base baseline would report every line of the file — including the whole shape table — as added; either way the gate could not discriminate. `git diff --no-index` compares the two named paths without consulting the index and exits `1` when they differ, which is the EXPECTED outcome here. Record it at `${FEATURE}/evidence/qa-gates/spec-shape6-correction-confinement.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the changed-line numbers and the changed-line count and stating that no changed line falls inside `spec.md:401-413` or inside any `- [ ]` / `- [x]` acceptance-criterion item; report line numbers and counts only and do NOT paste `spec.md` context lines into the artifact, so the artifact cannot acquire a parseable `ExpectedExitCode:` key line (SC7). Acceptance: both documents record the outcome and the deferred defect; neither `spec.md` location still asserts the exactly-one-`EXIT_CODE:` property of all eleven shapes and both name shape 06 as the exception; the `spec.md:177` trailing clause no longer claims over all eleven shapes that the deferred divergence cannot confound the comparison; the `git diff --no-index` run shows changed lines only within those two sentences, zero changed lines within `spec.md:401-413`, and zero changed lines within any acceptance-criterion item; the confinement artifact exists with all four required fields; and the issue-update mirror exists if any GitHub update was posted.
