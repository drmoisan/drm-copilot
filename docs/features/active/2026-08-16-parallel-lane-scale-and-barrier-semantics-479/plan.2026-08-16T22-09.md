# 2026-08-16-parallel-lane-scale-and-barrier-semantics (Plan)

- **Issue:** #479
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-16T23-58
- **Status:** Ready for preflight (revision 3 — preflight delta applied in place; same file path)
- **Version:** 3.0
- **Work Mode:** full-bug — `spec.md` is the sole acceptance-criteria source (AC1–AC41); `user-story.md` is correctly absent and is not a blocker.
- **Branch:** `bug/parallel-lane-scale-and-barrier-semantics-479`

## Normative Context

- `<FEATURE>` = `docs/features/active/2026-08-16-parallel-lane-scale-and-barrier-semantics-479`. All evidence paths in this plan resolve under `<FEATURE>/evidence/<kind>/`. No `artifacts/`-rooted evidence path is used anywhere in this plan.
- Requirements sources: `<FEATURE>/spec.md` (AC1–AC41, sole AC source), `<FEATURE>/research/2026-08-16T23-00-lane-scale-and-barrier-semantics-research.md` (authoritative change inventories with `file:line` citations), `<FEATURE>/issue.md`, `.claude/rules/parallel-orchestration.md`.
- Anchor re-verification: research `file:line` citations are current as of branch creation. Every edit task below requires the executor to re-verify the cited anchor text before editing and to record drift, if any, in the task's evidence artifact. Spec ACs are referenced by AC number, never by spec line number.
- **Search-command rule (revision 2):** `.claude/worktrees/` is gitignored (`.gitignore:21`) but holds a live `git worktree` (`.claude/worktrees/agent-afc9f4fd25ec235a5/`) containing a full second copy of the repository. Any recursive filesystem search (`grep -r`) rooted at a directory that can include `.claude/worktrees/`, `node_modules`, `.venv`, `out/`, or `dist/` is therefore invalid as an acceptance gate. Every acceptance search in this plan uses `git grep` with explicit pathspecs, which searches tracked files only.
- **Fail-closed evidence rule:** if any required baseline artifact, final-QC artifact, or coverage-comparison artifact is missing or lacks required numeric values, the outcome is remediation-required, never PASS.
- **Expect-fail statement:** this plan contains no `[expect-fail]` task. Every new or changed test is authored in the same phase as, and sequenced after, the production change it exercises, so no test is expected to fail at any planned execution point. The toolchain restart-on-failure rule applies without carve-outs.
- **Toolchain restart rule:** in Phase 7, if any step fails or changes files, restart that language's loop from its formatting step until a single clean pass completes. `EXIT_CODE: SKIPPED` is not a valid outcome for any command task in this plan.
- **File-size headroom facts (measured during planning):** `scripts/dev_tools/parallel_mutation_protocol.py` = 499 lines (compaction to `<= 480` required before extension, P1-T2); `tests/scripts/dev_tools/test_parallel_mutation_protocol_properties.py` = 500 lines (at the ceiling; behavior-preserving compaction to `<= 494` required, P1-T3, before P1-T4 adds one `highest_pinned_cohort=` argument line at each of the four exploded `recolor_unstarted(` call sites `:156`, `:255`, `:358`, `:488` under black at line-length 88); `tests/scripts/dev_tools/test_parallel_manifest_contract.py` = 497 lines (M8 tests therefore land in a new sibling module, P3-T5); `tests/scripts/dev_tools/test_validate_parallel_planner_state.py` = 499 lines with NO existing in-range accept test and NO recoverable headroom (its three multi-line parametrize blocks at `:392-409`, `:421-427`, `:464-471` and its three multi-line strings at `:1-11`, `:59-65`, `:432-436` cannot be condensed under black's 88-character limit without deleting content) — the parent receives only net-zero swaps and the new accept test lands in the new sibling module `tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py` (P2-T6); `scripts/dev_tools/_parallel_state_common.py` = 495 lines (P2-T2's docstring edit is net-zero; no later phase may consume the remaining 5 lines of headroom); `scripts/dev_tools/parallel_manifest_contract.py` = 312; `.claude/lib/bash/parallel-manifest-validate.sh` = 187; `.claude/lib/bash/parallel-items-validate.sh` = 244; `tests/scripts/dev_tools/test_parallel_mutation_recolor.py` = 311; `tests/shell/parallel_manifest_validate.bats` = 165.
- **Command inventory (verified against the repository):** Python — `poetry run black .` / `poetry run black --check .`, `poetry run ruff check .`, `poetry run pyright`, `poetry run pytest --cov --cov-branch --cov-report=term-missing`. TypeScript (cwd `extensions/drm-copilot`) — `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` (check mode; the package `format` script is write-mode), `npm run lint`, `npm run typecheck`, `npm run test:coverage` (the extension has no `test:unit:coverage` script; per-file values are read from `coverage/lcov.info` because the script's reporters are `lcov` + `text-summary`). Bash — no local runner; verification is CI dispatch of `.github/workflows/_shell-coverage.yml` (`gh workflow run _shell-coverage.yml --ref bug/parallel-lane-scale-and-barrier-semantics-479`, then `gh run watch` / `gh run view --json conclusion,url,headSha`), which runs the shell-QC `check` step (shfmt + shellcheck) and the bats/kcov coverage step printing `Bash coverage (lines): NN.N%`. PowerShell — MCP tools `mcp__drm-copilot__run_poshqc_format`, `mcp__drm-copilot__run_poshqc_analyze`, `mcp__drm-copilot__run_poshqc_test` (these take scan folders, not `.ps1` file paths).
- **Languages per defect:** D1 Markdown + Python; D2 Markdown + Python + TypeScript + bash + JSON fixtures; D3 Markdown + Python + bash; D4 Markdown only. PowerShell is touched by no defect; the Pester suite runs in Phase 7 as a regression gate only.
- **Mirror rule:** every phase that edits a mirrored `.claude` file re-syncs that file byte-identically to `extensions/drm-copilot/resources/claude-customizations/.claude/` inside the same phase and re-runs `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`, so each phase ends with the mirror contract green. `docs/features/templates/parallel/parallel-status.md` is not mirrored and no mirror is created for it. No new `.claude` file is created, so `pack-manifests/core.json` is untouched.
- **Out-of-range exemplar value:** every migrated out-of-range exemplar (`9` and `12`) becomes the single canonical value `33` (ceiling + 1), and every affected expected-error string reads `... from 1 through 32; found: 33.`. `100` stays an invalid exemplar where it already exists.
- **Phase-green statement:** Phases 1, 2, 4, and 5 end green on every locally runnable suite. Phases 3 and 4 modify bash sources, bats files, and shared JSON fixtures whose bash-lane execution cannot run locally on this Windows host; the Python parity lane (`tests/scripts/dev_tools/test_parallel_manifest_bash_parity.py`) validates locally that the shared fixtures agree with the Python authority in the same phase — it invokes no bash, so it covers fixture-versus-Python agreement only and leaves the bash implementation itself locally unverified — and full bash-lane verification (shfmt, shellcheck, bats, kcov) is restored to green by the Phase 7 CI dispatch (P7-T12). This is the only respect in which an intermediate phase is not fully verified, and it is a verification deferral, not a red suite.

### Phase 0 — Policy Reads and Baseline Capture

- [ ] [P0-T1] Read the policy files in the mandated order: (1) `.github/copilot-instructions.md`, (2) `.github/instructions/general-code-change.instructions.md`, (3) `.github/instructions/general-unit-test.instructions.md`, (4) language-specific for the in-scope languages: `.github/instructions/python-code-change.instructions.md`, `.github/instructions/python-unit-test.instructions.md`, `.github/instructions/typescript-code-change.instructions.md`, `.github/instructions/typescript-unit-test.instructions.md`, `.github/instructions/powershell-code-change.instructions.md`, `.github/instructions/powershell-unit-test.instructions.md`; then `.claude/rules/shell.md` and `.claude/rules/parallel-orchestration.md` (the prose contract being amended). Write `<FEATURE>/evidence/baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
  - Acceptance: artifact exists with all three required fields and lists every file above.
- [ ] [P0-T2] Record the git baseline: `git rev-parse HEAD`, `git merge-base origin/main HEAD`, `git status --porcelain --untracked-files=no`. Write `<FEATURE>/evidence/baseline/git-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch name, HEAD sha, merge-base sha, tracked-file cleanliness).
  - Acceptance: artifact exists; no tracked-file modifications are reported.
- [ ] [P0-T3] Python format baseline: run `poetry run black --check .` from repo root. Write `<FEATURE>/evidence/baseline/python-format-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: artifact exists with all four fields; exit code recorded verbatim.
- [ ] [P0-T4] Python lint baseline: run `poetry run ruff check .`. Write `<FEATURE>/evidence/baseline/python-lint-baseline.<ts>.md` with the four required fields.
  - Acceptance: artifact exists with all four fields.
- [ ] [P0-T5] Python type-check baseline: run `poetry run pyright`. Write `<FEATURE>/evidence/baseline/python-typecheck-baseline.<ts>.md` with the four required fields.
  - Acceptance: artifact exists with all four fields.
- [ ] [P0-T6] Python test + coverage baseline: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `<FEATURE>/evidence/baseline/python-test-baseline.<ts>.md` with the four required fields; `Output Summary:` MUST include the numeric total line-coverage and branch-coverage percentages and the pass/fail/test counts, plus the current per-module line values for `scripts/dev_tools/parallel_mutation_protocol.py` and `scripts/dev_tools/parallel_manifest_contract.py`.
  - Acceptance: artifact exists; numeric line and branch coverage values present (no placeholders).
- [ ] [P0-T7] TypeScript format baseline: from `extensions/drm-copilot`, run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`. Write `<FEATURE>/evidence/baseline/ts-format-baseline.<ts>.md` with the four required fields.
  - Acceptance: artifact exists with all four fields.
- [ ] [P0-T8] TypeScript lint baseline: from `extensions/drm-copilot`, run `npm run lint`. Write `<FEATURE>/evidence/baseline/ts-lint-baseline.<ts>.md` with the four required fields.
  - Acceptance: artifact exists with all four fields.
- [ ] [P0-T9] TypeScript type-check baseline: from `extensions/drm-copilot`, run `npm run typecheck`. Write `<FEATURE>/evidence/baseline/ts-typecheck-baseline.<ts>.md` with the four required fields.
  - Acceptance: artifact exists with all four fields.
- [ ] [P0-T10] TypeScript test + coverage baseline: from `extensions/drm-copilot`, run `npm run test:coverage`. Write `<FEATURE>/evidence/baseline/ts-test-baseline.<ts>.md` with the four required fields; `Output Summary:` MUST include the numeric text-summary line/branch totals plus the per-file line and branch values read from `extensions/drm-copilot/coverage/lcov.info` for `src/lib/validate/parallel-orchestrator-state-core.ts` and `src/lib/validate/parallel-planner-state-core.ts`.
  - Acceptance: artifact exists; numeric totals and the two per-file values present.
- [ ] [P0-T11] Bash baseline via CI: run `git push -u origin HEAD`, then `gh workflow run _shell-coverage.yml --ref bug/parallel-lane-scale-and-barrier-semantics-479`, poll with `gh run watch` / `gh run view --json conclusion,url,headSha`. Write `<FEATURE>/evidence/baseline/shell-baseline.<ts>.md` with the four required fields; `Output Summary:` MUST include the run URL, conclusion, headSha, and the numeric `Bash coverage (lines): NN.N%` log line. Branch coverage: `N/A — kcov does not measure branch coverage; PowerShell/bash are exempt from the branch threshold only`.
  - Acceptance: artifact exists; run conclusion and numeric line-coverage value recorded.
- [ ] [P0-T12] PowerShell regression-gate baseline: run `mcp__drm-copilot__run_poshqc_test` over the repository's standard PowerShell scan folders. Write `<FEATURE>/evidence/baseline/powershell-test-baseline.<ts>.md` with the four required fields; `Output Summary:` MUST include Pester pass/fail counts and the numeric line-coverage headline. Branch coverage: `N/A — Pester does not measure branch coverage`.
  - Acceptance: artifact exists; numeric line coverage and test counts present.

### Phase 1 — D1: Per-Edge Barrier Prose, Test Pin, and F6 Offset Generalization

All D1 prose, the `COHORT_BARRIER_FRAGMENTS` pin update, and the mirror re-sync land inside this single phase; the surface-contract suite is run only after both the skill edit (P1-T7) and the pin edit (P1-T14) are complete, so the phase ends green.

- [ ] [P1-T1] Re-verify the D1 anchors named in research section D1.1 before any edit: `.claude/skills/parallel-orchestrate/SKILL.md:118-123, :157-159, :311-314, :476, :505-510, :100-103, :609, :617`, `.claude/agents/parallel-orchestrator.md:190-193`, `.claude/skills/parallel-add/SKILL.md:81, :89-90`, `.claude/skills/parallel-remove/SKILL.md:81-82, :87-90`, `docs/features/templates/parallel/parallel-status.md:32`, `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py:154-159, :169-176, :184-192`, `scripts/dev_tools/parallel_mutation_protocol.py:253-262, :321-327`, `scripts/dev_tools/_parallel_mutation_models.py:301-311`. Record verified/drifted status per anchor in `<FEATURE>/evidence/other/d1-anchor-verification.<ts>.md`.
  - Acceptance: artifact lists every anchor above with a verified or drifted-to-`<new location>` disposition.
- [ ] [P1-T2] Behavior-preserving compaction of `scripts/dev_tools/parallel_mutation_protocol.py` (currently 499 lines) to `<= 480` lines, achieved by condensing the global-barrier justification narrative in the `recolor_unstarted` docstring/comment region (:253-262, :321-323) that P1-T4 rewrites anyway — no function, class, or mandated docstring section may be removed, and no behavior may change. The compaction target must be measured AFTER a dry accounting of P1-T4's additions: a new signature line, a mandated `Args:` entry for `highest_pinned_cohort` (required by `.claude/rules/self-explanatory-code-commenting.md`), and an updated offset comment — realistically 8-12 added lines.
  - Acceptance: `grep -c ^ scripts/dev_tools/parallel_mutation_protocol.py` reports `<= 480`; the dry accounting of P1-T4's additions is recorded in the task evidence and fits the remaining headroom; `poetry run pytest tests/scripts/dev_tools/test_parallel_mutation_recolor.py` passes with zero test-file edits; `poetry run black --check .`, `poetry run ruff check .`, and `poetry run pyright` are clean.
- [ ] [P1-T3] Behavior-preserving compaction of `tests/scripts/dev_tools/test_parallel_mutation_protocol_properties.py` (currently 500 lines, at the ceiling) to `<= 494` lines, creating headroom for the four `highest_pinned_cohort=` argument lines the next task (P1-T4) adds at the exploded `recolor_unstarted(` call sites `:156`, `:255`, `:358`, `:488` (each carries a magic trailing comma, so black at line-length 88 adds one line per site). No test function, parametrize case, property strategy, or assertion may be removed or weakened; compaction is limited to condensing the module's 112 lines of docstring narrative and 39 comment lines.
  - Acceptance: `grep -c ^ tests/scripts/dev_tools/test_parallel_mutation_protocol_properties.py` reports `<= 494`; `poetry run pytest tests/scripts/dev_tools/test_parallel_mutation_protocol_properties.py` passes with the same collected-test count recorded immediately before the edit in the task evidence; `poetry run black --check .` and `poetry run ruff check .` are clean.
- [ ] [P1-T4] Generalize the F6 pinned-barrier offset in `scripts/dev_tools/parallel_mutation_protocol.py`: add a required keyword-only parameter `highest_pinned_cohort: int` to `recolor_unstarted` and compute `cohort_offset = highest_pinned_cohort + 1 if crosses_pinned else current_cohort` (replacing `current_cohort + 1`), keeping the shift uniform to preserve injectivity. Update the docstrings in `parallel_mutation_protocol.py` (the former :253-262 region) and `scripts/dev_tools/_parallel_mutation_models.py:301-311` so neither cites the global increment rule; justify the offset by the per-edge rule (shift above the highest current-generation cohort index occupied by any pinned item). Update every in-repo caller found by `git grep -n "recolor_unstarted(" -- scripts tests` (signature-only updates in existing tests are permitted; no behavioral assertion may be weakened). At every existing call site the behavior-preserving substitution value for the new argument is the SAME value that call already passes as `current_cohort` (i.e., add `highest_pinned_cohort=<that call's existing current_cohort argument>`); this keeps every existing test on the identity path `highest_pinned_cohort == current_cohort` and does not break AC13's identity property.
  - Acceptance (AC11): the offset expression uses `highest_pinned_cohort`; `grep -n "current_cohort + 1" scripts/dev_tools/parallel_mutation_protocol.py` returns zero matches; neither docstring cites the global rule; both files `<= 500` lines; `poetry run pyright` clean.
- [ ] [P1-T5] Add the multi-cohort-pinned regression test to `tests/scripts/dev_tools/test_parallel_mutation_recolor.py` (311 lines; headroom confirmed): with pinned items occupying current-generation indices {0, 1} and `current_cohort == 0`, a deferred candidate conflicting with the index-1 pinned item lands at an index strictly greater than 1.
  - Acceptance (AC12): the new test exists, passes, and the file remains `<= 500` lines.
- [ ] [P1-T6] Add the single-frontier identity regression test to `tests/scripts/dev_tools/test_parallel_mutation_recolor.py`: when every pinned item occupies `current_cohort` (`highest_pinned_cohort == current_cohort`), the generalized offset produces the same recoloring as the previous `current_cohort + 1` behavior.
  - Acceptance (AC13): the new test exists and passes; all pre-existing recolor tests pass with signature-only edits (verified by reviewing the test-file diff for absence of assertion changes).
- [ ] [P1-T7] Replace the global-barrier definition in `.claude/skills/parallel-orchestrate/SKILL.md` (the `## Cohort Barrier and Max-Concurrency Slot Filling` section, anchor :118-123) with the per-edge rule, containing verbatim: "An item may start only when every conflicting neighbour (`conflict_edges[]`) that sits in a strictly prior current-generation cohort has `merge_status` of `merged` or `worktree_removed`", plus the `ci_green` exclusion sentence and the same-cohort/later-cohort/no-neighbour non-blocking clauses. Leave the same-cohort merge-order text at :100-103 byte-unchanged, and keep the sentence "Every cohort transition, meaning every `current_cohort` increment" verbatim wherever it appears in the skill.
  - Acceptance (AC1, AC7 anchor, AC9 text half): the quoted per-edge sentence is present verbatim; `git diff` shows no edit inside the :100-103 same-cohort block; `grep -F "Every cohort transition, meaning every \`current_cohort\` increment" .claude/skills/parallel-orchestrate/SKILL.md` matches.
- [ ] [P1-T8] Rewrite the dependent restatements in `.claude/skills/parallel-orchestrate/SKILL.md`: the fetch instruction (anchor :157-159) becomes per launch batch, not per cohort launch; the blocked-item scope (anchor :311-314) narrows to the item's conflicting later-cohort neighbours; the F6 narrative (anchors `:476` and `:505-510` — `:476` is a distinct surviving single-frontier claim, "third scheduling input — the current cohort index `current_cohort` that the pinned items occupy") is made consistent with the P1-T4 offset generalization. The `:311-314` rewrite MUST preserve verbatim the fragment ``terminal `merge_status: blocked_ci_loop_limit` ``, which `MERGE_CONFLICT_FRAGMENTS` (`tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py:169-176`) pins within `## Per-Item Merge-Conflict Handling` at `:311`; only the barrier-scope clause on `:312-314` is reworded.
  - Acceptance (AC3 skill half): each of the four sites carries the reworded text and no global-barrier wording; no section heading is added, removed, or moved; `git grep -c` for the fragment ``terminal `merge_status: blocked_ci_loop_limit` `` in `.claude/skills/parallel-orchestrate/SKILL.md` returns `1`; `git diff` shows no edit to `MERGE_CONFLICT_FRAGMENTS`; `git grep -n "pinned items occupy" -- .claude/skills/parallel-orchestrate/SKILL.md` returns zero matches (scoped to this task's only edited file — the remaining occurrence at `.claude/skills/parallel-remove/SKILL.md:88` is P1-T11's to remove, and the repository-wide zero-match gate is P1-T11's acceptance).
- [ ] [P1-T9] Add to `.claude/skills/parallel-orchestrate/SKILL.md`: (a) the `current_cohort` progress-indicator redefinition (lowest current-generation cohort index still containing a non-terminal, non-withdrawn item; updated only on durable confirmation via `git worktree list --porcelain`, `git branch`, `gh pr view --json state,mergedAt,headRefOid`; gating nothing); (b) the two layers' differing fail-closed behavior (Layer 1 target-side denials: missing/unparseable checkpoint, unresolved feature-folder token, missing `items[]` record, target without current-generation assignment, missing neighbour record, missing neighbour `merge_status`; versus its neighbour-side skip for a neighbour without current-generation assignment; Layer 2's three readings: structural same-cohort — which Layer 1 has no counterpart for — status, and temporal with status-only degradation); (c) the safety argument anchored to the unchanged :100-103 text; (d) the availability argument (a `blocked_ci_loop_limit`/`blocked_drift` item holds only its own conflict component's tail while unrelated lanes advance).
  - Acceptance (AC5 skill part, AC6, AC7, AC8): file inspection finds all four elements; no heading changes.
- [ ] [P1-T10] Rewrite the restatement in `.claude/agents/parallel-orchestrator.md` (anchor :190-193) to the same per-edge rule with no global-barrier wording, and state the `current_cohort` progress-indicator semantics.
  - Acceptance (AC2, AC5 agent part): the per-edge sentence is present; `grep -n "only after every cohort" .claude/agents/parallel-orchestrator.md` returns zero matches.
- [ ] [P1-T11] Correct the single-frontier phrasing in `.claude/skills/parallel-add/SKILL.md` (anchor :89-90 — the phrase wraps across these two lines, so a whole-phrase grep does not match it) and `.claude/skills/parallel-remove/SKILL.md` (anchor :87-90, single-line match at :88): remove or correct "the index the pinned items occupy" so both skills are consistent with in-flight items spanning multiple cohort indices and with the `highest_pinned_cohort` input to `recolor_unstarted`. The reworded text must not use the phrase "pinned items" in either file, so the acceptance grep below is a real detector. This task removes the last remaining occurrence of "pinned items occupy" in `.claude` (the `parallel-orchestrate/SKILL.md:476` occurrence was removed by P1-T8).
  - Acceptance (AC3 add/remove half): both sites reworded; `git grep -n "pinned items" -- .claude/skills/parallel-add/SKILL.md .claude/skills/parallel-remove/SKILL.md` returns zero matches (the shorter token is used deliberately: the whole phrase wraps across `parallel-add/SKILL.md:89-90` and would evade a whole-phrase grep); the repository-wide `git grep -n "pinned items occupy" -- .claude` returns zero matches.
- [ ] [P1-T12] Update every documented `recolor_unstarted` call signature to carry the new required keyword-only argument at `.claude/skills/parallel-add/SKILL.md:81`, `.claude/skills/parallel-remove/SKILL.md:81-82`, and `.claude/skills/parallel-orchestrate/SKILL.md:617` (lead-in at `:609`), including the surrounding prose that describes the call: the argument-count phrase at `.claude/skills/parallel-orchestrate/SKILL.md:616` ("Its call shape is the five-argument form") becomes "the six-argument form", and the keyword-only sentence at `:618` is updated to name BOTH `current_cohort` and `highest_pinned_cohort` as required and keyword-only. Each call becomes `recolor_unstarted(unstarted_items, conflict_edges, pinned, current_generation, current_cohort=current_cohort, highest_pinned_cohort=highest_pinned_cohort)`, and each site states how `highest_pinned_cohort` is derived from re-verified durable state (the highest current-generation cohort index occupied by any pinned item — singular "any pinned item"; the plural token "pinned items" MUST NOT be reintroduced into `parallel-add/SKILL.md` or `parallel-remove/SKILL.md`, or the P1-T11 zero-match grep gate is re-broken). `.claude/skills/parallel-close/SKILL.md:65` names the function only to forbid calling it and is NOT edited.
  - Acceptance (AC3 extension): `git grep -n "recolor_unstarted(" -- .claude` shows every call site carrying `highest_pinned_cohort`; `git grep -n "five-argument" -- .claude` returns zero matches; `git grep -n "pinned items" -- .claude/skills/parallel-add/SKILL.md .claude/skills/parallel-remove/SKILL.md` still returns zero matches after this task's edits; `.claude/skills/parallel-close/SKILL.md` is absent from `git diff --name-only`.
- [ ] [P1-T13] Reword `docs/features/templates/parallel/parallel-status.md` (anchor :32) to the progress-indicator semantics of `current_cohort`. This file is not mirrored; create no mirror for it. Make no edit to invariant 14 in `.claude/rules/parallel-orchestration.md`.
  - Acceptance (AC5 template part): the template line carries progress-indicator wording; `git diff -- .claude/rules/parallel-orchestration.md` shows no invariant-14 edit in this phase.
- [ ] [P1-T14] Update `COHORT_BARRIER_FRAGMENTS` in `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py` (anchor :154-159): remove the global sentence fragment and pin the new per-edge sentence exactly as written into the skill by P1-T7; keep the existing `"max_concurrency"` and `"ascending item-key order"` members. Leave `BOUNDARIES_REGENERATION_FRAGMENTS` (anchor :184-192) unmodified.
  - Acceptance (AC10 pin half, AC9 pin half): the tuple pins the per-edge sentence; `git diff` shows no edit to `BOUNDARIES_REGENERATION_FRAGMENTS`.
- [ ] [P1-T15] Re-sync the four mirrored files touched in this phase byte-identically: copy `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/agents/parallel-orchestrator.md`, `.claude/skills/parallel-add/SKILL.md`, `.claude/skills/parallel-remove/SKILL.md` (this set covers every file edited by P1-T12) to their counterparts under `extensions/drm-copilot/resources/claude-customizations/.claude/`, then run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`.
  - Acceptance (AC36 partial): the mirror contract test passes; per-pair byte-identity confirmed by the test.
- [ ] [P1-T16] Run the Phase 1 gate suite: `poetry run pytest tests/scripts/dev_tools` (covers the surface-contract suite including `test_orchestrate_skill_section_states_its_required_obligations`, the recolor suite, and the Layer-2 barrier suites, all unmodified except as authorized above). Write `<FEATURE>/evidence/qa-gates/p1-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts).
  - Acceptance (AC9, AC10, AC12, AC13; AC14 Layer-2 half): exit code 0; artifact recorded.
- [ ] [P1-T17] Run the AC4 grep gate: `git grep -n "only after every cohort" -- .claude docs/features/templates` must return zero matches (exit code 1). Run the positive control `git grep -c "conflicting neighbour" -- .claude/skills/parallel-orchestrate/SKILL.md`, which must report `>= 1`, proving the search reached the edited surface. Record both commands and results in `<FEATURE>/evidence/other/d1-grep-gates.<ts>.md` with the four required fields, and note in the artifact that a recursive `grep -r` rooted at `.claude/` is invalid in this checkout because `.claude/worktrees/` is a gitignored live worktree containing a full second copy of the repository (12 stale matches today).
  - Acceptance (AC4): the `git grep` gate records zero matches (exit code 1); the positive control records `>= 1`; the worktree note is present in the artifact.

### Phase 2 — D2: Raise the `max_concurrency` Ceiling to 32

Bound constants, prose, tests, and fixture migrations land together in this phase so no suite is left red. The three validator error messages already interpolate the bound constant and are not edited (see P2-T2/P2-T3/P2-T4). The bats/fixture edits (P2-T8) are validated locally through the Python parity lane in P2-T10; bash-lane execution is deferred to P7-T12 as stated in the phase-green statement.

- [ ] [P2-T1] Re-verify the D2 anchors from research D2.2 before editing: `scripts/dev_tools/parallel_manifest_contract.py:65, :264-267`, `scripts/dev_tools/validate_parallel_orchestrator_state.py:71, :144-148`, `scripts/dev_tools/validate_parallel_planner_state.py:64, :168-172`, `scripts/dev_tools/_parallel_state_common.py:196-197`, `extensions/drm-copilot/src/lib/validate/parallel-orchestrator-state-core.ts:70, :144-147`, `extensions/drm-copilot/src/lib/validate/parallel-planner-state-core.ts:66, :192-195`, `.claude/lib/bash/parallel-manifest-validate.sh:44-46, :118-119`, plus the test/fixture anchors listed in P2-T6–P2-T8. Record in `<FEATURE>/evidence/other/d2-anchor-verification.<ts>.md`.
  - Acceptance: artifact lists every anchor with verified or drifted disposition.
- [ ] [P2-T2] Python bound change: set the constant to 32 at `scripts/dev_tools/parallel_manifest_contract.py:65`, `scripts/dev_tools/validate_parallel_orchestrator_state.py:71`, and `scripts/dev_tools/validate_parallel_planner_state.py:64`. **The three error messages (M4 at `parallel_manifest_contract.py:265-266`, invariant 4 at `validate_parallel_orchestrator_state.py:146-148`, P2 at `validate_parallel_planner_state.py:170-172`) interpolate `MAX_CONCURRENCY` via f-strings and require no edit — do not hardcode the bound.** Update the `scripts/dev_tools/_parallel_state_common.py:196-197` docstring ("1 through 8 (A7)" → "1 through 32 (A7)"); this is a net-zero line swap in a 495/500-line file. The default of 4 is untouched.
  - Acceptance (AC15 Python part, AC17 docstring part): `git grep -n "MAX_CONCURRENCY = 8" -- scripts/dev_tools` returns zero matches and `git grep -c "MAX_CONCURRENCY = 32" -- scripts/dev_tools` reports a total of 3 matches across the three named files; `git grep -n "1 through 8" -- scripts/dev_tools/_parallel_state_common.py` returns zero matches; `git grep -n "through 32" -- scripts/dev_tools/parallel_manifest_contract.py scripts/dev_tools/validate_parallel_orchestrator_state.py scripts/dev_tools/validate_parallel_planner_state.py` returns ZERO matches, proving no hardcoded literal was introduced into the interpolated messages.
- [ ] [P2-T3] TypeScript bound change: set `const MAX_CONCURRENCY = 32` in `extensions/drm-copilot/src/lib/validate/parallel-orchestrator-state-core.ts:70` and `parallel-planner-state-core.ts:66`. **The two error templates (`parallel-orchestrator-state-core.ts:147` and `parallel-planner-state-core.ts:195`) interpolate the constant via `${...}` and require no edit — do not hardcode the bound.** Do not touch `parallel-state-shared.ts` or `parallel-state-structures.ts`.
  - Acceptance (AC15 TS part, AC39 partial): `git grep -n "MAX_CONCURRENCY = 8" -- extensions/drm-copilot/src` returns zero matches and `git grep -c "MAX_CONCURRENCY = 32" -- extensions/drm-copilot/src` reports 2 (one per core validator); `git grep -n "through 32" -- extensions/drm-copilot/src/lib/validate/parallel-orchestrator-state-core.ts extensions/drm-copilot/src/lib/validate/parallel-planner-state-core.ts` returns ZERO matches, proving no hardcoded literal was introduced; `git diff --name-only` contains neither `parallel-state-shared.ts` nor `parallel-state-structures.ts`.
- [ ] [P2-T4] Bash bound change: set `PM_MAX_CONCURRENCY=32` in `.claude/lib/bash/parallel-manifest-validate.sh:44-46`. **The M4 error string at `:119` interpolates `$PM_MIN_CONCURRENCY through $PM_MAX_CONCURRENCY` and requires no edit — do not hardcode the bound.**
  - Acceptance (AC15 bash part): `git grep -n "PM_MAX_CONCURRENCY=8" -- .claude/lib/bash` returns zero matches and `git grep -n "PM_MAX_CONCURRENCY=32" -- .claude/lib/bash/parallel-manifest-validate.sh` matches; `git grep -n "through 32" -- .claude/lib/bash/parallel-manifest-validate.sh` returns ZERO matches (the `:118-119` template is untouched); file `<= 500` lines.
- [ ] [P2-T5] Prose bound change: in `.claude/rules/parallel-orchestration.md` update invariant 4 ("from 1 through 32, and must not be a boolean") and M4 ("integer from 1 through 32"), and rewrite the entire "Concurrency Bound (A7)" section to: state the `1..32` bound with default 4; record that no constraint binds hard below O(100) concurrent worktrees; identify GitHub Actions job concurrency (~10–20 concurrent items, degrading by queuing, not failure) as the first-binding constraint; declare the ceiling a sanity limit, not a capacity limit; note that under the per-edge barrier `max_concurrency` is a pure throughput throttle; and contain no epic-symmetry rationale. Also update `.claude/skills/parallel-orchestrate/SKILL.md:67-68`, `.claude/skills/parallel-plan/SKILL.md:256` and `:288`, and `docs/features/templates/parallel/parallel-status.md:31` to `1..32`, default 4. Invariant 14 and M7 wording stay unchanged.
  - Acceptance (AC17 prose part, AC18): per-file POSITIVE assertions — each of the four commands `git grep -c "through 32" -- .claude/rules/parallel-orchestration.md`, `git grep -c "through 32" -- .claude/skills/parallel-orchestrate/SKILL.md`, `git grep -c "through 32" -- .claude/skills/parallel-plan/SKILL.md`, and `git grep -c "through 32" -- docs/features/templates/parallel/parallel-status.md` reports `>= 1`; `git grep -n "through 8" -- .claude/rules/parallel-orchestration.md .claude/skills/parallel-orchestrate/SKILL.md .claude/skills/parallel-plan/SKILL.md docs/features/templates/parallel/parallel-status.md` returns zero matches. The two-token tail `through 8` is used deliberately: the phrase wraps across `parallel-orchestrate/SKILL.md:67-68` ("an integer from 1\nthrough 8, defaulting to 4"), so a whole-phrase `1 through 8` grep cannot detect a miss on that file and is insufficient. Additionally: `git grep -n "symmetry" -- .claude/rules/parallel-orchestration.md` returns zero matches; the A7 section names the five required content elements.
- [ ] [P2-T6] Python test boundary migration: in `tests/scripts/dev_tools/test_parallel_manifest_contract.py` add 32 to the in-range set (:250), replace out-of-range exemplar `9` with `33` (out-of-range block :260-271, error string :268 → `found: 33.` with bound `32`), keeping `100` invalid, and update the stale `:252` docstring "inside the 1..8 bound" to the `1..32` bound (net-zero swaps; the file is at 497/500); in `tests/scripts/dev_tools/test_validate_parallel_orchestrator_state.py` add 32 to the in-range list (:191) and replace `9` with `33` (:201-213, error prefix :210); in `tests/scripts/dev_tools/test_validate_parallel_planner_state.py` (499/500, no headroom) make only the net-zero swaps: replace the out-of-range exemplar `9` with `33` at `:211` and update the error string bound at `:220`. Create `tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py` (new sibling module — record in its docstring that the parent is at 499/500 with no headroom, mirroring `[P3-T5]`'s rationale) containing the new in-range accept parametrization `[1, 4, 32]` (no such accept test exists today — the parent's only `max_concurrency` references are the fixture at `:72` and the reject parametrization). No assertion is deleted or weakened.
  - Acceptance (AC21 pytest part, AC22 part): the new sibling module `tests/scripts/dev_tools/test_validate_parallel_planner_state_bounds.py` exists, is `<= 500` lines, contains `test_invariant_p2_accepts_in_range_concurrency`, and passes; `grep -c ^ tests/scripts/dev_tools/test_validate_parallel_planner_state.py` reports `<= 499`; `tests/scripts/dev_tools/test_parallel_manifest_contract.py` remains `<= 500` lines (at 497 — swaps must be net-zero) and `tests/scripts/dev_tools/test_validate_parallel_orchestrator_state.py` remains `<= 500` lines; `grep -n '"9"\|found: 9\| 9,' tests/scripts/dev_tools/test_parallel_manifest_contract.py tests/scripts/dev_tools/test_validate_parallel_orchestrator_state.py tests/scripts/dev_tools/test_validate_parallel_planner_state.py` shows `9` no longer used as an out-of-range `max_concurrency` exemplar.
- [ ] [P2-T7] TypeScript test boundary migration: in `extensions/drm-copilot/test/lib/validate/parallel-orchestrator-state-core.test.ts` add 32 to the in-range list (:111) and replace the `[9, "9"]` rows with `[33, "33"]` (:118-133, error template :131); in `parallel-planner-state-core.test.ts` **add** an `it.each([1, 4, 32])` in-range accept case (no in-range accept `it.each` exists in that file today — only the reject list at `:181-196`) and replace the `[9, "9"]` row with `[33, "33"]`. Boolean-rejection cases stay, with error-text bound updated to 32 where embedded.
  - Acceptance (AC21 Jest part, AC22 part, AC23 TS part): the two test files pin accept-32/reject-33, including the NEW planner-state accept `it.each`, and retain boolean-rejection cases.
- [ ] [P2-T8] Bats and shared-fixture migration (single canonical exemplar 33; boundary re-exercised at 32): in `tests/shell/parallel_manifest_validate.bats` update the expected strings at :74, :119, :125, :130-131 to the `1 through 32` bound, move the out-of-range value `12` at `:70` → `33` (its expected-error line at `:74` → `found: 33.`), move the value at `:130` from `9` → `33` AND update the same case's accessor assertion at `:132` (`[ "$(pm_manifest_max_concurrency)" = "9" ]` → `"33"`), and update the valid-value case value `8` at `:91` (accessor assertion at `:94`) to 32 so the bash lane exercises the new upper bound. In `tests/fixtures/parallel_manifest_bash/`: `manifest_m4_above_upper_bound.json` (`9` → `33`; `expected_errors` AND `expected_max_concurrency` at `:9` BOTH updated to 33; stale `notes` text "the inclusive upper bound is 8" updated to 32), `manifest_m4_below_lower_bound.json` (error text bound only), `manifest_m4_boolean_rejected.json` (error text bound only), `manifest_m4_non_integer.json` (error text bound only), `manifest_multiple_identity_errors_in_field_order.json` (`12` → `33`; `expected_errors` AND `expected_max_concurrency` at `:12` BOTH updated to 33), `manifest_accessor_open_mode_max_cap.json` (value and `expected_max_concurrency` → 32; stale `notes` text "the upper-bound cap of 8" updated to 32). `tests/fixtures/parallel_cohorts/batches_cap_exceeds_cohort_size.json` is not changed. Fixture-count floors (`MINIMUM_FIXTURE_COUNT`) are `>=` assertions and need no edit.
  - Acceptance (AC21 bats part, AC22 part, AC23 fixture part): `git grep -n "1 through 8" -- tests/shell tests/fixtures/parallel_manifest_bash` returns zero matches; `git grep -n '"expected_max_concurrency": 9' -- tests/fixtures/parallel_manifest_bash` and `git grep -n '"expected_max_concurrency": 12' -- tests/fixtures/parallel_manifest_bash` both return zero matches; no fixture uses an in-range value as an out-of-range exemplar; local execution of bats is not attempted (deferred to P7-T12).
- [ ] [P2-T9] Re-sync the mirrored files touched in this phase byte-identically (`.claude/rules/parallel-orchestration.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/skills/parallel-plan/SKILL.md`, `.claude/lib/bash/parallel-manifest-validate.sh`) under `extensions/drm-copilot/resources/claude-customizations/.claude/`, then run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`.
  - Acceptance (AC36 partial): mirror contract test passes.
- [ ] [P2-T10] Run the Phase 2 gate suites: `poetry run pytest tests/scripts/dev_tools` (includes the Python parity lane `test_parallel_manifest_bash_parity.py` over the migrated fixtures, the default-of-4 tests, and the epic bound tests `test_validate_epic_orchestrator_state*` still pinning `1..8`), and from `extensions/drm-copilot` run `npm run test:unit`. Write `<FEATURE>/evidence/qa-gates/p2-pytest.<ts>.md` and `<FEATURE>/evidence/qa-gates/p2-jest.<ts>.md`, each with the four required fields.
  - Acceptance (AC16 Python/TS parity part, AC19 test half, AC20, AC21, AC23): both exit codes 0; artifacts recorded.

### Phase 3 — D3: M8 Assertion Seam and Lane-Assertion Diagnostic

- [ ] [P3-T1] Amend `.claude/rules/parallel-orchestration.md` with new manifest invariant **M8** appended to the "Invariants (parallel run manifest)" section: `expected_conflict_components`, when present, must be a list of objects each carrying a required non-empty `members` list of positive integers (each resolving to an `items[].issue_num`, no duplicate membership across components) and an optional non-empty-string `name`; absent contributes zero errors; the value must be authored as a block sequence because the bash YAML subset parser rejects non-empty flow collections; the field is an ASSERTION consumed by a planner diagnostic — it never overrides a derived edge, never feeds `compute_cohorts`, never influences scheduling — and its name deliberately references the derived conflict graph. M7 text and the Enum Ownership table are unchanged.
  - Acceptance (AC24): file inspection finds all M8 elements; `git diff` shows no M7 or invariant-14 edit.
- [ ] [P3-T2] Implement the key-gated M8 check in `scripts/dev_tools/parallel_manifest_contract.py` (312 lines; headroom confirmed) with the `Parallel manifest` error prefix, covering exactly the shape rules of P3-T1; a manifest without the key produces a byte-identical error list to before the change.
  - Acceptance (AC25 code half): file `<= 500` lines; `poetry run pyright` clean; key-absent behavior verified by P3-T5's test.
- [ ] [P3-T3] Create `scripts/dev_tools/parallel_lane_assertion.py` (new module, `<= 500` lines): pure connected-component derivation (BFS or union-find) over the same normalized adjacency `parallel_cohort_computation.py` builds internally, comparison against asserted components, and report construction for the four classes (expected-together-but-derived-apart; expected-apart-but-derived-together; member naming no manifest item; manifest item covered by no expected component — informational); plus a thin argparse CLI entry (`python -m scripts.dev_tools.parallel_lane_assertion`) as the module's only I/O. The module is imported by no cohort-computation, validation, or mutation module, and `scripts/dev_tools/parallel_cohort_computation.py` is not modified.
  - Acceptance (AC28 module half, AC30 partial): `grep -c ^ scripts/dev_tools/parallel_lane_assertion.py` `<= 500`; `git diff --name-only` does not contain `parallel_cohort_computation.py`; `poetry run pyright` clean.
- [ ] [P3-T4] Create `tests/scripts/dev_tools/test_parallel_lane_assertion.py` covering isolated vertices, chains, the 13-lane transpose (13 expected components over 69 items, all confirmed), and all four report classes, meeting `>= 85%` line and `>= 75%` branch coverage on the new module.
  - Acceptance (AC28 test half): `poetry run pytest tests/scripts/dev_tools/test_parallel_lane_assertion.py --cov=scripts.dev_tools.parallel_lane_assertion --cov-branch --cov-report=term` passes with numeric line `>= 85` and branch `>= 75` recorded in `<FEATURE>/evidence/qa-gates/p3-lane-assertion-coverage.<ts>.md` (four required fields). The DOTTED module form is required; both `--cov=<path>` and `--cov=<path>.py` resolve no module and collect no data, making the threshold vacuous (verified empirically: `Module ... was never imported` / `No data was collected`).
- [ ] [P3-T5] Create `tests/scripts/dev_tools/test_parallel_manifest_contract_m8.py` (new sibling module — the existing `test_parallel_manifest_contract.py` is at 497/500 lines and has no headroom; record this rationale in the new module's docstring): negative paths for non-list value, non-object entry, missing `members`, empty `members`, non-positive member, non-integer member, member resolving to no `items[].issue_num`, duplicate membership across components, and empty-string `name`; positive paths for a named and an unnamed component in block-sequence form; and the key-absent test asserting a byte-identical error list versus a pre-change expectation.
  - Acceptance (AC25 test half, AC26): all listed cases exist and pass; file `<= 500` lines.
- [ ] [P3-T6] Implement the same M8 check in the bash parity layer: extend `.claude/lib/bash/parallel-manifest-validate.sh` (187 lines) — using helpers in `.claude/lib/bash/parallel-items-validate.sh` (244 lines) only if genuinely shared — emitting byte-identical error strings to P3-T2 for every M8 error class.
  - Acceptance (AC27 code half): both files `<= 500` lines; the bash implementation emits the same error strings the P3-T7 fixtures declare BY CONSTRUCTION, and the Python parity lane confirms those fixtures agree with the Python authority. Byte-identity of the bash lane against the fixtures is NOT locally verifiable — `tests/scripts/dev_tools/test_parallel_manifest_bash_parity.py` imports the Python module and invokes no bash — and is discharged only by the P7-T12 CI dispatch of `tests/shell/parallel_manifest_parity.bats`. Record this limitation in the P3 parity/backward-compatibility artifact (P3-T13).
- [ ] [P3-T7] Add shared M8 fixtures under `tests/fixtures/parallel_manifest_bash/` consumed automatically by both parity lanes (the corpus is glob-discovered with `>=`-floor count assertions, so additions are safe): one positive named-component fixture, one positive unnamed-component fixture (both block-sequence form), and one fixture per M8 error class from P3-T5, each carrying `manifest_text` and the full `expected_errors` list. No fixture uses integral-float, quote-divergent, or boolean-equality values (preserving AC39).
  - Acceptance (AC27 fixture half, AC39 partial): fixtures exist; `poetry run pytest tests/scripts/dev_tools/test_parallel_manifest_bash_parity.py` passes over the extended corpus.
- [ ] [P3-T8] Add M8 accept/reject cases to `tests/shell/parallel_manifest_validate.bats` (165 lines; headroom confirmed): a block-sequence M8 fixture accepted by the bash YAML subset parser, plus at least one M8 rejection case with the exact error string. Local bats execution is not attempted (deferred to P7-T12).
  - Acceptance (AC27 bats half): cases exist; file `<= 500` lines.
- [ ] [P3-T9] Amend `.claude/skills/parallel-plan/SKILL.md`: in `## Cohort Seeding`, immediately after conflict-edge derivation (anchor :249-252), add the lane-assertion diagnostic invocation (`poetry run python -m scripts.dev_tools.parallel_lane_assertion ...`, covered by the planner's existing `Bash(poetry run *)` grant) with explicit advisory-only wording (never blocks, never modifies an edge, never feeds `compute_cohorts`, never influences scheduling); add its result as a required line-item of the planner completion report (anchor :448-461). Checkpoint recording remains a tolerated extra field; no validator change.
  - Acceptance (AC29): both additions present with advisory-only wording; no heading added, removed, or moved.
- [ ] [P3-T10] Run the D3 scope gates and record them in `<FEATURE>/evidence/other/d3-scope-gates.<ts>.md` (four required fields per command): (a) `git grep -n "parallel_lane_assertion" -- scripts/dev_tools` shows the module imported by no cohort-computation, validation, or mutation module; (b) `git diff --name-only` contains neither `scripts/dev_tools/parallel_cohort_computation.py` nor any M8/`expected_conflict_components` logic in `validate_parallel_orchestrator_state.py` / `validate_parallel_planner_state.py` or their helper modules; (c) `git grep -n "expected_conflict_components" -- extensions/drm-copilot/src` returns zero matches.
  - Acceptance (AC30, AC31): all three commands produce the stated results.
- [ ] [P3-T11] Re-sync the mirrored files touched in this phase byte-identically (`.claude/rules/parallel-orchestration.md`, `.claude/skills/parallel-plan/SKILL.md`, `.claude/lib/bash/parallel-manifest-validate.sh`, and `.claude/lib/bash/parallel-items-validate.sh` if edited) under `extensions/drm-copilot/resources/claude-customizations/.claude/`, then run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`.
  - Acceptance (AC36 partial): mirror contract test passes.
- [ ] [P3-T12] Run the Phase 3 gate suite: `poetry run pytest tests/scripts/dev_tools`. Write `<FEATURE>/evidence/qa-gates/p3-pytest.<ts>.md` with the four required fields.
  - Acceptance (AC25, AC26, AC27 Python-lane half, AC28): exit code 0; artifact recorded.
- [ ] [P3-T13] Write the backward-compatibility itemization `<FEATURE>/evidence/other/backward-compat-corpus.<ts>.md`: enumerate every fixture whose `expected_errors` changed (exactly the AC22 migrations and error-text bound updates from P2-T8) and every fixture added (the P3-T7 M8 set); assert via `git diff --name-only tests/fixtures/` that no other fixture changed; state that the parity suites (P3-T12 Python lane, P7-T12 bash lane) are the corpus test demonstrating byte-identical validation for all unmigrated fixtures; record the P3-T6 local-verification limitation (the Python parity lane invokes no bash, so bash-lane byte-identity against the fixtures is discharged only by the P7-T12 CI dispatch).
  - Acceptance (AC38 evidence half): artifact itemizes every changed/added fixture with its reason; no unexplained fixture appears in the diff.

### Phase 4 — D4: Bounded Preparation Fan-Out (Prose Only)

- [ ] [P4-T1] Rewrite `## Preparation Fan-Out` in `.claude/skills/parallel-plan/SKILL.md` (anchor :64-67): remove the "launch ALL item preparations concurrently" instruction; instruct launching preparations in waves of at most `max_concurrency`, batched with `bash .claude/lib/bash/compute-concurrency-batches.sh --keys "<all item keys>" --max-concurrency <n>` (already granted at `.claude/agents/parallel-planner.md:18`), launching wave k+1 only after wave k's children have terminated; document `/parallel-add` as incremental admission into an already-running open-mode queue and explicitly not the intake path; record the deferred `max_preparation_concurrency` manifest extension (same bounds and boolean rejection as M4, defaulting to `max_concurrency`) as considered and explicitly not adopted now.
  - Acceptance (AC32, AC34): `grep -n "launch ALL" .claude/skills/parallel-plan/SKILL.md` returns zero matches; all three prose elements present; no heading added, removed, or moved.
- [ ] [P4-T2] Reword the `.claude/agents/parallel-planner.md` frontmatter description (anchor :4) — and `## Delegation Model` only if it restates fan-out — to bounded-wave preparation.
  - Acceptance (AC33): description carries bounded-wave wording; no unrelated section edited.
- [ ] [P4-T3] Re-sync the mirrored files touched in this phase byte-identically (`.claude/skills/parallel-plan/SKILL.md`, `.claude/agents/parallel-planner.md`) under `extensions/drm-copilot/resources/claude-customizations/.claude/`, then run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`.
  - Acceptance (AC36 partial): mirror contract test passes.
- [ ] [P4-T4] Run the planner-surface contract suites unmodified: `poetry run pytest tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts_landed.py`. Record `<FEATURE>/evidence/qa-gates/p4-pytest.<ts>.md` (four required fields), and verify by `git diff --name-only` that the D4-attributable diff is Markdown-only (`parallel-plan/SKILL.md`, `parallel-planner.md`, their two mirrors).
  - Acceptance (AC35): suites pass with zero test-file edits; diff check recorded in the artifact.

### Phase 5 — Cross-Cutting Scope Gates

All commands in this phase are read-only checks against the accumulated diff. Record each command with the four required fields in `<FEATURE>/evidence/other/cross-cutting-gates.<ts>.md`. Diff commands use the merge base: `git diff --name-only $(git merge-base origin/main HEAD)`, paired with `git status --porcelain --untracked-files=no` so uncommitted work cannot make a diff check pass vacuously.

- [ ] [P5-T1] Enforcement-layer non-modification gate: the name-only diff contains no `.ps1` file, does not contain `scripts/dev_tools/_parallel_orchestrator_state_cohort_barrier.py`, and does not contain its TypeScript port under `extensions/drm-copilot/src/lib/validate/`.
  - Acceptance (AC14 diff half; suites discharged by P1-T16 and P7-T11): all three conditions hold and are recorded.
- [ ] [P5-T2] Epic-surface non-modification gate: the name-only diff contains none of `validate_epic_orchestrator_state.py`, `validate_epic_planner_state.py`, their TypeScript ports, or their tests; run `poetry run pytest tests/scripts/dev_tools -k "epic"` and confirm the epic bound tests still pin `1..8` and pass.
  - Acceptance (AC19): diff condition holds; targeted epic suite green.
- [ ] [P5-T3] Template/pack gate: no `templates/parallel` path exists under `extensions/drm-copilot/resources/` (directory inspection), and `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` is absent from the name-only diff.
  - Acceptance (AC37): both conditions hold and are recorded.
- [ ] [P5-T4] TS divergence-class gate: `extensions/drm-copilot/src/lib/validate/parallel-state-shared.ts` and `parallel-state-structures.ts` are absent from the name-only diff; review every fixture added or changed in this feature (P2-T8, P3-T7 sets) and confirm none exercises integral-float, quote-divergent, or boolean-equality values across runtimes beyond the pre-existing boolean-rejection fixtures.
  - Acceptance (AC39): both conditions hold; the fixture review lists each reviewed fixture by name.
- [ ] [P5-T5] No-JSON-Schema gate (diff-scoped, matching this phase's preamble and AC40's intent: no TOUCHED module imports a JSON-Schema library, and no NEW reference to the disqualified artifact is introduced): (a) `git diff --name-only $(git merge-base origin/main HEAD)` adds no `*.schema.json` or equivalent schema file; (b) `git diff $(git merge-base origin/main HEAD) -- scripts/dev_tools extensions/drm-copilot/src | grep -nE "^\+.*(jsonschema|json-schema)"` returns zero ADDED lines — record the 12 pre-existing matches in `scripts/dev_tools/validate_discovery_schema_artifacts.py` (including `from jsonschema import Draft202012Validator`) and `scripts/dev_tools/validate_json.py` as out-of-scope; (c) `git diff $(git merge-base origin/main HEAD) | grep -n "^\+.*mix-calculator"` returns zero ADDED lines — record the 3 pre-existing prohibition citations at `.claude/rules/orchestrator-state.md:7`, `.claude/rules/orchestrator-state.md:9`, and `.claude/rules/parallel-orchestration.md:9` (which deliberately quote the disqualified `$id` to record the prohibition) as intentional and untouched.
  - Acceptance (AC40): all three diff-scoped conditions hold and are recorded, including the out-of-scope notation for the 12 pre-existing import matches and the intentional-citation notation for the 3 pre-existing `mix-calculator` references.
- [ ] [P5-T6] Pre-QC synchronization: commit all work; run `git fetch origin main` and, if `origin/main` has advanced, rebase the branch and resolve conflicts; push with `git push --force-with-lease` (or plain push when no rebase occurred) so the Phase 7 CI dispatch sees the full change set at the branch head.
  - Acceptance: `git status --porcelain --untracked-files=no` reports no tracked-file modifications; `gh api` or `git ls-remote` confirms the remote branch head equals `git rev-parse HEAD`.

### Phase 6 — Acceptance-Criteria Reconciliation Prerequisites

- [ ] [P6-T1] Build the AC evidence index `<FEATURE>/evidence/other/ac-evidence-index.<ts>.md`: for each of AC1–AC41, list the discharging task IDs (per the traceability table at the end of this plan) and the concrete evidence paths produced so far, marking AC41 and the coverage-bearing ACs as pending Phase 7.
  - Acceptance: all 41 ACs appear with at least one task ID and either an evidence path or an explicit pending-Phase-7 marker.

### Phase 7 — Final QC Loop, Coverage Delta, and AC Check-Off

Run each language loop in order; if any step fails or changes files, restart that language's loop from its first step until a single clean pass completes. One artifact per command step; no aggregate-only artifact; `SKIPPED` is invalid for every task in this phase. Seven-stage mapping for AC41: formatting/linting/type-checking/unit are the per-language steps below; architecture-boundary — no architecture-boundary tool (dependency-cruiser or equivalent) is configured for the touched languages in this repository, recorded as `N/A — no configured stage` in the AC41 artifact; contract — the shared-fixture parity corpora (Python lane in P7-T4, bash lane in P7-T12) plus the surface-contract pytest suites; integration — the bats suites executed by the P7-T12 CI dispatch plus the 13-lane transpose tests (P3-T4).

- [ ] [P7-T1] Python format: run `poetry run black .`. Write `<FEATURE>/evidence/qa-gates/final-python-format.<ts>.md` (four required fields). If any file is reformatted, restart the Python loop from this step after recording the artifact.
  - Acceptance: clean pass recorded (exit 0, zero files changed on the final iteration).
- [ ] [P7-T2] Python lint: run `poetry run ruff check .`. Write `<FEATURE>/evidence/qa-gates/final-python-lint.<ts>.md` (four required fields).
  - Acceptance: exit 0 recorded.
- [ ] [P7-T3] Python type-check: run `poetry run pyright`. Write `<FEATURE>/evidence/qa-gates/final-python-typecheck.<ts>.md` (four required fields).
  - Acceptance: exit 0, zero errors recorded.
- [ ] [P7-T4] Python tests with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `<FEATURE>/evidence/qa-gates/final-python-test.<ts>.md` (four required fields); `Output Summary:` MUST include numeric total line and branch coverage, pass counts, and per-module line/branch values for `scripts/dev_tools/parallel_lane_assertion.py`, `scripts/dev_tools/parallel_mutation_protocol.py`, and `scripts/dev_tools/parallel_manifest_contract.py`.
  - Acceptance: exit 0; all numeric values present.
- [ ] [P7-T5] TypeScript format: from `extensions/drm-copilot`, run `npm run format`, then `git status --porcelain --untracked-files=no -- extensions/drm-copilot`. Write `<FEATURE>/evidence/qa-gates/final-ts-format.<ts>.md` (four required fields). If the formatter modified any file, restart the TypeScript loop from this step after recording the artifact.
  - Acceptance: clean pass recorded (no formatter-induced modifications on the final iteration).
- [ ] [P7-T6] TypeScript lint: from `extensions/drm-copilot`, run `npm run lint`. Write `<FEATURE>/evidence/qa-gates/final-ts-lint.<ts>.md` (four required fields).
  - Acceptance: exit 0 recorded.
- [ ] [P7-T7] TypeScript type-check: from `extensions/drm-copilot`, run `npm run typecheck`. Write `<FEATURE>/evidence/qa-gates/final-ts-typecheck.<ts>.md` (four required fields).
  - Acceptance: exit 0 recorded.
- [ ] [P7-T8] TypeScript tests with coverage: from `extensions/drm-copilot`, run `npm run test:coverage`. Write `<FEATURE>/evidence/qa-gates/final-ts-test.<ts>.md` (four required fields); `Output Summary:` MUST include the numeric text-summary totals plus per-file line/branch values from `coverage/lcov.info` for `src/lib/validate/parallel-orchestrator-state-core.ts` and `parallel-planner-state-core.ts`.
  - Acceptance: exit 0; all numeric values present.
- [ ] [P7-T9] PowerShell regression gate, format: run `mcp__drm-copilot__run_poshqc_format` over the standard scan folders. Write `<FEATURE>/evidence/qa-gates/final-powershell-format.<ts>.md` (four required fields).
  - Acceptance: clean pass; zero `.ps1` modifications (consistent with AC14's no-PowerShell diff).
- [ ] [P7-T10] PowerShell regression gate, analyze: run `mcp__drm-copilot__run_poshqc_analyze`. Write `<FEATURE>/evidence/qa-gates/final-powershell-analyze.<ts>.md` (four required fields).
  - Acceptance: zero errors recorded.
- [ ] [P7-T11] PowerShell regression gate, test: run `mcp__drm-copilot__run_poshqc_test` (includes the Layer-1 barrier hook Pester suite, unmodified). Write `<FEATURE>/evidence/qa-gates/final-powershell-test.<ts>.md` (four required fields); `Output Summary:` MUST include Pester pass counts and the numeric line-coverage headline; branch coverage recorded as `N/A — Pester does not measure branch coverage`.
  - Acceptance (AC14 Layer-1 suite half): exit 0; numeric line coverage present.
- [ ] [P7-T12] Bash final QC via CI: commit and push any Phase 7 formatting deltas, confirm the remote branch head equals `git rev-parse HEAD`, then run `gh workflow run _shell-coverage.yml --ref bug/parallel-lane-scale-and-barrier-semantics-479`, poll with `gh run watch` / `gh run view --json conclusion,url,headSha`. Write `<FEATURE>/evidence/qa-gates/final-shell-qc.<ts>.md` (four required fields); `Output Summary:` MUST include the run URL, green conclusion, headSha equal to the local HEAD, both the shell-QC `check` step and the coverage step green, and the numeric `Bash coverage (lines): NN.N%` value `>= 85`.
  - Acceptance (AC27 bats half, AC38 bash-lane half, bash leg of AC41): green run at the branch head with numeric coverage recorded; if the run fails, fix, push, and re-dispatch until green (restart rule).
- [ ] [P7-T13] Coverage delta and threshold verification: write `<FEATURE>/evidence/qa-gates/final-coverage-delta.<ts>.md` comparing, per language, baseline (P0-T6, P0-T10, P0-T11, P0-T12) versus post-change (P7-T4, P7-T8, P7-T12, P7-T11) numeric values, and reporting new/changed-code coverage: Python new module `parallel_lane_assertion.py` line `>= 85` / branch `>= 75`; changed Python modules (`parallel_mutation_protocol.py`, `parallel_manifest_contract.py`) at or above baseline; TypeScript changed files (the two core validators) line `>= 85` / branch `>= 75`; bash changed file (`parallel-manifest-validate.sh`, plus `parallel-items-validate.sh` if edited) reflected in the `>= 85` line headline, branch `N/A — kcov exemption`; PowerShell new/changed-code value `N/A — no PowerShell production or test file changed in this feature; regression gate only` (this literal N/A does not trigger the fail-closed clause). If any required numeric value is missing or below threshold, the outcome is remediation-required, not PASS.
  - Acceptance (AC41 coverage half): artifact present with every value numeric or an authorized literal `N/A — <reason>`.
- [ ] [P7-T14] AC41 discharge artifact: write `<FEATURE>/evidence/qa-gates/final-toolchain-summary.<ts>.md` mapping the seven stages (format, lint, type check, architecture, unit, contract, integration) to the executed evidence artifacts per the mapping in this phase's preamble, confirming a single clean pass per language on the final loop iteration.
  - Acceptance (AC41): artifact links every stage to at least one evidence artifact or the recorded `N/A — no configured stage` entry.
- [ ] [P7-T15] AC check-off per the `acceptance-criteria-tracking` skill: update the `## Acceptance Criteria` checkboxes in `<FEATURE>/spec.md` for AC1–AC41, each backed by the evidence recorded in the AC evidence index (P6-T1, now completed with Phase 7 artifacts). Write `<FEATURE>/evidence/other/ac-checkoff.<ts>.md` listing, per AC, the checkbox state, discharging tasks, and evidence paths. Any AC that cannot be checked with evidence is left unchecked and reported as remediation-required.
  - Acceptance: all 41 ACs checked with evidence, or the artifact names each unchecked AC with the missing evidence.

## AC Traceability (AC1–AC41 → tasks)

| AC | Discharging tasks |
|---|---|
| AC1 | P1-T7, P1-T16 |
| AC2 | P1-T10 |
| AC3 | P1-T8, P1-T11, P1-T12 |
| AC4 | P1-T17 |
| AC5 | P1-T9, P1-T10, P1-T13 |
| AC6 | P1-T9 |
| AC7 | P1-T7, P1-T9 |
| AC8 | P1-T9 |
| AC9 | P1-T7, P1-T14, P1-T16 |
| AC10 | P1-T14, P1-T16 |
| AC11 | P1-T4 |
| AC12 | P1-T5, P1-T16 |
| AC13 | P1-T6, P1-T16 |
| AC14 | P5-T1 (diff), P1-T16 (Layer-2 suite), P7-T11 (Layer-1 Pester suite) |
| AC15 | P2-T2, P2-T3, P2-T4 |
| AC16 | P2-T2, P2-T3, P2-T4, P2-T10, P7-T12 |
| AC17 | P2-T2 (docstring), P2-T5 |
| AC18 | P2-T5 |
| AC19 | P2-T10, P5-T2 |
| AC20 | P2-T10 |
| AC21 | P2-T6, P2-T7, P2-T8, P2-T10, P7-T12 |
| AC22 | P2-T6, P2-T7, P2-T8 |
| AC23 | P2-T7, P2-T8, P2-T10, P7-T12 |
| AC24 | P3-T1 |
| AC25 | P3-T2, P3-T5, P3-T12 |
| AC26 | P3-T5, P3-T12 |
| AC27 | P3-T6, P3-T7, P3-T8, P3-T12 (Python lane), P7-T12 (bash lane) |
| AC28 | P3-T3, P3-T4 |
| AC29 | P3-T9 |
| AC30 | P3-T3, P3-T10 |
| AC31 | P3-T10 |
| AC32 | P4-T1 |
| AC33 | P4-T2 |
| AC34 | P4-T1 |
| AC35 | P4-T4 |
| AC36 | P1-T15, P2-T9, P3-T11, P4-T3 |
| AC37 | P5-T3 |
| AC38 | P3-T13, P3-T12, P7-T12 |
| AC39 | P2-T3, P3-T7, P5-T4 |
| AC40 | P5-T5 |
| AC41 | P7-T1–P7-T14 |
