# Atomic Implementation Plan (Revision 3) — False-Approval Elimination, Issue #673

- Feature folder: `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/`
- Issue: #673. Work Mode: `full-bug` (`issue.md`). `spec.md` is the acceptance-criteria source. [P1-T2] amends it to add AC-34 through AC-38, so this plan's inventory is AC-1 through AC-38.
- Plan authored: 2026-09-18T16-00
- **Supersedes `plan.2026-09-18T13-30.md`** in the same folder, which itself superseded `plan.2026-09-13T20-48.md`. Neither earlier plan is edited or deleted. No task of `plan.2026-09-18T13-30.md` was executed; the checked tasks of `plan.2026-09-13T20-48.md` produced the reproduction evidence this plan reuses read-only.
- **Why it is superseded.** Revision 2 identified the item by a path in the delegation prompt (an absolute feature-folder path or file path) and treated a repository-relative feature folder as a placement signal. A probe on 2026-09-18 established that this is the wrong basis (§1.1): a relative folder token exists in every checkout branched from `main` and so is almost always ambiguous, it overrides a branch signal that is unique, and an absolute path makes an orchestration depend on the host that started it. The user approved a replacement design on 2026-09-18: checkpoint hygiene at coordinator roots, and target resolution by portable identity (issue number and branch) rather than by path. This plan re-expresses bindings 1, 2, and 3 on that design.
- Branch: `bug/false-approval-elimination-673`, tip `c50f82c2` when this plan was authored.
- Topology (user ruling, 2026-09-18): standalone pull request to `main`. Not an epic child. No push occurs inside this plan; the full Pester run of [P10-T4] precedes any push the caller makes.
- Anchor for every diff and log: `F5_BASE_SHA`, the merge base of `HEAD` and `origin/main`, recorded once by [P0-T3].

## Evidence and host-data rules (binding on every task)

1. Every artifact is written under `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/<kind>/` with `<kind>` one of `baseline`, `regression-testing`, `qa-gates`, `other`. New artifacts carry an `r3-` prefix so no earlier artifact is overwritten. Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. `EXIT_CODE: SKIPPED` is never a passing outcome.
2. **No host data.** No artifact, script, test, fixture, or document this plan writes may contain an absolute filesystem path, a drive letter, a user-profile path, or the executing user's account name. Refer to files by repository-relative path. Where a command prints such a value, the artifact records it with the value replaced: the executing worktree root becomes `<WORKSPACE_ROOT>`, the session scratchpad directory becomes `<SCRATCHPAD>`, the user profile directory becomes `<HOME>`. The executor computes these values at run time and never writes them.
3. Runner scripts for fresh PowerShell processes live in the session scratchpad and contain only repository-relative paths. They obtain the root at run time with `(Get-Location).Path` and are launched with the executing worktree as the working directory. The `Command:` field records the scratchpad directory as `<SCRATCHPAD>`, and the artifact quotes the script body.
4. Artifacts record Pester test names and pass or fail status, never failure-message text, and never decision JSON that carries a path.

## 1. Why this revision exists

### 1.1 Established facts (empirical probe, 2026-09-18; treated as established)

1. A PreToolUse hook runs in the directory of the process that made the call. A call made from inside an item's own worktree already reads that worktree's checkpoint through the relative path.
2. An isolated subagent (`Agent` with `isolation: worktree`) gets a worktree built from `origin/main`'s tip, and its hooks run inside that worktree using `main`'s copy of the hook scripts. A hook change on this branch therefore cannot be exercised end to end by isolated subagents before merge. **All pre-merge verification in this plan is in-process or child-process Pester and the Python suite. No task delegates to an isolated subagent to verify behaviour.**
3. The hook payload's `cwd` field is, for the calls that produce false approvals, the coordinator's directory, not the item's. It is never used to identify the item.
4. False approvals arise only from calls made by a process whose directory is not the item's worktree: a coordinator delegating gated work on an item's behalf, or a process acting on an item through `cd` or `git -C`. The incident was green because a stale per-feature checkpoint for issue 838 sat at the coordinator's root.
5. `Resolve-WorktreeCallTarget` reads signals in the order FeatureFolderPath, FilePath, Branch (`.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:276-280`) and treats any signal with other than exactly one candidate as `Ambiguous` (`:289-293`). A feature folder merged to `main` exists in every checkout branched from `main`, so a relative folder token is almost always `Ambiguous`, and it overrides a branch signal that is unique. Verified: `branch: <item branch>` alone resolves uniquely; the same text plus the relative folder path resolves `Ambiguous`. The folder identifies what the work is, not where it lives.

### 1.2 Delivered by #687 on `main` (re-derived against the tree; not re-implemented)

| Fact | Location |
| --- | --- |
| No-target reason code literal; accessor `Get-WorktreeResolutionNoTargetReasonCode` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:59`; `:475`, exported `:500` |
| Ambiguity reason code literal; accessor `Get-WorktreeResolutionAmbiguityReasonCode` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:55`; `:463`, exported `:499` |
| `WorktreeResolution.psm1` is at the 500-line cap | last line `:500` |
| The result constructor `New-WorktreeResolutionTargetResult` sets `ReasonCode` from the two accessors | `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:66`, `:128-132` |
| Branch reader `Find-WorktreeResolutionBranchSignal` (`--head`, `--branch`, `branch:`) | `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:164`, pattern `:61` |
| Worktree enumeration `Get-WorktreeResolutionWorktreeRoot` (optional `-Branch` filter) | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:334` |
| Worktree marker test `Test-WorktreeResolutionRootMarker`; ascent `Find-WorktreeResolutionRoot` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:228`, `:258` |
| pr-author seam `Resolve-PrAuthorWorktreeTarget`; mapping `Get-PrAuthorTargetCheckpointResolution` | `.claude/hooks/enforce-pr-author-skill-helpers.ps1:41-62`, `:64-107` |
| pr-author resolution runs after Case C and before the preflight (NoTarget and Ambiguous deny) | `.claude/hooks/enforce-pr-author-skill-helpers.ps1:314-320` |

### 1.3 Not delivered (this plan's scope)

1. **Binding 1, AC-4 form.** `.claude/hooks/enforce-pr-author-skill.ps1:49` still assigns the relative literal; `SessionRoot` returns it unchanged (`helpers:96`).
2. **Binding 2.** `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1:35` defaults `-CheckpointPath`; `:78` passes no argument, so check 6 reads the session root's checkpoint even when binding 1 resolved another worktree.
3. **Binding 3.** `.claude/hooks/enforce-model-routing-receipt.ps1:46` defaults `-CheckpointPath`; `:157` passes no argument. No resolution step exists.
4. **Design A and B** (checkpoint hygiene; identity resolution), which no earlier revision planned.

## 2. Design

### 2.1 Identity resolution module (design B)

New module `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, beside F1's two modules. It imports `WorktreeResolution.psm1` and `WorktreeTargetResolution.psm1` with `-ErrorAction Stop` and re-uses F1's enumeration, marker test, ascent, branch reader, path normaliser, path join, result constructor, and reason-code accessors. It re-implements none of them. `WorktreeResolution.psm1` (500 lines) and `WorktreeTargetResolution.psm1` are **not modified**, so no extraction is needed; the coupling guard in [P9-T5] asserts both are absent from the diff.

Exported functions (all `[CmdletBinding()]`, each with `[OutputType()]` declared to match what it returns, including `[string[]], [object[]]` where it returns `, [string[]]`):

| Function | Contract |
| --- | --- |
| `Get-WorktreeItemCheckpointRelativePath` | Returns the one definition of the literal `artifacts/orchestration/orchestrator-state.json`. |
| `Get-WorktreeItemCheckpointPath -WorktreeRoot` | Returns `Join-WorktreeResolutionPath -WorktreeRoot <root> -RepoRelativePath (Get-WorktreeItemCheckpointRelativePath)`, an absolute forward-slash path. |
| `Find-WorktreeItemIssueSignal -Text` | Returns, as `, [string[]]`, the distinct normalised issue numbers named by lines matching `Canonical issue number for this feature is <digits>` (case-sensitive, optional `#` before the digits), in order of first appearance. |
| `ConvertTo-WorktreeItemIssueNumber -Value` | Pure. Returns the positive-integer issue number as a digit string without leading zeros for an integer value or a string of the form optional `#` plus digits (whitespace trimmed); `$null` for anything else, including zero, `none`, and an empty value. |
| `Get-WorktreeItemCheckpointText -Path` | The module's only filesystem read (seam): raw text of a file, or `$null` when absent or unreadable. |
| `Get-WorktreeItemCheckpointIssue -WorktreeRoot` | Reads the checkpoint at `Get-WorktreeItemCheckpointPath` through the seam, parses it with `ConvertFrom-Json` inside `try`, and returns `ConvertTo-WorktreeItemIssueNumber` of its `issue-num` property; `$null` when the file is absent, empty, unparseable, not a JSON object, or lacks the key. |
| `Get-WorktreeItemLiveRoot -SessionRoot [-Branch]` | Seam. Ascends from `SessionRoot` with `Find-WorktreeResolutionRoot`; when no worktree root is found returns an empty array. Otherwise returns `Get-WorktreeResolutionWorktreeRoot -SessionRoot <ascended root> [-Branch <b>]` filtered to roots for which `Test-WorktreeResolutionRootMarker` is true. |
| `Resolve-WorktreeItemTarget -Text [-SessionRoot]` | Always returns an object built by `New-WorktreeResolutionTargetResult`. Algorithm in §2.2. `SessionRoot` defaults to `(Get-Location).ProviderPath`. |

**Definition of live.** A worktree root `W` is live when (L1) it is registered in the administrative layout of the repository containing the calling process's directory, which is the main checkout plus every `worktrees/<name>/gitdir` registration (`Get-WorktreeResolutionWorktreeRoot`), and (L2) `Test-WorktreeResolutionRootMarker -Path W` is true, meaning the directory exists and its `.git` entry is a directory or a well-formed `gitdir:` file. A registration whose directory was deleted (git's "prunable" state) fails L2 and is excluded. A worktree records issue `N` when `Get-WorktreeItemCheckpointIssue -WorktreeRoot W` equals `N`. Only the canonical checkpoint path is read, so a checkpoint archived under `artifacts/orchestration/handoff/` is never matched.

**Stale attempts at the same issue.** A worktree from an earlier attempt is excluded when its directory is gone (L2) or its checkpoint was archived or removed. When it is still live and still records issue `N`, the issue alone places the call in two worktrees, which is `Ambiguous`. The only tie-breaker is a branch signal in the same call: git checks a branch out in at most one worktree, so the branch names exactly one of them. No timestamp, `last_updated`, lifecycle step, or path proximity is ever read to break the tie, because a stale checkpoint carries those values too; choosing by them is the defect class this feature removes. The ambiguity detail names the remedy: archive the stale checkpoint (design A) or add a `branch:` label.

### 2.2 Resolution algorithm (`Resolve-WorktreeItemTarget`)

Let `I` be the issue numbers from `Find-WorktreeItemIssueSignal`, `B` the branch from `Find-WorktreeResolutionBranchSignal` (first match, F1's reader unchanged), and `S` the normalised session path. Decided in this order:

| # | Condition | Status | Detail (no absolute path is ever included) |
| --- | --- | --- | --- |
| 1 | `I` has more than one member | Ambiguous | `the call names <k> different issue numbers (<list>), and an item has exactly one` |
| 2 | `I` empty and `B` absent | NoTarget, returned before any enumeration or file read | `the call carries neither a canonical issue number line nor a branch signal (--head, --branch, or branch:), so the item it acts on cannot be identified` |
| 3 | `B` present: `MB = Get-WorktreeItemLiveRoot -SessionRoot S -Branch B` has 0 members | NoTarget | `branch '<B>' is checked out in no live worktree` |
| 4 | `B` present: `MB` has more than one member | Ambiguous | `branch '<B>' is checked out in <k> live worktrees` |
| 5 | `B` present, `MB = {W}`, `I` empty | resolved `W` | `branch '<B>' resolves to the live worktree that has it checked out` |
| 6 | `B` present, `MB = {W}`, `I = {N}`, `W` records `N` | resolved `W` (this breaks a stale-attempt tie) | `branch '<B>' and issue <N> resolve to the same live worktree` |
| 7 | `B` present, `MB = {W}`, `I = {N}`, `W` records a different issue `M` | Ambiguous | `branch '<B>' places the call in a worktree whose checkpoint records issue <M>, not issue <N>` |
| 8 | `B` present, `MB = {W}`, `I = {N}`, `W`'s issue unknown, and some other live worktree records `N` | Ambiguous | `branch '<B>' and issue <N> place the call in different worktrees` |
| 9 | `B` present, `MB = {W}`, `I = {N}`, `W`'s issue unknown, no live worktree records `N` | resolved `W` | as row 5 |
| 10 | `B` absent, `I = {N}`: `MI` = live worktrees recording `N` has 0 members | NoTarget | `issue <N> is recorded in the orchestrator checkpoint of no live worktree` |
| 11 | `B` absent, `MI = {W}` | resolved `W` | `issue <N> resolves to the live worktree recording it` |
| 12 | `B` absent, `MI` has more than one member | Ambiguous | `issue <N> is recorded in the orchestrator checkpoints of <k> live worktrees; move the stale checkpoint to artifacts/orchestration/handoff/ or add a branch: label naming the item's branch` |

A resolved result has `Status` `SessionRoot` when `W` equals `S` or equals `Find-WorktreeResolutionRoot -Path S` (the latter evaluated only when the former is false), and `OtherWorktree` otherwise; `Signal` is `Branch` with `SignalValue` `B` when a branch was used, and empty otherwise; `Candidate` is `@(W)`. Ambiguous results carry the candidate roots in `Candidates` (an object field, never rendered into a reason). `ReasonCode` is supplied by F1's constructor: `NoTarget` carries the no-target code and `Ambiguous` the ambiguity code (user mapping, 2026-09-18: zero live matches or no identity deny with the no-target code; several, or disagreeing signals, deny with the ambiguity code).

The calling process's directory is used for exactly two things: to find the repository whose worktrees are enumerated, and to label a resolved result `SessionRoot`. It is never the checkpoint location unless identity resolution selects it. The payload `cwd` field is not read.

### 2.3 Signal precedence decision (point 5) and the prd-feature gate

**Decision: a feature folder is used only to find documents and never to choose the worktree.** The three bindings in scope locate no documents, so `Resolve-WorktreeItemTarget` reads no path signal at all: an absolute or relative feature-folder path, and an absolute file path, are ignored for worktree selection. Ranking unique signals above location-ambiguous ones was rejected, because an absolute path is still host-dependent, and a ranked resolver would keep a path as a fallback selector.

**`enforce-prd-feature-before-planner.ps1` (issue #672) is out of scope and its behaviour is unchanged.** It calls F1's `Resolve-WorktreeCallTarget` (`.claude/hooks/enforce-prd-feature-before-planner.ps1:108`, `:278-280`), which this plan does not modify. After this plan it still ranks the feature folder first, so a relative folder token that exists in several worktrees still makes it deny `Ambiguous` (`:337-339`), and it still reads the envelope `cwd` as its session root (`:273-275`). AC-37 pins this: the gate, its helpers, and both F1 modules are absent from the diff, and the gate's three suites pass. Follow-up (§8): migrate that gate to `Resolve-WorktreeItemTarget` for where the work lives and use the folder only to locate the prerequisite documents beneath the resolved worktree.

### 2.4 Checkpoint hygiene (design A) and where it is enforced

**Rule.** A coordinating session never holds a per-feature `artifacts/orchestration/orchestrator-state.json` at its own root when it hands work off. It moves such a file to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json`, following the existing convention (`artifacts/orchestration/handoff/` already holds `orchestrator-state.epic-678-integration-pr-686.2026-09-18T10-37.json` in this worktree).

**Enforcement decision.**

1. **Skill contract (normative).** [P8-T2] to [P8-T4] add the rule to `.claude/skills/orchestrate/SKILL.md` `## Checkpoint Handling` (archive a checkpoint belonging to a different issue instead of overwriting or leaving it; archive on handing an item to another session or worktree), `.claude/skills/parallel-orchestrate/SKILL.md` `## Per-Item Branch and Worktree Lifecycle`, and `.claude/skills/epic-orchestrate/SKILL.md` `## Epic-Level Checkpoint` (archive before the first child delegation; write no per-feature checkpoint at the coordinator root). A Pester contract test ([P8-T6]) asserts each statement is present, so the rule cannot be removed silently.
2. **Mechanical check inside the three gates.** Under identity resolution a stale root checkpoint can affect a verdict only when it records the same issue as the call. In that case the root is a second live worktree recording the issue, and §2.2 row 12 denies `Ambiguous` with the archive remedy in the reason. A stale checkpoint for a different issue is never read by these gates. The gates therefore enforce hygiene exactly where it could change their verdict, with no new deny path.
3. **No separate hook or validator check is added.** A check that denied on the mere presence of a foreign checkpoint would add a deny path to gates for a condition that is not itself a gated action, and a Python validator check would require a TypeScript parity port. Neither is needed for the correctness of the three bindings. The gates that still read cwd-relative checkpoints are listed in §8 as the places where hygiene continues to matter.

**Delegation identity (skill contract).** `.claude/skills/orchestrate/SKILL.md:258` currently requires the canonical issue line only on delegations to `atomic-planner`, `atomic-executor`, and `feature-review`. [P8-T2] extends it to all six receipt-gated types (`atomic-planner`, `atomic-executor`, `feature-review`, `task-researcher`, `prd-feature`, `pr-author`, matching `Get-ModelRoutingGatedAgent` at `.claude/hooks/enforce-model-routing-receipt.ps1:73-80`) and adds a `branch:` label naming the item's branch, written as the first `branch:` occurrence in the prompt. Before promotion assigns an issue number, the branch label alone is required. The contract test pins the skill's agent list to the gate's list.

### 2.5 Hook outcomes (both families)

| Resolution state | Action | Reason text begins with |
| --- | --- | --- |
| `SessionRoot` or `OtherWorktree` | proceed with `Get-WorktreeItemCheckpointPath -WorktreeRoot <target.WorktreeRoot>`; all existing decision logic runs unchanged | existing reasons only |
| resolved; checkpoint absent, not ready, empty, or unparseable | existing deny, unchanged | `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` (pr-author) or `MODEL_ROUTING_RECEIPT_BLOCKED` (model-routing) |
| `NoTarget` | deny | `$target.ReasonCode` (the no-target code) |
| `Ambiguous` | deny | `$target.ReasonCode` (the ambiguity code) |

There is no working-directory fallback on any branch. A target-resolution deny never contains either existing reason, and an existing-reason deny never contains either target-resolution code.

### 2.6 Hook changes

- **DD-1 — pr-author seam body.** `Resolve-PrAuthorWorktreeTarget` keeps its name and `-CommandText` parameter; its body becomes `Resolve-WorktreeItemTarget -Text $CommandText -SessionRoot (Get-Location).Path`. `Get-PrAuthorTargetCheckpointResolution` merges `SessionRoot` and `OtherWorktree` into one branch returning `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot`; its deny branch and reason text are unchanged (#687 behaviour, including the `--head` remedy). `enforce-pr-author-skill-helpers.ps1` adds `Import-Module (Join-Path $PSScriptRoot '../lib/worktree-resolution/WorktreeItemResolution.psm1') -Force -ErrorAction Stop` after its two F1 imports (`:38-39`), which stay.
- **DD-2 — resolve once, pass explicitly (bindings 1 and 2).** `enforce-pr-author-skill.ps1:49` becomes `$script:OrchestratorStateCheckpointPath = $null` (the spec's required form). In `Get-PrAuthorBypassReason`, after the deny check, `$script:OrchestratorStateCheckpointPath` is assigned the resolved absolute path and passed to `Invoke-OrchestratorStatePreflight -CheckpointPath`, to `Test-PrAuthorReceiptVerification -CheckpointPath` (new mandatory parameter), and on to `Test-EpicBaseBranchOverride -CheckpointPath` (new mandatory parameter), which passes it to `Get-PrAuthorCheckpointContent -CheckpointPath` (now mandatory, default removed).
- **DD-3 — model-routing (binding 3).** `enforce-model-routing-receipt.ps1` imports `WorktreeItemResolution.psm1` at script scope with `-Force -ErrorAction Stop` after the `HookPayload.psm1` import at `:36`. New seam `Resolve-ModelRoutingWorktreeTarget -PromptText` calls `Resolve-WorktreeItemTarget -Text $PromptText -SessionRoot (Get-Location).Path`. New `Get-ModelRoutingTargetCheckpointResolution -PromptText` maps §2.5 and returns an ordered dictionary with `CheckpointPath` and `Reason`. The reason for an unresolved target is `"$($target.ReasonCode): $($target.Detail) The model-routing gate will not check this delegation against a checkpoint that may belong to a different item. Put the line 'Canonical issue number for this feature is <N>.' and a 'branch: <item branch>' label in the delegation prompt so the gate can identify the item."`. The deny sits between the scope filter (`:153-155`) and the checkpoint read (`:157`), which becomes `Get-ModelRoutingCheckpoint -CheckpointPath $resolution.CheckpointPath`; `Get-ModelRoutingCheckpoint`'s parameter becomes mandatory with no default. Only the `prompt` field is scanned.

## 3. Reconciliation record (every restated acceptance criterion)

[P1-T2] copies this section into `spec.md` verbatim.

| ID | AC | Original wording assumed | Restatement |
| --- | --- | --- | --- |
| RS-1 | AC-9, AC-10, AC-13, AC-15, AC-17 | one "ambiguity reason code" | Two target-resolution codes exist: the no-target code (no identity, or an identity that places in no live worktree) and the ambiguity code (several live worktrees, or disagreeing identities). Tests obtain both only from `Get-WorktreeResolutionNoTargetReasonCode` and `Get-WorktreeResolutionAmbiguityReasonCode`. AC-15 treats "the ambiguity reason code" as the set of both. AC-17 asserts one case per unresolved state. |
| RS-2 | AC-13 rows R8, R9 | R8 "target resolves outside any worktree"; R9 "a command naming two worktrees" | Paths no longer place a call. R8 becomes "an identity that places in no live worktree" (no-target code). R9 becomes "identities that place in different worktrees": two issue numbers, an issue and a branch that disagree, or an issue recorded by two live worktrees (ambiguity code); never first-match. |
| RS-3 | AC-9, AC-10 row R2 | the sibling-only case denies with the ambiguity code | R2's payload carries no identity, so it denies with the no-target code. A second model-routing R2 row uses the verbatim prompt of `evidence/baseline/repro-3-4-control-pair.md:28` (a relative feature folder only) and also denies with the no-target code, because a folder is not an identity. |
| RS-4 | AC-11 row R4 | own and sibling both present, verdict from own (mechanism unstated) | Proven through identity: the prompt or command names the own item by issue or branch, both fixture checkpoints are live, and the verdict comes from the own checkpoint. A further row proves a branch label breaks a stale-attempt tie. |
| RS-5 | AC-12 | fail-before observable for both families | pr-author R2 already denies on this tree (#687). Satisfied by the archived pre-#687 `allow` (`evidence/baseline/repro-3-2-control-pair.md`, AC-1, AC-2), the dossier `evidence/regression-testing/fail-before-exception.2026-09-18T16-00.md`, and the substitute binding-2 row `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present`, which allows before the fix and denies after it. The model-routing half is observed directly. |
| RS-6 | AC-14 row R7 | empty or unparseable checkpoint at a resolved target | Asserted on targets resolved by branch (§2.2 row 9) or by the hook seam. Under issue-only identity an unreadable checkpoint cannot be matched to its issue, so that call denies with the no-target code; that is still a deny and is stated, not hidden. |
| RS-7 | AC-8 | no fallback to the process working directory | The working directory only locates the repository and labels `SessionRoot` (§2.2). It is never the checkpoint location unless identity resolution selects that worktree. |
| RS-8 | AC-19 | protected rows at spec line numbers, edit forms (a) and (b) | #687 moved rows and renamed the end-to-end row (audited by [P0-T6]). This plan edits: form (a) at `enforce-pr-author-skill.epic-base-branch.Tests.ps1:34`, `:40`, `:48` (inside protected rows) plus `:58`, `:68`, `:78`, `:84` and `enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1:38`, `:52`; form (c), one Describe-level `BeforeEach` in `enforce-model-routing-receipt.Tests.ps1` mocking `Resolve-ModelRoutingWorktreeTarget` to `SessionRoot` at `(Get-Location).Path`, the form #687 used on `main`; no edit to `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (its end-to-end row resolves NoTarget before the pin at `:97` is read). |
| RS-9 | AC-21, AC-22 | F1 merged into an epic integration branch | F1 (#669) is on `main`. AC-21: both F1 module paths appear once in `core.json` `paths[]` (`:169-170`), `WorktreeResolution.Manifest.Tests.ps1` passes, and the commit adding `WorktreeTargetResolution.psm1` is an ancestor of `origin/main`. AC-22: the binding table is refreshed to current lines and gains the no-target rows and the identity-derivation row. |
| RS-10 | AC-23 | search the whole repository | Evaluated over files added or modified outside the feature folder, for both literals. Two #687 files carrying the literals are edited and cleaned: the doc comment at `helpers:74` and `:78`, and `enforce-pr-author-skill.TargetResolution.Tests.ps1`. Skill edits must not carry either literal. |
| RS-11 | AC-24 | F5 defines no target derivation; every use calls F1 | The identity derivation is a new resolution mode placed in the worktree-resolution library beside F1, re-using F1's enumeration, marker test, ascent, branch reader, join, constructor, and accessors. No hook file defines any derivation, normalisation, or reason code. |
| RS-12 | AC-25 | each hook imports F1's module | Each entry hook imports the library module it calls (`WorktreeItemResolution.psm1`), unconditionally at script scope with `-ErrorAction Stop`; that module imports F1's two modules the same way. `enforce-pr-author-skill.epic-base-branch.ps1` calls no library function (it receives the path) and gains no import. |
| RS-13 | AC-28 | diff against the epic integration branch | Diff against `F5_BASE_SHA`. |
| RS-14 | AC-30 | no file exceeds 500 lines | `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` is 509 lines on `main`; [P6-T2] moves its value-contract block into a sibling file. |
| RS-15 | AC-31 | four hooks | Coverage floor also applies to `WorktreeItemResolution.psm1`. |
| RS-16 | AC-32, spec test construction | out-of-process fixture spawns | Fixture rows run in process with both `Set-Location` and `[Environment]::CurrentDirectory` set and restored in `finally` (spec trap 1 permits this), so their lines are coverage-instrumented. |
| RS-17 | AC-1, AC-2 | verbatim decision JSON | [P1-T5] replaces host tokens in the archived runs with placeholders under the binding host-data rule; permission decisions and reason codes are unchanged, and a redaction log records per-file counts. |
| RS-18 | AC-33 | seven-stage loop for PowerShell | A Python test file changes ([P8-T5]), so format, lint, and type stages also run for it. |

## 4. Test construction

- **Committed fixtures cannot be real worktrees** (git does not track a `.git` component). Rows whose verdict depends on topology inject it: identity rows mock `Get-WorktreeItemLiveRoot` inside module `WorktreeItemResolution` (`Mock -ModuleName WorktreeItemResolution`), declaring fixture directories as live worktrees, while the identity module's checkpoint reads and the hook's checkpoint reader run unmocked against committed fixture files. Rows needing a state the hook reaches only through the hook seam mock `Resolve-PrAuthorWorktreeTarget` or `Resolve-ModelRoutingWorktreeTarget` with results built by `New-WorktreeResolutionTargetResult`.
- **Rows with no identity** (R2, R5, the relative-folder row) mock nothing: §2.2 row 2 returns before any enumeration or file read, so they are deterministic.
- **AC-9 and AC-10.** Both R2 rows run with the working directory set to a committed session-root fixture holding a ready sibling checkpoint and mock neither the resolver nor any checkpoint reader.
- **In-process location control.** `Invoke-WorktreeResolutionFixtureCall` saves `(Get-Location).Path` and `[System.Environment]::CurrentDirectory`, sets both to the fixture directory, invokes the script block, and restores both in `finally`. Both must be set because `Get-PrBodyFileBytes` reads with `[System.IO.File]::ReadAllBytes`.
- **Import order in test files.** `BeforeAll` dot-sources the hook first, then imports `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `WorktreeResolution.psm1`, and `WorktreeTargetResolution.psm1` without `-Force`, so the test binds to the module instance the hook loaded when the hook loads one, and still loads it before the fix, when the hook does not.
- **Synthetic roots** (unit and path-capture rows) are composed at run time as the path root of `$PSScriptRoot` joined with `f5-seam-root/<name>`; no test file contains a drive letter or absolute path.
- **No temporary files.** All fixture content is committed under `tests/fixtures/worktree-resolution/`. No test creates a file or directory, and none uses `TestDrive`, `New-TemporaryFile`, or a temp path.
- **Receipts.** Fixture receipt `created_at` is `2099-01-01T00:00:00Z`, strictly newer than any checkout time; `.gitattributes:1` (`* text=auto eol=lf`) keeps body bytes LF, so the recorded SHA-256 holds.
- **Schema coupling.** pr-author fixture checkpoints derive from the checkpoint at `evidence/baseline/repro-fixture-manifest.md:53-100`, observed to pass `Invoke-OrchestratorStatePreflight`. `tests/fixtures/worktree-resolution/README.md` and the head comment of the pr-author matrix file name `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`.

### 4.1 Fixture map (roots relative to `tests/fixtures/worktree-resolution/`)

| Root | Checkpoint `issue-num` | Content |
| --- | --- | --- |
| `pr-author/session-root` | `838` | base JSON; `artifacts/pr_context.summary.txt`; `artifacts/pr_body_1.md`; `artifacts/pr_body_1.receipt.json` |
| `pr-author/item-own-ready` | `901` | base JSON with `objective` `Own item B fixture.`; the same three artifact files |
| `pr-author/item-own-not-ready` | `901` | base JSON with `step5_status` `pending` |
| `pr-author/item-own-epic-mode` | `901` | base JSON plus `"epic_mode": true` and `"epic_context": {"integration_branch": "epic/f5-fixture-integration"}` |
| `model-routing/session-root` | `838` | `{"issue-num":"838","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `model-routing/item-own-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `model-routing/item-own-no-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"feature-review"}]}` |
| `model-routing/item-stale-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `shared/item-own-invalid-json` | unknown | checkpoint `{ this is not valid json` |
| `shared/item-own-empty` | unknown | checkpoint of zero bytes |
| `shared/item-no-checkpoint` | none | `artifacts/pr_context.summary.txt` only |

Fixture branch names used only inside mocks: `f5-fixture-own`, `f5-fixture-sibling`, `f5-fixture-missing`. Issue `902` is recorded by no fixture.

## 5. Derivation of asserted values (fixed; not left to the executor)

- **MCP PoshQC tools** return a summary composed before the child runs and no child output. No acceptance condition reads a value from them; every count comes from invoking the module directly.
- **Fresh process.** Every PowerShell toolchain run is a fresh `pwsh -NoProfile -File <SCRATCHPAD>/<script>.ps1` process (route `a`), or, where a direct `pwsh` command is refused, a bare `sh <SCRATCHPAD>/<script>.sh` whose body is `pwsh -NoProfile -File "$(dirname "$0")/<script>.ps1"` (route `a-prime`), per [P0-T7]. The script body begins `$ErrorActionPreference = 'Stop'`, `$root = (Get-Location).Path`, `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force`, then the task's command with `-Root $root`. A reused interactive session is not a valid route.
- **Formatter.** `Invoke-PoshQCFormat` logs `Formatted: <path>` per rewrite and `Already formatted: <path>` otherwise (`scripts/powershell/PoshQC/PoshQC.Analyzer.psm1:60-65`). Observation: the count of lines beginning `Formatted: `, the count beginning `Already formatted: `, and `git status --porcelain` before and after.
- **Analyzer.** `Invoke-PoshQCAnalyze -Root $root` runs with Error, Warning, and Information severities (`PoshQC.Analyzer.psm1:101`) and prints `PSScriptAnalyzer passed: no findings under <root>` on success (`:185`); otherwise it prints a table and throws `PSScriptAnalyzer reported <N> issue(s).` (`:181-183`). `Invoke-ScriptAnalyzer` with a severity filter is never used.
- **Pester.** `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` writes JUnit to `artifacts/pester/pester-junit.xml` (`pester.runsettings.psd1:15`) and CoverageGutters coverage to `artifacts/pester/powershell-coverage.xml` (`:21-22`); `CoveragePercentTarget = 0` (`:298`), so the 85% floor is an explicit comparison. A `testsuite` is matched by a `name` attribute ending with the test file's base filename; a `testcase` by a `name` attribute ending with the quoted `It` name; zero matching `testcase` elements is a failure. Testsuite passed count = `tests - failures - skipped - disabled`. Root failures come from the `testsuites` element's `failures` and `errors`. Every task reading a report records its last-write time and requires it at or after the task's `Timestamp:`.
- **Coverage.** Per file: the `sourcefile` whose `name` equals the base filename inside the `package` whose `name` ends with `.claude/hooks` or `.claude/lib/worktree-resolution`; percent = `covered / (covered + missed) x 100` from `counter type="LINE"`, raw counters recorded. Changed-line coverage: an added line number carrying a `line` element counts as covered when its `ci` attribute is greater than 0.
- **Baseline failures.** If [P0-T10] records failing testcases, every root `failures 0` condition before Phase 10 excludes exactly those names, which the artifact lists. [P10-T4] applies no exclusion: a baseline failure still present stops execution and is reported, because CI fails on it.
- **Python.** `poetry run pytest -q` final summary line (`<N> passed`, plus `failed` or `error` counts when present). `poetry run black --check <file>` prints `would be left unchanged` on success; `poetry run ruff check <file>` prints `All checks passed!`; `poetry run pyright <file>` prints `0 errors`. No Python production file changes, so no Python coverage obligation exists.

## 6. PowerShell batch-budget schedule

`.claude/hooks/enforce-powershell-batch-budget.ps1` denies a fourth distinct production or test PowerShell file written through Write or Edit in one session (`:10-11`) and counts `.psd1` as production (`:34`). A reset deletes every `.claude/state/powershell-batch-budget.*.json` in the executing worktree and appends an entry to `evidence/other/r3-batch-budget-resets.md` (file names deleted, or `none present`); a refused deletion is recorded verbatim and execution stops. Mirror copies are made with `Copy-Item -LiteralPath <source> -Destination <mirror> -Force` through a shell, so they consume no slot and are byte copies.

| Batch | Production files | Test files |
| --- | --- | --- |
| B1 ([P2]) | `WorktreeItemResolution.psm1`, `pester.runsettings.psd1` | `WorktreeItemResolution.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1` |
| B2 ([P4]) | none | `WorktreeResolutionFixture.Helpers.ps1`, `enforce-pr-author-skill.WorktreeResolution.Tests.ps1`, `enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` |
| B3 ([P5]) | `enforce-pr-author-skill.ps1`, `enforce-pr-author-skill-helpers.ps1`, `enforce-pr-author-skill.epic-base-branch.ps1` | `enforce-pr-author-skill.TargetResolution.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1` |
| B4 ([P6]) | none | `OrchestratorState.Tests.ps1`, `OrchestratorState.ValueContract.Tests.ps1` |
| B5 ([P7]) | `enforce-model-routing-receipt.ps1` | `enforce-model-routing-receipt.Tests.ps1`, `enforcement-hooks-checkpoint-path-explicit.Tests.ps1` |
| B6 ([P8]) | none | `checkpoint-hygiene-skill-contract.Tests.ps1` |

## 7. Named Pester tests (exact `It` names)

`tests/scripts/claude-runtime/test-name-uniqueness.Tests.ps1` requires distinct names per parent scope, including expanded `-ForEach` rows.

`tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1` (in process; topology through mocks of `Get-WorktreeItemLiveRoot`, `Get-WorktreeItemCheckpointText`, `Find-WorktreeResolutionRoot`, `Get-WorktreeResolutionWorktreeRoot`, and `Test-WorktreeResolutionRootMarker` inside module `WorktreeItemResolution`):
1. `returns the repository-relative orchestrator checkpoint path`
2. `matches the default checkpoint path of Invoke-OrchestratorStatePreflight` (parses `.claude/lib/orchestrator-state/OrchestratorState.psm1` with `[System.Management.Automation.Language.Parser]::ParseFile` and compares the `CheckpointPath` parameter default of `Invoke-OrchestratorStatePreflight`)
3. `composes the absolute checkpoint path under a worktree root`
4. `reads every distinct issue number from canonical issue lines`
5. `returns no issue number for text without a canonical issue line`
6. `normalises string and integer issue-num values`
7. `rejects zero, none, empty, and non-numeric issue-num values`
8. `reads the issue number from a worktree checkpoint through the checkpoint text seam`
9. `returns no issue number for an absent, empty, or unparseable checkpoint`
10. `keeps only registered worktrees that still carry a root marker`
11. `returns no live worktree when the session path is inside no repository`
12. `resolves NoTarget without enumerating worktrees when the text carries no identity`
13. `resolves the single live worktree whose checkpoint records the issue`
14. `resolves NoTarget when no live worktree records the issue`
15. `resolves Ambiguous when two live worktrees record the issue`
16. `breaks a stale-attempt tie with the branch signal`
17. `resolves Ambiguous when the branch worktree records a different issue`
18. `resolves Ambiguous when the branch and the issue name different worktrees`
19. `resolves the branch worktree when its checkpoint records no issue and no other worktree records it`
20. `resolves Ambiguous when the text names two different issue numbers`
21. `resolves the branch worktree when only a branch signal is present`
22. `resolves NoTarget when the branch is checked out in no live worktree`
23. `ignores a feature-folder path and resolves NoTarget when it is the only signal`
24. `resolves by branch when a relative feature folder and a unique branch are both present`
25. `labels the result SessionRoot when the resolved worktree is the session root`
26. `carries the no-target and ambiguity codes supplied by the worktree-resolution accessors`
27. `names no absolute path in the Detail of any result`

`tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.Tests.ps1` (all in process; "identity" = real `Resolve-WorktreeItemTarget` with mocked `Get-WorktreeItemLiveRoot`; "seam" = mocked `Resolve-PrAuthorWorktreeTarget`):
1. `pr-author R1 allows when the resolved own checkpoint is ready and the working directory is the sibling session root` (seam OtherWorktree `pr-author/item-own-ready`; cwd `pr-author/session-root`)
2. `pr-author R1 allows when the branch locates the own ready checkpoint from the sibling session root` (identity `--head f5-fixture-own`)
3. `pr-author R2 denies with the no-target code when only the sibling session-root checkpoint is present` (no mock; cwd `pr-author/session-root`; command `gh pr create --title "B" --body-file artifacts/pr_body_1.md`)
4. `pr-author R3 denies with the preflight reason when the resolved own checkpoint is not ready` (identity to `pr-author/item-own-not-ready`)
5. `pr-author R3 denies with the preflight reason when the checkpoint is absent at the resolved target` (identity to `shared/item-no-checkpoint`)
6. `pr-author R4 takes the preflight verdict from the own checkpoint located by branch when the sibling checkpoint is ready` (identity; live roots `pr-author/session-root` and `pr-author/item-own-not-ready`)
7. `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present` (seam OtherWorktree `pr-author/item-own-epic-mode`; command `gh pr create --head f5-fixture-own --base main --title "B" --body-file artifacts/pr_body_1.md`; substitute fail-before)
8. `pr-author R5 denies with the no-target code when the command names no target` (no fixture, no mock)
9. `pr-author R6 allows when the working directory is the item worktree and its own checkpoint is ready` (seam SessionRoot `pr-author/item-own-ready`; cwd `pr-author/item-own-ready`)
10. `pr-author R7 denies with the preflight reason when the resolved checkpoint is unparseable` (identity to `shared/item-own-invalid-json`)
11. `pr-author R7 denies with the preflight reason when the resolved checkpoint is empty` (identity to `shared/item-own-empty`)
12. `pr-author R8 denies with the no-target code when the branch is checked out in no live worktree` (identity `--head f5-fixture-missing`)
13. `pr-author R9 denies with the ambiguity code when the branch is checked out in two live worktrees` (identity; the mock returns two roots for the branch)
14. `pr-author R10 allows a command that is not a gated gh pr invocation when the target is unresolvable`
15. `pr-author genuine-absence and target-resolution reason codes never appear in each other's decisions`
16. `pr-author denies an unresolved <Status> target without reaching the orchestrator-state preflight`, `-ForEach` over `NoTarget` and `Ambiguous` (seam; `Invoke-OrchestratorStatePreflight` mocked to throw)
17. `pr-author passes one resolved checkpoint path to both the preflight and the epic base-branch check` (seam with a synthetic root; both readers mocked to capture `-CheckpointPath`; expected value is the normalised synthetic root followed by `/artifacts/orchestration/orchestrator-state.json`)

`tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` (prompt `P901` = `Plan the change for the item.` followed by a line `Canonical issue number for this feature is 901. All artifact content, file paths, and cross-references must use this number.`):
1. `model-routing R1 allows when the resolved own checkpoint records the receipt` (identity `P901`; live `model-routing/session-root`, `model-routing/item-own-receipt`; cwd `model-routing/session-root`)
2. `model-routing R2 denies with the no-target code when only the sibling session-root checkpoint is present` (no mock; cwd `model-routing/session-root`; prompt `Plan the change for the item.`)
3. `model-routing R2 denies with the no-target code when the only signal is a repository-relative feature folder` (no mock; prompt = the prompt at `evidence/baseline/repro-3-4-control-pair.md:28`)
4. `model-routing R3 denies with the blocked reason when the resolved own checkpoint records no receipt` (identity `P901` to `model-routing/item-own-no-receipt`)
5. `model-routing R3 denies with the blocked reason when the checkpoint is absent at the resolved target` (identity `branch: f5-fixture-own` to `shared/item-no-checkpoint`)
6. `model-routing R4 takes the verdict from the own checkpoint located by issue number when the sibling checkpoint records the receipt` (identity `P901`; live `model-routing/session-root`, `model-routing/item-own-no-receipt`; cwd `model-routing/session-root`)
7. `model-routing R4 selects the branch-named worktree when a stale attempt records the same issue` (identity `P901` plus `branch: f5-fixture-own`; live `model-routing/session-root`, `model-routing/item-stale-receipt`, `model-routing/item-own-no-receipt`; branch maps to `item-own-no-receipt`; expects the blocked reason)
8. `model-routing R5 denies with the no-target code when the prompt names no target`
9. `model-routing R6 allows when the working directory is the item worktree and its own receipt is present` (identity `P901`; live `model-routing/item-own-receipt`; cwd the same)
10. `model-routing R7 denies with the blocked reason when the resolved checkpoint is unparseable` (identity branch to `shared/item-own-invalid-json`)
11. `model-routing R7 denies with the blocked reason when the resolved checkpoint is empty` (identity branch to `shared/item-own-empty`)
12. `model-routing R8 denies with the no-target code when the issue is recorded in no live worktree` (prompt names issue 902)
13. `model-routing R9 denies with the ambiguity code when the issue and the branch name different worktrees` (`P901` plus `branch: f5-fixture-sibling` mapped to `model-routing/session-root`)
14. `model-routing R9 denies with the ambiguity code when two live worktrees record the issue` (`P901`; live `item-stale-receipt` and `item-own-no-receipt`)
15. `model-routing R10 allows a subagent outside the gated set when the target is unresolvable`
16. `model-routing genuine-absence and target-resolution reason codes never appear in each other's decisions`
17. `model-routing denies an unresolved <Status> target without reaching the checkpoint read`, `-ForEach` over `NoTarget` and `Ambiguous` (seam; `Get-ModelRoutingCheckpoint` mocked to throw)
18. `model-routing reads the checkpoint at the resolved absolute path for a <Status> target`, `-ForEach` over `SessionRoot` and `OtherWorktree` (seam with a synthetic root; reader mocked to capture)

`tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1`:
- `every production call to Invoke-OrchestratorStatePreflight supplies CheckpointPath explicitly`
- `every production call to Get-PrAuthorCheckpointContent supplies CheckpointPath explicitly`
- `every production call to Get-ModelRoutingCheckpoint supplies CheckpointPath explicitly`
- `every production call to Test-EpicBaseBranchOverride supplies CheckpointPath explicitly`
- `every production call to Test-PrAuthorReceiptVerification supplies CheckpointPath explicitly`
- `the four in-scope hook files carry the checkpoint path literal in no string expression`
- `the orchestrator-state module keeps its four hundred ninety-nine line count`

`tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1` (reads skill text with whitespace collapsed, section extracted by heading):
- `orchestrate skill archives a foreign per-feature checkpoint to the handoff folder`
- `parallel-orchestrate skill keeps the coordinator root free of a per-feature checkpoint`
- `epic-orchestrate skill keeps the coordinator root free of a per-feature checkpoint`
- `orchestrate skill requires the canonical issue line on every receipt-gated delegation`
- `orchestrate skill requires a branch label on every receipt-gated delegation`
- `orchestrate skill names every subagent type the model-routing gate receipt-gates` (dot-sources `.claude/hooks/enforce-model-routing-receipt.ps1` and compares `Get-ModelRoutingGatedAgent` with the names in the `## Issue Number Consistency` rule)

`tests/scripts/claude-hooks/enforce-pr-author-skill.TargetResolution.Tests.ps1` renames ([P5-T5]): `denies with TARGET_WORKTREE_NOT_DERIVABLE when the call names no target` becomes `denies with the no-target code when the call names no target`; `denies with TARGET_WORKTREE_AMBIGUOUS when signals disagree` becomes `denies with the ambiguity code when signals disagree`; `uses the session-root path unchanged when the target resolves to the session root` becomes `uses the absolute session-root checkpoint path when the target resolves to the session root`.

---

### Phase 0 — Policy Reading, Current-Tree Verification, and Baseline Capture

- [ ] [P0-T1] Read, in order, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/powershell.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md`, `.claude/rules/orchestrator-state.md`, `.claude/rules/plan-acceptance-gates.md`, and test whether `quality-tiers.yml` exists at the repository root. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-instructions-read.md` with `Timestamp:`, `Policy Order:` (the ten paths in order read), `Files Read:` (the same ten), and one line `QUALITY_TIERS_YML: present` or `QUALITY_TIERS_YML: absent`. Acceptance: three labels present, ten entries under `Files Read:`, exactly one `QUALITY_TIERS_YML:` line.
- [ ] [P0-T2] Read `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`, `.../issue.md`, `.../research/2026-09-13T22-10-false-approval-elimination-research.md`, `.../plan.2026-09-13T20-48.md`, `.../plan.2026-09-18T13-30.md`, and this plan. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-feature-documents-read.md` with `Timestamp:`, `Files Read:` (six paths), and `AC Inventory:` listing the identifiers under `spec.md` `## Acceptance Criteria`. Acceptance: exactly 33 identifiers, `AC-1` through `AC-33`.
- [ ] [P0-T3] Record base and scope. Run `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, `git merge-base HEAD origin/main`, then `git diff --name-only <F5_BASE_SHA> HEAD` with the merge-base value substituted, then `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-base-ref.md` with the four schema fields, lines `F5_BRANCH:`, `F5_HEAD_SHA:`, `F5_BASE_SHA:`, and `Pre-Change Diff:` and `Porcelain:` sections holding both outputs verbatim. No toplevel path is recorded. Acceptance: `F5_BRANCH:` reads `bug/false-approval-elimination-673`; `F5_BASE_SHA:` is 40 hexadecimal characters; every path in both sections begins with `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/`; `Porcelain:` lists `plan.2026-09-18T16-00.md`.
- [ ] [P0-T4] Verify AC-1 to AC-3 on the branch. Confirm `evidence/baseline/repro-3-2-control-pair.md`, `evidence/baseline/repro-3-4-control-pair.md`, `evidence/baseline/repro-fixture-manifest.md`, and `evidence/baseline/repro-verdict.md` exist under the feature folder; count lines of `repro-verdict.md` equal to `REPRODUCTION: CONFIRMED`; run `git log --format=%H --diff-filter=A <F5_BASE_SHA>..HEAD -- docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/repro-verdict.md` and `git log --format=%H <F5_BASE_SHA>..HEAD -- .claude/hooks`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-reproduction-evidence-audit.md` with the four schema fields, the four existence results, the token count, `EVIDENCE_COMMIT:` (first log output), and `HOOK_COMMITS_ON_BRANCH:` (second log output, or `none`). Acceptance: four files exist; token count 1; `EVIDENCE_COMMIT:` is one 40-hexadecimal SHA; `HOOK_COMMITS_ON_BRANCH:` reads `none`.
- [ ] [P0-T5] Re-verify the cited current-tree facts with `Get-Content` line indexing and write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-current-tree-facts.md` (four schema fields) quoting each line. Lines and required contained tokens, in order: `.claude/hooks/enforce-pr-author-skill.ps1:49` `$script:OrchestratorStateCheckpointPath =`; `.claude/hooks/enforce-pr-author-skill-helpers.ps1:38` `WorktreeResolution.psm1`; `:39` `WorktreeTargetResolution.psm1`; `:61` `Resolve-WorktreeCallTarget`; `:96` `CheckpointPath = $script:OrchestratorStateCheckpointPath`; `:99` `-RepoRelativePath $script:OrchestratorStateCheckpointPath`; `:211` `Test-EpicBaseBranchOverride -CommandText $CommandText`; `:315` `Get-PrAuthorTargetCheckpointResolution`; `:320` `Invoke-OrchestratorStatePreflight -CheckpointPath`; `:333` `Test-PrAuthorReceiptVerification -CommandText $CommandText`; `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1:35` `$CheckpointPath = 'artifacts`; `:78` `$checkpointRaw = Get-PrAuthorCheckpointContent`; `.claude/hooks/enforce-model-routing-receipt.ps1:36` `HookPayload.psm1`; `:46` `$CheckpointPath = 'artifacts`; `:157` `$checkpoint = Get-ModelRoutingCheckpoint`; `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:103` `'Branch', 'SessionRoot'`; `:164` `function Find-WorktreeResolutionBranchSignal`; `.claude/lib/worktree-resolution/WorktreeResolution.psm1:228` `function Test-WorktreeResolutionRootMarker`; `:334` `function Get-WorktreeResolutionWorktreeRoot`; `.claude/hooks/enforce-prd-feature-before-planner.ps1:108` `WorktreeTargetResolution.psm1`; `.claude/skills/orchestrate/SKILL.md:258` `Every delegation prompt to`; `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1:312` `$script:OrchestratorStateCheckpointPath = 'artifacts`; `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1:97` `$script:OrchestratorStateCheckpointPath = 'artifacts`; `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py:129` `.claude/skills/epic-orchestrate/SKILL.md`. Also record `@(Get-Content).Count` of `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`, `.claude/hooks/enforce-model-routing-receipt.ps1`, `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`, `.claude/lib/orchestrator-state/OrchestratorState.psm1`, `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`. Acceptance: every quoted line contains its token; the counts are 312, 340, 115, 180, 500, 347, 499, 509 in that order. Any mismatch stops execution and is reported as a plan-revision requirement.
- [ ] [P0-T6] Audit the seven AC-19 rows against #687. Run `git log --format=%H -S Resolve-PrAuthorWorktreeTarget <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`, take the last SHA printed as the #687 commit, and for `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1` compare `git show <that commit>^:<file>` with `git show <that commit>:<file>`; for `tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1` run `git log --format=%H <F5_BASE_SHA> -1 -- tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1` and record that it differs from the #687 commit. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-ac19-row-audit.md` (four schema fields) with one row per protected row: spec location, current location, every current `It` name in the span, and `unchanged by #687` or the verbatim before and after text. [P10-T4] reads the `It` names from this artifact. Acceptance: seven rows; the end-to-end row records a pre-#687 name ending `(exit 0, deny, ORCHESTRATOR_STATE_PREFLIGHT_FAILED)` and a current name ending `(exit 0, deny, TARGET_WORKTREE_NOT_DERIVABLE)`; both #687 `BeforeEach` blocks quoted verbatim.
- [ ] [P0-T7] Establish the fresh-process route. Write `<SCRATCHPAD>/r3-probe.ps1` printing `PSVersion: <$PSVersionTable.PSVersion>` and `LocationIsWorktreeRoot: <bool>`, where the bool compares `(Get-Location).Path` with the output of `git rev-parse --show-toplevel` after normalising both to forward slashes (neither value is printed). Attempt route `a` (`pwsh -NoProfile -File <SCRATCHPAD>/r3-probe.ps1`); if refused, record the refusal with host tokens replaced and attempt route `a-prime` (`sh <SCRATCHPAD>/r3-probe.sh`, body per §5). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-execution-route.md` (four schema fields) with exactly one `ROUTE_SELECTED: a` or `ROUTE_SELECTED: a-prime` line and the probe output. Acceptance: one `ROUTE_SELECTED:` line; the probe prints `LocationIsWorktreeRoot: True`. If both routes are refused, record `ROUTE_SELECTED: none`, stop, and report the blocked state.
- [ ] [P0-T8] Formatter baseline. Record `git status --porcelain`; in a fresh process run `Invoke-PoshQCFormat -Root $root` per §5 with `scripts/powershell/PoshQC/PoshQC.psd1` imported; record `git status --porcelain` again. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-poshqc-format.md` (four schema fields) with the count of lines beginning `Formatted: `, the count beginning `Already formatted: `, and a `Tree Delta:` section with both porcelain outputs. If any tracked file was rewritten, revert each with `git checkout -- <path>`, list the repository-relative paths, and record a third porcelain output. Acceptance: both counts are numbers; the `Already formatted: ` count is greater than 0; the final porcelain output equals the first.
- [ ] [P0-T9] Analyzer baseline. In a fresh process run `Invoke-PoshQCAnalyze -Root $root` per §5 with `scripts/powershell/PoshQC/PoshQC.psd1` imported. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-poshqc-analyze.md` (four schema fields) with either the line beginning `PSScriptAnalyzer passed: no findings under` (root replaced by `<WORKSPACE_ROOT>`) or the `PSScriptAnalyzer reported <N> issue(s).` line and the findings table with host tokens replaced. Acceptance: exactly one outcome recorded, `N` numeric when findings exist, and the output does not contain `No PowerShell files found under`.
- [ ] [P0-T10] Full Pester baseline with coverage. In a fresh process run `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` with `scripts/powershell/PoshQC/PoshQC.psd1` imported. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pester-coverage.md` (four schema fields) with, per §5: report last-write time; root `tests`, `failures`, `errors`, `disabled`; the name of every `testcase` carrying a `failure` child (or `none`); `tests`, `failures`, `skipped`, `disabled`, and passed count for the testsuites ending `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, `enforcement-hooks-no-python-invocation.Tests.ps1`, `OrchestratorState.Tests.ps1`, `enforce-model-routing-receipt.Tests.ps1`, `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.Tests.ps1`, `enforce-prd-feature-before-planner.Tests.ps1`, `enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`, `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`; overall line coverage; and per-file line coverage (percent, `covered`, `missed`) and `line`-element count for the four in-scope hooks. Acceptance: last-write time at or after `Timestamp:`; all ten testsuites present; four per-file percentages, one overall percentage, eight raw counters, and four `line`-element counts recorded as numbers. A non-zero failure count is recorded, not waived.
- [ ] [P0-T11] Python suite baseline. Run `poetry run pytest -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pytest.md` (four schema fields) with the final summary line verbatim and the node ID of every failed or errored test (or `none`). Acceptance: the summary line is recorded with a numeric passed count.
- [ ] [P0-T12] Python format baseline for the one Python file this plan edits. Run `poetry run black --check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-black.md` (four schema fields) with the output verbatim. Acceptance: `EXIT_CODE: 0` and the output contains `would be left unchanged`.
- [ ] [P0-T13] Python lint baseline. Run `poetry run ruff check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-ruff.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `All checks passed!`.
- [ ] [P0-T14] Python type baseline. Run `poetry run pyright tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pyright.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `0 errors`.
- [ ] [P0-T15] Mirror baseline. Compute `Get-FileHash -Algorithm SHA256` for `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`, `.claude/hooks/enforce-model-routing-receipt.ps1`, `.claude/skills/orchestrate/SKILL.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/skills/epic-orchestrate/SKILL.md` and each counterpart under `extensions/drm-copilot/resources/claude-customizations/`, and for `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` against `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-mirror-sha.md` (four schema fields), one row per pair with repository-relative paths. Acceptance: eight rows, sixteen 64-hexadecimal hashes, each pair marked equal or unequal.
- [ ] [P0-T16] Host-token inventory. In a fresh process, compute at run time the set of worktree roots from `git worktree list --porcelain`, the user profile directory, and the account name, and for every file under `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/` count lines matching any of: the regex `(?<![A-Za-z])[A-Za-z]:[\\/]`, the regex `(?<![A-Za-z0-9._-])/[A-Za-z]/[A-Za-z]`, the regex `[\\/](Users|home)[\\/]`, or a case-insensitive simple match of the account name. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-host-token-inventory.md` (four schema fields) listing each file with a non-zero count and its count, with no matched text. Acceptance: every listed path is repository-relative and each count is a number; `plan.2026-09-18T16-00.md` has count 0.

### Phase 1 — Spec Amendment, Binding Refresh, Redaction, and Fixture-Collision Check

- [ ] [P1-T1] Refresh `##### BINDING TABLE` in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: update each existing row's `file:line` to the current tree (derivation `WorktreeTargetResolution.psm1:233`, exported `:346`; sibling import `:27`; normalisation `WorktreeResolution.psm1:421`, exported `:498`; ambiguity accessor `:463`, exported `:499`; ambiguity literal `:55`, returned `:472`) and append `<NO_TARGET_REASON_CODE> accessor function name` = `Get-WorktreeResolutionNoTargetReasonCode` (`WorktreeResolution.psm1:475`, exported `:500`) and `<NO_TARGET_REASON_CODE> literal value returned by that accessor` = `TARGET_WORKTREE_NOT_DERIVABLE` (`:59`, returned `:487`). Archive the eight-row table with each cited source line quoted in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-f1-binding-table.md` (four schema fields). Acceptance: the spec table has eight rows, none containing `_unbound_`; the artifact quotes eight source lines each containing the identifier its row names.
- [ ] [P1-T2] Amend `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: set `**Version:** 0.4` and `**Last Updated:** 2026-09-18T16-00`; insert immediately before `## Acceptance Criteria` a section headed `## Revision 0.4 — Identity-Based Target Resolution` containing §1.1, §2.1 to §2.5, and the §3 table of this plan verbatim; and append after AC-33, as a new subsection `### Identity resolution, checkpoint hygiene, and host data (Revision 0.4)`, the five criteria quoted in §9 of this plan verbatim. Acceptance: `spec.md` contains the heading `## Revision 0.4 — Identity-Based Target Resolution` once, and its `## Acceptance Criteria` section now lists exactly 38 identifiers `AC-1` through `AC-38`.
- [ ] [P1-T3] Verify restated AC-21. Run `git log --format=%H --diff-filter=A origin/main -- .claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`, then `git merge-base --is-ancestor <that SHA> origin/main` and record its exit code; count occurrences of `.claude/lib/worktree-resolution/WorktreeResolution.psm1` and `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`; copy the `WorktreeResolution.Manifest.Tests.ps1` values from [P0-T10]. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-f1-on-main.md` (four schema fields). Acceptance: ancestry exit code 0; each count exactly 1; the testsuite shows `failures` 0 and a passed count greater than 0.
- [ ] [P1-T4] Fixture-collision check. Search `.claude/hooks`, `.claude/lib`, `.claude/rules`, `scripts/dev_tools`, and `scripts/bash` recursively with `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` and classify each matching file as root-anchored, suffix-matching, or documentation, and whether it enumerates `tests/fixtures/`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-fixture-collision-check.md` (four schema fields), one row per matching file with file and line. Acceptance: every matching file has a row; the row for `.claude/hooks/enforce-checkpoint-monotonic.ps1` cites `:196` as `suffix-matching`; a final line reads `COLLISION_BLOCKS_FIXTURES: NO` or `COLLISION_BLOCKS_FIXTURES: YES`. On `YES`, stop and report.
- [ ] [P1-T5] Redact host tokens from every file listed by [P0-T16] except `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-13T20-48.md` and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-18T13-30.md`, which are not edited. In a fresh process, with values computed at run time and never written, replace in this order and in both separator spellings: each worktree root with `<WORKTREE_ROOT>`; the user profile directory (including its `/<drive>/...` shell spelling) with `<HOME>`; a program-files prefix with `<PROGRAM_FILES>`; remaining occurrences of the account name with `<USER>`; then any remaining match of the [P0-T16] path regexes, up to the next whitespace, quote, or backtick, with `<HOST_PATH>`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-host-token-redaction.md` (four schema fields) with, per file, the repository-relative path and the replacement count per placeholder. Acceptance: re-running the [P0-T16] count over the redacted files returns 0 for each; every `permissionDecision` value and every reason-code token in `evidence/baseline/repro-3-2-control-pair.md` and `evidence/baseline/repro-3-4-control-pair.md` is unchanged, compared with `git show HEAD:<path>`.
- [ ] [P1-T6] Commit Phases 0 and 1. Stage `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673` with `git add` and a repository-relative pathspec; commit with the message read from `-F <SCRATCHPAD>/r3-msg-p1.txt`. If a PreToolUse gate denies, record its reason verbatim with host tokens replaced and report rather than routing around it. Append the command lines and gate response to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md` (four schema fields). Acceptance: `git log -1 --name-only` lists `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md` and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-18T16-00.md`, and no path outside the feature folder; `git status --porcelain` lists nothing under the feature folder.

### Phase 2 — Identity Resolution Library Module (batch B1)

- [ ] [P2-T1] Reset the batch budget per §6 and append entry `B1` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B1` entry lists deleted file names or `none present`.
- [ ] [P2-T2] Create `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1` implementing §2.1 and §2.2 exactly: comment-based help containing the line `CONVENTION: this module fails fast at module scope and imports its siblings with -ErrorAction Stop.`; `Set-StrictMode -Version Latest` followed immediately by `$ErrorActionPreference = 'Stop'`; column-0 `Import-Module (Join-Path $PSScriptRoot 'WorktreeResolution.psm1') -ErrorAction Stop` and the same for `WorktreeTargetResolution.psm1`; the eight functions of §2.1 with comment-based help, exported by one `Export-ModuleMember -Function` statement; an intent comment above every loop and a decision comment above every multi-branch conditional. The module contains neither reason-code literal and no `SilentlyContinue` outside `Get-WorktreeItemCheckpointText`. Acceptance: the file exists, is at most 400 lines, contains the convention sentence verbatim, contains `$ErrorActionPreference = 'Stop'` on the line after `Set-StrictMode -Version Latest`, and a `Select-String -SimpleMatch` for `TARGET_WORKTREE_` returns no match.
- [ ] [P2-T3] Create `tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1` with the 27 `It` names of §7 for this file, Arrange-Act-Assert structure, expected codes obtained only from the two F1 accessors, synthetic roots composed per §4, and mocks registered inside module `WorktreeItemResolution`. Acceptance: the file exists, is at most 500 lines, contains the 27 names verbatim, and contains no drive-letter path, `TestDrive`, `New-TemporaryFile`, or either reason-code literal.
- [ ] [P2-T4] Register the module: insert `".claude/lib/worktree-resolution/WorktreeItemResolution.psm1",` after `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json:170`; add `'.claude/lib/worktree-resolution/WorktreeItemResolution.psm1'` to `$script:ExpectedPaths` and to each of the three `-ForEach` lists in `tests/scripts/claude-lib/worktree-resolution/WorktreeResolution.Manifest.Tests.ps1` (`:27-30`, `:34-37`, `:45-48`, `:72-75`); add the same path after `scripts/powershell/PoshQC/settings/pester.runsettings.psd1:295` with a one-line issue #673 comment. Acceptance: `Select-String -SimpleMatch` counts of the path are 1 in `core.json`, 4 in the manifest test, and 1 in the runsettings file; `core.json` still parses with `ConvertFrom-Json`.
- [ ] [P2-T5] Mirror by byte copy per §6: `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1` to `extensions/drm-copilot/resources/claude-customizations/.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` to `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`. Record both commands (repository-relative) and SHA-256 pairs in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md` (four schema fields; appended by every later mirror task). Acceptance: both pairs equal.
- [ ] [P2-T6] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p2-scoped-pester.md` (four schema fields) with, per §5, report last-write time, root `failures` and `errors`, the testsuites ending `WorktreeItemResolution.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, and `ClaudeLibModuleConvention.Tests.ps1`, and the line coverage of `WorktreeItemResolution.psm1`. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0; the first testsuite has 27 passed; the other three have `failures` 0; module line coverage at least 85%.
- [ ] [P2-T7] Append a ninth row to the spec binding table in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: `<TARGET_DERIVATION> identity form (Revision 0.4)` = `Resolve-WorktreeItemTarget` with the `file:line` of its definition and export in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`; append both quoted lines to `evidence/other/r3-f1-binding-table.md`. Acceptance: the table has nine rows and each quoted line contains `Resolve-WorktreeItemTarget`.

### Phase 3 — Committed Fixture Roots

- [ ] [P3-T1] Create the committed fixtures of §4.1 under `tests/fixtures/worktree-resolution/`. Every checkpoint is at `artifacts/orchestration/orchestrator-state.json` under its root. Base JSON is the checkpoint at `evidence/baseline/repro-fixture-manifest.md:53-100` verbatim (its `issue-num` is `838`), with the per-root changes of §4.1; the `901` roots change only `objective` where stated and `issue-num`. The pr-author artifact files: `artifacts/pr_context.summary.txt` one line of text; `artifacts/pr_body_1.md` exactly `Sibling item A pull request body.` followed by one LF; `artifacts/pr_body_1.receipt.json` `{"number": 1, "sha256": "<SHA-256 of that body file>", "created_at": "2099-01-01T00:00:00Z"}`. Add `tests/fixtures/worktree-resolution/README.md` stating purpose, the §4.1 map, and the schema dependency on `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`. Record any PreToolUse interception and its resolution in `evidence/other/r3-fixture-collision-check.md`. Acceptance: `git status --porcelain --untracked-files=all -- tests/fixtures/worktree-resolution` lists every file of §4.1 plus `README.md`; `shared/item-own-empty/artifacts/orchestration/orchestrator-state.json` has length 0.
- [ ] [P3-T2] Fixture sanity check. In a fresh process import `.claude/lib/orchestrator-state/OrchestratorState.psm1` and `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`; run `Invoke-OrchestratorStatePreflight -CheckpointPath <path>` (absolute path composed at run time from `$root`) on the checkpoints of `pr-author/session-root`, `pr-author/item-own-ready`, `pr-author/item-own-epic-mode`, `pr-author/item-own-not-ready`, `shared/item-own-invalid-json`, `shared/item-own-empty`, and the absent `shared/item-no-checkpoint` path; run `Get-WorktreeItemCheckpointIssue` on every root of §4.1; recompute SHA-256 of both `pr_body_1.md` files and count their CR bytes. If `item-own-epic-mode` reports errors, add exactly the fields its error text names, re-run, and record both runs. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-committed-fixture-manifest.md` (four schema fields) with repository-relative fixture paths, each checkpoint's content, the §7 rows consuming it, and the results. Acceptance: `HasErrors` is `False` for the first three and `True` for the other four; the issue results equal the §4.1 column (`unknown` and `none` recorded as empty); both body hashes equal the receipts' `sha256`; both CR counts are 0.

### Phase 4 — Matrix Tests Authored Against the Unmodified Hooks (batch B2)

- [ ] [P4-T1] Reset the batch budget per §6; append entry `B2` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B2` entry exists.
- [ ] [P4-T2] Create `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` defining `Get-WorktreeResolutionFixturePath -RelativePath` (normalised absolute path under `tests/fixtures/worktree-resolution/`, computed from `$PSScriptRoot`), `Invoke-WorktreeResolutionFixtureCall -WorkingDirectory -ScriptBlock` (per §4, both locations restored in `finally`), and `New-WorktreeResolutionFixtureTarget -Status -WorktreeRoot [-Candidate]` (wraps `New-WorktreeResolutionTargetResult`). Acceptance: the file exists, is at most 200 lines, defines the three functions with `[CmdletBinding()]` and `[OutputType()]`, and contains none of `New-TemporaryFile`, `GetTempPath`, `GetTempFileName`, `TestDrive`.
- [ ] [P4-T3] Create `tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.Tests.ps1` with the 17 `It` definitions of §7 (18 expanded names), the §4 import order, a head comment naming `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`, and expected codes only from the two accessors. Rows 3 and 8 register no mock of any kind. Acceptance: the file exists, is at most 500 lines, contains the 18 expanded names verbatim, and contains neither reason-code literal and no drive-letter path. If it would exceed 500 lines, move the rows that use no fixture unchanged into `tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.InProcess.Tests.ps1` after an additional §6 reset recorded as `B2b`; rows 1 to 7 stay in the named file.
- [ ] [P4-T4] Create `tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` with the 18 `It` definitions of §7 (20 expanded names) under the rules of [P4-T3]. Rows 17 and 18 mock `Resolve-ModelRoutingWorktreeTarget` inside their own `It` only, because that function does not exist before [P7-T2]. Acceptance: the file exists, is at most 500 lines, contains the 20 expanded names verbatim, and contains neither reason-code literal and no drive-letter path; the split rule of [P4-T3] applies, with rows 1 to 7 kept in the named file.
- [ ] [P4-T5] [expect-fail] Run the new suites against the unmodified hooks. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-fail-before-matrix.md` (four schema fields) with the report last-write time and the name and status of every `testcase` under the two `WorktreeResolution` testsuites. Acceptance: last-write time at or after `Timestamp:`; both testsuites present; Failed: `model-routing R2 denies with the no-target code when only the sibling session-root checkpoint is present`, `model-routing R2 denies with the no-target code when the only signal is a repository-relative feature folder`, `model-routing R4 takes the verdict from the own checkpoint located by issue number when the sibling checkpoint records the receipt`, `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present`; Passed: `pr-author R2 denies with the no-target code when only the sibling session-root checkpoint is present`, `pr-author R1 allows when the resolved own checkpoint is ready and the working directory is the sibling session root`, `pr-author R6 allows when the working directory is the item worktree and its own checkpoint is ready`, `model-routing R1 allows when the resolved own checkpoint records the receipt`, `model-routing R6 allows when the working directory is the item worktree and its own receipt is present`; every pre-existing testsuite under `tests/scripts/claude-hooks` has `failures` 0 apart from [P0-T10] exclusions.
- [ ] [P4-T6] Write the AC-18 pre-fix record `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-prefix-r1-r6.md` (four schema fields; `Command:` names the [P4-T5] command) quoting the four R1 and R6 `testcase` entries named in [P4-T5], and the dossier `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/fail-before-exception.2026-09-18T16-00.md` with `Timestamp:`, `WhyFailingRunImpossible:` (pr-author R2 already denies on this tree because #687 added the no-target deny; commit from [P0-T6]), and an alternative-proof section citing `evidence/baseline/repro-3-2-control-pair.md`, `evidence/baseline/repro-verdict.md`, and the Failed status of the pr-author R4 epic row. Acceptance: four Passed entries; the dossier carries both fields and the three citations.
- [ ] [P4-T7] Commit Phases 2 to 4. Stage exactly `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `extensions/drm-copilot/resources/claude-customizations/.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, the two B1 test files, `tests/fixtures/worktree-resolution`, the B2 test files, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with `git add` and repository-relative pathspecs; commit with `-F <SCRATCHPAD>/r3-msg-p4.txt`. `tests/fixtures/` is not in the issue #539 exempt set, so the pre-implementation gate reads the executing worktree's checkpoint; if it denies, record the `PREIMPLEMENTATION_GATE_BLOCKED` reason verbatim with host tokens replaced and report rather than routing around it. Append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `tests/fixtures/worktree-resolution/README.md` and `tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` and no path beginning `.claude/hooks/`; `git status --porcelain` lists nothing under the staged locations.

### Phase 5 — pr-author Family Implementation (batch B3)

- [ ] [P5-T1] Reset the batch budget per §6; append entry `B3` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B3` entry exists.
- [ ] [P5-T2] In `.claude/hooks/enforce-pr-author-skill.ps1`, replace `:49` with `$script:OrchestratorStateCheckpointPath = $null` and a comment of at most two lines stating that the resolution step assigns the absolute path. Acceptance: `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` over the file matches no non-comment line, and the file declares `$script:OrchestratorStateCheckpointPath = $null` exactly once.
- [ ] [P5-T3] In `.claude/hooks/enforce-pr-author-skill-helpers.ps1` apply DD-1 and DD-2: add the `WorktreeItemResolution.psm1` import after `:39`; change the body of `Resolve-PrAuthorWorktreeTarget` (`:61`); merge the `SessionRoot` and `OtherWorktree` branches of `Get-PrAuthorTargetCheckpointResolution` into one returning `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot`, deny branch unchanged; rewrite the doc comment at `:69-81` and the header at `:18-21` to name the two accessor functions and describe identity resolution, containing neither reason-code literal; add `[Parameter(Mandatory)] [string] $CheckpointPath` to `Test-PrAuthorReceiptVerification` and pass it at `:211` as `Test-EpicBaseBranchOverride -CommandText $CommandText -CheckpointPath $CheckpointPath`; in `Get-PrAuthorBypassReason` assign `$script:OrchestratorStateCheckpointPath = $resolution.CheckpointPath` after the deny check and pass it to `Invoke-OrchestratorStatePreflight -CheckpointPath` and `Test-PrAuthorReceiptVerification -CheckpointPath`. Acceptance: the file is at most 500 lines; it contains neither reason-code literal and no `SilentlyContinue`; `Resolve-WorktreeCallTarget` no longer occurs; `Get-WorktreeItemCheckpointPath` occurs exactly once.
- [ ] [P5-T4] In `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`: make `Get-PrAuthorCheckpointContent`'s parameter `[Parameter(Mandatory)] [string] $CheckpointPath` with no default; add `[Parameter(Mandatory)] [string] $CheckpointPath` to `Test-EpicBaseBranchOverride`; change `:78` to `$checkpointRaw = Get-PrAuthorCheckpointContent -CheckpointPath $CheckpointPath`; update the doc comments at `:23` and `:26-28` to describe the resolved absolute path. Acceptance: the file is at most 500 lines and no line assigns a default to `$CheckpointPath`.
- [ ] [P5-T5] In `tests/scripts/claude-hooks/enforce-pr-author-skill.TargetResolution.Tests.ps1`: rename the three tests per §7; replace every reason-code literal with the matching accessor call (`ReasonCode = (Get-WorktreeResolutionNoTargetReasonCode)`; `Should -BeLike ("{0}*" -f (Get-WorktreeResolutionNoTargetReasonCode))`, and the ambiguity equivalent); replace the synthetic drive-letter roots at `:26`, `:37`, `:43` with roots composed per §4; change the assertion at `:54` to expect the normalised composed session root followed by `/artifacts/orchestration/orchestrator-state.json`. Acceptance: the file contains neither reason-code literal, no drive-letter path, and the three new names verbatim.
- [ ] [P5-T6] Apply AC-19 form (a): append ` -CheckpointPath (Join-Path $PSScriptRoot 'no-such-checkpoint.json')` to each `Test-EpicBaseBranchOverride` call at `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1:34`, `:40`, `:48`, `:58`, `:68`, `:78`, `:84` and `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1:38`, `:52`, changing nothing else. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md` (four schema fields) quoting each edited line before and after, marking the three inside AC-19's protected rows. Acceptance: nine before/after pairs, each after-line differing from its before-line only by the appended argument.
- [ ] [P5-T7] Mirror `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, and `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1` by byte copy to the same relative paths under `extensions/drm-copilot/resources/claude-customizations/`; append commands and SHA-256 pairs to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: three equal pairs.
- [ ] [P5-T8] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p5-scoped-pester.md` (four schema fields) with the report last-write time, the `tests` and `failures` of every testsuite whose name ends with a file named `enforce-pr-author-skill*.Tests.ps1`, `enforce-prd-feature-before-planner*.Tests.ps1`, or `enforce-model-routing-receipt.Tests.ps1`, and every `testcase` status of `enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` (still pre-fix and exempt). Acceptance: last-write time at or after `Timestamp:`; every listed pr-author testsuite, including `enforce-pr-author-skill.WorktreeResolution.Tests.ps1`, the three prd-feature testsuites, and `enforce-model-routing-receipt.Tests.ps1` have `failures` 0.

### Phase 6 — OrchestratorState.Tests.ps1 Pin Replacement and Size Reduction (batch B4)

- [ ] [P6-T1] Reset the batch budget per §6; append entry `B4` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B4` entry exists.
- [ ] [P6-T2] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.ValueContract.Tests.ps1` holding the `Describe 'Get-OrchestratorStateCheckpoint value contract'` block moved verbatim from `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1:485-508`, preceded by a `BeforeAll` reproducing `:23-28` (module import) and the `New-ReadyCheckpoint` and `Set-CheckpointFixture` helpers at `:34-68`; then delete `:485-508` and the blank line before them from the source file. Acceptance: both `It` names of that block appear in the new file and in no other file under `tests/scripts/claude-lib/orchestrator-state/`.
- [ ] [P6-T3] In `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, inside `It 'blocks a not-ready checkpoint with ORCHESTRATOR_STATE_PREFLIGHT_FAILED through the default path'`, replace the pin at `:312` with `$script:MissingWorktreeRoot = Join-Path $PSScriptRoot 'no-such-worktree-fixture'` and a `Mock -CommandName Resolve-PrAuthorWorktreeTarget` returning `Status = 'OtherWorktree'`, `WorktreeRoot = $script:MissingWorktreeRoot`, `ReasonCode = $null`, `Detail = 'absent fixture worktree'`, leaving every assertion line unchanged. Append the before and after text to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md`, labelled as outside AC-19's rows. Acceptance: the file is at most 500 lines and no longer contains `nonexistent-fixture.json`.
- [ ] [P6-T4] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p6-scoped-pester.md` (four schema fields) with the report last-write time, root `failures` and `errors`, the testsuites ending `OrchestratorState.Tests.ps1` and `OrchestratorState.ValueContract.Tests.ps1`, and both files' line counts. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0; the value-contract testsuite has 2 passed; both line counts at most 500.

### Phase 7 — model-routing Family Implementation (batch B5)

- [ ] [P7-T1] Reset the batch budget per §6; append entry `B5` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B5` entry exists.
- [ ] [P7-T2] In `.claude/hooks/enforce-model-routing-receipt.ps1` apply DD-3: the import after `:36`; `Get-ModelRoutingCheckpoint` parameter mandatory with no default; `Resolve-ModelRoutingWorktreeTarget` (`[CmdletBinding()]`, `[OutputType([pscustomobject])]`, `[Parameter(Mandatory)] [AllowEmptyString()] [string] $PromptText`); `Get-ModelRoutingTargetCheckpointResolution` (`[OutputType([System.Collections.Specialized.OrderedDictionary])]`, reason text exactly as DD-3); in `Invoke-ModelRoutingReceiptDecision`, between the scope filter and the read, obtain `prompt` with `Get-ClaudeHookToolInputString` (an absent prompt is passed as an empty string), resolve, return a deny carrying `$resolution.Reason` when set, otherwise call `Get-ModelRoutingCheckpoint -CheckpointPath $resolution.CheckpointPath`; update the header (`:7-9`, `:26-27`) to describe identity resolution and the delegation-identity contract. Acceptance: the file is at most 500 lines; it contains no `SilentlyContinue`, no reason-code literal, and no non-comment occurrence of `orchestrator-state.json`; the six decision branches of spec `### Boundaries and invariants to preserve` remain, with the new deny between branches 3 and 4.
- [ ] [P7-T3] In `tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1`, add one Describe-level `BeforeEach` directly after the `BeforeAll` ending at `:17`, mocking `Resolve-ModelRoutingWorktreeTarget` to return `[pscustomobject]@{ Status = 'SessionRoot'; WorktreeRoot = (Get-Location).Path; ReasonCode = $null; Detail = 'session root' }`, with a two-line comment citing issue #673 (RS-8 form (c)). No other line changes. Append the inserted block to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md`. Acceptance: `git diff <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1`, with the recorded SHA substituted, shows added lines only.
- [ ] [P7-T4] Create `tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1` with the seven `It` names of §7. The five call-site tests parse every `.ps1` and `.psm1` under `.claude/hooks` and `.claude/lib` with `[System.Management.Automation.Language.Parser]::ParseFile`, collect each `CommandAst` whose command name is the target function, assert the collection is non-empty, and assert each carries a `CommandParameterAst` named `CheckpointPath`. The literal test asserts that no `StringConstantExpressionAst` or `ExpandableStringExpressionAst` in the four in-scope hook files contains `orchestrator-state.json`. The line-count test asserts `@(Get-Content -LiteralPath .claude/lib/orchestrator-state/OrchestratorState.psm1).Count` equals 499, with the path resolved from `$PSScriptRoot`. Acceptance: the file exists with the seven names verbatim and creates no file.
- [ ] [P7-T5] Mirror `.claude/hooks/enforce-model-routing-receipt.ps1` by byte copy to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-model-routing-receipt.ps1`; append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: one equal pair.
- [ ] [P7-T6] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks,tests/scripts/claude-runtime,tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p7-scoped-pester.md` (four schema fields) with the report last-write time, root `failures` and `errors`, and every `testcase` name and status under the two `WorktreeResolution` hook testsuites, `WorktreeItemResolution.Tests.ps1`, and `enforcement-hooks-checkpoint-path-explicit.Tests.ps1`. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0 apart from [P0-T10] exclusions; every §7 name for those four files appears exactly once with status Passed.
- [ ] [P7-T7] Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-fail-before-pass-after.md` (four schema fields) pairing [P4-T5]'s Failed status with [P7-T6]'s Passed status for the four rows [P4-T5] requires Failed, and citing `fail-before-exception.2026-09-18T16-00.md` for the pr-author R2 row. Acceptance: four Failed-then-Passed pairs and the dossier citation.
- [ ] [P7-T8] Commit Phases 5 to 7 with the method and gate handling of [P4-T7], staging the four hook files, their four mirrors under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/`, every test file modified or created in B3 to B5, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with commit message `-F <SCRATCHPAD>/r3-msg-p7.txt`. Append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `.claude/hooks/enforce-model-routing-receipt.ps1` and `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-model-routing-receipt.ps1`; `git status --porcelain` lists nothing outside the feature folder.

### Phase 8 — Skill Contracts: Checkpoint Hygiene and Delegation Identity (batch B6)

- [ ] [P8-T1] Reset the batch budget per §6; append entry `B6` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B6` entry exists.
- [ ] [P8-T2] Edit `.claude/skills/orchestrate/SKILL.md`. In `## Checkpoint Handling` append step 4 beginning `4. **Checkpoint hygiene (issue #673).**` stating: when the checkpoint at `artifacts/orchestration/orchestrator-state.json` records an `issue-num` other than the item this invocation is about, move it to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json` before writing a new checkpoint, rather than overwriting it or leaving it; apply the same move when this session hands an item whose checkpoint it holds to another session or worktree; and give the reason in one factual sentence (a checkpoint left at a session root is read by gates that resolve against that root). In `## Issue Number Consistency` replace the sentence at `:258` with one requiring the line on every delegation prompt to a receipt-gated subagent type, naming `atomic-planner`, `atomic-executor`, `feature-review`, `task-researcher`, `prd-feature`, and `pr-author`; after the quoted line add a paragraph beginning `Each such prompt also carries a` requiring a `branch: <name>` label naming the branch checked out in the item's worktree, written as the first `branch:` occurrence in the prompt, required alone before promotion assigns an issue number, and stating that `enforce-model-routing-receipt.ps1` identifies the item from these two lines, denies a gated delegation it cannot identify, and never uses a feature-folder path to choose the worktree. Neither edit adds a `##` heading or a reason-code literal. Acceptance: the file contains `Checkpoint hygiene (issue #673)` once, `orchestrator-state.issue-` once, and `Each such prompt also carries a` once; its `##` heading count is unchanged from `F5_BASE_SHA`.
- [ ] [P8-T3] Edit `.claude/skills/parallel-orchestrate/SKILL.md`: append to the end of `## Per-Item Branch and Worktree Lifecycle` (after the paragraph ending at `:235`) a paragraph beginning `**Checkpoint hygiene (issue #673).**` stating that the coordinating session never holds a per-feature checkpoint at its own root; before the first item delegation of a run it moves any `artifacts/orchestration/orchestrator-state.json` at its root to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json` and writes none there for the rest of the run, because each item's checkpoint lives in that item's worktree; and that a gated call the coordinator issues on an item's behalf is resolved by the item's issue number and branch, never by the coordinator root. The paragraph adds no `##` heading and contains none of `Epic mode: true`, `--base epic/`, `integration-to-main`. Acceptance: the file contains `Checkpoint hygiene (issue #673)` once and `orchestrator-state.issue-` once; its `##` heading count is 16.
- [ ] [P8-T4] Edit `.claude/skills/epic-orchestrate/SKILL.md`: append to the end of `## Epic-Level Checkpoint` (after the paragraph ending at `:293`) the paragraph of [P8-T3], with "item delegation" read as "child delegation". Acceptance: the file contains `Checkpoint hygiene (issue #673)` once and `orchestrator-state.issue-` once; its `##` heading count is unchanged from `F5_BASE_SHA`.
- [ ] [P8-T5] Re-baseline the frozen-surface pin in `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`: replace the digest at `:130` with the lowercase SHA-256 of `.claude/skills/epic-orchestrate/SKILL.md` after [P8-T4], and add to the comment block at `:105-122` a paragraph beginning `RE-BASELINED by issue #673.` stating that the checkpoint-hygiene paragraph was added to `## Epic-Level Checkpoint` and that the pin stays live for `.claude/agents/epic-orchestrator.md`, whose digest at `:126` is unchanged. Record the old and new digests in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-frozen-pin-rebaseline.md` (four schema fields). Acceptance: the new digest equals `Get-FileHash -Algorithm SHA256` of the edited skill (lowercased); the digest at `:126` is unchanged.
- [ ] [P8-T6] Create `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1` with the six `It` names of §7. Each test extracts its section by heading from the skill file (resolved from `$PSScriptRoot`), collapses whitespace, and asserts the tokens that [P8-T2] to [P8-T4] require; the sixth dot-sources `.claude/hooks/enforce-model-routing-receipt.ps1` and asserts every name returned by `Get-ModelRoutingGatedAgent` appears in the `## Issue Number Consistency` section. Acceptance: the file exists, is at most 200 lines, contains the six names verbatim, and creates no file.
- [ ] [P8-T7] Mirror `.claude/skills/orchestrate/SKILL.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, and `.claude/skills/epic-orchestrate/SKILL.md` by byte copy to the same relative paths under `extensions/drm-copilot/resources/claude-customizations/`; append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: three equal pairs.
- [ ] [P8-T8] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-runtime -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, then run `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py tests/scripts/dev_tools/test_epic_bounded_child_return_contract.py tests/scripts/dev_tools/test_claude_rules_frontmatter.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p8-scoped.md` (four schema fields) with the Pester report last-write time, the `checkpoint-hygiene-skill-contract.Tests.ps1` testsuite values, and the pytest summary line. Acceptance: last-write time at or after `Timestamp:`; that testsuite has 6 passed and `failures` 0; the pytest summary carries no `failed` or `error` count. A failing pytest case stops execution and is reported with its node ID.
- [ ] [P8-T9] Commit Phase 8 with the method of [P4-T7], staging the three skills, their three mirrors, `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1`, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with `-F <SCRATCHPAD>/r3-msg-p8.txt`. Append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `.claude/skills/epic-orchestrate/SKILL.md` and `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`; `git status --porcelain` is empty.

### Phase 9 — Structural, Coupling, and Constraint Verification

- [ ] [P9-T1] AC-7 and AC-8. Run `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` over the four in-scope hook files and classify each match as code or comment; read every branch of `Get-PrAuthorTargetCheckpointResolution`, `Get-ModelRoutingTargetCheckpointResolution`, and `Resolve-WorktreeItemTarget` in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-binding-removal.md` (four schema fields). Acceptance: zero code matches; each comment match listed with file and line; one line per branch stating its outcome, with no branch yielding a checkpoint path other than through `Get-WorktreeItemCheckpointPath` of a resolved `WorktreeRoot`.
- [ ] [P9-T2] AC-23. Run `git diff --name-only <F5_BASE_SHA> HEAD` and `git status --porcelain`; drop paths under the feature folder; run `Select-String -SimpleMatch` for `TARGET_WORKTREE_AMBIGUOUS` and for `TARGET_WORKTREE_NOT_DERIVABLE` over the remaining files. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-reason-code-literal-search.md` (four schema fields) with the file list and both counts. Acceptance: `git status --porcelain` lists no path outside the feature folder; both counts are 0.
- [ ] [P9-T3] AC-24 and AC-25. Record every `Import-Module` line and every call to a worktree-resolution library function in the four in-scope hook files, every `SilentlyContinue` in them, and every function defined in them. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-library-consumption.md` (four schema fields). Acceptance: `enforce-model-routing-receipt.ps1` and `enforce-pr-author-skill-helpers.ps1` each import `WorktreeItemResolution.psm1` at column 0 with `-ErrorAction Stop`; zero `SilentlyContinue`; no function defined in a hook file derives a target, normalises a path, or returns a reason code; every such use is a call into `.claude/lib/worktree-resolution/` or a read of the result's `ReasonCode`.
- [ ] [P9-T4] AC-27. Run `Select-String -SimpleMatch` for `python`, `poetry`, `py -3`, and `Start-Process` over the four in-scope hook files and `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, classifying each match as code or comment. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-python-free.md` (four schema fields), naming `tests/scripts/claude-runtime/enforcement-hooks-no-python-invocation.Tests.ps1` as the #475 guard with scan roots at `:40-41`, enumerated at `:60`. Acceptance: zero code matches; the guard path and both citations recorded (its pass result is asserted in [P10-T4]).
- [ ] [P9-T5] AC-28 and AC-37. Run `git diff --name-only <F5_BASE_SHA> HEAD` and `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-coupling-guard.md` (four schema fields). Acceptance: the diff lists none of `.claude/hooks/hook-command-scanner.ps1`, `.claude/hooks/hook-command-invocation.ps1`, `.claude/hooks/enforce-epic-merge-gate.ps1`, `.claude/lib/orchestrator-state/OrchestratorState.psm1`, `.codex/hooks/enforce-codex-model-routing.ps1`, `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`; the porcelain output is empty.
- [ ] [P9-T6] AC-30. Record `@(Get-Content).Count` for every `.ps1`, `.psm1`, `.psd1`, and `.py` path in `git diff --name-only <F5_BASE_SHA> HEAD`, and run `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-line-counts.md` (four schema fields). Acceptance: `git status --porcelain` is empty; every count is at most 500; `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, and all four `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/` mirrors are listed.
- [ ] [P9-T7] AC-32. Search every test file and helper added or modified by this plan for `New-TemporaryFile`, `GetTempPath`, `GetTempFileName`, `TestDrive`, and `$env:TEMP`; list every assignment to `$env:CLAUDE_TOOL_INPUT` and every `Set-Location` or `CurrentDirectory` assignment with its enclosing construct. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-no-temp-files.md` (four schema fields). Acceptance: zero token matches; every `CLAUDE_TOOL_INPUT` assignment sits in a `try` whose `finally` restores it; every `Set-Location` or `CurrentDirectory` assignment sits in `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` inside a `try` whose `finally` restores both, or inside a child-process script string.
- [ ] [P9-T8] AC-19 and AC-20. Run `git diff <F5_BASE_SHA> HEAD -- tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1 tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1 tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-ac19-byte-identity.md` (four schema fields) with the diff and one verdict per protected row. Acceptance: the diff for `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` is empty; the diff for `enforce-model-routing-receipt.Tests.ps1` contains only the [P7-T3] insertion; the diff for `enforce-pr-author-skill.epic-base-branch.Tests.ps1` contains only the seven [P5-T6] argument additions; no `It` name and no line containing `Should` in a protected row changes.
- [ ] [P9-T9] AC-38. In a fresh process, with values computed at run time and never written, apply the four [P0-T16] matchers to every file in `git diff --name-only <F5_BASE_SHA> HEAD` except `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-13T20-48.md` and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-18T13-30.md`, and run `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-host-token-scan.md` (four schema fields) with the number of files scanned and the per-matcher match counts, no matched text. Acceptance: `git status --porcelain` is empty; the files-scanned count is greater than 0; all four match counts are 0.

### Phase 10 — Final QC Loop (fresh process, CI-equivalent)

Run [P10-T1] to [P10-T9] in order. If any step fails or changes a file, fix the cause and restart from [P10-T1]; artifacts of an abandoned pass are kept and labelled with their pass number. The gate is one pass in which every step succeeds and changes nothing.

- [ ] [P10-T1] Format. Record `git status --porcelain`; in a fresh process run `Invoke-PoshQCFormat -Root $root` (the command at `.github/workflows/_poshqc.yml:26`); record `git status --porcelain` again. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-poshqc-format.md` (four schema fields) with the pass number, the counts of lines beginning `Formatted: ` and `Already formatted: `, and a `Tree Delta:` section. Acceptance: `Formatted: ` count 0, `Already formatted: ` count greater than 0, the two porcelain outputs identical.
- [ ] [P10-T2] Analyze. In a fresh process run `Invoke-PoshQCAnalyze -Root $root` from `scripts/powershell/PoshQC/PoshQC.psd1` (`_poshqc.yml:36`). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-poshqc-analyze.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `PSScriptAnalyzer passed: no findings under`.
- [ ] [P10-T3] Python format, lint, and type for `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`: run `poetry run black --check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, `poetry run ruff check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, and `poetry run pyright tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, one artifact each: `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-black.md`, `.../r3-final-ruff.md`, `.../r3-final-pyright.md` (four schema fields each). Acceptance: all three `EXIT_CODE: 0`; the outputs contain `would be left unchanged`, `All checks passed!`, and `0 errors` respectively.
- [ ] [P10-T4] Full Pester with coverage over the whole configured `Run.Path` (`pester.runsettings.psd1:3`). In a fresh `pwsh -NoProfile -File` process (route per [P0-T7]) run `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` (`_poshqc.yml:42`). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-pester-coverage.md` (four schema fields), per §5: report last-write time; root `tests`, `failures`, `errors`, `disabled`; the status of every §7 name and every AC-19 `It` name recorded in [P0-T6]; the testsuites ending `enforcement-hooks-no-python-invocation.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, `OrchestratorState.Tests.ps1`, `test-name-uniqueness.Tests.ps1`, `ClaudeLibModuleConvention.Tests.ps1`, and the three `enforce-prd-feature-before-planner*.Tests.ps1` suites; and line coverage with raw counters for the four in-scope hooks and `WorktreeItemResolution.psm1`. Acceptance: `EXIT_CODE: 0`; last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0 with no exclusion; every §7 name and every AC-19 name appears exactly once with status Passed; the nine named testsuites have `failures` 0; each of the five files has line coverage of at least 85%.
- [ ] [P10-T5] Coverage delta. From [P0-T10] and [P10-T4], and from `git diff -U0 <F5_BASE_SHA> HEAD -- .claude/hooks/enforce-pr-author-skill.ps1 .claude/hooks/enforce-pr-author-skill-helpers.ps1 .claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1 .claude/hooks/enforce-model-routing-receipt.ps1 .claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, compute per file the baseline percent, post percent, and changed-line coverage per §5. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-coverage-delta.md` (four schema fields). Acceptance: fifteen values recorded (the new module's baseline as `n/a (new file)`); every post percent at least 85%; every hook's post percent at least its baseline; every changed-line figure at least 85%. If the report carries no `line` elements, record that and mark the task remediation-required.
- [ ] [P10-T6] Full Python suite. Run `poetry run pytest -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-pytest.md` (four schema fields) with the summary line verbatim. Acceptance: `EXIT_CODE: 0` and the summary line carries no `failed` or `error` count.
- [ ] [P10-T7] Mirror gate. Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -q` and compute SHA-256 for the pairs: the four in-scope hooks, `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, the three skills of [P8-T7], and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, each against its counterpart under `extensions/drm-copilot/resources/`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-mirror-gate.md` (four schema fields). Acceptance: `EXIT_CODE: 0`; the summary line reads `1 passed`; nine pairs with equal hashes.
- [ ] [P10-T8] Seven-stage record (AC-33). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-seven-stage-loop.md` (four schema fields) mapping the stages to the clean pass: formatting [P10-T1] and the black step of [P10-T3]; linting [P10-T2] and the ruff step; type checking the pyright step (PowerShell not applicable per `.claude/rules/powershell.md`); architecture-boundary tests not applicable (record `Select-String -SimpleMatch -Pattern 'dependency-cruiser','NetArchTest'` over `tests/scripts` with its count); unit tests [P10-T4] and [P10-T6]; contract and schema checks [P10-T7] plus the `WorktreeResolution.Manifest.Tests.ps1` and frozen-pin results; integration tests the fixture rows and the end-to-end child-process row inside [P10-T4]. This task text authorizes the one not-applicable architecture entry and no other. Acceptance: seven stage rows, each naming its task or its reason, and the pass number.
- [ ] [P10-T9] Final ordering and commit. Commit any files changed during Phase 9 or 10 with the method of [P4-T7] and `-F <SCRATCHPAD>/r3-msg-p10.txt`; run `git rev-list --reverse <F5_BASE_SHA>..HEAD` and `git log --reverse --format=%H <F5_BASE_SHA>..HEAD -- .claude/hooks`; append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md` the positions of `EVIDENCE_COMMIT` ([P0-T4]) and of the first `.claude/hooks` commit. Acceptance: `EVIDENCE_COMMIT`'s position is lower than the first hooks commit's (AC-3); `git status --porcelain` is empty.

## 8. Residual risk and follow-up (not tasks)

- **prd-feature gate (#672).** Unchanged by design (§2.3); it still resolves folder-first and denies `Ambiguous` on a merged relative folder token. Follow-up: move it to `Resolve-WorktreeItemTarget` for location and use the folder only to find prerequisite documents.
- **Other gates that read a cwd-relative checkpoint** are where hygiene (design A) still matters: `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1:27`, `.claude/hooks/enforce-completion-consistency.ps1:300`, `.claude/hooks/enforce-epic-merge-gate.ps1:63` (F3), and the prd gate fallback at `.claude/hooks/enforce-prd-feature-before-planner.ps1:160`. None is changed here.
- **First-match branch reading.** F1's reader returns the first `--head`, `--branch`, or `branch:` match. A prompt with an unrelated `branch:` phrase earlier than the item's label is misread. With the issue line present, a misread branch disagrees with the issue and denies (§2.2 rows 7 and 8); without it, a misread branch whose worktree holds a stale checkpoint with a matching receipt could approve, which hygiene (no stale checkpoint at coordinator roots) and the skill rule (label first; issue line always once assigned) are there to prevent.
- **Post-merge confirmation.** Isolated subagents run `main`'s hooks, so end-to-end behaviour is observable only after merge. The spec's rollout item (confirm no gate allows on a sibling's checkpoint in the next parallel run) is the confirmation.
- **Pre-promotion delegations** (for example research before S3) carry no issue number; they are identified by the `branch:` label alone and deny until prompts carry it.
- `gh pr edit --body-file` still carries no `--head` and still denies with the no-target code (#687 behaviour, unchanged).
- The two superseded plans retain host tokens; the instruction not to edit them takes precedence, and [P9-T9] excludes them by name.
- `.claude/skills/orchestrate/SKILL.md:256` says to record `issue_num`, while the validator requires `issue-num` (`scripts/dev_tools/validate_orchestrator_state.py:57`); the resolver reads `issue-num`. The Codex twin `.agents/skills/orchestrate/SKILL.md` is not updated (Codex surface excluded by the spec).

## 9. Criteria added to `spec.md` by [P1-T2] (verbatim)

[P1-T2] writes each criterion below into `spec.md` as an unchecked checkbox item: the characters `- [ ] ` followed by the criterion text exactly as quoted. They are shown here without the checkbox prefix so that the plan validator does not parse them as task lines.

- AC-34 `.claude/skills/orchestrate/SKILL.md` (`## Checkpoint Handling`), `.claude/skills/parallel-orchestrate/SKILL.md` (`## Per-Item Branch and Worktree Lifecycle`), and `.claude/skills/epic-orchestrate/SKILL.md` (`## Epic-Level Checkpoint`) each state the checkpoint-hygiene rule: a per-feature `artifacts/orchestration/orchestrator-state.json` that does not belong to the session's own item is moved to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json`, and a coordinating session writes no per-feature checkpoint at its own root. Verified by the three hygiene tests in `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1`.
- AC-35 `## Issue Number Consistency` in `.claude/skills/orchestrate/SKILL.md` requires the canonical issue number line and a `branch:` label on every delegation to each receipt-gated subagent type returned by `Get-ModelRoutingGatedAgent`. Verified by the three delegation-identity tests in the same file.
- AC-36 The three bindings select the target worktree by portable identity only, through `Resolve-WorktreeItemTarget` in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`: the canonical issue number, matched against the `issue-num` of each live worktree's checkpoint, and a branch signal (`--head`, `--branch`, `branch:`). A worktree is live when it is registered in the repository's worktree administrative layout and its root still carries a worktree marker. A feature-folder path or file path never selects the worktree. No identity, or an identity matching no live worktree, denies with the no-target code; several live matches or disagreeing identities deny with the ambiguity code; a branch signal breaks a tie among live worktrees recording the same issue. Verified by the named tests in `tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1`.
- AC-37 `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, and `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` are absent from `git diff --name-only` against the merge base with `main`, and the three `enforce-prd-feature-before-planner*.Tests.ps1` suites pass. The prd-feature gate (issue #672) keeps its folder-first resolution; migrating it is a follow-up.
- AC-38 No file added or modified by this change, other than `plan.2026-09-13T20-48.md` and `plan.2026-09-18T13-30.md`, contains a drive-letter path, a user-profile path, or the executing user's account name. Feature-folder artifacts that carried such tokens are redacted to placeholders, with per-file counts logged under `evidence/other/`.

## 10. Design requirement traceability (user-approved design, 2026-09-18)

| Item | Where planned | Verified by |
| --- | --- | --- |
| A. Checkpoint hygiene | §2.4; [P8-T2] to [P8-T5] | AC-34; [P8-T8]; [P10-T4] |
| B. Portable identity, liveness, stale-attempt rule | §2.1, §2.2; [P2-T2] to [P2-T6]; [P5-T3]; [P7-T2] | AC-35, AC-36; [P2-T6]; [P7-T6]; [P10-T4] |
| C. Deny unidentifiable targets only after the scope filter | §2.5, DD-3 | AC-16, AC-17; [P10-T4] |
| D. Standalone PR to `main` | header; RS-9, RS-13 | [P9-T5] |
| Signal precedence (point 5) and prd gate statement | §2.3 | AC-36, AC-37; [P9-T5] |
| No host data | Evidence rules; [P1-T5] | AC-38; [P9-T9] |
