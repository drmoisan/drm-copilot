# Feature Audit: orchestration-state-contract-divergences (#412) — Re-audit R4 (post remediation cycle 1)

---

**Audit Date:** 2026-07-25
**Feature Folder:** `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412`
**Base Branch:** `main`
**Head Branch:** `bug/orchestration-state-contract-divergences-412`
**Work Mode:** `full-bug`
**Audit Type:** Re-audit after remediation cycle 1 (prior audit: `feature-audit.2026-07-25T19-14.md`)

---

## Scope and Baseline

- **Base branch:** `main` (merge base `009808510363081d0db7684f7b555f2ded4b0b7c`, the tip of `origin/main`; the branch was rebased onto current `origin/main` before the first review)
- **Head branch/commit:** `bug/orchestration-state-contract-divergences-412` (commit `bfb73c75fafde8c1896f954f29473e1d23f12213`, seven commits, all authored by this feature; the seventh is remediation cycle 1)
- **Merge base:** `009808510363081d0db7684f7b555f2ded4b0b7c` (derived by this review with `git merge-base main HEAD`)
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this review against `main` at the post-remediation HEAD; the pre-existing artifacts were stale — recorded head `81f3df3f`, one commit behind)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt` (regenerated alongside)
  - Feature evidence: `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/**` (baseline, qa-gates, regression-testing, remediation-baseline, other)
  - Additional evidence: reviewer-executed toolchain re-runs this session (direct Pester 105/105 with `-PassThru` on changed suites + epic-merge-gate suite; targeted pytest 94/94; full Jest 168 suites / 2035 tests with the `--testMatch` override; Black/Ruff/Pyright/Prettier/ESLint/TSC all clean; PSScriptAnalyzer 0 findings and formatter idempotence on all 7 changed PowerShell files), reviewer behavioral probes (PowerShell readiness gate, Python readiness/plain/completion gates, floor truth table in both languages), and reviewer byte-comparison (`cmp`) of both `.psm1` resources mirror pairs
- **Feature folder used:** `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412` (sole active folder matching issue 412 in the branch name)
- **Requirements source:** `spec.md` only
- **Work mode resolution note:** `issue.md` carries the explicit persisted marker `- Work Mode: full-bug`; per the acceptance-criteria-tracking contract, `spec.md` is therefore the sole AC source. `user-story.md` is intentionally absent by design for `full-bug` and its absence is not a defect.
- **Scope note:** Scope is the full branch diff versus the resolved base (116 files), not the remediation delta. No caller scope narrowing was attempted or applied. Environmental facts (dot-directory Jest `testMatch` limitation, judged on the override run; pre-existing repo-wide `npm audit` advisory GHSA-mh99-v99m-4gvg; PoshQC published-bundle execution path and its baseline test-count drift) were not evaluated as branch defects.
- **Remediation context:** Cycle 1 closed the orchestrator-elevated Blocking finding F-1 (prior code-review CR-1: PowerShell portable PR-creation-readiness fail-open on `blocked_remediation_loop_limit`). Its non-documentation surface is exactly `.claude/lib/orchestrator-state/OrchestratorState.psm1`, its resources byte mirror, and `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` (verified via `git diff --name-only 81f3df3f..HEAD`). Note that no spec acceptance criterion covers the PowerShell readiness gate; CR-1 closure is verified in the policy audit and code review of this timestamp and recorded in the cycle's evidence, not as an AC row below.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/spec.md` §Acceptance Criteria (lines 237–273) — only source

### Acceptance criteria

From `spec.md` — Divergence 1 — step-status vocabulary:

1. The Python validator (`scripts/dev_tools/validate_orchestrator_state.py`) accepts `step9_status` values `passed`, `failed_remediation_required`, and `blocked_ci_loop_limit` in plain validation mode.
2. The Python validator accepts `step6_status: "blocked_remediation_loop_limit"` in plain validation mode.
3. The Python validator rejects each per-key extra value when written to any step-status key other than its owning key, with the error `Checkpoint has invalid <key>: <value>`.
4. The shared `VALID_STEP_STATUS` set in `scripts/dev_tools/validate_orchestrator_state.py` is unchanged; the new values are carried in a per-step-key additive map.
5. The Python `--require-complete` gate fails with a completion-validation error when any step status is `failed_remediation_required`.
6. The Python `--require-complete` gate fails with a completion-validation error when any step status is `blocked_ci_loop_limit`.
7. The Python `--require-complete` gate fails with a completion-validation error when any step status is `blocked_remediation_loop_limit`.
8. The Python `--require-complete` gate does not fail on `step9_status: "passed"` in an otherwise-complete checkpoint.
9. The Python `--require-pr-creation-ready` gate fails when `step6_status` is `blocked_remediation_loop_limit`.
10. The TypeScript mirror (`extensions/drm-copilot/src/lib/validate/orchestrator-state-core.ts`) implements the same per-key acceptance and rejection behavior with error strings byte-identical to the Python validator, verified by Jest cases.
11. The TypeScript completion check rejects `failed_remediation_required`, `blocked_ci_loop_limit`, and `blocked_remediation_loop_limit` with error strings byte-identical to the Python validator, and does not reject `step9_status: "passed"`.
12. `.claude/lib/orchestrator-state/OrchestratorState.psm1` implements the same per-key acceptance and rejection behavior, verified by Pester cases in `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`.
13. A checkpoint with `epic_mode: true` and `step9_status: "passed"` passes plain validation and satisfies `.claude/hooks/enforce-epic-merge-gate.ps1` with zero edits to that hook (regression scenario).
14. All pre-existing step-status validator tests (Python, Pester, Jest) pass without fixture modification, demonstrating that no previously valid checkpoint is newly rejected in plain mode.

From `spec.md` — Divergence 2 — complexity-floor semantics:

15. `compute_complexity_floor` returns `C1` for each single-element list containing a `"floor": false` signal (`single_file_localized_edit`, `mechanical_rename_or_move`, `docs_or_comment_only`) and for a single-element list containing an unknown signal name.
16. `compute_complexity_floor` returns `C3` for each single-element list containing a `"floor": true` signal and for any mixed list containing at least one floor signal, returns `C1` for `[]`, and never returns `C4`.
17. `Get-ComplexityFloor` in `.claude/lib/model-routing/ModelRouting.psm1` produces outputs identical to `compute_complexity_floor` for the same truth table, verified by Pester cases in `tests/scripts/claude-lib/model-routing/Get-ComplexityFloor.Tests.ps1`.
18. `tests/scripts/dev_tools/test_compute_complexity_floor.py` contains a static parity assertion that the embedded `FLOOR_SIGNAL_NAMES` set equals the set of names flagged `"floor": true` in `config/orchestration-routing.json`.
19. `tests/scripts/claude-lib/model-routing/ModelRouting.Parity.Tests.ps1` contains a static parity assertion pinning `$script:FLOOR_SIGNAL_NAMES` to the config's `"floor": true` names.
20. The complexity validator accepts a recorded `complexity_assessments` entry whose `signals_present` contains only non-floor signals and whose `floor` is `"C1"`.
21. The complexity validator rejects the same entry with `floor: "C3"`, producing a floor-mismatch error that names the recomputed value `C1`.
22. `scripts/dev_tools/compute_complexity_floor.py` docstrings no longer claim caller pre-filtering, the module performs no file I/O, and the file remains under 500 lines.
23. `.claude/lib/model-routing/ModelRouting.psm1` performs no file reads at runtime.
24. The PR body records the divergence-2 backward-compatibility statement: zero stored assessments invalidated (paths and counts per the research), and pre-change checkpoints with non-floor-only assessments require re-recording via the documented resume reconciliation.

From `spec.md` — Cross-cutting:

25. `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` passes, confirming the root `.claude/lib` modules and their `extensions/drm-copilot/resources/claude-customizations` mirrors are content-identical.
26. The full per-language toolchain (format, lint, type-check where applicable, tests) passes for every batch.
27. Line coverage >= 85% and branch coverage >= 75% are maintained on changed files.

---

## Acceptance Criteria Evaluation

Every criterion was re-verified at the post-remediation HEAD; none was carried forward on trust from the prior audit.

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Python accepts step9 `passed`/`failed_remediation_required`/`blocked_ci_loop_limit` plain | PASS | Reviewer probe matrix this session: all three values → `[]` errors in plain mode. Parametrized acceptance tests green in the targeted 94/94 run. | `PYTHONPATH=. poetry run python <probe>` from worktree root; `poetry run pytest <changed suites> -q` | Probes run per the documented stale-import mitigation (repo-root `PYTHONPATH=.`). |
| 2 | Python accepts step6 `blocked_remediation_loop_limit` plain | PASS | Reviewer probe: halted-loop checkpoint → `[]` plain errors. Re-verified post-remediation specifically to confirm the CR-1 fix did not regress plain-mode acceptance. | Same probe | Also confirmed on the PowerShell side: the readiness probe output contains no base-presence error. |
| 3 | Per-key extras rejected on non-owning keys with `Checkpoint has invalid <key>: <value>` | PASS | Reviewer probe: `step5_status: "passed"` → `['Checkpoint has invalid step5_status: passed']`; 20-triple matrix green in targeted pytest. | Probe + pytest | Exact message form preserved. |
| 4 | Shared `VALID_STEP_STATUS` unchanged; values in per-key additive map | PASS | Literal re-verified this session at `validate_orchestrator_state.py` lines 93–102 (exactly the 8 legacy members); branch diff touches only consumers; probe re-export prints the same 8 values; PS `$script:VALID_STEP_STATUS` and `$script:STEP_SPECIFIC_EXTRA_STATUS` verified unchanged by the remediation delta (module diff `81f3df3f..HEAD` touches only the readiness function); TS `VALID_STEP_STATUS` untouched by the cycle (zero TS files in the delta). | `git diff main...HEAD -- scripts/dev_tools/validate_orchestrator_state.py`; `git diff 81f3df3f..HEAD -- .claude/lib/orchestrator-state/OrchestratorState.psm1`; probe | Specifically re-checked as a remediation regression risk; clean. |
| 5 | `--require-complete` fails on `failed_remediation_required` | PASS | Reviewer probe under `require_complete=True`: completion error naming the value present; dedicated pytest and Jest cases green. | Probe + pytest + Jest | |
| 6 | `--require-complete` fails on `blocked_ci_loop_limit` | PASS | Same probe matrix: blocks; value present in `COMPLETION_BLOCKING_STEP_STATUS` (Python lines 73–79, TS lines 150–156, both re-inspected). | Same | |
| 7 | `--require-complete` fails on `blocked_remediation_loop_limit` | PASS | Same probe matrix: blocks on the halted-loop checkpoint. | Same | |
| 8 | `--require-complete` does not fail on `step9_status: "passed"` | PASS | Reviewer probe: zero step9-related errors under `require_complete=True` with `step9_status: "passed"`; `passed` deliberately absent from all three languages' blocklists (comment-documented). | Probe + pytest + Jest | |
| 9 | `--require-pr-creation-ready` fails on step6 `blocked_remediation_loop_limit` | PASS | Reviewer probe: PY readiness → `['Checkpoint PR-creation readiness validation failed: step6_status is blocked_remediation_loop_limit.']`; `test_pr_creation_readiness_rejects_step6_blocked_remediation_loop_limit` green. | Probe + pytest | The criterion covers the Python gate. The PowerShell portable fallback — outside this criterion — is now also closed by remediation cycle 1 with a byte-identical error string (verified by probe on root and mirror). |
| 10 | TS mirror: same per-key behavior, byte-identical strings, Jest-verified | PASS | Source re-inspected at HEAD (map at lines 133–138, message template at line 357); targeted Jest 34/34 and full 2035/2035 green (reviewer re-runs this session). Zero TS changes in the remediation delta. | `npx jest --testMatch "**/test/lib/validate/orchestrator-state-core*.test.ts"`; full-suite override run | |
| 11 | TS completion check rejects the three failure values byte-identically; does not reject `passed` | PASS | `COMPLETION_BLOCKING_STEP_STATUS` (TS lines 150–156) matches the Python set element-for-element; template `Checkpoint completion validation failed: ${key} is ${String(value)}.` (line 405) matches with trailing period; Jest literal-string cases green. | Same | |
| 12 | `OrchestratorState.psm1` same per-key behavior, Pester-verified | PASS | Reviewer direct Pester: suite green inside 105/105; plain acceptance/rejection cases present (lines 367–369 and the non-owning matrix); resources mirror byte-identical (`cmp` exit 0, re-run this session at the post-remediation content). | `Invoke-Pester` (`-PassThru`, exit branch); `cmp <root> <mirror>` | Remediation added readiness-gate cases to the same suite without altering the plain-mode cases. |
| 13 | `epic_mode: true` + `step9_status: "passed"` passes plain validation and satisfies the epic merge gate with zero hook edits | PASS | Plain acceptance verified by probe (criterion 1); hook re-verified at zero diff against merge base this session; hook suite re-run green by this reviewer (included in the 105/105 direct Pester run, 30 tests). | `git diff main...HEAD -- .claude/hooks/enforce-epic-merge-gate.ps1` (empty); direct Pester | Hook remains the unmodified regression witness. |
| 14 | All pre-existing step-status tests pass without fixture modification | PASS | `tests/scripts/dev_tools/test_validate_orchestrator_state.py` re-verified at zero diff; included green in the reviewer's targeted 94/94 run; executor full suites at post-remediation HEAD: pytest 2123/0, PoshQC 1394/0; reviewer full Jest 2035/0. | `git diff main...HEAD -- tests/scripts/dev_tools/test_validate_orchestrator_state.py` (empty); suite runs | |
| 15 | Floor returns `C1` for each `floor: false` single and for unknown name | PASS | Reviewer probe this session: `docs_or_comment_only`, `single_file_localized_edit`, `mechanical_rename_or_move`, `not_a_real_signal` each → `C1` (Python and PowerShell identically). | Probe + pytest/Pester | |
| 16 | Floor returns `C3` for each `floor: true` single and mixed lists; `C1` for `[]`; never `C4` | PASS | Reviewer probe: `[]` → `C1`; `cross_module_contract_change` → `C3`; mixed non-floor+floor → `C3`; never-C4 pinned by test; parametrized floor-true tests green. | Same | |
| 17 | `Get-ComplexityFloor` identical truth table, Pester-verified | PASS | Reviewer ran the identical 7-case matrix through both implementations this session: outputs identical (`C1 C1 C1 C1 C1 C3 C3`); Pester suites green; mirror byte-identical. | PS probe + Pester | `ModelRouting.psm1` untouched by the remediation cycle. |
| 18 | Python static parity assertion pinning `FLOOR_SIGNAL_NAMES` to config `floor: true` names | PASS | `test_floor_signal_names_match_config_floor_true_entries` present (`test_compute_complexity_floor.py` line 219 area; bidirectional equality via `floor_module.FLOOR_SIGNAL_NAMES`); green in targeted run. | Grep + pytest | |
| 19 | Pester static parity assertion pinning `$script:FLOOR_SIGNAL_NAMES` | PASS | `pins FLOOR_SIGNAL_NAMES to the model_policy.complexity signals flagged floor true` and the floor-false exclusion case (`ModelRouting.Parity.Tests.ps1` lines 93–113); green in the 105/105 run. | Grep + Pester | |
| 20 | Complexity validator accepts non-floor-only entry with `floor: "C1"` | PASS | `test_non_floor_only_assessment_with_floor_c1_accepted` (line 330) green in targeted run. | pytest | |
| 21 | Complexity validator rejects same entry with `floor: "C3"`, naming recomputed `C1` | PASS | `test_non_floor_only_assessment_with_floor_c3_rejected` (line 343) green; pins the documented compatibility consequence. | pytest | |
| 22 | `compute_complexity_floor.py`: no caller-pre-filter docstring claim, no file I/O, under 500 lines | PASS | Re-verified this session: docstrings state "no caller-side pre-filtering is required" (lines 25, 94); zero `open`/`Path`/`read_text`/`json.load` matches; 133 lines. | grep + `wc -l` | |
| 23 | `ModelRouting.psm1` performs no file reads at runtime | PASS | Re-verified this session: zero `Get-Content`/`Import-Csv`/`ConvertFrom-Json`/`ReadAllText` matches in the module; floor set is an embedded `$script:` literal; config read only by parity tests. | grep | |
| 24 | PR body records the divergence-2 backward-compatibility statement | UNVERIFIED | No pull request exists for this branch yet (regenerated PR context: no PR for the branch), so the PR body cannot be inspected. The statement text remains prepared and consistent with the research counts at `evidence/other/pr-body-backcompat-statement.md`. | `artifacts/pr_context.summary.txt` | Deferred by design, not unmet: satisfiable only at PR authoring time. Correctly left unchecked in `spec.md`; `Agent(pr-author)` must include the prepared text. Not a remediation trigger. |
| 25 | Push-down parity test passes (root/mirror content identity) | PASS | Reviewer `cmp` on both pairs at post-remediation content: byte-identical; `test_push_down_claude_resource_contracts.py` (unmodified) green in the reviewer's 94/94 targeted run and the executor's 2123/2123 full run at this HEAD. | `cmp` ×2; pytest | The remediation applied the module edit and its mirror in the same commit, as required. |
| 26 | Full per-language toolchain passes for every batch | PASS | Per-phase executor artifacts (both cycles, all EXIT_CODE 0) plus reviewer re-verification at HEAD this session: PSSA 0 findings / formatter idempotent (7 files), direct Pester 105/105, Black/Ruff/Pyright clean, ESLint/Prettier/TSC clean, full Jest 2035/2035. | Appendix B of `policy-audit.2026-07-25T20-39.md` | Jest judged on the `--testMatch` override run per the documented dot-directory limitation; both invocations recorded in evidence with real exit codes. |
| 27 | Line >= 85% / branch >= 75% maintained on changed files | PASS | Changed-file metrics at post-remediation HEAD: Python 97.50–100% lines / 92.86–100% branches; PowerShell `OrchestratorState.psm1` 97.17% lines / 96.67% commands (up from 96.64%; the remediation's added command is covered), `ModelRouting.psm1` 100%; TS 98.45% lines / 94.52% branches. Repo-wide: Py 91.00/81.84, PS 90.26 line / 89.73 commands, TS 96.34/89.22 — all at or above baseline; PowerShell branch metric unavailable by Pester 5 CoverageGutters tooling limitation (documented). | `artifacts/python/lcov.info`, `artifacts/pester/powershell-coverage.xml`, `extensions/drm-copilot/coverage/lcov.info`; `evidence/qa-gates/remediation1-coverage-comparison.md` | No regression on any metric in either cycle. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 26 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 1 criterion (#24 — PR-body statement; deferred by design until PR authoring, prepared text verified ready)
- **FAIL:** 0 criteria

**Remediation cycle 1 outcome (outside the AC set):** the orchestrator-elevated Blocking finding F-1 (PowerShell portable PR-creation-readiness fail-open) is confirmed closed by independent behavioral probes with byte-identical Python/PowerShell error strings, no plain-mode regression, frozen constants, byte-identical mirrors, and dedicated Pester coverage. No spec criterion covers this gate; closure is recorded in `policy-audit.2026-07-25T20-39.md` §8 and `code-review.2026-07-25T20-39.md`.

**Top gaps preventing PASS:**

1. None. The single UNVERIFIED criterion is structurally unsatisfiable before a PR exists and its deliverable text is prepared and verified; it does not indicate missing work on this branch.

**Recommended follow-up verification steps:**

1. At PR authoring, include the statement from `evidence/other/pr-body-backcompat-statement.md` in the PR body, then check off criterion #24 in `spec.md`.
2. No CR-1 follow-up issue is needed any longer; the gap was closed on-branch by remediation cycle 1.

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 26 PASS criteria were already checked off (`- [x]`) in `spec.md` with evidence-before-check-off (per-phase check-offs in `evidence/other/acceptance-criteria-status.md`; cycle reconciliation in `evidence/qa-gates/remediation1-ac-reconciliation.md`). This re-audit independently re-verified each criterion at the post-remediation HEAD and confirms every existing check-off is accurate; no optimistically-checked item was found, so no source-file change was required by the reviewer.
- Criterion #24 (UNVERIFIED, deferred) correctly remains unchecked (`- [ ]`) and must stay unchecked until the PR body exists with the required statement.
- No criteria were added, reworded, or removed by this review or by the remediation cycle.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/spec.md`
- Total AC items: 27
- Checked off (delivered): 26
- Remaining (unchecked): 1
- Items remaining: "The PR body records the divergence-2 backward-compatibility statement: zero stored assessments invalidated (paths and counts per the research), and pre-change checkpoints with non-floor-only assessments require re-recording via the documented resume reconciliation."

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/spec.md` | 27 | 26 | 1 | Checkbox-backed; sole authoritative source (`full-bug`) |

No source-file checkbox change was made by this review: every PASS criterion was already correctly checked, and the sole unchecked criterion must remain unchecked until PR creation.
