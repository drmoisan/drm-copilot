# Policy Audit — collect-pr-context-omits-claude-tree (#633)

- Reviewed: 2026-09-07
- Scope: full branch diff, `exec/collect-pr-context-omits-claude-tree-633-a919e040` vs `origin/epic/cleanup-merged-worktrees-hardening-integration` (merge-base `a36b6dca`)
- Work mode: `full-bug` (declared in `issue.md`) — AC source is `spec.md` "## Acceptance Criteria" only
- Changed files in scope: `extensions/drm-copilot/src/lib/pr-context/collector-core.ts`, `extensions/drm-copilot/jest.config.cjs`, `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, `extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts`, plus checkbox-only edits to `plan.2026-09-06T23-05.md` and `spec.md`, plus new evidence artifacts.
- Language(s) with changed files: TypeScript only. No Python, PowerShell, or C# production/test files are present in the branch diff.

## Rejected Scope Narrowing

No caller instruction in this delegation attempted to narrow audit scope to a plan/task/phase subset, mark a language as "out of scope," or instruct skipping a toolchain/coverage check. The directive's item 8 (branch-naming note) explicitly asked for observation only, not a scope reduction of the code/test audit, and no audit step was skipped as a result. Nothing to record under this heading.

## Policy Reading Order Compliance

Confirmed read, in order, per `CLAUDE.md` and the `policy-compliance-order` skill: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, and `.claude/rules/typescript.md` (the sole in-scope language). — **PASS**

## Evidence Location Compliance

Ran `python scripts/dev_tools/validate_evidence_locations.py --root .` — exit code 0, no violations reported. `git status --porcelain` shows no new files under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or `artifacts/evidence/`. All new evidence artifacts are under the canonical `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/{baseline,regression-testing,qa-gates,other}/` tree. — **PASS**

No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` entries were needed; no caller instruction specified a non-canonical evidence path.

## AC-Item-Level Policy Verdicts (spec.md AC source, full-bug mode)

| # | AC (paraphrased) | Verdict | Evidence |
|---|---|---|---|
| 1 | Terminal else routes every unmatched, non-renamed path into a bucket | PASS | Diff of `collector-core.ts` lines 329-334 confirmed by direct read; matches spec's Proposed Fix exactly. |
| 2 | `collector-core.test.ts` regression test fails pre-fix, passes post-fix | PASS | `evidence/regression-testing/fail-before-bucket-partition.2026-09-07T10-58.md` (EXIT_CODE 1) and `pass-after-bucket-partition.2026-09-07T10-58.md` (EXIT_CODE 0) independently re-verified: fail-before shows the missing paths, pass-after shows `1 passed`. |
| 3 | `collector-output.test.ts` confirms rendered overview includes the path | PASS | New test asserts fixture-built `bucketDocs` entries appear between the overview and next section headers; independently re-run, passes (0 failures). |
| 4 | Existing empty-bucket fixture in `collector-output-freshness.test.ts` unaffected | PASS | File byte-unmodified (`git status --porcelain` shows nothing); independently re-ran the 3 tests in that file — all pass. |
| 5 | No regression / no double-bucketing for rename, `.py`/`.ps1`, docs-prefixed paths | PASS | New negative assertions in `collector-core.test.ts` confirm mutual exclusivity; independently re-run, passes. |
| 6 | `collector.py` left unmodified; Known Limitation documented | PASS | `git diff origin/epic/... -- scripts/dev_tools/pr_context/collector.py` produced zero output (confirmed independently). Spec's pre-existing "Known Limitation (Python Parity Module)" section documents the gap and a follow-up recommendation. |
| 7 | Full toolchain pass (Prettier -> ESLint -> tsc -> Jest w/ coverage), `npm ci` first | PASS | Independently re-ran `npm run lint` (clean), `npm run typecheck` (clean), `npm run test:coverage` (203/203 suites, 2737/2737 tests) — all exit 0, matching reported evidence exactly. |
| 8 | Coverage >= 85% lines / >= 75% branches on changed files, no exclusion | PASS | See Coverage Verification below. |

## Coverage Verification (mandatory, TypeScript is the only changed language)

- Coverage artifact: `extensions/drm-copilot/coverage/lcov.info` — present, regenerated by independent re-run of `npm run test:coverage`.
- Repo-wide (TypeScript): Lines 96.73% (44238/45733), Branches 90.2% (6299/6983) — both well above the 80% mandatory floor and above the 85%/75% general policy floor. **PASS**
- `collector-core.ts` (modified file, newly gated): Lines 465/475 = 97.89%, Branches 61/68 = 89.71%. Both figures independently reproduced byte-for-byte from a from-scratch coverage run against the lcov output. Exceeds both the 85%/75% general threshold and the AC8-specific requirement. **PASS**
- `collector-output.ts` (pre-existing gate, unmodified by this fix but exercised by the new test): Lines 474/485 = 97.73%, Branches 65/79 = 82.28% — identical to the reported baseline, zero regression. **PASS**
- `summary-helpers.ts` (pre-existing gate, unmodified): Lines 363/388 = 93.56%, Branches 65/74 = 87.84% — identical to baseline, zero regression. **PASS**
- `jest.config.cjs` `collectCoverageFrom` remains `["src/**/*.ts", "!src/**/*.d.ts"]` (line 17) — confirmed by direct read. No production path under `src/` was added to any exclude list. **PASS** (no Blocking finding per the Coverage Exclusion Policy).
- No new production source files were added by this change (only a 3-line modification to an existing file), so the "new file >= 90% lines" sub-rule does not apply.

Python, PowerShell, and C# coverage artifacts were not checked because no file of those languages appears in the branch diff (confirmed via `git diff --stat` against the resolved base).

## Scope Boundary Compliance

- `git diff` confirms zero changes to `scripts/bash/cleanup_worktrees_*` and `.claude/hooks/*`.
- `scripts/dev_tools/pr_context/collector.py` confirmed byte-identical to base (independently re-verified, not just trusting the executor's report).
- `user-story.md` and `issue.md` are untouched (not applicable as AC sources under `full-bug`, and correctly not treated as such).
— **PASS**

## Tonality Compliance

- `spec.md`'s diff is checkbox-only (`- [ ]` -> `- [x]`); no new prose was introduced by this delivery. The pre-existing "Known Limitation" prose was authored during specification, not during this execution, and reads as factual, evidence-first, non-hyperbolic text consistent with `.claude/rules/tonality.md`.
- New evidence artifacts (`fail-before-bucket-partition`, `pass-after-bucket-partition`, `ac-reconciliation`, `coverage-delta`, etc.) use plain, measured, evidence-cited language with no hyperbole, humor, or unsupported certainty claims. — **PASS**

## Branch-Naming Note (informational only, not a defect)

`artifacts/orchestration/orchestrator-state.json`'s `branch_setup` block documents a pre-existing branch-name collision: the intended branch name `bug/collect-pr-context-omits-claude-tree-633` is locked by another worktree at an ancestor commit, so execution proceeded on `exec/collect-pr-context-omits-claude-tree-633-a919e040` with a documented fast-forward-push resolution planned at PR-creation time. This is recorded as required by the task directive and is **not treated as a policy or code defect** in this audit; it is out of scope for this reviewer to remediate.

## Overall Policy Verdict: PASS

No blocking findings. Coverage, scope boundary, evidence location, and tonality checks all pass on independent re-verification, not merely on the executor's self-report.
