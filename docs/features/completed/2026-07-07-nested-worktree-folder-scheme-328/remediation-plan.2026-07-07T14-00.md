# Remediation Plan: nested-worktree-folder-scheme (Issue #328)

- Timestamp: 2026-07-07T14-00
- Cycle: 1 (remediation)
- Work Mode: full-feature
- Base: `main` @ merge-base `3eda262ffbc3ab82e6eefed3e9a72ab4133b893c`
- Head: `drm-copilot-wt-2026-07-07-11-50` @ `f4bbfdf7c804f094ed11e42b56aed73444629f7d`
- Source findings: `remediation-inputs.2026-07-07T13-16.md` (R1, R2, R3)
- Target plan path (update in place across preflight revisions): `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/remediation-plan.2026-07-07T14-00.md`

## Objective

Make the changed PowerShell production file `scripts/dev-tools/new-claude-worktree-session.ps1` coverage-measurable and record a valid line figure (>= 85%) and a branch disposition (>= 75% or a sanctioned exception dossier), keep the full PowerShell toolchain green, and reconcile the AC9 checkbox state so the re-audit evaluates AC9 PASS with zero remaining Blocking findings. This cycle is measurement/evidence only; no production behavior change is permitted beyond a behavior-preserving dot-source guard.

## Guardrails (non-negotiable; apply to every task)

- Do NOT lower, remove, or reinterpret any coverage threshold (line >= 85%, branch >= 75%, uniform across tiers T1-T4).
- Do NOT exclude any production file from coverage measurement; do NOT add `exclude` entries matching production paths. Do NOT remove existing `CodeCoverage.Path` entries in `pester.runsettings.psd1`.
- Do NOT weaken, delete, or skip existing tests to influence coverage. Adding new tests is permitted and encouraged.
- Do NOT modify production behavior in `scripts/dev-tools/new-claude-worktree-session.ps1`, its bundled template, or any TypeScript production file. The single sanctioned production edit is a behavior-preserving dot-source guard that skips the top-level script body only when the file is dot-sourced (tests); direct invocation behavior is unchanged. Any script edit MUST be applied in lockstep to the template and re-verified with `git diff --no-index`.
- Do NOT mock `git` or other executables directly; respect the wrapper/scriptblock seam rules in `.claude/rules/powershell.md`.
- Evidence is written ONLY under `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/<kind>/`. Non-canonical paths (for example `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`) are rejected; substitute the canonical path and record `EVIDENCE_LOCATION_OVERRIDE_REJECTED: <supplied> replaced with <canonical>`.
- Do NOT modify policy documents under `.claude/rules/` or `.github/instructions/`.
- Prefer the direct fix (valid measurable coverage) over the exception dossier. The dossier is a fallback branch, taken only when a threshold is not numerically met after the direct fix because the remaining uncovered surface is structurally uncoverable without a prohibited production refactor or a prohibited direct executable mock.

## Root-Cause Summary (informs the fix)

The changed script is outside the committed `CodeCoverage.Path` denominator, and the feature test suite resolves functions via `Import-ScriptFunction` (`tests/scripts/powershell/Support/TestHelpers.ps1`), which re-parses each function's extracted AST text with `Parser::ParseInput`, restarting line numbers at 1 and defeating Pester breakpoint attribution (invalid 4.88% probe). The script also has a top-level body (lines 232-273) that calls `git rev-parse` directly (no wrapper seam) and is not exercised by any unit test. The direct fix restores valid attribution by dot-sourcing the whole file (guarded so the body does not execute during tests) and adds the file to `CodeCoverage.Path`. If the resulting file-level figure is below threshold solely because of the host-bound, seam-less top-level body, the fallback exception dossier discharges the threshold intent for that structurally uncoverable remainder.

---

### Phase 0 — Baseline capture and policy reads

- [x] [P0-T1] Read policy files in the required order (`CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, `.claude/rules/quality-tiers.md`) and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: artifact exists with all three fields populated.
- [x] [P0-T2] Capture the PowerShell full-suite baseline: run `mcp__drm-copilot__run_poshqc_test` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/2026-07-07T14-00-baseline-poshqc-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (test pass count and repo-wide line-coverage headline, noting the changed file is NOT yet in the denominator). Acceptance: artifact records `EXIT_CODE: 0` and the numeric baseline line-coverage headline.
- [x] [P0-T3] Capture the pre-fix targeted coverage of the changed file using the exact command `pwsh -NoProfile -Command "$cfg = New-PesterConfiguration; $cfg.Run.Path = 'tests/scripts/dev-tools/new-claude-worktree-session.Tests.ps1'; $cfg.CodeCoverage.Enabled = $true; $cfg.CodeCoverage.Path = 'scripts/dev-tools/new-claude-worktree-session.ps1'; $cfg.CodeCoverage.OutputFormat = 'JaCoCo'; $cfg.CodeCoverage.OutputPath = 'docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/2026-07-07T14-00-baseline-targeted-ps-coverage.xml'; Invoke-Pester -Configuration $cfg"` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/2026-07-07T14-00-baseline-targeted-ps-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` documenting the invalid ~4.88% attribution figure as the pre-fix reference. Acceptance: artifact records the pre-fix percentage and states it is an attribution artifact, not a valid coverage result.
- [x] [P0-T4] Capture the PSScriptAnalyzer baseline: run `mcp__drm-copilot__run_poshqc_analyze` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/2026-07-07T14-00-baseline-poshqc-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records no findings.
- [x] [P0-T5] Capture the template-parity baseline: run `git diff --no-index scripts/dev-tools/new-claude-worktree-session.ps1 extensions/drm-copilot/resources/templates/new-claude-worktree-session.ps1` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/remediation-baseline/2026-07-07T14-00-baseline-template-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records `EXIT_CODE: 0` (byte-identical).

### Phase 1 — Restore valid PowerShell coverage attribution (R1/R2 direct fix)

- [x] [P1-T1] In `scripts/dev-tools/new-claude-worktree-session.ps1`, insert a behavior-preserving dot-source guard immediately before the `# Script body` section (after the last function definition, before line 236 in the current file) that returns without executing the top-level body when the file is dot-sourced (for example `if ($MyInvocation.InvocationName -eq '.') { return }`), leaving all function definitions in scope. Acceptance: the guard is present; direct invocation still executes the body (unchanged behavior) and dot-sourcing defines the functions without running the body.
- [x] [P1-T2] Apply the identical guard edit in lockstep to `extensions/drm-copilot/resources/templates/new-claude-worktree-session.ps1`, then run `git diff --no-index scripts/dev-tools/new-claude-worktree-session.ps1 extensions/drm-copilot/resources/templates/new-claude-worktree-session.ps1`. Acceptance: `git diff --no-index` exits 0 (script and template byte-identical).
- [x] [P1-T3] In `tests/scripts/dev-tools/new-claude-worktree-session.Tests.ps1`, change the `BeforeAll` block to dot-source the whole script file (`. $script:scriptPath`) so functions are resolved from the original file for correct Pester line attribution, and remove the per-`Describe` `BeforeEach { . (Import-ScriptFunction ...) }` blocks that were the source of restarted line numbers. Keep the two Integration Validation structural tests (raw `Get-Content` text assertions) and the Template parity test unchanged. Acceptance: no `Import-ScriptFunction` call remains for function resolution in this test file; the suite still exercises all nine functions.
- [x] [P1-T4] Add a Pester `It` block to `tests/scripts/dev-tools/new-claude-worktree-session.Tests.ps1` covering the previously untested `Invoke-GitWorktreeAdd` function via its injected `$InvokeGit` scriptblock seam (capture the git argument array; assert `worktree add <path> -b <branch>` composition), without mocking the `git` executable directly. Acceptance: the new test exists and passes; `Invoke-GitWorktreeAdd` commands now execute under test.
- [x] [P1-T5] Add `'scripts/dev-tools/new-claude-worktree-session.ps1'` to the `CodeCoverage.Path` array in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` without removing or reordering any existing entry and without adding any `exclude` entry. Acceptance: the changed file path is present in `CodeCoverage.Path`; no existing entry is removed; no production `exclude` entry is added.

### Phase 2 — Measure coverage and record disposition (R1/R2)

- [x] [P2-T1] Run the targeted coverage measurement with the exact command `pwsh -NoProfile -Command "$cfg = New-PesterConfiguration; $cfg.Run.Path = 'tests/scripts/dev-tools/new-claude-worktree-session.Tests.ps1'; $cfg.CodeCoverage.Enabled = $true; $cfg.CodeCoverage.Path = 'scripts/dev-tools/new-claude-worktree-session.ps1'; $cfg.CodeCoverage.OutputFormat = 'JaCoCo'; $cfg.CodeCoverage.OutputPath = 'docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-targeted-ps-coverage.xml'; Invoke-Pester -Configuration $cfg"` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-targeted-ps-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the post-fix numeric line coverage for the changed file. Acceptance: the JaCoCo artifact contains `scripts/dev-tools/new-claude-worktree-session.ps1` in its denominator with a valid (non-attribution-artifact) line figure, and the suite is green (0 test failures).
- [x] [P2-T2] Run the full suite via `mcp__drm-copilot__run_poshqc_test` (now with the changed file in `CodeCoverage.Path`) and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-fullsuite-poshqc-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording pass count and the repo-wide post-change line-coverage headline. Acceptance: `EXIT_CODE: 0`, 0 test failures, no test regression versus P0-T2.
- [x] [P2-T3] Record the R1 line-coverage disposition. If the P2-T1 changed-file line coverage is >= 85%, write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-ps-line-coverage-disposition.md` recording PASS with the numeric figure and no dossier is required. Otherwise (the shortfall is caused solely by the host-bound, seam-less top-level script body, which cannot be unit-covered without a prohibited production refactor or a prohibited direct `git` mock), author a sanctioned tooling-limitation / structural-impossibility exception dossier at `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/regression-testing/fail-before-exception.2026-07-07T14-00-ps-line-coverage.md` per `evidence-and-timestamp-conventions` (`WhyFailingRunImpossible` and alternative-proof section) with a complete per-function and per-command enumeration proving the >= 85% intent for the testable surface and identifying each uncoverable top-level-body command. Acceptance: exactly one of the two outcomes is recorded — PASS with numeric line figure >= 85%, or a schema-valid dossier enumerating every command and its coverage status. This task's fallback branch is explicitly authorized.
- [x] [P2-T4] Record the R2 branch-coverage disposition. Parse the P2-T1 JaCoCo artifact for a `BRANCH` counter. If a `BRANCH` counter is present and shows >= 75% for the changed file, write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-ps-branch-coverage-disposition.md` recording PASS with the numeric figure. Otherwise (Pester emits command-based coverage only and no `BRANCH` counter is produced), author a sanctioned branch-coverage exception dossier at `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/regression-testing/fail-before-exception.2026-07-07T14-00-ps-branch-coverage.md` (may be combined with the P2-T3 dossier) containing a complete per-branch enumeration for every conditional in the changed/new functions (`Get-WorktreeTimestamp`, `Get-WorktreeGroupDirectory`, `Build-WorktreePath`, `Build-BranchName` empty/non-empty branch, `New-WorktreeParentDirectory` ShouldProcess branch, `Test-PreconditionsMet` git/claude/path-exists branches, `Start-ClaudeBackground` Windows/non-Windows and Objective branches, `Invoke-GitWorktreeAdd`) mapped to the tests that exercise each. Acceptance: exactly one of the two outcomes is recorded — PASS with numeric branch figure >= 75%, or a schema-valid dossier enumerating every conditional. This task's fallback branch is explicitly authorized.

### Phase 3 — Final PowerShell QA loop, coverage evidence, and AC9 reconciliation

- [x] [P3-T1] Run the formatting stage `mcp__drm-copilot__run_poshqc_format` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-final-poshqc-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 0` with no residual formatting changes to committed files.
- [x] [P3-T2] Run the linting stage `mcp__drm-copilot__run_poshqc_analyze` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-final-poshqc-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: no PSScriptAnalyzer findings.
- [x] [P3-T3] Run the testing stage in coverage mode `mcp__drm-copilot__run_poshqc_test` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-final-poshqc-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass count plus repo-wide and changed-file line-coverage figures). If P3-T1, P3-T2, or P3-T3 changed any file or failed, restart the loop at P3-T1. Acceptance: a single clean pass of P3-T1 -> P3-T2 -> P3-T3 with `EXIT_CODE: 0` and 0 test failures.
- [x] [P3-T4] Write the coverage delta/threshold verification artifact `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-ps-coverage-delta.md` recording baseline line coverage (P0-T2), post-change line coverage (P2-T2/P3-T3), the changed-file line figure (P2-T1), and the branch disposition (P2-T4). Acceptance: the artifact records all four values; if any required threshold is unmet and no sanctioned dossier discharges it, the cycle outcome is remediation-required (not PASS).
- [x] [P3-T5] Re-verify template parity with `git diff --no-index scripts/dev-tools/new-claude-worktree-session.ps1 extensions/drm-copilot/resources/templates/new-claude-worktree-session.ps1` and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-final-template-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 0`.
- [x] [P3-T6] Reconcile AC9 (R3). Confirm the AC9 checkbox in `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/spec.md` (line 273) and `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/user-story.md` (line 80) is `[x]` and consistent with a PASS evaluation, making NO text change to the criterion, and write `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/other/2026-07-07T14-00-ac9-reconciliation.md` documenting the confirmed state. Acceptance: both AC9 checkboxes remain `[x]` with identical criterion text, and the evidence artifact records the consistency with the coverage disposition from Phase 2.
- [x] [P3-T7] Write the cycle exit-condition summary `docs/features/active/2026-07-07-nested-worktree-folder-scheme-328/evidence/qa-gates/2026-07-07T14-00-cycle-exit-summary.md` asserting: PowerShell coverage disposition PASS (valid numeric line figure >= 85% for the changed file, or sanctioned dossier per P2-T3; branch figure >= 75% or sanctioned dossier per P2-T4), AC9 PASS (P3-T6), and zero remaining Blocking findings. Acceptance: the artifact links every supporting evidence file and states the exit condition is met.

---

## Finding-to-Task and Exit-Condition Map

| Finding | Severity | Tasks | Exit-condition contribution |
|---|---|---|---|
| R1 — changed PS file line coverage measurable + valid >= 85% | Blocking | P1-T1, P1-T2, P1-T3, P1-T4, P1-T5, P2-T1, P2-T2, P2-T3, P3-T3, P3-T4 | policy-audit PowerShell coverage disposition PASS (line) |
| R2 — PS branch coverage >= 75% or sanctioned exception dossier | Blocking (may bundle with R1) | P1-T5, P2-T1, P2-T4, P3-T4 | policy-audit PowerShell coverage disposition PASS (branch) |
| R3 — AC9 source-file state consistent with PASS (no text change) | Non-blocking bookkeeping | P3-T6 | AC9 PASS in re-audit |
| Cycle exit condition (aggregate) | — | P3-T7 | PS coverage PASS + AC9 PASS + zero Blocking |

## Preflight

Return this plan for validation-only preflight through `atomic-executor` using the directive `DIRECTIVE: PREFLIGHT VALIDATION ONLY`, and require one of `PREFLIGHT: ALL CLEAR` or `PREFLIGHT: REVISIONS REQUIRED`. Reuse this exact file path for all revision iterations. Before treating the plan as approved, run `mcp__drm-copilot__validate_orchestration_artifacts` with `artifact_type: "plan"` and `artifact_path` set to this file, and reject the plan if the validator exits non-zero.
