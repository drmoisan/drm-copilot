# Remediation Plan — Issue #412, Cycle 1

- **Timestamp:** 2026-07-25T19-30
- **Issue:** #412
- **Work Mode:** full-bug (AC source: `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/spec.md`)
- **Feature folder:** `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412`
- **Base branch:** `main`
- **Blocking finding:** F-1 in `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/remediation-inputs.2026-07-25T19-30.md` (elevates CR-1 in `code-review.2026-07-25T19-14.md`)
- **Plan path continuity:** this file is the sole plan file for cycle 1; all preflight revisions update this file in place. No timestamped sibling plan files.

## Scope

Close the fail-open path in the portable PowerShell PR-creation readiness gate: `Get-OrchestratorStatePrCreationReadinessError` in `.claude/lib/orchestrator-state/OrchestratorState.psm1` (line 319) blocks only `pending`/`blocked` on steps 5–8, while the Python reference `scripts/dev_tools/_orchestrator_state_pr_creation_readiness.py` (`PR_CREATION_BLOCKING_STEP_STATUS`, lines 63–67) also blocks `blocked_remediation_loop_limit`. This cycle is PowerShell-only.

**File budget (hard cap):** 2 production PowerShell files and 1 test file, within the direct-mode cap of 2 production files:

1. `.claude/lib/orchestrator-state/OrchestratorState.psm1` (production)
2. `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorState.psm1` (production, byte mirror)
3. `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` (test)

## Hard Constraints

- **500-line limit:** `.claude/lib/orchestrator-state/OrchestratorState.psm1` is at 498 lines against the 500-line hard limit (2-line budget). The intended minimal edit — changing the single `if` condition at line 319 to test membership in a blocked set and updating the `.DESCRIPTION` wording — can be line-neutral. If the post-edit count would exceed 500, the sanctioned response is to reflow the `.DESCRIPTION` block and the loop comment to the module's existing maximum line width (123 characters; no line-length rule is enforced and PSScriptAnalyzer does not run `PSAvoidLongLines` here), which reclaims lines without adding declarations. Hoisting the blocked set into the existing `$script:` constant block or extracting a helper function is NOT sanctioned as a line-budget remedy: in the module's established constant style a hoist costs +5 lines (503), the minimum viable hoist costs +2 (exactly 500, consuming the entire budget), and a helper extraction costs more still. Creating a fourth production PowerShell file is NOT sanctioned. Raising the limit is NOT sanctioned.
- **Untouchable constants:** the shared `$script:VALID_STEP_STATUS` array and the per-key `$script:STEP_SPECIFIC_EXTRA_STATUS` map MUST NOT change. `blocked_remediation_loop_limit` must remain plain-valid on `step6_status`. Only the readiness gate changes.
- **Message parity:** the emitted message must stay byte-identical to the existing form `Checkpoint PR-creation readiness validation failed: <key> is <value>.` and to the Python gate's corresponding string (`_orchestrator_state_pr_creation_readiness.py` line 109).
- **Mirror atomicity:** root module and byte mirror land in the same phase; `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` enforces content identity.
- **MUST NOT CHANGE:** `config/orchestration-routing.json` and its bundled mirror, `.claude/skills/orchestrate/SKILL.md`, `.claude/rules/orchestrator-state.md`, `.claude/hooks/validate-orchestrator-output.ps1`, `.claude/hooks/enforce-epic-merge-gate.ps1`, `.claude/settings.json`, both batch-budget hooks, `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`, `extensions/drm-copilot/jest.config.cjs`, and every Python and TypeScript file.
- **PoshQC MCP calls:** every `mcp__drm-copilot__run_poshqc_*` task carries `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585` and records that value in the evidence artifact's `Command:` field.
- **PoshQC test scope caveat:** `mcp__drm-copilot__run_poshqc_test` executes the npx-cached published bundle, not the working tree, so it does not exercise edits to bundled `.claude/lib` modules. It remains the mandated gate and coverage denominator. Every phase that must exercise the edited module pairs it with a direct repo-root Pester invocation.
- **Pester exit code:** every direct repo-root Pester invocation uses `-PassThru` plus an explicit `if ($r.FailedCount -gt 0) { exit 1 } else { exit 0 }`. A bare `Invoke-Pester` exits 0 even when tests fail. `-CI` is prohibited: it enables TestResult output and overwrites the tracked repo-root `testResults.xml`.
- **Push-down guard:** before every full-suite `pytest` run and every mirror-parity task, run `pwsh -NoProfile -Command "Remove-Item -Path 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585/.claude/state' -Recurse -Force -ErrorAction SilentlyContinue; exit 0"` (the trailing `exit 0` is required because `Remove-Item` against an absent path otherwise leaves a non-zero exit). Record the guard invocation in the same task's evidence artifact `Output Summary:`.
- **Working directory:** the Bash tool resets the working directory before every call; every command inlines its location (`Set-Location` inside the `pwsh -Command` string, or absolute paths).
- **No temporary files:** all test fixtures and probe checkpoints are constructed in memory. The probe checkpoint MUST be a `[pscustomobject]` (or the output of `ConvertFrom-Json`), never a `[hashtable]`. `Get-OrchestratorStateField` reads `$State.PSObject.Properties.Name`; a hashtable exposes no checkpoint keys there, so every field reads as absent and the function returns 0 errors regardless of content. The module MUST be imported by `./`-prefixed relative path or absolute path; a bare relative path resolves as a module name and fails to load.
- **Coverage reporting:** Pester 5 with `CoverageGutters` reports command coverage, not branch coverage. Coverage tasks record the obtainable values — overall percent, covered/analyzed command counts, and per-file counts for `OrchestratorState.psm1` — plus an explicit tooling-limitation note. `UNVERIFIED` is never a valid recorded value.
- **Evidence schema:** every command-bearing task names its evidence artifact; each artifact carries `Timestamp:` (format `yyyy-MM-ddTHH-mm`), `Command:`, `EXIT_CODE:`, `Output Summary:`. All evidence paths resolve under `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/<kind>/`. `artifacts/`-rooted evidence paths are invalid.
- **QA restart rule:** within any toolchain sequence (format → analyze → test), if a step fails or changes files, restart that phase's sequence from the format step and repeat until a clean single pass completes.
- **No-SKIPPED rule:** every command task in this plan must execute its stated command; `EXIT_CODE: SKIPPED` is not a passing outcome for any task in this plan.

## Recorded Baselines for Comparison (from feature post-Phase-6 evidence)

- PowerShell: 1391 tests / 0 failures / 0 errors / 9 disabled; line coverage 90.26% (2159/2392); overall command coverage 89.73% (2945/3282); `OrchestratorState.psm1` 144/149 commands (96.64%).
- Python guard: 2123 passed.

### Phase 0 — Remediation Baseline

- [x] [P0-T1] Read the policy files in `policy-compliance-order` sequence: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/powershell.md`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields and lists all four files in the stated order.
- [x] [P0-T2] Record the pre-edit line count of the target module. Command: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; (Get-Content .claude/lib/orchestrator-state/OrchestratorState.psm1).Count"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/module-line-count-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the count; expected 498). In the same task, also record the pre-cycle head SHA: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; git rev-parse HEAD"`. The expected value at preflight time is `81f3df3fb122db6d2dd8c51520e9ab8a2b1f7da5` (`docs(412): record Phase 6 full-repo QA evidence and close plan/spec`); if the actual value differs, record the actual value and use it downstream. Write both the line count and the SHA into the same artifact under a `Pre-cycle HEAD:` field. Acceptance: artifact records the numeric count.
- [x] [P0-T3] Run PowerShell format baseline via `mcp__drm-copilot__run_poshqc_format` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/poshqc-format-baseline.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 and no files changed by the formatter.
- [x] [P0-T4] Run PowerShell analyzer baseline via `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/poshqc-analyze-baseline.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 with 0 findings.
- [x] [P0-T5] Run PowerShell test-with-coverage baseline via `mcp__drm-copilot__run_poshqc_test` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/poshqc-test-baseline.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, and an `Output Summary:` recording numeric values: total/passed/failed/disabled test counts, overall command-coverage percent with covered/analyzed command counts, and per-file covered/analyzed counts for `OrchestratorState.psm1` (expected to match the recorded baseline: 1391/0/0/9; 89.73% (2945/3282); 144/149 (96.64%)). Include the note that this tool executes the npx-cached published bundle (not the working tree) and that Pester 5 `CoverageGutters` reports command coverage, not branch coverage. Acceptance: exit 0, 0 failures, all numeric values recorded (never `UNVERIFIED`).
- [x] [P0-T6] Run the direct repo-root Pester baseline over the working-tree module: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; $r = Invoke-Pester -Path tests/scripts/claude-lib/orchestrator-state -Output Detailed -PassThru; Write-Host ('SUMMARY Passed=' + $r.PassedCount + ' Failed=' + $r.FailedCount + ' Skipped=' + $r.SkippedCount); if ($r.FailedCount -gt 0) { exit 1 } else { exit 0 }"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/remediation-baseline/pester-direct-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (passed/failed/skipped counts). Acceptance: exit 0 with `Failed=0` in the `SUMMARY` line.
- [x] [P0-T7] [expect-fail] Demonstrate the fail-open before the fix. PowerShell probe command (single invocation, in-memory `[pscustomobject]` checkpoint, `./`-prefixed module import, plus a mandatory positive control): `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; $m = Import-Module ./.claude/lib/orchestrator-state/OrchestratorState.psm1 -Force -PassThru; $state = [pscustomobject]@{ step5_status='verified'; step6_status='blocked_remediation_loop_limit'; step7_status='verified'; step8_status='verified'; blocked_reason='none' }; $errs = @(& $m { param($s) Get-OrchestratorStatePrCreationReadinessError -State $s } $state); Write-Host ('PROBE count=' + $errs.Count); $errs | ForEach-Object { Write-Host ('  [' + $_ + ']') }; $ctl = [pscustomobject]@{ step5_status='pending'; step6_status='verified'; step7_status='verified'; step8_status='verified'; blocked_reason='none' }; $c = @(& $m { param($s) Get-OrchestratorStatePrCreationReadinessError -State $s } $ctl); Write-Host ('POSITIVE-CONTROL count=' + $c.Count); $c | ForEach-Object { Write-Host ('  [' + $_ + ']') }"`. Then run the Python gate on the equivalent in-memory dict: `poetry run python -c "from scripts.dev_tools._orchestrator_state_pr_creation_readiness import validate_orchestrator_state_pr_creation_readiness as v; print(v({'step6_status': 'blocked_remediation_loop_limit'}))"` (run from `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`). Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/regression-testing/remediation1-fail-open-probe-before.md` with `Timestamp:`, both `Command:` lines, both `EXIT_CODE:` values, and an `Output Summary:` recording that the PowerShell path returned 0 errors (fail-open, the F-1 defect) while the Python gate returned the readiness error `Checkpoint PR-creation readiness validation failed: step6_status is blocked_remediation_loop_limit.`. Acceptance: artifact records the divergent before-fix behavior exactly as described and records the positive control returning exactly one error (`Checkpoint PR-creation readiness validation failed: step5_status is pending.`). The positive control is mandatory: without it a `PROBE count=0` result is not distinguishable from a probe that is not reaching the function at all.

### Phase 1 — Close the Readiness Fail-Open

- [x] [P1-T1] Extend `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`: add one `It` block in the existing `Context 'rejection conditions'` (Describe `Test-OrchestratorStatePrCreationReadiness`) asserting that a ready checkpoint fixture with `step6_status = 'blocked_remediation_loop_limit'` yields `ExitCode` 1 and an `Output` containing the byte-exact string `Checkpoint PR-creation readiness validation failed: step6_status is blocked_remediation_loop_limit.`. Use the assertion form `$result.Output | Should -BeLike '*Checkpoint PR-creation readiness validation failed: step6_status is blocked_remediation_loop_limit.*'` — not `Should -Match`, whose regex semantics make the trailing `.` a wildcard and do not verify a byte-exact string. Use the existing in-memory `New-ReadyCheckpoint` + `Set-CheckpointFixture` pattern; no temporary files. As part of acceptance, verify (do not duplicate) that the following existing cases remain present and unmodified: the readiness `pending` rejection (`step5_status is pending`), the readiness `blocked` rejection (`step6_status is blocked`), and the base-validation acceptance `It 'accepts step6_status value blocked_remediation_loop_limit'` in Describe `Get-OrchestratorStateBasePresenceError per-step-key status vocabulary`. Acceptance: the new `It` exists with the byte-exact expected string; the three existing cases are present and untouched; only this one test file is modified.
- [x] [P1-T2] [expect-fail] Run the direct repo-root Pester suite against the not-yet-edited module: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; $r = Invoke-Pester -Path tests/scripts/claude-lib/orchestrator-state -Output Detailed -PassThru; Write-Host ('SUMMARY Passed=' + $r.PassedCount + ' Failed=' + $r.FailedCount + ' Skipped=' + $r.SkippedCount); if ($r.FailedCount -gt 0) { exit 1 } else { exit 0 }"`. Expected: exactly the new `It` from P1-T1 fails; all pre-existing tests pass. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/regression-testing/remediation1-new-test-expect-fail.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming the single failing test. Acceptance: artifact records `EXIT_CODE: 1` and a `SUMMARY` line showing exactly one failure, naming the P1-T1 test; every pre-existing test passes. The non-zero exit is produced by the explicit `exit 1` branch, because `Invoke-Pester` alone does not propagate a failing status and `-CI` is prohibited (it rewrites the tracked repo-root `testResults.xml`).
- [x] [P1-T3] Edit `.claude/lib/orchestrator-state/OrchestratorState.psm1`: change the readiness condition at line 319 in `Get-OrchestratorStatePrCreationReadinessError` from the two-literal comparison to a membership test against the blocked set `pending`, `blocked`, `blocked_remediation_loop_limit`, preserving the byte-identical message form `Checkpoint PR-creation readiness validation failed: $key is $($field.Value).`; update the function's `.DESCRIPTION` (lines 294–300) and the loop comment (lines 315–316) so they state the actual blocked set instead of "pending/blocked". Do NOT modify `$script:VALID_STEP_STATUS`, `$script:STEP_SPECIFIC_EXTRA_STATUS`, or any other function. Prefer a line-neutral edit. Acceptance: the readiness loop blocks all three values, the message interpolation string is unchanged, and no other function or constant in the module is modified.
- [x] [P1-T4] Measure the post-edit line count: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; (Get-Content .claude/lib/orchestrator-state/OrchestratorState.psm1).Count"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-module-line-count-post.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the count and the delta versus the P0-T2 baseline of 498). Acceptance: recorded count is <= 500. If the count exceeds 500, revise P1-T3 by reflowing the `.DESCRIPTION` block and the loop comment to the module's existing maximum line width (123 characters; no line-length rule is enforced and PSScriptAnalyzer does not run `PSAvoidLongLines` here), which reclaims lines without adding declarations. Do NOT hoist the blocked set into the `$script:` constant block or extract a helper as a line-budget remedy: in the module's established constant style a hoist costs +5 lines (503) and the minimum viable hoist costs +2 (exactly 500, consuming the entire budget), and a helper extraction costs more still. Do NOT create a fourth production PowerShell file and do NOT raise the limit. Re-run this task until the count is <= 500.
- [x] [P1-T5] Verify the untouchable constants are unchanged: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; git diff -U0 -- .claude/lib/orchestrator-state/OrchestratorState.psm1"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-constants-unchanged.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming no diff hunk touches the `$script:VALID_STEP_STATUS` array or the `$script:STEP_SPECIFIC_EXTRA_STATUS` map, and that `blocked_remediation_loop_limit` remains listed under `step6_status` in the map. Acceptance: all diff hunks fall within `Get-OrchestratorStatePrCreationReadinessError` (the P1-T4 reflow contingency, if fired, also stays within that function's `.DESCRIPTION` and loop comment); neither constant definition appears in any hunk.
- [x] [P1-T6] Byte-mirror the edited module: copy `.claude/lib/orchestrator-state/OrchestratorState.psm1` over `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorState.psm1`, then verify identity: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; Copy-Item .claude/lib/orchestrator-state/OrchestratorState.psm1 extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorState.psm1 -Force; if ((Get-FileHash .claude/lib/orchestrator-state/OrchestratorState.psm1).Hash -eq (Get-FileHash extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorState.psm1).Hash) { exit 0 } else { exit 1 }"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-byte-mirror.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (both hashes). Acceptance: exit 0 with matching hashes.
- [x] [P1-T7] Run `mcp__drm-copilot__run_poshqc_format` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase1-poshqc-format.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0. If the formatter changes any file, re-apply P1-T6 (mirror) and restart the Phase 1 QA sequence from this task.
- [x] [P1-T8] Run `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase1-poshqc-analyze.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 with 0 findings. On failure, fix and restart from P1-T7.
- [x] [P1-T9] Run `mcp__drm-copilot__run_poshqc_test` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase1-poshqc-test.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, and `Output Summary:` with numeric values (test counts; overall command-coverage percent and covered/analyzed counts; per-file counts for `OrchestratorState.psm1`), the npx-bundle caveat, and the command-coverage tooling-limitation note. Acceptance: exit 0, 0 failures, numeric values recorded. On failure, fix and restart from P1-T7.
- [x] [P1-T10] Run the direct repo-root Pester suite against the edited working-tree module: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; $r = Invoke-Pester -Path tests/scripts/claude-lib/orchestrator-state -Output Detailed -PassThru; Write-Host ('SUMMARY Passed=' + $r.PassedCount + ' Failed=' + $r.FailedCount + ' Skipped=' + $r.SkippedCount); if ($r.FailedCount -gt 0) { exit 1 } else { exit 0 }"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/regression-testing/remediation1-new-test-pass-after.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (passed/failed counts; explicit note that the P1-T1 test now passes). Acceptance: exit 0 with `Failed=0` in the `SUMMARY` line; the P1-T1 test passes. On failure, fix and restart from P1-T7.
- [x] [P1-T11] Run the push-down guard, then the mirror-parity check. Commands: `pwsh -NoProfile -Command "Remove-Item -Path 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585/.claude/state' -Recurse -Force -ErrorAction SilentlyContinue; exit 0"` followed by `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` (run from `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`). Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-mirror-parity.md` with `Timestamp:`, both `Command:` lines, the pytest `EXIT_CODE:`, and an `Output Summary:` that records the guard invocation and the pytest pass count. Acceptance: guard executed, pytest exit 0 with all tests passed.

### Phase 2 — Verification and Reconciliation

- [x] [P2-T1] Pass-after probe mirroring P0-T7 exactly: re-run the identical P0-T7 command verbatim (including the positive control) and the same Python one-liner. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/regression-testing/remediation1-fail-open-probe-after.md` with `Timestamp:`, both `Command:` lines, both `EXIT_CODE:` values, and an `Output Summary:` recording that the PowerShell path now returns exactly one error and that its string is byte-identical to the Python gate's `Checkpoint PR-creation readiness validation failed: step6_status is blocked_remediation_loop_limit.`. Acceptance: both paths reject with byte-identical message strings; the probe returns exactly one error whose string is byte-identical to the Python gate's, and the positive control still returns exactly one error.
- [x] [P2-T2] Full-repo QA re-run, step 1: `mcp__drm-copilot__run_poshqc_format` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase2-poshqc-format.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 and no files changed. If any file changes, re-apply the P1-T6 mirror copy and restart the Phase 2 QA sequence from this task.
- [x] [P2-T3] Full-repo QA re-run, step 2: `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase2-poshqc-analyze.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 with 0 findings. On failure, fix and restart from P2-T2.
- [x] [P2-T4] Full-repo QA re-run, step 3: `mcp__drm-copilot__run_poshqc_test` with `workspace_root: C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase2-poshqc-test.md` with `Timestamp:`, `Command:` (including the `workspace_root` value), `EXIT_CODE:`, and `Output Summary:` with numeric values (test counts; overall command-coverage percent with covered/analyzed counts; per-file counts for `OrchestratorState.psm1`), the npx-bundle caveat, and the command-coverage tooling-limitation note. Acceptance: exit 0, 0 failures, numeric values recorded (never `UNVERIFIED`). On failure, fix and restart from P2-T2.
- [x] [P2-T5] Full-repo QA re-run, step 4: direct repo-root Pester over the edited working-tree module: `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; $r = Invoke-Pester -Path tests/scripts/claude-lib/orchestrator-state -Output Detailed -PassThru; Write-Host ('SUMMARY Passed=' + $r.PassedCount + ' Failed=' + $r.FailedCount + ' Skipped=' + $r.SkippedCount); if ($r.FailedCount -gt 0) { exit 1 } else { exit 0 }"`. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-phase2-pester-direct.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (passed/failed counts). Acceptance: exit 0 with `Failed=0` in the `SUMMARY` line. On failure, fix and restart from P2-T2.
- [x] [P2-T6] Python no-regression guard (no Python files changed in this cycle). Run the push-down guard `pwsh -NoProfile -Command "Remove-Item -Path 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585/.claude/state' -Recurse -Force -ErrorAction SilentlyContinue; exit 0"`, then `poetry run pytest --cov --cov-branch` (run from `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`). Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-pytest-guard.md` with `Timestamp:`, both `Command:` lines, the pytest `EXIT_CODE:`, and an `Output Summary:` recording the guard invocation, the pass count (recorded baseline: 2123 passed), and the numeric line and branch coverage percentages. Acceptance: exit 0, 0 failures, pass count >= 2123, numeric coverage recorded.
- [x] [P2-T7] Coverage comparison against the recorded baselines. Compare: (a) PowerShell overall command coverage and covered/analyzed counts from P2-T4 versus the recorded baseline 89.73% (2945/3282) and line coverage 90.26% (2159/2392); (b) per-file `OrchestratorState.psm1` counts from P2-T4 versus the recorded baseline 144/149 (96.64%); (c) Python line/branch coverage from P2-T6 versus its own run (guard only; no Python change expected). Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-coverage-comparison.md` with `Timestamp:`, `Command:` (source-artifact references and any extraction commands), `EXIT_CODE:`, and an `Output Summary:` stating baseline value, post-change value, and delta for each metric, plus the explicit note that Pester 5 `CoverageGutters` reports command coverage rather than branch coverage. Acceptance: all values numeric (never `UNVERIFIED`); no coverage regression on `OrchestratorState.psm1` (post-change per-file coverage >= 96.64% or covered-command delta explained by the changed lines being covered by the P1-T1 test); overall PowerShell coverage not reduced below the >= 85% line-coverage policy floor.
- [x] [P2-T8] Changed-file scope verification. Commands, both run from `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585`: (a) `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; git diff --name-only <PRE_CYCLE_HEAD>..HEAD -- ':!docs/features'"` where `<PRE_CYCLE_HEAD>` is the SHA recorded in P0-T2; (b) `pwsh -NoProfile -Command "Set-Location 'C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a682ed107a9c0c585'; git status --short"`. The union of (a) and (b), excluding `docs/features`, is this cycle's modified file set. Do NOT use `main...HEAD`: that returns the whole branch (18 files at preflight time), which is the main plan's already-reviewed work and not this cycle's scope. Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-scope-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` confirming that the only files this cycle modified beyond the feature folder are the three budgeted files (`.claude/lib/orchestrator-state/OrchestratorState.psm1`, its resources byte mirror, `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`) and that every MUST-NOT-CHANGE file listed in the Hard Constraints section is untouched by this cycle (no Python, TypeScript, config, hook, skill, rule, or settings file changed relative to the pre-cycle head). Acceptance: cycle-modified file set outside `docs/features` equals exactly the three budgeted files.
- [x] [P2-T9] Acceptance-criteria reconciliation against `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/spec.md` (work mode full-bug; spec is the AC source). For each acceptance criterion in `spec.md`, record PASS/UNCHANGED with the evidence artifact that supports it, confirming no criterion regressed as a result of this cycle (in particular: `blocked_remediation_loop_limit` remains plain-valid on `step6_status` — pinned by the untouched base-validation acceptance test verified in P1-T1 and the constants check in P1-T5). Write `docs/features/active/2026-07-25-orchestration-state-contract-divergences-412/evidence/qa-gates/remediation1-ac-reconciliation.md` with `Timestamp:`, `Command:` (or the reviewed-artifact list), `EXIT_CODE:`, and an `Output Summary:` containing the per-AC result table. Acceptance: every spec AC is reconciled with a supporting evidence reference and none regressed.
