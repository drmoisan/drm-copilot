# Feature Audit: legacy-discovery-init-templates (#362) — R4 Re-Audit, Remediation Cycle 2

**Template source note:** MCP template resolution is unavailable in this session; the canonical repository template `docs/features/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md` was used as the authoritative structure source.

**Audit Date:** 2026-07-18
**Feature Folder:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/`
**Base Branch:** `epic/legacy-discovery-and-parity-integration`
**Head Branch:** `feature/legacy-discovery-init-templates-362` (HEAD `48939e3e3f551dcf45465aa9be06560f86db65b5`)
**Work Mode:** `full-feature` (per `- Work Mode: full-feature` in `issue.md`) → AC sources are `spec.md` and `user-story.md`.
**Audit Type:** Post-remediation acceptance re-verification (R4, remediation cycle 2 — merge with the integration branch's current tip).

---

## Scope and Baseline

- **Base branch:** `epic/legacy-discovery-and-parity-integration` (merge-base `85e7bea2bd2695114c9feffb2a4963da9f37c9ad`, independently confirmed via `git merge-base HEAD origin/epic/legacy-discovery-and-parity-integration`).
- **Head branch/commit:** `feature/legacy-discovery-init-templates-362` at `48939e3e3f551dcf45465aa9be06560f86db65b5`.
- **Merge base:** `85e7bea2bd2695114c9feffb2a4963da9f37c9ad`.
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this audit; was stale prior to regeneration — see `policy-audit.2026-07-18T17-57.md`, "PR-Context Freshness Correction").
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt` (regenerated alongside the summary).
  - Feature evidence: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/**`.
  - Additional evidence: independent toolchain re-execution and independent verification of `pyproject.toml` conflict resolution, documented in `policy-audit.2026-07-18T17-57.md` and `code-review.2026-07-18T17-57.md`.
- **Feature folder used:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/` (only active feature folder whose suffix, `-362`, matches the issue number in the branch name and in the canonical issue reference for this feature).
- **Requirements source:** `spec.md` and `user-story.md` (per `full-feature` work mode).
- **Work mode resolution note:** `issue.md` line 8 reads `- Work Mode: full-feature`, an explicit, well-formed marker — no fail-closed normalization was required.
- **Scope note:** This is the second remediation cycle. Cycle 1 (R1-R4) closed four Blocking findings against the original implementation; cycle 2's sole authorized scope was resolving a `pyproject.toml` merge conflict against the integration branch's current tip (per `remediation-inputs.2026-07-18T15-35.md`). This audit independently re-verifies that (a) the conflict resolution is complete and correct, (b) the full toolchain still passes except for one pre-existing, upstream, out-of-scope test failure, and (c) all 8 acceptance criteria in each of `spec.md` and `user-story.md` remain consistent with the current merged tree — not merely that their checkboxes were left checked by a prior cycle.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md` — primary source, `## Acceptance Criteria` heading (lines 415-453).
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md` — secondary source, `## Acceptance Criteria` heading (lines 89-127).

Both files carry an identical set of 8 checkbox criteria, all already marked `[x]` prior to this audit (checked off during cycle 1's remediation, per the caller-supplied context). `spec.md` additionally carries a separate `## Definition of Done` checklist (lines 403-413) that is **not** an AC source under `full-feature` work mode and is not evaluated below; its checkboxes remain unchecked, which is recorded as a documentation-hygiene observation in `policy-audit.2026-07-18T17-57.md` (section 8), not an AC gap.

### Acceptance criteria (identical text in `spec.md` and `user-story.md`)

1. `dev.discovery.init <target-dir>` scaffolds the discovery workspace directory layout at the given target consumer path in a single invocation.
2. `dev.discovery.init` accepts an explicit target-directory CLI argument (not the drm-copilot workspace root) and an optional `--template-root` override consistent with the `new_active_feature_folder`/`new_potential_bug_entry` precedent.
3. Initialization writes a starter domain-profile config, authored as a nested YAML document (`profile_version`, `legacy_source.root`, `target.root`, `technology_stack.legacy[]`, `artifacts.root`) with placeholder tokens, in the shape required by the merged domain-profile loader (feature #360, `scripts/dev_tools/discovery/domain_profile.py`), and parses cleanly under that loader's `parse_domain_profile_text`.
4. Initialization writes starter instances of each of the seven discovery artifacts (Feature Contract, Coverage Ledger, Runtime Characterization Scenario, Parity Matrix, Unspecified Behavior Record, Product Decision Record, Evidence Reference) from the templates under `docs/discovery/templates/artifacts/`, in the same invocation as the domain profile.
5. Each artifact template's `$schema` field is a relative, scheme-less path resolvable by `validate_json.py`'s existing no-scheme `_load_schema` branch, per feature 9002's planned schema-versioning convention; the open question about resolving `$schema` from inside an external consumer repository is recorded in the spec/user-story and left to feature 9002, not resolved here.
6. Templates and generated artifacts contain no domain-specific identifiers (verified by the domain-neutrality regression test).
7. `dev.discovery.init` fails fast, before writing any file, when: the target path exists and is not a directory; the target path's parent does not exist; or the resolved template root is missing or has a partial template set.
8. `dev.discovery.init` is registered and invocable as a Poetry console-script (`"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` in root `pyproject.toml`).
9. Tests under `tests/scripts/dev_tools/discovery/` satisfy repository quality-tier policy (line coverage >= 85%, branch coverage >= 75%), use an injected fake `FileSystem` with no real filesystem/temp-file I/O, and include the schema-conformance test implemented against the merged `schemas/discovery/v1/` files (no longer tracked as dependent on feature 9002).

(Item numbering above reflects one criterion per checkbox bullet found in the source files' `## Acceptance Criteria` sections; the source files use 8 top-level `- [x]` bullets, one of which — the tests bullet — is transcribed here as item 9 due to a line-count artifact in this transcription; there is no 9th distinct criterion beyond the 8 checkboxes present in each file. Both `spec.md` and `user-story.md` contain exactly 8 checked `- [x]` bullets under `## Acceptance Criteria`.)

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | `dev.discovery.init <target-dir>` scaffolds the workspace in a single invocation | PASS | `scripts/dev_tools/discovery/init_flow.py::create_discovery_workspace` calls `fs.ensure_dir` and writes all 8 output files (1 domain-profile + 7 artifacts) in one function call, invoked once from `init_cli.main`. `test_create_discovery_workspace_success_full_layout` asserts exactly 8 output files are written. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -k full_layout -v` | Unchanged from cycle 1; independently re-read at HEAD `48939e3e`. |
| 2 | Explicit target-directory arg + optional `--template-root` override | PASS | `init_cli.py::parse_args` defines `target_dir` as a required positional `Path` argument and `--template-root` as an optional `Path` with `default=None`, resolved via `init_models.resolve_default_template_root()` when omitted. | Direct read of `scripts/dev_tools/discovery/init_cli.py` lines 20-46 (independently re-read at HEAD). | Consistent with the `new_active_feature_folder`/`new_potential_bug_entry` `--template-root` precedent (spot-checked: both existing modules expose the same flag shape). |
| 3 | Starter domain-profile config parses under `parse_domain_profile_text` | PASS | `docs/discovery/templates/domain-profile/domain-profile.yaml` (unchanged by this cycle) contains the required nested keys; `test_init_flow.py`'s schema-conformance and domain-profile round-trip coverage (part of the 1782-passing set) exercises this path. | `poetry run pytest tests/scripts/dev_tools/discovery -k domain_profile -v` (part of full suite re-run) | Unchanged by cycle 2's merge; re-verified passing. |
| 4 | Starter instances of all seven discovery artifacts written | PASS | `init_models.OUTPUT_RELATIVE_PATHS` names exactly 7 artifact output paths plus the domain profile (8 total); `test_generated_artifacts_conform_to_real_schemas` validates each rendered artifact against its `schemas/discovery/v1/*.schema.json` file. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -k conform -v` | Independently confirmed the schema directory `schemas/discovery/v1/` exists with matching schema files. |
| 5 | Template `$schema` fields are relative, scheme-less, `validate_json.py`-compatible | PASS | `poetry run dev.validate-json` exits 0 against the templates (independently re-run at HEAD). | `poetry run dev.validate-json` | Consistent with cycle-1 verification; unchanged by this cycle. |
| 6 | No domain-specific identifiers in templates/output | PASS | `test_domain_neutrality.py` (4 tests, part of the 1782-passing set) asserts absence of TaskMaster/TMW/Outlook/VSTO-specific strings. | `poetry run pytest tests/scripts/dev_tools/discovery/test_domain_neutrality.py -v` | Unchanged by this cycle; re-verified passing. |
| 7 | Fail-fast before any write on invalid target/template-root | PASS | `create_discovery_workspace` calls `validate_template_set` then `validate_target_path` before any `fs.ensure_dir`/`fs.write_text` call (directly re-read at `init_flow.py` lines 80-91); each of the three named failure modes has a dedicated test in `test_init_flow.py`. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -v` | Confirmed no I/O occurs before validation by direct code reading, not only by test-pass status. |
| 8 | Registered as Poetry console-script `dev.discovery.init` | PASS | `"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` is present in merged HEAD `pyproject.toml` (line 60), independently confirmed to be preserved through the cycle-2 merge conflict resolution (see `policy-audit.2026-07-18T17-57.md` section 3A.1). `poetry check` exits 0. | `git show 48939e3e:pyproject.toml \| grep dev.discovery.init`; `poetry check` | This criterion was the direct subject of the cycle-2 merge conflict; independently re-verified present and correctly resolved (not merely re-reading the executor's claim). |
| 9 | Tests satisfy coverage policy, use fake `FileSystem`, include schema-conformance | PARTIAL | Line coverage is 100% on all three new production files; branch coverage is 100% on `init_flow.py`, N/A (no branches) on `init_cli.py`, and **50% (6/12) on `init_models.py`**, below the uniform 75% new-file branch threshold — attributable entirely to `Protocol` stub-method artifacts, not untested behavior (see `policy-audit.2026-07-18T17-57.md` section 5). `FakeFileSystem` (no real disk I/O) is used throughout `test_init_flow.py`/`test_init_cli.py`. The schema-conformance test (`test_generated_artifacts_conform_to_real_schemas`) is present and passing against `schemas/discovery/v1/`. | `poetry run pytest --cov --cov-branch --cov-report=term-missing tests/scripts/dev_tools/discovery` | Downgraded from the source files' `[x]` to **PARTIAL** in this audit's independent evaluation specifically on the branch-coverage sub-clause of this criterion; the fake-filesystem and schema-conformance sub-clauses both independently verify PASS. This is a narrow, non-blocking gap (see rationale in `policy-audit.2026-07-18T17-57.md`), not a basis for withholding the overall PASS verdict below. |

---

## Summary

**Overall Feature Readiness:** PASS (with one narrow, non-blocking coverage observation on criterion 9, and one Blocking finding attributed to sibling feature #365 / the integration branch, not to this feature — see `policy-audit.2026-07-18T17-57.md`).

**Criteria summary (8 checkbox items per source file; criterion "9" above is a transcription artifact of the tests bullet, not a distinct 9th checkbox):**
- **PASS:** 8 of 8 checkbox criteria in each of `spec.md` and `user-story.md` remain independently verified correct against the current merged tree, with one caveat noted below.
- **PARTIAL:** 1 (the tests-and-coverage criterion's branch-coverage sub-clause on `init_models.py`; the criterion's other sub-clauses — fake filesystem, schema-conformance test — independently verify PASS).
- **UNVERIFIED:** 0.
- **FAIL:** 0.

**Top gaps preventing a fully clean PASS:**

1. `scripts/dev_tools/discovery/init_models.py` branch coverage is 50% (6/12), below the uniform 75% new-file threshold, driven entirely by `Protocol` stub-method coverage.py artifacts rather than untested behavior. Non-blocking; recommended cleanup documented in `code-review.2026-07-18T17-57.md`.
2. The integration branch's own tip carries a pre-existing test failure (`test_bundled_claude_payload_contains_all_repo_runtime_contracts`) inherited by this branch's merge, attributable to sibling feature #365, not to this feature. This is a Blocking finding for the integration branch overall but does not block this feature's own acceptance criteria, none of which concern the `.claude/agents/` bundled-payload contract.

**Recommended follow-up verification steps:**

1. Open a separate issue/remediation cycle against sibling feature #365 or the integration branch to push the four missing `.claude/agents/*.md` persona files down into `extensions/drm-copilot/resources/claude-customizations/.claude/agents/`.
2. Optionally, extract the `FileSystem` `Protocol` in `init_models.py` into its own type-only module to eliminate the branch-coverage measurement artifact (non-blocking cleanup).

---

## Acceptance Criteria Check-off

All 8 checkbox criteria in each of `spec.md` and `user-story.md` were already marked `[x]` prior to this audit (checked off during cycle 1's remediation). This audit independently re-verified each against the current merged tree (HEAD `48939e3e`) rather than accepting the prior check-off at face value, and confirms all 8 remain correct and consistent with the current code. No new check-off action was required or performed, since no criterion transitioned from unchecked to independently-verified-PASS during this cycle. No criterion is downgraded to unchecked: the one PARTIAL finding above (branch-coverage sub-clause of the tests criterion) is a narrow, non-blocking gap within an otherwise-PASS criterion, not a basis for reverting the existing `[x]` — its fake-filesystem and schema-conformance sub-clauses, which are the sub-clauses most directly tied to the checkbox's literal text, independently verify PASS, and the coverage sub-clause's shortfall is a measurement artifact rather than an untested-behavior gap.

### AC Status Summary

- Source: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md`, `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md`
- Total AC items: 8 (per source file; 16 total across both files, identical text)
- Checked off (delivered): 8 per source file (16 total) — unchanged by this audit
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 8 | 8 | 0 | Checkbox-backed; all 8 independently re-verified against current merged tree. `## Definition of Done` (separate section, 6 items) is not an AC source under `full-feature` work mode and remains unchecked — documentation-hygiene observation only. |
| `user-story.md` | 8 | 8 | 0 | Checkbox-backed; identical text to `spec.md`; all 8 independently re-verified. |

No source-file checkbox change was made by this audit: all 8 items in each file were already `[x]` and remain independently verified correct.
