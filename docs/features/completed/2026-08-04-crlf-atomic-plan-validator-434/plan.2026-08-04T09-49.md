# crlf-atomic-plan-validator (Plan)

- **Issue:** #434
- **Work Mode:** full-bug
- **Requirements:** `issue.md`, `spec.md`, and `artifacts/research/2026-08-04T09-53-crlf-atomic-plan-validator-434-research.md`
- **Scope:** Only `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts` and `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts` may change. No schema, route, dependency, configuration, release-workflow, or unrelated validator change is permitted.
- **Evidence root:** `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/`

**Evidence rule:** Every evidence artifact named below must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Any required artifact that is absent, has a non-zero exit code, or lacks required numeric coverage values makes the result remediation-required; it cannot be reported as PASS. Do not use `SKIPPED` for a planned command.

**TypeScript restart rule:** Run the seven stages in the stated order. If a command fails or the formatting command changes files, restart at the formatting stage and retain the final clean-pass artifacts.

### Phase 0 — Policy and Baseline Capture

- [x] [P0-T1] Read `AGENTS.md`, `.agents/skills/general-code-change/SKILL.md`, `.agents/skills/general-unit-test/SKILL.md`, `.agents/skills/typescript/SKILL.md`, and `.agents/skills/architecture-boundaries/SKILL.md` in that order; write the ordered file list and timestamp to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T2] From `extensions/drm-copilot/`, run `npm run format`; record the baseline result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/ts-format.2026-08-04T09-49.md`.
- [x] [P0-T3] From `extensions/drm-copilot/`, run `npm run lint`; record the baseline result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/ts-lint.2026-08-04T09-49.md`.
- [x] [P0-T4] From `extensions/drm-copilot/`, run `npm run typecheck`; record the baseline result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/ts-typecheck.2026-08-04T09-49.md`.
- [x] [P0-T5] From `extensions/drm-copilot/`, run `npm run test:unit`; record the baseline suite count in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/ts-unit.2026-08-04T09-49.md`.
- [x] [P0-T6] From `extensions/drm-copilot/`, run `npm run test:coverage`; record numeric line and branch coverage in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/ts-coverage.2026-08-04T09-49.md`.
- [x] [P0-T7] From the repository root, run `git rev-parse HEAD` and `git status --porcelain`; record the exact baseline commit on a `BaselineCommit: <sha>` line and the clean-status result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/git-baseline.2026-08-04T09-49.md`.

### Phase 1 — Fail-Before Regression

- [x] [P1-T1] Add the table-driven `accepts a valid plan with LF, CRLF, or CR line endings` Jest case to `extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts`, deriving each variant from the existing `VALID_PLAN` fixture and retaining the completed `- [x] [P1-T1] Third task` fixture line.
- [x] [P1-T2] [expect-fail] From `extensions/drm-copilot/`, run `npm run test:unit -- --runTestsByPath test/lib/validate/orchestration-artifacts.test.ts`; confirm the CRLF and CR table entries fail while the LF entry passes, and record the command output in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/regression-testing/fail-before-crlf-cr.2026-08-04T09-49.md`.

### Phase 2 — Delimiter-Only Production Fix

- [x] [P2-T1] In `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts`, replace only `text.split("\n")` in `validatePlanText` with `text.split(/\r\n|\n|\r/)`, preserving the regular expressions, diagnostics, source-order traversal, and public dispatcher contract.
- [x] [P2-T2] From `extensions/drm-copilot/`, rerun `npm run test:unit -- --runTestsByPath test/lib/validate/orchestration-artifacts.test.ts`; record LF, CRLF, and CR success plus retained malformed-plan assertions in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/regression-testing/post-fix-crlf-cr.2026-08-04T09-49.md`.

### Phase 3 — Seven-Stage TypeScript QA

- [x] [P3-T1] Run stage 1 from `extensions/drm-copilot/` with `npm run format`; write the final clean-pass result to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage1-format.2026-08-04T09-49.md`.
- [x] [P3-T2] Run stage 2 from `extensions/drm-copilot/` with `npm run lint`; write the final clean-pass result to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage2-lint.2026-08-04T09-49.md`.
- [x] [P3-T3] Run stage 3 from `extensions/drm-copilot/` with `npm run typecheck`; write the final clean-pass result to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage3-typecheck.2026-08-04T09-49.md`.
- [x] [P3-T4] From the repository root, deterministically inspect `package.json`, `extensions/drm-copilot/package.json`, lockfiles, CI workflows, and repository root for a dependency-cruiser script, configuration, or installed executable. When none exists, document the absence and perform a manual import/boundary inspection of the two approved changed files, recording the search commands, results, inspected imports, and conclusion in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage4-architecture.2026-08-04T09-49.md`; this repository-approved documented-absence procedure is the architecture gate for this package.
- [x] [P3-T5] Run stage 5 from `extensions/drm-copilot/` with `npm run test:unit`; write the final clean-pass suite count to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage5-unit.2026-08-04T09-49.md`.
- [x] [P3-T6] Run stage 6 from the repository root with `$baseline = (Get-Content 'docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/baseline/git-baseline.2026-08-04T09-49.md' | Select-String '^BaselineCommit: ' | ForEach-Object { $_.Line.Substring(16) }); if ([string]::IsNullOrWhiteSpace($baseline)) { exit 1 }; $featurePath = 'docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/'; $allowed = @('extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts', 'extensions/drm-copilot/test/lib/validate/orchestration-artifacts.test.ts'); $changed = @((git diff --name-only $baseline)) + @((git ls-files --others --exclude-standard)); $unexpected = @($changed | Where-Object { $_ -notin $allowed -and -not $_.StartsWith($featurePath) } | Sort-Object -Unique); if ($unexpected.Count -gt 0) { $unexpected; exit 1 }`; record the baseline commit, command result, and confirmation that the working tree and untracked paths contain only the two approved code/test paths plus feature-folder documents/evidence in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage6-contract.2026-08-04T09-49.md`.
- [x] [P3-T7] Run stage 7 from `extensions/drm-copilot/` with `npm run test:unit -- --runTestsByPath test/extension.integration.test.ts test/extension.collect-commit-context.integration.test.ts test/lib/pr-context/collector-integration.test.ts`; write the final clean-pass suite count to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-stage7-integration.2026-08-04T09-49.md`.
- [x] [P3-T8] From `extensions/drm-copilot/`, run `npm run test:coverage`; write numeric repository line and branch coverage and numeric `src/lib/validate/orchestration-artifacts.ts` line and branch coverage to `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-coverage-final.2026-08-04T09-49.md`.
- [x] [P3-T9] Compare `evidence/baseline/ts-coverage.2026-08-04T09-49.md` with `evidence/qa-gates/ts-coverage-final.2026-08-04T09-49.md`; record baseline, post-change, changed-module line and branch coverage, and confirmation of the 85% line/75% branch/no-regression requirements in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/ts-coverage-comparison.2026-08-04T09-49.md`.

### Phase 4 — Generated MCP Bundle and Package Readiness

- [x] [P4-T1] From `extensions/drm-copilot/`, run `npm run bundle:mcp-server`; record the generated bundle path and exit code in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/mcp-extension-bundle.2026-08-04T09-49.md`.
- [x] [P4-T2] From the repository root, run `npm --prefix packages/mcp-server run prepack`; record the result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/mcp-package-prepack.2026-08-04T09-49.md`.
- [x] [P4-T3] From the repository root, run `npm --prefix packages/mcp-server run build`; record the generated executable path and exit code in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/qa-gates/mcp-package-build.2026-08-04T09-49.md`.
- [x] [P4-T4] Perform the authorized manual QA smoke, not a Jest test: prefer an in-memory input only if the packaged MCP protocol accepts one; otherwise create one uniquely named directory with `New-Item` beneath the exact verified absolute `Join-Path ([System.IO.Path]::GetTempPath()) 'drm-copilot-434-mcp-smoke-<GUID>'` path, create LF/CRLF/CR byte variants of the same canonical completed plan only inside that verified directory, invoke `packages/mcp-server/out/mcp-server.js` through its normal stdio MCP protocol for each `artifact_type: "plan"` path, record all three successful responses in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/regression-testing/mcp-package-line-ending-smoke.2026-08-04T09-49.md`, remove only that verified directory with `Remove-Item -LiteralPath`, and record the cleanup receipt plus P3-T6 proof that no working-tree or untracked path exists outside the two-file allowlist and feature folder; do not create tracked fixtures or use temporary assets in Jest.

### Phase 5 — Acceptance, Review, and Release Follow-Up

- [x] [P5-T1] Check off only full-bug acceptance criteria in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/spec.md` that are supported by completed evidence; mirror the Issue #434 update text and posting result in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/issue-updates/issue-434.2026-08-04T09-49.md`.
- [ ] [P5-T2] Produce the feature-review and pull-request handoff using `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/issue.md`, `spec.md`, this plan, and the evidence tree; require all changed files, the delimiter-only boundary, coverage comparison, generated-bundle smoke, and unresolved review findings to be recorded before merge.
- [ ] [P5-T3] After the merge commit is on the default branch, derive the MCP version with `node -p "require('./packages/mcp-server/package.json').version"`, create and push the supported release tag with `$version = node -p "require('./packages/mcp-server/package.json').version"; git tag "mcp-server-v$version"; git push origin "mcp-server-v$version"`, then locate the tag-push-triggered run with `gh run list --workflow publish-mcp-npm.yml --branch "mcp-server-v$version" --limit 1 --json databaseId,url,status,conclusion`, wait with `gh run watch <run-id> --exit-status`, and record the version, tag, run ID, workflow URL, and successful terminal status in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/other/npm-patch-release-workflow.2026-08-04T09-49.md`.
- [ ] [P5-T4] Derive the published version with `$version = node -p "require('./packages/mcp-server/package.json').version"` and retrieve immutable npm metadata with `$metadata = npm view "@danmoisan/drm-copilot-mcp@$version" dist.tarball dist.integrity --json | ConvertFrom-Json`; after verifying the exact absolute path, create a uniquely named directory only beneath `Join-Path ([System.IO.Path]::GetTempPath()) 'drm-copilot-434-npm-inspect-<GUID>'`, download `$metadata.'dist.tarball'` with `Invoke-WebRequest`, extract it with `tar -xf`, invoke the extracted `package/out/mcp-server.js` through normal stdio against LF, CRLF, and CR byte variants created only in that verified directory, remove only the verified directory with `Remove-Item -LiteralPath`, use P3-T6 to prove no working-tree or untracked path exists outside the two-file allowlist and feature folder, and record `$version`, `$metadata.'dist.tarball'`, `$metadata.'dist.integrity'`, three successful responses, cleanup receipt, and allowlist proof in `docs/features/active/2026-08-04-crlf-atomic-plan-validator-434/evidence/other/npm-patch-release-follow-up.2026-08-04T09-49.md`.
