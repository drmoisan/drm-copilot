Timestamp: 2026-04-13T22:00:47-04:00
Fixed Contract: `extensions/drm-copilot/src/repo-automation-service.ts` now marshals multi-folder `scan_folders` for `run_poshqc_format`, `run_poshqc_analyze`, and `run_poshqc_test` as one `-ScanFoldersJson` payload, and the bundled wrappers decode that JSON into string arrays before calling `Invoke-PoshQCFormat`, `Invoke-PoshQCAnalyze`, and `Invoke-PoshQCTest`.
Closed This Remediation Loop:
- The restarted final Phase 2 pass completed cleanly for `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`, `npm run lint`, `npm run typecheck`, `npx jest extensions/drm-copilot/test/repo-automation-service.test.ts --runInBand`, and `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "Invoke-Pester -Path 'tests/scripts/powershell/PoshQC/PoshQC.ScanFolders.Tests.ps1','tests/scripts/powershell/PoshQC/PoshQC.Tests.ps1' -Output Detailed"`.
- Live wrapper verification succeeded for format, analyze, and test against the installed `drm-copilot` extension bundle using the repaired multi-folder transport.
- The wrapper regression tests now cover the service-to-wrapper JSON transport and remain analyzer-clean.
Preserved Closed Findings:
- The `.claude/settings.json` schema-validation finding remains closed.
- The PowerShell coverage-output-path finding remains closed.
- The stale MCP token finding remains closed.
Remaining Environment-Only `UNVERIFIED` Criteria:
- Live `/orchestrate`, `/commit-message`, `/pr-author`, and `/research-issue` invocation.
- Live subagent allowlist enforcement.
- Live checkpoint resume verification.
- Live `SubagentStop` verification.
Session Note: Restarting the stale `mcp-server.js` host cleared the original duplicate-`ScanFolders` process state but left this session's MCP transport closed. Equivalent direct invocation of the same installed extension bundle was used to complete this session's live wrapper verification.
Handoff: The wrapper defect is closed, no acceptance checkboxes were changed in `spec.md` or `user-story.md`, and the branch is ready for post-remediation feature review.
