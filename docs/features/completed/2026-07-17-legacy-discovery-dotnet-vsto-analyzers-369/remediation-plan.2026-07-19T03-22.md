# Remediation Plan — legacy-discovery-dotnet-vsto-analyzers (#369), Remediation Cycle 3

- Timestamp: 2026-07-19T03-22
- Issue: #369
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/issue.md`)
- Branch: `feature/legacy-discovery-dotnet-vsto-analyzers-369`
- PR: #384
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-inputs.2026-07-18T23-16.md` (single Blocking finding R-1 — the mandatory PowerShell coverage artifact `artifacts/pester/powershell-coverage.xml` is absent for two pushed-down `.ps1` bundle mirror files, so PowerShell has a coverage FAIL). No scope beyond that document is authorized.
- Remediation approach: acceptance option 1 only — produce the PowerShell coverage artifact and record a coverage summary. Acceptance option 2 (recording a coverage-exclusion classification in `.claude/rules/general-unit-test.md`) is PROHIBITED: editing any policy document under `.claude/rules/` or `.github/instructions/` is forbidden by `CLAUDE.md` and the policy-compliance-order hard constraints. This plan modifies no policy rule file.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`). No `artifacts/`-rooted evidence path is permitted for feature evidence. Prior full-feature-cycle evidence, cycle-1 (`r1c1-*`), and cycle-2 (`r2c2-*`) evidence MUST NOT be overwritten. All new artifacts for this cycle use the `r3c3-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root unless a task states otherwise. Shell is PowerShell unless a task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- The two pushed-down bundle mirror files that make PowerShell a language with changed files on this branch are:
  - `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1`
  - `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1`
  These bundle mirrors are byte-identical copies of the repo-root originals `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1`. This plan does NOT modify either the bundle mirrors or the repo-root originals; no PowerShell authored-logic change is in scope.
- The hook logic is exercised by the existing Pester suites, which dot-source the repo-root originals:
  - `tests/scripts/claude-hooks/enforce-discovery-artifact-gate.Tests.ps1`
  - `tests/scripts/claude-hooks/validate-discovery-artifact-gate.Tests.ps1`
- The repository Pester runsettings `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` already scopes code coverage to the two discovery-artifact-gate hooks (`.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1`) and emits `CodeCoverage.OutputPath = artifacts/pester/powershell-coverage.xml` in CoverageGutters format. Running `mcp__drm-copilot__run_poshqc_test` against these runsettings produces the required coverage artifact. This plan does NOT modify the runsettings.
- `artifacts/` is git-ignored (`.gitignore` entry `/artifacts`). `artifacts/pester/powershell-coverage.xml` is therefore CI/local-produced coverage output and is not a committed file. It is an allowed non-evidence coverage-output path per the orchestrate skill's Evidence Location Authority. The committed, audit-durable proof for this remediation is the human-readable coverage summary under `<FEATURE>/evidence/qa-gates/` (r3c3- prefix). This plan does not attempt to `git add` any path under `artifacts/`.
- The residual risk is an evidence/classification gap, not a code defect: the mirrored logic is tested and its byte-identity to the tested originals is contract-verified by `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` (passing as of cycle 2). This cycle only produces the missing coverage evidence and re-verifies.

### Phase 0 — Policy Read and Baseline Capture

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated and the list includes `.claude/rules/powershell.md`.
- [x] [P0-T2] Capture the fail-before / absent-artifact baseline: run `Test-Path artifacts/pester/powershell-coverage.xml` and `git status --porcelain --ignored -- artifacts/pester/` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-coverage-artifact-absence-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording whether the coverage artifact is currently present and confirming `/artifacts` is git-ignored. Acceptance: the artifact exists with all four fields and the `Output Summary:` records the current presence/absence and git-ignored status of `artifacts/pester/powershell-coverage.xml`.
- [x] [P0-T3] Capture the baseline PowerShell format state: run `mcp__drm-copilot__run_poshqc_format` (repo settings) in check/no-write mode and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-posh-format-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run; no PowerShell source file is modified.
- [x] [P0-T4] Capture the baseline PowerShell analyzer state: run `mcp__drm-copilot__run_poshqc_analyze` (repo settings) and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-posh-analyze-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the `Output Summary:` records the PSScriptAnalyzer error/warning counts for the two discovery-artifact-gate hooks.
- [x] [P0-T5] Capture the baseline PowerShell/Pester coverage state: run `mcp__drm-copilot__run_poshqc_test` using `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-pester-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the Pester pass/fail counts and the current numeric line-coverage and branch-coverage percentages for `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1`. Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid) and confirms both discovery-artifact-gate hooks are present in the coverage set.
- [x] [P0-T6] Capture the baseline Python coverage state (no-regression anchor): run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric baseline line-coverage and branch-coverage percentages and the pass/fail/skip counts. Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid).

### Phase 1 — Produce the PowerShell Coverage Artifact

- [x] [P1-T1] Produce the mandatory coverage artifact: run `mcp__drm-copilot__run_poshqc_test` using `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which executes the discovery-artifact-gate Pester suites (`tests/scripts/claude-hooks/enforce-discovery-artifact-gate.Tests.ps1` and `tests/scripts/claude-hooks/validate-discovery-artifact-gate.Tests.ps1`) and emits `artifacts/pester/powershell-coverage.xml`. If any Pester test fails, correct the invocation/environment and re-run this task until the suite is green; `EXIT_CODE: SKIPPED` is not a valid outcome. Acceptance: exit code 0 and all executed Pester tests pass.
- [x] [P1-T2] Confirm the coverage artifact was produced: run `Test-Path artifacts/pester/powershell-coverage.xml` and `(Get-Item artifacts/pester/powershell-coverage.xml).Length` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-coverage-artifact-present.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording that the file exists and is non-empty. Acceptance: `Test-Path` returns `True` and the recorded file length is greater than zero.
- [x] [P1-T3] Record the human-readable coverage summary: extract the line-coverage and branch-coverage percentages for `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1` from `artifacts/pester/powershell-coverage.xml` (and the aggregate hook-logic coverage) and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-powershell-coverage-summary.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric per-hook and aggregate line/branch coverage percentages. Acceptance: the artifact exists with numeric line and branch coverage values recorded (placeholders such as `UNVERIFIED` are invalid).

### Phase 2 — Verification (Coverage Thresholds, Python Toolchain Green, No Policy Edit)

- [x] [P2-T1] Verify the PowerShell coverage thresholds: using the numeric values recorded in `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-powershell-coverage-summary.<TS>.md`, write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-coverage-threshold-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` confirming line coverage >= 85% and branch coverage >= 75% for the discovery-artifact-gate hook logic. Acceptance: the artifact records line coverage >= 85% and branch coverage >= 75%; if either threshold is not met, the remediation is not complete and P1-T1 through P1-T3 must be revisited.
- [x] [P2-T2] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P2-T2. Acceptance: exit 0 with no files changed, recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T3] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P2-T2. Acceptance: exit 0 recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T4] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P2-T2. Acceptance: exit 0 with 0 errors recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T5] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-change line-coverage and branch-coverage percentages and the pass/fail/skip counts, and explicit confirmation that the full suite (including `test_bundled_claude_payload_contains_all_repo_runtime_contracts`) is green. If this stage fails, fix, then restart the loop from P2-T2. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid). `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T6] Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-python-coverage-delta.<TS>.md` comparing the Phase 0 baseline Python coverage (from `evidence/remediation-baseline/r3c3-phase0-pytest-baseline.<TS>.md`) against the P2-T5 post-change coverage, reporting: baseline line/branch %, post-change line/branch %, and confirmation that line coverage >= 85% and branch coverage >= 75% hold with no regression. Acceptance: the artifact contains all numeric values and both threshold checks pass. (This cycle adds no Python production code, so no coverage regression is expected.)
- [x] [P2-T7] Verify no policy-rule file was modified: run `git diff --name-only origin/epic/legacy-discovery-and-parity-integration...HEAD -- .claude/rules/ .github/instructions/` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-no-policy-edit-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the (expected empty) output. Acceptance: the command produces no output (no file under `.claude/rules/` or `.github/instructions/` appears in the branch diff).

### Phase 3 — Commit and Push

- [x] [P3-T1] Stage and commit this cycle's plan-checklist updates and all Phase 0–2 evidence artifacts (`docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-plan.2026-07-19T03-22.md` and every new file under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r3c3-*` and `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-*`) in a single commit with a professional, factual message referencing issue #369 remediation cycle 3 and the PowerShell coverage evidence for the discovery-artifact-gate bundle mirrors. The git-ignored `artifacts/pester/powershell-coverage.xml` is not staged. Acceptance: `git log -1 --name-only` lists only files under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/` (plan and `r3c3-*` evidence) and no policy-rule, source, test, or `artifacts/` path.
- [x] [P3-T2] Confirm the working tree is clean of untracked feature evidence: run `git status --porcelain -- docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/` and confirm no uncommitted `r3c3-*` evidence remains. Acceptance: the command produces no output for the feature evidence paths.
- [x] [P3-T3] Push the updated branch: `git push origin feature/legacy-discovery-dotnet-vsto-analyzers-369`. Acceptance: the command exits 0 and the remote branch HEAD matches the local branch HEAD (verify with `git rev-parse HEAD` and `git rev-parse origin/feature/legacy-discovery-dotnet-vsto-analyzers-369` after the push).
- [x] [P3-T4] Confirm PR #384 remains mergeable after the push: run `gh pr view 384 --json mergeable,mergeStateStatus` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r3c3-pr-mergeable-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the recorded `mergeable` value is `MERGEABLE` (not `CONFLICTING` or `UNKNOWN`).

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; `artifacts/pester/powershell-coverage.xml` is produced via `mcp__drm-copilot__run_poshqc_test` and demonstrates line coverage >= 85% and branch coverage >= 75% for the discovery-artifact-gate hook logic; a human-readable coverage summary is recorded under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/` with the `r3c3-` prefix; the full Python toolchain (Black, Ruff, Pyright, Pytest with coverage) passes with line coverage >= 85% and branch coverage >= 75% and no regression; no file under `.claude/rules/` or `.github/instructions/` is modified; fresh evidence exists under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/` with the `r3c3-` prefix without overwriting prior-cycle evidence; the branch is pushed; and `gh pr view 384 --json mergeable` reports `MERGEABLE`.
