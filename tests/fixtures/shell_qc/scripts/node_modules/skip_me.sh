#!/usr/bin/env bash
# This fixture lives under an excluded directory (node_modules) and MUST NOT be
# discovered by discover_shell_scripts. Its presence proves the prune works.
echo "should be skipped"
