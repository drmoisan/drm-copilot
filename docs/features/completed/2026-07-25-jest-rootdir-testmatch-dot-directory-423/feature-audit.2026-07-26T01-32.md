# Feature Audit: Jest rootDir/testMatch dot-directory fix (Issue #423)

---

**Audit Date:** 2026-07-26
**Feature Folder:** `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/`
**Base Branch:** `origin/main`
**Head Branch:** `bug/jest-no-tests-found-dot-directory-worktree` (HEAD `914e9fea`)
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

**Template source note:** MCP asset resolution is unavailable in this session; the on-disk bundled asset `extensions/drm-copilot/resources/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md` was used as the template source.

---

## Scope and Baseline

- **Base branch:** `origin/main` (commit `fb483b8468204e4385b5583c3b3ec4c0a987eede`)
- **Head branch/commit:** `bug/jest-no-tests-found-dot-directory-worktree` (commit `914e9fea6842538476ec26220cbaab76a650539c`)
- **Merge base:** `fb483b8468204e4385b5583c3b3ec4c0a987eede` (reviewer-verified: `git merge-base HEAD origin/main`)
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer on 2026-07-26 against `origin/main`; artifacts were absent at review start)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/evidence/{baseline,regression-testing,qa-gates,other}/` (30 artifacts)
  - Additional evidence: reviewer live re-execution of the full check-only toolchain, all six guard invocations, and independent lcov parsing (see `policy-audit.2026-07-26T01-32.md`)
- **Feature folder used:** `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/`
- **Requirements source:** `spec.md` (only source)
- **Work mode resolution note:** Explicit persisted marker `- Work Mode: full-bug` in `issue.md` (line 12). Per the work-mode contract, `spec.md` is the sole acceptance-criteria source and `user-story.md` is intentionally absent; its absence is not a finding.
- **Scope note:** Scope is the full branch diff vs merge-base (42 files: 6 code/test files + 36 feature-folder docs/evidence). The caller-supplied parallel-orchestration file-ownership constraint (no modification of root `package.json`, `tsconfig*.json`, `.vscode-test.*`, `.claude/rules/**`, `.agents/skills/**`, `extensions/drm-copilot/resources/claude-customizations/**`) coincides with spec AC17 and was verified against the diff file list — zero matches.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/spec.md` — only source (`## Acceptance Criteria`, 17 checkbox items, all pre-checked by the executor; this audit independently re-verifies each)

### Acceptance criteria

1. `jest.config.cjs` (root) exports `testMatch: ["**/tests/unit/**/*.test.ts", "**/extensions/drm-copilot/test/**/*.test.ts"]` and `extensions/drm-copilot/jest.config.cjs` exports `testMatch: ["**/test/**/*.test.ts"]`; no `testMatch` entry in either file contains `<rootDir>` or a backslash.
2. Neither config adds or modifies a `roots` key, and `testPathIgnorePatterns`, `collectCoverageFrom`, `coverageThreshold`, `coverageDirectory`, and the ts-jest `tsconfig` references are byte-identical to base `fb483b84`.
3. Fail-before evidence is captured: outputs of `node run-jest.cjs` (root) and `npm --prefix extensions/drm-copilot run test` against the unfixed configs in this dot-prefixed worktree, each showing `No tests found, exiting with code 1` with 0 testMatch matches, stored under the feature evidence directory.
4. Pass-after evidence is captured: with the fix applied, the root run discovers a non-zero suite count matching the on-disk inventory (expected 171) and the extension run a non-zero count (expected 169); both exit 0 with all tests passing.
5. `node run-jest.cjs --passWithNoTests` exits 1 at the repository root without spawning Jest, with a stderr message naming the flag and citing issue #423; captured in evidence.
6. Same as #5 run from `extensions/drm-copilot/`.
7. Both entry points also reject `--onlyChanged` and `--lastCommit` with exit 1 and a message citing issue #423; verified once per flag per entry point and captured in evidence.
8. The guard in both `run-jest.cjs` files is inline (no new helper module), and the existing `--testPathPattern` → `--testPathPatterns` rewrite and exit-code propagation are unchanged.
9. `tests/unit/jest-config-resolution.test.ts` exists, runs under the root Jest project, and passes with assertion groups 1–6.
10. `extensions/drm-copilot/test/jest-config-resolution.test.ts` exists, runs under the extension Jest project, and passes with assertion groups 1–6.
11. Each configured `testMatch` pattern is individually asserted (via `globsToMatcher([pattern])` or equivalent) to match both a representative synthetic dot-prefixed Windows path and a representative POSIX path.
12. The defect-witness assertion is present in both test files: a hard-coded broken normalized pattern containing the retained `\.` byte pair asserted NOT to match the corresponding synthetic path.
13. Both regression test files use Jest with `@jest/globals` imports (not Vitest), perform no filesystem reads/writes other than `require` of the config module, create no temporary files/directories, and do not materialise a dot-prefixed checkout.
14. `config.passWithNoTests` is asserted strictly falsy and `config.testPathIgnorePatterns` is asserted to contain `"/node_modules/"` and `"/out/"` in both regression test files.
15. Full toolchain pass, root package: `npm run format:check`, `npm run lint`, `npm run typecheck`, and `node run-jest.cjs` all succeed in a single pass.
16. Full toolchain pass, extension package: format leaves no diff; lint, typecheck, and test all succeed in a single pass.
17. No forbidden file is modified: the changed-file set contains no entry matching root `package.json`, `tsconfig*.json`, `.vscode-test.*`, `.claude/rules/**`, `.agents/skills/**`, or `extensions/drm-copilot/resources/claude-customizations/**`, and is limited to the six in-scope files plus feature-folder documentation and evidence.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | testMatch values and shape | PASS | Both config files read in full at head: root exports exactly the two relative patterns, extension exactly `["**/test/**/*.test.ts"]`; no `<rootDir>`, no backslash. Enforced ongoing by group-1 assertions in both test files. | Direct file read; `node run-jest.cjs` (group-1 tests pass) | — |
| 2 | No `roots`; other keys byte-identical | PASS | Full diff of both configs vs `fb483b84` shows only the `testMatch` lines changed (root: 2 lines; extension: 1 line). No `roots` key present at head. | `git diff fb483b84..HEAD -- jest.config.cjs extensions/drm-copilot/jest.config.cjs` | Corroborated by `evidence/other/config-diff.2026-07-26T01-03.md`. |
| 3 | Fail-before evidence | PASS | `evidence/regression-testing/fail-before-root-jest.2026-07-26T00-55.md` (exit 1, `No tests found`, 435 files checked, 0 matches, retained `\.claude` visible) read in full; `fail-before-extension-jest.2026-07-26T00-56.md` (exit 1) schema-parsed by the PR-context collector as a fail record. | Artifact inspection; PR-context Verification Evidence section | Captured at pre-fix state (`jest.config.cjs` unmodified relative to base, recorded in the artifact). |
| 4 | Pass-after evidence | PASS | `pass-after-root-jest.2026-07-26T01-14.md` and `pass-after-extension-jest.2026-07-26T01-15.md` (both exit 0); independently reproduced live by reviewer: root 171 suites / 2061 tests, extension 169 suites / 2046 tests, both exit 0. Counts equal the expected inventory exactly. | `node run-jest.cjs`; `npm --prefix extensions/drm-copilot run test` | Reviewer runs executed in this dot-prefixed worktree — the affected environment class. |
| 5 | Root `--passWithNoTests` guard | PASS | Reviewer live run: exit 1, stderr `--passWithNoTests is prohibited in this repository: zero discovered tests must fail (issue #423).`, no Jest output (not spawned). Evidence artifact `guard-root.2026-07-26T01-06.md` concurs. | `node run-jest.cjs --passWithNoTests` (repo root) | — |
| 6 | Extension `--passWithNoTests` guard | PASS | Reviewer live run from `extensions/drm-copilot/`: exit 1, same message, no Jest spawn. Evidence artifact `guard-extension.2026-07-26T01-07.md` concurs. | `node run-jest.cjs --passWithNoTests` (from `extensions/drm-copilot/`) | — |
| 7 | `--onlyChanged` / `--lastCommit` rejection | PASS | Reviewer live runs: all four invocations (2 flags x 2 entry points) exit 1 with flag-specific messages citing issue #423. Evidence artifacts record the same six-invocation matrix. | `node run-jest.cjs --onlyChanged`; `node run-jest.cjs --lastCommit` (both roots) | — |
| 8 | Inline guard; rewrite/propagation unchanged | PASS | Diff of both `run-jest.cjs` files vs base shows only the inserted guard block; the `--testPathPattern` rewrite, spawn wiring, `result.error` handling, and `result.status ?? 1` propagation are byte-identical. No new module file in the diff. | `git diff fb483b84..HEAD -- run-jest.cjs extensions/drm-copilot/run-jest.cjs` | Corroborated by `evidence/other/run-jest-diff.2026-07-26T01-05.md`. |
| 9 | Root regression test exists/runs/passes groups 1–6 | PASS | File read in full: 14 tests in six labelled groups. Included in the passing root run (171 suites, exit 0). | `node run-jest.cjs` | — |
| 10 | Extension regression test exists/runs/passes groups 1–6 | PASS | File read in full: 11 tests in six labelled groups. Included in the passing extension run (169 suites, exit 0). | `npm --prefix extensions/drm-copilot run test` | — |
| 11 | Per-pattern individual assertions, Windows + POSIX | PASS | Root file: `patternAt(UNIT_PATTERN_INDEX)` and `patternAt(EXTENSION_PATTERN_INDEX)` each asserted separately via `globsToMatcher([pattern])` against a Windows dot-prefixed path (group 2) and a POSIX path (group 3). Extension file: same per-pattern style for its single pattern. | Test file inspection (lines 131–157 root; 122–136 ext) | Partial-collapse gap closed as specified. |
| 12 | Defect witness in both files | PASS | `DEFECTIVE_PATTERN` constants with the retained `\.` byte pair (root line 94–95, ext line 88–89) asserted NOT to match in group 4 of each file. | Test file inspection; tests pass in both runs | Pins picomatch escaped-dot semantics against future Jest changes. |
| 13 | Jest + `@jest/globals`; no fs/temp/spawn; synthetic strings only | PASS | Both files import only from `@jest/globals` and `jest-util`; sole filesystem access is `require` of the config under test; all matcher inputs are hard-coded string constants; no temp files, no process spawning, no dot-prefixed checkout materialised. | Full-file inspection of both test files | — |
| 14 | Loudness config guard (group 6) in both files | PASS | Both files assert `config.passWithNoTests` falsy (`toBeFalsy`) and `testPathIgnorePatterns` containing `"/node_modules/"` and `"/out/"`. | Test file inspection (lines 181–190 root; 154–163 ext) | — |
| 15 | Root toolchain single pass | PASS | Reviewer re-ran all four stages consecutively on 2026-07-26: `format:check` exit 0, `lint` exit 0, `typecheck` exit 0, `node run-jest.cjs` exit 0 (171/171 suites) — single pass, no fixes needed. Executor evidence records the same with 0 loop restarts. | `npm run format:check && npm run lint && npm run typecheck && node run-jest.cjs` | `evidence/qa-gates/final-root-*.md`, `final-loop-summary.2026-07-26T01-27.md`. |
| 16 | Extension toolchain single pass | PASS | Reviewer re-ran on 2026-07-26: check-only Prettier over the extension format globs exit 0 (no diff possible), `lint` exit 0, `typecheck` exit 0, `test` exit 0 (169/169 suites) — single pass. Executor evidence records formatter-run-with-no-diff plus the same stages at exit 0. | Extension prettier `--check`; `npm --prefix extensions/drm-copilot run lint` / `typecheck` / `test` | `evidence/qa-gates/final-extension-*.md`. |
| 17 | No forbidden file; changed set limited to scope | PASS | `git diff --name-only fb483b84...HEAD` (42 files) grep-matched against all six forbidden patterns: zero matches. Changed set = exactly the six in-scope code/test files + 36 files under the feature folder. | `git diff --name-only fb483b84...HEAD \| grep -E '^(package\.json\|tsconfig[^/]*\.json\|\.vscode-test\.\|\.claude/rules/\|\.agents/skills/\|extensions/drm-copilot/resources/claude-customizations/)'` → no output | Also satisfies the caller's parallel-orchestration ownership constraint. Executor scope-check artifact (`evidence/qa-gates/scope-check.2026-07-26T01-26.md`) concurs. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 17 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. After PR creation, confirm the `_drm-copilot-extension-tests.yml` runs (windows-latest and ubuntu-latest) pass at the PR head — these execute the CI-visible extension regression test.
2. Track the three recorded out-of-scope follow-ups with their owning orchestrations: root Jest CI wiring, `.claude/rules/typescript.md` Vitest/Jest reconciliation, and `jest-util` direct devDependency declaration (spec Rollout & Follow-up).

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 17 criteria evaluate to **PASS**, and all 17 checkboxes in `spec.md` were already checked by the executor (with per-AC evidence mapping in `evidence/other/ac-verification.2026-07-26T01-28.md`).
- This audit independently re-verified each criterion and confirms every existing check-off is justified. **No source-file checkbox change was made by this audit** because no unchecked PASS item and no unjustified checked item exists.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/spec.md`
- Total AC items: 17
- Checked off (delivered): 17
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/spec.md` | 17 | 17 | 0 | Checkbox-backed; all check-offs pre-existing and independently re-verified by this audit |
