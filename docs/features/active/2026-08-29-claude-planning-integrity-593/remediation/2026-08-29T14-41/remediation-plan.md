# Remediation Plan: Enforce Parseable Planner Internal Review

## Objective

Close the review blocker identified in `docs/features/active/2026-08-29-claude-planning-integrity-593/remediation/2026-08-29T14-41/remediation-inputs.md`: the atomic-planner stop hook currently accepts a label-only `PLANNER-INTERNAL-REVIEW` declaration. The remediation makes the complete internal-review record machine-validated, tests its rejection and acceptance paths without temporary files, maintains exact published Claude bundle parity, and restores AC2 only after final QA succeeds.

## Current-Tree Citations and Baselines

- The required behavior is AC2 in `docs/features/active/2026-08-29-claude-planning-integrity-593/spec.md:67` and its equivalent in `docs/features/active/2026-08-29-claude-planning-integrity-593/user-story.md:38`; both require review before the executor-owned preflight.
- The single executor-owned loop, convergence reporting, and iteration ceiling are protected by `docs/features/active/2026-08-29-claude-planning-integrity-593/spec.md:51`, `docs/features/active/2026-08-29-claude-planning-integrity-593/user-story.md:45-46`, and `.claude/skills/remediation-handoff-atomic-planner/SKILL.md:101-124`.
- `.claude/hooks/validate-planner-output.ps1:113-120` performs a label-presence check only, and `.claude/hooks/validate-planner-output.ps1:284-285` admits that result; these are the production defect locations.
- `.claude/skills/atomic-plan-contract/SKILL.md:160` and `.claude/agents/atomic-planner.md:60-62` require the three review dimensions but do not prescribe a parseable complete record. `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1:296-305` confirms that current fixture coverage treats label presence as sufficient. `tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py:97-109` performs keyword-presence assertions only.
- The required published mirror root is `extensions/drm-copilot/resources/claude-customizations/.claude/`; `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py:55-78` already defines whole-runtime root-to-bundle byte-parity behavior and will receive focused coverage for the changed runtime resources.
- The current PowerShell remediation baseline reports formatter success at `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/prd-feature-registration-powershell-format.2026-08-29T13-53.md` and analyzer success at `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/prd-feature-registration-powershell-analyze.2026-08-29T13-53.md`. The latest direct planner-hook coverage evidence is `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/powershell-coverage.2026-08-29T12-07.md` at 112 of 118 eligible lines (94.92%). P0-T4 must take a fresh hook-specific baseline before edits; it must not substitute the later 50-pass task-researcher/PRD-hook run.
- The current Python remediation baseline reports Black success (`458` files unchanged), Ruff success, Pyright `0` diagnostics, focused contracts `13` passed, and full `scripts.dev_tools` coverage of `93%` in the corresponding `prd-feature-registration-python-*.2026-08-29T13-53.md` artifacts under `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/`. These values are baseline evidence only; the execution must re-run and compare the same checks.

## Required Record Contract

The canonical grammar belongs in `.claude/skills/atomic-plan-contract/SKILL.md`; `.claude/agents/atomic-planner.md` uses that grammar, and `.claude/hooks/validate-planner-output.ps1` enforces it. The parser scopes the record from the sole `PLANNER-INTERNAL-REVIEW: PASS` line through the required `PREFLIGHT:` line, so labels elsewhere in the output cannot satisfy the contract. A complete record contains exactly one occurrence of each required declaration and one or more citation and AC-mapping records:

```text
PLANNER-INTERNAL-REVIEW: PASS
CITATION-TO-TREE: PASS
CITATION: <repository-relative path> | <nonblank locator>
ACCEPTANCE-CRITERION-TO-IMPLEMENTATION: PASS
AC-INVENTORY: AC1, AC2, ...
AC-MAPPING: <AC id> | IMPLEMENTATION: <nonblank path or identifier> | TESTS: <nonblank path or identifier> | EVIDENCE: <nonblank path or identifier>
SCOPE-BOUNDARY: PASS
UNRESOLVED-GAPS: NONE
```

The parser must reject a missing, blank, duplicate, or malformed declaration; a label-only declaration; an omitted, blank, partial, failed, blocked, or non-`PASS` review dimension; no citation record or a citation without a repository-relative path and locator; an empty or duplicate `AC-INVENTORY`; no AC mapping or a mapping without every implementation, tests, and evidence field; duplicate mapping identifiers; an inventory identifier without a mapping; a mapping identifier absent from the inventory; unresolved, failed, blocked, or absent gap disposition; and any label that occurs outside the bounded record. The hook must require exact unique-set equality between `AC-INVENTORY` and `AC-MAPPING` IDs. The hook remains the enforcement point; the canonical skill and agent prose state the same machine-checked grammar and do not substitute for validation.

## Acceptance-Criterion Traceability

| Requirement | Implementation | Focused tests | Evidence / completion gate |
| --- | --- | --- | --- |
| AC2 internal review is complete and recorded before executor preflight | `.claude/skills/atomic-plan-contract/SKILL.md`; `.claude/hooks/validate-planner-output.ps1`; `.claude/agents/atomic-planner.md`; exact bundle mirrors | `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`; `tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py`; `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` | Pester coverage and Python QA artifacts under `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/`; byte-parity result; AC2 checkoff artifact |
| Issue #586 executor preflight ownership, convergence, and iteration controls are preserved | No change to `.claude/skills/remediation-handoff-atomic-planner/SKILL.md` or executor-loop ownership; hook continues to validate only planner output | Existing preflight-contract tests remain in `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` | Focused Pester/Python results and final scope review in the AC2 checkoff artifact |
| Canonical runtime changes are published byte-identically | Canonical `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/hooks/validate-planner-output.ps1`, and `.claude/agents/atomic-planner.md`, then their three exact bundle paths | Push-down resource contract assertions and byte comparison | Bundle parity artifact in `evidence/qa-gates/` |

### Phase 0 — Policy Reads and Independent Baselines

- [ ] [P0-T1] Read policies in this exact order: `AGENTS.md`; `.agents/skills/general-code-change/SKILL.md`; `.agents/skills/general-unit-test/SKILL.md`; `.agents/skills/powershell/SKILL.md`; `.agents/skills/python/SKILL.md`; `.agents/skills/python-suppressions/SKILL.md`; `.agents/skills/atomic-plan-contract/SKILL.md`; and `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`; record `Timestamp:`, `Command:` used to read them, `EXIT_CODE:`, `Output Summary:`, files read, and order in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-phase0-instructions-read.2026-08-29T14-41.md`.
- [ ] [P0-T2] Capture the PowerShell formatting baseline by running `mcp__drm-copilot__run_poshqc_format` with scan folders `.claude/hooks` and `tests/scripts/claude-hooks`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, every actually scanned file, confirmation that `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` were included, and formatter-change observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-powershell-format-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T3] Capture the PowerShell analysis baseline by running `mcp__drm-copilot__run_poshqc_analyze` with scan folders `.claude/hooks` and `tests/scripts/claude-hooks`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, every actually scanned file, confirmation that `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` were included, and analyzer finding count in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-powershell-analyze-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T4] Capture a hook-specific PowerShell Pester coverage baseline by running `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` with JaCoCo coverage for `.claude/hooks/validate-planner-output.ps1`; write the XML and a summary containing `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, passed/failed/skipped totals, eligible/covered/missed lines, and numeric percentage to `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-pester-baseline.2026-08-29T14-41.xml` and `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-powershell-tests-and-coverage-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T5] Capture the Python formatting baseline by running `poetry run black .`, requiring the success output `left unchanged`, and observing `git status --porcelain --untracked-files=all` before and after; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and changed-file observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-python-format-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T6] Capture the Python lint baseline by running `poetry run ruff check .`, requiring the success output `All checks passed`, and observing `git status --porcelain --untracked-files=all` before and after; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and changed-file observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-python-lint-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T7] Capture the Python type baseline by running `poetry run pyright`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and diagnostic totals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-python-type-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T8] Capture the focused Python contract baseline by running `poetry run pytest tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -p no:cacheprovider -q`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and test totals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-python-focused-baseline.2026-08-29T14-41.md`.
- [ ] [P0-T9] Capture the full Python coverage baseline by running `poetry run pytest --cov=scripts.dev_tools --cov-report=term-missing`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, pass/fail/skip totals, statements, missed lines, and numeric percentage in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/remediation-baseline/planner-review-python-full-coverage-baseline.2026-08-29T14-41.md`.

### Phase 1 — Enforce the Runtime Contract

- [ ] [P1-T1] Add the single canonical line-record grammar to `.claude/skills/atomic-plan-contract/SKILL.md`, including bounded-record parsing from `PLANNER-INTERNAL-REVIEW: PASS` through the existing `PREFLIGHT:` signal, required PASS declarations, citation syntax, `AC-INVENTORY`, `AC-MAPPING`, exact unique-set equality, and `UNRESOLVED-GAPS: NONE`; retain the issue #586 single executor-owned loop unchanged.
- [ ] [P1-T2] Replace the label-presence logic in `.claude/hooks/validate-planner-output.ps1` with a pure parser/validator for the P1-T1 grammar that rejects every missing, blank, partial, malformed, duplicate, failed, blocked, out-of-bounds, and AC-inventory/mapping disagreement condition in this plan's Required Record Contract, emits actionable failure messages, and preserves the existing plan-path, structural-plan, and presence-based exact preflight-signal matcher.
- [ ] [P1-T3] Update `.claude/agents/atomic-planner.md` to require the exact P1-T1 line record before preflight, including passing outcomes, bounded current-tree citations, complete AC inventory/mappings, scope-boundary result, and no-unresolved-gaps disposition; keep its existing single validation-only `atomic-executor` handoff and do not add a second preflight loop.
- [ ] [P1-T4] Extend `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` with isolated in-memory payload and mocked-plan fixtures that preserve a valid plan path while positively accepting both `PREFLIGHT: ALL CLEAR` and `PREFLIGHT: REVISIONS REQUIRED`, a multi-citation/multi-AC record, and surrounding unrelated prose. Independently reject no record; a bare four-token or label-only record; blank or non-`PASS` record header; each missing, blank, duplicate, and non-`PASS` review dimension; no citation and citation path versus locator failures; no, blank, or duplicate `AC-INVENTORY`; no or blank `AC-MAPPING`; each missing or blank AC ID, `IMPLEMENTATION`, `TESTS`, and `EVIDENCE` mapping field; duplicate, missing, and extra mapping IDs; missing, blank, or non-`NONE` `UNRESOLVED-GAPS`; and valid-looking labels before or after the bounded record. Do not create or use temporary files.
- [ ] [P1-T5] Extend `tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py` with an independently constructed static contract check that aligns the schema stated in `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/agents/atomic-planner.md`, and `.claude/hooks/validate-planner-output.ps1`, and verifies that `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` names the required positive and negative fixture inventory; do not duplicate or claim to execute the PowerShell parser, because Pester is the runtime-behavior authority.
- [ ] [P1-T6] Extend `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` with focused assertions that the canonical and bundled `atomic-plan-contract/SKILL.md`, `validate-planner-output.ps1`, and `atomic-planner.md` resources each exist and are byte-identical, in addition to the existing complete `.claude/**` parity check.
- [ ] [P1-T7] Copy only the completed canonical runtime files `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/hooks/validate-planner-output.ps1`, and `.claude/agents/atomic-planner.md` to their exact `extensions/drm-copilot/resources/claude-customizations/.claude/skills/atomic-plan-contract/SKILL.md`, `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1`, and `extensions/drm-copilot/resources/claude-customizations/.claude/agents/atomic-planner.md` mirror paths, then verify each pair with a byte comparison before proceeding.

### Phase 2 — PowerShell Final QA

- [ ] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` with scan folders `.claude/hooks` and `tests/scripts/claude-hooks`; if formatting changes files, restart this phase from P2-T1, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, every actually scanned file, confirmation that `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` were included, and formatter-change observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-powershell-format.2026-08-29T14-41.md`.
- [ ] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` with scan folders `.claude/hooks` and `tests/scripts/claude-hooks`; if analysis fails or changes files, restart this phase from P2-T1, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, every actually scanned file, confirmation that `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` were included, and analyzer finding count in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-powershell-analyze.2026-08-29T14-41.md`.
- [ ] [P2-T3] Run `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` and `tests/scripts/claude-runtime/claude-settings.Tests.ps1` using a Pester configuration with JaCoCo coverage for `.claude/hooks/validate-planner-output.ps1`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, test totals, eligible/covered/missed lines, and numeric coverage in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-pester-post.2026-08-29T14-41.xml` and `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-powershell-tests-and-coverage.2026-08-29T14-41.md`.
- [ ] [P2-T4] Compare the P2-T3 hook coverage with the fresh P0-T4 baseline and the re-derived prior 94.92% baseline, require retention or improvement of the fresh baseline, no drop below 94.92%, and at least 90% line coverage for all changed production code in `.claude/hooks/validate-planner-output.ps1`, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the three numeric values, and verdict in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-powershell-coverage-comparison.2026-08-29T14-41.md`.

### Phase 3 — Python, Publication, and Acceptance QA

- [ ] [P3-T1] Run `poetry run black .`, require the success output `left unchanged`, and observe `git status --porcelain --untracked-files=all` before and after; if formatting changes files, restart the applicable PowerShell and Python ordered QA loops from P2-T1 and P3-T1 respectively, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and changed-file observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-format.2026-08-29T14-41.md`.
- [ ] [P3-T2] Run `poetry run ruff check .`, require the success output `All checks passed`, and observe `git status --porcelain --untracked-files=all` before and after; if it fails or changes files, restart the applicable PowerShell and Python ordered QA loops from P2-T1 and P3-T1 respectively, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and changed-file observation in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-lint.2026-08-29T14-41.md`.
- [ ] [P3-T3] Run `poetry run pyright`; if it fails, restart the applicable PowerShell and Python ordered QA loops from P2-T1 and P3-T1 respectively, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and diagnostic totals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-type.2026-08-29T14-41.md`.
- [ ] [P3-T4] Run `poetry run pytest tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -p no:cacheprovider -q`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and focused test totals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-focused.2026-08-29T14-41.md`.
- [ ] [P3-T5] Run `poetry run pytest --cov=scripts.dev_tools --cov-report=term-missing`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, test totals, statements, missed lines, and numeric percentage in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-full-coverage.2026-08-29T14-41.md`.
- [ ] [P3-T6] Compare P3-T5 full Python coverage with P0-T9, require no regression from the recorded baseline, and record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the two numeric percentages, and verdict in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-python-coverage-comparison.2026-08-29T14-41.md`; no Python production module is changed, so this repository-level comparison is the applicable Python coverage gate.
- [ ] [P3-T7] Run the focused push-down parity test and an independent byte comparison for all three canonical/bundle pairs: `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/hooks/validate-planner-output.ps1`, and `.claude/agents/atomic-planner.md`; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, each pair path, and both result signals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-bundle-parity.2026-08-29T14-41.md`.
- [ ] [P3-T8] Verify that reusable PowerShell/Python production and test scripts `.claude/hooks/validate-planner-output.ps1`, its PowerShell bundle mirror, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `tests/scripts/dev_tools/test_claude_planning_integrity_contracts.py`, and `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` each remain at or below 500 lines; record `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and per-file line totals in `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-file-size.2026-08-29T14-41.md`. Markdown contract and agent parity remain exclusively covered by P3-T7.
- [ ] [P3-T9] Only after P2-T1 through P3-T8 are recorded as passing, check AC2 in `docs/features/active/2026-08-29-claude-planning-integrity-593/spec.md` and `docs/features/active/2026-08-29-claude-planning-integrity-593/user-story.md`; write `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the AC-to-implementation/test/evidence reconciliation, and preserved issue #586 scope boundary to `docs/features/active/2026-08-29-claude-planning-integrity-593/evidence/qa-gates/planner-review-ac2-checkoff.2026-08-29T14-41.md`.

## Internal Preflight-Shaped Self-Review

`SELF-REVIEW: RE-DERIVED THIS PASS`

- Citation-to-tree verification: re-derived the current AC2, scope, atomic-plan contract, hook, planner-agent, Pester, Python-contract, bundle-parity, and remediation-loop citations listed above against the current tree. P0/P2 now include both the changed hook and focused Pester file; P3-T7 names the same three canonical/bundle pairs as P1-T7 and the traceability table. No citation points to a non-existent file or stale required behavior.
- AC-to-implementation/test/evidence traceability: AC2 maps to P1-T1 through P1-T7, P2-T1 through P3-T8, and P3-T9. P1-T4 provides complete runtime fixture coverage; P1-T5 independently checks the canonical skill/agent/hook schema and named Pester fixture inventory without executing the parser. The issue #586 preservation requirement maps to P1-T1/P1-T2 and the focused QA artifacts. Every evidence-producing task explicitly requires `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` plus its task-specific evidence.
- Scope-boundary consistency: the plan changes only the Claude atomic-plan contract, planner hook, planner-agent contract, their three exact Claude bundle mirrors, targeted tests, AC2 checkoff, and evidence. It explicitly excludes Codex runtime files, settings, generic counters, numeric-provenance surfaces, parallel intake, and issue #589-owned `SELF-REVIEW:` presence/exclusivity/enumeration, `CONVERGENCE:`, atomic-executor wording, and iteration-ceiling enforcement. It does not modify issue #586 executor-loop/remediation skill controls.
- Task-reference and count check: phases are numbered 0 through 3; task identifiers are sequential in every phase; all referenced task IDs exist; every stated pair count resolves to the three explicitly named canonical/bundle paths; every task names one or more explicit paths; evidence output resolves only under the feature's `evidence/` tree.
- Unresolved gaps: none. The label-only-record defect, incomplete policy/read and production-hook baseline scope, pair-count inconsistency, incomplete fixture inventory, Python-runtime-authority overlap, and incomplete evidence schemas are each resolved by the revised Phase 0 through Phase 3 tasks.

## Handoff Conditions

The executor must receive this exact path unchanged through preflight revisions and the directive `DIRECTIVE: PREFLIGHT VALIDATION ONLY`. It must return `PREFLIGHT: ALL CLEAR` or `PREFLIGHT: REVISIONS REQUIRED`; existing convergence handling remains outside this remediation's scope. The orchestrator records every pass; for this well-scoped remediation, a second or later pass requires the process-defect investigation required by `.claude/skills/remediation-handoff-atomic-planner/SKILL.md:110-111`. Do not execute, stage, commit, push, publish, create a pull request, or merge from this plan.
