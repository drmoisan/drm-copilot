# Policy Compliance Audit — F6 `ts-new-potential-bug-entry` (Issue #240)

- Timestamp: 2026-06-26T02-34
- Reviewer: feature-review agent
- Feature: F6 — `ts-new-potential-bug-entry`
- Feature folder: `docs/features/active/2026-06-25-port-python-commands-to-typescript-240`
- Work mode (from `issue.md`): `full-feature`

## Scope and Baseline

- Resolved base branch: `main` (`origin/main`)
- Merge-base SHA: `1bccb0c37d81e803dd2df096e4c7005c596986d0`
- Head SHA: `70ccc851fbfc9928d3b7c556363f0e8e183ece01`
- Audit scope: full branch diff against the resolved base branch (feature-vs-base), not any plan/task subset.

### Authoritative changed-file inventory (from `git diff --name-status <merge-base>..HEAD`)

Production TypeScript:
- `A extensions/drm-copilot/src/lib/new-potential-bug-entry.ts`
- `A extensions/drm-copilot/src/lib/new-potential-bug-entry-service-call.ts`
- `M extensions/drm-copilot/src/repo-automation-service.ts`

Test TypeScript:
- `A extensions/drm-copilot/test/extension.new-potential-bug-entry-inprocess.test.ts`
- `A extensions/drm-copilot/test/lib/new-potential-bug-entry.test.ts`
- `A extensions/drm-copilot/test/lib/new-potential-bug-entry-launcher.test.ts`
- `A extensions/drm-copilot/test/lib/new-potential-bug-entry-service-call.test.ts`
- `M extensions/drm-copilot/test/extension.workflow-commands.test.ts`

Docs / evidence (non-code):
- `A docs/.../plans/F6-new-potential-bug-entry.plan.md` and 10 evidence artifacts under `evidence/baseline/`, `evidence/qa-gates/`, `evidence/regression-testing/`.

### PR-context summary discrepancy (noted, not blocking)

`artifacts/pr_context.summary.txt` reports "Core logic changes: 0 files" and classifies all changes as "Docs/templates/agents/tooling." This is a misclassification: the authoritative `git diff` shows three changed production TypeScript files and five changed test files. Per the task input, the appendix git name-status and direct `git diff` are authoritative and were used as the basis for this audit.

## Languages With Changed Files (coverage obligation)

| Language | Changed files in diff? | Coverage verdict |
|---|---|---|
| TypeScript | Yes | PASS (see Coverage section) |
| Python | No | N/A (zero changed files) |
| PowerShell | No | N/A (zero changed files) |
| C# | No | N/A (zero changed files) |

## Toolchain Verification (independently re-run by reviewer)

All commands run from `extensions/drm-copilot/`.

| Stage | Command | Exit | Verdict |
|---|---|---|---|
| Format | `npx prettier --check "src/**/*.ts" "test/**/*.ts"` | 0 | PASS — "All matched files use Prettier code style!" |
| Lint | `npm run lint` (`eslint --no-error-on-unmatched-pattern src test`) | 0 | PASS — 0 errors, 0 warnings |
| Type check | `npm run typecheck` (`tsc -p ./ --noEmit`) | 0 | PASS — 0 errors |
| Tests + coverage | `node run-jest.cjs --coverage --collectCoverageFrom="src/lib/**/*.ts"` | 0 | PASS — 64 suites, 725/725 tests passed |

Architecture-boundary (dependency-cruiser) and contract/schema stages were not separately invoked; the changed files are host-neutral `src/lib/**` modules and a service-wiring delegation, none of which introduce cross-layer imports (verified by inspection: imports are limited to sibling `lib/` modules, `node:fs`, `node:path`, and `../repo-automation-service-support`). No Office.js / Graph / COM references are introduced.

## Coverage Compliance

Coverage artifact present: `extensions/drm-copilot/coverage/lcov.info` (regenerated by the reviewer's own coverage run, exit 0). Per-file figures cross-checked against the executor evidence `evidence/qa-gates/f6-final-test-coverage.md` and `evidence/qa-gates/f6-coverage-delta.md`; values match.

Thresholds (uniform tier rule, `.claude/rules/quality-tiers.md`): line >= 85%, branch >= 75%.

| File | Tier | Line % | Branch % | Threshold | Verdict |
|---|---|---|---|---|---|
| `src/lib/new-potential-bug-entry.ts` (new) | new code | 95.87 | 82.97 | 85 / 75 | PASS |
| `src/lib/new-potential-bug-entry-service-call.ts` (new) | new code | 100 | 100 | 85 / 75 | PASS |
| `src/repo-automation-service.ts` (modified) | modified | not in `src/lib/**` collection set | — | no-regression on changed lines | PASS (changed lines are a delegation + import; behavior covered by service-call and in-process tests) |
| Repo-wide `src/lib/**` (TypeScript) | repo-wide | 96.33 | 87.87 | 85 / 75 | PASS |

Overall `src/lib/**` line coverage: 96.30% -> 96.33% (no regression). Overall branch: 88.06% -> 87.87% (-0.19%), a weighted-average effect of adding a new production file whose own branch coverage (82.97%) is above the 75% floor but below the prior aggregate. The residual uncovered branches in `new-potential-bug-entry.ts` (lines 263-272, 348-351, 404-408) are host-bound production defaults (real `git` subprocess construction, real PATH/`existsSync` probe) that the hermetic unit-test policy prohibits exercising; the service/MCP path always injects these seams. The file is not excluded from coverage; the uncovered glue remains a visible, minimal cost in the metric, consistent with the coverage-exclusion policy.

TypeScript coverage verdict: **PASS**.

## Coverage Exclusion Policy

No `exclude` entry matching a production `src/` path was introduced. No production file is excluded from coverage measurement. Verdict: **PASS**.

## File Size Limit (500 lines)

Independently verified with `wc -l`.

| File | Lines | Verdict |
|---|---|---|
| `src/lib/new-potential-bug-entry.ts` | 461 | PASS |
| `src/lib/new-potential-bug-entry-service-call.ts` | 101 | PASS |
| `src/repo-automation-service.ts` | 498 | PASS (<= 500; was 500 at baseline, net -2) |
| `test/extension.new-potential-bug-entry-inprocess.test.ts` | 258 | PASS |
| `test/lib/new-potential-bug-entry.test.ts` | 414 | PASS |
| `test/lib/new-potential-bug-entry-launcher.test.ts` | 154 | PASS |
| `test/lib/new-potential-bug-entry-service-call.test.ts` | 160 | PASS |
| `test/extension.workflow-commands.test.ts` | 790 | PARTIAL (over 500) — see finding P-1 |

### Finding P-1 (PARTIAL, pre-existing, non-blocking)

`extension.workflow-commands.test.ts` is 790 lines, exceeding the 500-line limit. This file existed at the merge-base at 880 lines and was reduced to 790 by F6 (`git diff --stat`: 7 insertions, 97 deletions; the in-process cases were extracted to the new `extension.new-potential-bug-entry-inprocess.test.ts`). F6 reduced the file by 90 lines and did not grow it. The over-limit condition is pre-existing and out of F6 scope (the plan's P2-T4 mandate was "do not grow it," which is satisfied). Recorded as a PARTIAL observation for repository tracking; full remediation belongs to a separate file-decomposition effort, not F6. This is not a blocking finding for the F6 PR.

## General Code-Change Policy

- Simplicity / separation of concerns: PASS. Pure library logic (`new-potential-bug-entry.ts`) is separated from service wiring (`new-potential-bug-entry-service-call.ts`), mirroring the F2/F5 precedents. All external interactions (filesystem, git, PATH, env, editor launch) flow through injectable seams.
- Error handling: PASS. `validateShortName` fails fast with a byte-identical message; missing template surfaces as a file-not-found read error; no broad catch-all handlers.
- Naming / ES modules / no `any`: PASS. Kebab-case filenames; ES module imports/exports throughout; no TypeScript `any` type (grep matches were the English word "any" in doc comments only).
- I/O boundary: PASS. The library entry point performs no direct host I/O; production defaults that touch `node:fs`/PATH/git are isolated as injectable seams.

## General Unit-Test Policy

- Independence / isolation / determinism: PASS. Each test targets one behavior, uses AAA structure, and injects all seams. The single `process.env["PATH"]` mutation in the launcher test is restored in a `finally` block, leaving no global side effect.
- No temp files / no external processes: PASS. A `Map`-backed in-memory `FileSystem` fake and stub `CommandRunner` are used; no real git, no real VS Code launch, no temp files.
- Test file location: PASS. Library tests live under `test/lib/` mirroring `src/lib/`; extension tests live under `test/`. No colocation in the production tree.
- Framework divergence (Jest vs Vitest): the package uses Jest, which `.claude/rules/typescript.md` text names as Vitest. This is accepted decision D1 in `spec.md` (pre-existing, package-wide, CI-exercised toolchain is Jest). Treated as a policy-reconciliation item, not an F6 blocker.

## Evidence Location Compliance

- Branch diff scanned for files under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`, `artifacts/coverage/`: NONE found.
- `python scripts/dev_tools/validate_evidence_locations.py --root .` exit code: 0 (no violations).
- All F6 evidence artifacts are written under the canonical `<FEATURE>/evidence/<kind>/` scheme.

Verdict: **PASS**.

## modified-workflow-needs-green-run

Branch diff scanned for `.github/workflows/**`, `scripts/benchmarks/**`, `.github/actions/**`: NONE matched. The rule does not fire. Verdict: **PASS (rule not triggered)**.

## Benchmark Baseline Provenance

No baseline JSON files were added or modified under any benchmark-consumed path. Rule not triggered. Verdict: **PASS (rule not triggered)**.

## Rejected Scope Narrowing

The caller prompt states: "Feature F6 ... TypeScript port of the bundled `new_potential_bug_entry.py` ... Python sources retained (removal is F11)." This describes the feature's intended scope and does not instruct the reviewer to narrow the audit, skip a language check, or mark any language with changed files as out of scope. No scope-narrowing instruction was detected. The audit was performed against the full branch diff regardless. No verbatim narrowing text requires recording.

## Verdict Summary

| Policy area | Verdict |
|---|---|
| Toolchain (format / lint / typecheck / test) | PASS |
| TypeScript coverage (new files, repo-wide, no regression) | PASS |
| Coverage exclusion policy | PASS |
| File size limit | PARTIAL (P-1: pre-existing over-limit test file, reduced by F6, out of scope) |
| General code-change policy | PASS |
| General unit-test policy | PASS |
| Evidence location compliance | PASS |
| modified-workflow-needs-green-run | PASS (not triggered) |
| Benchmark baseline provenance | PASS (not triggered) |

Blocking findings: 0. Material PARTIAL findings requiring F6 remediation: 0 (P-1 is pre-existing and explicitly out of F6 scope).

## Appendix B — Command Reference

- `git diff --name-status 1bccb0c37d81e803dd2df096e4c7005c596986d0..HEAD`
- `git diff --stat 1bccb0c37d81e803dd2df096e4c7005c596986d0..HEAD -- extensions/drm-copilot/test/extension.workflow-commands.test.ts`
- `git show 1bccb0c37d81e803dd2df096e4c7005c596986d0:extensions/drm-copilot/test/extension.workflow-commands.test.ts | wc -l`
- `wc -l` on all changed production and test files (from `extensions/drm-copilot/`)
- `npx prettier --check "src/**/*.ts" "test/**/*.ts"` (from `extensions/drm-copilot/`)
- `npm run lint` (from `extensions/drm-copilot/`)
- `npm run typecheck` (from `extensions/drm-copilot/`)
- `node run-jest.cjs --coverage --collectCoverageFrom="src/lib/**/*.ts"` (from `extensions/drm-copilot/`)
- `python scripts/dev_tools/validate_evidence_locations.py --root .` (from repo root)
