Timestamp: 2026-08-23T09-29
Command: `& { $trimChars = [char[]]@([System.IO.Path]::DirectorySeparatorChar, [System.IO.Path]::AltDirectorySeparatorChar); $comparison = [System.StringComparison]::OrdinalIgnoreCase; $workspaceOutput = @(& git rev-parse --show-toplevel); if ($LASTEXITCODE -ne 0 -or $workspaceOutput.Count -ne 1 -or [string]::IsNullOrWhiteSpace([string]$workspaceOutput[0])) { throw 'Unable to resolve exactly one workspace root.' }; $workspaceRoot = [System.IO.Path]::GetFullPath(([string]$workspaceOutput[0]).Trim()).TrimEnd($trimChars); $expectedWorkspaceRoot = [System.IO.Path]::GetFullPath('C:/Users/DanMoisan/repos/drm-copilot-wt/2026-08-17T07-01').TrimEnd($trimChars); if (-not [string]::Equals($workspaceRoot, $expectedWorkspaceRoot, $comparison) -or [string]::Equals($workspaceRoot, [System.IO.Path]::GetPathRoot($workspaceRoot).TrimEnd($trimChars), $comparison)) { throw 'Resolved workspace root is unexpected or broad.' }; $evidenceDirectory = [System.IO.Path]::GetFullPath((Join-Path -Path $workspaceRoot -ChildPath 'docs/features/active/2026-08-17-orchestrator-remediation-loop-control-484/evidence/regression-testing')).TrimEnd($trimChars); $workspacePrefix = $workspaceRoot + [System.IO.Path]::DirectorySeparatorChar; if (-not $evidenceDirectory.StartsWith($workspacePrefix, $comparison)) { throw 'Evidence directory is outside the workspace.' }; $evidenceItem = Get-Item -LiteralPath $evidenceDirectory -Force -ErrorAction Stop; if (-not $evidenceItem.PSIsContainer -or $null -ne $evidenceItem.LinkType -or (($evidenceItem.Attributes -band [System.IO.FileAttributes]::ReparsePoint) -ne 0)) { throw 'Evidence directory must be a real in-workspace directory, not a link or reparse point.' }; $target = [System.IO.Path]::GetFullPath((Join-Path -Path $evidenceDirectory -ChildPath 'packed-mcp')).TrimEnd($trimChars); $expectedTarget = [System.IO.Path]::GetFullPath((Join-Path -Path $evidenceDirectory -ChildPath 'packed-mcp')).TrimEnd($trimChars); $targetParent = [System.IO.Path]::GetDirectoryName($target).TrimEnd($trimChars); if (-not [string]::Equals($target, $expectedTarget, $comparison) -or -not [string]::Equals($targetParent, $evidenceDirectory, $comparison) -or [string]::Equals($target, $workspaceRoot, $comparison) -or [string]::Equals($target, $evidenceDirectory, $comparison)) { throw 'Cleanup target identity or parent is invalid.' }; $targetItem = $null; try { $targetItem = Get-Item -LiteralPath $target -Force -ErrorAction Stop } catch [System.Management.Automation.ItemNotFoundException] { $targetItem = $null }; if ($null -ne $targetItem) { if (-not $targetItem.PSIsContainer -or $null -ne $targetItem.LinkType -or (($targetItem.Attributes -band [System.IO.FileAttributes]::ReparsePoint) -ne 0)) { throw 'Refusing to remove a non-directory, symbolic link, junction, mount point, or other reparse point.' }; Remove-Item -LiteralPath $target -Recurse -Force -ErrorAction Stop }; $remaining = $null; try { $remaining = Get-Item -LiteralPath $target -Force -ErrorAction Stop } catch [System.Management.Automation.ItemNotFoundException] { $remaining = $null }; if ($null -ne $remaining) { throw 'Exact packed-mcp target still exists after cleanup.' } }`
EXIT_CODE: 0
Output Summary: The exact existing feature-local extraction directory passed an independent read-only guard audit before deletion and the unchanged bounded command completed successfully. The workspace root and target were resolved with case-insensitive Windows comparisons; the workspace was not a broad filesystem root; the evidence directory was inside the exact workspace; and both the evidence directory and target were real directories with null `LinkType` and no `ReparsePoint`. The target existed, was removed only through `Remove-Item -LiteralPath $target -Recurse -Force -ErrorAction Stop`, and was proven absent before the packed smoke command. The command also treats an already-absent target as success without deletion and rejects non-directories, symbolic links, junctions, mount points, and every other reparse point. No glob, unresolved target, inexact parent, broad root, or out-of-workspace path was used.
Resolved Workspace Root: `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-17T07-01`
Resolved Evidence Directory: `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-17T07-01\docs\features\active\2026-08-17-orchestrator-remediation-loop-control-484\evidence\regression-testing`
Exact Target: `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-17T07-01\docs\features\active\2026-08-17-orchestrator-remediation-loop-control-484\evidence\regression-testing\packed-mcp`
Target State Before Cleanup: `present; PSIsContainer=true; LinkType=null; ReparsePoint=false`
Deletion Result: `exact target removed`
Target State Before Smoke: `absent`
