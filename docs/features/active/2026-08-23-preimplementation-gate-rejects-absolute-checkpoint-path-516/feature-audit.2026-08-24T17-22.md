# Feature Audit: Preimplementation Gate Rejects Absolute Checkpoint Path (#516)

**Audit Date:** 2026-08-24
**Feature Folder:** `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516`
**Base Branch:** `main`
**Head Branch:** `bug/preimplementation-gate-rejects-absolute-checkpoint-path-516`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

**Template provenance:** Created from the bundled asset source at `extensions/drm-copilot/resources/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md`, the identical file the MCP tool `resolve_policy_audit_template_asset` resolves for selector `feature-audit-template`; that tool is unavailable in this delegated review environment.

---

## Scope and Baseline

- **Base branch:** `main` (merge-base commit `fb3e1f331cc52d1dd7a61332d6d23fcc0b495e24`)
- **Head branch/commit:** `bug/preimplementation-gate-rejects-absolute-checkpoint-path-516` (commit `b50f4e2881545685f13d6ce2ae22b2dd1d107542`)
- **Merge base:** `fb3e1f331cc52d1dd7a61332d6d23fcc0b495e24`
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer against base `main`; the artifacts were absent at review start)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516/evidence/**`
  - Additional evidence: reviewer-side independent re-runs (Pester 68/68 and 176/176, PSScriptAnalyzer 0 findings, pytest parity 10/10, SHA256 parity, coverage-XML parse, commit-content inspection)
- **Feature folder used:** `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516`
- **Requirements source:** `spec.md` only (its `## Acceptance Criteria` section)
- **Work mode resolution note:** `issue.md` carries the explicit marker `- Work Mode: full-bug`, so `spec.md` is the sole AC source and `user-story.md` is correctly absent; its absence is not a finding.
- **Scope note:** The plan's `## Base State` section names baseline `c308dd92`, which predates the merge of PR #536 and is now an ancestor of `main`. The branch was rebased onto the current `origin/main` tip, so all diff evidence in this audit uses the merge-base `fb3e1f33`, consistent with the executor's recorded baseline substitution (`evidence/qa-gates/file-set-discipline.2026-08-23T23-25.md`). The branch carries exactly two commits: `9c12d20a` (feature-folder documents only) and `b50f4e28` (the implementation).

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516/spec.md` — only source (`full-bug`)

The section contains 39 checkbox criteria in ten groups. At review start 37 were ticked and 2 were unticked (the Codex-pair single-commit criterion and the branch-coverage-exemption criterion, the latter naming the reviewer as a required actor). Criteria are abbreviated below for the evaluation table; the source wording is preserved unmodified in `spec.md`.

### Acceptance criteria (abbreviated labels; grouped as in the source)

Positive half: 1. Seven forward-slash absolute allow cases, one per literal, with explicit not-ready `-CheckpointRaw`. 2. Seven backslash absolute allow cases. 3. At least one POSIX-shaped absolute allow case. 4. Equivalent Codex-suite absolute allow cases. 5. Leading `./` relative allow case.
No regression on relative spellings: 6. Seven repo-relative allow cases under explicit not-ready checkpoint. 7. Existing Claude gate suite passes with zero modifications. 8. Existing Codex legacy-contracts suite passes with zero modifications, including its two pre-existing deny cases.
Negative half: 9. Absolute production `.ps1` denies. 10. Absolute production `.py` denies. 11. Absolute non-literal `artifacts/orchestration/` JSON denies. 12. Absolute checkpoint-named JSON without the preceding segment denies. 13. Absolute `..`-hop checkpoint path denies (accepted fail-closed miss asserted). 14. Every negative-half case passes in both fail-before and pass-after captures.
Documentation exemption and case sensitivity: 15. Repo-relative feature-folder `.json` allows. 16. Absolute forward-slash and backslash feature-folder spellings allow. 17. Case-varied documentation prefix denies (`-cmatch` preserved). 18. Case-varied checkpoint literal allows (`-match` preserved).
Idempotence and untouched behavior: 19. Codex `apply_patch` file-marker idempotence in both directions. 20. Only the two named function bodies changed per copy; all other named symbols byte-unchanged. 21. Schema-contract and transport suites pass with zero modifications. 22. No-Python-invocation suite passes with zero modifications.
Four-copy parity: 23. All four copies modified and carrying the segment-anchored predicates. 24. Claude pair SHA256-identical. 25. Codex pair SHA256-identical and landed in a single commit. 26. Push-down parity pytest exits 0.
Toolchain: 27. Format reports no files requiring reformatting on the final clean pass. 28. Analyze reports zero findings across the six changed files. 29. Test reports zero failed and zero errored. 30. Format, analyze, test complete in a single ordered pass.
Coverage: 31. Per-file line coverage >= 85% for both canonical hook copies. 32. Every changed line covered; no regression. 33. Spec records, and the reviewer confirms, the PowerShell branch-coverage exemption without removing either copy from the coverage denominator; neither copy in any exclusion list.
Regression evidence: 34. Fail-before capture with positive-half absolute cases failing and negative-half passing. 35. Pass-after capture with every case passing. 36. Every case in both captures supplied an explicit not-ready `-CheckpointRaw`; no vacuous pass.
File-set discipline: 37. Both new suites exist and are under 500 lines. 38. Every absolute path string in both suites is a literal constant; no environment-derived test paths. 39. Changed-path union contains exactly the seven declared paths plus this feature folder; nothing under `.claude/rules/`, `.github/instructions/`, or `quality-tiers.yml`.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Seven forward-slash absolute allow cases | PASS | Suite text: `$CheckpointAllowCases` emits one forward-slash case per literal via `Get-GateDecisionFor`, which passes `-CheckpointRaw` unconditionally; reviewer re-run all pass | `Invoke-Pester` on the Claude suite (68/68 combined) | Fail-before capture shows the same 7 cases failing pre-fix, proving non-vacuity |
| 2 | Seven backslash absolute allow cases | PASS | Same case table, backslash spelling built by `-replace '/', '\'`; re-run pass | same | Proves upstream separator normalization feeds the new predicate |
| 3 | POSIX-shaped absolute allow case | PASS | `$AdditionalCheckpointAllowCases` entry with `/synthetic-posix-root/...`; re-run pass | same | |
| 4 | Codex-suite equivalent absolute allow cases | PASS | Codex suite mirrors the full matrix against the dot-sourced `.codex` hook with the flat payload shape; re-run pass (35/35) | `Invoke-Pester` on the Codex suite | |
| 5 | Leading `./` relative allow case | PASS | `$AdditionalCheckpointAllowCases` dot-slash entry in both suites; re-run pass | same | Anchor admits via the `/` of `./` |
| 6 | Seven repo-relative allow cases (regression) | PASS | Case table includes repo-relative spelling per literal; passed in fail-before too | same | |
| 7 | Existing Claude gate suite unmodified and passing | PASS | Absent from `git diff --name-status fb3e1f33..b50f4e28`; reviewer re-run pass (part of 176/176) | `git diff --name-status fb3e1f33..b50f4e28`; `Invoke-Pester` | |
| 8 | Existing Codex legacy-contracts suite unmodified and passing | PASS | Absent from the diff; reviewer re-run pass; suite carries the byte-identity assertion over the Codex pair | same | Its two pre-existing deny cases pass within the zero-failure run |
| 9 | Absolute production `.ps1` denies | PASS | `$ImplementationDenyCases`; re-run pass in both suites; also passed in fail-before | `Invoke-Pester` | |
| 10 | Absolute production `.py` denies | PASS | same | same | |
| 11 | Absolute non-literal orchestration JSON denies | PASS | same | same | |
| 12 | Checkpoint-named JSON without preceding segment denies | PASS | same | same | |
| 13 | `..`-hop checkpoint path denies | PASS | same; the hop sits inside the `artifacts/orchestration/` segment, so no contiguous literal exists in the text | same | Accepted fail-closed miss asserted, not left undocumented |
| 14 | Negative half passes in both captures | PASS | `fail-before-new-suites...md` lists all five deny cases among passers in the 38-failure run; pass-after and reviewer re-run both pass | evidence inspection + `Invoke-Pester` | Demonstrates the fix did not open the gate |
| 15 | Repo-relative feature-folder `.json` allows | PASS | `$DocumentationAllowCases`; re-run pass | `Invoke-Pester` | |
| 16 | Absolute forward-slash and backslash documentation spellings allow | PASS | same; both failed in fail-before, pass after | same | |
| 17 | Case-varied documentation prefix denies | PASS | `$CaseSensitivityCases` deny entry; re-run pass | same | Confirms `-cmatch` preserved `StartsWith` case semantics |
| 18 | Case-varied checkpoint literal allows | PASS | `$CaseSensitivityCases` allow entry; failed in fail-before, pass after | same | Confirms `-match` preserved `-contains` case semantics |
| 19 | Codex `apply_patch` idempotence both directions | PASS | Two `Get-GateDecisionForCommand` cases (deny `.ps1` marker, allow checkpoint marker); both passed in fail-before too, which is what makes them an idempotence proof | `Invoke-Pester` | |
| 20 | Only the two predicate bodies changed per copy | PASS | Raw diff inspection by this reviewer: exactly two hunks per copy, both inside the two named functions; extension regex line byte-unchanged | `git diff fb3e1f33..b50f4e28 -- <each copy>` | Corroborated by `evidence/qa-gates/scope-confinement.2026-08-23T23-25.md` |
| 21 | Schema-contract and transport suites unmodified and passing | PASS | Absent from diff; reviewer re-run pass | `git diff --name-status`; `Invoke-Pester` | |
| 22 | No-Python-invocation suite unmodified and passing | PASS | Absent from diff; re-run pass (27 cases within 176/176) | same | |
| 23 | All four copies modified with segment-anchored predicates | PASS | `git diff --name-status` lists all four as M; predicates confirmed in each diff | `git diff --name-status fb3e1f33..b50f4e28` | |
| 24 | Claude pair SHA256-identical | PASS | Both `658c50a98fb14ea06cc6705a384cf46ece11a5793de0e8e854cdf18c34fe6207` (reviewer-computed) | `sha256sum` over both Claude copies | |
| 25 | Codex pair SHA256-identical and landed in a single commit | PASS | Both `98dc6917ae5ae3239dbe89c31391960d260ab74b83a51d93fa9d575aa16dbabd` (reviewer-computed); commit inspection shows `9c12d20a` touched docs only and `b50f4e28` carried both Codex copies together — no intermediate commit holds a split state | `sha256sum`; `git show --name-only --format="" 9c12d20a b50f4e28` | Previously unticked (single-commit half was orchestrator-side); now independently confirmed and ticked by this reviewer |
| 26 | Push-down parity pytest exits 0 | PASS | Reviewer re-run: 10 passed, exit 0, after removal of a gitignored reviewer-session state file that postdates the executor evidence (Info finding in the code review) | `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` | Executor evidence: `evidence/qa-gates/final-pytest-pushdown-parity.2026-08-23T23-25.md` |
| 27 | Format clean on final pass | PASS | `evidence/qa-gates/final-poshqc-format.2026-08-23T23-25.md`: EXIT_CODE 0, zero rewrites, SHA256-confirmed | evidence inspection | MCP format tool not re-runnable in this review environment; evidence is internally consistent and the analyzer/test re-runs corroborate a clean tree |
| 28 | Analyze zero findings across the six files | PASS | Executor evidence plus reviewer's independent `Invoke-ScriptAnalyzer` per file: 0 findings on all six | `Invoke-ScriptAnalyzer -Path <file> -Settings <PoshQC settings>` | |
| 29 | Full test run zero failed, zero errored | PASS | Executor: 3476/0/0 (`final-poshqc-test...md`); reviewer targeted re-runs 68/68 and 176/176 | `Invoke-Pester` | |
| 30 | Single ordered format→analyze→test pass | PASS | `evidence/qa-gates/final-clean-pass.2026-08-23T23-25.md` records the confirmed pass and the two abandoned earlier attempts with causes | evidence inspection | |
| 31 | Per-file line coverage >= 85% both canonical copies | PASS | 90.09% (100/111) and 99.19% (122/123), independently re-parsed by this reviewer from `artifacts/pester/powershell-coverage.xml`; values match the evidence artifact exactly | Python XML parse of the JaCoCo counters | Aggregate 96.17% |
| 32 | Changed-line coverage with no regression | PASS | 4 instrumented changed lines per canonical copy, all `ci > 0`; per-file percentages rose (+0.09 pp, +0.01 pp); missed-line counts unchanged | `evidence/qa-gates/coverage-delta.2026-08-23T23-25.md` + reviewer XML parse | |
| 33 | Branch-coverage exemption recorded and reviewer-confirmed; no denominator removal; no exclusion list | PASS | Reviewer confirmation (this audit is the named actor's action): the spec records the exemption in Test Strategy → Coverage; `.claude/rules/quality-tiers.md` grounds it (Pester measures no branch coverage); `pester.runsettings.psd1` uses an inclusion list (`CodeCoverage.Path`) registering both canonical copies at lines 131 and 198 and carries no exclusion list; both copies appear with counters in the coverage XML this reviewer parsed | `grep -n "preimplementation-gate" scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; XML parse | Previously unticked pending reviewer action; now confirmed and ticked by this reviewer |
| 34 | Fail-before capture with the predicted failure signature | PASS | Exit 38; exactly the 19 positive-half cases failed per suite; every deny and relative-regression case passed in the same run | `evidence/regression-testing/fail-before-new-suites.2026-08-23T23-25.md` | Failure-set composition matches the defect signature exactly |
| 35 | Pass-after capture with every case passing | PASS | Exit 0, all 68 named and passing; identical command and scan set; reviewer re-run reproduces | `evidence/regression-testing/pass-after-new-suites.2026-08-23T23-25.md`; `Invoke-Pester` | |
| 36 | Explicit not-ready `-CheckpointRaw` on every case; no vacuous pass | PASS | Structural verification against the suite text by this reviewer: every `It` routes through helpers that pass `-CheckpointRaw (ConvertTo-NotReadyCheckpointRaw)` unconditionally (one call site in the Claude suite, two in the Codex suite); no direct call to the decision function exists in any case body | full-file read of both suites | Verified against suite text as the caller required, not accepted from the executor's assertion |
| 37 | Both new suites exist, under 500 lines | PASS | 223 and 242 lines, reviewer-measured | `wc -l` | |
| 38 | All absolute path strings are literal constants | PASS | Full-file read: three synthetic prefix constants per suite, all bare string literals; the single `Resolve-Path`/`$PSScriptRoot` use per suite is the `BeforeAll` hook-locating line, which constructs no asserted test path; zero `Get-Location`, `$PWD`, or git-derived paths | full-file read; corroborated by `evidence/qa-gates/synthetic-path-constant-audit.2026-08-23T23-25.md` | |
| 39 | Changed-path union confined to the seven declared paths plus this feature folder | PASS | `git diff --name-status fb3e1f33..b50f4e28`: the six PowerShell paths plus feature-folder documents only; nothing under `.claude/rules/` or `.github/instructions/`, no `quality-tiers.yml` | `git diff --name-status fb3e1f33..b50f4e28` | Corroborated by `evidence/qa-gates/file-set-discipline.2026-08-23T23-25.md` |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 39 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. In a follow-up change, extend the `Test-FeatureDocumentationOrEvidencePath` comment (all four copies) to record the nested-segment widening, mirroring the checkpoint predicate's documented-widening pattern, and align the spec's backward-compatibility sentence (Minor finding in `code-review.2026-08-24T17-22.md`; zero measured exposure today).
2. On the next orchestration bootstrap, confirm live that a `Write` of the checkpoint through its absolute path is allowed with no checkpoint present (the spec's own post-fix monitoring step).

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- Criteria evaluated as **PASS** may be checked off in the authoritative source file if not already checked.
- No criterion was evaluated PARTIAL, FAIL, or UNVERIFIED.

Newly checked off by this reviewer (both independently confirmed before ticking):
- The four-copy-parity criterion requiring identical Codex-pair SHA256 values **and** a single-commit landing — confirmed by reviewer-computed hashes and by commit-content inspection showing no split state in any commit.
- The coverage criterion requiring the reviewer to confirm the PowerShell branch-coverage exemption and the intact coverage denominator — confirmed against `.claude/rules/quality-tiers.md`, the runsettings inclusion list, and the coverage XML.

### AC Status Summary

- Source: `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516/spec.md`
- Total AC items: 39
- Checked off (delivered): 39
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 39 | 39 | 0 | Checkbox-backed; 37 pre-ticked by the executor/orchestrator, 2 ticked by this reviewer after independent confirmation |
