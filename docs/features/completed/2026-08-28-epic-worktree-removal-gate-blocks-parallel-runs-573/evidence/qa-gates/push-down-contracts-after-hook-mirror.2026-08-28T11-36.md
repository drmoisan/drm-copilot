# Bundle-Parity Contract After the Hook Mirror (P3-T2)

Timestamp: 2026-08-28T11-36

Task: [P3-T2]
Issue: #573
Acceptance criterion supported: AC-14
Branch: `bug/epic-worktree-removal-gate-blocks-parallel-runs-573-r2`

Command: `poetry run pytest tests/scripts/dev_tools/ -k test_bundled_claude_payload_contains_all_repo_runtime_contracts -q`

EXIT_CODE: 0

## Two readings, per the procedure fixed in the [P0-T5] baseline

| Reading | `.claude/state/` contents | Exit | Result |
| --- | --- | --- | --- |
| As found | `powershell-batch-budget.default.json` | 1 | `1 failed` naming `.claude/state/powershell-batch-budget.default.json` |
| After emptying `.claude/state/` | empty | 0 | `1 passed, 4111 deselected in 0.75s` |

The `EXIT_CODE: 0` recorded above is the second reading, which is the one that answers the question this task asks — whether the hook mirror is correct.

## Attribution of the as-found failure

The as-found failure text is byte-comparable with the [P0-T5] baseline: same test, same assertion at `tests\scripts\dev_tools\test_push_down_claude_resource_contracts.py:120`, same failure mode, and a named path of the form `.claude/state/<name>-batch-budget.default.json`. That path is gitignored (`.gitignore:68:.claude/state/`) and untracked, and it is regenerated by the PowerShell change-budget `PreToolUse` hook on any `.ps1` write, which is why it reappeared between [P0-T5] and this task.

**The failure text names no in-scope path.** None of the seven paths listed under the plan's "Scope — exactly seven files" appears in it. Per the task's attribution rule the failure is therefore attributed to the [P0-T5] baseline and to issue #510, and is not caused by this change. No re-copy was required.

## Positive confirmation for the hook mirror

The second reading is decisive rather than merely suggestive. The test asserts UTF-8 text equality over the whole `.claude/**` tree with only `.claude/settings.local.json` and `.claude/agent-memory/**` exempt. With the gitignored state file absent it reports `1 passed`, which means the mirrored hook copied in [P3-T1] is byte-equal to its repository original as the contract requires. This corroborates the direct `Get-FileHash` equality recorded in [P3-T1] by an independent mechanism.

Output Summary: `1 passed` on the reading that isolates the change (`1 passed, 4111 deselected in 0.75s`, exit 0), confirming the [P3-T1] hook mirror satisfies the bundle-parity contract. The as-found reading was `1 failed` naming `.claude/state/powershell-batch-budget.default.json`; that text matches the [P0-T5] issue #510 attribution baseline, the named path is gitignored and untracked, and the failure names no in-scope path, so it is not caused by this change and no re-copy was needed.
