# Remediation Plan — Issue #462, Cycle 1 (2026-08-10T21-03)

- **Issue:** #462
- **Work Mode:** full-feature (remediation cycle 1)
- **Preflight revision applied:** 1 (2026-08-10, in place at this same path)
- **Branch:** `drm-copilot-wt-2026-08-10T09-25`, baseline head `f7711fb4` (PR #463 open against `main`, currently green)
- **Remediation inputs:** `docs/features/active/2026-08-10-parallel-surface-destination-portability-bash-462/remediation-inputs.2026-08-10T21-03.md`
- **Scope:** RI-1 through RI-4 only. RI-5 is complete (issues #464 and #465 opened by the orchestrator) and carries no plan task. No scope beyond the four findings; no parallel-surface schema field, enum member, or validator invariant may change (AC17).
- **Feature folder:** `docs/features/active/2026-08-10-parallel-surface-destination-portability-bash-462` (referred to below as `<FEATURE>`)
- **Evidence locations:** all evidence artifacts resolve to `<FEATURE>/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. `<ts>` in artifact names is the actual capture timestamp in `yyyy-MM-ddTHH-mm` form.

## Binding Environment Constraints (verified facts)

1. **Bash cannot be fully verified locally.** `bats` and `kcov` are unavailable in this environment. Verification of shell changes is `gh workflow run _shell-coverage.yml --ref drm-copilot-wt-2026-08-10T09-25`, polled to completion, with the run URL and the printed `Bash coverage (lines): NN.N%` line recorded under `<FEATURE>/evidence/qa-gates/`.
2. **Local `shellcheck` 0.11.0 and `shfmt` are available** and may be used as a fast pre-check, but local shellcheck does not reproduce the CI SC2015 finding (verified: zero findings on the current files even with the suppressions removed, at `-S style`), because CI installs the older apt-packaged shellcheck. A clean local shellcheck run is NOT sufficient evidence for RI-3.
3. **`poetry run python -c` with a multi-line string silently produces no output and exits 0 here.** Use a single-line `-c` form only. A command returning no output has not run.
4. **`python -m scripts.dev_tools.validate_orchestrator_state` exits 0 without validating.** It must never be used as evidence.
5. **npm root scripts do not reach `extensions/**`.** Use `npm --prefix extensions/drm-copilot run <script>` if an extension script is ever needed; the extension script is `test:coverage`, not `test:unit:coverage`. (No task in this plan requires an npm lane; no TypeScript, Python, or PowerShell production file changes in this cycle.)

## Lanes Touched

This cycle changes: shell (`scripts/bash/shell_qc_lib.sh`, `scripts/bash/cleanup_worktrees_lib.sh`), JSON (`.claude/settings.json` and its bundled mirror), Markdown (`spec.md`, two agent definitions and their bundled mirrors, one evidence artifact), and `.gitignore`. The branch is already green at `f7711fb4`, so the baseline is that CI run rather than a fresh full-toolchain capture; Phase 0 captures only the shell, JSON, and Markdown lanes. No TypeScript, Python, PowerShell, or workflow-YAML production file changes, so no npm or Pester lane is required and no Python coverage baseline applies. A pytest contract-test lane IS required as the executable verification gate for RI-2: two existing Python contract tests read the RI-2 target files at run time — `tests/scripts/dev_tools/test_parallel_orchestrator_permission_contracts.py` parses the `Bash(...)` grants from `.claude/agents/parallel-orchestrator.md` frontmatter and asserts that every command invocation prescribed in `.claude/skills/parallel-orchestrate/SKILL.md` is covered by a grant, and `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` asserts byte-identical text parity for every repo `.claude/**` file against the bundle. P5-T4 executes this gate.

### Phase 0 — Policy reads, environment constraints, and remediation baseline

- [x] [P0-T1] Read the policy files in order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/shell.md`, `.claude/rules/tonality.md` — and re-read `<FEATURE>/remediation-inputs.2026-08-10T21-03.md` in full. Write `<FEATURE>/evidence/remediation-baseline/phase0-instructions-read.<ts>.md` containing `Timestamp:`, `Policy Order:`, the explicit list of files read, and a verbatim restatement of the five Binding Environment Constraints above. Acceptance: the artifact exists with all required fields and lists all six files.
- [x] [P0-T2] Capture the shell-lane baseline from the existing green CI run at head `f7711fb4`. Command: `gh run list --commit f7711fb42b3d3351b1a89871859fbf6b33ffede1 --json databaseId,name,conclusion,url` — the full 40-character SHA is required; the abbreviated form returns `[]` with exit 0 and locates nothing. The target run is `31414584576` (`Shell Coverage (reusable)`, `workflow_dispatch`, conclusion `success`). Then `gh run view 31414584576 --log | grep -E "Bash coverage \(lines\): [0-9]"` to extract the numeric baseline coverage. The digit-anchored pattern is mandatory: the unanchored grep also matches three `shell-qc.sh` usage-banner lines whose text literally contains the placeholder `"Bash coverage (lines): NN.N%" summary.`, and recording `NN.N%` is exactly the outcome this acceptance forbids. The true baseline value is 92.4%. Write `<FEATURE>/evidence/remediation-baseline/shell-ci-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including the run URL, conclusion `success`, and the numeric `Bash coverage (lines): NN.N%` value (expected 92.4%). Acceptance: the artifact records a green run at `f7711fb42b3d3351b1a89871859fbf6b33ffede1` and a numeric coverage percentage; the literal placeholder text `NN.N%` from the usage banner is not a valid value.
- [x] [P0-T3] Capture the local shell pre-check baseline on the two RI-3 target files. Commands: `shellcheck --version`, then `shellcheck -S style scripts/bash/shell_qc_lib.sh scripts/bash/cleanup_worktrees_lib.sh`, then `shfmt -d scripts/bash/shell_qc_lib.sh scripts/bash/cleanup_worktrees_lib.sh`. Write `<FEATURE>/evidence/remediation-baseline/shell-local-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command, recording the local shellcheck version (expected 0.11.0) and the explicit caveat that local shellcheck does not reproduce the CI SC2015 finding and is not sufficient RI-3 evidence (Constraint 2). Acceptance: the artifact exists with all fields and the caveat.
- [x] [P0-T4] Capture the JSON/Markdown-lane baseline. Commands: `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8')); console.log('repo settings.json valid')"`; the same for `extensions/drm-copilot/resources/claude-customizations/.claude/settings.json`; then `pwsh -NoProfile -Command "(Get-FileHash .claude/settings.json).Hash; (Get-FileHash extensions/drm-copilot/resources/claude-customizations/.claude/settings.json).Hash; (Get-FileHash .claude/agents/parallel-planner.md).Hash; (Get-FileHash extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-planner.md).Hash; (Get-FileHash .claude/agents/parallel-orchestrator.md).Hash; (Get-FileHash extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md).Hash"`. Write `<FEATURE>/evidence/remediation-baseline/json-markdown-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command, recording that all three repo/bundle pairs hash-match at baseline (verified at planning), and the documented absence of a configured Markdown lint lane in this repository. Acceptance: the artifact exists, both JSON parses succeed, and the three baseline pair-hash equalities are recorded.

### Phase 1 — RI-4: anchor the `.gitignore` build-artifact rule

Sequenced first so no later task's added file can be silently un-committed by the unanchored rule.

- [x] [P1-T1] Edit `.gitignore` (line references are pre-edit): change line 20 `lib/` to `/lib/`; change line 38 `lib64/` to `/lib64/`; delete the negation lines 22-25 (`!extensions/drm-copilot/src/lib/`, `!extensions/drm-copilot/src/lib/**`, `!extensions/drm-copilot/test/lib/`, `!extensions/drm-copilot/test/lib/**`), 29-32 (`!.claude/lib/`, `!.claude/lib/**`, `!extensions/drm-copilot/resources/claude-customizations/.claude/lib/`, `!extensions/drm-copilot/resources/claude-customizations/.claude/lib/**`), and 36-37 (`!tests/fixtures/shell_qc/.claude/lib/`, `!tests/fixtures/shell_qc/.claude/lib/**`), together with the explanatory comment lines 21, 26-27, and 33-35 that exist only to justify those negations. Preserve line 28 (`.claude/worktrees`) and every other line unchanged. Acceptance: `git diff -- .gitignore` shows exactly the two anchoring edits and the deletions listed; `grep -n "^!" .gitignore` reports zero remaining negation lines referencing `lib`.
- [x] [P1-T2] Verify the anchoring change. Commands: (a) `git check-ignore -v extensions/drm-copilot/src/lib extensions/drm-copilot/test/lib .claude/lib extensions/drm-copilot/resources/claude-customizations/.claude/lib tests/fixtures/shell_qc/.claude/lib/bash/lib_entry.sh` — expected output empty and `EXIT_CODE: 1` (git check-ignore exits 1 when no path is ignored; that is the pass condition here); (b) positive control `git check-ignore -v lib/placeholder.txt` — expected to report the `/lib/` rule with exit 0; (c) `git status --porcelain` — expected to show no newly untracked files relative to the pre-change state (only the `.gitignore` modification and this cycle's own edits). Write `<FEATURE>/evidence/qa-gates/gitignore-anchoring.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command. Note: checks (a) and (c) are non-discriminating — both already produce these expected results before the P1-T1 edit, so this task is a regression guard, not proof of the anchoring; the discriminating check for RI-4 is P1-T1's `git diff` acceptance. Acceptance: all five named paths are unignored, the positive control matches `/lib/`, and no new untracked files appear.

### Phase 2 — RI-1: remove the unwritable sixth test scenario from the spec

- [x] [P2-T1] Edit `<FEATURE>/spec.md`, section `## Seeded Test Conditions (from potential)`, first bullet (pre-edit lines 400-401): delete exactly the words `, and generation handling` and change the checkbox from `- [ ]` to `- [x]`, yielding a checked bullet ending `...fully connected items, deterministic tie-breaking.`. Add no test and change no other spec content. `user-story.md` was verified at planning time to not contain this bullet (repo grep for `generation handling` matches only `spec.md`, `issue.md`'s historical pre-promotion seed, and remediation/audit artifacts, which are historical records and remain unchanged). Decision of record, confirmed at preflight: `issue.md` also contains the "generation handling" phrase, but it is outside RI-1's scope and `issue.md` is not an acceptance-criteria source under `full-feature` mode; leaving it unchanged is deliberate and is not an oversight. Acceptance: `grep -n "generation handling" <FEATURE>/spec.md` returns zero matches; the first seeded-condition bullet is checked; the executor re-runs the grep against `<FEATURE>/user-story.md` and records the empty result in the P5-T2 artifact.

### Phase 3 — RI-2: narrow the Bash allowlist grant to the three entry points

The replacement set for every location is exactly these three entries, replacing the single entry `Bash(bash .claude/lib/bash/*)`:

```
Bash(bash .claude/lib/bash/compute-cohorts.sh*)
Bash(bash .claude/lib/bash/compute-concurrency-batches.sh*)
Bash(bash .claude/lib/bash/validate-parallel-manifest.sh*)
```

- [x] [P3-T1] Edit `.claude/settings.json` line 8: replace the single `"Bash(bash .claude/lib/bash/*)"` allow entry with the three JSON string entries above, in that order, preserving JSON validity and surrounding entries. Acceptance: `node -e "const s=JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8')); const a=s.permissions.allow; console.log(a.filter(x=>x.includes('lib/bash')).join('|'))"` prints exactly the three new entries and not the old pattern.
- [x] [P3-T2] Edit `.claude/agents/parallel-planner.md`: in the `tools:` frontmatter list (pre-edit line 17), replace the single entry with the three entries above as three list items; in the prose at pre-edit line 154, rewrite the sentence referencing the `"Bash(bash .claude/lib/bash/*)"` allowlist entry to name the three entry-point-specific entries (plural) instead; and at pre-edit line 161 — `**Manifest validation — bash entry point.** The same allowlist entry covers:` — rewrite the sentence to name the `Bash(bash .claude/lib/bash/validate-parallel-manifest.sh*)` entry specifically, because after the narrowing `validate-parallel-manifest.sh` has its own distinct entry and "the same allowlist entry" is false. Acceptance: `grep -n "lib/bash" .claude/agents/parallel-planner.md` shows only the three new patterns (plus any entry-point invocation examples that already name specific files) and zero occurrences of `lib/bash/*)`; and `grep -n "The same allowlist entry" .claude/agents/parallel-planner.md` returns zero matches.
- [x] [P3-T3] Edit `.claude/agents/parallel-orchestrator.md`: same treatment at the `tools:` frontmatter (pre-edit line 18) and the prose at pre-edit line 72 (`grants ... for it:` sentence rewritten to the three entries). Acceptance: `grep -n "lib/bash" .claude/agents/parallel-orchestrator.md` shows only the three new patterns plus file-specific invocation references, and zero occurrences of `lib/bash/*)`.
- [x] [P3-T4] Apply byte-identical edits to the three bundled mirrors: `extensions/drm-copilot/resources/claude-customizations/.claude/settings.json` (line 8), `.../claude-customizations/.claude/agents/parallel-planner.md` (lines 17, 154, and 161 — including the P3-T2 rewrite of the `The same allowlist entry` sentence), `.../claude-customizations/.claude/agents/parallel-orchestrator.md` (lines 18 and 72). All three repo/bundle pairs were byte-identical at baseline (P0-T4), so full-file hash equality is the mirror contract. Acceptance: `pwsh -NoProfile -Command` comparing `Get-FileHash` for each of the three repo/bundle pairs reports equality for all three pairs; and `grep -n "The same allowlist entry" extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-planner.md` returns zero matches.
- [x] [P3-T5] Update the disclosure artifact `<FEATURE>/evidence/other/permission-surface-callout.2026-08-10T17-08.md` to describe the delivered grant: (a) replace the fenced grant block with the three entries; (b) rewrite the "Scope of the pattern, stated precisely" paragraph: each entry is a prefix wildcard on a specific filename, so it permits trailing arguments — that is the intent — but no longer matches nested subdirectories or relative-traversal paths, and the six sourceable libraries are not directly invocable under the grant; (c) rewrite the section "Available narrowing (recommended follow-up, not applied here)" to state the narrowing is now applied (remediation cycle 1, per RI-2 of `remediation-inputs.2026-08-10T21-03.md`); (d) add an `Amended: <ts> (remediation cycle 1, RI-2)` line under the original `Timestamp:` line, preserving the rest of the historical record including the earlier-revision correction note. Acceptance: the artifact contains the three-entry block, the amended-scope paragraph, the applied-narrowing statement, and the amendment line; it contains no claim that the narrowing is "not applied".
- [x] [P3-T6] Re-verify the permission-grant consumers and sweep for the old string. Commands: (a) parse each repo agent frontmatter with a single-line invocation (Constraint 3), e.g. `poetry run python -c "import re,yaml; t=open('.claude/agents/parallel-planner.md',encoding='utf-8').read(); m=re.match(r'^---\r?\n(.*?)\r?\n---',t,re.S); d=yaml.safe_load(m.group(1)); print(sorted(x for x in d['tools'] if 'lib/bash' in x))"` and the same for `parallel-orchestrator.md` — each must print exactly the three new entries; (b) re-run the P3-T1 node assertion on both `settings.json` copies; (c) repo-wide sweep `git grep -n "bash .claude/lib/bash/\*)"` — expected matches only under `docs/features/**` historical artifacts (plan, audits, reviews, research, remediation-inputs, prior evidence), and zero matches under `.claude/`, `extensions/`, `tests/`, `scripts/`, or `src/` (planning-time search confirmed no test or doc outside the feature folder pins the old string; the executor re-confirms and records the match list). Write `<FEATURE>/evidence/qa-gates/allowlist-narrowing-verification.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command. Acceptance: both frontmatters parse and list exactly the three entries, both JSON copies parse with the three entries and without the old one, and the sweep shows zero production-surface occurrences of the old pattern.

### Phase 4 — RI-3: remove the file-wide SC2015 suppressions and rewrite the eight sites

- [x] [P4-T1] Edit `scripts/bash/shell_qc_lib.sh`: delete the file-wide `# shellcheck disable=SC2015` directive at pre-edit line 13 together with its justification comment block (the adjacent comment lines claiming `.claude/rules/shell.md` mandates the idiom); rewrite the seven sites at pre-edit lines 92, 115, 191, 197, 247, 347, 356 from the `<test> && <action> || true` form to `if <test>; then <action>; fi` (e.g. `[[ -d $root ]] && roots+=("$root") || true` becomes `if [[ -d $root ]]; then roots+=("$root"); fi`; `((rc > exit_code)) && exit_code=$rc || true` becomes `if ((rc > exit_code)); then exit_code=$rc; fi`), preserving each site's existing intent comment. Add no replacement suppression of any scope. Do NOT touch the two non-SC2015 `|| true` occurrences at pre-edit lines 263 (`match=$(grep -oE ... ) || true`) and 361 (`cp -f ... || true`): they are the `|| rc=$?`-family exit-code capture that `.claude/rules/shell.md` mandates, they are load-bearing under `set -e`, and removing them breaks the scripts — both lines retain their `|| true` unmodified. If shfmt expands the single-line `if` form, accept its output; note that local shfmt 3.12.0 was probed at preflight against the exact rewritten `if x; then y; fi` forms and does not expand them, while CI installs shfmt 3.8.0 — if 3.8.0 differs, `shell-qc check` fails at P5-T3 and the executor iterates. Acceptance: `grep -n "disable=SC2015" scripts/bash/shell_qc_lib.sh` returns zero matches; `grep -nE '&&.*\|\| true' scripts/bash/shell_qc_lib.sh` returns zero matches (this pattern targets only the `<test> && <action> || true` SC2015 form and deliberately does not match the retained capture sites at pre-edit lines 263 and 361); the file remains under 500 lines.
- [x] [P4-T2] Edit `scripts/bash/cleanup_worktrees_lib.sh`: delete the file-wide `# shellcheck disable=SC2015` directive at pre-edit line 16 together with its justification comment block; rewrite the site at pre-edit line 479 from `((crc > rc)) && rc=$crc || true` to `if ((crc > rc)); then rc=$crc; fi`, preserving the intent comment. Add no replacement suppression. Acceptance: `grep -n "disable=SC2015" scripts/bash/cleanup_worktrees_lib.sh` returns zero matches; the rewritten site uses the `if` form; the file remains under 500 lines.
- [x] [P4-T3] Run the local fast pre-check on both files. Commands: `shfmt -d scripts/bash/shell_qc_lib.sh scripts/bash/cleanup_worktrees_lib.sh` (must exit 0 with no diff; if shfmt proposes a rewrite, apply it with `shfmt -w` and re-run until clean) and `shellcheck -S style scripts/bash/shell_qc_lib.sh scripts/bash/cleanup_worktrees_lib.sh` (must exit 0). Write `<FEATURE>/evidence/qa-gates/shell-local-precheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command, and the explicit statement: this clean local run is a pre-check only and is NOT sufficient evidence for RI-3, because local shellcheck 0.11.0 does not emit the CI finding even on the unfixed code (Constraint 2); the authoritative RI-3 verification is the P5-T3 CI dispatch, whose apt-packaged shellcheck flagged the original sites. Acceptance: both commands exit 0 and the artifact carries the insufficiency statement.

### Phase 5 — Final QA, acceptance-criteria re-check, and terminal CI dispatch

- [x] [P5-T1] Final JSON/Markdown lane check: re-run the two `node -e "JSON.parse(...)"` validity commands from P0-T4 against the post-change `settings.json` copies, and re-run the three repo/bundle pair-hash comparison. Write `<FEATURE>/evidence/qa-gates/json-markdown-final.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` per command, and record the documented absence of a configured Markdown toolchain lane (Markdown edits verified by the content acceptance in P2-T1, P3-T2, P3-T3, and P3-T5). Acceptance: both JSON parses succeed and all three pairs hash-match post-change.
- [x] [P5-T2] Acceptance-criteria re-check: confirm each of the 17 acceptance criteria in `<FEATURE>/spec.md` and each of the 17 in `<FEATURE>/user-story.md` remains satisfied after RI-1 through RI-4, citing existing or updated evidence per criterion. Specifically record: the RI-1 edit is reflected (spec seeded-condition bullet now describes five scenarios and is checked; `grep -n "generation handling"` empty in both `spec.md` and `user-story.md`); AC15's evidence is now the three-entry grant plus the amended `permission-surface-callout` artifact; AC17 (no schema/enum/validator change) is unaffected — no file under `scripts/dev_tools/` or `extensions/drm-copilot/src/lib/validate/` is touched by this cycle (confirm via `git diff --name-only f7711fb4..HEAD`); AC13's coverage evidence is superseded by the P5-T3 run. Write `<FEATURE>/evidence/qa-gates/acceptance-criteria-recheck.<ts>.md` with `Timestamp:`, the per-criterion table for both documents, and `Output Summary:`. Acceptance: all 34 criteria (17 + 17) are individually confirmed with named evidence; zero regressions.
- [ ] [P5-T3] Terminal shell CI dispatch (single batched run verifying the RI-3 rewrites and accompanying the RI-2 mirror changes; one dispatch for the whole cycle). Steps: (a) confirm all RI-1 through RI-4 production changes are committed and record `git rev-parse HEAD`; (b) `gh workflow run _shell-coverage.yml --ref drm-copilot-wt-2026-08-10T09-25`; (c) poll to completion with `gh run watch <id>` or repeated `gh run view <id> --json status,conclusion`; (d) capture `gh run view <id> --json url,conclusion,headSha,jobs` and `gh run view <id> --log | grep -E "Bash coverage \(lines\): [0-9]"` — the digit-anchored pattern is mandatory for the same reason as in P0-T2: the unanchored grep matches the `shell-qc.sh` usage banner's literal `NN.N%` placeholder text. Note: `shell-qc.sh test --coverage` prints the coverage value but enforces no threshold, and the workflow has no automated coverage floor — a low-coverage run still concludes `success`. The `>= 85.0` and no-regression conditions below are therefore manual executor gates: read the printed number and compare it against the P0-T2 baseline (92.4%); do not treat the run conclusion alone as satisfying them. Write `<FEATURE>/evidence/qa-gates/shell-coverage-dispatch.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording: the run URL; conclusion `success`; both verification steps green — `Run shell-qc check (shfmt diff + shellcheck)` and `Run shell-qc test with coverage`; the numeric `Bash coverage (lines): NN.N%` value with `NN.N >= 85.0` and no regression against the P0-T2 baseline value; and `headSha` equal to the `git rev-parse HEAD` recorded in step (a). Only evidence/documentation commits may follow this dispatch on the branch; if any production file changes after it, the dispatch must be repeated at the new head. Acceptance: green run at the post-change head containing all RI-1 through RI-4 production changes, both steps green, numeric coverage >= 85% recorded, headSha equality recorded. `SKIPPED` is not a valid outcome for this task.
- [ ] [P5-T4] Execute the RI-2 pytest verification gate. Two existing Python contract tests read the RI-2 target files at run time (see Lanes Touched) and constitute the executable verification for the allowlist narrowing. Preflight verified against the live repository that the narrowed grants keep the permission seam satisfied (`NARROWED uncovered=0`), so this is a required gate that is expected to pass — it is a missing verification gate being added, not an expected-failure task, and carries no `[expect-fail]` tag. Command: `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_permission_contracts.py tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`. Write `<FEATURE>/evidence/qa-gates/ri2-pytest-gate.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). If any test fails, remediate the RI-2 edits, re-run this gate, and — because a fix would change production files after the P5-T3 dispatch — repeat P5-T3 at the new head. Acceptance: the pytest command exits 0 with all tests passed and the artifact records the counts. `SKIPPED` is not a valid outcome for this task.

## Traceability

| Finding | Satisfied by | Verification evidence |
| --- | --- | --- |
| RI-1 (spec sixth-scenario removal) | P2-T1 | P2-T1 grep acceptance; P5-T2 re-check artifact |
| RI-2 (allowlist narrowing, six locations + disclosure artifact) | P3-T1, P3-T2, P3-T3, P3-T4, P3-T5 | P3-T6 consumer re-verification and old-string sweep; P5-T1 JSON/parity final check; P5-T4 pytest contract-test gate (executable RI-2 verification); batched into the P5-T3 dispatch head |
| RI-3 (SC2015 suppression removal, eight-site rewrite) | P4-T1, P4-T2 | P4-T3 local pre-check (explicitly insufficient alone); P5-T3 CI dispatch (authoritative) |
| RI-4 (`.gitignore` anchoring) | P1-T1 | P1-T2 check-ignore and status verification |
| RI-5 (file two defect issues) | No plan task — complete; issues #464 and #465 opened by the orchestrator | `remediation-inputs.2026-08-10T21-03.md` RI-5 |

## Notes

- The re-audit the user requested after the RI-2 narrowing is the orchestrator's post-execution feature-review pass for this remediation cycle, not a plan task.
- The 35 SC2015 findings fixed properly in the new `.claude/lib/bash/**` files during the original execution are out of scope; only the two pre-existing `scripts/bash/` files carry the RI-3 work. No `.github/workflows/**` file changes in this cycle, so `modified-workflow-needs-green-run` is not triggered; the P5-T3 dispatch is required by the shell verification constraint, not by that rule.
- All pre-edit line numbers in task text are from the remediation inputs and were spot-verified against the working tree at planning time (suppression directives at `shell_qc_lib.sh:13` and `cleanup_worktrees_lib.sh:16`; allowlist entries at the six named locations; `.gitignore` structure at lines 20-38).
