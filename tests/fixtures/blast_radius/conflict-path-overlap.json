{
  "description": "path_overlap in isolation: two identical concrete paths overlap by equality while the module and contract levels are disjoint, so exactly one reason is reported.",
  "input": {
    "radius_a": {
      "paths": ["scripts/dev_tools/shared.py"],
      "modules": ["alpha"],
      "shared_surfaces": [],
      "contracts": ["Alpha"],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "radius_b": {
      "paths": ["scripts/dev_tools/shared.py"],
      "modules": ["beta"],
      "shared_surfaces": [],
      "contracts": ["Beta"],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "config": {
      "version": 1,
      "shared_surfaces": ["config/orchestration-routing.json", "poetry.lock"],
      "shared_surface_globs": ["scripts/dev_tools/validate_*.py"],
      "modules": {
        "config": ["config/**"],
        "docs": ["docs/**"],
        "python-dev-tools": ["scripts/dev_tools/**"],
        "tests": ["tests/**"]
      },
      "over_breadth_fraction": 0.25
    }
  },
  "expected": {
    "conflict": true,
    "reasons": [
      {
        "kind": "path_overlap",
        "detail": "scripts/dev_tools/shared.py ~ scripts/dev_tools/shared.py"
      }
    ]
  }
}
