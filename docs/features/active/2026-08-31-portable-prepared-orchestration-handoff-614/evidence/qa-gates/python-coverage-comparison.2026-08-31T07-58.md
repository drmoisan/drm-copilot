Timestamp: 2026-08-31T16:46:58-04:00
Command: `$baselineText = Get-Content -LiteralPath 'docs/features/active/2026-08-31-portable-prepared-orchestration-handoff-614/evidence/baseline/python-pytest-coverage.2026-08-31T07-58.md' -Raw; $postText = Get-Content -LiteralPath 'docs/features/active/2026-08-31-portable-prepared-orchestration-handoff-614/evidence/qa-gates/python-pytest-coverage.2026-08-31T07-58.md' -Raw; $baselineLine = [double]([regex]::Match($baselineText,'Repository line coverage(?: is|:)?\s*([0-9.]+)%').Groups[1].Value); $baselineBranch = [double]([regex]::Match($baselineText,'Repository branch coverage(?: is|:)?\s*([0-9.]+)%').Groups[1].Value); $postLine = [double]([regex]::Match($postText,'Repository line coverage(?: is|:)?\s*([0-9.]+)%').Groups[1].Value); $postBranch = [double]([regex]::Match($postText,'branch coverage(?: is|:)?\s*([0-9.]+)%').Groups[1].Value); $base = git merge-base origin/main HEAD; $changed = @{}; $current = $null; git diff --unified=0 --no-color $base -- '*.py' | ForEach-Object { if ($_ -match '^\+\+\+ b/(.+)$') { $current = $Matches[1].Replace('/','\').ToLowerInvariant(); if (-not $changed.ContainsKey($current)) { $changed[$current] = [System.Collections.Generic.HashSet[int]]::new() } } elseif ($current -and $_ -match '^@@ -\d+(?:,\d+)? \+(\d+)(?:,(\d+))? @@') { $start = [int]$Matches[1]; $count = if ($Matches[2]) { [int]$Matches[2] } else { 1 }; for ($offset = 0; $offset -lt $count; $offset++) { [void]$changed[$current].Add($start + $offset) } } }; $covered = 0; $total = 0; $source = $null; Get-Content -LiteralPath 'artifacts/python/lcov.info' | ForEach-Object { if ($_ -match '^SF:(.+)$') { $source = $Matches[1].Replace('/','\').ToLowerInvariant() } elseif ($source -and $_ -match '^DA:(\d+),(\d+)') { $line = [int]$Matches[1]; $hits = [int]$Matches[2]; if ($changed.ContainsKey($source) -and $changed[$source].Contains($line)) { $total++; if ($hits -gt 0) { $covered++ } } } }; $changedPercent = if ($total) { [math]::Round(100*$covered/$total,2) } else { 100 }; [pscustomobject]@{BaselineLine=$baselineLine;PostLine=$postLine;BaselineBranch=$baselineBranch;PostBranch=$postBranch;ChangedCovered=$covered;ChangedTotal=$total;ChangedPercent=$changedPercent;Base=$base} | ConvertTo-Json -Compress; if ($postLine -lt 85 -or $postBranch -lt 75 -or $changedPercent -lt 90 -or $postLine -lt $baselineLine -or $postBranch -lt $baselineBranch) { exit 1 }`
EXIT_CODE: 0
Output Summary: PASS. Baseline repository line coverage was 92.7087% and post-change line coverage is 92.86%, a +0.1513 percentage-point delta. Baseline repository branch coverage was 85.2994% and post-change branch coverage is 85.42%, a +0.1206 percentage-point delta. New/changed executable-line coverage relative to merge base `1432ff895c57113702db70deb2dbb092cefe0296` is 96.84% (552/570). Repository line coverage is at least 85%, repository branch coverage is at least 75%, new/changed-line coverage is at least 90%, and neither repository metric regressed.
