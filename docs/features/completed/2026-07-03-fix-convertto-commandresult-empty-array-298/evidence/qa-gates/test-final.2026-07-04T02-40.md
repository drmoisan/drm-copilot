Timestamp: 2026-07-04T02-40
Command: mcp__drm-copilot__run_poshqc_test (scan_folders: ["tests/scripts/dev-tools/Invoke-FullReleaseFlow.Tests.ps1", "tests/scripts/dev-tools/Invoke-FullReleaseFlow.AdditionalFailurePaths.Tests.ps1"], updated scripts/powershell/PoshQC/settings/pester.runsettings.psd1)
EXIT_CODE: 0
Output Summary:
- Test count (from the MCP tool's regenerated `artifacts/pester/pester-junit.xml`): aggregate `tests="26" errors="0" failures="0"` — 26 passed, 0 failed, matching the required total across every invocation in this cycle.
- Coverage anomaly observed and resolved: repeated `mcp__drm-copilot__run_poshqc_test` invocations after the P2-T1 edit to `pester.runsettings.psd1` continued to write a coverage report with only 15 `<class>` entries (missing `Invoke-FullReleaseFlow.ps1`), even though `(Select-String -Path scripts/powershell/PoshQC/settings/pester.runsettings.psd1 -Pattern "Invoke-FullReleaseFlow.ps1").Count` and a freshly-loaded `Import-PowerShellDataFile` both confirm the entry is present in the file on disk. This indicates the MCP tool's backing PowerShell session cached the `PoshQC` module/settings from the first `run_poshqc_test` call in this session (P0-T6), which predates the P2-T1 edit, and did not reload the updated settings file on subsequent calls within the same session.
- Cross-check: invoking the identical underlying function directly (`Import-Module ./scripts/powershell/PoshQC -Force; Invoke-PoshQCTest -Root <repo> -ScanFolders @(...)`) in a fresh PowerShell process reloads the updated settings and reports `Tests Passed: 26, Failed: 0` with a 16-file/16-class coverage report that includes `Invoke-FullReleaseFlow.ps1` (see `evidence/qa-gates/coverage-per-file-verification.<timestamp>.md` for the extracted counters). This confirms the settings-file edit is correct and effective; the discrepancy is a session-scoped module-caching staleness in the long-running MCP tool process, not a defect in the repository change.
- The coverage.xml currently on disk after this fresh-process run reflects `Invoke-FullReleaseFlow.ps1` per-file coverage; test pass/fail counts are identical (26/0) whether measured via the MCP tool or the direct fresh-process invocation.
- Numeric aggregate coverage (from the fresh-process-generated, current `artifacts/pester/powershell-coverage.xml`, top-level `<report>` counters): `LINE missed="1079" covered="90"` = 90 / (90 + 1079) * 100 = 7.70% aggregate line coverage across all 16 files in `CodeCoverage.Path`. No `BRANCH` counter type is emitted by this repo's JaCoCo output (only INSTRUCTION/LINE/METHOD/CLASS), consistent with the same limitation noted in the Phase 0 baseline (`evidence/remediation-baseline/test-baseline.<timestamp>.md`); a branch-coverage percentage is therefore not available from this artifact. The low aggregate percentage is expected: 15 of the 16 allowlisted files are hook/release scripts not exercised by this scoped two-file test run, so they contribute their full line count as "missed."
