# 2026-07-22-mcp-promotion-tooling-defects (Plan)

- **Issue:** #401
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-22T11-30
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug
- **Requirements source:** `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/spec.md` (AC-1..AC-14)
- **Research source:** `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/research/2026-07-22T10-20-mcp-promotion-tooling-defects-research.md`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Evidence location rule (non-overridable):** All evidence artifacts produced by this plan MUST be written under `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/evidence/<kind>/` where `<kind>` is one of `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`. `EVIDENCE:` below abbreviates that root. No `artifacts/` evidence path is permitted.

**Timestamp convention:** `<ts>` in artifact filenames denotes the ISO-8601 execution timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.

**Command working directories:** TypeScript toolchain commands run from `extensions/drm-copilot/`. Python toolchain commands run from the repository root via `poetry run`.

**Parity constraint (binding):** `extensions/drm-copilot/src/lib/potential-to-issue/promotion.ts` and `scripts/dev_tools/potential_to_issue.py` are byte-parity twins for decision branches, messages, constants, and emitted lines. The Defect B branch reorder (Phase 2) must land in both files within this change set. `extensions/drm-copilot/src/lib/potential-to-issue/promotion-filesystem.ts`, `extensions/drm-copilot/src/lib/potential-to-issue/content.ts`, and `scripts/dev_tools/potential_to_issue_content.py` must NOT be modified.

**Tool-definition mirror constraint (binding):** `extensions/drm-copilot/src/mcp-tool-definitions.ts` (exporting `toolDefinitions`) is an in-scope changed file for the `required`-array changes. It is a test-only base mirror of the authoritative runtime array `REPO_AUTOMATION_TOOL_DEFINITIONS` and must be kept consistent with it for the `required` changes in this change set. It is NOT part of the P5-T11 protected set.

### Phase 0 — Policy Reading and Baseline Capture

- [x] [P0-T1] Read policy files in the required order: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`. Write `EVIDENCE:/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: artifact exists with all three fields and lists all nine files.
- [x] [P0-T2] Record the branch name and HEAD commit SHA (`git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`) in `EVIDENCE:/baseline/phase0-branch-commit.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with branch and SHA recorded.
- [x] [P0-T3] TypeScript format baseline: from `extensions/drm-copilot/` run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`. Write `EVIDENCE:/baseline/baseline-ts-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; exit code recorded verbatim.
- [x] [P0-T4] TypeScript lint baseline: from `extensions/drm-copilot/` run `npm run lint`. Write `EVIDENCE:/baseline/baseline-ts-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T5] TypeScript type-check baseline: from `extensions/drm-copilot/` run `npm run typecheck`. Write `EVIDENCE:/baseline/baseline-ts-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T6] TypeScript test + coverage baseline: from `extensions/drm-copilot/` run `npm run test:coverage`. Write `EVIDENCE:/baseline/baseline-ts-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric line-coverage and branch-coverage percentages from the text-summary reporter and the pass/fail test counts. Acceptance: artifact exists and `Output Summary:` contains numeric line % and branch % values (not placeholders).
- [x] [P0-T7] Python format baseline: from repo root run `poetry run black --check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/baseline/baseline-py-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Python lint baseline: from repo root run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/baseline/baseline-py-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T9] Python type-check baseline: from repo root run `poetry run pyright scripts/dev_tools/potential_to_issue.py scripts/dev_tools/potential_to_issue_content.py tests/scripts/dev_tools`. Write `EVIDENCE:/baseline/baseline-py-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T10] Python test + coverage baseline: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term`. Write `EVIDENCE:/baseline/baseline-py-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric total line-coverage percentage, branch-coverage data, the per-file coverage line for `scripts/dev_tools/potential_to_issue.py`, and the pass/fail test counts. Acceptance: artifact exists and `Output Summary:` contains numeric coverage values (not placeholders).
- [x] [P0-T11] Record the current line count of `extensions/drm-copilot/src/mcp-tool-inputs.ts` (expected ~499) in `EVIDENCE:/baseline/baseline-mcp-tool-inputs-linecount.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with the numeric line count, establishing the AC-14 baseline.

### Phase 1 — Expect-Fail Regression Tests

- [x] [P1-T1] [expect-fail] Add a Jest regression test to `extensions/drm-copilot/test/lib/potential-to-issue/promotion.test.ts`: a bug potential with populated bug-template sections promoted with `work_mode=minor-audit` must produce a body headed by the bug sections (`Summary`, `Environment`, `Steps to Reproduce`, `Expected Behavior`, `Actual Behavior`, `Logs / Screenshots`, `Impact / Severity`) containing the authored content, with zero occurrences of `(not provided in potential file)` for populated sections and first body line `- Work Mode: minor-audit` (AC-1). Acceptance: test file compiles and the new test is present.
- [x] [P1-T2] [expect-fail] Run the new (bug, minor-audit) Jest test from `extensions/drm-copilot/` (`npm run test -- test/lib/potential-to-issue/promotion.test.ts`) and confirm it fails against the unfixed code. Write `EVIDENCE:/regression-testing/expect-fail-ts-bug-minor-audit.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming the failing test. Acceptance: artifact records a non-zero exit code with the new test failing.
- [x] [P1-T3] [expect-fail] Add a pytest regression test to `tests/scripts/dev_tools/test_potential_to_issue.py` mirroring P1-T1: a bug potential promoted with `work_mode=minor-audit` yields a bug-headed body with authored content and first line `- Work Mode: minor-audit` (AC-3). Acceptance: the new test is present and collectible.
- [x] [P1-T4] [expect-fail] Run the new pytest case from repo root (`poetry run pytest tests/scripts/dev_tools/test_potential_to_issue.py -k <new-test-name>`) and confirm it fails against the unfixed code. Write `EVIDENCE:/regression-testing/expect-fail-py-bug-minor-audit.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records a non-zero exit code with the new test failing.
- [x] [P1-T5] [expect-fail] Add a Jest test to `extensions/drm-copilot/test/mcp-tool-inputs.test.ts`: calling a per-tool resolver (e.g. `resolveNewPotentialBugEntryToolInput`) with `workspace_root` omitted and no explicit `fallbackWorkspaceRoot` argument must throw with an actionable message naming `workspace_root` (AC-4). Acceptance: the new test is present and compiles.
- [x] [P1-T6] [expect-fail] Add a Jest test to `extensions/drm-copilot/test/mcp-tool-inputs.test.ts`: `resolvePotentialToIssueToolInput` given a workspace-relative `potential_path` and an explicit `workspace_root` must return the `workspace_root`-joined absolute path, and given an absolute `potential_path` must preserve it (AC-6). Acceptance: the new test is present and compiles.
- [x] [P1-T7] [expect-fail] Add a Jest test to `extensions/drm-copilot/test/mcp-repo-automation-tool-definitions.test.ts`: every entry in the authoritative runtime array `REPO_AUTOMATION_TOOL_DEFINITIONS` (all 28 tools, equal to `REPO_AUTOMATION_TOOLS`) lists `workspace_root` in `inputSchema.required`, and `workspaceRootProperty.description` does not contain the substring `process.cwd()` (AC-5). Acceptance: the new test is present and compiles.
- [x] [P1-T8] [expect-fail] Run the Phase 1 Defect A Jest tests from `extensions/drm-copilot/` (`npm run test -- test/mcp-tool-inputs.test.ts test/mcp-repo-automation-tool-definitions.test.ts`) and confirm the P1-T5, P1-T6, and P1-T7 tests fail against the unfixed code. Write `EVIDENCE:/regression-testing/expect-fail-ts-defect-a.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming each failing test. Acceptance: artifact records a non-zero exit code with all three new tests failing.

### Phase 2 — Defect B Implementation (Heading Mapping, TS + Python Lockstep)

- [x] [P2-T1] Reorder `buildIssueBody` in `extensions/drm-copilot/src/lib/potential-to-issue/promotion.ts` (lines ~201-262) so `promotionType === "bug"` routes to `buildBugBody` first regardless of work mode, then `selectedMode === "minor-audit"` routes non-bug types to `buildMinorAuditBody`, else `buildBody`. Do not modify `content.ts`. Acceptance: the bug branch precedes the minor-audit branch and the P1-T1 test passes when run.
- [x] [P2-T2] Update the `buildIssueBody` routing-table docblock in `extensions/drm-copilot/src/lib/potential-to-issue/promotion.ts` (lines ~187-199) to describe the new branch order (AC-9). Acceptance: docblock text matches the implemented order.
- [x] [P2-T3] Correct the stale parity-header path in `extensions/drm-copilot/src/lib/potential-to-issue/promotion.ts` (line ~6) from `resources/scripts/dev_tools/potential_to_issue.py` to `scripts/dev_tools/potential_to_issue.py` (AC-9). Acceptance: the header cites the actual Python source path.
- [x] [P2-T4] Apply the identical branch reorder to `scripts/dev_tools/potential_to_issue.py` (lines ~437-485): `promotion_type == "bug"` routes to the bug body builder before the minor-audit branch; update any adjacent routing comment to match (AC-3, parity constraint). Acceptance: the Python decision-branch order is byte-parity-equivalent to the TypeScript order and the P1-T3 test passes when run.
- [x] [P2-T5] Add Jest matrix guard tests to `extensions/drm-copilot/test/lib/potential-to-issue/promotion.test.ts` (AC-2): (feature, minor-audit), (refactor, minor-audit), (epic, minor-audit) route to `buildMinorAuditBody` output; (bug, full-bug) and (bug, full) route to `buildBugBody` output; (feature, full-feature) and (feature, full) route to `buildBody` output; (bug, full-feature) and (feature, full-bug) throw before body build. Acceptance: all listed cells are asserted and pass.
- [x] [P2-T6] Add a Jest edge-case test to `extensions/drm-copilot/test/lib/potential-to-issue/promotion.test.ts`: a bug potential in minor-audit mode with some empty sections emits `(not provided in potential file)` only for the empty sections while populated sections carry authored content. Acceptance: test present and passing.
- [x] [P2-T7] Update existing minor-audit routing assertions in `tests/scripts/dev_tools/test_potential_to_issue.py` for the reorder, and review `tests/scripts/dev_tools/test_potential_to_issue_content.py` and `tests/scripts/dev_tools/test_potential_to_issue_missing_label_regression.py` for affected cases, updating only assertions invalidated by the branch reorder. Acceptance: no pytest assertion still expects a bug potential in minor-audit mode to route to the minor-audit body.
- [x] [P2-T8] Run the Defect B pass-after verification: from `extensions/drm-copilot/` run `npm run test -- test/lib/potential-to-issue/` and from repo root run `poetry run pytest tests/scripts/dev_tools/test_potential_to_issue.py tests/scripts/dev_tools/test_potential_to_issue_content.py tests/scripts/dev_tools/test_potential_to_issue_missing_label_regression.py`. Write `EVIDENCE:/regression-testing/pass-after-defect-b.<ts>.md` with `Timestamp:`, both `Command:` lines, both `EXIT_CODE:` values, `Output Summary:` with pass counts. Acceptance: both commands exit 0 and the artifact exists.

### Phase 3 — Defect A Implementation (Fail-Closed workspace_root and potential_path)

- [x] [P3-T1] Modify `normalizeWorkspaceRoot` in `extensions/drm-copilot/src/workflow-command-arguments.ts` (lines ~275-284): when `value === undefined` and the caller supplied no explicit `fallbackWorkspaceRoot` argument, throw an error with the message `workspace_root is required. The MCP server cannot infer the calling agent's checkout; pass the absolute worktree root explicitly.` Enabling the throw for the omitted-with-no-explicit-fallback case requires making the second parameter optional WITHOUT a `process.cwd()` default (e.g. `fallbackWorkspaceRoot?: string`) so the function distinguishes "no argument supplied" from an explicit value, while remaining a two-argument signature. Preserve VS Code behavior: callers passing an explicit fallback (VS Code command surface) keep current behavior; valid string values and invalid-type errors are unchanged (AC-4). Acceptance: omitted value with no fallback throws; omitted value with explicit fallback returns the fallback; string and invalid-type behavior unchanged.
- [x] [P3-T2] Add unit tests for `normalizeWorkspaceRoot` in the appropriate existing Jest suite covering all four cases: (a) omitted value + no fallback throws with the message naming `workspace_root`; (b) omitted value + explicit fallback returns the fallback; (c) valid string value normalizes as before; (d) invalid-type value preserves the existing error; plus empty-string and whitespace-only `workspace_root` preserve existing validation semantics (AC-4). Acceptance: all cases present and passing.
- [x] [P3-T3] Add `"workspace_root"` to the `inputSchema.required` array of every tool definition in `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` (AC-5). Acceptance: every repo-automation tool definition in that file lists `workspace_root` in `required`.
- [x] [P3-T4] Add `"workspace_root"` to the `inputSchema.required` array of every tool definition in `extensions/drm-copilot/src/mcp-discovery-tool-definitions.ts` so that all 28 tools in `REPO_AUTOMATION_TOOLS` require it (AC-5). Acceptance: every discovery tool definition in that file lists `workspace_root` in `required`; the P1-T7 test (asserting `workspace_root` in `inputSchema.required` for every entry of `REPO_AUTOMATION_TOOL_DEFINITIONS`) passes when run.
- [x] [P3-T5] Add `"workspace_root"` to the `inputSchema.required` array of every tool definition in `extensions/drm-copilot/src/mcp-tool-definitions.ts` (`toolDefinitions`) so it stays consistent with `REPO_AUTOMATION_TOOL_DEFINITIONS` (AC-5, AC-12). Acceptance: `toolDefinitions` and `REPO_AUTOMATION_TOOL_DEFINITIONS` list identical `required` arrays for every tool.
- [x] [P3-T6] Update the existing assertions in `extensions/drm-copilot/test/mcp-repo-automation-tool-definitions.test.ts` invalidated by the required-array change: the `push_down_copilot_customizations` "workspace-root-only" case (~L87-102, `required` now `["workspace_root"]`), the `run_codex_native_converter` toMatchObject `required` array (~L133-154), and the `render_subagent_tree` case and its title (~L179-195, `required` now includes `workspace_root`, and reword the "optional workspace_root" title). Change ONLY assertions invalidated by the required-array change. Acceptance: no assertion still expects a repo-automation tool's `required` to omit `workspace_root`.
- [x] [P3-T7] Update `extensions/drm-copilot/test/mcp-tools.push-down-claude.test.ts` (~L150-160 claude, ~L188-197 codex) so the claude/codex push-down schemas assert `required` includes `workspace_root` rather than being absent; review `extensions/drm-copilot/test/mcp-epic-validation-definitions.test.ts` for `required`-array assertions invalidated by the change and update only those. Acceptance: both files pass against the new required arrays with no unrelated assertion weakened.
- [x] [P3-T8] Rewrite `workspaceRootProperty.description` in `extensions/drm-copilot/src/mcp-push-down-schema-properties.ts` to state that the absolute root of the target checkout/worktree is required, removing the "Defaults to process.cwd() when omitted" text (AC-5). Acceptance: the description no longer contains `process.cwd()`.
- [x] [P3-T9] Modify `resolvePotentialToIssueToolInput` in `extensions/drm-copilot/src/mcp-tool-inputs.ts` (lines ~316-336) to resolve `potential_path` against the resolved `workspace_root` via the existing `normalizeWorkspaceDestinationPath(potentialPath, workspaceRoot, "potential_path")` helper before the value reaches `promotePotential`; do not modify `extensions/drm-copilot/src/lib/potential-to-issue/promotion-filesystem.ts` (AC-6). Acceptance: the P1-T6 test passes when run and `promotion-filesystem.ts` shows no diff.
- [x] [P3-T10] Check the post-change line count of `extensions/drm-copilot/src/mcp-tool-inputs.ts`; if it exceeds 500 lines, extract the `potential_to_issue` resolver (or the normalization logic) into a sibling module following the `extensions/drm-copilot/src/mcp-tool-inputs-push-down.ts` precedent, updating imports at all call sites (AC-14). Write `EVIDENCE:/other/mcp-tool-inputs-linecount-final.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the final line count(s). Acceptance: `mcp-tool-inputs.ts` (and any extracted sibling) is <= 500 lines and the artifact records the numeric count.
- [x] [P3-T11] Invert the existing fallback-default Jest tests in `extensions/drm-copilot/test/mcp-tool-inputs.test.ts` (cases at lines ~50, ~248-256, ~342 and per-tool omission cases) and the analogous cases in `extensions/drm-copilot/test/mcp-tool-inputs-discovery.test.ts`, `extensions/drm-copilot/test/mcp-tool-inputs-epic-validation.test.ts`, and `extensions/drm-copilot/test/mcp-tool-inputs.codex-native-converter.test.ts`: omitted `workspace_root` with no fallback now asserts the thrown required-field error; explicit-fallback cases remain asserted green (AC-4). Acceptance: no test still asserts a silent `process.cwd()` default for the no-fallback case; explicit-fallback cases pass.
- [x] [P3-T12] Add a Jest test asserting the failure envelope (AC-8): an MCP dispatch with `workspace_root` omitted returns `ok: false` through `toFailureToolResult` with the `RepoAutomationMcpToolResult` envelope shape (`ok`, `workspace_root`, `summary`) and the actionable message text, in the appropriate existing suite (`extensions/drm-copilot/test/mcp-tools.*.test.ts` or `extensions/drm-copilot/test/mcp-server.test.ts`). Acceptance: test present and passing.
- [x] [P3-T13] Add a Jest test pinning the `potentialToIssueServiceCall` summary string form (`Promoted '<path>' ...`) for a relative `potential_path` input after resolver normalization, in `extensions/drm-copilot/test/lib/potential-to-issue/potential-to-issue-service-call.test.ts` or the resolver suite (AC-6 scope). Acceptance: the intended summary form for relative inputs is asserted and passing.
- [x] [P3-T14] Run the VS Code command-surface suites from `extensions/drm-copilot/` (`npm run test -- test/extension.potential-to-issue.test.ts test/extension.new-potential-bug-entry-inprocess.test.ts`) and confirm they pass with no behavioral modification to those test files (AC-7). Write `EVIDENCE:/regression-testing/vscode-surface-intact.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 and the artifact confirms the two files required no semantic changes.
- [x] [P3-T15] Run the Defect A pass-after verification: from `extensions/drm-copilot/` run `npm run test -- test/mcp-tool-inputs.test.ts test/mcp-tool-inputs-discovery.test.ts test/mcp-tool-inputs-epic-validation.test.ts test/mcp-tool-inputs.codex-native-converter.test.ts test/mcp-repo-automation-tool-definitions.test.ts`. Write `EVIDENCE:/regression-testing/pass-after-defect-a.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` with pass counts. Acceptance: exit code 0 and the artifact exists.

### Phase 4 — Documentation Sweep (workspace_root Required)

- [x] [P4-T1] Update `.claude/skills/execute-hard-lock/SKILL.md` (line ~37): change the `workspace_root` parameter description from optional/defaults-to-cwd to required, stating the caller must pass the absolute root of its checkout/worktree (AC-13). Acceptance: the file no longer describes `workspace_root` as optional or defaulting.
- [x] [P4-T2] Sweep all in-repo instructional docs (`.claude/skills/**`, `.claude/agents/**`, `.claude/rules/**`, `README`/`docs` guides; excluding historical evidence under `docs/features/completed/**`, `docs/features/active/**` feature artifacts, and `docs/research/**` research snapshots) for remaining claims that `workspace_root` is optional or defaults to `process.cwd()` for the drm-copilot MCP tools; fix any found (AC-13). Write `EVIDENCE:/other/doc-sweep-workspace-root.<ts>.md` with `Timestamp:`, `SearchScope:`, `SearchPatterns:` (at minimum `workspace_root`, `process.cwd`), `SearchResult:` (paths found or `none`), and the list of files edited. Acceptance: artifact exists with the negative-evidence fields and no remaining instructional doc claims a `process.cwd()` default.

### Phase 5 — Final QA Loop (TypeScript + Python) and Coverage Verification

Loop rule: if any step in this phase fails or changes files, restart this phase from P5-T1 and re-run every step until all steps pass in a single pass without file changes.

- [x] [P5-T1] TypeScript format: from `extensions/drm-copilot/` run `npm run format`; confirm via `git status --porcelain` whether files changed. Write `EVIDENCE:/qa-gates/final-ts-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including whether files were modified). Acceptance: exit code 0; if files changed, the loop restarts at P5-T1 after re-verification.
- [x] [P5-T2] TypeScript lint: from `extensions/drm-copilot/` run `npm run lint`. Write `EVIDENCE:/qa-gates/final-ts-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 with zero errors.
- [x] [P5-T3] TypeScript type-check: from `extensions/drm-copilot/` run `npm run typecheck`. Write `EVIDENCE:/qa-gates/final-ts-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 with zero errors.
- [x] [P5-T4] TypeScript tests with coverage: from `extensions/drm-copilot/` run `npm run test:coverage`. Write `EVIDENCE:/qa-gates/final-ts-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change line-coverage and branch-coverage percentages and pass counts. Acceptance: exit code 0, all tests pass, and `Output Summary:` records numeric line % >= 85 and branch % >= 75.
- [x] [P5-T5] Python format: from repo root run `poetry run black scripts/dev_tools tests/scripts/dev_tools`; confirm via `git status --porcelain` whether files changed. Write `EVIDENCE:/qa-gates/final-py-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0; if files changed, the loop restarts at P5-T1.
- [x] [P5-T6] Python lint: from repo root run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools`. Write `EVIDENCE:/qa-gates/final-py-lint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 with zero errors; if ruff auto-fixed files, the loop restarts at P5-T1.
- [x] [P5-T7] Python type-check: from repo root run `poetry run pyright scripts/dev_tools/potential_to_issue.py scripts/dev_tools/potential_to_issue_content.py tests/scripts/dev_tools`. Write `EVIDENCE:/qa-gates/final-py-typecheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0 with zero errors.
- [x] [P5-T8] Python tests with coverage: from repo root run `poetry run pytest tests/scripts/dev_tools --cov=scripts/dev_tools --cov-branch --cov-report=term`. Write `EVIDENCE:/qa-gates/final-py-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric total coverage percentage, the per-file line for `scripts/dev_tools/potential_to_issue.py`, and pass counts. Acceptance: exit code 0, all tests pass, numeric coverage values recorded.
- [x] [P5-T9] TypeScript coverage delta verification: compare P0-T6 baseline against P5-T4 post-change coverage. Write `EVIDENCE:/qa-gates/coverage-delta-ts.<ts>.md` with `Timestamp:`, baseline line %/branch %, post-change line %/branch %, confirmation that post-change line >= 85 and branch >= 75, and confirmation that changed lines in `workflow-command-arguments.ts`, `mcp-tool-inputs.ts` (and any extracted sibling), and `promotion.ts` are exercised by the new/updated tests (AC-10). Acceptance: artifact records numeric values meeting both thresholds with no regression on changed lines; otherwise the outcome is remediation-required, not PASS.
- [x] [P5-T10] Python coverage delta verification: compare P0-T10 baseline against P5-T8 post-change coverage for `scripts/dev_tools/potential_to_issue.py`. Write `EVIDENCE:/qa-gates/coverage-delta-py.<ts>.md` with `Timestamp:`, baseline and post-change numeric values, confirmation that thresholds (line >= 85, branch >= 75) hold for the changed module and that changed lines are exercised (AC-11). Acceptance: artifact records numeric values meeting both thresholds; otherwise remediation-required, not PASS.
- [x] [P5-T11] Scope-integrity verification (AC-12): run `git diff --name-only <baseline-SHA from P0-T2>` and confirm no diff exists in `extensions/drm-copilot/src/lib/potential-to-issue/content.ts`, `extensions/drm-copilot/src/lib/potential-to-issue/promotion-filesystem.ts`, `extensions/drm-copilot/src/lib/prompt-mode-contract.ts`, `scripts/dev_tools/potential_to_issue_content.py`, and that the Python CLI workspace default in `scripts/dev_tools/potential_to_issue.py` is unchanged. Write `EVIDENCE:/qa-gates/scope-integrity.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the changed-file set. Acceptance: none of the protected files appear in the diff.

### Phase 6 — Documentation and Status

- [x] [P6-T1] Update `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/spec.md`: check off each delivered acceptance criterion AC-1 through AC-14 in the `## Acceptance Criteria` section, citing the verifying evidence artifact path for each, and update `Last Updated`. Acceptance: all 14 AC checkboxes reflect delivered state with evidence citations.
- [x] [P6-T2] Update `docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/issue.md`: record the fix outcome (both defects resolved, files changed, breaking-change note for the required `workspace_root` schema) and update `Last Updated`. Write the mirror artifact `EVIDENCE:/issue-updates/issue-401.<ts>.md` with `Timestamp:`, the exact update text, and `PostedAs: unknown` (or the posted-comment URL if the update is posted to GitHub issue #401). Acceptance: `issue.md` reflects outcomes and the mirror artifact exists.
- [x] [P6-T3] Update this plan file (`docs/features/active/2026-07-22-mcp-promotion-tooling-defects-401/plan.2026-07-22T09-56.md`): mark completed tasks `[x]`, ensuring checklist state matches evidence on disk for every evidence-bearing task. Acceptance: no task is checked without its named artifact existing.
