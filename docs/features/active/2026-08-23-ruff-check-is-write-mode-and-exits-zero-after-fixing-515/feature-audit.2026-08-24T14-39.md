# Feature Audit: Ruff check write-mode removal (#515)

---

**Audit Date:** 2026-08-24
**Feature Folder:** `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515`
**Base Branch:** `main`
**Head Branch:** `bug/ruff-check-is-write-mode-and-exits-zero-after-fixing-515-r2`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

**Template source note:** Created from the bundled asset `extensions/drm-copilot/resources/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md` (the file the MCP `feature-audit-template` selector resolves); the MCP tool surface was unavailable in this session.

---

## Scope and Baseline

- **Base branch:** `main` (resolved `origin/main` @ `80b65d2ed843d2dd72d722f3b6d88b8b84634227`)
- **Head branch/commit:** `bug/ruff-check-is-write-mode-and-exits-zero-after-fixing-515-r2` (commit `4a926a0383e3c18aea475de1edad461d0f95998b`)
- **Merge base:** `80b65d2ed843d2dd72d722f3b6d88b8b84634227`
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer at review time; the pair was absent on entry)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515/evidence/**` (20 artifacts)
  - Additional evidence: reviewer-executed check-only commands (pytest, black --check, pyright, ruff check bracketed by status snapshots, coverage-JSON parse, `git diff --name-only`)
- **Feature folder used:** `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515` (single-version feature; no `v1/`/`v2/` subfolders)
- **Requirements source:** `spec.md` only
- **Work mode resolution note:** `issue.md` carries the explicit marker `- Work Mode: full-bug` (line 12). Per the acceptance-criteria tracking rules, `full-bug` resolves the AC source to `spec.md` only; `user-story.md` is correctly absent from the feature folder.
- **Scope note:** PR-context artifacts were missing at review start and were regenerated with the repo collector against base `main`; the resolved merge base matches the caller-supplied SHA exactly. Audit scope is the full branch diff versus the merge base; no scope narrowing was requested or applied.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515/spec.md` (`## Acceptance Criteria`) — only source

### Acceptance criteria

1. The `[tool.ruff]` table in `pyproject.toml` no longer enables fix mode, and `tests/scripts/dev_tools/test_ruff_config_alignment.py::test_ruff_config_does_not_enable_fix_mode` passes against the post-change tree.
2. `show-fixes = true` remains present in the `[tool.ruff]` table, and `tests/scripts/dev_tools/test_ruff_config_alignment.py::test_ruff_config_retains_show_fixes` passes.
3. Neither `ruff.toml` nor `.ruff.toml` exists at the repository root, and `tests/scripts/dev_tools/test_ruff_config_alignment.py::test_no_standalone_ruff_config_at_repository_root` passes (research residual 2).
4. The Ruff lint step in `.github/workflows/_quality-checks.yml` still exists and still invokes the linter, and `tests/scripts/dev_tools/test_ruff_config_alignment.py::test_quality_checks_workflow_still_runs_a_ruff_lint_step` passes, so the preceding three criteria cannot be satisfied by deleting the gate.
5. `poetry run pytest tests/scripts/dev_tools/test_ruff_config_alignment.py -v` collects exactly the four named tests above and reports 4 passed with 0 failed and 0 errors; the run output is recorded as evidence under this feature's `evidence/regression-testing/` folder with `Timestamp`, `Command`, and `EXIT_CODE` fields.
6. Fail-before evidence is recorded: a run of `test_ruff_config_does_not_enable_fix_mode` against the pre-change `pyproject.toml` fails, and the same test passes after the deletion. Both runs are recorded in a single artifact under this feature's `evidence/regression-testing/` folder, with the failing run declaring its expected non-zero exit code per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`.
7. `git diff --name-only` against the merge base lists exactly `pyproject.toml`, `tests/scripts/dev_tools/test_ruff_config_alignment.py`, and paths under `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515/`. In particular the list contains neither `.claude/rules/python.md`, nor `.github/instructions/python-code-change.instructions.md`, nor `scripts/dev_tools/atomic_executor/qc_runner_loop.py`.
8. The lint stage performs no write: a working-tree status snapshot taken immediately before `poetry run ruff check` and another taken immediately after are byte-identical, and the pair is recorded as QA-gate evidence under this feature's `evidence/qa-gates/` folder. A criterion stating only that the lint stage exits 0 is explicitly insufficient and is not accepted in its place, because that formulation is satisfiable by a run that rewrote production files.
9. The manual verification required by `issue.md` line 98 is recorded as QA-gate evidence under this feature's `evidence/qa-gates/` folder: against a scratch input outside the repository containing an unfixable violation the lint stage exits non-zero, and against a scratch input containing a fixable violation it exits non-zero, reports the violation with the `[*]` fixable marker, and leaves the input byte-identical. The artifact declares `ExpectedExitCode` for each non-zero expectation.
10. The seven-stage toolchain in `.claude/rules/general-code-change.md` completes with all applicable stages passing in a single pass with no stage having changed a file, and the stage outputs are recorded as QA-gate evidence under this feature's `evidence/qa-gates/` folder.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Fix mode removed; primary guard test passes | PASS | Branch diff shows the single deleted line `fix = true` in `[tool.ruff]`; reviewer run: test passed | `git diff 80b65d2e..HEAD -- pyproject.toml`; `poetry run pytest tests/scripts/dev_tools/test_ruff_config_alignment.py -v` | Independently re-verified at review time, not taken from executor evidence alone |
| 2 | `show-fixes = true` retained; guard test passes | PASS | Diff context shows `show-fixes = true` untouched; reviewer run: test passed | Same commands as #1 | Diagnostic property preserved (fixable-count reporting confirmed by criterion 9 artifact) |
| 3 | No standalone root Ruff config; guard test passes | PASS | Reviewer run: test passed (the test itself performs the existence checks for both filenames) | `poetry run pytest tests/scripts/dev_tools/test_ruff_config_alignment.py -v` | Closes research residual 2 |
| 4 | CI Ruff lint step still present; guard test passes | PASS | `.github/workflows/_quality-checks.yml` is absent from the branch diff (unchanged); reviewer run: test passed | `git diff --name-only 80b65d2e..HEAD`; pytest run as above | Guard asserts on the invocation, not the step name |
| 5 | Exactly 4 tests collected, 4 passed, evidence recorded | PASS | Reviewer run: `collected 4 items`, 4 passed, 0 failed, 0 errors, exit 0; executor artifact carries Timestamp/Command/EXIT_CODE | `poetry run pytest tests/scripts/dev_tools/test_ruff_config_alignment.py -v` | Evidence: `evidence/regression-testing/regression-ruff-config-alignment-suite.2026-08-24T14-03.md` (EXIT_CODE 0) |
| 6 | Fail-before / pass-after in a single artifact with ExpectedExitCode | PASS | Single artifact records the P1-T2 failing run (1 failed, 3 passed, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, verbatim assertion output naming `test_ruff_config_does_not_enable_fix_mode` with `Offending: ['fix = true']`) and the post-deletion passing run | Read of `evidence/regression-testing/fail-before-pass-after-ruff-config-alignment.2026-08-24T13-57.md` | PR-context collector normalized the artifact to `pass` (expected exit code honored) |
| 7 | Diff name-list is exactly the authorized write set | PASS | Reviewer-run listing: `pyproject.toml`, `tests/scripts/dev_tools/test_ruff_config_alignment.py`, and 24 paths under the feature folder; nothing else. None of the three prohibited paths appears | `git diff --name-only 80b65d2e..HEAD` | Verified directly at review time |
| 8 | Lint stage performs no write (byte-identical snapshot pair) | PASS | Executor artifact: `cmp` exit 0 and equal SHA-256 digests for the before/after `git status --porcelain` captures around the bare `poetry run ruff check`. Reviewer independently repeated the bracketed run on the clean review tree: exit 0, status output identical (empty) before and after | Read of `evidence/qa-gates/lint-stage-no-write.2026-08-24T14-06.md`; reviewer: `git status --porcelain` / `poetry run ruff check .` / `git status --porcelain` | The artifact itself documents why the pair alone is non-discriminating and defers discrimination to criterion 9, which is intellectually honest and correct |
| 9 | Manual differential on scratch inputs with ExpectedExitCode | PASS | Artifact declares `ExpectedExitCode: 1`; unfixable input (F821) exits 1 with no `[*]` marker; fixable input (F401) exits 1 WITH `[*]` marker and fixable-count summary; before/after SHA-256 pairs equal for both inputs; `--show-settings` proves resolution to the repo `pyproject.toml` with `fix = false` | Read of `evidence/qa-gates/lint-stage-manual-differential.2026-08-24T14-09.md` | Scratch files were outside the repository; working-tree snapshots unaffected |
| 10 | Seven-stage toolchain single clean pass, evidence recorded | PASS | Pass 2 of the final loop: format exit 0 (0 reformats), lint exit 0 (no write), pyright exit 0, pytest 4116 passed / 0 failed exit 0; entry/exit status snapshots byte-identical (`cmp` exit 0, equal SHA-256). Stages 4/6/7 (architecture, contract, integration) not applicable to this diff. Pass-1 restart was caused by pre-existing filed issue #510, documented with non-attribution evidence | Read of `evidence/qa-gates/final-python-{format,lint,typecheck,test-coverage}.*.md` and `final-qa-loop-single-pass.2026-08-24T14-18.md` | Restart-from-stage-1 rule was honored, which is itself the behavior this fix exists to make enforceable |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 10 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. After merge, confirm the first `Lint with Ruff` run on `main` passes (spec Rollout note); a failure would surface a pre-existing fixable violation previously hidden by the write-mode step and should be fixed with an explicit `--fix` run, not a revert.
2. Consider promoting spec follow-up recommendation 1 (executor lint-step diff snapshot, research direction (d)) to a filed issue so a future writing-linter route becomes detectable at execution time.

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 10 criteria evaluate to **PASS**.
- All 10 checkbox items in the authoritative source (`spec.md`, `## Acceptance Criteria`) were already marked `[x]` by the executor at delivery time. This audit independently re-verified each one; no additional check-off edits were required, and no source file was modified by this review.
- Criterion text was preserved verbatim in the inventory above; no criterion was added, removed, or rewritten.

### AC Status Summary

- Source: `docs/features/active/2026-08-23-ruff-check-is-write-mode-and-exits-zero-after-fixing-515/spec.md`
- Total AC items: 10
- Checked off (delivered): 10
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 10 | 10 | 0 | Checkbox-backed; all pre-checked by executor, all independently confirmed PASS by this audit |

No source-file checkbox change was made by this review because every PASS criterion was already checked; the reviewer's obligation under the tracking rules is therefore confirmation, which this table records.
