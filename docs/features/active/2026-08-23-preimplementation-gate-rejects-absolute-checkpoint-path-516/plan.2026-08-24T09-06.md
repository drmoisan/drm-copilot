# 2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path (Plan)

- **Issue:** #516
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-24T11-00
- **Status:** Ready for Preflight (revision pass 1 applied)
- **Version:** 1.1
- **Work Mode:** full-bug (persisted marker in `issue.md`; `spec.md` is the sole acceptance-criteria source; `user-story.md` is absent by design)

**Requirements sources:** `spec.md` (authoritative acceptance criteria, decision table, preserved invariants) and `research/research.2026-08-24T09-50.md` (normalization algorithm, seam, test strategy) in this feature folder.

**Path abbreviation:** `FEATURE/` denotes `docs/features/active/2026-08-23-preimplementation-gate-rejects-absolute-checkpoint-path-516/`. All evidence artifacts are written under `FEATURE/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. No `artifacts/` evidence sub-path is used anywhere in this plan. `<timestamp>` in an evidence filename denotes the executor's run timestamp in `yyyy-MM-ddTHH-mm` form.

**Fail-closed evidence rule:** If any required baseline artifact, fail-before artifact, final-QC artifact, or coverage-comparison artifact is missing or incomplete, the outcome is remediation-required, never PASS.

## Change summary (binding on the executor)

Add a pure string helper named `ConvertTo-WorkspaceRelativePath` (parameters `-FilePath` and `-WorkspaceRoot`; no filesystem access, no subprocess, no environment read) to each of the four hook copies, implementing the seven-step normalization contract in `spec.md` section "Normalization contract". Add an optional seam parameter `[string] $WorkspaceRoot = (Get-Location).Path` to `Invoke-OrchestrationPreimplementationGateDecision` (Claude variant) and to the Codex equivalent decision function. Replace the two-line separator-only normalization (Claude hook lines 259–260; Codex hook lines 272–273) with a single helper call. In the Codex variant only, additionally add an optional `[string] $WorkspaceRoot = (Get-Location).Path` parameter to the Codex copy of `Test-ImplementationCommand` and normalize each apply-patch hunk path it harvests (Codex lines 92–103) with the same helper, passing the root from the decision function's command branch (line 277) — the same defect reaches the same exemption classifier through that second route. The mapped Edit/Write loop (Codex lines 321–331) is entry-point code and is not modified: the decision-function parameter default already supplies the correct root on that call path. `Test-ImplementationPath`, `Test-FeatureDocumentationOrEvidencePath`, `$script:CheckpointPaths`, the readiness logic, the block-message text, and the entry point are not modified (spec "Invariants Preserved" 1–8; the Codex `Test-ImplementationCommand` carve-out is recorded as a clarifying sub-bullet under spec Invariant 5, and the Claude copy of `Test-ImplementationCommand` remains unmodified).

Non-widening rule: every implementation task carries a DENY acceptance condition in addition to its ALLOW condition. The gate gains no exemption it did not have; unresolvable forms (spec decision-table rows 8, 11, 12, 14, 20) pass through unchanged and remain denied.

## Batch split (PowerShell per-batch cap)

Four production files exceed the 3-production-file per-batch cap in `.claude/rules/powershell.md`. Execution is split into two batches, mirroring the issue #535 precedent, with a batch-budget state reset between them:

- **Batch 1 (Phases 1–2):** production files `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` and `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` (2 production), plus test file `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1` (1 test). Within the 3+3 cap.
- **Batch 2 (Phases 3–4):** production files `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` and `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` (2 production), plus test file `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1` (1 test). Within the 3+3 cap.

The pre-existing test files `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` (461 lines) and `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` (494 lines) are not modified in any batch.

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read the policy files in this order: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/plan-acceptance-gates.md`; then read `FEATURE/spec.md`, `FEATURE/research/research.2026-08-24T09-50.md`, and `FEATURE/issue.md`. Write `FEATURE/evidence/baseline/phase0-instructions-read.<timestamp>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists and contains all three required fields with the nine files listed.
- [x] [P0-T2] Record the branch and commit baseline by running `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD`. Write `FEATURE/evidence/baseline/branch-commit.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch name and commit SHA). Acceptance: the artifact exists with all four fields and `EXIT_CODE: 0`.
- [x] [P0-T3] Capture the PowerShell formatting baseline with the `mcp__drm-copilot__run_poshqc_format` MCP tool. Write `FEATURE/evidence/baseline/poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail and count of files changed). Record the baseline unconditionally: this task captures state and does not gate; the pass/fail formatting gate is P6-T1. Acceptance: the artifact exists with all four fields recorded.
- [x] [P0-T4] Capture the PSScriptAnalyzer baseline with the `mcp__drm-copilot__run_poshqc_analyze` MCP tool. Write `FEATURE/evidence/baseline/poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count). Acceptance: the artifact exists with all four fields and reports zero findings.
- [x] [P0-T5] Capture the Pester baseline in coverage mode with the `mcp__drm-copilot__run_poshqc_test` MCP tool. Write `FEATURE/evidence/baseline/poshqc-test-coverage.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` containing the numeric pass/fail counts and the numeric baseline line-coverage percentage (overall, plus per-file percentages for the four hook copies where the coverage report itemizes them). Acceptance: the artifact exists with all four fields, `EXIT_CODE: 0`, zero test failures, and a numeric coverage percentage (no placeholder value).

### Phase 1 — Regression Tests, Claude Facet (must fail first)

- [x] [P1-T1] Author the new facet test file `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1`, dot-sourcing the hook exactly as `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` does and reusing its nested-envelope helper pattern. Every end-to-end case calls `Invoke-OrchestrationPreimplementationGateDecision` with an explicitly not-ready `-CheckpointRaw` and `-WorkspaceRoot 'C:\synthetic\root'`, with one exception: the named case 'allows a trailing-separator workspace root' supplies a synthetic root that ends in a trailing separator, so that algorithm step 5 is exercised. No filesystem access, no temporary files. The file must contain exactly these named `It` blocks (spec decision-table row in parentheses):
  - Allow cases: 'allows the absolute backslash checkpoint path' (row 3); 'allows absolute backslash twins of all seven checkpoint literals' (row 3 generalized, iterating the literal list); 'allows absolute forward-slash twins of all seven checkpoint literals' (row 4); 'allows a lower-case drive letter checkpoint path' (row 5); 'allows an upper-cased root segment checkpoint path' (row 6); 'allows a trailing-separator workspace root' (row 7); 'allows duplicated separators in the checkpoint path' (row 9); 'allows an identity dot segment in the checkpoint path' (row 10); 'allows a leading dot-slash checkpoint path' (row 15); 'allows mixed separators in the checkpoint path' (row 21); 'allows an absolute gated-extension evidence path under the feature tree' (row 16); 'allows the absolute twin of the feature spec path'.
  - Deny cases: 'denies an absolute production source write under the root' (row 17); 'denies an absolute non-literal file under the orchestration artifacts folder' (row 18); 'denies an absolute checkpoint basename outside its literal path' (row 19); 'denies a checkpoint-shaped path under a different root' (row 14); 'denies a segment-misaligned root prefix' (row 8); 'denies a parent-directory segment path' (row 11); 'denies a UNC path under a non-UNC root' (row 12); 'denies a drive-relative path' (row 20); 'denies an upper-cased docs tail under the root' (row 23).
  - Helper unit cases, calling `ConvertTo-WorkspaceRelativePath` directly and asserting the returned string: 'strips a lower-case drive letter root' (row 5); 'collapses duplicated separators before stripping' (row 9); 'removes identity dot segments before stripping' (row 10); 'strips a UNC root from a UNC path' (row 13); 'passes a drive-relative path through unchanged' (row 20); 'preserves tail case after stripping' (row 22); 'returns the input when the path equals the root' (row 24).
  Acceptance: the file exists, contains all 28 named `It` blocks listed above, and `Get-Content` piped to `Measure-Object -Line` reports fewer than 500 lines.
- [x] [P1-T2] [expect-fail] Run only the new facet file with `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1 -CI"` and capture the failing run at `FEATURE/evidence/regression-testing/fail-before-claude.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero), `Output Summary:` (failed-test count and representative failure reasons — the unfixed hook lacks both the helper function and the `-WorkspaceRoot` parameter). Acceptance: the artifact exists with all four fields and a non-zero `EXIT_CODE:`. If a genuine failing run proves structurally impossible, instead write `FEATURE/evidence/regression-testing/fail-before-exception.<timestamp>.md` with `WhyFailingRunImpossible:` and an alternative proof section; this fallback is not expected to be needed.

### Phase 2 — Implementation Batch 1 (Claude pair)

- [x] [P2-T1] Implement the fix in `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`: add the pure helper `ConvertTo-WorkspaceRelativePath` implementing the seven-step contract of `spec.md` (separator normalization; duplicate-separator collapse preserving a leading UNC `//`; identity dot-segment removal; fail-closed pass-through on any remaining `..` segment; trailing-slash trim on the root; segment-aligned `OrdinalIgnoreCase` root-prefix strip; unchanged pass-through otherwise); add `[string] $WorkspaceRoot = (Get-Location).Path` to `Invoke-OrchestrationPreimplementationGateDecision`; replace the two-line normalization at lines 259–260 with a single helper call. Do not modify `Test-ImplementationPath`, `Test-FeatureDocumentationOrEvidencePath`, `$script:CheckpointPaths`, `Test-PreparationModeDelegation`, `Test-ImplementationCommand`, `Test-OrchestrationReady`, the block-message text, or the entry point. Do not introduce the forbidden environment-literal strings scanned by `tests/scripts/claude-hooks/PreToolUsePayload.Contract.Tests.ps1` anywhere in the file, including comments, and do not invoke Python. Acceptance (positive AND negative pinned): in the Phase 1 facet file, the named test 'allows the absolute backslash checkpoint path' passes and the named test 'denies an absolute production source write under the root' passes; the hook file remains under 500 lines per `Get-Content` piped to `Measure-Object -Line`. Verification command: `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1 -CI"`.
- [x] [P2-T2] Copy the modified Claude hook byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`. Write `FEATURE/evidence/other/claude-pair-hash.<timestamp>.md` with `Timestamp:`, `Command:` (the `Get-FileHash -Algorithm SHA256` invocation over both paths), `EXIT_CODE:`, `Output Summary:` (the two hashes). Acceptance: the two SHA-256 hashes recorded in the artifact are identical.
- [x] [P2-T3] Run the Batch 1 toolchain loop to a clean single pass: `mcp__drm-copilot__run_poshqc_format`, then `mcp__drm-copilot__run_poshqc_analyze`, then `mcp__drm-copilot__run_poshqc_test`, restarting from format whenever any stage fails or changes files (if format changes the Claude hook, re-run P2-T2 to restore byte parity before continuing). Acceptance: all three stages pass in one uninterrupted pass with zero formatter changes, zero analyzer findings, and zero Pester failures — including all 28 Phase 1 facet tests passing and every assertion in the unmodified `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` passing.

### Phase 3 — Regression Tests, Codex Facet (must fail first)

- [x] [P3-T1] Reset the PowerShell batch-budget session state by deleting the current session's state file under `.claude/state/` for the PowerShell batch budget — the file matching `.claude/state/powershell-batch-budget.*.json` whose name carries the current session id — so Batch 2 begins with a zero production-file count, following the issue #535 execution precedent. The reset mechanism is documented in the comment-based help of `.claude/hooks/enforce-powershell-batch-budget.ps1`: the `.DESCRIPTION` block states that the running count is persisted per session under `.claude/state/` and that the session must explicitly reset the counter by deleting the state file before starting a new batch. Write `FEATURE/evidence/other/batch-budget-reset.<timestamp>.md` with `Timestamp:` and a one-line description of the reset performed, naming the deleted file. Acceptance: the artifact exists and Batch 2 file writes are not blocked by the batch-budget hook.
- [x] [P3-T2] Author the new facet test file `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1`, loading the Codex hook the same way `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` does, with all decision cases driven through an explicitly not-ready injected checkpoint and an injected synthetic workspace root (no filesystem access, no temporary files, no entry-point invocation). The file must contain exactly these named `It` blocks:
  - Decision cases: 'allows the absolute checkpoint path'; 'allows absolute twins of all seven checkpoint literals'; 'allows an absolute gated-extension evidence path under the feature tree'; 'denies an absolute production source write under the root'; 'denies a checkpoint-shaped path under a different root'; 'denies a parent-directory segment path'; 'denies a UNC path under a non-UNC root'; 'denies a drive-relative path'.
  - 'normalizes absolute apply-patch hunk paths' — build an apply-patch command payload whose hunk path is the absolute spelling of `artifacts/orchestration/orchestrator-state.json` under the synthetic root and assert that the decision function, called with a not-ready checkpoint and the synthetic `-WorkspaceRoot`, returns `allow` (exercises the P4-T1 `Test-ImplementationCommand` hunk-path normalization).
  - 'normalizes absolute mapped edit paths' — dot-source `codex-pretooluse-file-mapping.ps1` alongside the hook, build a `Write` payload whose `file_path` is the absolute backslash spelling of `artifacts/orchestration/orchestrator-state.json` under the synthetic root, map it with `ConvertTo-CodexFileEditInput`, and assert that `Invoke-OrchestrationPreimplementationGateDecision` called with the mapped raw payload, a not-ready checkpoint, and the synthetic `-WorkspaceRoot` returns `allow`. This mirrors the direct-mapping pattern at `tests/scripts/codex-hooks/codex-pretooluse-transport.Tests.ps1:233-252` and requires no filesystem access and no entry-point invocation.
  - Helper unit cases: 'strips the workspace root from an absolute path'; 'passes an outside-root path through unchanged'.
  Acceptance: the file exists, contains all 12 named `It` blocks, and reports fewer than 500 lines per `Get-Content` piped to `Measure-Object -Line`.
- [x] [P3-T3] [expect-fail] Run only the new Codex facet file with `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1 -CI"` and capture the failing run at `FEATURE/evidence/regression-testing/fail-before-codex.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero), `Output Summary:` (failed-test count). Acceptance: the artifact exists with all four fields and a non-zero `EXIT_CODE:`. The `fail-before-exception.<timestamp>.md` fallback of P1-T2 applies here identically if needed.

### Phase 4 — Implementation Batch 2 (Codex pair)

- [x] [P4-T1] Implement the fix in `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`: add the same pure helper `ConvertTo-WorkspaceRelativePath` (identical algorithm and behaviour to the Claude copy); add the optional `[string] $WorkspaceRoot = (Get-Location).Path` seam to the Codex decision function; replace the two-line normalization at lines 272–273 with a single helper call. The mapped Edit/Write loop at lines 321–331 is entry-point code and is NOT modified: once the decision function carries the `$WorkspaceRoot` parameter, its `(Get-Location).Path` default already supplies the workspace root on that call path, so mapped Edit/Write paths normalize identically with no edit. Do not source the root from the payload `cwd` field and do not alter the call at line 314. The Codex copy of `Test-ImplementationCommand` is explicitly carved out of the P2-T1 non-modification list for this task only: add `[string] $WorkspaceRoot = (Get-Location).Path` to its parameter block, apply `ConvertTo-WorkspaceRelativePath` to each hunk path harvested at lines 92–103, and pass `-WorkspaceRoot $WorkspaceRoot` from the decision function's command branch (line 277). The parameter is optional, so the existing call `Test-ImplementationCommand -Command $Command` at `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1:357` continues to bind unchanged. Spec Invariant 5 is unaffected in the Claude hook because that copy's `Test-ImplementationCommand` parses no hunk paths and is not modified. All other non-modification constraints of P2-T1 apply to the Codex copies of the exemption functions, literal list, message text, and entry point; no Python invocation. Acceptance (positive AND negative pinned): in the Phase 3 facet file, the named test 'allows the absolute checkpoint path' passes, the named test 'denies an absolute production source write under the root' passes, and the named test 'normalizes absolute apply-patch hunk paths' passes; the pre-existing parameterized case 'classifies an apply_patch add of a script as implementation command True' in `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` still passes; the hook file remains under 500 lines per `Get-Content` piped to `Measure-Object -Line`. Verification command: `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1 -CI"`.
- [x] [P4-T2] Copy the modified Codex hook byte-for-byte to `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`. Write `FEATURE/evidence/other/codex-pair-hash.<timestamp>.md` with `Timestamp:`, `Command:` (the `Get-FileHash -Algorithm SHA256` invocation over both paths), `EXIT_CODE:`, `Output Summary:` (the two hashes). Acceptance: the two SHA-256 hashes recorded in the artifact are identical.
- [x] [P4-T3] Run the Batch 2 toolchain loop to a clean single pass: `mcp__drm-copilot__run_poshqc_format`, then `mcp__drm-copilot__run_poshqc_analyze`, then `mcp__drm-copilot__run_poshqc_test`, restarting from format whenever any stage fails or changes files (if format changes the Codex hook, re-run P4-T2 to restore byte parity before continuing). Acceptance: all three stages pass in one uninterrupted pass with zero formatter changes, zero analyzer findings, and zero Pester failures — including all 12 Phase 3 facet tests passing and every assertion in the unmodified `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` passing.

### Phase 5 — Verification

- [x] [P5-T1] Verify the two pre-existing test files are unmodified: run `git diff --stat main -- tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1 tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1`. Write `FEATURE/evidence/qa-gates/preexisting-tests-unmodified.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the diff output is empty for both paths (both files unchanged relative to main).
- [x] [P5-T2] Verify the helper `ConvertTo-WorkspaceRelativePath` landed in exactly the intended files: run `git grep --untracked -l 'ConvertTo-WorkspaceRelativePath' -- '*.ps1'`. The `--untracked` flag is required because plain `git grep` searches tracked files only and the facet test files created by P1-T1 and P3-T2 are never staged by any task in this plan; `--untracked` was verified supported by the installed git in this worktree. Write `FEATURE/evidence/qa-gates/helper-scope.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the matched path list). Acceptance: the matched `.ps1` paths are exactly the four hook copies named in the Batch split section plus `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1` and `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1`, and no other `.ps1` file matches.
- [x] [P5-T3] Verify both push-down byte-parity relations with `Get-FileHash -Algorithm SHA256` over all four hook copies. Write `FEATURE/evidence/qa-gates/pushdown-pair-hashes.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (all four hashes). Acceptance: the hash of copy 1 equals the hash of copy 3, and the hash of copy 2 equals the hash of copy 4.
- [x] [P5-T4] Run the two push-down parity test modules: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`. Write `FEATURE/evidence/qa-gates/pushdown-pytest.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: `EXIT_CODE: 0` with zero test failures.
- [x] [P5-T5] Run the two source-scan contract suites: `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/PreToolUsePayload.Contract.Tests.ps1, tests/scripts/claude-runtime/enforcement-hooks-no-python-invocation.Tests.ps1 -CI"`. Write `FEATURE/evidence/qa-gates/source-scan-contracts.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 0` with zero failures (no forbidden environment literals and no Python invocation in the changed hooks).
- [x] [P5-T6] Verify line budgets: run `Get-Content` piped to `Measure-Object -Line` for each of the four hook copies and each of the two new facet test files. Write `FEATURE/evidence/qa-gates/line-counts.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the six line counts). Acceptance: every count is below 500.
- [x] [P5-T7] Re-run the repro as pass-after evidence: execute both facet files with `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1, tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate.path-normalization.Tests.ps1 -CI"`. Write `FEATURE/evidence/regression-testing/pass-after.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass counts, cross-referencing the fail-before artifacts from P1-T2 and P3-T3). Acceptance: `EXIT_CODE: 0`, all 40 facet tests pass — confirming the previously failing allow cases now pass and every named deny case (rows 8, 11, 12, 14, 17, 18, 19, 20, 23) still denies.

### Phase 6 — Final QC Loop and Closeout

The three QC command tasks below are unconditional; `SKIPPED` is not a valid outcome for any of them. If any stage fails or changes files, restart the loop at P6-T1 and re-record fresh artifacts for all three stages (re-running P2-T2 or P4-T2 first if a formatter change broke pair parity).

- [x] [P6-T1] Final QC formatting: run the `mcp__drm-copilot__run_poshqc_format` MCP tool. Write `FEATURE/evidence/qa-gates/final-poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 0` and zero files changed.
- [x] [P6-T2] Final QC linting: run the `mcp__drm-copilot__run_poshqc_analyze` MCP tool. Write `FEATURE/evidence/qa-gates/final-poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 0` and zero findings.
- [x] [P6-T3] Final QC testing in coverage mode: run the `mcp__drm-copilot__run_poshqc_test` MCP tool. Write `FEATURE/evidence/qa-gates/final-poshqc-test-coverage.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` containing numeric pass/fail counts and the numeric post-change line-coverage percentage (overall, plus per-file percentages for the four hook copies where itemized). Acceptance: `EXIT_CODE: 0`, zero failures, and a numeric coverage value (no placeholder), completing a clean single pass together with P6-T1 and P6-T2.
- [x] [P6-T4] Coverage delta and threshold verification: compare the P0-T5 baseline against the P6-T3 post-change coverage. Write `FEATURE/evidence/qa-gates/coverage-delta.<timestamp>.md` recording the numeric baseline percentage, the numeric post-change percentage, the per-file percentages for the four changed hook copies, and a changed-lines statement. Acceptance: post-change line coverage for the changed hook files is at or above 85%, and there is no coverage regression on changed lines relative to baseline. (Pester measures line coverage only; no branch-coverage condition applies to PowerShell per `.claude/rules/quality-tiers.md`.)
- [x] [P6-T5] Check off satisfied acceptance criteria in `FEATURE/spec.md` per `.claude/skills/acceptance-criteria-tracking/SKILL.md`: for each of the 17 items under `## Acceptance Criteria`, mark `[x]` only where the mapped verification in this plan has passed, one item at a time, preserving criterion text; leave any unverified item unchecked and document the gap. Report the required AC Status Summary (source file, total, checked, remaining). Acceptance: every criterion whose mapped plan tasks passed is checked in `spec.md`, and the AC Status Summary appears in the executor's completion report.

## Acceptance-criteria mapping (spec.md → plan tasks)

The 17 checkbox items under `## Acceptance Criteria` in `FEATURE/spec.md`, in document order, map to plan tasks as follows:

| AC | Criterion (abbreviated) | Delivering tasks | Verifying tasks |
| --- | --- | --- | --- |
| 1 | Row 3 defect case: absolute backslash checkpoint path allowed ('allows the absolute backslash checkpoint path') | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 2 | Absolute backslash and forward-slash twins of all seven literals allowed | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 3 | Confident-resolution variants allowed (rows 5, 6, 7, 9, 10, 15, 21; one named test per row) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 4 | Absolute feature-tree gated-extension path and absolute spec twin allowed (row 16) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 5 | Absolute production-source write denied (row 17) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 6 | Absolute non-literal orchestration artifact denied (row 18) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 7 | Absolute basename-match path denied (row 19) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 8 | Remaining fail-closed forms denied (rows 8, 11, 12, 14, 20, 23; one named test per row) | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 9 | Helper unit tests for rows 5, 9, 10, 13, 20, 22, 24 | P1-T1, P2-T1 | P2-T3, P5-T7 |
| 10 | Codex facet coverage (positive and negative) | P3-T2, P4-T1 | P4-T3, P5-T7 |
| 11 | Pre-existing test files unmodified and passing | all implementation tasks (non-modification constraint) | P5-T1, P2-T3, P4-T3, P6-T3 |
| 12 | Helper present in exactly the four hook copies | P2-T1, P2-T2, P4-T1, P4-T2 | P5-T2 |
| 13 | Both push-down parity relations hold and both parity test modules pass | P2-T2, P4-T2 | P5-T3, P5-T4 |
| 14 | Payload-contract and no-Python source-scan suites pass | P2-T1, P4-T1 (constraints) | P5-T5 |
| 15 | Full PowerShell toolchain clean in a single pass | P2-T3, P4-T3 | P6-T1, P6-T2, P6-T3 |
| 16 | Line coverage at or above 85% with no changed-line regression | P1-T1, P3-T2 (test coverage) | P0-T5, P6-T3, P6-T4 |
| 17 | Every changed or added `.ps1` file under 500 lines | P2-T1, P3-T2, P4-T1, P1-T1 | P5-T6 |

## Out-of-scope guard (restated from spec)

No file other than the four hook copies and the two new facet test files may change. Specifically unchanged: `.claude/lib/hook-payload/HookPayload.psm1`, `enforce-evidence-locations.ps1`, `enforce-feature-folder-order.ps1`, `.claude/settings.json`, everything under `.claude/rules/` and `.github/instructions/`, the block-message wording, and checkpoint-content validation (`Test-OrchestrationReady`).
