# Remediation Plan: planner-hook-em-dash-mismatch-357 (Issue #357) — Cycle 3

**Plan Timestamp:** 2026-07-17T18-00
**Feature Folder:** `docs/features/active/planner-hook-em-dash-mismatch-357`
**Triggering Input:** `docs/features/active/planner-hook-em-dash-mismatch-357/remediation-inputs.2026-07-17T18-00.md`
**Trigger:** S9 CI-failure handling on PR #358 (required check `quality-checks7 / Code Quality & Tests (3.10)` failed at head SHA `5d1431ae02939c372b34a5cbe33985fd8754d871`); not triggered by a `feature-review` finding. No new policy-audit/code-review/feature-audit artifacts precede this cycle. Prior cycle's clean audits remain: `docs/features/active/planner-hook-em-dash-mismatch-357/{policy-audit,code-review,feature-audit}.2026-07-17T17-15.md`.

**Scope of this cycle:** Implement both required/blocking items from the Enumerated Fix List in `remediation-inputs.2026-07-17T18-00.md`: (1) sync the bundled mirror `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` to be byte-for-byte identical to the canonical `.claude/hooks/validate-planner-output.ps1` (including its leading UTF-8 BOM, the em-dash phase-heading regex and error message, the `.DESCRIPTION` docstring's em-dash reference, and the strict-mode-safe `$null -ne $payload.PSObject.Properties['output']` check); (2) re-run the full toolchain and confirm no regression.

**Change budget for this cycle:** exactly one file — `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` (bundled resource mirror; made byte-identical to the canonical copy). No other file is modified.

**Explicitly out of scope for this cycle (verbatim from `remediation-inputs.2026-07-17T18-00.md`):**
- Do not modify `.claude/hooks/validate-planner-output.ps1` itself (already correct); only the bundled mirror changes.
- Do not modify any other file in the bundled `extensions/drm-copilot/resources/claude-customizations/` tree.
- Do not widen the change budget beyond this one bundled-mirror file.
- Do not run `mcp__drm-copilot__push_down_claude_customizations` (copies the bundle INTO a workspace — the wrong direction for this fix) or `mcp__drm-copilot__run_codex_native_converter`.

The bundled mirror file MUST be updated by a raw byte-for-byte file copy from the canonical source (for example `Copy-Item -LiteralPath '.claude/hooks/validate-planner-output.ps1' -Destination 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1' -Force`), never by a text-editing tool, because a text-editing tool may re-encode the file and silently drop the leading UTF-8 BOM that this cycle is required to preserve. No PoshQC formatter or analyzer autofix step may run against the bundled mirror file at any point in this plan, because either could rewrite its bytes and break the byte-identity this cycle establishes.

---

### Phase 0 — Remediation Baseline Capture (Cycle 3)

- [x] [P0-T1] Read `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/powershell.md` in that policy-read order, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/phase0-instructions-read-remediation3.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three required fields populated and lists all four files in the stated order.
- [x] [P0-T2] [expect-fail] Run `python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -q` prior to the mirror-sync fix, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/pytest-bundle-parity-baseline-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` quoting the `AssertionError: Bundle content differs from repo for: .claude/hooks/validate-planner-output.ps1` failure. Acceptance: `EXIT_CODE` is non-zero and the recorded failure message names `.claude/hooks/validate-planner-output.ps1`.
- [x] [P0-T3] Run `(Get-FileHash -Algorithm SHA256 -LiteralPath '.claude/hooks/validate-planner-output.ps1').Hash` and `(Get-FileHash -Algorithm SHA256 -LiteralPath 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1').Hash`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/hash-diff-baseline-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` stating both SHA256 values and confirming they differ prior to the fix. Acceptance: the two recorded hash values are unequal.
- [x] [P0-T4] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` (both unchanged this cycle), and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-format-baseline-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE` is zero and no file is reported modified.
- [x] [P0-T5] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to the same two files as P0-T4, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-analyze-baseline-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE` is zero with zero findings.
- [x] [P0-T6] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-test-baseline-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` restating the pass/fail test count and the numeric line coverage percentage for `.claude/hooks/validate-planner-output.ps1` carried forward from `evidence/qa-gates/coverage-delta-remediation2.md`. Acceptance: `EXIT_CODE` is zero and all Pester tests pass.

---

### Phase 1 — Sync Bundled Mirror to Canonical Byte-For-Byte

- [x] [P1-T1] Copy the exact byte content of `.claude/hooks/validate-planner-output.ps1` onto `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` using a raw binary file copy (for example `Copy-Item -LiteralPath '.claude/hooks/validate-planner-output.ps1' -Destination 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1' -Force`), preserving the leading UTF-8 BOM. Do not use a text-editing tool for this step. Acceptance: `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` exists and its byte length equals the byte length of `.claude/hooks/validate-planner-output.ps1`.
- [x] [P1-T2] Run `(Get-FileHash -Algorithm SHA256 -LiteralPath '.claude/hooks/validate-planner-output.ps1').Hash -eq (Get-FileHash -Algorithm SHA256 -LiteralPath 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1').Hash`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/hash-diff-post-fix-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` stating the boolean result. Acceptance: the recorded result is `True`.
- [x] [P1-T3] Run `python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -q`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/pytest-bundle-parity-final-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE` is `0` and the test reports passed.

---

### Phase 2 — Final QA and Regression Verification

- [x] [P2-T1] Run `python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/pytest-resource-contracts-file-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the pass/fail test count for the file. If any test fails, remediate and restart the loop from P2-T1. Acceptance: `EXIT_CODE` is `0` and all tests in the file pass.
- [x] [P2-T2] Run `python -m pytest -q` (full suite) to confirm no other file in this feature's diff has the same bundle-mirror drift, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/pytest-full-suite-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the total pass/fail count. If any test fails, remediate and restart the loop from P2-T1. Acceptance: `EXIT_CODE` is `0`.
- [x] [P2-T3] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1` and `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-format-remediation3-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero or the run modifies either file, restart the full toolchain loop from P2-T1. Acceptance: `EXIT_CODE` is `0` and neither file is modified.
- [x] [P2-T4] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to the same two files as P2-T3, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-analyze-remediation3-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero, remediate the reported finding(s) and restart the full toolchain loop from P2-T1. Acceptance: `EXIT_CODE` is `0` with zero findings.
- [x] [P2-T5] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-test-remediation3-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including the pass/fail test count and the numeric line coverage percentage for `.claude/hooks/validate-planner-output.ps1`, confirming it is unchanged from the P0-T6 baseline. If any test fails or the run modifies files, restart the full toolchain loop from P2-T1. Acceptance: `EXIT_CODE` is `0`, all tests pass, and coverage matches the P0-T6 baseline.
- [x] [P2-T6] Run `(Get-FileHash -Algorithm SHA256 -LiteralPath '.claude/hooks/validate-planner-output.ps1').Hash -eq (Get-FileHash -Algorithm SHA256 -LiteralPath 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1').Hash`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/hash-diff-post-toolchain-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming the two files remain byte-identical after the Phase 2 toolchain runs (proving no formatter/analyzer step altered the bundled mirror). Acceptance: the recorded result is `True`.
- [x] [P2-T7] Run `git diff --stat` against the merge-base for the repository, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/change-scope-check-remediation3.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming the only non-evidence file changed is `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1`, and that `.claude/hooks/validate-planner-output.ps1` and every other file under `extensions/drm-copilot/resources/claude-customizations/` remain unmodified. Acceptance: the diff stat lists exactly one non-evidence production/resource file, matching the declared change budget.
