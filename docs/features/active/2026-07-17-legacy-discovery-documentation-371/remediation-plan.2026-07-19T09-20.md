# Remediation Plan — legacy-discovery-documentation (#371), Remediation Cycle 1

- Timestamp: 2026-07-19T09-20
- Issue: #371
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-documentation-371/issue.md`, `- Work Mode: full-feature`) — AC source files are `spec.md` and `user-story.md`, per `acceptance-criteria-tracking`.
- Branch: `feature/legacy-discovery-documentation-371`
- Reviewed commit: `de2cc7f9`
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-documentation-371/remediation-inputs.2026-07-19T09-20.md` (single Blocking Finding 1 — unsubstantiated schema/init-template distribution claim in `consumer-onboarding.md` item 2). No scope beyond that document is authorized, with one narrow exception: Phase 0's grep sweep (`P0-T3`) also checks for the same inaccurate claim repeated elsewhere within `consumer-onboarding.md` itself (not just other pages), because the Onboarding Sequence section of that same file independently restates the identical unverified claim in different words; if found, correcting it is treated as the same underlying finding, confined to the same file, not new scope.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-documentation-371`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`, `evidence/other/`). No `artifacts/`-rooted evidence path is permitted. Existing evidence files from the original delivery cycle (`evidence/baseline/*`, `evidence/qa-gates/structural-completeness.*`, `evidence/qa-gates/link-resolution.*`, `evidence/qa-gates/domain-neutrality.*`, `evidence/qa-gates/naming-collision.*`, `evidence/qa-gates/end-state.*`, `evidence/other/integration-reconciliation.*`, `evidence/other/optional-contract-test-decision.*`) are read-only historical inputs for this cycle and MUST NOT be edited or deleted. All new artifacts for this cycle use the `r1c1-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root unless a task states otherwise. Shell is PowerShell unless a task states otherwise.
- This is a documentation-only remediation: no Python, PowerShell, TypeScript, or C# production or test file is in scope. Phase 0 therefore includes an explicit scope-confirmation task (`P0-T4`) in place of a language-toolchain baseline; no format/lint/type-check/test command applies to this cycle's changes.

## Ground-Truth Contracts (read-only inputs; do not modify)

- **Affected file:** `docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`.
- **Item 2, as currently authored (lines 24–35):** begins `2. **Schemas and initialization templates — distributed as Python package data via the MCP-server npm package, not pushed down.**` and asserts the seven schemas and the init templates "reach a consumer repository as Python source/data through the `@danmoisan/drm-copilot-mcp` npm package." Independently re-verified false: `packages/mcp-server/package.json`'s `files` field is `["out/mcp-server.js", "resources"]`; `packages/mcp-server/prepack.cjs`'s `shouldCopy()` excludes every `.py` file and every `scripts/`-segment path when populating `resources/` from `extensions/drm-copilot/resources/`; neither `schemas/discovery/v1/` nor `docs/discovery/templates/` appears anywhere under `extensions/drm-copilot/resources/` or `packages/mcp-server/**`.
- **Onboarding Sequence step 2 (lines 64–66), as currently authored:** `2. The consumer repository adds \`@danmoisan/drm-copilot-mcp\` (or an equivalent workspace dependency resolving the discovery schemas and templates) so its own tooling can reach the discovery schemas and initialization templates.` This restates the same unverified claim within the same file and must be corrected alongside item 2 for internal consistency.
- **Source of the classification decision (do not modify; cross-reference only):** `docs/features/active/2026-07-17-legacy-discovery-publishing-372/spec.md`, section `## Schema/Init-Template Placement — Resolved` (lines 132–161), which documents `Decision: scripts-non-mirrored` — a classification of `schemas/` and `docs/discovery/templates/` as outside the byte-identical `.claude`/`.codex` mirror contract. That section documents a design classification only; it does not itself deliver a distribution mechanism, and no feature in the merged epic has implemented one.
- **AC items affected (currently `[x]`, to be unchecked in Phase 0 and re-checked in Phase 2 only after independent re-verification):** `spec.md` AC6 (consumer-onboarding page item), AC9 (provisional-content handling), AC10 (reconciliation pass); `user-story.md` AC4 (consumer-onboarding path documented). Ordinal positions confirmed by reading both files' `## Acceptance Criteria` sections in full before drafting this plan.
- **Not in scope:** `docs/engineering/legacy-discovery-and-parity/README.md`, `domain-profile.md`, `artifacts-and-schemas.md`, `running-the-workflow.md`, `workflow.md` — none of these repeats the distribution claim (confirmed by pre-plan grep; `P0-T3` re-confirms this formally as an executable task).

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read in order. Acceptance: the artifact exists with all three fields populated and lists exactly these four files in this order.
- [x] [P0-T2] Capture the verbatim current wording of the affected passages: run `Get-Content -Path docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md` and extract lines 10–36 (the "What Is Delivered, and How" section, including item 2) and lines 58–66 (the Onboarding Sequence section, including step 2). Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase0-current-wording.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that quotes both passages verbatim with their line numbers. Acceptance: the artifact's quoted text matches the file's current on-disk content byte-for-byte for both line ranges.
- [x] [P0-T3] Confirm no other page in the six-page doc set repeats the false distribution claim, and confirm whether it is repeated a second time within `consumer-onboarding.md` itself: run `grep -n -E "Python source/data|Python package data" docs/engineering/legacy-discovery-and-parity/*.md` and `grep -n -E "schemas/discovery|docs/discovery/templates" docs/engineering/legacy-discovery-and-parity/*.md`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase0-doc-set-grep.<TS>.md` with `Timestamp:`, both `Command:` entries, `EXIT_CODE:`, and an `Output Summary:` that classifies every match as either (a) one of the two known `consumer-onboarding.md` passages being remediated in Phase 1, or (b) a schema/template path reference elsewhere in the doc set that does not assert or imply a current delivery mechanism (for example `artifacts-and-schemas.md`'s schema-name table, `domain-profile.md`'s `dev.discovery.init` template-scaffolding sentence). Acceptance: the artifact enumerates every match found across all six pages and confirms zero matches outside the two named `consumer-onboarding.md` passages assert or imply that any package or tool currently delivers the schemas/templates to a consumer repository.
- [x] [P0-T4] Confirm this remediation cycle is documentation-only and no language-toolchain baseline applies: run `git status --porcelain` and `git diff --stat HEAD -- docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`, confirming both are empty prior to any edit. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase0-scope-confirmation.<TS>.md` with `Timestamp:`, both `Command:` entries, `EXIT_CODE:`, and an `Output Summary:` stating that no `.py`, `.ps1`, `.ts`, or `.cs` file is in scope for this cycle and that this determination is consistent with the "Non-Blocking Observations" section of `remediation-inputs.2026-07-19T09-20.md` (no coverage/toolchain remediation required). Acceptance: the artifact records both commands as clean/empty and states the no-toolchain-baseline determination explicitly.
- [x] [P0-T5] Uncheck the four AC items affected by the inaccurate claim, pending independent re-verification: in `docs/features/active/2026-07-17-legacy-discovery-documentation-371/spec.md`, change AC6, AC9, and AC10 in the `## Acceptance Criteria` section from `- [x]` to `- [ ]`; in `docs/features/active/2026-07-17-legacy-discovery-documentation-371/user-story.md`, change AC4 in its `## Acceptance Criteria` section from `- [x]` to `- [ ]`. Do not alter any criterion's text or any other checkbox in either file. Acceptance: exactly these four checkboxes read `- [ ]` after the edit; every other checkbox in both files is unchanged; no criterion text differs from before the edit (verify via `git diff -- docs/features/active/2026-07-17-legacy-discovery-documentation-371/spec.md docs/features/active/2026-07-17-legacy-discovery-documentation-371/user-story.md` showing exactly four one-character-class changes, `x` to ` `, and no other diff hunks).

### Phase 1 — Rewrite the Affected Section (Branch A)

- [x] [P1-T1] Edit item 2 of the "What Is Delivered, and How" section in `docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md` (the passage captured verbatim in `P0-T2`). Replace it with corrected text that satisfies all of the following elements: (a) states plainly that the seven discovery JSON schemas (`schemas/discovery/v1/*.schema.json`) and the initialization templates (`docs/discovery/templates/`) currently have **no consumer-facing distribution mechanism**; (b) states they exist only in the `drm-copilot` repository itself, at `schemas/discovery/v1/` and `docs/discovery/templates/`; (c) cross-references `docs/features/active/2026-07-17-legacy-discovery-publishing-372/spec.md`, section "Schema/Init-Template Placement — Resolved", by name, as the source of the `scripts-non-mirrored` classification decision; (d) does not state or imply that `@danmoisan/drm-copilot-mcp` or any other npm package currently ships these two asset kinds; (e) marks the gap as an open/planned item rather than landed behavior. Acceptance: the rewritten passage satisfies elements (a) through (e), verified by re-reading the edited section against this checklist.
- [x] [P1-T2] Edit the introductory sentence of the "What Is Delivered, and How" section in the same file (currently: "The capability's assets are distributed to a consumer repository through two different mechanisms, depending on the asset's kind.") so it no longer implies both asset kinds have a delivered distribution mechanism. Acceptance: the sentence distinguishes the one delivered push-down mechanism (item 1) from the currently-undelivered schema/template gap (item 2) rather than describing both as equally "distributed."
- [x] [P1-T3] Edit Onboarding Sequence step 2 in the same file (the passage captured verbatim in `P0-T2`, lines 64–66) to remove the implication that adding `@danmoisan/drm-copilot-mcp` delivers the discovery schemas/templates. Replace with text describing the currently available path (manual retrieval from the `drm-copilot` repository) and cross-referencing item 2's gap statement. Acceptance: step 2 no longer states or implies that `@danmoisan/drm-copilot-mcp` resolves or delivers the discovery schemas/templates; it accurately describes the current manual path and references item 2.
- [x] [P1-T4] Grep-verify the corrected file contains no remaining assertion that a package currently ships the schemas or templates: run `grep -n -i -E "Python source/data|Python package data|resolving the discovery schemas" docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase1-post-edit-grep.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact records `EXIT_CODE: 1` (zero matches) for the retired claim language, or, if any match survives, the `Output Summary:` demonstrates it is a quoted description of the "scripts-non-mirrored" classification decision (attributed to #372's spec) rather than a present-tense delivery claim about the current file's own asserted behavior.
- [x] [P1-T5] Re-read the fully edited `docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md` end to end and confirm the "What Is Delivered, and How" section, the introductory sentence (`P1-T2`), and the Onboarding Sequence section (`P1-T3`) are mutually consistent — no remaining sentence in the file states or implies the gap identified in `P1-T1` is closed. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-phase1-internal-consistency-check.<TS>.md` with `Timestamp:`, `Output Summary:` listing each of the three edited passages and confirming they agree. Acceptance: the artifact confirms zero contradicting statements remain in the file.

### Phase 2 — Re-verification

- [x] [P2-T1] Independently re-verify `packages/mcp-server/package.json`'s `files` field directly (not against `#372`'s spec.md prose): run `grep -n -A3 '"files"' packages/mcp-server/package.json`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-package-json-files-field.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the exact `files` array and confirming neither `schemas/discovery` nor `docs/discovery/templates` appears in it. Acceptance: the artifact records the two-entry array (`out/mcp-server.js`, `resources`) verbatim and states neither asset tree is listed.
- [x] [P2-T2] Independently re-verify `packages/mcp-server/prepack.cjs`'s exclusion filter directly: run `grep -n -E "\.py|scripts" packages/mcp-server/prepack.cjs`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-prepack-exclusion-filter.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` quoting the `shouldCopy()` exclusion logic and confirming it contains no allow-list entry or special case that copies `schemas/discovery/v1/` or `docs/discovery/templates/`. Acceptance: the artifact confirms the `.py`-suffix exclusion and the `scripts/`-segment exclusion are the only filters, with no code path in the file that copies either asset tree.
- [x] [P2-T3] Independently re-verify `extensions/drm-copilot/resources/**` contains neither asset tree: run `Get-ChildItem -Recurse -Path extensions/drm-copilot/resources -Filter "*schema*"` and `Get-ChildItem -Recurse -Path extensions/drm-copilot/resources | Select-String -Pattern "templates"`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-resources-tree-check.<TS>.md` with `Timestamp:`, both `Command:` entries, `EXIT_CODE:`, `Output Summary:` recording zero hits for `schemas/discovery` content and confirming any `templates` hits found are pre-existing, unrelated directories (for example scaffolding `feature-templates/` or PowerShell snippet `templates/` folders), not discovery init templates. Acceptance: the artifact confirms zero occurrences of either asset tree under `extensions/drm-copilot/resources`.
- [x] [P2-T4] Re-run the domain-neutrality check scoped to the corrected file only: run `grep -ni -E "TaskMaster|TMW|Outlook|VSTO|email|task-management" docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-domain-neutrality-recheck.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` comparing the resulting match set against the original disposition table in `evidence/qa-gates/domain-neutrality.2026-07-19T07-18.md` and confirming the Phase 1 edit introduced no new domain-specific match outside the existing Worked Example section. Acceptance: the artifact confirms the match set for `consumer-onboarding.md` is unchanged in kind and classification from the original disposition.
- [x] [P2-T5] Re-run the naming-collision check scoped to the corrected file only: run `grep -ni -E "modernize-|legacy-analyst|business-rules-extractor|architecture-critic|version-delta-analyst|scaffolder|security-auditor|test-engineer" docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-naming-collision-recheck.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact confirms zero matches (`EXIT_CODE: 1`).
- [x] [P2-T6] Re-run the relative-link resolution check scoped to the corrected file only: run `grep -n "](" docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-link-resolution-recheck.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming every link target still resolves to a present file in the doc set (cross-checked against the original inventory in `evidence/qa-gates/link-resolution.2026-07-19T07-14.md`) and that the Phase 1 edit added no new link target. Acceptance: the artifact confirms the link set for `consumer-onboarding.md` is unchanged from the original inventory.
- [x] [P2-T7] Write a reconciliation addendum documenting the correction without modifying the original evidence record: write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/other/r1c1-reconciliation-addendum.<TS>.md` stating that the original reconciliation pass's "Pack-Manifest Placement Decision" disposition (`evidence/other/integration-reconciliation.2026-07-19T07-30.md`) relied on `legacy-discovery-publishing-372`'s spec.md prose describing a design classification rather than independently verifying `packages/mcp-server/package.json`, `packages/mcp-server/prepack.cjs`, and `extensions/drm-copilot/resources/**`; that this remediation cycle's independent verification (`P2-T1`–`P2-T3`) found no consumer-facing distribution mechanism exists for either asset kind; and that the original evidence file remains the unmodified historical record, with this addendum superseding only its "Pack-Manifest Placement Decision" disposition. Acceptance: the addendum artifact exists, references the original evidence file path and the `P2-T1`–`P2-T3` evidence file paths, and the original evidence file is confirmed unmodified (`git diff -- docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/other/integration-reconciliation.2026-07-19T07-30.md` is empty).
- [x] [P2-T8] Re-check `spec.md` AC6 (the consumer-onboarding-page criterion) from `- [ ]` to `- [x]` only after `P1-T1`–`P1-T5` and `P2-T1`–`P2-T6` confirm the corrected page accurately documents both the delivered push-down mechanism (item 1) and the schema/template gap (item 2) without asserting undelivered behavior as landed. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-spec-ac6-recheck.<TS>.md` with `Timestamp:`, the quoted AC6 text, and an `Output Summary:` naming the specific evidence artifacts (`P1-T1`–`P1-T5`, `P2-T1`–`P2-T6`) that ground the re-check. Acceptance: `spec.md` AC6 reads `- [x]` and the evidence artifact documents the basis.
- [x] [P2-T9] Re-check `spec.md` AC9 (provisional-content handling) from `- [ ]` to `- [x]` only after confirming the corrected item 2 (`P1-T1`) genuinely marks the schema/template gap as planned/open per the doc set's provisional-content convention, rather than asserting landed behavior. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-spec-ac9-recheck.<TS>.md` with `Timestamp:`, the quoted AC9 text, and an `Output Summary:` referencing the corrected item 2 wording. Acceptance: `spec.md` AC9 reads `- [x]` and the evidence artifact documents the basis.
- [x] [P2-T10] Re-check `spec.md` AC10 (reconciliation pass) from `- [ ]` to `- [x]` only after `P2-T7`'s addendum and `P2-T1`–`P2-T3`'s independent verification together satisfy the criterion. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-spec-ac10-recheck.<TS>.md` with `Timestamp:`, the quoted AC10 text, and an `Output Summary:` referencing the `P2-T7` addendum and `P2-T1`–`P2-T3` evidence. Acceptance: `spec.md` AC10 reads `- [x]` and the evidence artifact documents the basis.
- [x] [P2-T11] Re-check `user-story.md` AC4 (consumer-onboarding path documented) from `- [ ]` to `- [x]` only after the same evidence chain as `P2-T8` confirms accuracy. Write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-user-story-ac4-recheck.<TS>.md` with `Timestamp:`, the quoted AC4 text, and an `Output Summary:` naming the grounding evidence artifacts. Acceptance: `user-story.md` AC4 reads `- [x]` and the evidence artifact documents the basis.

### Phase 3 — Commit-Readiness Capture

- [x] [P3-T1] Run `git status --porcelain` and write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-git-status.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing every changed/untracked file path. Acceptance: the recorded file list contains only `docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md`; `docs/features/active/2026-07-17-legacy-discovery-documentation-371/spec.md`; `docs/features/active/2026-07-17-legacy-discovery-documentation-371/user-story.md`; this remediation plan file; and new files under `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/remediation-baseline/r1c1-*`, `evidence/qa-gates/r1c1-*`, and `evidence/other/r1c1-*` — no other file appears.
- [x] [P3-T2] Run `git diff --stat -- docs/engineering/legacy-discovery-and-parity/consumer-onboarding.md docs/features/active/2026-07-17-legacy-discovery-documentation-371/spec.md docs/features/active/2026-07-17-legacy-discovery-documentation-371/user-story.md` and write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-diff-stat.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the per-file insertion/deletion counts. Acceptance: the artifact records nonzero changes for `consumer-onboarding.md` and changes in `spec.md`/`user-story.md` limited to the four AC checkbox toggles (no unrelated line changes).
- [x] [P3-T3] Cross-check the `P3-T1` changed-file list against the authorized scope (the ground-truth contracts section of this plan plus this plan's own evidence namespace) and write `docs/features/active/2026-07-17-legacy-discovery-documentation-371/evidence/qa-gates/r1c1-scope-creep-check.<TS>.md` with `Timestamp:`, `Output Summary:` enumerating the authorized file set and confirming the `P3-T1` change list is a subset of it. Acceptance: the artifact confirms zero files outside the authorized set were modified.

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; `consumer-onboarding.md` item 2 and the Onboarding Sequence step 2 no longer assert or imply that the seven discovery schemas or the initialization templates are currently distributed to consumer repositories via any package, and instead state plainly that no consumer-facing distribution mechanism exists today, cross-referencing `legacy-discovery-publishing-372`'s spec.md "Schema/Init-Template Placement — Resolved" section as the source of the classification decision only; the domain-neutrality, naming-collision, and link-resolution checks re-run clean on the corrected file; `spec.md` AC6, AC9, AC10 and `user-story.md` AC4 are re-checked only on the strength of independent re-verification against `packages/mcp-server/package.json`, `packages/mcp-server/prepack.cjs`, and `extensions/drm-copilot/resources/**`; and Phase 3 confirms the changed-file set is limited to the authorized scope with no unintended scope creep.
