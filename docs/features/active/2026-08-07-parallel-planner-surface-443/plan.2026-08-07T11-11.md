# parallel-planner-surface — Plan

- **Issue:** #443
- **Parent:** epic `parallel-orchestration` (`docs/features/epics/parallel-orchestration/epic.md`, feature F4, wave 2)
- **Owner:** drmoisan
- **Last Updated:** 2026-08-08
- **Status:** Revised (contingency fired; preflight revision iteration 2 applied; pending preflight re-validation)
- **Version:** 2.1
- **Work Mode:** full-feature (marker verified in `docs/features/active/2026-08-07-parallel-planner-surface-443/issue.md`)

## Revision Record (v1.0 → v2.0)

Phases 0 and 1 executed under v1.0 and their evidence is on disk. Task [P1-T3] evaluated the spec-R5 kickoff-validation contingency and returned verdict `fired`: F3 (`parallel-schema-validators`, #444) landed WITHOUT `scripts/dev_tools/parallel_kickoff_contract.py` and WITHOUT the `artifact_type: "parallel-kickoff"` wiring. This is the adjudicated, expected outcome, not a defect — `docs/features/epics/parallel-orchestration/epic.md` section "Planner Adjudication: the kickoff-contract boundary (F3 / F4)" (line 161) assigns the module and the wiring to F4 by producer ownership, and `.claude/rules/parallel-orchestration.md` section "F3 Scope Boundary — kickoff contract deferred to F4" records the same boundary from F3's side. F3's Decision 3.2-A pinned its MCP surface to exactly two `artifact_type` values as an acceptance criterion.

This revision adds the production deliverables the contingency requires, applies the Phase 1 contract supersessions to the skill-authoring acceptance criteria, and adds the F4-owned obligations F3's landed text assigns to the planner surface.

**Phase-numbering choice.** New work is inserted as Phase 2 (kickoff-contract module) and Phase 3 (`parallel-kickoff` wiring); the previously-numbered Phases 2 through 8 are renumbered to 4 through 10. Phase 0 and Phase 1 task IDs are untouched and their `[x]` state is preserved verbatim; the final QA loop remains the last phase. Insertion (rather than appending after the old Phase 8) was chosen because the skill's `## Kickoff Artifact` section must cite the delivered module as landed rather than as a pending recommendation, and the contract test in Phase 6 asserts against that text, so the module must precede both. All intra-plan task-ID cross-references below use the revised numbering.

**Revision 2.1 (preflight iteration 2).** Eight executor-identified defects were corrected without touching Phase 0 or Phase 1. The three TypeScript gates in Phase 10 were retargeted from the repository root to the `extensions/drm-copilot` package because the root `format`, `lint`, and `typecheck` scripts do not reach `extensions/drm-copilot/**` and would have recorded a vacuous `EXIT_CODE: 0`; a dependency-install task was inserted as [P10-T5] because `extensions/drm-copilot/node_modules` is absent in a fresh worktree, renumbering the former [P10-T5]–[P10-T10] to [P10-T6]–[P10-T11]; the Jest coverage gate was pinned explicitly to the ROOT `test:unit:coverage` script; conditional 500-line split instructions were added to [P2-T8] and [P3-T9]; the [P3-T9] parity mechanism was constrained to hardcoded transcribed error strings with no external-process dependency; [P3-T1] was given a concrete committed fixture path; [P3-T10] was moved from `npx jest` to the repository-canonical `node run-jest.cjs` wrapper; and two factual slips were corrected.

**Executed Phase 0/1 evidence carried forward:**

- Baseline line coverage **91.72%** (12266 / 13373), branch coverage **83.58%** (4124 / 4934), 2886 tests passed — `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/pytest-coverage-baseline.2026-08-08T13-56.md`. These are the no-regression reference values for Phase 10.
- Upstream reconciliation and contingency verdict — `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.2026-08-08T13-56.md`.

## Required References

- Spec (AC source 1): `docs/features/active/2026-08-07-parallel-planner-surface-443/spec.md` (resolutions R1–R11, 19 acceptance criteria at revision time; Phase 9 adds three)
- User story (AC source 2): `docs/features/active/2026-08-07-parallel-planner-surface-443/user-story.md` (8 operator acceptance criteria)
- Phase 1 findings (authoritative for landed upstream contracts): `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.2026-08-08T13-56.md`
- Research: `docs/features/active/2026-08-07-parallel-planner-surface-443/research/parallel-planner-surface.2026-08-07T12-30.md`
- Accepted design (§N references): `docs/research/2026-08-07-parallel-orchestration-design-research.md`
- Epic manifest and kickoff-boundary adjudication: `docs/features/epics/parallel-orchestration/epic.md`
- Landed F3 invariants: `.claude/rules/parallel-orchestration.md`
- Mirror analogues: `scripts/dev_tools/epic_kickoff_contract.py`, `tests/scripts/dev_tools/test_epic_kickoff_contract.py`, `extensions/drm-copilot/src/lib/validate/epic-kickoff-artifact.ts`, `extensions/drm-copilot/test/lib/validate/epic-kickoff-artifact.test.ts`
- General code change policy: `.claude/rules/general-code-change.md`; unit test policy: `.claude/rules/general-unit-test.md`; Python: `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/self-explanatory-code-commenting.md`; TypeScript: `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`

All work must comply with these policies; their content is not duplicated here.

## Scope Summary

Deliverables:

1. `.claude/agents/parallel-planner.md` — agent persona per spec Deliverable 1 (R7).
2. `.claude/skills/parallel-plan/SKILL.md` — planning skill per spec Deliverable 2 (R1, R2, R3, R4, R5, R6, R8).
3. `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py` — pytest contract test per spec R9.
4. `scripts/dev_tools/parallel_kickoff_contract.py` — kickoff-prompt contract module (spec R5 contingency, fired; epic-manifest adjudication assigns it to F4), plus a `_`-prefixed helper module `scripts/dev_tools/_parallel_kickoff_tables.py` if and only if the single-module form would exceed 500 lines.
5. The minimal additive `artifact_type: "parallel-kickoff"` wiring across five registration surfaces: the CLI subparser and dispatch branch in `scripts/dev_tools/validate_orchestration_artifacts.py`; `VALID_ARTIFACT_TYPES` in `extensions/drm-copilot/src/mcp-tool-inputs.ts`; the `artifact_type` enum in `extensions/drm-copilot/src/mcp-tool-definitions.ts`; the `artifact_type` enum in `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`; and the dispatch case in `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts`.
6. `extensions/drm-copilot/src/lib/validate/parallel-kickoff-artifact.ts` — TypeScript parity core module (determination recorded below).
7. `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` and `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact.test.ts` — unit tests for deliverables 4 and 6, plus updates to the five existing tests that currently assert `parallel-kickoff` is unsupported.
8. Bundled-payload mirror obligations verified during planning against the real test suite (see the Phase 7 preamble): byte-identical copies of deliverables 1 and 2 at `extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-planner.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-plan/SKILL.md`, plus registration of both `.claude`-relative paths in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`.

**TypeScript determination (explicit).** The TypeScript surface MUST change, and a TypeScript parity core module IS required. Verified during this revision: the MCP tool `validate_orchestration_artifacts` dispatches through the TypeScript function `validateArtifact` in `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts` (call site `extensions/drm-copilot/src/lib/validate/validate-orchestration-service-call.ts` line 76); it does not shell out to the Python CLI. The `epic-kickoff` analogue is handled by a full TypeScript parity port at `extensions/drm-copilot/src/lib/validate/epic-kickoff-artifact.ts`, dispatched at `orchestration-artifacts.ts` line 260. Adding `parallel-kickoff` to `VALID_ARTIFACT_TYPES` alone would admit the input and then fall through to the `default:` branch, returning `Unsupported artifact type: parallel-kickoff` — leaving the kickoff artifact unvalidatable through MCP, which spec R5 explicitly forbids ("the kickoff cannot ship unvalidatable"). TypeScript is therefore a language in scope, with its own baseline-free but mandatory final QA loop in Phase 10.

**Consequence for pre-existing tests.** Five landed tests currently encode the absence of `parallel-kickoff` and become false by design once the adjudicated wiring lands. Each is updated by an explicit task in Phase 3: `tests/scripts/dev_tools/test_validate_orchestration_artifacts_parallel_dispatch.py` (line 222), `extensions/drm-copilot/test/lib/validate/orchestration-artifacts-parallel-dispatch.test.ts` (line 142), `extensions/drm-copilot/test/mcp-tool-inputs-parallel-validation.test.ts` (lines 119–137), `extensions/drm-copilot/test/mcp-parallel-validation-definitions.test.ts` (lines 104–112), and `extensions/drm-copilot/test/mcp-server-parallel-validation.test.ts` (lines 160–178).

Non-negotiable constraints encoded as task acceptance criteria: `parallel` naming throughout; no epic-worthiness gate analogue and no dependency-graph authoring; planner-computed radius with V1/V2 Blocking and V3 Advisory; no integration branch; no modification of `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/agents/epic-planner.md`, `.claude/skills/epic-plan/SKILL.md`, `.claude/skills/orchestrate/SKILL.md`, or `config/orchestration-routing.json`; F4 calls F1/F2 and conforms to F3-owned schemas without reimplementing or defining any of them; and F4 does not modify the F3-owned validator, manifest-contract, rules, or routing surfaces enumerated in [P8-T4].

Languages in scope: Markdown runtime surfaces, Python (production module plus tests), TypeScript (parity core module, wiring, and tests), and one JSON manifest edit. **Spec R10's premise that the base scope "introduces no executable production code" no longer holds**: the fired R5 contingency introduces production Python and production TypeScript. Both carry the full toolchain loop and the uniform coverage thresholds (line >= 85%, branch >= 75%) per `.claude/rules/quality-tiers.md`, plus no-regression against the Phase 0 baseline (91.72% line / 83.58% branch) and new/changed-code coverage meeting the same thresholds. Test files remain excluded from the coverage denominator by policy (`.claude/rules/general-unit-test.md`, Coverage Requirements); no production file is excluded. [P9-T1] amends the spec so its base-scope file list and its R10 / Testing coverage-position statements agree with this delivered scope.

Evidence root (non-overridable): every evidence artifact in this plan resolves under `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/<kind>/` where `<kind>` is one of `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`, `remediation-baseline`. Timestamps use `yyyy-MM-ddTHH-mm` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. `<ts>` in artifact names below denotes the capture-time timestamp in that format.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture and Policy Compliance

- [x] [P0-T1] Read the repository policy files in the order defined by `.claude/skills/policy-compliance-order/SKILL.md`: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) language rules in scope: `.claude/rules/python.md` and `.claude/rules/python-suppressions.md`; also read `.claude/rules/tonality.md` and `.claude/rules/quality-tiers.md` as referenced policies, and write the evidence artifact `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/phase0-instructions-read.md`
  - Acceptance: the artifact exists and contains `Timestamp:`, `Policy Order:` (the numbered order above), and the explicit list of files read
- [x] [P0-T2] Run the Python formatting baseline `poetry run black --check .` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/black-baseline.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (pass/fail signal and file counts)
- [x] [P0-T3] Run the Python linting baseline `poetry run ruff check .` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/ruff-baseline.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (error count or all-clear signal)
- [x] [P0-T4] Run the Python type-checking baseline `poetry run pyright` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/pyright-baseline.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (error/warning counts)
- [x] [P0-T5] Run the Python test-and-coverage baseline `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/pytest-coverage-baseline.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording numeric coverage headline values (total line coverage percent and total branch coverage percent) plus pass/fail test counts; placeholder values such as `UNVERIFIED` are not permitted. No targeted-module baseline percent applies because the base scope adds no production Python module (spec R10); record that statement explicitly in the artifact

### Phase 1 — Upstream Dependency Reconciliation (F1/F2/F3)

Spec context: at spec-authoring time (commit base `5a0becb0`) F1 (`parallel-blast-radius`), F2 (`parallel-cohort-scheduler`), and F3 (`parallel-schema-validators`) had NOT landed on `epic/parallel-orchestration-integration`; every F1/F2/F3 interface in the spec is an `[ASSUMPTION]` entry citing design sections. All Phase 2–10 tasks depend on the outcome recorded here.

- [x] [P1-T1] Re-verify the F1/F2/F3 landing status at execution time. Run `git fetch origin`, then resolve `<UPSTREAM>` = `origin/epic/parallel-orchestration-integration` and record its resolved commit. Perform each check below against **both** the current worktree filesystem **and** the `<UPSTREAM>` ref, recording both results per check: (a) `git ls-tree --name-only <UPSTREAM> docs/features/active/` plus a worktree listing of `docs/features/active/`, for any `parallel-blast-radius`, `parallel-cohort-scheduler`, or `parallel-schema-validators` feature folder; (b) `git ls-tree --name-only <UPSTREAM> scripts/dev_tools/` plus a worktree listing of `scripts/dev_tools/`, for `compute_blast_radius.py`, `parallel_cohort_computation.py`, and any `validate_parallel_*` module; (c) `git show <UPSTREAM>:extensions/drm-copilot/src/mcp-tool-inputs.ts` plus the worktree copy, for any `parallel` entry in `VALID_ARTIFACT_TYPES`; (d) `git show <UPSTREAM>:.claude/rules/parallel-orchestration.md` and `git show <UPSTREAM>:config/orchestration-routing.json` plus the worktree copies, for the rules file and a `parallel` route. Write the findings to `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.<ts>.md`
  - Acceptance: the artifact records `Timestamp:`, the resolved `<UPSTREAM>` commit, every command run with its `EXIT_CODE:`, the worktree result and the `<UPSTREAM>`-ref result for each of checks (a)-(d), and a per-upstream-feature verdict of `unlanded`, `landed-consistent`, or `landed-divergent`; a feature is treated as landed when its artifacts are present on **either** surface; each landed verdict additionally records the landing granularity as `spec-landed` (planning artifacts only) or `implementation-landed` (production module present)
- [x] [P1-T2] Reconcile any landed upstream spec against the spec's `[ASSUMPTION]` entries (R3, R4, F3 ownership boundary, kickoff parser/validator boundary) and record the disposition in the same artifact `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.<ts>.md`, applying exactly one of the following executor behaviors per upstream feature: (case 1) upstream still unlanded — proceed under the `[ASSUMPTION]` regime; Phase 3 skill text retains its assumption labels citing the design sections; (case 2) upstream landed and consistent with the corresponding `[ASSUMPTION]` entries — record the confirmation and proceed; the landed spec supersedes the assumption labels, so Phase 3 skill text cites the landed contract instead of the assumption; (case 3) upstream landed and divergent — record each divergence; when the divergence changes only contract details quoted in the deliverable text (module paths, CLI shapes, JSON field names, checkpoint field names, sentinel values), author the affected Phase 2/3 content against the landed contract and record the supersession; when the divergence contradicts any non-negotiable constraint in this plan's Scope Summary or invalidates a task's acceptance criterion, stop, report remediation-required, and return this plan for revision before any dependent Phase 2–7 task executes; read each landed upstream spec by ref via `git show <UPSTREAM>:docs/features/active/<folder>/spec.md` when the folder is absent from the worktree, and cite the `<UPSTREAM>` commit alongside each disposition line
  - Acceptance: the artifact contains a per-`[ASSUMPTION]`-entry disposition line and, for case 3, the explicit stop-or-supersede decision with rationale
- [x] [P1-T3] Evaluate the spec's R5 kickoff-validation contingency by checking, on **both** the current worktree and the `<UPSTREAM>` ref resolved in P1-T1, for `scripts/dev_tools/parallel_kickoff_contract.py` (via `git ls-tree --name-only <UPSTREAM> scripts/dev_tools/`) and an `artifact_type: "parallel-kickoff"` entry in `extensions/drm-copilot/src/mcp-tool-inputs.ts` (via `git show <UPSTREAM>:extensions/drm-copilot/src/mcp-tool-inputs.ts`); F3 counts as landed when its artifacts are present on either surface; record the outcome in `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.<ts>.md`: if F3 is unlanded, or F3 landed WITH `parallel_kickoff_contract.py`, the contingency does not fire and the base scope stands (Phase 3 kickoff section documents validation as F3-owned, pending or landed respectively); if F3 landed WITHOUT `parallel_kickoff_contract.py`, the contingency fires — stop and report remediation-required, because this plan deliberately contains no production-Python module tasks and must be revised to add `scripts/dev_tools/parallel_kickoff_contract.py`, the minimal additive `artifact_type: "parallel-kickoff"` wiring, `tests/scripts/dev_tools/test_parallel_kickoff_contract.py`, and the full toolchain plus uniform coverage obligations before dependent work proceeds
  - Acceptance: the artifact records the contingency verdict (`not-fired` or `fired`) with the file-existence evidence from **both** surfaces (the worktree result and the `<UPSTREAM>`-ref result for each checked artifact); if `fired`, the plan run terminates in remediation-required with no Phase 2–7 task executed

### Phase 2 — Kickoff-Contract Module `scripts/dev_tools/parallel_kickoff_contract.py`

Adjudication basis: `docs/features/epics/parallel-orchestration/epic.md` "Planner Adjudication: the kickoff-contract boundary (F3 / F4)" assigns this module to F4 by producer ownership; `.claude/rules/parallel-orchestration.md` "F3 Scope Boundary — kickoff contract deferred to F4" records the same boundary. The mirror analogue is `scripts/dev_tools/epic_kickoff_contract.py` (263 lines). The module is production Python under `.claude/rules/python.md`, `.claude/rules/general-code-change.md`, and `.claude/rules/self-explanatory-code-commenting.md`.

- [x] [P2-T1] Create `scripts/dev_tools/parallel_kickoff_contract.py` with its module docstring, module-level pattern constants, header tuples, and frozen dataclasses, mirroring `scripts/dev_tools/epic_kickoff_contract.py` lines 1–52 with the parallel deltas: `KICKOFF_HEADING_RE` matching `^# Parallel Kickoff: (?P<slug>[a-z0-9][a-z0-9-]*)$`; `PARALLEL_RUN_RE` matching the invocation `` Run `/parallel-run <slug>` ``; `MANIFEST_RE` matching `docs/features/parallel/[a-z0-9][a-z0-9-]*/parallel\.md`; `PLAN_BRANCH_RE` matching `parallel/[a-z0-9][a-z0-9-]*-plan`; `RESUME_RE` matching the per-item-branch resume-boundary sentence (each item resumes at atomic execution from its committed plan-path on its own pushed feature branch), case-insensitive; `INTEGRITY_COMMIT_RE` matching the run-level head-commit field of `parallel/<slug>-plan` with a 7–64 hex value; `ITEM_HEADERS = ("issue_num", "feature_folder", "cohort", "complexity", "branch", "plan-path")` in that exact order; `HASH_HEADERS`; frozen dataclasses `KickoffItem` (`issue_num: int`, `feature_folder: str`, `cohort: int`, `complexity: str`, `branch: str`, `plan_path: str`) and `ParsedParallelKickoff` (`slug`, `invocation_slug`, `manifest_path`, `plan_home_branch`, `items: tuple[KickoffItem, ...]`, `planning_commit: str | None`, `plan_hashes: dict[str, str]`)
  - Acceptance: the file exists; `ITEM_HEADERS` is exactly the six-element ordered tuple above with no `wave` member and no `integration_branch` constant; both dataclasses are `@dataclass(frozen=True)` with full type hints and class docstrings; the module imports nothing from `scripts/dev_tools/epic_kickoff_contract.py`
- [x] [P2-T2] Add the section-splitting and strict-table helpers to `scripts/dev_tools/parallel_kickoff_contract.py`, mirroring `epic_kickoff_contract.py` lines 54–124: `_split_sections` (exact level-two sections, duplicate-heading rejection, required sections `Invocation Prompt` and `Item Summary`), `_parse_cells`, `_is_separator`, and `_table_rows` (exact ordered headers, valid separator row, per-row cell-count check, at-least-one-row check); all error strings use the literal `Parallel kickoff` context prefix
  - Acceptance: all four helpers exist with docstrings and full type hints; `_split_sections` emits a missing-section error for each of `Invocation Prompt` and `Item Summary` when absent and a duplicate-section error for a repeated level-two heading; every emitted error string begins with `Parallel kickoff`
- [x] [P2-T3] Add `_parse_items` to `scripts/dev_tools/parallel_kickoff_contract.py`, mirroring `epic_kickoff_contract.py` lines 127–149 over `ITEM_HEADERS`: parse the `## Item Summary` table into `KickoffItem` values, requiring `issue_num` to be an integer, `cohort` to be an integer, and `complexity` to be a member of `{"C1", "C2", "C3", "C4"}`; emit one row-indexed error per violation and continue where the epic analogue continues
  - Acceptance: `_parse_items` returns `tuple[tuple[KickoffItem, ...], list[str]]`; a non-integer `issue_num`, a non-integer `cohort`, and a `complexity` outside C1–C4 each produce a distinct row-indexed error; a table with zero data rows produces the at-least-one-row error from `_table_rows`
- [x] [P2-T4] Add `_parse_integrity` to `scripts/dev_tools/parallel_kickoff_contract.py`, mirroring `epic_kickoff_contract.py` lines 152–206: parse the optional `## Integrity` section into the run-level head commit of `parallel/<slug>-plan` (duplicate-field rejection) and the per-item `plan-path | plan-hash` table, requiring exactly two columns with header `plan-path` plus a member of `HASH_HEADERS`, a valid separator row, a 40–64 hex hash per row, and no repeated plan path; a non-table, non-commit line in the section is an error
  - Acceptance: `_parse_integrity` returns `tuple[str | None, dict[str, str], list[str]]`; a 39-hex and a 65-hex hash are each rejected while 40-hex and 64-hex are each accepted; a repeated plan path produces the repeat error; an absent `## Integrity` section produces zero errors
- [x] [P2-T5] Add the public entry points `parse_parallel_kickoff(text: str) -> tuple[ParsedParallelKickoff | None, list[str]]` and `validate_parallel_kickoff_text(text: str) -> list[str]` to `scripts/dev_tools/parallel_kickoff_contract.py`, mirroring `epic_kickoff_contract.py` lines 209–263: reject empty text; require the first line to match `# Parallel Kickoff: <slug>`; search the `## Invocation Prompt` section for the `/parallel-run <slug>` invocation, the manifest path, the `parallel/<slug>-plan` plan-home branch, and the resume-boundary sentence, emitting the structural-naming error when any is absent; aggregate item and integrity errors; return `(None, errors)` when any error is present and the parsed structure otherwise. The function must never mutate its input and must return a list of error strings for unparseable text rather than raising
  - Acceptance: both functions exist with docstrings and full type hints; `validate_parallel_kickoff_text` on a fully valid document returns `[]`; on an empty string it returns exactly one error; the module contains no `integration_branch` requirement and no `/epic-run` invocation pattern
- [x] [P2-T6] Verify the size of `scripts/dev_tools/parallel_kickoff_contract.py` against the 500-line production limit in `.claude/rules/general-code-change.md`; if the file is at or over 500 lines, extract the table-parsing helpers (`_parse_cells`, `_is_separator`, `_table_rows`, `_parse_items`, `_parse_integrity`) into `scripts/dev_tools/_parallel_kickoff_tables.py` following the repository's existing `_`-prefixed helper-module convention (precedent: `scripts/dev_tools/_parallel_state_common.py`, `_parallel_state_structures.py`, `_parallel_state_records.py`) and import them back; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/kickoff-module-size.<ts>.md`
  - Acceptance: the artifact records `Timestamp:`, the measured line count of every production module produced by this phase, and the split decision with its rationale; every produced module is under 500 lines. The artifact is appended to after [P2-T8] with the measured line count of every produced test module, so its final state records both the production and the test module sizes
- [x] [P2-T7] Create `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` with the positive scenarios, following the `tests/scripts/dev_tools/test_epic_kickoff_contract.py` precedent and `.claude/rules/general-unit-test.md` (Arrange–Act–Assert, one behavior per test, descriptive `test_...` names, docstrings, full type hints, no temporary files, no external dependencies): a fully valid kickoff document with heading, invocation prompt, a multi-row `## Item Summary`, and an `## Integrity` section validates with zero errors and parses into the expected `ParsedParallelKickoff`; a valid document with the optional `## Integrity` section omitted validates with zero errors; each of the six `ITEM_HEADERS` columns round-trips into the corresponding `KickoffItem` field; each of `C1`, `C2`, `C3`, `C4` is accepted as `complexity`; 40-hex and 64-hex plan hashes are each accepted
  - Acceptance: the file exists under `tests/` (never colocated with production sources), is under 500 lines, contains every listed positive scenario as a distinct test function with a docstring, and constructs all fixture documents as in-memory string literals
- [x] [P2-T8] Add the negative and boundary scenarios to `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` per `.claude/rules/general-unit-test.md` scenario completeness — one test per malformed variant: empty text; a first line that is not `# Parallel Kickoff: <slug>`; a slug that violates the `[a-z0-9][a-z0-9-]*` pattern; a missing `## Invocation Prompt` section; a missing `## Item Summary` section; a duplicate level-two heading; an invocation prompt missing the `/parallel-run <slug>` call; an invocation prompt missing the manifest path; an invocation prompt missing the `parallel/<slug>-plan` branch; an invocation prompt missing the resume-boundary sentence; an item table with wrong header text; an item table with headers in the wrong order; an item table with a missing separator row; an item table with a row whose cell count differs from six; an item table with zero data rows; a non-integer `issue_num`; a non-integer `cohort`; a `complexity` outside C1–C4 (assert both `C0` and `C5`); an integrity table with a wrong header; an integrity table with a missing separator row; a 39-hex hash; a 65-hex hash; a non-hex hash; a repeated plan path; a duplicate run-level commit field; and a stray non-table line inside `## Integrity`. Each test asserts on the specific error string produced, not merely on a non-empty error list. If the combined file reaches or exceeds 500 lines, split it by scenario class: keep the positive and structural-heading scenarios in `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` and move the item-table and integrity-table negative scenarios to `tests/scripts/dev_tools/test_parallel_kickoff_contract_tables.py`, sharing the document-builder helpers via a module-level import from the first file. Record the measured line count of every produced test module in the [P2-T6] size artifact
  - Acceptance: every listed malformed variant has its own test function with a docstring; each asserts a specific expected error string or substring; no test asserts only `len(errors) > 0`; every produced test module is under 500 lines, and when the split fires both `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` and `tests/scripts/dev_tools/test_parallel_kickoff_contract_tables.py` exist with their measured line counts recorded in the [P2-T6] size artifact
- [x] [P2-T9] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_kickoff_contract.py -v --cov=scripts/dev_tools/parallel_kickoff_contract --cov-branch --cov-report=term-missing` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/regression-testing/kickoff-contract-test-run.<ts>.md`. If the [P2-T8] conditional split fired, append `tests/scripts/dev_tools/test_parallel_kickoff_contract_tables.py` to the same pytest invocation so both test modules are collected in this one run; the command itself is unconditional and its `EXIT_CODE: 0` requirement applies regardless
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with the passed-test count and the numeric module line and branch coverage percentages; every test passes and the module's line coverage is >= 85% and branch coverage is >= 75%; if a helper module was created in [P2-T6], its coverage is recorded alongside and meets the same thresholds

### Phase 3 — `artifact_type: "parallel-kickoff"` Wiring (Python CLI and TypeScript)

Confinement discipline (epic wave-4 rule, restated by spec R5): each registration edit is a single distinct named addition with no reflow, reordering, or reformatting of existing entries. The five existing tests that assert `parallel-kickoff` is unsupported are updated by [P3-T2] and [P3-T8]; those tests are not F3-owned validator surfaces and their update is the direct consequence of the epic-manifest adjudication.

- [x] [P3-T1] Register `parallel-kickoff` on the Python CLI in `scripts/dev_tools/validate_orchestration_artifacts.py`: add `"parallel-kickoff"` as one new member appended to the simple-artifact-type tuple in `build_parser()` (currently `"plan"`, `"policy-audit"`, `"code-review"`, `"feature-audit"`, `"epic-kickoff"`), add `from scripts.dev_tools.parallel_kickoff_contract import validate_parallel_kickoff_text` to the import block in existing alphabetical position, and add the dispatch branch `if args.artifact_type == "parallel-kickoff": return validate_parallel_kickoff_text(text)` to `_validate_from_args` immediately after the `parallel-planner-state` branch and before the unsupported-type fallback. Also create the fixture `tests/fixtures/parallel_kickoff/valid-kickoff.md` containing a minimal valid parallel-kickoff document (heading `# Parallel Kickoff: <slug>`, an `## Invocation Prompt` section carrying the `/parallel-run <slug>` invocation, the manifest path, the `parallel/<slug>-plan` plan-home branch and the resume-boundary sentence, and a single-row `## Item Summary` table with the six ordered headers) and use it as `<valid-fixture-path>`; this is a committed repository fixture, not a temporary file, so it does not engage the runtime temporary-file prohibition in `.claude/rules/general-unit-test.md`
  - Acceptance: `poetry run python -m scripts.dev_tools.validate_orchestration_artifacts parallel-kickoff tests/fixtures/parallel_kickoff/valid-kickoff.md` exits 0 and prints the passing message; the fixture file exists at that committed path; `git diff` for `scripts/dev_tools/validate_orchestration_artifacts.py` shows only the added import line, the added tuple member, and the added dispatch branch, with no other line changed
- [x] [P3-T2] Update the pre-existing negative assertion in `tests/scripts/dev_tools/test_validate_orchestration_artifacts_parallel_dispatch.py` (line 222: `assert errors == ["Unsupported artifact type: parallel-kickoff"]`) so it no longer encodes the absence of the adjudicated F4 artifact type: convert that test into a positive-dispatch assertion that `parallel-kickoff` routes to `validate_parallel_kickoff_text`, and preserve unsupported-type fallback coverage with a separate test using a genuinely unregistered artifact-type name; update the test's docstring to cite the epic-manifest adjudication as the reason the prior expectation changed
  - Acceptance: the file contains no assertion that `parallel-kickoff` is unsupported; a positive-dispatch test for `parallel-kickoff` exists; an unsupported-type fallback test using a different unregistered name exists; both pass
- [x] [P3-T3] Create the TypeScript parity core module `extensions/drm-copilot/src/lib/validate/parallel-kickoff-artifact.ts`, porting `scripts/dev_tools/parallel_kickoff_contract.py` and mirroring the structure of `extensions/drm-copilot/src/lib/validate/epic-kickoff-artifact.ts`: the same pattern constants, the same six-element `ITEM_HEADERS`, `KickoffItem` and `ParsedParallelKickoff` interfaces, the section/table/item/integrity helpers, and the exported `validateParallelKickoffText(text: string): string[]`. Error strings must be byte-identical to the Python module's error strings for every construct the two runtimes represent identically; line splitting must tolerate LF, CRLF, and CR per the repository's existing extraction convention. The file must use ES module syntax, avoid `any`, and stay under 500 lines
  - Acceptance: the file exists at the stated path, exports `validateParallelKickoffText`, contains no `any` and no suppression comment, is under 500 lines, and produces error strings that match the Python module's for the fixture documents shared with [P3-T9]. Parity is established by comparing against expected error strings hardcoded in the [P3-T9] test file and transcribed from the Python module's literals; neither this module nor its test may spawn a Python process, read a generated file, or otherwise depend on an external process, per `.claude/rules/general-unit-test.md`
- [x] [P3-T4] Add the `parallel-kickoff` dispatch case to `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts`: import `validateParallelKickoffText` from `./parallel-kickoff-artifact` in existing import order, and add `case "parallel-kickoff": return validateParallelKickoffText(input.text);` immediately after the `parallel-planner-state` case and before `default:`
  - Acceptance: the new case exists; `git diff` for this file shows only the added import statement and the added case, with no existing case, option block, or doc comment reflowed
- [x] [P3-T5] Append `"parallel-kickoff"` as one new member to `VALID_ARTIFACT_TYPES` in `extensions/drm-copilot/src/mcp-tool-inputs.ts` (currently ten members at lines 427–438), placed after `"parallel-planner-state"`
  - Acceptance: the set contains eleven members; `git diff` for this file shows exactly one added line and zero modified lines
- [x] [P3-T6] Append `"parallel-kickoff"` as one new member to the `artifact_type` enum in the `validate_orchestration_artifacts` tool definition in `extensions/drm-copilot/src/mcp-tool-definitions.ts` (currently ten members at lines 403–414), placed after `"parallel-planner-state"`
  - Acceptance: the enum contains eleven members; `git diff` for this file shows exactly one added line and zero modified lines
- [x] [P3-T7] Append `"parallel-kickoff"` as one new member to the `artifact_type` enum in the `validate_orchestration_artifacts` tool definition in `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` (currently ten members at lines 336–347), placed after `"parallel-planner-state"`
  - Acceptance: the enum contains eleven members; `git diff` for this file shows exactly one added line and zero modified lines
- [x] [P3-T8] Update the four pre-existing TypeScript negative assertions that encode the absence of `parallel-kickoff`, each rewritten to reflect the adjudicated F4 addition with a comment citing `docs/features/epics/parallel-orchestration/epic.md` "Planner Adjudication: the kickoff-contract boundary (F3 / F4)": (a) `extensions/drm-copilot/test/lib/validate/orchestration-artifacts-parallel-dispatch.test.ts` line 142 — change the `Unsupported artifact type: parallel-kickoff` expectation to a positive-dispatch assertion and add a fallback assertion using a genuinely unregistered type name; (b) `extensions/drm-copilot/test/mcp-tool-inputs-parallel-validation.test.ts` lines 119–126 — change the "rejects an artifact type outside the allow-list" case to use a genuinely unregistered type name and add a case asserting `parallel-kickoff` is accepted; (c) the same file lines 128–137 — update the exact expected rejection-message string to the eleven-member list ending `..., parallel-planner-state, parallel-kickoff.`; (d) `extensions/drm-copilot/test/mcp-parallel-validation-definitions.test.ts` lines 104–112 — invert the "does not advertise an unplanned parallel kickoff artifact type" case into an assertion that both tool-definition enums DO contain `parallel-kickoff`, renaming the test accordingly; (e) `extensions/drm-copilot/test/mcp-server-parallel-validation.test.ts` lines 160–178 — change the server-level rejection case to use a genuinely unregistered type name and add a case asserting the server accepts `parallel-kickoff` and reaches the validation service
  - Acceptance: no test file in `extensions/drm-copilot/test/` asserts that `parallel-kickoff` is rejected, unsupported, or absent from an enum; each updated test carries the adjudication citation comment; all five updated cases pass
- [x] [P3-T9] Create `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact.test.ts` following the `extensions/drm-copilot/test/lib/validate/epic-kickoff-artifact.test.ts` precedent and `.claude/rules/typescript.md` (Jest, `*.test.ts` naming, Arrange–Act–Assert, one behavior per test, no temporary files): positive cases for a fully valid document with and without `## Integrity`; negative cases for the same malformed variants enumerated in [P2-T8]; and a cross-runtime parity block asserting that the TypeScript error strings equal the Python module's error strings for a shared set of fixture documents. The parity block asserts against expected error strings hardcoded in the test file and transcribed from the Python module's literals. It must not spawn a Python process, read a generated file, or otherwise depend on an external process, per `.claude/rules/general-unit-test.md`; the epic analogue `extensions/drm-copilot/test/lib/validate/epic-kickoff-artifact.test.ts` likewise contains no Python invocation. If the file reaches or exceeds 500 lines, split it by scenario class: keep the positive, structural-heading, and parity scenarios in `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact.test.ts` and move the item-table and integrity-table negative scenarios to `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact-tables.test.ts`, sharing the document-builder helpers via a module-level import from a non-test helper module `extensions/drm-copilot/test/lib/validate/parallel-kickoff-fixtures.ts` (a `.ts` file that does not match the `**/extensions/drm-copilot/test/**/*.test.ts` `testMatch` pattern in the repository-root `jest.config.cjs`, so importing it registers no duplicate suites). Record the measured line count of every produced test module in the artifact written by [P3-T10]
  - Acceptance: the file exists, contains a positive case, one case per malformed variant from [P2-T8], and the parity block; the parity block contains no child-process, file-read, or Python invocation; every produced test module is under 500 lines, and when the split fires all three files (`parallel-kickoff-artifact.test.ts`, `parallel-kickoff-artifact-tables.test.ts`, and the non-test `parallel-kickoff-fixtures.ts`) exist with the two test modules' measured line counts recorded in the [P3-T10] artifact; all tests pass
- [x] [P3-T10] Run the wiring test set from the repository root — `poetry run pytest tests/scripts/dev_tools/test_validate_orchestration_artifacts_parallel_dispatch.py -v` and `node run-jest.cjs extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact.test.ts extensions/drm-copilot/test/lib/validate/orchestration-artifacts-parallel-dispatch.test.ts extensions/drm-copilot/test/mcp-tool-inputs-parallel-validation.test.ts extensions/drm-copilot/test/mcp-parallel-validation-definitions.test.ts extensions/drm-copilot/test/mcp-server-parallel-validation.test.ts` — and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/regression-testing/kickoff-wiring-test-run.<ts>.md`. The Jest invocation uses the repository-canonical wrapper `run-jest.cjs` at the repository root rather than `npx jest`, because that wrapper enforces the issue-#423 prohibited-flag guard (`--passWithNoTests`, `--onlyChanged`, `--lastCommit`) and pins `--config jest.config.cjs`; a bare `npx jest` bypasses both. If the [P3-T9] conditional split fired, append `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact-tables.test.ts` to the same invocation. Also record in this artifact the measured line count of every test module produced by [P3-T9]
  - Acceptance: the artifact contains `Timestamp:`, both `Command:` entries, `EXIT_CODE: 0` for each, `Output Summary:` with per-command passed-test counts, and the measured line count of every [P3-T9] test module; the Jest command line begins `node run-jest.cjs` and contains no `npx jest`; every test passes

### Phase 4 — Agent Persona `.claude/agents/parallel-planner.md`

- [x] [P4-T1] Create `.claude/agents/parallel-planner.md` with YAML frontmatter per spec Deliverable 1 (R7): `name: parallel-planner`; `model: opus`; a description stating the planning-half role; `tools` exactly: `"Agent(orchestrator)"`, `Read`, `Grep`, `Glob`, `"Write(docs/features/parallel/**)"`, `"Edit(docs/features/parallel/**)"`, `"Write(artifacts/orchestration/**)"`, `"Edit(artifacts/orchestration/**)"`, `"Bash(git *)"`, `"Bash(gh *)"`, `"Bash(poetry run *)"`, `"mcp__drm-copilot__validate_orchestration_artifacts"`; `skills` exactly: `policy-compliance-order`, `parallel-plan`, `feature-promotion-lifecycle`, `atomic-plan-contract`, `evidence-and-timestamp-conventions`; `memory: project`; no `hooks` block. The `"Bash(poetry run *)"` entry is RETAINED, and its justification is recorded explicitly in the persona body rather than left implicit: per the Phase 1 evidence artifact, F1 (`compute_blast_radius.py`) and F2 (`parallel_cohort_computation.py`) landed as import-only Python libraries with no CLI entry point, so the planner invokes them through a poetry-run Python invocation (for example `poetry run python -c "from scripts.dev_tools.compute_blast_radius import derive_blast_radius, ..."`), which requires exactly this allowlist entry
  - Acceptance: the file exists; the frontmatter contains every listed field and value including `"Bash(poetry run *)"`; the body states the import-only justification for that entry; the file contains no `hooks` key, no `parallel-orchestrate` skill entry, no `docs/features/epics/**` write or edit scope, and no `docs/features/active/**` write or edit scope
- [x] [P4-T2] Author the body of `.claude/agents/parallel-planner.md` mirroring the structure of `.claude/agents/epic-planner.md` with these sections: a role statement (planning half only, no execution, distinct from the future `parallel-orchestrator`; terminal deliverable = pushed per-item feature branches, the `parallel/<slug>-plan` run branch, the manifest, the checkpoint, and the kickoff artifact); `## Skill` (defers the full procedure to `.claude/skills/parallel-plan/SKILL.md`); `## Invocation Origin` (main-session invocation only; delegates to `Agent(orchestrator)`; enforcement is the F7-owned extension of `.claude/hooks/enforce-epic-invocation-origin.ps1` adding `'parallel-planner'` and `'parallel-orchestrator'` to `$script:GatedSubagentTypes`, deny-reason pattern `EPIC_INVOCATION_ORIGIN_BLOCKED` or an F7-chosen renamed shared reason; the constraint is documented-but-unenforced until F7 lands); `## Startup Protocol` (read `artifacts/orchestration/parallel-planner-state.json`, resume from `next_step`, re-derive durable ground truth from `git branch` / `git worktree list --porcelain` / pushed refs); `## Delegation Model` (exclusively `Agent(orchestrator)` preparation-mode children; never delegates to `parallel-orchestrator`); `## Checkpoint Persistence` (write after every completed step; field set per spec R6); `## Completion Requirements` (per the spec's Completion report section)
  - Acceptance: all seven body sections are present; the `## Invocation Origin` section contains the `$script:GatedSubagentTypes` extension point, both agent names to add, and the documented-but-unenforced statement; the body contains no epic-worthiness gate section and no integration-branch instruction
- [x] [P4-T3] Verify `.claude/agents/parallel-planner.md` size and negative constraints: count lines (target approximately 120–150, hard requirement under 500) and grep-confirm the absence of `docs/features/epics/**`, `docs/features/active/**` write scoping, `hooks:`, `Epic mode: true`, and any `depends_on` authoring instruction; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/agent-persona-verification.<ts>.md`
  - Acceptance: the artifact records `Timestamp:`, the line count, and each negative-check result; all negative checks pass and the line count is under 500

### Phase 5 — Planning Skill `.claude/skills/parallel-plan/SKILL.md`

Contract basis (supersedes the spec's `[ASSUMPTION]` labels per the Phase 1 case-2 / case-3 dispositions): all skill text below cites the LANDED contracts recorded in `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.2026-08-08T13-56.md`, not the assumption labels.

- [x] [P5-T1] Create `.claude/skills/parallel-plan/SKILL.md` with YAML frontmatter: `name: parallel-plan`; a description covering intake, preparation fan-out, radius validation, cohort seeding, manifest/checkpoint authoring, and kickoff emission; `argument-hint: "[items: issue numbers and/or potential-entry paths]"`; `context: fork`; `agent: parallel-planner`; followed by a title, the `$ARGUMENTS` binding, and a Prerequisites section mirroring `.claude/skills/epic-plan/SKILL.md` (read `CLAUDE.md`, applicable `.claude/rules/` files, and the policy compliance order)
  - Acceptance: the file exists with all listed frontmatter fields; it contains no worthiness-gate section heading and no dependency/wave-design section heading anywhere in the document
- [x] [P5-T2] Author the `## Item Intake` section per spec R8: invocation shape `/parallel-plan <slug> <item> [<item> ...]` accepting GitHub issue numbers and/or potential-entry paths (the same intake domain as §8.3 `/parallel-add`); unpromoted items are promoted by their own preparation-mode child via the `preparation` route's promotion MCP tools; `issue_num` known at intake for issue items, negative placeholders (-1, -2, ...) in intake order for potential-entry items, back-filled from each child's promotion receipt; the manifest is committed fully resolved (no negative keys) before kickoff emission; `feature_folder` recorded as a resolvable-hint basename resolved to `docs/features/active/<basename>` after promotion with lifecycle-prefix stripping and pinned by the recorded per-item `branch_name`; `kind: feature | bug` recorded at intake, defaulting to `feature` when indeterminate with the `[recommendation, not upstream-constrained]` note
  - Acceptance: the section contains the invocation shape, the negative-placeholder and back-fill convention, the fully-resolved-before-kickoff rule, and the `kind` default rule; it contains no instruction asking the operator for `depends_on`, waves, or a worthiness verdict
- [x] [P5-T3] Author the `## Preparation Fan-Out` section per spec R2: one preparation-mode `Agent(orchestrator)` run per item, launched concurrently (one message, N `Agent` calls, each `isolation: "worktree"` and `run_in_background: true`), each worktree's branch created from `origin/main`; include the literal kickoff line exactly as specified in spec R2 (verbatim markers `Preparation mode: true.` and `route_id: preparation.`; `parallel_slug: <slug>.`; the scope sentence; the push-to-origin instruction; downstream attribution "executed later by parallel-orchestrator"; the ALL CLEAR / commit / `next_step: S5_atomic_execution` / stop-and-report tail) with the `model_budget.fable_policy: <disabled|available|preferred>.` marker line appended per the `.claude/skills/epic-orchestrate/SKILL.md` Model Selection pattern; state the deliberate omission of `Epic mode: true` and `Parallel mode: true` from the kickoff line; state that no edit is made to `.claude/skills/orchestrate/SKILL.md` or `config/orchestration-routing.json`; list the per-child collection set (plan-path, preflight status, promotion receipt, model-routing receipt with `logical_agent: "orchestrator"`, topology receipt, `branch_name`, `worktree_path`) and state that there is no fan-in merge step
  - Acceptance: the section contains the full literal kickoff line including `Preparation mode: true.`, `route_id: preparation.`, `parallel_slug: <slug>.`, the push instruction, and the `parallel-orchestrator` attribution; the strings `Epic mode: true` and `Parallel mode: true` appear only in the deliberate-omission statement and not inside the kickoff line itself; the `origin/main` base-ref instruction is present
- [x] [P5-T4] Author the `## Artifact Home` section per spec R1: per-item prepared feature folder and approved atomic plan on the item's own feature branch created from `origin/main`, committed by the unchanged `route_id: preparation` terminal step, pushed to `origin` before worktree removal, and reused as the execution branch with `branch_name` and `worktree_path` recorded per item; run-level artifacts (`docs/features/parallel/<slug>/parallel.md` and `docs/features/parallel/<slug>/parallel-kickoff.md`) on the planner-owned branch `parallel/<slug>-plan` created off `origin/main` and pushed, explicitly not an integration branch (no item branch merges into it; it merges into no item branch; it holds only `docs/features/parallel/<slug>/**`, which no item's radius may include); readers access both artifact classes by ref via `git fetch origin <branch>` plus `git show <ref>:<path>` without checkout per the `.claude/skills/epic-run/SKILL.md` precedent; record the three residual risks (stale execution base with F5-owned mitigation, branch accumulation on withdrawal with the F6 `--disposition abandon` cleanup expectation, and the per-branch git-integrity requirement — now recorded as **F4-owned**, per F3's landed rules file which leaves git-integrity checks, launch-evidence binding, and kickoff-contract cross-checks to F4)
  - Acceptance: the section contains the per-item branch contract, the `parallel/<slug>-plan` not-an-integration-branch statement, the `git fetch` + `git show` read protocol, and all three residual risks with the third attributed to F4 rather than F3; it contains no integration-branch creation instruction
- [x] [P5-T5] Author the `## Radius Computation and Validation` section per spec R3, authored against the LANDED F1 contract recorded in the Phase 1 evidence artifact (this supersedes the spec's `[ASSUMPTION]` label and its `python -m` CLI clause): F1 landed as an **import-only Python library with no CLI entry point** — matching the cited precedent `scripts/dev_tools/epic_wave_computation.py`, which is likewise import-only — so document the calling convention as an importable-library call executed through the `"Bash(poetry run *)"` allowlist entry, for example `poetry run python -c "from scripts.dev_tools.compute_blast_radius import derive_blast_radius"`. Document the landed signatures: `derive_blast_radius(plan_text, spec_text, feature_folder, config, *, source, computed_at) -> BlastRadius` taking document TEXT plus the parsed `config/blast-radius.json`, not paths; the `BlastRadius` serialized key set `("paths", "modules", "shared_surfaces", "contracts", "source", "computed_at")`; `validate_blast_radius(...)` in `scripts/dev_tools/_blast_radius_validation.py` with `RULE_COVERAGE = "V1"`, `RULE_SHARED_SURFACE = "V2"`, `RULE_OVER_BREADTH = "V3"`; and the contention relation `conflicts(a, b, config) -> ConflictResult` with the three-argument signature and the reason vocabulary `{path_overlap, module_overlap, shared_surface_overlap, contract_dependency}`, fail-closed. State that the declared radius is computed by the F1a-corrected derivation (issue #452, merged PR #453), which reaches separator-free repository-root shared surfaces sourced from the config `shared_surfaces` list and honours listed-directory prefixes on both sides of the conflicts path comparison, both in the fail-closed direction — the skill must not instruct any workaround around either correction. Then document F4's procedure: record the `declared` radius (authoritative for scheduling, §5.2) and per-item `radius_validation`; V1 (coverage) or V2 (shared-surface enumeration) Blocking failure — the item does not transition to `prepared`, findings are recorded in the checkpoint, and the item is re-planned via a follow-up preparation-mode delegation carrying the findings as plan-revision instructions (re-planned, not rejected; withdrawal is a caller decision via F6, never a planner default); V3 (over-breadth) Advisory — recorded in the checkpoint and surfaced in the completion report with no state effect; `prepared` requires the conjunction of `PREFLIGHT: ALL CLEAR` and a `declared` radius with V1/V2 pass
  - Acceptance: the section documents the import-only calling convention through `Bash(poetry run *)` and contains no `python -m scripts.dev_tools.compute_blast_radius` CLI instruction; it writes `conflicts(a, b, config)` with three arguments and never `conflicts(a, b)`; it states that `derive_blast_radius` takes document text plus the parsed `config/blast-radius.json` and never that it takes paths; it names the F1a corrections and instructs no workaround around them; it documents the V1/V2 Blocking re-plan loop, the V3 Advisory recording, and the `prepared` conjunction; it contains no reimplementation of derivation, of V1–V3 rules, or of the contention relation
- [x] [P5-T6] Author the `## Cohort Seeding` section per spec R4, authored against the LANDED F2 contract recorded in the Phase 1 evidence artifact (this supersedes the spec's `[ASSUMPTION]` label, its CLI clause, and its pinned-set clause): F2 landed as an **import-only Python library with no CLI entry point**, invoked through the `"Bash(poetry run *)"` allowlist entry; the landed seeding entry point is `compute_cohorts(item_keys: Iterable[int], conflict_edges: Iterable[tuple[int, int]]) -> list[list[int]]`, which takes item keys and conflict edges only — **there is no pinned-set parameter** — and returns a plain list-of-lists in deterministic Welsh-Powell order (composite key `(-degree, item_key)` ascending, that is descending distinct-neighbour degree with ties broken by ascending item key). Document that F4 maps that return value into the F3-owned `cohorts[]` object shape, supplying `index` from the outer-list position and `generation: 0` itself; state that recoloring support is F6/F8's concern, which is why the landed signature needs no pinned set. Record the F4 procedure: exactly one invocation over the full conflict graph after every item is `prepared` and radius-validated, with the conflict edge set derived by F1's `conflicts(a, b, config)` over `declared` radii; record `cohorts[]` at `generation: 0` with `item_keys[]` sorted ascending, `conflict_edges[]` (`{a, b, reason}`), `recolor_generation: 0`, and `current_cohort: 0`; state that `max_concurrency` (default 4, bounded 1–8 by F3, slots filled in ascending item key via the landed `compute_concurrency_batches(cohort_item_keys, max_concurrency)`) is recorded by F4 but enforced by F5, and that recoloring under add/remove/drift is F6/F8 scope — this skill performs seeding only
  - Acceptance: the section documents the import-only calling convention and contains no CLI-subprocess instruction for F2; it states the landed `compute_cohorts(item_keys, conflict_edges)` signature and contains no reference to a pinned-set argument or an "empty pinned set at seeding time"; it documents the list-of-lists-to-`cohorts[]` mapping; it contains the single-invocation rule, all four recorded fields with their seed values, the `max_concurrency` recorded-not-enforced statement, and the recoloring-out-of-scope statement
- [x] [P5-T7] Add the **cohort recomputation-parity obligation** to the `## Cohort Seeding` section of `.claude/skills/parallel-plan/SKILL.md` as a documented planner procedure. Basis: F3's landed planner invariant P5 in `.claude/rules/parallel-orchestration.md` is deliberately absent from F3's validator and states that "Recomputation parity against the cohort-computation module is the planner-surface feature's check (the analogue of the epic planner's wave-number cross-check)" — assigning it to F4. The documented procedure must state: after writing `cohorts[]` into the checkpoint and before emitting the kickoff artifact, the planner re-invokes `compute_cohorts` over the same `item_keys` and `conflict_edges` recorded in the checkpoint and asserts that the recomputed list-of-lists maps to exactly the recorded `cohorts[]` at `generation: 0` (same partition, same `index` assignment, same ascending `item_keys[]` ordering); a mismatch is a Blocking condition that stops the run and is reported rather than auto-corrected. Keep the section addition bounded to this procedure — introduce **no new production module** for it
  - Acceptance: the `## Cohort Seeding` section contains the recomputation-parity procedure with the re-invocation step, the exact-match assertion, the before-kickoff ordering, and the Blocking-on-mismatch rule, and cites F3 planner invariant P5 as its basis; no new file under `scripts/dev_tools/` is created by this task
- [x] [P5-T8] Author the `## Manifest Authoring` section: write `docs/features/parallel/<slug>/parallel.md` conforming to the F3-owned §11 frontmatter schema (`parallel`, `mode` `closed | open` default `closed`, `max_concurrency` default 4 bounded 1–8, `created_at`, `items[]` with per-item `issue_num`, `feature_folder`, `kind`, `state`, and `blast_radius` carrying `paths`, `modules`, `shared_surfaces`, `contracts`, `source: "declared"`, `computed_at`); state that the manifest carries no `depends_on` field and is committed in fully resolved form (no negative `issue_num`) to `parallel/<slug>-plan` before the kickoff artifact is written; state that manifest validation is a library call to `scripts/dev_tools/parallel_manifest_contract.py` and deliberately not an MCP `artifact_type`, per F3's landed rules file
  - Acceptance: the section names the manifest path pattern, every listed schema field, the no-`depends_on` statement, the fully-resolved-before-kickoff commit rule, and the library-call validation route
- [x] [P5-T9] Author the `## Checkpoint Persistence` and `## F3 Ownership Boundary` sections per spec R6, citing the LANDED F3 contract: the checkpoint `artifacts/orchestration/parallel-planner-state.json` with the top-level field set (`objective`, `parallel_slug`, `parallel_manifest_path`, `mode`, `max_concurrency`, `plan_home_branch`, `items[]`, `cohorts[]`, `conflict_edges[]`, `recolor_generation`, `kickoff_prompt_path` = `artifacts/orchestration/parallel-kickoff-<slug>.md`, `completed_steps`, `next_step`, `last_updated`), noting that F3's landed P1 required-key set is a strict subset and that `plan_home_branch` is a permitted additional field; the per-item field set from spec R6; the deliberate absence of any `epic_worthiness`, `depends_on`, or `wave` field; the readiness contract including the `PARALLEL_EXECUTION_READY` ready sentinel (F3 P8) and **F4-owned git integrity against per-item branch refs plus `parallel/<slug>-plan`** — stated as F4-owned because F3's landed rules file declares its `require_ready_for_execution` gate STRUCTURAL ONLY and leaves git-integrity checks, launch-evidence binding, and kickoff-contract cross-checks to F4, layered behind an additional keyword without changing the schema; write-after-every-step and resume-re-derivation obligations (checkpoint is a cache, not the source of truth, §12); the F3 boundary statement (manifest and checkpoint schemas, `scripts/dev_tools/validate_parallel_planner_state.py`, the two state MCP `artifact_type` values, `.claude/rules/parallel-orchestration.md`, and `route_id: parallel` are F3-owned; F4 writes conforming instances only and validates via `mcp__drm-copilot__validate_orchestration_artifacts`); the statement that `scripts/dev_tools/parallel_kickoff_contract.py` and `artifact_type: "parallel-kickoff"` are **F4-owned and delivered by this feature** per the epic-manifest adjudication and F3's "F3 Scope Boundary — kickoff contract deferred to F4" rule section, replacing the prior F3-recommendation-with-contingency framing; and the single-item-run floor question, noted as an F3 schema decision resolved by F3's landed P6 ready-gate cardinality requirement of at least two items
  - Acceptance: both sections exist; the checkpoint section contains the `PARALLEL_EXECUTION_READY` sentinel, the deliberate-absence statement, and the F4-owned git-integrity attribution; the boundary section lists the F3-owned surfaces, states that the kickoff-contract module and `parallel-kickoff` artifact type are F4-owned and delivered here (not recommended-with-contingency), and records the single-item-run disposition against F3's landed P6
- [x] [P5-T10] Author the `## Kickoff Artifact` section per spec R5 with a fenced template: working copy `artifacts/orchestration/parallel-kickoff-<slug>.md` and durable copy `docs/features/parallel/<slug>/parallel-kickoff.md` committed to `parallel/<slug>-plan`; heading `# Parallel Kickoff: <slug>`; `## Invocation Prompt` naming `` Run `/parallel-run <slug>` ``, the manifest path `docs/features/parallel/<slug>/parallel.md`, the plan-home branch `parallel/<slug>-plan`, and the resume-boundary sentence that each item resumes at atomic execution from its committed plan-path on its own pushed feature branch; `## Item Summary` strict pipe table with exact ordered headers `issue_num | feature_folder | cohort | complexity | branch | plan-path` (at least one row; `issue_num` and `cohort` integers; `complexity` in C1–C4); optional `## Integrity` with a per-item `plan-path | plan-hash` table (40–64 hex, no repeated paths) and the head commit of `parallel/<slug>-plan` as run-level provenance; validation through the **F4-delivered** `scripts/dev_tools/parallel_kickoff_contract.py` invoked via `mcp__drm-copilot__validate_orchestration_artifacts` with `artifact_type: "parallel-kickoff"` (delivered by Phases 2 and 3 of this feature), per the boundary stated in [P5-T9]
  - Acceptance: the section contains both kickoff paths, the exact heading pattern, the `/parallel-run <slug>` invocation, the plan-home branch, the per-item-branch resume-boundary sentence, and the exact six-column header row; the validation sentence names `parallel_kickoff_contract.py` and `artifact_type: "parallel-kickoff"` as delivered by this feature rather than as an F3 recommendation
- [x] [P5-T11] Author the `## Completion Report` section: the final report lists the manifest path and `parallel/<slug>-plan` branch; per item one `plan-path:` line, branch name, preflight status, and radius-validation result including any V3 Advisory findings; the cohort table at `generation 0` with the recomputation-parity result from [P5-T7]; and both kickoff artifact paths; it ends with the statement that execution has NOT started and begins only when the user runs `/parallel-run <slug>` or replays the kickoff prompt from the main session
  - Acceptance: the section lists every named report element including the recomputation-parity result, and the execution-not-started closing statement
- [x] [P5-T12] Verify `.claude/skills/parallel-plan/SKILL.md` size and negative constraints: count lines (target approximately 220–300, hard requirement under 500) and grep-confirm that the document contains no epic-worthiness gate analogue, no `depends_on` authoring instruction, no wave-design section, no integration-branch creation instruction, no `conflicts(a, b)` two-argument form, no pinned-set reference, no `python -m` CLI instruction for F1 or F2, and that `Epic mode: true` and `Parallel mode: true` do not appear inside the kickoff line; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/skill-verification.<ts>.md`
  - Acceptance: the artifact records `Timestamp:`, the line count, and each negative-check result; all checks pass and the line count is under 500

### Phase 6 — Contract Test

- [x] [P6-T1] Create `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py` following the `tests/scripts/dev_tools/test_epic_run_kickoff_discovery_contract.py` precedent (module docstring, `REPO_ROOT` resolution, read-file helper with docstring, fragment-assertion style, full type hints, intent comments per `.claude/rules/self-explanatory-code-commenting.md`) with the positive assertions from spec R9: `.claude/agents/parallel-planner.md` and `.claude/skills/parallel-plan/SKILL.md` exist; agent frontmatter declares `Agent(orchestrator)`, the `docs/features/parallel/**` write scope, `Bash(poetry run *)`, and `mcp__drm-copilot__validate_orchestration_artifacts`; skill frontmatter declares `context: fork` and `agent: parallel-planner`; the skill contains the literal markers `Preparation mode: true` and `route_id: preparation` and instructs branching preparation worktrees from `origin/main`; the skill names `artifacts/orchestration/parallel-planner-state.json`, the manifest path pattern `docs/features/parallel/<slug>/parallel.md`, and both kickoff paths (`artifacts/orchestration/parallel-kickoff-<slug>.md`, `docs/features/parallel/<slug>/parallel-kickoff.md`)
  - Acceptance: the file exists in the `tests/` tree (never colocated with production sources), contains all listed positive assertions as distinct test functions or grouped fragment loops with docstrings, and is under 500 lines
- [x] [P6-T2] Add the negative assertions from spec R9 to `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py`: agent frontmatter declares no `docs/features/epics/**` scope; the skill's preparation kickoff line contains no `Epic mode: true` and no `Parallel mode: true`, and the skill contains no `depends_on` authoring instruction and no integration-branch creation instruction; unmodified-surface content guards asserting that `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/agents/epic-planner.md`, and `.claude/skills/epic-plan/SKILL.md` still contain pre-existing identifying lines (select stable identifying fragments from each file's current text)
  - Acceptance: all listed negative assertions are present; each unmodified-surface guard cites a literal fragment verified to exist in the guarded file's current content
- [x] [P6-T3] Add the landed-contract and F4-obligation assertions to `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py`, encoding the Phase 1 supersessions and the F4-owned obligations as text assertions over `.claude/skills/parallel-plan/SKILL.md`: the skill contains `conflicts(a, b, config)` and does NOT contain the two-argument form `conflicts(a, b)`; the skill contains no `python -m scripts.dev_tools.compute_blast_radius` and no `python -m scripts.dev_tools.parallel_cohort_computation` CLI instruction; the skill contains `compute_cohorts(item_keys, conflict_edges)` and contains no pinned-set reference; the skill names `config/blast-radius.json`; the skill contains the **cohort recomputation-parity** procedure required by [P5-T7], asserted by a fragment naming the re-invocation of `compute_cohorts` and the Blocking-on-mismatch rule; the skill attributes the per-branch git-integrity verification to F4; and the skill names `parallel_kickoff_contract.py` together with `artifact_type: "parallel-kickoff"` as delivered by this feature
  - Acceptance: each listed assertion exists as a distinct test function or a named entry in a fragment table with a docstring; the recomputation-parity assertion and the `conflicts(a, b, config)` assertion are each present and each fails against skill text lacking them
- [x] [P6-T4] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py -v` and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/regression-testing/contract-test-run.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with the passed-test count; every test passes

### Phase 7 — Bundled Payload Mirror and Pack-Manifest Registration

Deviation record: the spec states "No other file is created or modified in the base scope." Planning-time verification against the real test suite found two enforced gates that make that statement unsatisfiable for new `.claude` files: `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` requires every repo `.claude` file (excluding `settings.local.json` and `agent-memory/**`) to exist byte-identically in `extensions/drm-copilot/resources/claude-customizations/`, and `tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py` requires every bundled agent/skill to appear in a pack manifest. The epic precedent conforms: `.claude/agents/epic-planner.md` and `.claude/skills/epic-plan/SKILL.md` are registered in `pack-manifests/core.json`. These mirror edits touch none of the protected surfaces in the Scope Summary and are the minimal additions required for a passing final QA loop.

Mirror scope confirmed against the enforcing test rather than assumed: `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` line 19 declares `SCOPED_ROOTS: tuple[Path, ...] = (Path(".claude"),)`, so the mirror obligation covers `.claude/**` only. The Phase 2 and Phase 3 deliverables live under `scripts/dev_tools/**`, `extensions/drm-copilot/src/**`, and `tests/**`, none of which is a `.claude` path; they therefore require no bundled mirror and no pack-manifest entry. [P7-T4] re-verifies this by execution rather than by assertion.

- [x] [P7-T1] Copy `.claude/agents/parallel-planner.md` byte-identically to `extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-planner.md`
  - Acceptance: the bundled file exists and its content is byte-identical to the root file
- [x] [P7-T2] Copy `.claude/skills/parallel-plan/SKILL.md` byte-identically to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-plan/SKILL.md`
  - Acceptance: the bundled file exists and its content is byte-identical to the root file
- [x] [P7-T3] Add `.claude/agents/parallel-planner.md` and `.claude/skills/parallel-plan/SKILL.md` to the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, placed in the alphabetically correct position within the existing `.claude/agents/**` and `.claude/skills/**` groupings, without reordering or reformatting any existing entry
  - Acceptance: both new paths are present in `core.json`; `git diff` for that file shows only the two added lines
- [x] [P7-T4] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py -v` and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/regression-testing/mirror-gate-run.<ts>.md`, including in the artifact the deviation record above, the quoted `SCOPED_ROOTS` declaration from the enforcing test, and the explicit confirmation that the Phase 2 and Phase 3 deliverables under `scripts/dev_tools/**`, `extensions/drm-copilot/src/**`, and `tests/**` introduce no new `.claude` file and therefore require no mirror or pack-manifest entry
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` with passed-test counts, the deviation record, the quoted `SCOPED_ROOTS` line, and the non-`.claude` confirmation; every test passes

### Phase 8 — Non-Modification and Constraint Verification

- [x] [P8-T1] Verify constraint 5 (atomic-plan contract unchanged): run the merge-base-anchored trio `git merge-base origin/epic/parallel-orchestration-integration HEAD` (record the result as `<BASE>`), `git diff --name-only <BASE>`, and `git status --porcelain --untracked-files=all`; define the change set as the union of the outputs of the second and third commands (covering committed, staged, unstaged, and untracked paths) and assert `.claude/skills/atomic-plan-contract/SKILL.md` is absent from that union; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/non-modification-atomic-plan-contract.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, both change-set `Command:` entries with the resolved `<BASE>` commit recorded, `EXIT_CODE:` for each command, the union change-file list derived from both commands (covering committed, staged, unstaged, and untracked paths), and an explicit statement that the protected path does not appear in that union
- [x] [P8-T2] Verify constraint 6 (additive-only epic surfaces): run the merge-base-anchored trio `git merge-base origin/epic/parallel-orchestration-integration HEAD` (record the result as `<BASE>`), `git diff --name-only <BASE>`, and `git status --porcelain --untracked-files=all`; define the change set as the union of the outputs of the second and third commands (covering committed, staged, unstaged, and untracked paths) and assert both `.claude/agents/epic-planner.md` and `.claude/skills/epic-plan/SKILL.md` are absent from that union; additionally assert that `scripts/dev_tools/epic_kickoff_contract.py` and `extensions/drm-copilot/src/lib/validate/epic-kickoff-artifact.ts` — the mirror analogues copied from, not edited — are absent from that union; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/non-modification-epic-surfaces.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, both change-set `Command:` entries with the resolved `<BASE>` commit recorded, `EXIT_CODE:` for each command, the union change-file list derived from both commands (covering committed, staged, unstaged, and untracked paths), and an explicit statement that none of the four protected paths appears in that union
- [x] [P8-T3] Verify constraint 7 (orchestrate skill and preparation route unchanged): run the merge-base-anchored trio `git merge-base origin/epic/parallel-orchestration-integration HEAD` (record the result as `<BASE>`), `git diff --name-only <BASE>`, and `git status --porcelain --untracked-files=all`; define the change set as the union of the outputs of the second and third commands (covering committed, staged, unstaged, and untracked paths) and assert both `.claude/skills/orchestrate/SKILL.md` and `config/orchestration-routing.json` are absent from that union; write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/non-modification-orchestrate-route.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, both change-set `Command:` entries with the resolved `<BASE>` commit recorded, `EXIT_CODE:` for each command, the union change-file list derived from both commands (covering committed, staged, unstaged, and untracked paths), and an explicit statement that neither protected path appears in that union
- [x] [P8-T4] Verify constraint 9 as amended by the epic-manifest adjudication (F3-owned surfaces not defined or altered by F4; the two adjudicated F4 additions present). Run the merge-base-anchored trio `git merge-base origin/epic/parallel-orchestration-integration HEAD` (record the result as `<BASE>`), `git diff --name-only <BASE>`, and `git status --porcelain --untracked-files=all`; define the change set as the union of the outputs of the second and third commands (covering committed, staged, unstaged, and untracked paths). **Assert absent** from that union — the surfaces that remain F3-owned: any `scripts/dev_tools/validate_parallel_*` path, any `scripts/dev_tools/_parallel_state_*` path, `scripts/dev_tools/parallel_manifest_contract.py`, `.claude/rules/parallel-orchestration.md`, and `config/orchestration-routing.json`. **Assert present** in that union — the two adjudicated F4 additions: `scripts/dev_tools/parallel_kickoff_contract.py` and `extensions/drm-copilot/src/mcp-tool-inputs.ts`. Record in the artifact that the presence of these two paths is required by design, citing `docs/features/epics/parallel-orchestration/epic.md` section "Planner Adjudication: the kickoff-contract boundary (F3 / F4)" ("Adjudication: F4 owns it") and `.claude/rules/parallel-orchestration.md` section "F3 Scope Boundary — kickoff contract deferred to F4", and that the prior v1.0 assertion of their absence was superseded by the fired R5 contingency recorded in `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.2026-08-08T13-56.md`. Also record that `scripts/dev_tools/validate_orchestration_artifacts.py`, `extensions/drm-copilot/src/mcp-tool-definitions.ts`, `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`, and `extensions/drm-copilot/src/lib/validate/orchestration-artifacts.ts` are shared registration surfaces receiving the adjudicated additive wiring, not F3-owned surfaces. Write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/non-modification-f3-surfaces.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, both change-set `Command:` entries with the resolved `<BASE>` commit recorded, `EXIT_CODE:` for each command, the union change-file list derived from both commands (covering committed, staged, unstaged, and untracked paths), an explicit statement that no remaining-F3-owned path appears in that union, an explicit statement that both adjudicated F4 paths DO appear in that union with the two citations above, and the shared-registration-surface note

### Phase 9 — Spec Deviation Record and Acceptance-Criteria Check-Off

Per `.claude/skills/acceptance-criteria-tracking/SKILL.md`: check-off is per-item and evidence-gated; each task below cites the phase evidence that satisfies the items it checks, and no item is checked without its evidence on disk.

- [x] [P9-T1] Record the kickoff-contract boundary deviation in `docs/features/active/2026-08-07-parallel-planner-surface-443/spec.md`, as spec R5's own contingency text requires ("F4 delivers that module and the minimal additive `artifact_type: \"parallel-kickoff\"` wiring itself and records the boundary deviation in this spec"). Make exactly these edits: (a) add a `## Boundary Deviation Record — Kickoff Contract (R5 contingency fired)` section stating that the contingency fired, citing `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/upstream-reconciliation.2026-08-08T13-56.md` and `docs/features/epics/parallel-orchestration/epic.md` section "Planner Adjudication: the kickoff-contract boundary (F3 / F4)", and recording that F4 delivers `scripts/dev_tools/parallel_kickoff_contract.py`, the TypeScript parity core module, and the five-surface `artifact_type: "parallel-kickoff"` wiring; (b) amend the "Deliverables" list and the sentence "No other file is created or modified in the base scope" so the spec's file list matches the delivered scope, including the Phase 7 mirror deviation already recorded in this plan; (c) amend the Testing section's "Coverage position" paragraph, which asserts "no executable production code is introduced", to state that production Python and production TypeScript are introduced and carry the full toolchain loop and the uniform coverage thresholds; (d) amend Resolution R10's file-size paragraph to add the size limits for the new Python and TypeScript modules; (e) append three new acceptance criteria to the `## Acceptance Criteria` section, numbered 20–22 in document order: 20 — `scripts/dev_tools/parallel_kickoff_contract.py` exists, validates the R5 kickoff shape (heading, invocation prompt, six-column `## Item Summary`, optional `## Integrity`), is under 500 lines, and passes `tests/scripts/dev_tools/test_parallel_kickoff_contract.py`; 21 — the `artifact_type: "parallel-kickoff"` wiring is registered additively on the Python CLI, `VALID_ARTIFACT_TYPES`, both MCP tool-definition enums, and the TypeScript dispatcher, with no reflow of existing entries, and the TypeScript parity core module `extensions/drm-copilot/src/lib/validate/parallel-kickoff-artifact.ts` exists and is dispatched; 22 — the delivered skill documents the F4-owned cohort recomputation-parity obligation (F3 planner invariant P5) and the F4-owned per-branch git-integrity verification. All 19 pre-existing acceptance criteria remain intact, in their existing order, with their existing text
  - Acceptance: the spec contains the new deviation-record section with both citations; the Deliverables list, the "no other file" sentence, the Testing coverage-position paragraph, and R10 all agree with this plan's Scope Summary; the `## Acceptance Criteria` section contains exactly 22 items, of which items 1–19 are byte-identical to their pre-edit text; `git diff` for `spec.md` shows no deletion of any pre-existing acceptance-criterion line
- [x] [P9-T2] Check off the agent-persona acceptance criteria in `docs/features/active/2026-08-07-parallel-planner-surface-443/spec.md` (items 1–3: frontmatter and tool allowlist; preloaded skills; `## Invocation Origin` content), citing the Phase 4 verification artifact and the Phase 6 test run as evidence for each item
  - Acceptance: exactly those spec checkboxes whose evidence exists are changed to `[x]`; each check-off is traceable to a named evidence artifact
- [x] [P9-T3] Check off the skill acceptance criteria in `docs/features/active/2026-08-07-parallel-planner-surface-443/spec.md` (items 4–15: frontmatter; kickoff-line markers; `origin/main` branching and no orchestrate/routing change; R1 artifact home with residual risks; F1 invocation without reimplementation; V1/V2/V3 semantics; cohort seeding; kickoff artifact; F3 boundary; checkpoint contract; item intake; no worthiness/dependency/integration-branch content), citing the Phase 5 verification artifact, the Phase 6 test run, and the Phase 8 artifacts as evidence per item; where an item's spec text was superseded by a Phase 1 landed-contract disposition (item 8's `Bash(poetry run *)` CLI phrasing and item 10's cohort-seeding phrasing), record the supersession alongside the check-off citing the Phase 1 evidence artifact rather than editing the criterion text
  - Acceptance: exactly those spec checkboxes whose evidence exists are changed to `[x]`; each check-off is traceable to a named evidence artifact; each superseded item carries its supersession note citing the Phase 1 artifact
- [x] [P9-T4] Check off the remaining spec acceptance criteria in `docs/features/active/2026-08-07-parallel-planner-surface-443/spec.md` (item 16: contract test exists, follows precedent, and passes — Phase 6 evidence; item 17: no change to the three protected files — Phase 8 evidence; item 18: both Markdown deliverables under 500 lines — Phase 4/5 verification artifacts; item 19: F1/F2/F3 landing status re-verified and reconciled — Phase 1 artifact; item 20: kickoff-contract module — Phase 2 evidence including the size artifact and the module test run; item 21: five-surface wiring and TypeScript parity module — Phase 3 evidence; item 22: recomputation-parity and git-integrity documentation — Phase 5 and Phase 6 evidence)
  - Acceptance: exactly those spec checkboxes whose evidence exists are changed to `[x]`; each of the seven items cites a named evidence artifact
- [x] [P9-T5] Check off the operator acceptance criteria in `docs/features/active/2026-08-07-parallel-planner-surface-443/user-story.md` (all 8 items), verified against the documented procedure in the delivered skill text; because the Phase 1 reconciliation found F1, F1a, F2, and F3 all implementation-landed, the user-story preamble's `[ASSUMPTION]` regime no longer applies and criteria over live upstream tooling are verified against the landed contracts; cite the Phase 5 sections and the Phase 6/8 evidence per item
  - Acceptance: exactly those user-story checkboxes whose evidence exists are changed to `[x]`; each check-off cites the skill section and evidence artifact that satisfies it; the artifact trail records that the `[ASSUMPTION]` regime was superseded by the Phase 1 landed-contract findings

### Phase 10 — Final QA Loop (Python and TypeScript)

Run the full language-appropriate QA loop for each language in scope, in order: format → lint → type-check → test. If any step fails or modifies any file, fix the cause, re-sync the bundled mirror copies if a `.claude` deliverable changed (Phase 7 byte-identity must still hold), and restart this phase from P10-T1 until every step passes in a single clean pass. Every task below is unconditional; `EXIT_CODE: SKIPPED` is not a passing outcome for any task in this phase. The TypeScript loop is mandatory because the TypeScript surface is modified (see the Scope Summary determination).

Two Phase 10 corrections are load-bearing and are recorded here so they are not re-litigated during execution. First, the TypeScript format, lint, and type-check gates target the `extensions/drm-copilot` package, not the repository root: the root `format` script globs only `src/**` and `tests/**`, the root `lint` script is `eslint --no-error-on-unmatched-pattern src tests`, and the root `typecheck` script compiles the root `tsconfig.json` whose `include` is `["src/**/*.ts","tests/**/*.ts"]`. None of the three reaches `extensions/drm-copilot/**`, so running them at the root would record `EXIT_CODE: 0` without inspecting any file this feature adds. Second, `extensions/drm-copilot/node_modules` is not present in a fresh worktree and `@eslint/js` (a devDependency of that package, imported by `extensions/drm-copilot/eslint.config.mjs`) resolves from no ancestor `node_modules`, so [P10-T5] installs the extension package dependencies before those three gates run. The Jest coverage gate [P10-T9] is the one TypeScript gate that deliberately runs at the repository root, for the reason stated in that task.

- [x] [P10-T1] Run `poetry run black .` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/black-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; if Black reformatted any file, the loop restarts from P10-T1 after re-verifying mirror byte-identity, and only the final clean pass is recorded as the gate result
- [x] [P10-T2] Run `poetry run ruff check .` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/ruff-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` (zero errors); any failure restarts the loop from P10-T1
- [x] [P10-T3] Run `poetry run pyright` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/pyright-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` (zero errors); any failure restarts the loop from P10-T1
- [x] [P10-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/pytest-coverage-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` recording numeric post-change coverage values (total line coverage percent and total branch coverage percent), the per-file line and branch coverage for `scripts/dev_tools/parallel_kickoff_contract.py` and any helper module created in [P2-T6], and pass/fail test counts; placeholder values are not permitted; any failure restarts the loop from P10-T1
- [x] [P10-T5] Install the extension package dependencies by running `npm --prefix extensions/drm-copilot ci` from the repository root, and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/npm-ci-extension.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` (installed package count); `extensions/drm-copilot/node_modules/.bin/eslint` exists afterwards
- [x] [P10-T6] Run `npm --prefix extensions/drm-copilot run format` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/prettier-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; the recorded `Command:` targets the `extensions/drm-copilot` package, because the repository-root `format` script globs only `src/**` and `tests/**` and therefore never reaches `extensions/drm-copilot/**`; if Prettier reformatted any file, the loop restarts from P10-T1, and only the final clean pass is recorded as the gate result
- [x] [P10-T7] Run `npm --prefix extensions/drm-copilot run lint` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/eslint-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` (zero errors); the recorded `Command:` targets the `extensions/drm-copilot` package, because the repository-root `lint` script is `eslint --no-error-on-unmatched-pattern src tests` and therefore never reaches `extensions/drm-copilot/**`; any failure restarts the loop from P10-T1
- [x] [P10-T8] Run `npm --prefix extensions/drm-copilot run typecheck` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/tsc-final.<ts>.md`
  - Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` (zero errors); the recorded `Command:` targets the `extensions/drm-copilot` package, whose `typecheck` script is `tsc -p ./ --noEmit` against `extensions/drm-copilot/tsconfig.json`, because the repository-root `typecheck` script compiles the root `tsconfig.json` whose `include` is `["src/**/*.ts","tests/**/*.ts"]` and therefore never reaches `extensions/drm-copilot/**`; this matches the CI precedent at `.github/workflows/_drm-copilot-extension-tests.yml:27`; any failure restarts the loop from P10-T1
- [x] [P10-T9] Run `npm run test:unit:coverage` from the repository root and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/jest-coverage-final.<ts>.md`. This is the ROOT `package.json` script; `extensions/drm-copilot/package.json` has no `test:unit:coverage` script and its `test:coverage` script emits `text-summary` only, which cannot satisfy the per-file requirement below. Do not redirect this task to the extension package. The root `jest.config.cjs` `testMatch` includes `**/extensions/drm-copilot/test/**/*.test.ts`, so the root run collects the extension test suite and reports per-file coverage for `extensions/drm-copilot/src/**`
  - Acceptance: the artifact contains `Timestamp:`, `Command:` recorded as `npm run test:unit:coverage` executed from the repository root, `EXIT_CODE: 0`, and `Output Summary:` recording numeric post-change line and branch coverage totals, the per-file line and branch coverage for `extensions/drm-copilot/src/lib/validate/parallel-kickoff-artifact.ts`, and pass/fail test counts; placeholder values are not permitted; any failure restarts the loop from P10-T1
- [x] [P10-T10] Verify the coverage delta and thresholds for both languages and write `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/coverage-delta.<ts>.md`. Compare `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/pytest-coverage-baseline.2026-08-08T13-56.md` (recorded baseline: **91.72%** line, **83.58%** branch, 2886 tests passed) against `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/pytest-coverage-final.<ts>.md`, and report the TypeScript totals from `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/jest-coverage-final.<ts>.md`. Report, with numeric values in every entry: (a) baseline Python line/branch coverage; (b) post-change Python line/branch coverage; (c) new/changed-code coverage as real percentages for `scripts/dev_tools/parallel_kickoff_contract.py`, for any helper module created in [P2-T6], and for `extensions/drm-copilot/src/lib/validate/parallel-kickoff-artifact.ts`; (d) post-change TypeScript line/branch totals. Enumerate the change set via the merge-base-anchored trio `git merge-base origin/epic/parallel-orchestration-integration HEAD` (recorded as `<BASE>`), `git diff --name-only <BASE>`, and `git status --porcelain --untracked-files=all`, with the change set defined as the union of the second and third outputs covering committed, staged, unstaged, and untracked paths, and list every production `.py` and `.ts` file in that union together with its measured line and branch coverage. Required outcome: post-change Python line coverage >= 85% and branch coverage >= 75%; post-change TypeScript line coverage >= 85% and branch coverage >= 75%; no regression versus the recorded baseline of 91.72% line / 83.58% branch; and new/changed-code coverage meeting the same >= 85% line / >= 75% branch thresholds for every production file in the change set
  - Acceptance: the artifact contains `Timestamp:`, all four reported entry groups with numeric values and no placeholder such as `UNVERIFIED`, both change-set `Command:` entries with the resolved `<BASE>` commit and `EXIT_CODE:` for each, the union change-file list with per-production-file coverage, and an explicit pass/fail verdict against each of the four required outcomes; if any required coverage value is unavailable or any verdict fails, the plan outcome is remediation-required, never PASS
- [x] [P10-T11] Write the final acceptance-criteria status summary to `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/other/ac-status-summary.<ts>.md`, enumerating all 22 `spec.md` criteria (19 pre-existing plus the three added by [P9-T1]) and all 8 `user-story.md` criteria with per-item status (`checked` with evidence path, or `unchecked` with reason) and the overall counts
  - Acceptance: the artifact contains `Timestamp:` and one status line per criterion (30 total) with evidence paths for every checked item; any unchecked item carries an explicit reason; the recorded overall counts sum to 30

## Test Plan

- New unit tests: `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` (Phase 2; positive, negative, and boundary scenarios per `.claude/rules/general-unit-test.md` scenario completeness) and `extensions/drm-copilot/test/lib/validate/parallel-kickoff-artifact.test.ts` (Phase 3; the same scenario matrix plus a cross-runtime parity block).
- Updated existing tests (Phase 3): `tests/scripts/dev_tools/test_validate_orchestration_artifacts_parallel_dispatch.py`, `extensions/drm-copilot/test/lib/validate/orchestration-artifacts-parallel-dispatch.test.ts`, `extensions/drm-copilot/test/mcp-tool-inputs-parallel-validation.test.ts`, `extensions/drm-copilot/test/mcp-parallel-validation-definitions.test.ts`, `extensions/drm-copilot/test/mcp-server-parallel-validation.test.ts` — each currently encodes the absence of `parallel-kickoff`, which the epic-manifest adjudication makes false by design.
- Contract test: `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py` (Phase 6; positive and negative fragment assertions per spec R9, plus the Phase 1 landed-contract supersession assertions and the F4-obligation assertions in [P6-T3]).
- Mirror gates exercised in Phase 7: `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and `tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py`.
- Full suites: `poetry run pytest --cov --cov-branch --cov-report=term-missing` at baseline (P0-T5) and final QA (P10-T4); `npm run test:unit:coverage` from the repository root at final QA (P10-T9).
- Coverage evidence: Python baseline `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/baseline/pytest-coverage-baseline.2026-08-08T13-56.md`; Python post-change `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/pytest-coverage-final.<ts>.md`; TypeScript post-change `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/jest-coverage-final.<ts>.md`; comparison `docs/features/active/2026-08-07-parallel-planner-surface-443/evidence/qa-gates/coverage-delta.<ts>.md`.

## Open Questions / Notes

- **Readiness git-integrity obligation — no new task required.** F3's landed rules file leaves git-integrity checks, launch-evidence binding, and kickoff-contract cross-checks to F4. This obligation is already covered by the existing checkpoint-section task [P5-T9], which requires documenting "git integrity against per-item branch refs plus `parallel/<slug>-plan`" and now attributes it explicitly to F4; [P6-T3] asserts that attribution in the contract test. No additional task is added for it.
- **Cohort recomputation parity is documentation plus a text assertion, not a module.** [P5-T7] documents the procedure in the skill and [P6-T3] asserts it in the contract test. No new production module is created for it, per F3's framing of P5 as the planner surface's own check.
- The Phase 7 mirror obligation is a verified, planning-time-discovered addition beyond the spec's "no other file" sentence; the deviation record, its enforcing test names, and the `SCOPED_ROOTS` confirmation that `scripts/dev_tools/**` and `extensions/drm-copilot/src/**` need no mirror are captured in the [P7-T4] evidence artifact and recorded in `spec.md` by [P9-T1].
- The R5 contingency has FIRED and is the operative path; the epic manifest instructs F4 to "treat its contingency branch as the selected path rather than re-checking whether F3 delivered the module." Phases 2 and 3 are that path. No task in this plan re-evaluates the contingency.
- Spec R10's "no executable production code" premise is superseded by this revision; [P9-T1] amends the spec so the spec and the delivered scope agree.
