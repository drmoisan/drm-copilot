# PoshQC Test Baseline (with coverage) — Issue #272

Timestamp: 2026-07-02T18-36
Command: `mcp__drm-copilot__run_poshqc_test` (scan folder: `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`), supplemented by a direct `Invoke-Pester -Configuration $config` coverage run (see Infrastructure Note below) with `CodeCoverage.Path = '.claude/hooks/enforce-pr-author-skill.ps1'`.
EXIT_CODE: 0
Output Summary: 46/46 tests passed, 0 failed (confirmed via `artifacts/pester/pester-junit.xml`, `tests="46" errors="0" failures="0"`). Baseline coverage for `.claude/hooks/enforce-pr-author-skill.ps1` (command-level, Pester/JaCoCo instrumentation — PowerShell tooling in this repo reports a single command-level percentage; no separate branch-coverage metric is produced by the JaCoCo `INSTRUCTION`/`LINE`/`METHOD` counters this toolchain emits): **90.99% (101/111 commands covered, 10 missed)**. This is well above the repository's 85% line-coverage floor.

Infrastructure Note: the `mcp__drm-copilot__run_poshqc_test` MCP tool's bundled `pester.runsettings.psd1` `CodeCoverage.Path` allowlist did not include `.claude/hooks/enforce-pr-author-skill.ps1` prior to this feature. The repo-tracked copies at `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` were both updated to add this file to the `CodeCoverage.Path` list (see Phase 5 deviations artifact). The MCP tool itself loads its settings from a separately-installed, non-repo-tracked extension package under the local VS Code Insiders server profile, which did not pick up the repo edit within this session (module already imported/cached). To obtain real numeric coverage evidence for this feature's changed file without substituting the MCP tool for the pass/fail test gate (the MCP tool call above already confirmed 46/46 passing), a supplementary direct `Invoke-Pester -Configuration $config` coverage run was executed using the exact same test file and the same Pester v5.x framework mandated by `.claude/rules/powershell.md`, with `CodeCoverage.Path` set explicitly to the target hook file. This is not a substitute test runner; it is the same Pester engine, invoked to work around a stale bundled-extension coverage allowlist.
