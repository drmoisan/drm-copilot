# Remediation Plan — portable-orchestrator-state-preflight (cycle 1)

- **Issue:** none
- **Timestamp:** 2026-07-06T15-01
- **Scope:** Resolve R-1 (Blocking — `.claude/hooks/enforce-pr-author-skill.ps1` exceeds the 500-line hard limit), R-1b (Blocking — bundled push-down snapshot not mirrored, byte-parity test fails), and R-2 (Low — duplicated `Test-PythonOrchestratorValidatorAvailable` probe across both hooks), per `docs/features/active/portable-orchestrator-state-preflight/remediation-inputs.2026-07-06T10-56.md`.
- **Out of scope:** R-3 (canonical coverage artifact membership), R-4 (documented Non-Goal, no action). Do not touch `.github/workflows/**` or `scripts/benchmarks/**`. Do not touch the `.codex`-flavored push-down snapshot (`extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/**`) — the byte-parity test governing R-1b (`test_bundled_claude_payload_contains_all_repo_runtime_contracts`) scopes only `.claude/**`, so the `.codex` copy is not required by any finding in scope (see Open Questions).

## Baseline Line Counts (observed before this cycle)

| File | Lines |
|---|---|
| `.claude/hooks/enforce-pr-author-skill.ps1` | 553 |
| `.claude/hooks/validate-orchestrator-output.ps1` | 369 |
| `.claude/lib/orchestrator-state/OrchestratorState.psm1` | 380 |

## Fix Approach

**R-1 / R-2 (file size, duplicated probe):** Move the shared capability-detection probe `Test-PythonOrchestratorValidatorAvailable` (duplicated verbatim in both hooks, ~27 lines each) into `.claude/lib/orchestrator-state/OrchestratorState.psm1` (already listed in `core.json`; no new module is introduced, so no manifest-membership test is required). If that alone does not bring `enforce-pr-author-skill.ps1` under 500 lines, also move `Invoke-OrchestratorStatePreflight` (~57 lines) into the same module, changing its `$CheckpointPath` default from the hook-local `$script:OrchestratorStateCheckpointPath` variable to the literal default `'artifacts/orchestration/orchestrator-state.json'`, and updating the hook's call site to pass `-CheckpointPath $script:OrchestratorStateCheckpointPath` explicitly so the existing test-time path-override mechanism is preserved unchanged.

Because `Invoke-OrchestratorStatePreflight` will live inside the module and call sibling module functions (`Test-PythonOrchestratorValidatorAvailable`, `Test-OrchestratorStatePrCreationReadiness`) without qualification, any Pester test that mocks those sibling functions while exercising `Invoke-OrchestratorStatePreflight`'s default `$Invoker` must add `-ModuleName OrchestratorState` (module-internal calls resolve in the module's own session state and are invisible to an unqualified `Mock` issued from outside the module). Tests that mock `Invoke-OrchestratorStatePreflight` itself from the hook's call site (`Get-PrAuthorBypassReason`, which is not part of any module) are unaffected and require no such change.

**R-1b (bundle parity):** Confirmed by running `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts`, which requires byte-identical parity between repo-root `.claude/**` and the bundled snapshot `extensions/drm-copilot/resources/claude-customizations/.claude/**` (agent-memory and `.claude-variants` excepted). This feature's `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/validate-orchestrator-output.ps1`, `.claude/lib/orchestrator-state/OrchestratorState.psm1`, and `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` were never mirrored into the bundle. After the R-1/R-2 edits land on the live `.claude/` tree (Phases 1-3), the FINAL content of every touched `.claude/**` file must be mirrored byte-for-byte into the bundle (Phase 4), then the parity test re-run to confirm the fix. The pack manifest `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` already lists both `.claude/lib/orchestrator-state/*.psm1` modules and lives outside the `.claude/**` byte-parity scope (under `pack-manifests/`), so it requires verification only, not an edit.

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/powershell.md` in that order, then write `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/validate-orchestrator-output.ps1`, and `.claude/lib/orchestrator-state/OrchestratorState.psm1`, and write `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-baseline-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (pass/fail and whether any file was reformatted).
- [x] [P0-T3] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to the same three files and write `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-baseline-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (finding count by severity).
- [x] [P0-T4] Run `mcp__drm-copilot__run_poshqc_test` (full suite, coverage-enabled, using `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`) and write `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-baseline-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including numeric baseline line-coverage and branch-coverage percentages.
- [x] [P0-T5] Record the pre-change line counts of `.claude/hooks/enforce-pr-author-skill.ps1` (553), `.claude/hooks/validate-orchestrator-output.ps1` (369), and `.claude/lib/orchestrator-state/OrchestratorState.psm1` (380) in `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-baseline-line-counts.md`.
- [x] [P0-T6] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and confirm it currently FAILS (pre-mirror baseline for R-1b), capturing the `test_bundled_claude_payload_contains_all_repo_runtime_contracts` assertion message identifying the mismatched bundle path(s); write `docs/features/active/portable-orchestrator-state-preflight/evidence/remediation-baseline/remediation-cycle-1-baseline-bundle-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the failing test name, pass/fail counts, and the assertion text.

### Phase 1 — Extract the Shared Capability-Detection Probe (resolves R-2; partial R-1)

- [x] [P1-T1] Add a `Test-PythonOrchestratorValidatorAvailable` function to `.claude/lib/orchestrator-state/OrchestratorState.psm1` (identical signature, doc comment, and body to the existing duplicated definitions), and add `Test-PythonOrchestratorValidatorAvailable` to the module's `Export-ModuleMember -Function` list.
- [x] [P1-T2] Delete the local `Test-PythonOrchestratorValidatorAvailable` function definition from `.claude/hooks/enforce-pr-author-skill.ps1` and add an unconditional `Import-Module (Join-Path $PSScriptRoot '../lib/orchestrator-state/OrchestratorState.psm1') -Force` statement immediately after the existing `$script:PrContextArtifactPath` / `$script:OrchestratorStateCheckpointPath` variable declarations, before the first function definition.
- [x] [P1-T3] Delete the local `Test-PythonOrchestratorValidatorAvailable` function definition from `.claude/hooks/validate-orchestrator-output.ps1` and add an unconditional `Import-Module (Join-Path $PSScriptRoot '../lib/orchestrator-state/OrchestratorState.psm1') -Force` statement immediately after `$ErrorActionPreference = 'Stop'`, before the first function definition.
- [x] [P1-T4] Dot-source `.claude/hooks/enforce-pr-author-skill.ps1` in a throwaway `pwsh -NoProfile -Command`. call and confirm it completes with no parse or resolution error, verifying no residual reference to the deleted local probe definition remains.
- [x] [P1-T5] Dot-source `.claude/hooks/validate-orchestrator-output.ps1` in a throwaway `pwsh -NoProfile -Command` call and confirm it completes with no parse or resolution error.

### Phase 2 — Extract the Orchestrator-State Preflight Helper (resolves remaining R-1 overage)

- [x] [P2-T1] Add an `Invoke-OrchestratorStatePreflight` function to `.claude/lib/orchestrator-state/OrchestratorState.psm1`, moved from `.claude/hooks/enforce-pr-author-skill.ps1`: change the `$CheckpointPath` parameter default from `$script:OrchestratorStateCheckpointPath` to the literal string `'artifacts/orchestration/orchestrator-state.json'`; simplify the default `$Invoker` scriptblock's portable branch to call `Test-OrchestratorStatePrCreationReadiness` directly with no `Import-Module`/`Get-Command` guard (both functions already share this module file); preserve the `{ ExitCode, Output }` return contract and the `{ HasErrors, ErrorText }` wrapper contract unchanged.
- [x] [P2-T2] Add `Invoke-OrchestratorStatePreflight` to the module's `Export-ModuleMember -Function` list in `.claude/lib/orchestrator-state/OrchestratorState.psm1`.
- [x] [P2-T3] Delete the `Invoke-OrchestratorStatePreflight` function definition from `.claude/hooks/enforce-pr-author-skill.ps1`.
- [x] [P2-T4] Update the call site inside `Get-PrAuthorBypassReason` in `.claude/hooks/enforce-pr-author-skill.ps1` from `Invoke-OrchestratorStatePreflight` to `Invoke-OrchestratorStatePreflight -CheckpointPath $script:OrchestratorStateCheckpointPath`, preserving the existing test-time path-override mechanism.
- [x] [P2-T5] Update the module's top-of-file `.DESCRIPTION` doc comment in `.claude/lib/orchestrator-state/OrchestratorState.psm1` to state that it now also hosts the capability-detection probe and the PR-creation preflight orchestration helper shared by both hooks.
- [x] [P2-T6] Count the lines of `.claude/hooks/enforce-pr-author-skill.ps1` and confirm the result is below 500 with margin; record the exact count in `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-cycle-1-line-count-verification.md`.
- [x] [P2-T7] Count the lines of `.claude/hooks/validate-orchestrator-output.ps1` and confirm the result remains below 500; record the exact count in the same evidence file as P2-T6.
- [x] [P2-T8] Count the lines of `.claude/lib/orchestrator-state/OrchestratorState.psm1` and confirm the result remains below 500; record the exact count in the same evidence file as P2-T6.

### Phase 3 — Relocate and Update Pester Coverage

- [x] [P3-T1] In `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, add a `Context 'Invoke-OrchestratorStatePreflight (direct seam tests)'` block that reproduces the four existing explicit-`$Invoker`-stub tests currently in `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (lines 54-81), calling the module's exported `Invoke-OrchestratorStatePreflight` directly with no module-scope changes required (these tests supply their own `$Invoker` and never rely on unqualified mocking of sibling functions).
- [x] [P3-T2] In the same test file, add a `Context 'capability detection (portable-path routing)'` block that reproduces the three existing routing tests currently in `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (lines 92-132), adding `-ModuleName OrchestratorState` to every `Mock -CommandName Test-PythonOrchestratorValidatorAvailable`, `Mock -CommandName Test-OrchestratorStatePrCreationReadiness`, and `Should -Invoke -CommandName Test-OrchestratorStatePrCreationReadiness` call so the mocks intercept the module-internal, unqualified calls made from within `Invoke-OrchestratorStatePreflight`.
- [x] [P3-T3] Delete the "Invoke-OrchestratorStatePreflight (direct seam tests)" `Context` block (lines 54-81) from `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`.
- [x] [P3-T4] Delete the "capability detection (portable-path routing)" `Context` block (lines 83-133) from `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`, leaving the "orchestrator-state preflight" (full-function-mock) and "script entrypoint (end-to-end)" contexts intact and unmodified — both mock or exercise `Invoke-OrchestratorStatePreflight` from the hook's own (non-module) call site, so they continue verifying capability-detection routing via the injected seam without depending on module-internal scope.
- [x] [P3-T5] Run `Invoke-Pester` scoped to `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and confirm every remaining test in that file passes, including the "script entrypoint (end-to-end)" real-subprocess test that still overrides `$script:OrchestratorStateCheckpointPath` before invoking the decision function.
- [x] [P3-T6] Run `Invoke-Pester` scoped to `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` and confirm every test (existing plus the two new contexts from P3-T1/P3-T2) passes.
- [x] [P3-T7] Run `Invoke-Pester` scoped to `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1` and confirm no test that references `Test-PythonOrchestratorValidatorAvailable`, `Invoke-OrchestratorStatePreflight`, or the receipt-verification order regresses.
- [x] [P3-T8] Run `Invoke-Pester` scoped to `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` and `tests/scripts/claude-hooks/validate-orchestrator-output.model-routing.Tests.ps1` and confirm all tests pass unmodified, including the `Test-PythonOrchestratorValidatorAvailable` mocks at line 527 (main file) and lines 116/132 (model-routing file), which continue to work unqualified because `Invoke-RoutingContractValidation` remains a plain (non-module) function in the hook.

### Phase 4 — Mirror the Push-Down Bundle Snapshot (resolves R-1b)

- [x] [P4-T1] Run `git diff --name-only 75eac1a..HEAD -- .claude/` and `git status --porcelain -- .claude/`, combine and deduplicate the resulting paths, exclude any path under `.claude/agent-memory/` and `.claude-variants/`, and write the final in-scope file list to `docs/features/active/portable-orchestrator-state-preflight/evidence/other/remediation-cycle-1-bundle-mirror-file-list.md`.
- [x] [P4-T2] Copy the current (post-Phase-3) content of `.claude/hooks/enforce-pr-author-skill.ps1` byte-for-byte, preserving line endings and encoding, to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-pr-author-skill.ps1`, overwriting the existing snapshot.
- [x] [P4-T3] Copy `.claude/hooks/validate-orchestrator-output.ps1` byte-for-byte, preserving line endings and encoding, to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1`, overwriting the existing snapshot.
- [x] [P4-T4] Create the directory `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/` (absent from the bundle today) and copy `.claude/lib/orchestrator-state/OrchestratorState.psm1` byte-for-byte into it as `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorState.psm1`.
- [x] [P4-T5] Copy `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` byte-for-byte into `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1`.
- [x] [P4-T6] For every path in the P4-T1 file list not already covered by P4-T2 through P4-T5, copy that file's final content byte-for-byte to its mirror path under `extensions/drm-copilot/resources/claude-customizations/.claude/**`, creating any missing parent directories.
- [x] [P4-T7] For each mirrored file pair from P4-T2 through P4-T6, run a byte-identity comparison (e.g. `git diff --no-index --stat <repo-root-path> <bundle-path>` or `Compare-Object (Get-Content -Raw ...)`) and confirm zero differences for every pair; write `docs/features/active/portable-orchestrator-state-preflight/evidence/other/remediation-cycle-1-bundle-byte-diff.md` listing each pair and its diff result.
- [x] [P4-T8] Read `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` and confirm it already lists both `.claude/lib/orchestrator-state/OrchestratorState.psm1` and `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1`; make no edit (the manifest is outside the `.claude/**` byte-parity scope and is already correct), and record the confirmation in `docs/features/active/portable-orchestrator-state-preflight/evidence/other/remediation-cycle-1-bundle-manifest-check.md`.
- [x] [P4-T9] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and confirm all 7 tests pass with exit code 0; write `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-bundle-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the pass count.

### Phase 5 — Final QA Loop

- [x] [P5-T1] Run `mcp__drm-copilot__run_poshqc_format` across the full repository; if any file is reformatted, restart this loop from this step.
- [x] [P5-T2] Run `mcp__drm-copilot__run_poshqc_analyze` across the full repository; if any finding is reported or any file is changed, restart the loop from P5-T1.
- [x] [P5-T3] Run `mcp__drm-copilot__run_poshqc_test` (full suite, coverage-enabled) and confirm zero failed tests; if any test fails or any file changes as a side effect, restart the loop from P5-T1.
- [x] [P5-T4] Compare the post-change line-coverage and branch-coverage percentages from P5-T3 against the Phase 0 baseline recorded in P0-T4 and confirm no regression on either metric.
- [x] [P5-T5] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` as the final-QA-gate confirmation (post Phase 1-4) that bundle parity holds; confirm all 7 tests pass with exit code 0. If this fails, return to Phase 4 to correct the mirrored file(s) — do not restart the PowerShell loop in P5-T1, since this is a separate, Python-only toolchain gate.
- [x] [P5-T6] Write `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-cycle-1-qa-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` for the P5-T1 run.
- [x] [P5-T7] Write `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-cycle-1-qa-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` for the P5-T2 run.
- [x] [P5-T8] Write `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-cycle-1-qa-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` for the P5-T3 run, including the baseline-vs-post-change coverage comparison from P5-T4.
- [x] [P5-T9] Write `docs/features/active/portable-orchestrator-state-preflight/evidence/qa-gates/remediation-cycle-1-qa-python-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` for the P5-T5 run.
- [x] [P5-T10] Write `docs/features/active/portable-orchestrator-state-preflight/evidence/other/remediation-cycle-1-r1-r1b-r2-closure.md` recording that R-1 (file-size overage), R-1b (bundle snapshot parity), and R-2 (duplicated probe) are resolved, citing the final line counts from P2-T6/P2-T7/P2-T8, the passing test evidence from Phase 3, the mirror/byte-diff/parity-test evidence from Phase 4 (P4-T7, P4-T9), and the final QA evidence from P5-T3 and P5-T5.

---

## Test Plan

- **Probe relocation (R-2):** `Test-PythonOrchestratorValidatorAvailable` exists in exactly one place (`OrchestratorState.psm1`) after this cycle; both hooks import it. No test invokes the real `python` executable directly (unchanged from before this cycle — every existing test mocks the probe or the `$Invoker` seam, never `python` itself).
- **Preflight-helper relocation (partial R-1):** `Invoke-OrchestratorStatePreflight` moves into `OrchestratorState.psm1` with its `{ ExitCode, Output }` → `{ HasErrors, ErrorText }` contract, injectable `$Invoker` seam, and `$CheckpointPath` override mechanism (now via explicit caller argument rather than an internal default) all preserved and covered by relocated tests (P3-T1, P3-T2) plus the retained hook-level tests (P3-T4).
- **Fail-closed / block-reason invariants:** The exact string `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` (enforce-pr-author-skill.ps1) and `MODEL_ROUTING_BLOCKED:` (validate-orchestrator-output.ps1) are untouched by this change and remain covered by the existing, unmodified tests verified in P3-T5 and P3-T8.
- **Receipt verification order:** `Test-PrAuthorReceiptVerification`'s five-check order is untouched by this refactor; existing coverage in `enforce-pr-author-skill.Tests.ps1` is reconfirmed in P3-T7.
- **File-size limit:** Explicit line-count checks for all three touched production files (P2-T6, P2-T7, P2-T8) confirm each is below 500 lines; the bundle mirror (Phase 4) inherits the same fixed line counts because the mirror is a byte-for-byte copy.
- **Coverage non-regression:** P5-T4 compares numeric baseline (P0-T4) vs. post-change (P5-T3) line and branch coverage percentages and requires no regression, per `.claude/rules/general-unit-test.md`.
- **Bundle parity (R-1b):** P0-T6 establishes the pre-mirror failing state of `test_bundled_claude_payload_contains_all_repo_runtime_contracts`; P4-T9 confirms the test passes immediately after mirroring; P5-T5 reconfirms the test still passes as part of the final QA gate, after all other phases have completed. P4-T7 independently confirms byte-identity for every mirrored pair.

## Open Questions

1. `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-pr-author-skill.ps1` is a sibling snapshot copy of the hook used for a different push-down flavor. The byte-parity test that governs R-1b (`test_bundled_claude_payload_contains_all_repo_runtime_contracts`) scopes only `.claude/**`, so this `.codex` copy is not required by any finding in scope for this cycle and this plan does not update it. Confirm this understanding before merge if the `.codex` flavor is expected to track `.claude/hooks/` changes through some other mechanism.
2. `Test-PythonOrchestratorValidatorAvailable`'s internal `try/catch` branches (real subprocess success, non-zero exit, and thrown exception) do not appear to have dedicated unit-test coverage independent of the mocked seam, either before or after this extraction. This is a pre-existing characteristic, not a regression introduced by this remediation; flag for a future, separate hardening pass if branch-coverage tooling surfaces it against the module.
