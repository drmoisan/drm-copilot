# Manual Validation — Allow-Through Case — Issue #272

Timestamp: 2026-07-02T19-42
Command: A real `pwsh` process dot-sourcing `.claude/hooks/enforce-pr-author-skill.ps1`, re-defining `Invoke-OrchestratorStatePreflight` to return `{ HasErrors = $false }` (standing in for a checkpoint that passes `--require-complete`), then invoking `Invoke-PrAuthorSkillDecision` with the same `CLAUDE_TOOL_INPUT` from the block-case validation.
EXIT_CODE: 0
Output Summary: `permissionDecision: "deny"`, but with `permissionDecisionReason` = `PR_AUTHOR_RECEIPT_MISSING: ...` — **not** `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`. This confirms the decision proceeded past the preflight check into the existing receipt-verification flow exactly as spec.md's Manual Validation step requires, and only stopped there because no real `artifacts/pr_body_1.receipt.json` file exists on disk (expected, since this is a validation run, not a real PR-authoring workflow).

Deviation from the plan's literal text: P7-T2 calls for "restoring a valid checkpoint" (implying the real `artifacts/orchestration/orchestrator-state.json` file would first have been deleted/renamed in P7-T1, then restored here). Since P7-T1 did not delete/rename the real live checkpoint (see `manual-validation-block.md`), there is nothing to "restore." Instead, this validation stands in for a passing checkpoint by overriding `Invoke-OrchestratorStatePreflight`'s return value directly (the same seam Pester's unit tests already exercise), which is the safest way to prove the "checkpoint passes -> falls through to receipt checks" code path without writing a fixture checkpoint file to disk (writing one would need to satisfy the full `--require-complete` schema, which is a large, brittle fixture to construct and maintain, and is unnecessary given the seam-level proof already demonstrates the correct control flow).
