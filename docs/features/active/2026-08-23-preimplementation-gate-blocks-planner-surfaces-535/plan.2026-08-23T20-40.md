# 2026-08-23-preimplementation-gate-blocks-planner-surfaces (Plan)

- **Issue:** #535
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-23T21-45
- **Status:** Revised per preflight deltas D1–D10 (`preflight-revisions.2026-08-23T21-30.md`); ready for re-validation
- **Version:** 1.1
- **Work Mode:** full-bug (AC source: `spec.md` only, per `acceptance-criteria-tracking`)

**Feature folder (FEATURE):** `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535`. All evidence paths below are relative to FEATURE and MUST resolve under `FEATURE/evidence/<kind>/` per `evidence-and-timestamp-conventions`. Replace `<timestamp>` in evidence filenames with the actual ISO-8601 `yyyy-MM-ddTHH-mm` value at write time.

**Fail-closed evidence rule:** Baseline artifacts, final-QA artifacts, and the coverage-comparison artifact are mandatory. If any required artifact is missing or lacks numeric coverage values, the outcome is remediation-required, never PASS.

**Evidence accounting rule:** Every evidence-producing task names its expected artifact path. Do not mark evidence-backed work complete without the artifact. Each command-step artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.

**Language scope:** PowerShell toolchain plus a targeted pytest leg for the two push-down contract suites, which gate the two bundle mirrors (`tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and `tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`). PowerShell toolchain per `.claude/rules/powershell.md`: format via `mcp__drm-copilot__run_poshqc_format`, analyze via `mcp__drm-copilot__run_poshqc_analyze`, test via `mcp__drm-copilot__run_poshqc_test` (Pester v5, coverage-enabled). Restart the loop from format whenever any stage fails or changes files. No branch-coverage gate applies to PowerShell; the uniform line-coverage threshold is >= 85%.

**Change budget and batching:** Four production PowerShell files exceed the direct-mode cap of 2, so implementation is split into two batches, each at most 3 production files + 3 test files. The executor delegates each implementation batch to `powershell-typed-engineer`. Batch 1: the two Claude hook copies plus the Claude test suite. Batch 2: the two `.codex` hook copies (byte-identical pair, same commit) plus the codex contract test suite.

**Production files in scope (four hook copies, from research section 1.3):**

1. `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` (canonical Claude)
2. `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` (Claude bundle mirror)
3. `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` (canonical Codex)
4. `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` (Codex bundle mirror; must stay byte-identical to the canonical `.codex` copy in the same commit)

**Test files in scope:**

- `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` (extend in place; no new test file)
- `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` (decision assertions only; the byte-identity `It` is not edited)

**Seven exempt checkpoint literals (the widened set for `Test-ImplementationPath`):**

1. `artifacts/orchestration/orchestrator-state.json`
2. `artifacts/orchestration/parallel-planner-state.json`
3. `artifacts/orchestration/parallel-orchestrator-state.json`
4. `artifacts/orchestration/epic-planner-state.json`
5. `artifacts/orchestration/epic-orchestrator-state.json`
6. `artifacts/orchestration/powershell-orchestrator-state.json`
7. `artifacts/orchestration/csharp-orchestrator-state.json`

**Naming pinned by this plan (the executor creates these identifiers):** the script-scoped array `$script:CheckpointPaths` (seven repo-relative literals behind one membership check), the helper function `Test-PreparationModeDelegation`, the Pester `Context` block named `issue #535 checkpoint write exemptions`, and the Pester `Context` block named `issue #535 preparation-mode delegation exemption`. The two preparation markers asserted by tests are the verbatim literals `Preparation mode: true.` and `route_id: preparation.` (trailing periods included), drawn from `.claude/skills/parallel-plan/SKILL.md` and `.claude/skills/epic-plan/SKILL.md`.

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md` at repo root. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [x] [P0-T4] Read `.claude/rules/powershell.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [x] [P0-T5] Write the policy-read evidence artifact to `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the P0-T1 through P0-T4 sequence), and the explicit list of files read. Acceptance: artifact exists with all three fields.
- [x] [P0-T6] Baseline format check: run `mcp__drm-copilot__run_poshqc_format` over the repository PowerShell sources. Record the result in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/baseline/baseline-poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (state whether any file was reformatted; expected: none). Acceptance: artifact exists with all four fields.
- [x] [P0-T7] Baseline lint: run `mcp__drm-copilot__run_poshqc_analyze` with repo settings. Record the result in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/baseline/baseline-poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count; expected: 0). Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Baseline test with coverage (Claude suite): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` with code coverage enabled for `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/baseline/baseline-pester-claude-hooks.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and the numeric line-coverage percentage for the canonical Claude hook file. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: artifact exists with a numeric coverage headline.
- [x] [P0-T9] Baseline test with coverage (codex contract suite): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` with code coverage enabled for `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/baseline/baseline-pester-codex-hooks.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and the numeric line-coverage percentage for the canonical Codex hook file. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: artifact exists with a numeric coverage headline.

### Phase 1 — Regression Tests and Fail-Before Evidence

Batch content: 0 production files, 1 test file (within the per-batch cap).

- [x] [P1-T1] Delegate to `powershell-typed-engineer`: extend `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` in place with the new cases for research groups 1 through 10, all driven through the existing pure seam `Invoke-OrchestrationPreimplementationGateDecision` with `-ToolInputRaw` (and no `-CheckpointRaw` for the bootstrap allow cases) — no disk I/O, no child processes, no temporary files. Required structure: a `Context` named `issue #535 checkpoint write exemptions` containing (a) one allow case per exempt literal for all seven literals listed in the plan preamble, `Write` payload, no checkpoint supplied; (b) a deny case for `artifacts/orchestration/some-other-file.json` (literal set, not directory prefix); (c) a deny case for `scripts/parallel-planner-state.json` (full-path equality); (d) an allow case using the backslash spelling `artifacts\orchestration\parallel-planner-state.json`. And a `Context` named `issue #535 preparation-mode delegation exemption` containing (e) an allow case with `subagent_type` `orchestrator` and the verbatim parallel-plan kickoff line from `.claude/skills/parallel-plan/SKILL.md` as the `prompt`; (f) an allow case with the verbatim epic-plan kickoff line from `.claude/skills/epic-plan/SKILL.md`; (g) a deny case with `subagent_type` `atomic-executor` and both markers in `prompt`; (h) a deny case with `subagent_type` `orchestrator` and a regex-matching execution prompt without the markers; (i) deny cases for one-marker-only and for a marker missing its trailing period, with an otherwise regex-matching payload; (j) a deny case with both markers placed in a non-`prompt` field while `prompt` is regex-matching text. Acceptance: all listed cases exist in the named `Context` blocks, the file remains under 500 lines, and no existing test is removed or weakened. Every deny case (b, c, g, h, i, j) MUST supply an explicit not-ready `-CheckpointRaw` built with the existing `ConvertTo-CheckpointRaw -RouteId '' -LifecycleReady $false` helper. Omitting it makes the decision depend on the on-disk `artifacts/orchestration/orchestrator-state.json`, which is currently ready, so the deny assertion would read allow and the test would be environment-dependent, violating the determinism requirement in `.claude/rules/general-unit-test.md`.
- [x] [P1-T2] [expect-fail] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` against the unmodified canonical hook. Expected: every new allow case from P1-T1 fails (the unfixed hook denies); all pre-existing tests pass. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/regression-testing/fail-before-pester.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode:` (the observed non-zero value), `Output Summary:` listing the failing test names and counts. Acceptance: artifact exists and shows the new allow cases failing against the unfixed hook.
- [x] [P1-T3] Check off the spec.md acceptance criterion for fail-before evidence (the `Fail-before evidence exists` item) in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/spec.md`, changing only `- [ ]` to `- [x]`. Acceptance: exactly that one checkbox is checked and the P1-T2 artifact exists.

### Phase 2 — Implementation Batch 1: Claude Hook Copies

Batch content: 2 production files, 1 test file (within the per-batch cap). Delegated to `powershell-typed-engineer`.

- [x] [P2-T1] Delegate to `powershell-typed-engineer`: edit `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` per spec design items 1 and 2: (a) add the script-scoped array `$script:CheckpointPaths` holding exactly the seven repo-relative literals from the plan preamble and replace the single-literal equality in `Test-ImplementationPath` with one membership check against the normalized path, evaluated after the existing backslash-to-forward-slash normalization; keep a distinct scalar pointed at `artifacts/orchestration/orchestrator-state.json` for `Get-CheckpointContent` and the block-message text; (b) add the helper `Test-PreparationModeDelegation` (`[CmdletBinding()]`, `[OutputType([bool])]`, mandatory `$ToolInput` with `[AllowNull()]`) returning `$true` if and only if `subagent_type` extracted via `Get-ClaudeHookToolInputString` equals exactly `orchestrator` AND the field-scoped `prompt` string contains both literals `Preparation mode: true.` and `route_id: preparation.`; call it first in `Test-ImplementationDelegation`, with any extraction failure falling through to the unchanged whole-payload regex. No change to `Test-OrchestrationReady`, `Test-ImplementationCommand`, the anomaly path, the extension regex, or the decision-JSON shape; the deny reason keeps the `PREIMPLEMENTATION_GATE_BLOCKED` prefix and the phrases `route metadata` and `lifecycle readiness`. Acceptance: both changes present, file under 500 lines.
- [x] [P2-T2] Delegate to `powershell-typed-engineer`: apply the identical behavioral change to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` so the file is byte-identical to `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`. Byte-identity is required, not merely identical hunks: `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` compares every repo `.claude/**` file to its bundled counterpart for content equality. Verify by comparing `Get-FileHash -Algorithm SHA256` of the two paths and record both hash values in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/other/claude-pair-hash.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the two SHA256 hashes are equal and recorded; file under 500 lines.
- [x] [P2-T3] Batch 1 toolchain hygiene: run `mcp__drm-copilot__run_poshqc_format` then `mcp__drm-copilot__run_poshqc_analyze` covering the three files edited in P1-T1, P2-T1, and P2-T2; restart from format if either stage fails or changes files, until both pass clean in a single pass. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/batch1-format-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each stage. Acceptance: artifact exists; both stages clean.
- [x] [P2-T4] Pass-after run: run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` with code coverage enabled for `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`. Expected: every test passes, including the two new `Context` blocks and the pre-existing anomaly, denial, and ready-checkpoint contexts (research groups 11 and 12). Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/regression-testing/pass-after-claude.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass counts and the numeric line-coverage percentage for the canonical Claude hook. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: exit code 0, zero failed tests, numeric coverage recorded.
- [x] [P2-T5] Commit batch 1: stage and commit the three batch-1 files (canonical Claude hook, Claude bundle hook, Claude test suite) in one commit. Acceptance: a single commit contains all three paths and no other production files.
- [x] [P2-T6] Check off spec.md AC items 1 through 4 (seven-literal membership check; allow for all seven literals in both spellings; deny for the non-checkpoint `.json`; deny for the checkpoint-named file outside `artifacts/orchestration/`) individually in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/spec.md`, each only after confirming its verifying tests passed in P2-T4. Acceptance: those four checkboxes are `[x]`; criterion text unchanged.
- [x] [P2-T7] Check off spec.md AC items 5 through 7 (three-conjunct delegation exemption with both verbatim kickoff lines allowed; spoofed-marker denials; unchanged classification for all other delegations) individually, each only after confirming its verifying tests passed in P2-T4. Acceptance: those three checkboxes are `[x]`; criterion text unchanged.
- [x] [P2-T8] Check off spec.md AC item 8 (fail-closed semantics preserved: anomaly denials, malformed-checkpoint denial, implementation write/command/delegation denials, ready-checkpoint allows, and unmodified `Test-OrchestrationReady`, `Test-ImplementationCommand`, and decision-JSON schema) after confirming the full pre-existing suite passed unmodified in intent in P2-T4. Acceptance: that checkbox is `[x]`; criterion text unchanged.
- [x] [P2-T9] Reset the PowerShell batch-budget state before Phase 3 so the four production hook copies do not exhaust the per-session production cap of 3 enforced by `.claude/hooks/enforce-powershell-batch-budget.ps1`. Run `Remove-Item -LiteralPath (Join-Path '.claude/state' ("powershell-batch-budget." + ($(if ($env:CLAUDE_SESSION_ID) { $env:CLAUDE_SESSION_ID } else { 'default' })) + ".json")) -Force -ErrorAction SilentlyContinue`, which is the reset mechanism named in the hook's own deny message. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/other/batch-budget-reset.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (state file absent after the reset). Acceptance: artifact exists; the state file does not exist when Phase 3 begins.

### Phase 3 — Implementation Batch 2: Codex Hook Copies

Batch content: 2 production files, 1 test file (within the per-batch cap). Delegated to `powershell-typed-engineer`. The two `.codex` copies must land byte-identical in the same commit to keep the hash-binding contract test green.

- [x] [P3-T1] Delegate to `powershell-typed-engineer`: apply the same two behavioral exemptions (seven-literal checkpoint membership check with a retained readiness scalar; three-conjunct preparation-mode delegation predicate evaluated before the existing regex) to `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` in that file's own idiom (its own line numbering and Codex transport are preserved; its exit-code contract is unchanged). Acceptance: both exemptions present; file under 500 lines; all other classification logic unchanged. The `.codex` copy has no access to `Get-ClaudeHookToolInputString` (it dot-sources only `codex-pretooluse-file-mapping.ps1`, which does not import the Claude `HookPayload.psm1`). Read `subagent_type` and `prompt` with the `Get-StringProperty` helper already defined in that file. Do not add a cross-runtime import of `.claude/lib/hook-payload/HookPayload.psm1`.
- [x] [P3-T2] Copy the updated canonical `.codex` hook byte-identically to `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` and verify identity by comparing `Get-FileHash -Algorithm SHA256` of the two paths. Record the two hash values in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/other/codex-pair-hash.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the two SHA256 hashes are equal and recorded.
- [x] [P3-T3] Delegate to `powershell-typed-engineer`: add codex-side decision coverage for the two exemptions to `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1`. The file's existing preimplementation-gate assertions (the deny cases in the `It` named 'denies preimplementation and batch-budget violations through their pure decisions') are unchanged by the exemptions, so this task is purely additive. The file is 478 lines against the 500-line limit, so the addition MUST be a single table-driven `It` of at most 18 lines covering one exempt checkpoint-literal allow and one preparation-mode delegation allow, in the file's existing builder and assertion style. The byte-identity `It` is not edited. Acceptance: the file's line count after the edit is under 500 (recorded in P4-T5); no existing assertion is removed or weakened.
- [x] [P3-T4] Batch 2 toolchain hygiene: run `mcp__drm-copilot__run_poshqc_format` then `mcp__drm-copilot__run_poshqc_analyze` covering the three files edited in P3-T1 through P3-T3; if the formatter changes either `.codex` copy, re-apply byte-identity (P3-T2) before proceeding; restart from format until both stages pass clean in a single pass. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/batch2-format-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each stage. Acceptance: artifact exists; both stages clean; hash equality still holds.
- [x] [P3-T5] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` with code coverage enabled for `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`. Expected: every test passes, including the byte-identity `It` over `$script:PreToolHookNames` and the updated decision assertions. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/regression-testing/pass-after-codex.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass counts and the numeric line-coverage percentage for the canonical Codex hook. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: exit code 0, zero failed tests, numeric coverage recorded.
- [x] [P3-T6] Check off the spec.md AC item for the extended test file (thirteen case groups through the pure seam, verbatim SKILL.md kickoff lines, no disk I/O, child processes, or temporary files) after P2-T4 and P3-T5 have both passed; group 13 is the Codex parity group, first verified at P3-T5. Acceptance: that checkbox is `[x]`; criterion text unchanged.
- [x] [P3-T7] Commit batch 2: stage and commit the two `.codex` hook copies and the codex contract test suite in one commit (the byte-identical pair lands together). Acceptance: a single commit contains all three paths, including both `.codex` copies.
- [x] [P3-T8] Check off the spec.md AC item for four-copy parity (identical behavioral fix present in all four hook copies) after P2-T4 and P3-T5 both passed. Acceptance: that checkbox is `[x]`; criterion text unchanged.
- [x] [P3-T9] Check off the spec.md AC item for `.codex` byte-identity and the hash-binding contract test (same commit, passing test, decision assertions updated per the established mechanism) after P3-T5 and P3-T7 completed. Acceptance: that checkbox is `[x]`; criterion text unchanged.

### Phase 4 — Final QA Loop, Coverage Verification, and Closure

Full PowerShell QA loop: format → analyze → test with coverage. If any stage fails or changes any file, restart the loop from format and repeat until all stages pass clean in a single pass. Every command task below is unconditional; `SKIPPED` is not a valid outcome for any of them.

- [x] [P4-T1] Final format: run `mcp__drm-copilot__run_poshqc_format` over the repository PowerShell sources. If any file changes, re-verify `.codex` pair hash equality and restart the loop from this task. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/final-poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: clean pass recorded (no file changed on the final iteration).
- [x] [P4-T2] Final lint: run `mcp__drm-copilot__run_poshqc_analyze` with repo settings. On failure, remediate and restart the loop from P4-T1. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/final-poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count 0). Acceptance: zero findings on the final iteration.
- [x] [P4-T3] Final test with coverage: run `mcp__drm-copilot__run_poshqc_test` over the folders `tests/scripts/claude-hooks` and `tests/scripts/codex-hooks`, which contain all four affected suites, including `tests/scripts/claude-hooks/PreToolUseSchema.Contract.Tests.ps1` and `tests/scripts/codex-hooks/codex-pretooluse-transport.Tests.ps1`, both of which dot-source the changed hooks; code coverage enabled for `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` and `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`. On any failure, remediate and restart the loop from P4-T1. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/final-pester.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including total pass/fail counts and the numeric line-coverage percentage for each of the two covered hook files. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: exit code 0, zero failed tests, two numeric coverage values recorded.
- [x] [P4-T4] Coverage delta and threshold verification: write `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/coverage-delta.<timestamp>.md` recording (a) baseline coverage from P0-T8 and P0-T9, (b) post-change coverage from P4-T3, (c) the changed-code result: line coverage >= 85% on `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` and on `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`, with no regression versus baseline, and (d) mirror-inheritance evidence for the two bundle copies that no test suite executes directly — the `.codex` bundle inherits the canonical `.codex` measurement via the SHA256 hash equality recorded in P3-T2/P3-T4, and the Claude bundle inherits the canonical Claude measurement via the SHA256 hash equality recorded in P2-T2, not via a hunk comparison. If any numeric value is unavailable or below threshold, the outcome is remediation-required. Extract the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and record the extracted numeric value in `Output Summary:`. Coverage instrumentation is supplied by the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, which already registers both canonical hook files; the MCP test tool exposes no per-invocation coverage parameter. Acceptance: artifact exists with all four sections and every threshold met.
- [x] [P4-T5] Scope and size verification: confirm no out-of-scope change is present — issue #516 absolute-path normalization not implemented (exempt entries remain repo-relative literals behind one membership check, no per-path normalization logic added), `Test-ImplementationCommand` and `Test-OrchestrationReady` unmodified, `enforce-promotion-mcp-only.ps1` untouched — and confirm each of the six files — the four hook copies, `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1`, and `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` — is under 500 lines by counting lines in each file. Record the per-file line counts and the scope confirmation in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/scope-and-size.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; all six counts under 500; scope confirmations recorded.
- [x] [P4-T6] Push-down parity verification: run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`. These suites gate the two bundle mirrors edited in P2-T2 and P3-T2; the Claude suite asserts content equality for every repo `.claude/**` file against its bundled counterpart. On failure, restore byte-identity and restart the PowerShell loop from P4-T1. Record in `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/evidence/qa-gates/pushdown-parity.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: exit code 0, zero failed tests.
- [x] [P4-T7] Check off the spec.md AC item for the clean PoshQC toolchain pass with line coverage >= 85% on every changed production hook file, after P4-T1 through P4-T4 completed clean. Acceptance: that checkbox is `[x]`; criterion text unchanged.
- [x] [P4-T8] Check off the spec.md AC item for no out-of-scope changes and file-size limits, after P4-T5 completed. Acceptance: that checkbox is `[x]`; criterion text unchanged.
- [x] [P4-T9] Report the Acceptance Criteria Status summary (source `docs/features/active/2026-08-23-preimplementation-gate-blocks-planner-surfaces-535/spec.md`, total 14, checked-off count, remaining items) in the executor's final completion report per `acceptance-criteria-tracking`. Acceptance: summary present in the completion report; all 14 items `[x]` or each remaining item documented with its gap.
