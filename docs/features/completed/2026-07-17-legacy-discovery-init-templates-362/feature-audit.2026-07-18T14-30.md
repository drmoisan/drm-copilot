# Feature Audit: legacy-discovery-init-templates (#362) — R4 Re-Audit, Remediation Cycle 3

**Template source note:** MCP template resolution is unavailable in this session; the canonical repository template `docs/features/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md` was used as the authoritative structure source.

**Audit Date:** 2026-07-18
**Feature Folder:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/`
**Base Branch:** `epic/legacy-discovery-and-parity-integration`
**Head Branch:** `feature/legacy-discovery-init-templates-362` (HEAD `f17f1af08c67568fbc14140a25882c068a50d2b0`)
**Work Mode:** `full-feature` (per `- Work Mode: full-feature` in `issue.md`) -> AC sources are `spec.md` and `user-story.md`.
**Audit Type:** Post-remediation acceptance re-verification (R4, remediation cycle 3 -- bundled-resources push-down fix).

---

## Scope and Baseline

- **Base branch:** `epic/legacy-discovery-and-parity-integration` (merge-base `85e7bea2bd2695114c9feffb2a4963da9f37c9ad`, caller-supplied and independently confirmed via `git merge-base HEAD origin/epic/legacy-discovery-and-parity-integration` at the start of this audit, before the remote tip advanced further during this session).
- **Head branch/commit:** `feature/legacy-discovery-init-templates-362` at `f17f1af08c67568fbc14140a25882c068a50d2b0`.
- **Merge base:** `85e7bea2bd2695114c9feffb2a4963da9f37c9ad`.
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this audit; was stale prior to regeneration -- see `policy-audit.2026-07-18T14-30.md`, "PR-Context Freshness Correction").
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt` (regenerated alongside the summary).
  - Feature evidence: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/**`, including cycle-3's own `evidence/baseline/r3c1-*` and `evidence/qa-gates/r3c1-*` artifacts.
  - Additional evidence: independent toolchain re-execution, independent SHA-256 verification of the four copied files, and independent reproduction of the previously-failing test now passing, all documented in `policy-audit.2026-07-18T14-30.md` and `code-review.2026-07-18T14-30.md`.
- **Feature folder used:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/` (only active feature folder whose suffix, `-362`, matches the issue number in the branch name and the canonical issue reference for this feature).
- **Requirements source:** `spec.md` and `user-story.md` (per `full-feature` work mode).
- **Work mode resolution note:** `issue.md` line 8 reads `- Work Mode: full-feature`, an explicit, well-formed marker -- no fail-closed normalization was required.
- **Scope note:** This is the third remediation cycle. Cycle 1 closed four Blocking findings against the original implementation. Cycle 2 resolved a `pyproject.toml` merge conflict against the integration branch's then-current tip. Cycle 3's sole authorized scope was the byte-for-byte push-down copy of four `.claude/agents/*.md` persona files into the bundled extension payload, closing the sole Blocking finding recorded by the cycle-2 re-audit (attributed to sibling feature #365, not to this feature's own commits). This audit independently re-verifies that (a) the four files are present and byte-identical, (b) the previously-failing test now passes and the full toolchain completes clean with zero failures, and (c) all 8 acceptance criteria in each of `spec.md` and `user-story.md` remain consistent with the current merged tree -- not merely that their checkboxes were left checked by a prior cycle. This audit also independently reproduces, and records as informational and out of this cycle's scope per explicit caller instruction, a separately-identified `pyproject.toml` conflict against the integration branch's *current* remote tip (which has advanced past this feature's merge-base via sibling feature #363 since cycle 2). That conflict does not touch or affect the `85e7bea2..HEAD` diff evaluated below.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md` -- primary source, `## Acceptance Criteria` heading (lines 415-453).
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md` -- secondary source, `## Acceptance Criteria` heading (lines 89-127).

Both files carry an identical set of 8 checkbox criteria, all already marked `[x]` prior to this audit (checked off during cycle 1's remediation). `spec.md` additionally carries a separate `## Definition of Done` checklist (lines 403-413) that is not an AC source under `full-feature` work mode and is not evaluated below; its checkboxes remain unchecked, recorded as a non-blocking documentation-hygiene observation in `policy-audit.2026-07-18T14-30.md` (section 8), not an AC gap.

### Acceptance criteria (identical text in `spec.md` and `user-story.md`)

1. `dev.discovery.init <target-dir>` scaffolds the discovery workspace directory layout at the given target consumer path in a single invocation.
2. `dev.discovery.init` accepts an explicit target-directory CLI argument (not the drm-copilot workspace root) and an optional `--template-root` override consistent with the `new_active_feature_folder`/`new_potential_bug_entry` precedent.
3. Initialization writes a starter domain-profile config, authored as a nested YAML document (`profile_version`, `legacy_source.root`, `target.root`, `technology_stack.legacy[]`, `artifacts.root`) with placeholder tokens, in the shape required by the merged domain-profile loader (feature #360, `scripts/dev_tools/discovery/domain_profile.py`), and parses cleanly under that loader's `parse_domain_profile_text`.
4. Initialization writes starter instances of each of the seven discovery artifacts (Feature Contract, Coverage Ledger, Runtime Characterization Scenario, Parity Matrix, Unspecified Behavior Record, Product Decision Record, Evidence Reference) from the templates under `docs/discovery/templates/artifacts/`, in the same invocation as the domain profile.
5. Each artifact template's `$schema` field is a relative, scheme-less path resolvable by `validate_json.py`'s existing no-scheme `_load_schema` branch, per feature 9002's planned schema-versioning convention; the open question about resolving `$schema` from inside an external consumer repository is recorded in the spec/user-story and left to feature 9002, not resolved here.
6. Templates and generated artifacts contain no domain-specific identifiers (verified by the domain-neutrality regression test).
7. `dev.discovery.init` fails fast, before writing any file, when: the target path exists and is not a directory; the target path's parent does not exist; or the resolved template root is missing or has a partial template set.
8. `dev.discovery.init` is registered and invocable as a Poetry console-script (`"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` in root `pyproject.toml`).
9. Tests under `tests/scripts/dev_tools/discovery/` satisfy repository quality-tier policy (line coverage >= 85%, branch coverage >= 75%), use an injected fake `FileSystem` with no real filesystem/temp-file I/O, and include the schema-conformance test implemented against the merged `schemas/discovery/v1/` files (no longer tracked as dependent on feature 9002).

(Both `spec.md` and `user-story.md` contain exactly 8 checked `- [x]` bullets under `## Acceptance Criteria`; item 9 above is the transcription of the final of those 8 bullets, not a distinct 9th checkbox.)

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | `dev.discovery.init <target-dir>` scaffolds the workspace in a single invocation | PASS | `scripts/dev_tools/discovery/init_flow.py::create_discovery_workspace` writes all 8 output files in one function call, invoked once from `init_cli.main`. Unchanged by cycle 3; independently re-confirmed passing at HEAD `f17f1af0`. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -k full_layout -v` | Not affected by cycle 3's change (a bundled `.claude/agents/` payload fix, unrelated to the discovery module). |
| 2 | Explicit target-directory arg + optional `--template-root` override | PASS | `init_cli.py::parse_args` unchanged since cycle 1; independently re-read at HEAD `f17f1af0`. | Direct read of `scripts/dev_tools/discovery/init_cli.py`. | Unaffected by cycle 3. |
| 3 | Starter domain-profile config parses under `parse_domain_profile_text` | PASS | `docs/discovery/templates/domain-profile/domain-profile.yaml` unchanged by cycle 3; re-verified passing as part of the 1783-passing full-suite run at HEAD `f17f1af0`. | `poetry run pytest tests/scripts/dev_tools/discovery -k domain_profile -v` | Unaffected by cycle 3. |
| 4 | Starter instances of all seven discovery artifacts written | PASS | `init_models.OUTPUT_RELATIVE_PATHS` unchanged; `test_generated_artifacts_conform_to_real_schemas` re-verified passing. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -k conform -v` | Unaffected by cycle 3. |
| 5 | Template `$schema` fields are relative, scheme-less, `validate_json.py`-compatible | PASS | `poetry run dev.validate-json` exits 0 against the templates (independently re-run at HEAD `f17f1af0`). | `poetry run dev.validate-json` | Unaffected by cycle 3. |
| 6 | No domain-specific identifiers in templates/output | PASS | `test_domain_neutrality.py` (4 tests) re-verified passing at HEAD `f17f1af0`. | `poetry run pytest tests/scripts/dev_tools/discovery/test_domain_neutrality.py -v` | Unaffected by cycle 3. |
| 7 | Fail-fast before any write on invalid target/template-root | PASS | `create_discovery_workspace` validates before any write (unchanged since cycle 1, directly re-read at HEAD `f17f1af0`); each named failure mode has a dedicated passing test. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -v` | Unaffected by cycle 3. |
| 8 | Registered as Poetry console-script `dev.discovery.init` | PASS | `"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` present in `pyproject.toml` at HEAD `f17f1af0` (unchanged since cycle 2's merge-conflict resolution; cycle 3 touched no line of `pyproject.toml`). `poetry check` exits 0. | `git show f17f1af0:pyproject.toml \| grep dev.discovery.init`; `poetry check` | Unaffected by cycle 3. |
| 9 | Tests satisfy coverage policy, use fake `FileSystem`, include schema-conformance | PARTIAL | Line coverage remains 100% on all three new production files; branch coverage remains 100% on `init_flow.py`, N/A (no branches) on `init_cli.py`, and 50% (6/12) on `init_models.py`, below the uniform 75% new-file branch threshold -- attributable entirely to `Protocol` stub-method artifacts, not untested behavior, unchanged since cycle 2 (see `policy-audit.2026-07-18T14-30.md` section 5). `FakeFileSystem` (no real disk I/O) is used throughout. The schema-conformance test is present and passing. | `poetry run pytest --cov --cov-branch --cov-report=term-missing tests/scripts/dev_tools/discovery` | Unchanged PARTIAL classification from the cycle-2 audit's independent evaluation, on the narrow branch-coverage sub-clause only; not affected or resolved by cycle 3's change, which touched no Python file. |

---

## Summary

**Overall Feature Readiness:** PASS (with one narrow, non-blocking, unchanged coverage observation on criterion 9; zero Blocking findings remain in this feature's own scope after cycle 3's fix).

**Criteria summary (8 checkbox items per source file; criterion "9" above is the tests bullet's transcription, not a distinct 9th checkbox):**
- **PASS:** 8 of 8 checkbox criteria in each of `spec.md` and `user-story.md` remain independently re-verified correct against the current merged tree at HEAD `f17f1af0`, with one caveat noted below.
- **PARTIAL:** 1 (the tests-and-coverage criterion's branch-coverage sub-clause on `init_models.py`; unchanged since cycle 2, not affected by cycle 3).
- **UNVERIFIED:** 0.
- **FAIL:** 0.

**Top gaps preventing a fully clean PASS:**

1. `scripts/dev_tools/discovery/init_models.py` branch coverage is 50% (6/12), below the uniform 75% new-file threshold, driven entirely by `Protocol` stub-method coverage.py artifacts rather than untested behavior. Non-blocking; unchanged since cycle 2; not affected by cycle 3.

**Change from the cycle-2 audit's gap list:** the cycle-2 audit's gap #2 ("the integration branch's own tip carries a pre-existing test failure ... attributable to sibling feature #365") is now **resolved**: cycle 3's push-down copy independently verified byte-identical, and `test_bundled_claude_payload_contains_all_repo_runtime_contracts` independently re-confirmed passing at HEAD `f17f1af0`. That gap no longer appears in this audit's top-gaps list.

**Out-of-cycle-scope observation (not a gap in this feature's acceptance criteria):** the integration branch's current remote tip has advanced past this feature's merge-base via a separately-merged sibling feature (#363), reintroducing a `pyproject.toml` merge conflict against that current tip (independently reproduced via `git merge-tree`). None of this feature's own 8 acceptance criteria concern `pyproject.toml`'s relationship to the integration branch's *current* tip (criterion 8 concerns only the presence and correctness of this feature's own console-script entry, both of which are independently confirmed intact at HEAD). Per the caller's explicit instruction, this observation is deferred to a planned cycle 4 and does not affect this feature's acceptance-criteria verdict.

**Recommended follow-up verification steps:**

1. Optionally, extract the `FileSystem` `Protocol` in `init_models.py` into its own type-only module to eliminate the branch-coverage measurement artifact (non-blocking cleanup, carried forward from cycle 2).
2. Proceed with the orchestrator's planned cycle 4 to resolve the reintroduced `pyproject.toml` conflict against the integration branch's current remote tip before final PR merge.

---

## Acceptance Criteria Check-off

All 8 checkbox criteria in each of `spec.md` and `user-story.md` were already marked `[x]` prior to this audit (checked off during cycle 1's remediation). This audit independently re-verified each against the current merged tree (HEAD `f17f1af0`) rather than accepting the prior check-off at face value, and confirms all 8 remain correct and consistent with the current code. No new check-off action was required or performed, since no criterion transitioned from unchecked to independently-verified-PASS during this cycle. No criterion is downgraded to unchecked: the one PARTIAL finding above (branch-coverage sub-clause of the tests criterion) is a narrow, non-blocking gap within an otherwise-PASS criterion, unchanged from the cycle-2 audit's classification, not a basis for reverting the existing `[x]`.

### AC Status Summary

- Source: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md`, `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md`
- Total AC items: 8 (per source file; 16 total across both files, identical text)
- Checked off (delivered): 8 per source file (16 total) -- unchanged by this audit
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 8 | 8 | 0 | Checkbox-backed; all 8 independently re-verified against current merged tree at HEAD `f17f1af0`. `## Definition of Done` (separate section, 6 items) is not an AC source under `full-feature` work mode and remains unchecked -- documentation-hygiene observation only. |
| `user-story.md` | 8 | 8 | 0 | Checkbox-backed; identical text to `spec.md`; all 8 independently re-verified. |

No source-file checkbox change was made by this audit: all 8 items in each file were already `[x]` and remain independently verified correct.
