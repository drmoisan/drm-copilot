# PoshQC Test — Final QA and Coverage Verification (Issue #357, Remediation Cycle 1)

Timestamp: 2026-07-17T15-05

Command: `mcp__drm-copilot__run_poshqc_test` (scan_folders: ["tests/scripts/claude-hooks/validate-planner-output.Tests.ps1"], using the updated `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`)

EXIT_CODE: 0

Output Summary: `artifacts/pester/pester-junit.xml` reports `tests="7" errors="0" failures="0"` (all 7 tests pass, including the new malformed-phase-heading test and the retained structural-contract test after the duplicate em-dash test was removed). However, a post-run grep of `artifacts/pester/powershell-coverage.xml` for `sourcefilename="validate-planner-output` still returns zero matches (exit code 1) — the canonical artifact still does not measure `.claude/hooks/validate-planner-output.ps1`.

**Root cause (discovered during this task, not anticipated by the plan or remediation inputs):** `mcp__drm-copilot__run_poshqc_test` executes `extensions/drm-copilot/resources/templates/run-poshqc-test.ps1`, which imports the PoshQC module from the bundled path `extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.psd1` (fixed relative to the extension's own template directory, independent of workspace root). That bundled module resolves its Pester settings to `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` — a separate, byte-for-byte-duplicate file from this cycle's budgeted `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. `git log` confirms every prior issue (#272, #214, #275, #301, #305, #312, #328, #334, #344) that added an entry to the `CodeCoverage.Path` allowlist updated **both** copies in the same commit (e.g. commit `f3701f7f` touched both paths). This cycle's change budget explicitly restricts edits to the `scripts/` copy only and explicitly forbids "any other file outside this budget," so the bundled extension copy was intentionally left unedited per that constraint. As a direct consequence, the canonical MCP-tool-driven coverage artifact cannot measure this file within this cycle's authorized change budget.

**Ad hoc, non-repo-modifying verification** (per the established gotcha in agent memory `project_powershell_coverage_gotchas.md`, item 1): ran `Invoke-Pester -Configuration $config` directly with `$config.CodeCoverage.Path = '.claude/hooks/validate-planner-output.ps1'` and an explicit scratchpad `OutputPath` (repo's tracked `coverage.xml` and `artifacts/pester/powershell-coverage.xml` were left untouched by this ad hoc run; confirmed via `git status --short` showing no diff to `coverage.xml`).
- Tests: 7 passed, 0 failed (consistent with the MCP-tool run).
- Line/command coverage: 115 of 156 analyzed commands executed = **73.72%** (up from the 69.87% pre-remediation baseline).
- Remaining uncovered line numbers: 45, 46, 49, 50, 51, 53, 54, 57, 75, 78, 81, 162, 176, 180, 184, 200, 209, 212, 217, 223, 244, 252, 261, 282, 283, 284, 285, 288 (28 unique lines). Lines 137 and 138 (this cycle's target) are now covered, confirming P1-T2's new test exercises the intended `-not $phaseMatch.Success` branch. Lines 167, 195, 196 also became covered as a side effect of the same new test (the "task before first phase" and "no canonical phase headings" branches it also triggers).
- Lines 45-57 (the entire body of `Get-PlanFileContent`) remain uncovered because every existing test (before and after this cycle) mocks `Get-PlanFileContent` rather than exercising its real disk-read logic; closing that gap would require additional tests beyond this cycle's one-test-case budget.

**Result: 73.72% ad hoc line coverage remains below the 85% uniform threshold in `.claude/rules/quality-tiers.md`, and the canonical `artifacts/pester/powershell-coverage.xml` still does not measure this file at all under this cycle's authorized change budget.** This cycle's fixes (allowlist entry in the budgeted settings copy + one new test case + duplicate-test removal) do not close the coverage gap; the toolchain loop is not restarted for this reason alone, since no step failed or modified files in this run, but the coverage acceptance criterion in P1-T1/P2-T3 remains unmet as of this task and is escalated in the completion report rather than silently marked as resolved.
