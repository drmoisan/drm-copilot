# Remediation Plan — legacy-discovery-dotnet-vsto-analyzers (#369), Remediation Cycle 1

- Timestamp: 2026-07-19T02-21
- Issue: #369
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/issue.md`)
- Branch: `feature/legacy-discovery-dotnet-vsto-analyzers-369`
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-inputs.2026-07-19T02-21.md` (single Blocking finding — PR #384 `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`, against base `epic/legacy-discovery-and-parity-integration`; the sole conflicted file is `pyproject.toml` in the `[tool.poetry.scripts]` table). No scope beyond that document is authorized.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`). No `artifacts/`-rooted evidence path is permitted. Prior full-feature-cycle evidence under `evidence/baseline/`, `evidence/other/`, and `evidence/qa-gates/` (the `phase0-*`, `final-qc-*`, and `coverage-delta.2026-07-18T21-15` files) MUST NOT be overwritten. All new artifacts for this cycle use the `r1c1-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root unless a task states otherwise. Shell is PowerShell unless a task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- Integration branch tip at capture: `origin/epic/legacy-discovery-and-parity-integration` = `2215ebf992ebfb46ab10674188c48d5a3a15cf3a`. Feature branch point of divergence: base `3a4985fa`. PR #384 head: `e5b890024824d258987f7d1350cfe93f7317c92f`.
- Conflicted region: `pyproject.toml`, the `[tool.poetry.scripts]` table only. The trial merge produced exactly one conflicted file; no other file, and no production or test logic (including `test_domain_neutrality.py`), conflicts.
- The conflict is additive on both sides. This feature (HEAD) adds two console-script entries: `"dev.discovery.dotnet" = "scripts.dev_tools.discovery.analyzer.stack_cli:main_dotnet"` and `"dev.discovery.vsto" = "scripts.dev_tools.discovery.analyzer.stack_cli:main_vsto"`. The integration branch (from sibling wave-2 feature #368) adds one entry: `"dev.discovery.parity-report" = "scripts.dev_tools.discovery.parity_report:main"`.
- Required resolution: retain all three console-script entries with no line dropped, edited, or duplicated; preserve the table's existing sorted-by-suffix ordering (the `"dev.discovery.*"` run ordered by the alias suffix, i.e. `dotnet` < `parity-report` < `vsto`); remove only the conflict-marker lines (`<<<<<<<`, `=======`, `>>>>>>>`). Change nothing else in `pyproject.toml`: no dependency changes, no coverage-configuration changes, no reordering or editing of any line outside the `"dev.discovery.*"` run.
- The merge must be recorded as a genuine two-parent merge commit (no squash, no rebase).

### Phase 0 — Baseline Capture and Divergent-Line Enumeration

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated.
- [x] [P0-T2] Run `git fetch origin epic/legacy-discovery-and-parity-integration` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-fetch.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the resulting SHA of `origin/epic/legacy-discovery-and-parity-integration`. Acceptance: exit code 0 and the artifact records the fetched SHA.
- [x] [P0-T3] Enumerate the true divergent `[tool.poetry.scripts]` lines and confirm the conflict shape matches the headline finding (no additional siblings landed after the remediation-inputs capture): run `git merge-base HEAD origin/epic/legacy-discovery-and-parity-integration` to obtain `<merge-base>`; then run `git show <merge-base>:pyproject.toml`, `git show HEAD:pyproject.toml`, and `git show origin/epic/legacy-discovery-and-parity-integration:pyproject.toml`, each piped to `Select-String -Pattern '"dev\.discovery\.'` to isolate the `dev.discovery.*` run; and separately run `git diff <merge-base> origin/epic/legacy-discovery-and-parity-integration -- pyproject.toml` to confirm the full set of hunks the integration side added since the merge base. Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-pyproject-scripts-enumeration.<TS>.md` with `Timestamp:`, the four commands and their verbatim output, and an explicit derived list of (a) lines present on HEAD but absent at the merge base, (b) lines present on the integration branch but absent at the merge base, and (c) any key present in both derived sets (expected: none). Acceptance: derived list (a) includes `"dev.discovery.dotnet"` and `"dev.discovery.vsto"`; derived list (b) includes `"dev.discovery.parity-report"`; and the artifact explicitly states whether any further `[tool.poetry.scripts]` lines beyond those three, or any other diverging `pyproject.toml` hunk, were found. If the integration side diverges anywhere in `pyproject.toml` outside the `[tool.poetry.scripts]` table, record it; that outcome is outside this plan's authorized single-conflict scope and must be escalated as a new finding rather than resolved ad hoc.
- [x] [P0-T4] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-black-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T5] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-ruff-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T6] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-pyright-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T7] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records the numeric pre-merge baseline line-coverage and branch-coverage percentages and the pass/fail/skip counts. Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid).

### Phase 1 — Merge the Integration Branch and Resolve the `pyproject.toml` Conflict

- [x] [P1-T1] Run `git merge --no-commit --no-ff origin/epic/legacy-discovery-and-parity-integration` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase1-merge-attempt.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the conflict result. Follow with `git diff --name-only --diff-filter=U` and record its output in the same artifact. Acceptance: the conflicted-file list is exactly `pyproject.toml` (no other file). If any other file is also conflicted, stop and do not proceed to P1-T2; that outcome is out of this plan's authorized scope and must be escalated as a new finding rather than resolved ad hoc.
- [x] [P1-T2] Resolve the `pyproject.toml` conflict by editing the conflicted region directly: remove the conflict-marker lines (`<<<<<<<`, `=======`, `>>>>>>>`); retain all three console-script entries — this feature's `"dev.discovery.dotnet"` and `"dev.discovery.vsto"` and the integration branch's `"dev.discovery.parity-report"` — with no line dropped and no line duplicated; order the resulting contiguous `"dev.discovery.*"` run by alias suffix (`dotnet`, then `parity-report`, then `vsto`), matching the table's existing sorted-by-suffix ordering; leave every other line in `[tool.poetry.scripts]` and everywhere else in the file unedited and in its pre-merge position; make no dependency changes and no coverage-configuration changes. Acceptance (all must hold): `Select-String -Pattern '<<<<<<<|=======|>>>>>>>' pyproject.toml` returns no matches; the contiguous `"dev.discovery.*"` block extracted via `Select-String -Pattern '"dev\.discovery\.' pyproject.toml` lists `"dev.discovery.dotnet"`, `"dev.discovery.parity-report"`, `"dev.discovery.vsto"` in that order; and each of those three keys appears exactly once in the resolved file.
- [x] [P1-T3] Verify no duplicate keys exist anywhere in the resolved `[tool.poetry.scripts]` block (not only the `"dev.discovery.*"` run): extract every assignment key in the block (all lines between the `[tool.poetry.scripts]` heading and the next `[tool.` heading that assign a value) and confirm no key string appears more than once. Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase1-duplicate-key-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact records zero duplicate keys.
- [x] [P1-T4] Confirm no unintended change was introduced outside the `"dev.discovery.*"` run: run `git diff pyproject.toml` and confirm the only added/removed lines are the resolution of the `"dev.discovery.*"` run (the three retained entries) and the removal of conflict markers — no dependency table, coverage table, or other `[tool.poetry.scripts]` line changed. Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase1-scoped-diff-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact confirms the diff is confined to the `[tool.poetry.scripts]` `"dev.discovery.*"` run and conflict-marker removal, with no dependency or coverage-config change.
- [x] [P1-T5] Run `poetry check` against the resolved `pyproject.toml` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase1-poetry-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0.

### Phase 2 — Final QA Loop (Python, Post-Merge)

- [x] [P2-T1] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P2-T1. Acceptance: exit 0 with no files changed, recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T2] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P2-T1. Acceptance: exit 0 recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T3] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with 0 errors recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [ ] [P2-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-merge line-coverage and branch-coverage percentages and the pass/fail/skip counts for the merged tree (this feature's code plus the integration branch's code, including sibling features #367 and #368). If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid). `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T5] Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-coverage-delta.<TS>.md` comparing the Phase 0 pre-merge baseline coverage (from `evidence/remediation-baseline/r1c1-phase0-pytest-baseline.<TS>.md`) against the P2-T4 post-merge coverage, reporting: baseline line/branch %, post-merge line/branch %, and confirmation that line coverage >= 85% and branch coverage >= 75% hold with no regression on the merged tree. Acceptance: the artifact contains all numeric values and both threshold checks pass.

### Phase 3 — Commit the Merge, Push, and Confirm Mergeability

- [x] [P3-T1] Immediately before committing, re-verify the integration branch tip has not advanced past the SHA fetched in P0-T2: run `git fetch origin epic/legacy-discovery-and-parity-integration` again and compare the resulting SHA to the SHA recorded in `r1c1-phase0-fetch.<TS>.md`. Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-phase3-pre-commit-tip-recheck.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: if the SHA is unchanged, proceed to P3-T2. If the SHA has advanced, stop, abort the in-progress merge (`git merge --abort`), and escalate as a new finding rather than proceeding with a merge base that is already stale.
- [x] [P3-T2] Stage the resolved `pyproject.toml` and any files the merge auto-resolved, then commit the merge as a genuine two-parent merge commit: `git add pyproject.toml`, then `git add -A` to pick up any remaining auto-merged files, then `git commit --no-edit` (do not use `git merge --squash` or `git rebase`; the commit must record both HEAD and `origin/epic/legacy-discovery-and-parity-integration` as parents). Acceptance: `git log -1 --format=%P` on the new commit lists exactly two parent hashes, and `git diff --name-only --diff-filter=U` is empty.
- [ ] [P3-T3] Stage and commit this cycle's plan-checklist updates and all Phase 0–2 evidence artifacts (`docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-plan.2026-07-19T02-21.md` and every new file under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r1c1-*` and `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-*`) in a follow-up commit with a professional, factual commit message referencing issue #369 remediation cycle 1. Acceptance: `git status --porcelain` is empty.
- [ ] [P3-T4] Push the updated branch: `git push origin feature/legacy-discovery-dotnet-vsto-analyzers-369`. Acceptance: the command exits 0 and the remote branch's HEAD matches the local branch's HEAD (verify with `git rev-parse HEAD` and `git rev-parse origin/feature/legacy-discovery-dotnet-vsto-analyzers-369` after the push).
- [ ] [P3-T5] Confirm PR #384 is no longer conflicting: run `gh pr view 384 --json mergeable` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r1c1-pr-mergeable-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the recorded `mergeable` value is `MERGEABLE` (not `CONFLICTING` or `UNKNOWN`).

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; the `pyproject.toml` `[tool.poetry.scripts]` conflict is resolved with all three console-script entries retained (`"dev.discovery.dotnet"`, `"dev.discovery.parity-report"`, `"dev.discovery.vsto"`), ordered by alias suffix within the contiguous `"dev.discovery.*"` run, with no dependency or coverage-config change and no other line altered; the merge is recorded as a genuine two-parent merge commit; fresh QA evidence exists under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/` with the `r1c1-` prefix without overwriting prior full-feature-cycle evidence; post-merge line coverage >= 85% and branch coverage >= 75% hold with no regression; the branch is pushed; and `gh pr view 384 --json mergeable` reports `MERGEABLE`.
