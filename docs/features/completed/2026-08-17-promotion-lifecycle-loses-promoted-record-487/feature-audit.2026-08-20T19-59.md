# Feature Audit: Promotion Lifecycle Promoted-Record Retention (#487)

**Audit Date:** 2026-08-20
**Feature Folder:** `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487`
**Base Branch:** `origin/main`
**Head Branch:** `bug/promotion-lifecycle-loses-promoted-record-487`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

---

## Scope and Baseline

- **Base branch:** `origin/main` (commit `cd4b887f4e56606a7aca4bd02e093829b33bf8db`)
- **Head branch/commit:** `bug/promotion-lifecycle-loses-promoted-record-487` (commit `6f7f864baa04523fb629a85e76acf58e5939d6f1`)
- **Merge base:** `cd4b887f4e56606a7aca4bd02e093829b33bf8db` (branch rebased onto base; `git diff origin/main...HEAD` is the exact change set)
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer against `origin/main`; merge base confirmed)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/**` (48 files across `baseline/`, `regression-testing/`, `qa-gates/`, `other/`)
  - Additional evidence: `extensions/drm-copilot/coverage/lcov.info` and `artifacts/python/lcov.info` (parsed independently); reviewer re-runs of the changed Jest suites (37/37) and pytest modules (28/28)
- **Feature folder used:** `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487`
- **Requirements source:** `spec.md` (sole source)
- **Work mode resolution note:** `issue.md` carries the explicit marker `- Work Mode: full-bug`; per the work-mode contract, `spec.md` is the sole acceptance-criteria source and the absence of `user-story.md` is correct, not a gap.
- **Scope note:** All 12 AC were pre-checked by the executor; every one was re-verified independently by this reviewer against the diff, the code, the evidence artifacts, and re-executed tests before accepting the check-off. `docs/features/potential/promoted/2026-08-17-promotion-lifecycle-loses-promoted-record.md` is deliberately absent (retained evidence of the defect; retroactive repair is out of scope per the spec) — its absence was confirmed and is not a finding.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` — only source (checkbox-based, `## Acceptance Criteria`, AC-1 through AC-12)

### Acceptance criteria

1. **AC-1** — The promoted record survives `new_active_feature_folder` in full mode.
2. **AC-2** — The promoted record survives in minor-audit mode.
3. **AC-3** — The active folder's `issue.md` is still produced with identical content.
4. **AC-4** — An unpromoted source is still moved.
5. **AC-5** — The `new_active_feature_folder` receipt post-condition holds.
6. **AC-6** — The `potential_to_issue` receipt post-condition holds.
7. **AC-7** — The sequenced lifecycle regression exists and reproduces the defect before the fix.
8. **AC-8** — The two defect-codifying Python assertions are inverted.
9. **AC-9** — The Python cluster stays in parity with the TypeScript behavior.
10. **AC-10** — `flow.ts` remains within the 500-line limit.
11. **AC-11** — The full toolchain passes for both languages with coverage recorded.
12. **AC-12** — The conflicting documentation is corrected.

(Wording abbreviated here; the full criterion text in `spec.md` is authoritative and was evaluated verbatim.)

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | AC-1 promoted record survives, full mode | PASS | `flow.promoted-disposition.test.ts` case 1 asserts the promoted path is present and byte-identical (`fs.files.get(promoted)).toBe(POTENTIAL_BODY)`) after `createActiveFolder` with `workMode: "full-bug"`, and asserts the `Copied potential file to <path>` emission. Fail-before capture shows this exact test failing pre-fix. Predicate read directly: the full-branch placement site takes `copyFile` when `retainsPotentialSource`. | Reviewer re-ran the suite: 37/37 pass (`npx jest ...`) | Copy arm verified at the code level, not only by test name. |
| 2 | AC-2 promoted record survives, minor-audit mode | PASS | `flow.promoted-disposition.test.ts` case 2 exercises the second placement site (minor-audit branch) with the same retention, byte-identity, and copy-emission assertions; fail-before capture shows it failing pre-fix. Both placement sites confirmed in the diff (`flow.ts` minor-audit branch and full branch each carry the copy/move conditional). | Same re-run | The spec's `flow.ts:283`/`:346` are pre-change line numbers; both sites verified at their post-change locations. |
| 3 | AC-3 `issue.md` content unchanged | PASS | Copy and move branches both feed the identical read-back-and-upsert of the work-mode marker; the diff leaves those lines untouched. New tests assert `- Work Mode: full-bug` / `minor-audit` / `full-feature` markers; the pre-existing `flow.test.ts` content assertions are unmodified and pass. | Diff inspection + suite re-run | The only behavioral difference is source retention; destination bytes are produced by the same code path. |
| 4 | AC-4 unpromoted source still moved | PASS | The predicate matches only paths under `docs/features/potential/promoted`, so a source directly under `docs/features/potential/` takes the move branch. Verified by: (a) predicate reading in both languages; (b) the unmodified existing `flow.test.ts` move case; (c) the new TS sibling-prefix boundary test (`promoted-notes-feature.md` is moved); (d) the new pytest `test_create_feature_folder_moves_unpromoted_potential`. | Suite re-runs, both languages | This is the adversarial MOVE invariant: the fix did not trade the copy defect for a move defect. |
| 5 | AC-5 `new_active_feature_folder` receipt post-condition | PASS | `requireReportedPathExists` runs on the hoisted filesystem against `result.target` and non-null `result.potentialIssuePath` — exactly the two values the receipt reports (`destinationPath`, sole `artifacts` entry). Three tests cover both throw arms (message contains tool name and path) and the success arm. Fail-before shows both throw-arm tests failing pre-fix (`Received function did not throw`). | Suite re-run | Hoisting verified: the assertion observes the same instance passed as `fs`. |
| 6 | AC-6 `potential_to_issue` receipt post-condition | PASS | Existence check on `outcome.destination` before the result record is constructed; throw names the tool and path. The `artifacts` entry is the GitHub issue URL and is excluded from the check — confirmed by reading the return construction (no URL reaches `exists`) and by the success-arm test in which the URL artifact passes without a stat. Two tests cover both arms. | Suite re-run | The directive's URL-not-stat-ed concern is confirmed satisfied. |
| 7 | AC-7 sequenced lifecycle regression, fail-before demonstrated | PASS | `promotion-lifecycle-sequence.test.ts` runs `promotePotential` then `createActiveFolder` against one shared in-memory filesystem implementing both seams, asserts the intermediate state, and asserts the promoted record survives byte-identical. Pre-fix failure recorded at `evidence/regression-testing/fail-before-ts-jest.2026-08-20T19-16.md`; post-fix pass at `evidence/regression-testing/post-fix-ts-flow.2026-08-20T19-32.md` and by reviewer re-run. | Suite re-run | Location note: the criterion names `evidence/baseline/` and `evidence/qa/`; the executor used the canonical `evidence/regression-testing/` kind per `evidence-and-timestamp-conventions`, which is the correct location. Substance fully satisfied; the spec's stated paths were non-canonical. |
| 8 | AC-8 Python assertions inverted | PASS | Diff confirms `assert potential_path not in fs.files` → `assert potential_path in fs.files` at `test_new_active_feature_folder.py:333` and `assert active_file not in fs.files` → `assert active_file in fs.files` at `_part4.py:284`, with the first test renamed to `..._retains_promoted_potential_...`. Both pass. | `poetry run pytest tests/scripts/dev_tools/test_new_active_feature_folder.py tests/scripts/dev_tools/test_new_active_feature_folder_part4.py` → 28/28 (with part5) | Move-arm coverage preserved via the new part5 unpromoted test, as the spec requires. |
| 9 | AC-9 Python/TS parity | PASS | Side-by-side comparison performed by this reviewer: both implementations compute the disposition once from the resolved source path, apply it at both placement sites, and emit byte-identical wording (`Copied potential file to ...` / `Moved potential file to ...`). The receipt assertion is deliberately not mirrored (Python CLI emits no receipt). Executor's inspection record at `evidence/other/ts-python-parity-inspection.2026-08-20T20-08.md` is consistent. | Diff inspection of both files in one view | Containment mechanics differ idiomatically (`isRelativeTo` vs `Path.relative_to`) but are behaviorally equivalent, including the sibling-prefix boundary. |
| 10 | AC-10 `flow.ts` within 500 lines | PASS | 492 lines post-change (444 baseline + 48). | `wc -l extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts` | 8 lines of headroom; noted in code review. |
| 11 | AC-11 full toolchain + coverage | PASS | Both loops completed a single consecutive clean pass (TS iteration 3, Py iteration 4) with per-stage numeric exit codes in `evidence/qa-gates/final-qc-loop-ledger.2026-08-20T20-42.md`. Coverage: TS 96.66%/90.04%, Py 92.60%/89.81% repo-wide; every changed file above 85/75; changed-line coverage 100%/100% — all re-derived independently from the lcov artifacts (uncovered-line sets match exactly). Reviewer re-ran the changed-scope suites in both languages. | Independent lcov parse; targeted `npx jest`/`poetry run pytest` re-runs | Location note: figures live in `evidence/baseline/` and `evidence/qa-gates/` rather than the criterion's literal `evidence/coverage/`; `coverage/` is not a canonical evidence kind, so the canonical placement is correct. Runner-enforcement gap for per-file thresholds recorded as a Minor non-blocking finding (code review CR row 1). |
| 12 | AC-12 documentation corrected | PASS | `Feature Playbook.md:14` now states copy-and-retain for promoted sources and move for unpromoted; `docs/features/potential/README.md:6` states copy with the record retained under `promoted/`; the duplicate-audit doc carries an appended dated correction note (2026-08-20, issue #487) covering the falsified line-15/28 claim and the two-way scoping error of the line-24 grep, without rewriting the historical body. Bonus consistency: the promotion-lifecycle SKILL gained check 4b and its bundle mirror is byte-identical (`cmp` exit 0). | Diff inspection; `cmp` | All three named files verified; the optional skill correction was also delivered. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 12 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. (Non-gating) Add per-file `coverageThreshold` entries in `jest.config.cjs` for the three changed TypeScript production files so the 85/75 gate this branch satisfies by evidence becomes runner-enforced.
2. (Optional) Add a Python boundary test for a sibling path that is a string prefix of the promoted root, for cross-language test parity.

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 12 criteria were already checked `[x]` in `spec.md` by the executor. This reviewer re-verified each criterion independently (diff inspection, code reading, evidence artifacts, independent lcov parsing, and re-executed test suites) and confirms every check-off is justified. No criterion was unchecked; no criterion required unchecking; no new check-off was made.
- No phantom criteria were added and no criterion text was modified.

### AC Status Summary

- Source: `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md`
- Total AC items: 12
- Checked off (delivered): 12
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `spec.md` | 12 | 12 | 0 | Checkbox-backed; sole authoritative source under `full-bug` work mode |

No source-file checkbox change was made by this audit: every criterion was already correctly checked and independently re-verified as PASS.
