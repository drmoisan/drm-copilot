# Issue #467 Authorized Extension Cycle 3 / Remediation Pass 6 Plan

- Canonical planner path: `/root/issue467_authorized_cycles_3_4/s28_exception_plan_revision`
- Planner route: `atomic-planner-c4`
- Required executor and preflight route: `atomic-executor-c4`
- Primary requirements: docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-inputs.2026-08-15T03-09.md
- Plan of record: docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-plan.2026-08-15T03-09.md
- Feature root: docs/features/active/2026-08-10-codex-native-parallel-orchestration-467
- Canonical evidence root: docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence
- Work mode: full-feature
- Reviewed base / merge base: main / 768e485ddf3b48b16aa7588a72709e17568ee5f5
- Latest reviewed exact committed HEAD: 2d44e14f48706bb317ee8b81d23b2b2f7cee1c5d
- Execution starting HEAD: 80fd06b835f6ec5c257b6c670a0bdfaf46cded0e
- Latest completed R5 policy status: NON-COMPLIANT
- Latest completed R5 feature readiness: NEEDS REVISION
- Latest completed R5 findings: exactly 1 Blocker; Major 0; Minor 0; Nit 0; Info 0
- Latest completed R5 acceptance criteria: 43 total; 39 PASS; 2 FAIL; 2 UNVERIFIED; 0 PARTIAL
- GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO
- RAW_BRANCH_RESULT: 0/0 UNAVAILABLE
- COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED
- Active authorization: `requested=2 consumed=0 remaining=2`
- Authorized remediation passes: 6 and 7 only; current remediation pass: 6 resumed before P1-T6; next pass remains 6 until R5
- EXECUTION_AUTHORIZED_AFTER_INDEPENDENT_PREFLIGHT: YES

## Authority, Historical State, and Fail-Closed Contract

The primary remediation input remains the immutable final-R5 record for the prior authorization. Its `TERMINAL_HANDOFF_ONLY: YES`, `EXECUTION_AUTHORIZED: NO`, and `CYCLE_3_AUTHORIZED: NO` markers accurately record exhaustion of that earlier authorization and are historical context only. They were first superseded by `artifacts/orchestration/orchestrator-state.json` at `remediation-loop-exit.user_authorized_additional_cycles_extension_2`, which records the separate exact user authorization `I authorize two more remediation cycles`, `requested=2 consumed=0 remaining=2`, and permitted remediation passes 6 and 7. Pass 6 reached a pre-R5 fail-closed handback after P1-T5, so no cycle was consumed. The earlier authorization remains consumed at 2 with 0 remaining; it is not reset or reinterpreted. No remediation pass after 7 is authorized.

On 2026-08-16 the user separately authorized the exact instruction `Please enable a one-time exception for the branch requirement and continue`. `artifacts/orchestration/orchestrator-state.json` records `response=exception` under `human_interaction.requirements[]`, the repository-relative runbook path `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/runbooks/powershell-branch-coverage-one-time-exception.runbook.md`, and the canonical receipt `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/cycle3-pass6-powershell-branch-one-time-exception.2026-08-16T21-00.md`. The runbook SHA-256 is `1C0761047A7EB4FF8C084A6762DC832004FBD1AB2469B84D0E8158DF9E5B2C7F`; the receipt SHA-256 is `1BBD4C323BEB8D9F76BF4FB4916452D9087EC89C1AD88C6B9F41AAA625B68B65`.

This plan remains extension cycle 3 and remediation pass 6 and resumes at P1-T6 without replaying P0-T1 through P1-T5. Execution may begin only after the outer orchestrator obtains an independent exact `atomic-executor-c4` validation-only preflight returning `PREFLIGHT: ALL CLEAR` for this plan's final SHA-256. A newly authorized cycle is consumed only after the outer orchestrator completes a full feature-vs-`main` R5 review and that review returns `REVIEW_STATUS: REMEDIATION_REQUIRED`; pre-R5 planning, preflight, execution, staging, commit, PR-context refresh, or other mechanical blockers do not consume a cycle.

The raw branch finding remains factual and unchanged: `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO`, source-attributable covered outcomes 0, missed outcomes 0, denominator 0, and `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`. No measured result demonstrates that the 75% PowerShell branch threshold passed, and no plan, evidence, QA, audit, checkpoint, status, commit, or PR artifact may describe it as a measured branch PASS. The authorized disposition is exactly `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`. It changes only the compliance disposition for this unavailable PowerShell branch measurement for issue #467 and this delivery. It is not a permanent policy or threshold change, reusable waiver, synthetic metric, or exception for tests, line coverage, owner coverage, formatting, analysis, type checks, other languages, unrelated acceptance criteria, hosted CI, or checkpoint validation.

All work must remain automated. No manual bootstrap, human validation, screenshots, or user-supplied runtime evidence may be introduced. The exception continuation authorizes no PowerShell production, test, runtime, configuration, dependency, lockfile, policy, threshold, exclusion, or suppression mutation; the formerly proposed collector implementation is obsolete. The only permitted requirement-source writes are the individual S-D14 checkbox token in `spec.md` and U20 checkbox token in `user-story.md` after every non-branch element passes and the one-time disposition is cited; criterion text and all other checkboxes remain unchanged. Executor-owned writes are limited to this plan's checklist state and canonical feature evidence. The retained PowerShell result is 2,456 total tests, 2,447 passed, 9 disabled, 0 failures/errors, and 4,040/4,260 line coverage = 94.835681%, together with every retained owner and non-branch gate.

The pre-exception terminal dirty boundary contained 18 untracked paths. The one-time-exception runbook and receipt are the only two authorized additions, so the resume boundary is exactly 0 staged paths, 2 unstaged tracked deletions for the preserved user-directed grouped-to-flat relocation, and 20 untracked paths including the flat input/plan pair. Execution must preserve those relocations and distinguish later executor-created canonical evidence from this 20-path starting boundary.

Every `evidence/...` path below resolves beneath `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/<kind>/`. Every command receipt must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. PowerShell coverage receipts must report numeric line covered/missed/denominator/percentage values, source-attributable branch numerator/denominator 0/0, `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, and `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`; they must not invent a branch percentage or measured PASS. Tool-owned Pester output may remain under `artifacts/pester/`; no other non-canonical evidence location is permitted.

### Phase 0 — Authorization, Policy, and Baseline Capture

- [x] [P0-T1] Read `AGENTS.md`, `.agents/skills/policy-compliance-order/SKILL.md`, `.agents/skills/general-code-change/SKILL.md`, `.agents/skills/general-unit-test/SKILL.md`, `.agents/skills/powershell/SKILL.md`, `.agents/skills/quality-tiers/SKILL.md`, `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/acceptance-criteria-tracking/SKILL.md`, and `.agents/skills/repo-automation-adapter/SKILL.md` in that order; accept only when `evidence/remediation-baseline/cycle3-pass6-phase0-instructions-read.2026-08-15T10-36.md` contains the literal fields `Timestamp:`, `Policy Order:`, and an explicit ordered list of every file read.
- [x] [P0-T2] Read `artifacts/orchestration/orchestrator-state.json` without modifying it and verify `remediation-loop-exit.user_authorized_additional_cycles_extension_2`; accept only when `evidence/remediation-baseline/cycle3-pass6-authorization-gate.2026-08-15T10-36.md` records the exact authorization text, `requested=2 consumed=0 remaining=2`, permitted passes 6 and 7, current pass 5, next pass 6, preserved historical consumption 2/0, R5-only consumption, and the independent exact `atomic-executor-c4` `PREFLIGHT: ALL CLEAR` receipt bound to original execution-starting Plan of Record SHA-256 `2CD620E0ED22DB2CA896C8805550224CF7088E8C2B48CE3D8D668353D3013DD0`; treat that receipt as historical evidence for completed P0-T2 and require the separate S28 independent preflight for the revised plan before P1-T6, without replaying P0-T2.
- [x] [P0-T3] Hash `artifacts/pr_context.summary.txt`, `artifacts/pr_context.appendix.txt`, `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/audit-2026-08-15T03-09/policy-audit.2026-08-15T03-09.md`, `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/audit-2026-08-15T03-09/code-review.2026-08-15T03-09.md`, `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/audit-2026-08-15T03-09/feature-audit.2026-08-15T03-09.md`, and `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-inputs.2026-08-15T03-09.md`; accept only when `evidence/remediation-baseline/cycle3-pass6-context-integrity.2026-08-15T10-36.md` records every path/hash, the reviewed head `2d44e14f48706bb317ee8b81d23b2b2f7cee1c5d`, and the historical-only disposition of the input's exhausted-authorization markers.
- [x] [P0-T4] Run `git status --short --branch`, `git rev-parse HEAD`, `git branch --show-current`, and `git merge-base HEAD main` without changing Git state; accept only when `evidence/remediation-baseline/cycle3-pass6-repository-state.2026-08-15T10-36.md` records branch `feature/codex-native-parallel-orchestration-467`, HEAD `80fd06b835f6ec5c257b6c670a0bdfaf46cded0e`, merge base `768e485ddf3b48b16aa7588a72709e17568ee5f5`, and the complete index/worktree inventory before execution.
- [x] [P0-T5] Enumerate every checkbox in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/spec.md` and `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/user-story.md` without editing either file; accept only when `evidence/remediation-baseline/cycle3-pass6-acceptance-baseline.2026-08-15T10-36.md` records 43 total, 39 checked/PASS, S-D14 and U20 unchecked/FAIL, S-D15 and U21 unchecked/UNVERIFIED, and 0 PARTIAL.
- [x] [P0-T6] Hash and count lines in `scripts/powershell/PoshQC/PoshQC.Testing.psm1`, `extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1`, `scripts/powershell/PoshQC/convert-poshqc-coverage.ps1`, `tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1`, and `tests/scripts/powershell/PoshQC/PoshQC.Tests.ps1`; accept only when `evidence/remediation-baseline/cycle3-pass6-powershell-ownership.2026-08-15T10-36.md` records the exact hashes and starting line counts 463, 463, 35, 259, and 579 respectively, proves the two module copies byte-identical, and marks `PoshQC.Tests.ps1` read-only for this pass.
- [x] [P0-T7] Build a sorted UTF-8 path/content SHA-256 manifest for all executable, test, reusable-script, dependency, lockfile, policy, threshold, exclusion, suppression, and coverage-configuration inputs; accept only when `evidence/remediation-baseline/cycle3-pass6-executable-input-fingerprint.2026-08-15T10-36.md` records the path count, ordered paths, aggregate hash, and allowed pre-existing plan/evidence boundary.
- [x] [P0-T8] Invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root=C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-10T19-25`; accept only when `evidence/remediation-baseline/cycle3-pass6-powershell-format.2026-08-15T10-36.md` records `ok=true`, exit 0, and zero file mutation.
- [x] [P0-T9] After P0-T8 passes without mutation, invoke `mcp__drm-copilot__run_poshqc_analyze` with the same workspace root; accept only when `evidence/remediation-baseline/cycle3-pass6-powershell-analyze.2026-08-15T10-36.md` records `ok=true`, exit 0, and zero findings.
- [x] [P0-T10] After P0-T9, invoke `mcp__drm-copilot__run_poshqc_test` with the same workspace root and repository-default scan configuration; accept only when `evidence/remediation-baseline/cycle3-pass6-powershell-test.2026-08-15T10-36.md` records the complete result, fresh `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml` hashes, and zero test failures/errors.
- [x] [P0-T11] Independently parse the fresh `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml`; accept only when `evidence/remediation-baseline/cycle3-pass6-powershell-coverage.2026-08-15T10-36.md` records test counts, numeric line and genuine-branch covered/missed/denominator/percentage values, all 25 owner attributions, and a baseline FAIL whenever the genuine branch denominator is zero or coverage is below 75%.

### Phase 1 — Completed Capability Findings and Authorized Exception Resume Gate

- [x] [P1-T1] Inspect `scripts/powershell/PoshQC/PoshQC.Testing.psm1`, `extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1`, `scripts/powershell/PoshQC/convert-poshqc-coverage.ps1`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `scripts/powershell/PoshQC/PoshQC.psd1`, `tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1`, and `tests/scripts/powershell/PoshQC/PoshQC.Tests.ps1` without editing them; accept only when `evidence/other/cycle3-pass6-branch-capability-inventory.2026-08-15T10-36.md` lists every existing callable capability that can observe distinct runtime control-flow outcomes and separately rejects command, line, AST-position, source-position, and correlation proxies.
- [x] [P1-T2] Run read-only deterministic probes for every P1-T1 candidate against existing repository-owned PowerShell sources and `artifacts/pester/powershell-coverage.xml`; accept only when `evidence/regression-testing/cycle3-pass6-branch-capability-probe.2026-08-15T10-36.md` records exact commands, source identities, observed taken/not-taken outcomes, input/output hashes, and numeric covered/missed/denominator values for each candidate without changing repository files.
- [x] [P1-T3] Validate P1-T2 against the uniform 75% branch threshold and proxy prohibitions; accept only when `evidence/other/cycle3-pass6-branch-capability-decision.2026-08-15T10-36.md` states exactly `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: YES` only with a positive source-attributable observed-outcome denominator and observed branch coverage at least 75%, otherwise states exactly `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO` without a synthetic percentage.
- [x] [P1-T4] If P1-T3 is `NO`, write `evidence/other/cycle3-pass6-fail-closed-decision.2026-08-15T10-36.md`; accept only when it records `REVIEW_STATUS: REMEDIATION_REQUIRED`, `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO`, `POWERSHELL_BRANCH_POLICY_UNRESOLVED`, the complete automated search/probe scope, no source/test/configuration/dependency/policy change, and `requested=2 consumed=0 remaining=2` because R5 was not reached.
- [x] [P1-T5] If P1-T3 is `NO`, write `evidence/other/cycle3-pass6-executor-to-orchestrator-handback.2026-08-15T10-36.md` with completed task/evidence hashes, repository/index status, the unchanged 39 PASS/2 FAIL/2 UNVERIFIED AC disposition, and an explicit stop before staging, commit, PR-context refresh, grouped review, R5, push, PR mutation, or CI monitoring; accept only when Phase 2 and later tasks remain unchecked and execution terminates without consuming a cycle.
- [x] [P1-T6] Verify the issue #467 one-time exception before resuming pass 6 and write `evidence/other/cycle3-pass6-exception-resume-verification.2026-08-16T21-00.md`; accept only when it records the separate S28 independent exact `atomic-executor-c4` `PREFLIGHT: ALL CLEAR` receipt bound to this revised plan's current SHA-256, branch `feature/codex-native-parallel-orchestration-467`, the exact authorization text/date, checkpoint `response=exception`, the runbook and receipt paths plus exact SHA-256 values, `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO`, source-attributable branch numerator/denominator 0/0, `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, no measured 75% branch PASS, `requested=2 consumed=0 remaining=2`, and the exact resume boundary of 0 staged, 2 preserved unstaged tracked deletions, and 20 untracked paths; fail closed if any preflight, plan-hash, scope, hash, checkpoint, branch, issue, expiry, or non-reuse prerequisite differs.

### Phase 2 — One-Time Exception Reconciliation and No-Implementation Boundary

- [x] [P2-T1] Re-hash the completed P1-T1 through P1-T5 evidence and bind the raw branch finding in `evidence/other/cycle3-pass6-exception-raw-branch-reconciliation.2026-08-16T21-00.md`; accept only when every original evidence hash is recorded, `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO` remains exact, source-attributable covered/missed/denominator values remain 0/0/0, and neither a synthetic percentage nor measured branch PASS is introduced.
- [x] [P2-T2] Verify the exception runbook against `.agents/skills/human-exception-runbook/SKILL.md` without modifying either file and record the result in `evidence/other/cycle3-pass6-exception-runbook-conformance.2026-08-16T21-00.md`; accept only when the canonical path, ordered Cue/Prerequisites/Step-by-step Instructions/Verification/Source and Citation sections, dated sources, issue/branch scope, expiry, non-reuse rule, and SHA-256 all conform.
- [x] [P2-T3] Compare every PowerShell production, test, runtime, configuration, dependency, lockfile, policy, threshold, exclusion, suppression, and coverage-configuration input with P0-T6 and P0-T7 and write `evidence/other/cycle3-pass6-exception-no-implementation-delta.2026-08-16T21-00.md`; accept only when all executable inputs are unchanged, both `PoshQC.Testing.psm1` copies remain byte-identical, `tests/scripts/powershell/PoshQC/PoshQC.Tests.ps1` remains read-only, and the obsolete collector implementation produced zero path or byte mutation.
- [x] [P2-T4] Reconcile the retained PowerShell baseline in `evidence/other/cycle3-pass6-exception-retained-gates.2026-08-16T21-00.md`; accept only when it records 2,456 total / 2,447 passed / 9 disabled / 0 failures or errors, line coverage 4,040/4,260 = 94.835681%, all 25/25 owners attributed, all 17 added owners at least 90% lines, all 8 modified owners at least 80% with no regression, and states that each retained gate remains mandatory.
- [x] [P2-T5] Write `evidence/other/cycle3-pass6-exception-continuation-decision.2026-08-16T21-00.md`; accept only when it states `EXCEPTION_SCOPE_VALID: YES`, `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, no measured branch PASS, no policy/threshold/exclusion/suppression/dependency change, no exception for any retained gate, and authorizes continuation to P3-T1 without consuming a cycle; otherwise record `REVIEW_STATUS: REMEDIATION_REQUIRED`, leave P3 onward unchecked, and return to the outer orchestrator.

### Phase 3 — Ordered PowerShell QA and Numeric Coverage Gate

If P3-T1, P3-T2, P3-T3, P3-T4, or P3-T5 cannot satisfy every non-branch requirement after the authorized correction/restart loop, the failing current task must write `evidence/other/cycle3-pass6-phase3-fail-closed-decision.2026-08-15T10-36.md` and `evidence/other/cycle3-pass6-phase3-executor-to-orchestrator-handback.2026-08-15T10-36.md`, record the exact failed retained gate, preserve `requested=2 consumed=0 remaining=2`, complete only as the terminal `REVIEW_STATUS: REMEDIATION_REQUIRED` branch, and stop before R5 with every later task unchecked. The raw 0/0 branch measurement is not a failure under the verified exception, but any invalid, withdrawn, expired, reused, or scope-drifted exception restores `POWERSHELL_BRANCH_POLICY_UNRESOLVED` and requires the same fail-closed handback. P3-T5 may be reached only after P3-T1 through P3-T4 complete with the exact raw/disposition distinction.

- [x] [P3-T1] Invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root=C:\Users\DanMoisan\repos\drm-copilot-wt\2026-08-10T19-25`; accept only when `evidence/qa-gates/cycle3-pass6-powershell-format.2026-08-15T10-36.md` records `ok=true`, exit 0, and zero further mutation; any mutation or failure requires an authorized correction and restart at P3-T1.
- [x] [P3-T2] After P3-T1 passes cleanly, invoke `mcp__drm-copilot__run_poshqc_analyze` with the same workspace root; accept only when `evidence/qa-gates/cycle3-pass6-powershell-analyze.2026-08-15T10-36.md` records `ok=true`, exit 0, and zero findings.
- [x] [P3-T3] After P3-T2 passes, invoke `mcp__drm-copilot__run_poshqc_test` with the same workspace root and repository-default scan configuration; accept only when `evidence/qa-gates/cycle3-pass6-powershell-test.2026-08-15T10-36.md` records `ok=true`, 2,456 total / 2,447 passed / 9 disabled / 0 failures or errors, and fresh JUnit/coverage hashes.
- [x] [P3-T4] Independently parse fresh `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml`; accept only when `evidence/qa-gates/cycle3-pass6-powershell-coverage.2026-08-15T10-36.md` records line coverage 4,040/4,260 = 94.835681%, source-attributable branch numerator/denominator 0/0, `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO`, `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, no proxy-derived or synthetic counter, and no claim that the measured 75% branch threshold passed.
- [x] [P3-T5] Compare every Phase 3 result with P0-T11 and the preserved owner matrix; accept only when `evidence/qa-gates/cycle3-pass6-powershell-owner-comparison.2026-08-15T10-36.md` records a clean format -> analyze -> test loop, all 25/25 owners attributed, all 17 added owners at least 90% lines, all 8 modified owners at least 80% with no regression, changed-owner coverage non-regressing, PowerShell line/test/owner gates PASS, and the exact raw/disposition markers without a measured branch PASS; if any retained gate fails, write the two Phase 3 fail-closed artifacts, preserve the exact failing measurement, retain `REVIEW_STATUS: REMEDIATION_REQUIRED` and `requested=2 consumed=0 remaining=2`, leave Phase 4 onward unchecked, and stop before R5.

### Phase 4 — Cross-Language Freshness and Ordered QA

- [x] [P4-T1] Compare all Python executable/test/configuration inputs with P0-T7 and the accepted cycle-2 hashes; accept only when `evidence/qa-gates/cycle3-pass6-python-freshness.2026-08-15T10-36.md` records `UNCHANGED` or `CHANGED` with a complete sorted path/hash delta.
- [x] [P4-T2] If P4-T1 is `CHANGED`, run `poetry run black . --check`; if `UNCHANGED`, re-hash the accepted formatter receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-python-format.2026-08-15T10-36.md` records exit 0 and the selected branch without `EXIT_CODE: SKIPPED`.
- [x] [P4-T3] After P4-T2, if P4-T1 is `CHANGED`, run `poetry run ruff check .`; if `UNCHANGED`, re-hash the accepted lint receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-python-lint.2026-08-15T10-36.md` records exit 0 and zero findings or new suppressions.
- [x] [P4-T4] After P4-T3, if P4-T1 is `CHANGED`, run `poetry run pyright`; if `UNCHANGED`, re-hash the accepted type-check receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-python-typecheck.2026-08-15T10-36.md` records exit 0 and zero errors.
- [x] [P4-T5] After P4-T4, if P4-T1 is `CHANGED`, run `poetry run pytest -o addopts='' -q --cov --cov-branch`; if `UNCHANGED`, re-hash the accepted coverage JSON, test receipt, and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-python-coverage.2026-08-15T10-36.md` records exit 0, lines at least 85%, genuine branches at least 75%, added-owner coverage at least 90%, and no changed-owner regression.
- [x] [P4-T6] Compare all TypeScript executable/test/configuration inputs with P0-T7 and the accepted cycle-2 hashes; accept only when `evidence/qa-gates/cycle3-pass6-typescript-freshness.2026-08-15T10-36.md` records `UNCHANGED` or `CHANGED` with a complete sorted path/hash delta.
- [x] [P4-T7] If P4-T6 is `CHANGED`, run `npm --prefix extensions/drm-copilot exec -- prettier --check src test`; if `UNCHANGED`, re-hash the accepted formatter receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-typescript-format.2026-08-15T10-36.md` records exit 0 and the selected branch without `EXIT_CODE: SKIPPED`.
- [x] [P4-T8] After P4-T7, if P4-T6 is `CHANGED`, run `npm --prefix extensions/drm-copilot run lint`; if `UNCHANGED`, re-hash the accepted lint receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-typescript-lint.2026-08-15T10-36.md` records exit 0 and zero findings or new suppressions.
- [x] [P4-T9] After P4-T8, if P4-T6 is `CHANGED`, run `npm --prefix extensions/drm-copilot run typecheck`; if `UNCHANGED`, re-hash the accepted type-check receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-typescript-typecheck.2026-08-15T10-36.md` records exit 0 and zero type errors.
- [x] [P4-T10] After P4-T9, if P4-T6 is `CHANGED`, run `npm --prefix extensions/drm-copilot run test:coverage`; if `UNCHANGED`, re-hash the accepted coverage summary, test receipt, and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-typescript-coverage.2026-08-15T10-36.md` records exit 0, lines at least 85%, genuine branches at least 75%, and no modified-owner regression.
- [x] [P4-T11] Compare all Bash executable/test/configuration inputs with P0-T7 and the accepted cycle-2 hashes; accept only when `evidence/qa-gates/cycle3-pass6-bash-freshness.2026-08-15T10-36.md` records `UNCHANGED` or `CHANGED` with a complete sorted path/hash delta.
- [x] [P4-T12] If P4-T11 is `CHANGED`, run `bash scripts/bash/shell-qc.sh check`; if `UNCHANGED`, re-hash the accepted Bash check receipt and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-bash-check.2026-08-15T10-36.md` records exit 0, shfmt diff-clean, zero ShellCheck errors, no `.claude/**` mutation, and no `EXIT_CODE: SKIPPED`.
- [x] [P4-T13] After P4-T12, if P4-T11 is `CHANGED`, run `bash scripts/bash/shell-qc.sh test --coverage`; if `UNCHANGED`, re-hash the accepted kcov/test receipts and executable inputs; accept only when `evidence/qa-gates/cycle3-pass6-bash-coverage.2026-08-15T10-36.md` records exit 0, at least 85% lines, all Bats tests passing, and branch `N/A/not-PASS` without a fabricated numeric claim.

If any Phase 4 command fails or mutates files, correct only authorized scope and restart that language's ordered loop at its formatter/check task; no later language or repository gate may be treated as current until the restarted loop passes cleanly.

### Phase 5 — Repository Integrity and All 43 Acceptance Criteria

- [x] [P5-T1] Run `git diff --check 768e485ddf3b48b16aa7588a72709e17568ee5f5` against the working tree; accept only when `evidence/qa-gates/cycle3-pass6-diff-check.2026-08-15T10-36.md` records exit 0 and no output.
- [x] [P5-T2] Compare every `.claude/**` path and byte with merge base `768e485ddf3b48b16aa7588a72709e17568ee5f5` across HEAD, index, and working tree; accept only when `evidence/qa-gates/cycle3-pass6-claude-invariance.2026-08-15T10-36.md` records zero path or byte delta.
- [x] [P5-T3] Compare root and bundle paths/hashes across `.agents/**`, `.codex/**`, `extensions/drm-copilot/resources/codex-and-agents-customizations/**`, `scripts/powershell/PoshQC/PoshQC.Testing.psm1`, and `extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1`, then run `poetry run pytest tests/scripts/dev_tools/test_poshqc_bundled_parity.py`; accept only when `evidence/qa-gates/cycle3-pass6-root-bundle-parity.2026-08-15T10-36.md` records exit 0, complete parity, byte-identical module hashes, and zero missing, extra, or mismatched path.
- [x] [P5-T4] Count lines and compare hashes for every P0-T6 production, test, and reusable-script path; accept only when `evidence/qa-gates/cycle3-pass6-file-sizes.2026-08-15T10-36.md` records zero executor path or byte change, preserves the exact P0-T6 line counts, proves both `PoshQC.Testing.psm1` copies byte-identical, and confirms `tests/scripts/powershell/PoshQC/PoshQC.Tests.ps1` remains read-only.
- [x] [P5-T5] Compare dependency manifests, lockfiles, suppression directives, `AGENTS.md`, `.agents/skills/**`, quality-tier authority, coverage configuration, thresholds, and exclusions with P0-T7; accept only when `evidence/qa-gates/cycle3-pass6-policy-scope.2026-08-15T10-36.md` records zero dependency, lockfile, suppression, reusable waiver, policy, threshold, exclusion, or coverage-configuration change and separately records that the issue-scoped runbook/receipt implement only `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, not a policy mutation or measured branch PASS.
- [x] [P5-T6] Run `poetry run python scripts/dev_tools/validate_evidence_locations.py --root .`; accept only when `evidence/qa-gates/cycle3-pass6-evidence-locations.2026-08-15T10-36.md` records exit 0 and zero non-canonical evidence path anywhere in the repository.
- [x] [P5-T7] After P3-T4 and P3-T5 pass, re-evaluate all 22 criteria in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/spec.md` and all 21 criteria in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/user-story.md` individually, change only the S-D14 and U20 checkbox tokens from `[ ]` to `[x]` one at a time without changing criterion text, and leave S-D15 and U21 unchecked; accept only when `evidence/issue-updates/cycle3-pass6-acceptance-reconciliation.2026-08-15T10-36.md` contains 43 path-keyed rows, preserves every criterion's text, records 41 checked/PASS, 0 FAIL, 2 unchecked/UNVERIFIED, 0 PARTIAL, identifies S-D14 and U20 as satisfied only through `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED` after all other criterion elements pass, records `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, prohibits a measured branch PASS claim, and reports no other checkbox or requirement-source change.
- [x] [P5-T8] Compare the complete working-tree path set with P0-T4, the P1-T6 0-staged/2-deletion/20-untracked resume boundary, and P5-T7; accept only when `evidence/qa-gates/cycle3-pass6-final-scope.2026-08-15T10-36.md` records zero PowerShell source/test/runtime/configuration change, validates only the S-D14 checkbox token in `spec.md` and U20 checkbox token in `user-story.md` as requirement-source changes, preserves the user-directed flat input/plan relocation and the runbook/receipt, records no unexpected index entry, and limits every other executor-owned path to this plan and canonical feature evidence.

### Phase 6 — Final Comparison and Executor-to-Orchestrator Handback

- [x] [P6-T1] Reconcile Phase 0 with Phases 2 through 5 in `evidence/qa-gates/cycle3-pass6-final-comparison.2026-08-15T10-36.md`; accept only when it records numeric baseline/final/delta values for every applicable non-excepted measurement, PowerShell 2,456 total / 2,447 passed / 9 disabled / 0 failures or errors, line coverage 4,040/4,260 = 94.835681%, all owner thresholds, `GENUINE_BRANCH_COLLECTOR_ESTABLISHED: NO`, source-attributable branch numerator/denominator 0/0, `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE`, `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, no measured branch PASS, all non-PowerShell branch thresholds, artifact hashes, no-regression results, 41 PASS/0 FAIL/2 UNVERIFIED on the successful path, and no policy or scope violation.
- [x] [P6-T2] Capture `git status --short`, index paths, unstaged paths, untracked paths, and SHA-256 hashes for every executor-touched path without staging anything; accept only when `evidence/other/cycle3-pass6-repository-handback-state.2026-08-15T10-36.md` records the exact path sets and proves the index remains unchanged by the executor.
- [x] [P6-T3] Write `evidence/other/cycle3-pass6-executor-to-orchestrator-handback.2026-08-15T10-36.md`; accept only when it records completed task/evidence hashes, P6-T2 repository state, all 43 AC dispositions, both exact raw/disposition branch markers with no measured branch PASS, `requested=2 consumed=0 remaining=2`, pass 6 still active, R5-only cycle consumption, and the ordered outer-orchestrator boundary for exact-path staging, MCP commit-context collection, exact `commit-steward-c4` message generation, commit, MCP PR-context refresh against `main`, grouped full feature review, artifact validation, and R5; require every new audit group to be exactly `audit-yyyy-MM-ddTHH-mm` and every later remediation group to be exactly `remediation-yyyy-MM-ddTHH-mm`, with supporting evidence only under `<FEATURE>/evidence/<kind>/`.
- [x] [P6-T4] Stop executor scope before staging, MCP commit-context collection, `commit-steward-c4` delegation, commit, PR-context collection, reviewer delegation, R5 adjudication, push, PR creation/update, or CI monitoring; accept only when control returns to the outer orchestrator, neither newly authorized cycle is consumed by this pre-R5 execution, and no exception is asserted for hosted CI or checkpoint validation.

## Planner and Executor Boundary

This planning turn changes only this exact plan file, runs the authoritative MCP plan validator, and does not execute any plan task or launch an execution agent. The outer orchestrator owns the independent exact `atomic-executor-c4` validation-only preflight for the final plan SHA-256. After all-clear, the executor resumes at P1-T6, preserves P0-T1 through P1-T5 and their evidence without replay, may perform only P1-T6 through P6-T4, and must stop before all staging, commit-context, commit-message, commit, PR-context, full feature review, R5 cycle adjudication, push, PR, CI, and checkpoint-completion operations.

## Outer Orchestrator Continuation Contract

After P6-T4 returns control, the outer orchestrator must perform these actions in order and must stop on any failed gate: build an explicit issue-467 exact-path staging manifest and stage only that set; run cached diff validation; invoke `mcp__drm-copilot__collect_commit_context` with the exact workspace root and use only its on-disk artifact; delegate exactly `commit-steward-c4` with no fallback and use its single returned conventional commit message without alteration; commit exactly the staged manifest; invoke `mcp__drm-copilot__collect_pr_context` against `main` for the new exact HEAD; bind one immutable `yyyy-MM-ddTHH-mm` review timestamp; create exactly one `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/audit-yyyy-MM-ddTHH-mm/` folder; delegate the grouped full feature-vs-`main` review; validate its policy-audit, code-review, and feature-audit through the MCP artifact validator; and only then adjudicate R5 and cycle consumption in `artifacts/orchestration/orchestrator-state.json`.

Every grouped review artifact and R5 receipt must state both `RAW_BRANCH_RESULT: 0/0 UNAVAILABLE` and `COMPLIANCE_DISPOSITION: ONE_TIME_EXCEPTION_AUTHORIZED`, cite the runbook and exception receipt, and prohibit any statement that the measured PowerShell branch threshold passed. The review may return `REVIEW_STATUS: PASS` under this issue-scoped exception only if every retained non-branch gate and every non-hosted acceptance criterion passes and no other finding remains. If R5 returns `REVIEW_STATUS: REMEDIATION_REQUIRED`, consume exactly one cycle and create any next input/plan pair together only under a new sibling `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-yyyy-MM-ddTHH-mm/` folder; otherwise do not consume a cycle. Hosted CI and both repository-local and MCP checkpoint validation remain mandatory and unexcepted. Push, PR mutation, and CI monitoring remain outside executor scope and require the outer orchestration's existing authorization gates.
