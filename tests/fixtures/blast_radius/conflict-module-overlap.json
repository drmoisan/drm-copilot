{
  "description": "module_overlap in isolation: the two concrete paths are distinct so no path pair overlaps, but both radii resolve to the same module, which is the second and wider net after path overlap.",
  "input": {
    "radius_a": {
      "paths": ["scripts/dev_tools/a.py"],
      "modules": ["python-dev-tools"],
      "shared_surfaces": [],
      "contracts": [],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "radius_b": {
      "paths": ["scripts/dev_tools/b.py"],
      "modules": ["python-dev-tools"],
      "shared_surfaces": [],
      "contracts": [],
      "source": "declared",
      "computed_at": "2026-08-07T12-00"
    },
    "config": {
      "version": 1,
      "shared_surfaces": ["config/orchestration-routing.json", "poetry.lock"],
      "shared_surface_globs": ["scripts/dev_tools/validate_*.py"],
      "modules": {
        "config": ["config/**"],
        "docs": ["docs/**"],
        "python-dev-tools": ["scripts/dev_tools/**"],
        "tests": ["tests/**"]
      },
      "over_breadth_fraction": 0.25
    }
  },
  "expected": {
    "conflict": true,
    "reasons": [{ "kind": "module_overlap", "detail": "python-dev-tools" }]
  }
}
