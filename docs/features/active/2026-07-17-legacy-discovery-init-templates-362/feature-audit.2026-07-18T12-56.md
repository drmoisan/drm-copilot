# Feature Audit: legacy-discovery-init-templates (#362) — R4 Re-Audit, Remediation Cycle 1

**Audit Date:** 2026-07-18
**Feature Folder:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/`
**Base Branch:** `epic/legacy-discovery-and-parity-integration`
**Head Branch:** `feature/legacy-discovery-init-templates-362` (HEAD `7610bf2539f62bba5e4489f559ed486fb368043a`)
**Work Mode:** `full-feature`
**Audit Type:** Post-remediation acceptance verification (R4 of the R1–R5 remediation loop, cycle 1). Supersedes `feature-audit.2026-07-18T13-00.md`.

---

## Scope and Baseline

- **Base branch:** `epic/legacy-discovery-and-parity-integration` (merge-base commit `f18c1c16f3eb111f0acef5eb3c46be1fb563aac0`)
- **Head branch/commit:** `feature/legacy-discovery-init-templates-362` (commit `7610bf2539f62bba5e4489f559ed486fb368043a`, includes feature commit `48d16f6f` and remediation commit `7610bf25`)
- **Merge base:** `f18c1c16f3eb111f0acef5eb3c46be1fb563aac0`
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this review against the merge-base at HEAD)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/**` (baseline, qa-gates, remediation-baseline, regression-testing, other)
  - Additional evidence: reviewer's independent verification runs recorded in `policy-audit.2026-07-18T12-56.md` (Appendix B) — clean-checkout worktree inspection, real-loader parse, seven `jsonschema.validate` runs, import-surface check, end-to-end CLI invocation, full toolchain re-run
- **Feature folder used:** `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/`
- **Requirements source:** `spec.md` and `user-story.md` (multiple files, identical 9-item AC sets)
- **Work mode resolution note:** `issue.md` carries the explicit marker `- Work Mode: full-feature`; per the acceptance-criteria-tracking rules, `spec.md` and `user-story.md` are therefore the authoritative AC sources.
- **Scope note:** The audit scope is the full branch diff against the merge-base (both commits), not the remediation delta alone. The four cycle-1 Blocking findings (R1–R4 in `remediation-inputs.2026-07-18T13-00.md`) were re-verified independently; the executor's self-reported results were not accepted without reproduction.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md` — primary source (section `## Acceptance Criteria`)
- `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md` — co-authoritative source (section `## Acceptance Criteria`, identical 9 items)

### Acceptance criteria

1. `dev.discovery.init <target-dir>` scaffolds the discovery workspace directory layout at the given target consumer path in a single invocation.
2. `dev.discovery.init` accepts an explicit target-directory CLI argument (not the drm-copilot workspace root) and an optional `--template-root` override consistent with the `new_active_feature_folder`/`new_potential_bug_entry` precedent.
3. Initialization writes a starter domain-profile config, authored as a nested YAML document (`profile_version`, `legacy_source.root`, `target.root`, `technology_stack.legacy[]`, `artifacts.root`) with placeholder tokens, in the shape required by the merged domain-profile loader (feature #360, `scripts/dev_tools/discovery/domain_profile.py`), and parses cleanly under that loader's `parse_domain_profile_text`.
4. Initialization writes starter instances of each of the seven discovery artifacts (Feature Contract, Coverage Ledger, Runtime Characterization Scenario, Parity Matrix, Unspecified Behavior Record, Product Decision Record, Evidence Reference) from the templates under `docs/discovery/templates/artifacts/`, in the same invocation as the domain profile.
5. Each artifact template's `$schema` field is a relative, scheme-less path resolvable by `validate_json.py`'s existing no-scheme `_load_schema` branch, per feature 9002's planned schema-versioning convention; the open question about resolving `$schema` from inside an external consumer repository is recorded in the spec and left to feature 9002, not resolved here.
6. Templates and generated artifacts contain no domain-specific identifiers (verified by the domain-neutrality regression test).
7. `dev.discovery.init` fails fast, before writing any file, when: the target path exists and is not a directory; the target path's parent does not exist; or the resolved template root is missing or has a partial template set.
8. `dev.discovery.init` is registered and invocable as a Poetry console-script (`"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` in root `pyproject.toml`).
9. Tests under `tests/scripts/dev_tools/discovery/` satisfy repository quality-tier policy (line coverage >= 85%, branch coverage >= 75%), use an injected fake `FileSystem` with no real filesystem/temp-file I/O, and include the schema-conformance test implemented against the merged `schemas/discovery/v1/` files (no longer tracked as dependent on feature 9002).

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Single-invocation workspace scaffold | PASS | Reviewer invoked the real console script against an empty scratchpad target; one invocation produced `domain-profile.yaml` plus `artifacts/` with all seven artifact JSON files (8 files total). Also covered by `test_create_discovery_workspace_success_full_layout`. | `poetry run dev.discovery.init <scratch>/ws362` then `find <scratch>/ws362 -type f` | All 8 template sources are tracked at HEAD (R1 resolved): `git ls-tree` lists all 8; a fresh detached checkout of `7610bf25` contains all 8. |
| 2 | Explicit target-dir arg + `--template-root` override | PASS | `init_cli.py` defines a required positional `target_dir` and optional `--template-root` (default `None` -> bundled root via `resolve_default_template_root()`), matching the precedent tools; no `resolve_workspace()` default. Tests: `test_parse_args_*`, `test_main_honors_template_root_override`. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_cli.py -v` | `--force` flag additionally implements the spec's "explicit override to proceed" for non-empty targets. |
| 3 | Nested #360-shape domain profile parses under the real loader | PASS | Template is the nested shape (`profile_version: 1`, `legacy_source.root`, `target.root`, `technology_stack.legacy[]`, `artifacts.root`). Reviewer ran the real `parse_domain_profile_text` against both raw template text and rendered scaffold output; both return a `DomainProfile` with `profile_version == 1`, no `DomainProfileError` (R2 resolved). Regression test `test_domain_profile_template_parses_with_real_loader` passes. | Reviewer verification script (recorded in `policy-audit.2026-07-18T12-56.md` Appendix B); `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -v` | Independent reproduction, not executor self-report. |
| 4 | Seven artifact instances written from templates in the same invocation | PASS | The end-to-end CLI run in criterion 1 wrote all seven artifact instances alongside the domain profile in one pass; `create_discovery_workspace` iterates the 8-entry template/output tuples with `zip(..., strict=True)`. | `poetry run dev.discovery.init <scratch>/ws362`; `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py::test_create_discovery_workspace_success_full_layout` | All seven schema kinds represented (feature-contract, coverage-ledger, runtime-characterization-scenario, parity-matrix, unspecified-behavior-record, product-decision-record, evidence-reference). |
| 5 | `$schema` relative scheme-less paths resolve via `_load_schema` no-scheme branch | PASS | Each template's `"$schema"` is `../../../../schemas/discovery/v1/<name>.schema.json`; reviewer computed `template.parent / uri` (the `_load_schema` no-scheme rule) for all seven and each resolves to an existing schema file. `poetry run dev.validate-json` exits 0 over the governed `docs/**/*.json` glob. Reviewer additionally ran `jsonschema.validate` on all seven rendered instances against their real schemas — all validate (R3 resolved). | `poetry run dev.validate-json`; reviewer `$schema` resolution + `jsonschema.validate` script | Cross-repo resolution from an external consumer checkout remains the recorded 9002-owned open question in `spec.md`; unchanged by this branch, as the criterion itself specifies. |
| 6 | Domain neutrality of templates and rendered output | PASS | Reviewer read all 8 templates: only generic placeholder tokens, no TaskMaster/TMW/Outlook/VSTO or email/task-management identifiers. Both domain-neutrality regression tests pass (templates and rendered output via a real scaffold pass against an in-memory `FileSystem`). | `poetry run pytest tests/scripts/dev_tools/discovery/test_domain_neutrality.py -v` | Case-insensitive compiled regex over the fixed disallowed-token list per spec. |
| 7 | Fail-fast before any write | PASS | `create_discovery_workspace` validates the template set and target path before the first write; negative tests assert both the exception type and that the fake filesystem is unchanged (`set(fs.files) == pre_seeded_files`) for: non-directory target, missing parent, non-empty target without force, missing template root, partial template set. Reviewer also ran the real CLI against a missing-parent target: exit 1, clear stderr message, nothing written. | `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -v`; `poetry run dev.discovery.init <scratch>/no-such-parent/ws` (exit 1) | Partial template set raises one `FileNotFoundError` naming every missing path. |
| 8 | Poetry console-script registration and invocability | PASS | `pyproject.toml` diff adds `"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"` in the alphabetized `[tool.poetry.scripts]` block; reviewer invoked `poetry run dev.discovery.init` end-to-end (success and failure paths). Guarded by `test_console_script_registered_in_pyproject`. | `git diff f18c1c16..HEAD -- pyproject.toml`; `poetry run dev.discovery.init <scratch>/ws362` | Two-level dotted alias works via TOML quoted keys as specified. |
| 9 | Test-suite quality gates: coverage, fake FS, implemented schema-conformance test | PASS | Repo-wide 88.17% lines / 78.90% branches (>= 85 / >= 75), new production files at 100% line coverage, no regression vs baseline (88.16%/78.90%). All flow tests use the injected in-memory `FakeFileSystem`; grep confirms zero `tempfile`/`tmp_path`/`NamedTemporaryFile` usage. `test_generated_artifacts_conform_to_real_schemas` is implemented against `schemas/discovery/v1/` and passes; the suite has 0 skipped tests (the "pending 9002" skip was removed, R3/R4 follow-through). Package import surface restored and regression-guarded by `test_package_exports.py` (R4 resolved: the five-name import succeeds). | `poetry run pytest --cov --cov-branch --cov-report=term-missing` (exit 0, 1708 passed, 0 skipped); `poetry run python -c "from scripts.dev_tools.discovery import DomainProfile, DomainProfileError, load_domain_profile, parse_domain_profile_text, DEFAULT_PROFILE_FILENAME"` | `init_models.py`'s 6/12 lcov branch records are phantom Protocol-stub branches (documented in `policy-audit.2026-07-18T12-56.md` §1.2.1); all executable code covered. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 9 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

The four Blocking findings and the four FAIL / one PARTIAL acceptance verdicts from the initial review (`feature-audit.2026-07-18T13-00.md`) are all resolved and were re-verified by independent reproduction at HEAD `7610bf25`: committed template presence (fresh detached checkout), real-loader parse of the domain profile, real-schema validation of all seven rendered artifacts, restored public re-export surface, and a clean full-toolchain pass with coverage above the uniform gates and zero skipped tests.

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. During PR polish (optional, non-blocking): amend the seven test-only `# noqa: ARG001` comments to the pre-authorized reason format (see `code-review.2026-07-18T12-56.md` findings).
2. Track the 9002-owned cross-repository `$schema` resolution question (already recorded in `spec.md`) before templates are declared final for external-consumer use.

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 9 criteria evaluate as **PASS**.
- All 9 checkbox items were already checked (`[x]`) in both authoritative source files by the remediation executor; this audit independently verified each and confirms the checked state is correct. No source-file change was required by this audit (nothing to check off, nothing to un-check).

### AC Status Summary

- Source: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md`, `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md`
- Total AC items: 9 (per file; identical sets)
- Checked off (delivered): 9
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md` | 9 | 9 | 0 | Checkbox-backed; verified, no edit needed |
| `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md` | 9 | 9 | 0 | Checkbox-backed; verified, no edit needed |

No source-file checkbox change was made by this audit because every PASS criterion was already checked; this audit's role was independent verification of those check-offs, which succeeded for all 9 items.
