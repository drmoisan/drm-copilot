# Atomic Plan — mergeable project-file overlaps (Issue #643)

- **Issue:** #643
- **Feature folder:** `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643`
- **Branch:** `feature/mergeable-project-file-overlaps-643` (rebased; merge base with `origin/main` is `c3ffb080`, which is also the `origin/main` head at revision time; branch head `903151ff` at revision time). Every scope diff in this plan is anchored to the fixed ancestor `c3ffb080`, not to the moving `origin/main` ref.
- **Work mode:** `full-feature` (marker `- Work Mode: full-feature` in `issue.md`)
- **AC sources:** `spec.md` (`## Acceptance Criteria`, 39 items; `## Definition of Done`, 7 items; `## Seeded Test Conditions (from potential)`, 5 items) and `user-story.md` (`## Acceptance Criteria`, 11 items)
- **Plan timestamp:** 2026-09-07T08-13 (path fixed by the caller; content revised 2026-09-07; revision round 1 applied against `evidence/other/preflight-round-1.2026-09-07T13-38.md`, revision round 2 applied against `evidence/other/preflight-round-2.2026-09-07T14-20.md`, revision round 3 applied against `evidence/other/preflight-round-3.2026-09-07T14-45.md`)
- **Repository root (worktree):** `C:\Users\DanMoisan\repos\drm-copilot-wt\2026-09-07T08-09`

Authoritative inputs, read in this order: `issue.md`; `spec.md`; `user-story.md`; the research artifact `research/2026-09-07T09-45-mergeable-project-file-overlaps-research.md`. The research artifact's five decisions (`## Recommended Approach`), its `## Testing Implications`, and its `## Requirements Mapping` are settled. This plan implements them and does not reopen them.

---

## Binding Constraints Recorded Before the Phases

### C1 — Evidence location invariant (non-overridable)

Every evidence artifact resolves to `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Baselines go under `evidence/baseline/`, phase gates and final QC under `evidence/qa-gates/`, the AC status summary under `evidence/issue-updates/`, everything else under `evidence/other/`. In task text below, `<FEATURE>` abbreviates `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643` and `<ISO8601>` is a `yyyy-MM-ddTHH-mm` stamp. The evidence set of this plan also includes the single batch-budget reset artifact `<FEATURE>/evidence/other/batch-budget-resets.<ISO8601>.md` defined in constraint C9, to which every reset section is appended.

`EVIDENCE_LOCATION_OVERRIDE_REJECTED: docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/qa/ replaced with docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/qa-gates/` — the final `spec.md` acceptance criterion names `evidence/qa/`, which is not a canonical kind. The criterion text is not modified (no phantom or edited criteria); the QA gate artifacts it refers to are written under `evidence/qa-gates/`, and P8-T17 records this substitution beside that criterion.

Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Baseline and final-QC test artifacts additionally carry numeric coverage values in `Output Summary:`. The literal string `UNVERIFIED` must not appear in any artifact.

The Python coverage tasks write a JSON report to `artifacts/python/coverage.json`. That is a gitignored coverage-tool output path already used by the project `addopts` for `artifacts/python/lcov.info`; it is a tool artifact read to extract a number, not an evidence artifact. The evidence lands under `<FEATURE>/evidence/`.

### C2 — The 500-line ceiling and the named splits

`.claude/rules/general-code-change.md` sets a 500-line ceiling covering production and test code (Markdown and raw text fixtures are exempt). `wc -l` values on the current tree (branch head `903151ff`) at revision time:

| File | Lines | Disposition |
| --- | --- | --- |
| `tests/scripts/claude-lib/blast-radius/BlastRadiusConfig.Tests.ps1` | 500 | not edited |
| `tests/scripts/dev_tools/test_blast_radius_config_parity.py` | 499 | not edited; the new key reaches it through `BYTE_EQUAL_KEYS` |
| `.claude/lib/blast-radius/BlastRadius.psm1` | 495 | two helpers and one constant relocate out to `BlastRadiusConflict.psm1`; one import, one filter, and help text come in; target at or under 450 |
| `tests/scripts/dev_tools/test_validate_parallel_orchestrator_state.py` | 486 | not edited; the tolerance case lives in a new sibling module |
| `extensions/drm-copilot/test/lib/push-down/blast-radius-derive-core.test.ts` | 482 | two cases rewritten in place, one comment edited; net change at most +4 |
| `.claude/lib/blast-radius/BlastRadiusConfig.psm1` | 473 | not edited; the new reader lives in `BlastRadiusConflict.psm1` |
| `extensions/drm-copilot/test/lib/push-down/blast-radius-derive.test.ts` | 472 | two literals rewritten; net 0 |
| `extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts` | 468 | manifest constants, predicates, classification, and `compareOrdinal` move to `claude-blast-radius-derive-manifests.ts`; target at or under 420 |
| `scripts/dev_tools/_blast_radius_validation.py` | 464 | not edited; the new reader lives in `_blast_radius_mergeable.py` |
| `extensions/drm-copilot/test/lib/push-down/claude-config-carriage.test.ts` | 461 | two comments edited; net change at most +2 |
| `tests/scripts/claude-lib/blast-radius/BlastRadius.Conflict.Tests.ps1` | 435 | one `Context` with three `It` blocks added; target at or under 490 |
| `tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py` | 420 | one test added |
| `extensions/drm-copilot/test/lib/validate/parallel-orchestrator-state-core.test.ts` | 417 | one `describe` with two `it` blocks added |
| `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1` | 325 | one `It` block added |
| `tests/scripts/claude-lib/blast-radius/BlastRadius.KeyPartition.Tests.ps1` | 268 | one array element added |
| `scripts/dev_tools/_blast_radius_conflicts.py` | 241 | filter and docstring edit |

Every task that touches a file in this table names the split or relocation target and carries a `wc -l` observation with the bound stated in the task text.

### C3 — Toolchain state and command shapes

The scripts this plan invokes are declared in `extensions/drm-copilot/package.json`: `format` (`prettier --write "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`), `lint` (`eslint --no-error-on-unmatched-pattern src test`), `typecheck` (`tsc -p ./ --noEmit`), `test:unit` (`node run-jest.cjs`), and `test:coverage` (`node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary`). There is no `test:unit:coverage` script. Working-directory mechanism for every `extensions/drm-copilot` command: the executor's Bash working directory resets between calls, `npm` is not on the `atomic-executor` tool allowlist (`.claude/agents/atomic-executor.md` lines 11-20 grant `Bash(npx prettier *)`, `Bash(npx eslint *)`, `Bash(npx tsc *)`, `Bash(npx jest *)`, `Bash(pwsh *)`, and `Bash(git *)`), and `.claude/hooks/validate-bash.ps1` denies a `cd`-chained read command. Every extension command in this plan is therefore stated in the form `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; <command>; $code = $LASTEXITCODE; Pop-Location; exit $code'`, run from the worktree root, so the repo-defined npm script runs with the extension as its working directory and its exit code is propagated. P0-T2 is a verify-or-install task for `extensions/drm-copilot/node_modules`. C3 fixes the invocation form for wrapped commands only and does not assert that every unwrapped command (`wc`, `grep`, `poetry run python -c`) is in the executor allowlist; those execute in this runtime under the session permission mode.

Every PowerShell cmdlet observation in this plan (`Get-FileHash`, `Get-ChildItem`, `Test-Path`, `Import-PowerShellDataFile`, `Invoke-Pester`, and the fixture byte probes) is likewise stated in the form `pwsh -NoProfile -Command "..."` run from the worktree root, because a bare cmdlet is not a Bash command and the executor's shell is Bash. The `-Command` payload is double-quoted when it contains neither `$` nor a backtick and single-quoted otherwise, so Bash passes the payload through unexpanded. Every SHA-256 comparison, including each task that states only that two hashes are equal, is taken as `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 <self-hosted path>, <mirror path>).Hash"`, which prints one uppercase hex line per path in argument order; the pair is equal when the two lines are identical.

The project `addopts` (`pyproject.toml` line 115) supplies an LCOV reporter only, so every Python coverage command passes `--cov-report=term-missing` and the dotted `--cov=scripts.dev_tools` form.

`scripts/powershell/PoshQC/settings/pester.runsettings.psd1` `CodeCoverage.Path` is an explicit per-file allow-list (its own comment at line 159 states this). Every new PowerShell production file is appended to it, and PowerShell coverage numbers are read from the self-hosted run's JaCoCo report `artifacts/pester/powershell-coverage.xml` (per-`sourcefile` `LINE` counters), not from the MCP payload, which carries only `ok`, `tool`, `workspace_root`, and `summary`. Both the MCP tool call (required by `.claude/rules/powershell.md`) and the self-hosted `Invoke-PoshQCTest -Root` call are recorded, following the precedent at `docs/features/completed/2026-08-28-conflictresult-truthiness-always-true-576/evidence/qa-gates/final-powershell-test.2026-08-28T12-46.md`.

Success-case outputs observed from recorded runs before being asserted below: Black prints `N files left unchanged.` (and `M files reformatted` only when it rewrote); Ruff prints `All checks passed!`; Pyright prints `0 errors, 0 warnings, 0 informations`; pytest prints `N passed, M skipped in Xs` plus a `TOTAL` row with a single combined `Cover` column; Prettier `--write` prints one line per matched file carrying `(unchanged)` when nothing was rewritten; ESLint and `tsc` print nothing on success; Jest prints `Test Suites: N passed, N total` and `Tests: N passed, N total`, and with `--coverageReporters=text` an `All files` row with separate `% Stmts`, `% Branch`, `% Funcs`, `% Lines` columns; Pester prints `Tests Passed: N, Failed: 0, Skipped: K, Inconclusive: 0, NotRun: 0`; the self-hosted PoshQC test prints a `Covered X% / 0%. N analyzed Commands in M Files.` line.

### C4 — Pre-existing local test failure (bounded exemption)

A local-only failure of `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` naming a path under `.claude/state/` is known from issue #510 and is green in CI. P0-T7 records whether it is present on this tree. Any later task that requires `EXIT_CODE: 0` from a pytest run that includes that module accepts `EXIT_CODE: 1` only when all three hold: the failing node ID set is exactly the set recorded by P0-T7; the run reports exactly that many failures; every assertion message names a path under `.claude/state/`. Because `list_scoped_files` sorts paths and `.claude/agents`, `.claude/lib`, `.claude/rules`, and `.claude/skills` all sort before `.claude/state`, a mirror defect introduced by this plan surfaces before the exempted assertion and is never masked by it. No other failure is exempt.

### C5 — Surface-contract tests that pin files this plan edits

`tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` pins: exactly nine `##` headings in `.claude/agents/parallel-orchestrator.md`; the first thirteen `##` headings of `.claude/skills/parallel-orchestrate/SKILL.md`; the fragments in `MERGE_CONFLICT_FRAGMENTS` (`with the cap of 3`, `terminal \`merge_status: blocked_ci_loop_limit\``, `Boundary with F8`, and the F8 hand-off sentence) inside `## Per-Item Merge-Conflict Handling`; exactly six backticked header fields and exactly three `section \`## ...\`` projection references inside `## Documentation Maintenance Boundaries`; and no `pr-author` entry in the agent `tools:` list. Consequently: no task adds a `##` heading to the agent or the skill; the existing five merge-conflict steps are retained verbatim as the escalation path; the new projection is described in the boundaries section without using the phrase `section \`## Mergeable Conflicts Resolved\``; and the four allowlist entries name no `pr-author` channel. `tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py` and `test_parallel_planner_surface_contracts_landed.py` pin `parallel-plan/SKILL.md` and `parallel-planner.md`; Phase 6 edits are additive sentences only and both suites are run as the gate.

### C6 — Falsifiable acceptance conditions

Per `.claude/rules/plan-acceptance-gates.md`: coverage targets are importable dotted names with the `=` form; every `git diff` is anchored to the merge-base commit `c3ffb080` and every name-listing diff is paired with `git status --porcelain --untracked-files=all` (the `--untracked-files=all` flag is required because an untracked directory otherwise collapses to a single entry); every write-mode command records an observation beyond its exit code; every condition that a test can carry is carried by a named test node ID.

Search-tool rules applied throughout: `git grep` is used only against a path that is tracked at `c3ffb080`, because `git grep` never reads an untracked file and the plan commits nothing during execution; every assertion against a file or directory that an earlier task of this plan creates uses plain `grep`. `git grep -c` prints `<path>:<count>` per matched path, so its acceptance is worded "reports a count of N for that path"; with no match it prints nothing and exits 1, worded "reports no match (exit 1)". Any Bash pattern operand that contains a backtick or a `$` is single-quoted so the shell does not treat it as command substitution or a variable.

Check-off assertions count checkbox lines directly, because `spec.md` (253 lines) and `user-story.md` (70 lines) are added files relative to `c3ffb080`, so a `--numstat` removed count is structurally `0` and no added-versus-removed pairing can hold. The two counted literals are `- [x] ` (checked, with a trailing space) and `- [ ] ` (unchecked, with a trailing space); `spec.md` carries 51 `- [ ] ` lines and `user-story.md` 11 on the current tree, and neither file carries a `- [x] ` line. Every checkbox count supplies the pattern through `-e`, because the patterns `- [x] ` and `- [ ] ` begin with a hyphen and GNU grep otherwise reads them as an option cluster and exits 2 without searching. `grep -c` prints `0` and exits 1 when there is no match, so every assertion in this plan that expects a printed `0` from `grep -c` (the checkbox counts of [P7-T8] on `user-story.md` and [P8-T17] on both files, the `System.Xml` counts of [P5-T2] and [P5-T19], the staging-command count of [P5-T3], and the barrier-token count of [P7-T7]) is satisfied by the printed value and not by the exit code. No other search pattern in this plan begins with a hyphen.

Literals this plan instructs the executor to create, quoted here verbatim so an assertion naming one is exonerated: `add no field to it that the rule file does not declare`, `add no field to it that`, `mergeable_conflicts_resolved` is the one declared optional item field, `.claude/rules/parallel-orchestration.md` (named in the narrowed skill sentence), `mergeable_paths`, `CONFIG_MERGEABLE_PATHS`, `config_mergeable_paths`, `matches_mergeable_path`, `exclude_mergeable_paths`, `Get-ConfigMergeablePath`, `Test-MergeablePath`, `Get-NonMergeablePathEntry`, `BlastRadiusConflict.psm1`, `MODULE_MANIFEST_SUFFIXES`, `NON_MODULE_MANIFEST_SUFFIXES`, `structureObserved`, `modulePaths`, `claude-blast-radius-derive-manifests`, `ProjectFileMergeGrammar.psm1`, `ProjectFileMerge.psm1`, `Resolve-MergeableConflict.ps1`, `Get-MergeableUnit`, `Compare-UnitVersion`, `Get-ProjectFileKind`, `Get-ConflictHunk`, `Merge-ConflictedText`, `Test-NeverDropPostCondition`, `Get-UnitKeySet`, `Invoke-MergeableConflictResolution`, `Read-ConflictedFile`, `Write-MergedFile`, `mergeable_conflicts_resolved`, `escalate_paths`, `## Mergeable Conflicts Resolved`, `### Mechanically-mergeable path class (issue #643)`, `Bash(pwsh -NoProfile -File .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1*)`, `Bash(dotnet tool restore*)`, `Bash(dotnet csharpier check *)`, `Bash(dotnet build *)`, `cohorts_mergeable_only_overlaps`, `conflict-mergeable-csproj-no-edge`, `conflict-mergeable-glob-still-contends`, `test_blast_radius_mergeable_paths`, `test_parallel_mergeable_cohort`, `test_validate_parallel_orchestrator_state_mergeable`, `blast-radius-derive-mergeable.test.ts`, `blast-radius-derive-manifests.test.ts`, `BlastRadiusConflict.Tests.ps1`, `ProjectFileMergeGrammar.Tests.ps1`, `ProjectFileMerge.Tests.ps1`, `Resolve-MergeableConflict.Tests.ps1`, `ProjectFileMerge.Manifest.Tests.ps1`, the `.gitattributes` line `tests/fixtures/project_file_merge/crlf-*.csproj -text`, and the sentence fragment `` `version`, `over_breadth_fraction`, `mandate_reads`, and `mergeable_paths` ``.

### C7 — Fixed design values

- Truth-table key order in both copies: `version`, `shared_surfaces`, `shared_surface_globs`, `mandate_reads`, `mergeable_paths`, `modules`, `over_breadth_fraction`. Value: `["**/*.csproj", "**/packages.config", "**/app.config", "**/*.vbproj", "**/*.props"]`, one entry per line, byte-equal between the copies.
- Derived destination document key order: `version`, `shared_surfaces`, `shared_surface_globs`, `mandate_reads`, `mergeable_paths`, `modules`, `over_breadth_fraction`; `mergeable_paths` is `CARRIED_KEYS[5]` and is omitted entirely when the source lacks it.
- Mergeable matcher semantics (both runtimes): exact ordinal equality first; then, for a concrete (non-glob) entry only, glob containment; then, for a concrete entry only and for every configured pattern beginning with `**/`, the same two-step test against the pattern with that prefix removed (ordinal equality when the stripped pattern has no wildcard, glob containment otherwise). A glob entry is never mergeable unless it equals a configured pattern verbatim. `_glob_to_regex_text` and `BlastRadiusGlob.psm1` are not modified.
- Application point: only inside `conflicts` (`scripts/dev_tools/_blast_radius_conflicts.py`) and `Test-BlastRadiusConflict` (`.claude/lib/blast-radius/BlastRadius.psm1`), filtering both radii's `paths` immediately before the smallest-path-overlap computation. Radius records are unchanged.
- Entry keys of the merge record: `<ItemType>:<Include>` for MSBuild items, `package:<id>` for `packages.config`, `bindingRedirect:<name>` for `app.config`.
- `Resolve-MergeableConflict.ps1` exits 0 for both verdicts; the verdict is the `result` field of the single JSON object on stdout. A non-zero exit occurs only on an unexpected error, whose message goes to stderr.

### C8 — Out of scope

Consumer copies of `config/blast-radius.json` and `.claude/hooks/enforce-parallel-cohort-barrier.ps1` are not patched; `PARALLEL_COHORT_BARRIER_VIOLATION`, the barrier hook, its tests, and the cohort-barrier validator tests are not modified; no orchestrator invariant is added or changed; no JSON Schema is authored; over-breadth gating at planning time, a Python or bash merge step, rebase-based resolution, and SDK-style project handling are excluded. No task below addresses any of these. Committing, rebasing, pushing, and pull-request authoring are the orchestrator's and `pr-author`'s steps and appear nowhere in this plan.

### C9 — Per-batch file budgets

`.claude/rules/powershell.md` `## Change Budget` caps a batch at 3 production and 3 test PowerShell files. `.claude/hooks/enforce-powershell-batch-budget.ps1` and `.claude/hooks/enforce-python-batch-budget.ps1` are registered on the `Write|Edit` PreToolUse matcher in `.claude/settings.json` and enforce the same 3/3 caps for `.ps1`, `.psm1`, `.psd1` and for `.py`. Both count distinct paths for the whole session, classify a path under `tests/` (and any `*.Tests.ps1`) as a test file and everything else as production, deny the fourth distinct file of a kind (`enforce-powershell-batch-budget.ps1:293`), and clear only when `.claude/state/powershell-batch-budget.<session_id>.json` or `.claude/state/python-batch-budget.<session_id>.json` is deleted. `.claude/state/` is gitignored, so a reset changes no listing in P0-T15, P7-T7, P8-T1, or P8-T15.

This plan writes 6 distinct production and 8 distinct test PowerShell files and 3 distinct production and 5 distinct test Python files, so both caps are reached during execution. Every mirror copy in this plan (P3-T5, P5-T14) is performed with `pwsh -NoProfile -Command "Copy-Item -LiteralPath <source> -Destination <mirror> -Force"` rather than the Write or Edit tool, so a mirror consumes no budget slot; using Write for a mirror invalidates the reset points below.

Reset command for either runtime, with `<kind>` replaced by `powershell` or `python`:

`pwsh -NoProfile -Command 'Get-ChildItem -Path .claude/state -Filter "<kind>-batch-budget.*.json" -ErrorAction SilentlyContinue | ForEach-Object { Write-Output ("PRE-RESET " + $_.FullName + " " + (Get-Content -Raw $_.FullName)); Remove-Item -LiteralPath $_.FullName -Force }'`

Every reset appends a section to the single artifact `<FEATURE>/evidence/other/batch-budget-resets.<ISO8601>.md` carrying `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that lists each `PRE-RESET` line with the file's pre-reset `prodFiles` and `testFiles` arrays, or records explicitly that zero state files were enumerated. The post-reset observation is `pwsh -NoProfile -Command "(Get-ChildItem -Path .claude/state -Filter '<kind>-batch-budget.*.json' -ErrorAction SilentlyContinue | Measure-Object).Count"` printing `0`; the exit code is recorded but carries no acceptance condition, because `Get-ChildItem` on a missing directory returns exit 1 through `pwsh -Command` even when the enumeration is empty.

Contingency, applying to every task in this plan: if a Write or Edit is denied by either batch-budget hook, run the reset for that runtime, append the section to the same artifact naming the denied path and the state file quoted in the deny reason, and retry the write once.

---

## Acceptance-Criteria Traceability

IDs are assigned in document order. `S1`–`S39` are the 39 items under `## Acceptance Criteria` in `spec.md`; `D1`–`D7` are the 7 items under `## Definition of Done`; `T1`–`T5` are the 5 items under `## Seeded Test Conditions (from potential)`; `U1`–`U11` are the 11 items under `## Acceptance Criteria` in `user-story.md`.

| ID | Subject | Implementation tasks | Verifying tests / evidence | Check-off task |
| --- | --- | --- | --- | --- |
| S1 | both copies carry `mergeable_paths`, byte-equal | P1-T1, P1-T2, P1-T3, P1-T4 | `test_class_one_keys_are_equal_across_both_committed_copies[mergeable_paths]`; KeyPartition `declares equal values for the runtime-describing keys in both copies` (P1-T9, P1-T10) | P1-T12 |
| S2 | optional and fail-closed (absent = empty list) | P2-T1, P3-T1, P3-T2 | `test_conflicts_absent_key_and_empty_list_produce_identical_results`; `BlastRadiusConflict.Tests.ps1` `returns identical results for an absent key and an empty list` (P2-T5, P3-T9) | P3-T13 |
| S3 | readers reject non-list and blank; absent yields empty | P1-T6, P3-T1 | four reader tests in `test_blast_radius_mergeable_paths.py`; four `Get-ConfigMergeablePath` cases (P1-T8, P3-T9) | P3-T13 |
| S4 | `.csproj`-only overlap yields no edge, path retained (parity fixture) | P2-T1, P3-T2, P3-T6 | `test_conflict_fixture_reproduces_the_expected_verdict[conflict-mergeable-csproj-no-edge]`; Parity Pester `reproduces the expected verdict for conflict-mergeable-csproj-no-edge` (P3-T9, P3-T10) | P3-T13 |
| S5 | declared glob entry still contends (parity fixture) | P1-T6, P3-T1, P3-T6 | `...[conflict-mergeable-glob-still-contends]` in both parity suites (P3-T9, P3-T10) | P3-T13 |
| S6 | `**/` matches root-level file, glob translation untouched | P1-T6, P3-T1 | `test_matches_mergeable_path_matches_a_root_level_file_for_a_double_star_prefix`; `matches a root-level packages.config for a double-star prefix` (P1-T8, P3-T9); P7-T7 diff shows `_blast_radius_glob.py` and `BlastRadiusGlob.psm1` unchanged | P3-T13 |
| S7 | exclusion only inside the relation; V1/V2/V3 identical | P2-T1 | `test_validate_blast_radius_findings_are_identical_with_and_without_the_key`, `test_derive_blast_radius_keeps_a_cited_csproj_in_paths`, `test_detect_escaped_paths_is_unaffected_by_the_key` (P2-T5) | P2-T8 |
| S8 | drift recomputation inherits the exclusion | P2-T1, P2-T4 | `test_recompute_conflicts_reports_no_pair_for_a_csproj_only_observed_overlap` (P2-T5) | P2-T8 |
| S9 | `BlastRadiusConflict.psm1` exists; relation imports it and filters | P3-T1, P3-T2 | `BlastRadiusConflict.Tests.ps1`; `BlastRadius.Conflict.Tests.ps1` new Context (P3-T9) | P3-T13 |
| S10 | three procedural consumers state the relation reads the key | P6-T7 | P6-T8 grep counts and suites | P6-T9 |
| S11 | `CARRIED_KEYS` carriage, position, omission | P4-T2, P4-T4, P4-T6 | `blast-radius-derive-mergeable.test.ts` three cases; key-order cases in the two existing files (P4-T11) | P4-T13 |
| S12 | rule documents the class with three constraints | P6-T1 | P6-T8 grep count of the heading in both copies; mirror byte-identity test | P6-T9 |
| S13 | rule key-partition sentence names four keys | P6-T2 | P6-T8 grep count of the fragment in both copies | P6-T9 |
| S14 | manifests module exists with the split, re-exported | P4-T1, P4-T2, P4-T3 | `blast-radius-derive-manifests.test.ts` family-split and re-export cases (P4-T11); the new file's own `coverageThreshold` entry gates it in P4-T12 | P4-T13 |
| S15 | .NET layout derives exactly `{ "config": ["config/**"] }` | P4-T1, P4-T2 | `derives exactly the config payload module for a nine-project .NET layout` (P4-T11) | P4-T13 |
| S16 | nested `.sln` no module; mixed layout keeps non-.NET module | P4-T1, P4-T2 | `yields no module for a nested solution file`; `keeps the non-.NET module beside config in a mixed layout` (P4-T11) | P4-T13 |
| S17 | `assembleModules`/`PAYLOAD_MODULES` unchanged; guard non-vacuous | P4-T2, P4-T4 | existing `blast-radius-derive-core.test.ts` guard cases with re-pointed manifests (P4-T11) | P4-T13 |
| S18 | three merge files exist, mirrored, in `core.json`; convention test; pack e2e | P5-T1, P5-T2, P5-T3, P5-T13, P5-T14 | `ClaudeLibModuleConvention.Tests.ps1`, `ProjectFileMerge.Manifest.Tests.ps1` (P5-T19); `test_push_down_no_arguments_publishes_full_tree` (P5-T20) | P5-T22 |
| S19 | five MSBuild item types, both forms, keyed union | P5-T1, P5-T2, P5-T4 | `ProjectFileMerge.Tests.ps1` ten item-type cases (P5-T19) | P5-T22 |
| S20 | `packages.config` union and higher version recorded | P5-T1, P5-T2, P5-T5 | `ProjectFileMerge.Tests.ps1` packages cases (P5-T19) | P5-T22 |
| S21 | `app.config` union; `newVersion`/`oldVersion` rule | P5-T1, P5-T2, P5-T5 | `ProjectFileMerge.Tests.ps1` app.config cases (P5-T19) | P5-T22 |
| S22 | unparseable version and same-key-different-attributes escalate | P5-T1, P5-T2, P5-T5, P5-T6 | `ProjectFileMergeGrammar.Tests.ps1` and `ProjectFileMerge.Tests.ps1` escalate cases (P5-T19) | P5-T22 |
| S23 | `merge` and `diff3` hunks parse; non-grammar line escalates | P5-T2, P5-T6 | `ProjectFileMerge.Tests.ps1` diff3 and non-grammar cases (P5-T19) | P5-T22 |
| S24 | never-drop post-condition against `:2:`/`:3:`/`:1:` | P5-T2, P5-T3, P5-T7 | `ProjectFileMerge.Tests.ps1` post-condition cases; `Resolve-MergeableConflict.Tests.ps1` never-drop case (P5-T19) | P5-T22 |
| S25 | BOM, encoding, terminators preserved; no DOM | P5-T2, P5-T3, P5-T6 | CRLF and BOM cases in `ProjectFileMerge.Tests.ps1` and the three `Byte-level seams` cases in `Resolve-MergeableConflict.Tests.ps1` (P5-T19); P5-T19 grep count 0 for `System.Xml` in the three files; the `.gitattributes` exemption of P5-T6 keeps the CRLF fixture CRLF on a fresh checkout | P5-T22 |
| S26 | non-mergeable path in the conflict set escalates, no write | P5-T3, P5-T7 | `Resolve-MergeableConflict.Tests.ps1` escalate case (P5-T19) | P5-T22 |
| S27 | single JSON object; shared matcher; stages nothing | P5-T3 | `Resolve-MergeableConflict.Tests.ps1` stdout-shape and no-git-write cases (P5-T19) | P5-T22 |
| S28 | skill documents the dotnet checks and revert as first step | P5-T16 | P5-T20 grep counts in skill and mirror | P5-T22 |
| S29 | skill documents `merge --abort` and fall-through with `escalate_paths` | P5-T16 | P5-T20 grep counts | P5-T22 |
| S30 | agent carries exactly the four allowlist entries | P5-T15 | P5-T20 grep counts (4 entries, each once, in agent and mirror) and `test_agent_tools_allowlist_excludes_pr_author_channel` | P5-T22 |
| S31 | validator tolerance, Python and TypeScript | P7-T3, P7-T4 | `test_item_carrying_mergeable_conflicts_resolved_yields_no_errors`, `test_item_omitting_mergeable_conflicts_resolved_yields_no_errors`; two `it` cases (P7-T5, P7-T6) | P7-T8 |
| S32 | rule declares the field; "add no field" narrowed; invariant 9 unchanged | P6-T4, P6-T6 | P6-T8 grep counts; invariant 9 line unchanged in diff | P6-T9 |
| S33 | template section; skill states the four citation places | P5-T17, P5-T18 | P5-T20 grep counts; `test_seam_status_template_realises_projections_prescribed_by_skill` still passes | P5-T22 |
| S34 | zero edges, single cohort, Python and bash-lane fixture | P7-T1, P7-T2 | `test_csproj_only_overlaps_place_every_item_in_a_single_cohort`; `test_reference_implementation_reproduces_the_fixture[cohorts_mergeable_only_overlaps]` (P7-T5); the same fixture is consumed by `tests/shell/parallel_cohorts_parity.bats` in CI | P7-T8 |
| S35 | barrier hook, violation code, and their tests unmodified | none | P7-T7 anchored diff and porcelain | P7-T8 |
| S36 | registries extended in both copies | P1-T3, P1-T4 | `test_every_top_level_key_is_classified_and_shared_by_both_copies`; KeyPartition `requires every top-level key in both copies to be classified and shared` (P1-T9, P1-T10) | P1-T12 |
| S37 | TruthTable shape case; Python non-vacuity in the new module | P1-T5, P1-T8 | `declares mergeable_paths as a list of non-empty strings`; `test_committed_mergeable_paths_is_a_non_empty_list_in_both_copies` (P1-T8, P1-T10) | P1-T12 |
| S38 | every touched file under 500 lines by the named splits | all split tasks | P8-T1 line-count sweep; `ClaudeLibModuleConvention.Tests.ps1` | P8-T17 |
| S39 | full toolchain single pass, coverage thresholds, no temp files | P4-T3 (per-file jest threshold for the new production file), P8-T2 through P8-T14 | final QC artifacts under `evidence/qa-gates/` | P8-T17 |
| D1 | AC documented and mapped | this table | this table | P8-T17 |
| D2 | behavior matches AC | all | P8-T17 reconciliation | P8-T17 |
| D3 | tests updated/added | P1-T8, P2-T2, P2-T3, P2-T4, P3-T7, P3-T8, P4-T8, P4-T9, P5-T8 through P5-T11, P7-T2 through P7-T4 | phase gates | P8-T17 |
| D4 | edge cases and error handling covered | same as D3 | escalate, absent-key, blank-entry, non-grammar, unparseable-version cases | P8-T17 |
| D5 | docs updated | P5-T16, P5-T17, P5-T18, P6-T1 through P6-T7 | P6-T8 | P8-T17 |
| D6 | telemetry/logging | P5-T3 (JSON report), P5-T17 (evidence artifact and citations), P5-T18 | P5-T20 | P8-T17 |
| D7 | toolchain pass | P8-T2 through P8-T13 | P8-T13 | P8-T17 |
| T1 | unit coverage: present/absent, no edge, path retained, V1-V3 | P1-T8, P2-T2, P2-T3 | P2-T5 | P3-T13 |
| T2 | module derivation fixture with nine `.csproj` directories | P4-T8 | P4-T11 | P4-T13 |
| T3 | merge-step fixtures per item type; non-mergeable escalates | P5-T4 through P5-T10 | P5-T19 | P5-T22 |
| T4 | key-partition parity (Python, TypeScript, Pester) | P1-T3, P1-T4, P4-T2, P4-T9 | P1-T9, P1-T10, P4-T11 | P4-T13 |
| T5 | zero-edge single-cohort run | P7-T1, P7-T2 | P7-T5 | P7-T8 |
| U1 | empty conflict-edge list in the projection | P2-T1, P3-T2, P7-T2 | `test_csproj_only_overlaps_yield_no_edge_for_any_pair` (P7-T5); `## Conflict Edges` projection unchanged (P7-T7) | P7-T8 |
| U2 | items placed in one cohort and launched together | P7-T1, P7-T2 | P7-T5 | P7-T8 |
| U3 | radius still lists the project file; drift still reports undeclared write | P2-T1, P2-T3 | `test_derive_blast_radius_keeps_a_cited_csproj_in_paths`, `test_detect_escaped_paths_is_unaffected_by_the_key` (P2-T5) | P2-T8 |
| U4 | no per-assembly module; no `module_overlap` edge | P4-T1, P4-T2 | P4-T11 | P4-T13 |
| U5 | behavior arrives by push-down and survives it | P3-T4, P3-T5, P4-T2, P5-T13, P5-T14 | `test_push_down_no_arguments_publishes_full_tree`, `test_push_down_claude_repeated_generation_is_deterministic`, mirror byte-identity (P5-T20) | P5-T22 |
| U6 | conflict confined to project files resolved without re-delegation, no entry lost | P5-T2, P5-T3, P5-T16 | P5-T19 never-drop and union cases | P5-T22 |
| U7 | higher version selected and reported | P5-T1, P5-T2 | P5-T19 version cases | P5-T22 |
| U8 | escalation names the paths | P5-T3, P5-T16 | P5-T19 escalate case (`escalate_paths` populated) | P5-T22 |
| U9 | formatter and analyzer after resolution; revert on failure | P5-T16 | P5-T20 grep counts | P5-T22 |
| U10 | resolution cited in projection, PR body, commit, evidence artifact | P5-T17, P5-T18 | P5-T20 grep counts | P5-T22 |
| U11 | no change without a conflict or without the key | P2-T1, P3-T2, P7-T3, P7-T4 | absent-key equivalence cases (P2-T5, P3-T9); tolerance cases (P7-T5, P7-T6) | P7-T8 |

---

### Phase 0 — Policy reads, environment verification, and baseline capture

- [x] [P0-T1] Read the policy files in the order `CLAUDE.md` and `.claude/skills/policy-compliance-order/SKILL.md` mandate, and write the read record to `<FEATURE>/evidence/baseline/phase0-instructions-read.<ISO8601>.md` with fields `Timestamp:`, `Policy Order:`, and an explicit list of the files read. The list must contain, in order: `.github/copilot-instructions.md`; `.github/instructions/general-code-change.instructions.md`; `.github/instructions/general-unit-test.instructions.md`; `.github/instructions/python-code-change.instructions.md`; `.github/instructions/python-unit-test.instructions.md`; `.github/instructions/typescript-code-change.instructions.md`; `.github/instructions/typescript-unit-test.instructions.md`; `.github/instructions/powershell-code-change.instructions.md`; `.github/instructions/powershell-unit-test.instructions.md`; `.claude/rules/general-code-change.md`; `.claude/rules/general-unit-test.md`; `.claude/rules/quality-tiers.md`; `.claude/rules/parallel-orchestration.md`; `.claude/rules/plan-acceptance-gates.md`; `.claude/rules/python.md`; `.claude/rules/python-suppressions.md`; `.claude/rules/typescript.md`; `.claude/rules/typescript-suppressions.md`; `.claude/rules/powershell.md`; `.claude/rules/self-explanatory-code-commenting.md`; `.claude/rules/tonality.md`; `.claude/skills/atomic-plan-contract/SKILL.md`; `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`; `.claude/skills/acceptance-criteria-tracking/SKILL.md`. Acceptance: the artifact exists and names every file above in that order.

- [x] [P0-T2] Verify the npm dependency tree for `extensions/drm-copilot` and install it only if missing: probe with `pwsh -NoProfile -Command "Test-Path extensions/drm-copilot/node_modules"` (prints `True` or `False`), and run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm ci; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root only when the probe prints `False`. Artifact: `<FEATURE>/evidence/baseline/npm-ci-extension.<ISO8601>.md` with the four required fields; `Output Summary:` states which branch was taken. Acceptance: `pwsh -NoProfile -Command "Test-Path extensions/drm-copilot/node_modules/jest/package.json"` prints `True` at the end of the task and the artifact states the branch taken.

- [x] [P0-T3] Record the current line count of every file named in constraint C2 by running `wc -l` on each path from the worktree root, plus `wc -l scripts/dev_tools/compute_blast_radius.py tests/scripts/dev_tools/blast_radius_parity_test_support.py extensions/drm-copilot/test/lib/push-down/config-carriage.test-helpers.ts`. Artifact: `<FEATURE>/evidence/baseline/edit-target-line-counts.<ISO8601>.md` with the four required fields. Acceptance: the artifact records one numeric count per path, states the 500-line headroom for each, and reconciles each count against the C2 table (a count differing from the table is recorded as the authoritative value and the task text bound still applies).

- [x] [P0-T4] Capture the Python formatting baseline by running `poetry run black --check .` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/python-black.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and the verbatim `files would be left unchanged` line (and any `would reformat` lines).

- [x] [P0-T5] Capture the Python lint baseline by running `poetry run ruff check .` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/python-ruff.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and the verbatim final line (`All checks passed!` on a clean tree, or the `Found N errors` line otherwise).

- [x] [P0-T6] Capture the Python type-check baseline by running `poetry run pyright` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/python-pyright.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and the verbatim `N errors, N warnings, N informations` line.

- [x] [P0-T7] Capture the Python test and coverage baseline by running the whole suite from the worktree root: `poetry run pytest --cov=scripts.dev_tools --cov-branch --cov-report=term-missing --cov-report=json:artifacts/python/coverage.json`. Artifact: `<FEATURE>/evidence/baseline/python-pytest-coverage.<ISO8601>.md` with the four required fields. `Output Summary:` must carry the verbatim pytest summary line (`N passed, M skipped` with any `F failed`), the verbatim `TOTAL` row, and two numeric percentages computed from the `totals` object of `artifacts/python/coverage.json`: statement coverage as `covered_lines / num_statements * 100` and branch coverage as `covered_branches / num_branches * 100`, each named and each showing the two integers it was computed from. If any test failed, the artifact lists every failing node ID and its assertion message verbatim; this list is the C4 anchor. Acceptance: the artifact records both numeric percentages with their source integers, and records the exit code and the failing-node list (possibly empty).

- [x] [P0-T8] Capture the TypeScript formatting baseline by running `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/typescript-prettier.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and either the verbatim `All matched files use Prettier code style!` line or every `[warn]` line naming an unformatted file.

- [x] [P0-T9] Capture the TypeScript lint baseline by running `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run lint; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/typescript-eslint.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and states either that ESLint printed no diagnostic line or the verbatim `problems` summary line.

- [x] [P0-T10] Capture the TypeScript type-check baseline by running `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run typecheck; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/typescript-typecheck.<ISO8601>.md` with the four required fields. Acceptance: the artifact records the exit code and states either that `tsc` printed no `error TS` line or lists each such line.

- [x] [P0-T11] Capture the TypeScript test and coverage baseline by running `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run test:coverage -- --coverageReporters=text; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/typescript-jest-coverage.<ISO8601>.md` with the four required fields. `Output Summary:` must carry the verbatim `Test Suites:` and `Tests:` lines and the verbatim `All files` row with its four numeric columns named as statements, branches, functions, and lines. Acceptance: the artifact records numeric line and branch percentages read from the `All files` row.

- [x] [P0-T12] Capture the PowerShell formatting baseline: record the two lines printed by `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 .claude/lib/blast-radius/BlastRadius.psm1, tests/scripts/claude-lib/blast-radius/BlastRadius.Conflict.Tests.ps1).Hash"` and the output of `git status --porcelain --untracked-files=all`, invoke the `mcp__drm-copilot__run_poshqc_format` MCP function with `workspace_root` set to the worktree root, then run the same hash command and `git status --porcelain --untracked-files=all` again. Artifact: `<FEATURE>/evidence/baseline/powershell-poshqc-format.<ISO8601>.md` with the four required fields plus the tool's verbatim `ok` and `summary` values. Acceptance: the artifact records `ok` as `true`, both before-and-after digest pairs, both porcelain listings, and states whether any tracked `.ps1` or `.psm1` path appeared in the after-listing that was absent from the before-listing.

- [x] [P0-T13] Capture the PowerShell analyzer baseline by invoking the `mcp__drm-copilot__run_poshqc_analyze` MCP function with `workspace_root` set to the worktree root. Artifact: `<FEATURE>/evidence/baseline/powershell-poshqc-analyze.<ISO8601>.md` with the four required fields plus the verbatim `ok` and `summary` values. Acceptance: the artifact records `ok` as `true` and the verbatim summary string beginning `Ran bundled PoshQC analyze against`. No diagnostic count is asserted, because the payload carries none.

- [x] [P0-T14] Capture the PowerShell test and coverage baseline by invoking the `mcp__drm-copilot__run_poshqc_test` MCP function with `workspace_root` set to the worktree root, then running `pwsh -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest -Root (Get-Location).Path"` from the worktree root. Artifact: `<FEATURE>/evidence/baseline/powershell-poshqc-test.<ISO8601>.md` with the four required fields. `Output Summary:` must carry the MCP `ok` value, the self-hosted run's verbatim `Tests Passed:` line, the verbatim `Covered` line, the report-level `LINE` `covered` and `missed` counters from `artifacts/pester/powershell-coverage.xml` with the derived line-coverage percentage, and the per-`sourcefile` `LINE` counters for `BlastRadius.psm1`. The artifact must state that Pester measures no branch coverage, so no branch threshold applies. Acceptance: the artifact records a numeric report-level line-coverage percentage and the `BlastRadius.psm1` per-file percentage.

- [x] [P0-T15] Record the pre-change change set by running `git merge-base c3ffb080 HEAD`, `git diff c3ffb080 --name-only`, and `git status --porcelain --untracked-files=all` from the worktree root. `c3ffb080` is the merge base of this branch with `origin/main` (the branch was rebased onto it), so the anchored diff is stable even if `origin/main` advances again. Artifact: `<FEATURE>/evidence/baseline/pre-change-scope.<ISO8601>.md` with the four required fields and all three outputs verbatim. Acceptance: `git merge-base c3ffb080 HEAD` prints `c3ffb080` followed by its remaining hexadecimal digits; the anchored diff listing contains each of these six paths: `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/issue.md`, `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/plan.2026-09-07T08-13.md`, `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md`, `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md`, `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/research/2026-09-07T09-45-mergeable-project-file-overlaps-research.md`, and `docs/features/potential/promoted/2026-09-07-mergeable-project-file-overlaps.md`; every path in either listing (tracked or untracked) is one of those six or lies under `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/`; and the artifact states that no path outside that set is listed. On the tree that carries this revision (branch head `903151ff` at revision time, plus the commit that adds the round-3 preflight report and this plan revision) the anchored diff lists nine tracked paths, not six: the six enumerated above plus `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/other/preflight-round-1.2026-09-07T13-38.md`, `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/other/preflight-round-2.2026-09-07T14-20.md`, and `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/evidence/other/preflight-round-3.2026-09-07T14-45.md`, all three admitted by the evidence-folder clause (at `903151ff` itself the third report is not yet committed and the listing has eight). The artifact records the listing verbatim and does not restate it as exactly the six enumerated paths; the acceptance condition still fails on any path outside the admitted set.

### Phase 1 — Truth-table key, key-partition registries, and the Python leaf reader

- [x] [P1-T1] Insert the `mergeable_paths` key into `config/blast-radius.json` immediately after the `mandate_reads` array (currently lines 20-32) and before `"modules"`, with exactly the five entries `**/*.csproj`, `**/packages.config`, `**/app.config`, `**/*.vbproj`, `**/*.props` in that order, one entry per line with two-space indentation matching `mandate_reads`. Acceptance: `poetry run python -c "import json; c=json.load(open('config/blast-radius.json')); print(list(c)); print(c['mergeable_paths'])"` prints the key list `['version', 'shared_surfaces', 'shared_surface_globs', 'mandate_reads', 'mergeable_paths', 'modules', 'over_breadth_fraction']` and the five-entry list in the stated order.

- [x] [P1-T2] Insert the identical `mergeable_paths` block into `extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json` after its `mandate_reads` array (currently lines 12-24) and before `"modules"`. Acceptance: the same Python one-liner against the bundled path prints the same seven-key order and the same five-entry list, and the output of `git diff --no-index config/blast-radius.json extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json` contains no line beginning with `+` or `-` that mentions `mergeable_paths` or any of the five entries (the two blocks are byte-identical; the copies differ only in `shared_surfaces`, `shared_surface_globs`, and `modules`).

- [x] [P1-T3] Append `"mergeable_paths"` to `BYTE_EQUAL_KEYS` at `tests/scripts/dev_tools/blast_radius_parity_test_support.py:105` so the tuple reads `("version", "over_breadth_fraction", "mandate_reads", "mergeable_paths")`, and extend the comment above it to state that `mergeable_paths` describes the runtime (the mechanically-mergeable path class of issue #643). Acceptance: `wc -l tests/scripts/dev_tools/blast_radius_parity_test_support.py` reports at most 260, and P1-T9 passes.

- [x] [P1-T4] Append `'mergeable_paths'` to `$script:ClassOneKeys` at `tests/scripts/claude-lib/blast-radius/BlastRadius.KeyPartition.Tests.ps1:30` so it reads `@('version', 'over_breadth_fraction', 'mandate_reads', 'mergeable_paths')`, and extend the comment above it with the issue #643 reference. Acceptance: `wc -l tests/scripts/claude-lib/blast-radius/BlastRadius.KeyPartition.Tests.ps1` reports at most 275, and P1-T10 passes.

- [x] [P1-T5] Add an `It 'declares mergeable_paths as a list of non-empty strings'` block to the `Context 'Read-by-mandate exclusions'` of `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`, immediately after the `mandate_reads` case ending at line 254, asserting for both `$script:CommittedConfig` and `$script:BundledConfig` that `mergeable_paths` has a count of exactly 5, contains no blank entry, and equals `@('**/*.csproj', '**/packages.config', '**/app.config', '**/*.vbproj', '**/*.props')` in order. Acceptance: `wc -l tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1` reports at most 350, and P1-T10 passes.

- [x] [P1-T6] Create `scripts/dev_tools/_blast_radius_mergeable.py` as a leaf module importing only `scripts.dev_tools._blast_radius_glob` (`is_glob_entry`, `matches_glob`), `scripts.dev_tools._blast_radius_normalization` (`matches_mandate_read`), and `scripts.dev_tools._blast_radius_validation` (`config_string_list`), with: `CONFIG_MERGEABLE_PATHS = "mergeable_paths"`; `config_mergeable_paths(config) -> tuple[str, ...]` returning `config_string_list(config, CONFIG_MERGEABLE_PATHS)`; `matches_mergeable_path(entry, mergeable) -> bool` implementing the C7 matcher semantics (delegate the first two steps to `matches_mandate_read`, then apply the `**/`-stripped test for concrete entries only); and `exclude_mergeable_paths(entries, mergeable) -> tuple[str, ...]` returning the sorted, deduplicated survivors. Every function carries a Google-style docstring, every loop and branch an intent comment, and the module docstring states that the exclusion is applied only inside the contention relation and that no radius record is changed. Acceptance: `poetry run pyright scripts/dev_tools/_blast_radius_mergeable.py` prints `0 errors, 0 warnings, 0 informations`, and `wc -l scripts/dev_tools/_blast_radius_mergeable.py` reports at most 160.

- [x] [P1-T7] Re-export the reader from the facade: add `from scripts.dev_tools._blast_radius_mergeable import config_mergeable_paths` to `scripts/dev_tools/compute_blast_radius.py` immediately before the `_blast_radius_normalization` import at line 49 (isort places `scripts.dev_tools._blast_radius_mergeable` between `_blast_radius_glob` at line 48 and `_blast_radius_normalization` at line 49), and add `"config_mergeable_paths"` to `__all__` (lines 64-75) in alphabetical position immediately before `"conflicts"` (line 69; `config_m` sorts before `confl`). Acceptance: `poetry run python -c "from scripts.dev_tools.compute_blast_radius import config_mergeable_paths as a; from scripts.dev_tools._blast_radius_mergeable import config_mergeable_paths as b; print(a is b)"` prints `True`, `poetry run ruff check scripts/dev_tools/compute_blast_radius.py` prints `All checks passed!`, and `wc -l scripts/dev_tools/compute_blast_radius.py` reports at most 430.

- [x] [P1-T8] Create `tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py`, modelled on `tests/scripts/dev_tools/test_blast_radius_mandate_reads.py` (repo-root resolution at lines 30-34, in-memory configs, no temporary files), with exactly these ten tests: `test_config_mergeable_paths_returns_the_present_entries_sorted`, `test_config_mergeable_paths_absent_key_yields_an_empty_tuple`, `test_config_mergeable_paths_rejects_a_non_list_value` (expects `TypeError` matching `must be a list or tuple`), `test_config_mergeable_paths_rejects_a_blank_entry` (expects `ValueError` matching `must not be empty`), `test_matches_mergeable_path_matches_a_root_level_file_for_a_double_star_prefix` (entry `packages.config` against `**/packages.config` is `True`; entry `Foo.csproj` against `**/*.csproj` is `True`), `test_matches_mergeable_path_never_treats_a_glob_entry_as_mergeable` (entry `Proj/*.csproj` against the five defaults is `False`; entry `**/*.csproj` equal to a configured pattern is `True`), `test_exclude_mergeable_paths_keeps_non_matching_entries_sorted`, `test_committed_mergeable_paths_is_a_non_empty_list_in_both_copies` (reads both committed copies from disk), `test_committed_mergeable_paths_carries_the_five_default_entries` (both copies equal the exact five-entry list in order), and `test_facade_re_exports_the_mergeable_reader` (`compute_blast_radius.config_mergeable_paths is _blast_radius_mergeable.config_mergeable_paths`). Acceptance: `poetry run pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` prints `10 passed`.

- [x] [P1-T9] Run `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py tests/scripts/dev_tools/test_blast_radius_config.py tests/scripts/dev_tools/test_blast_radius_mandate_reads.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase1-python-parity.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`, and the verbose output contains `test_class_one_keys_are_equal_across_both_committed_copies[mergeable_paths] PASSED` and `test_every_top_level_key_is_classified_and_shared_by_both_copies PASSED`.

- [x] [P1-T10] Run `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-lib/blast-radius/BlastRadius.KeyPartition.Tests.ps1, tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1 -Output Detailed"` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase1-pester-key-partition.<ISO8601>.md` with the four required fields and the verbatim `Tests Passed:` line. Acceptance: the `Tests Passed:` line reports `Failed: 0`, and the detailed output lists `declares mergeable_paths as a list of non-empty strings` and `requires every top-level key in both copies to be classified and shared` as passed.

- [x] [P1-T11] Run `poetry run black --check scripts/dev_tools/_blast_radius_mergeable.py scripts/dev_tools/compute_blast_radius.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/blast_radius_parity_test_support.py` and `poetry run ruff check scripts/dev_tools/_blast_radius_mergeable.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` from the worktree root. Acceptance: Black prints `4 files would be left unchanged.` and Ruff prints `All checks passed!`.

- [x] [P1-T12] Check off `spec.md` acceptance criteria S1, S36, and S37 (change `- [ ]` to `- [x]` on those three lines only, text unchanged) per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, citing P1-T9 and P1-T10 in the task's progress output. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `3`, `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `48`, and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` reports a removed count of `0` and an added count of `253` (no line other than the three checkboxes changed).

### Phase 2 — Python contention filter and its tests

- [x] [P2-T1] Edit `scripts/dev_tools/_blast_radius_conflicts.py`: import `config_mergeable_paths` and `exclude_mergeable_paths` from `scripts.dev_tools._blast_radius_mergeable`; in `conflicts` (lines 152-192) replace line 175 with a block that reads `mergeable = config_mergeable_paths(config)` and calls `_smallest_path_overlap(exclude_mergeable_paths(a.paths, mergeable), exclude_mergeable_paths(b.paths, mergeable))`, preceded by an intent comment stating that the exclusion lives only here so every recorded radius stays byte-identical; replace the `config` docstring sentence at lines 160-162 so it states that the relation reads exactly one key, `mergeable_paths`, and is otherwise unchanged; and add one sentence to the module docstring naming the mergeable exclusion. Acceptance: `poetry run pyright scripts/dev_tools/_blast_radius_conflicts.py` prints `0 errors, 0 warnings, 0 informations`, `git grep -c -F "reads no key from it today" -- scripts/dev_tools/_blast_radius_conflicts.py` reports no match (exit 1), and `wc -l scripts/dev_tools/_blast_radius_conflicts.py` reports at most 265.

- [x] [P2-T2] Add four contention tests to `tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` using in-memory `BlastRadius` records built through `normalize_declared_radius` or the `BlastRadius` constructor with `paths` `["QuickFiler.Test/QuickFiler.Test.csproj", "QuickFiler.Test/A.cs"]` and `["QuickFiler.Test/QuickFiler.Test.csproj", "QuickFiler.Test/B.cs"]`, empty modules, surfaces, and contracts, and a config carrying the five default entries: `test_conflicts_yields_no_edge_for_a_csproj_only_overlap` (`conflicts(a, b, config).conflict is False` and `reasons == ()`), `test_conflicts_leaves_the_csproj_in_both_radii_paths` (after the call both `a.paths` and `b.paths` still contain the `.csproj`), `test_conflicts_still_contends_for_a_declared_glob_entry` (radius paths `["Proj/**"]` against `["Proj/*.csproj"]` yields one `path_overlap` reason with detail `Proj/** ~ Proj/*.csproj`), and `test_conflicts_absent_key_and_empty_list_produce_identical_results` (the config without the key and the config with `"mergeable_paths": []` produce equal `ConflictResult` values for the `.csproj` pair, both `conflict is True` with a `path_overlap` reason). Acceptance: `poetry run pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` prints `14 passed`.

- [x] [P2-T3] Add three symmetry tests to the same module: `test_validate_blast_radius_findings_are_identical_with_and_without_the_key` (derive a radius from a plan citing `QuickFiler.Test/QuickFiler.Test.csproj` and `QuickFiler.Test/A.cs` against the committed config and against the same config with `mergeable_paths` removed, then assert `validate_blast_radius` returns equal finding tuples for both and that both derived radii list the `.csproj` in `paths`), `test_derive_blast_radius_keeps_a_cited_csproj_in_paths` (the derived radius under the committed config contains the `.csproj` in `paths`), and `test_detect_escaped_paths_is_unaffected_by_the_key` (`detect_escaped_paths(["QuickFiler.Test/QuickFiler.Test.csproj"], ["QuickFiler.Test/A.cs"])` reports the `.csproj` as escaped, and with the `.csproj` declared reports nothing; the config key is not an input to this function, which the test states in its docstring). Acceptance: `poetry run pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` prints `17 passed`, and `wc -l tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py` reports at most 420.

- [x] [P2-T4] Add `test_recompute_conflicts_reports_no_pair_for_a_csproj_only_observed_overlap` to `tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py`, modelled on `test_recompute_conflicts_uses_the_real_relation_without_mocking` at line 325 (no monkeypatch): items `item(446, ["QuickFiler.Test/QuickFiler.Test.csproj", "QuickFiler.Test/A.cs"])` and `item(445, ["QuickFiler.Test/QuickFiler.Test.csproj", "QuickFiler.Test/B.cs"])`, changed paths equal to item 446's two paths, an empty edge list, and a config equal to `{**CONFIG, "mergeable_paths": [the five defaults]}`; assert the returned pair tuple is `()`. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py` prints `16 passed` (the module collects 15 today: 13 `def test_` functions, two of which are parametrized with two cases each), and `wc -l tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py` reports at most 460.

- [x] [P2-T5] Run `poetry run pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/test_blast_radius_conflicts.py tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py tests/scripts/dev_tools/test_blast_radius_parity.py tests/scripts/dev_tools/test_blast_radius_mandate_reads.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase2-python-contention.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`, and the verbose output contains `test_conflicts_yields_no_edge_for_a_csproj_only_overlap PASSED`, `test_conflicts_still_contends_for_a_declared_glob_entry PASSED`, `test_validate_blast_radius_findings_are_identical_with_and_without_the_key PASSED`, and `test_recompute_conflicts_reports_no_pair_for_a_csproj_only_observed_overlap PASSED`.

- [x] [P2-T6] Run `poetry run pytest tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/test_blast_radius_conflicts.py tests/scripts/dev_tools/test_blast_radius_parity.py --cov=scripts.dev_tools._blast_radius_mergeable --cov=scripts.dev_tools._blast_radius_conflicts --cov-branch --cov-report=term-missing` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase2-python-scoped-coverage.<ISO8601>.md` with the four required fields and the two verbatim table rows. Acceptance: `EXIT_CODE: 0`; the row whose path ends with `_blast_radius_mergeable.py` shows `Cover` at or above 90%; the row whose path ends with `_blast_radius_conflicts.py` shows `Cover` at or above 90%. (The `Missing` column is recorded verbatim but carries no acceptance condition, because an empty `Missing` column co-holds with a percentage bound only at 100%.)

- [x] [P2-T7] Run `poetry run black --check scripts/dev_tools/_blast_radius_conflicts.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py`, `poetry run ruff check scripts/dev_tools/_blast_radius_conflicts.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py`, and `poetry run pyright scripts/dev_tools/_blast_radius_conflicts.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py tests/scripts/dev_tools/test_parallel_drift_detection_conflicts.py` from the worktree root. Acceptance: Black prints `3 files would be left unchanged.`, Ruff prints `All checks passed!`, Pyright prints `0 errors, 0 warnings, 0 informations`.

- [x] [P2-T8] Check off `spec.md` S7 and S8 and `user-story.md` U3, citing P2-T5. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `5` (S1, S7, S8, S36, S37) and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `46`; `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `1` (U3) and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `10`; and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` reports a removed count of `0` for both files with added counts of `253` and `70`.

### Phase 3 — PowerShell port: `BlastRadiusConflict.psm1`, the filter, fixtures, mirrors, manifest

- [x] [P3-T1] Create `.claude/lib/blast-radius/BlastRadiusConflict.psm1` with a comment-based-help header ending in the sentence `CONVENTION: this module fails fast at module scope and imports its siblings with -ErrorAction Stop.`, then `Set-StrictMode -Version Latest` immediately followed by `$ErrorActionPreference = 'Stop'`, then column-0 `Import-Module` lines with `-Force -ErrorAction Stop` for `BlastRadiusGlob.psm1`, `BlastRadiusConfig.psm1`, and `BlastRadiusNormalization.psm1`; module-scope `$script:ConfigMergeablePathKey = 'mergeable_paths'` and `$script:PairDetailSeparator = ' ~ '`; and five exported functions: `Get-ConfigMergeablePath -Config` (returns `@(Get-ConfigStringList -Config $Config -Key $script:ConfigMergeablePathKey)`, help modelled on `Get-ConfigMandateRead` at `BlastRadiusConfig.psm1:285-313`), `Test-MergeablePath -Entry -MergeablePath` (delegates to `Test-MandateRead -Entry $Entry -MandateRead $MergeablePath`, then for a non-glob entry applies the C7 `**/`-stripped test), `Get-NonMergeablePathEntry -Entry -MergeablePath` (survivors through `Get-OrdinalSortedEntry`, modelled on `Get-NonMandateReadEntry` at `BlastRadiusNormalization.psm1:246-291`), and `Get-SmallestPathOverlap` and `Get-SmallestCommonEntry` relocated verbatim from `BlastRadius.psm1:344-404`. Acceptance: `pwsh -NoProfile -Command "Import-Module ./.claude/lib/blast-radius/BlastRadiusConflict.psm1 -Force; (Get-Command -Module BlastRadiusConflict).Name -join ','"` prints exactly the five function names, and `wc -l .claude/lib/blast-radius/BlastRadiusConflict.psm1` reports at most 260.

- [x] [P3-T2] Edit `.claude/lib/blast-radius/BlastRadius.psm1`: add a column-0 `Import-Module (Join-Path -Path $PSScriptRoot -ChildPath 'BlastRadiusConflict.psm1') -Force -ErrorAction Stop` after line 60; delete the `$script:PairDetailSeparator` comment and assignment (lines 84-86) and the two relocated functions (lines 344-404); in `Test-BlastRadiusConflict` replace lines 464-465 with `$mergeable = [string[]]@(Get-ConfigMergeablePath -Config $Config)` followed by a `Get-SmallestPathOverlap` call whose `-PathA` and `-PathB` are `[string[]]@(Get-NonMergeablePathEntry -Entry ([string[]]@($left['paths'])) -MergeablePath $mergeable)` and the same for `$right`, preceded by an intent comment stating the exclusion lives only here; replace the `.PARAMETER Config` text at lines 426-428 so it states the relation reads exactly one key, `mergeable_paths`; and amend the header line 9 list so it names `BlastRadiusConflict.psm1` as the home of the relocated helpers. Apply the edits in descending line order (464-465, then 426-428, then 344-404, then 84-86, then the insertion after line 60, then the header line 9), so no edit invalidates a later anchor; the line numbers are those of the current tree (`Import-Module` lines 56-60, `$script:PairDetailSeparator` at 86, `function Get-SmallestPathOverlap` at 347, `function Get-SmallestCommonEntry` at 381 ending at 404, `.PARAMETER Config` of `Test-BlastRadiusConflict` at 425 with its text at 426-428, and the `Get-SmallestPathOverlap -PathA` call at 464). Acceptance: `git grep -c -F "function Get-SmallestPathOverlap" -- .claude/lib/blast-radius/BlastRadius.psm1` reports no match (exit 1), `git grep -c -F "reads no key from it today" -- .claude/lib/blast-radius/BlastRadius.psm1` reports no match (exit 1), and `wc -l .claude/lib/blast-radius/BlastRadius.psm1` reports at most 450.

- [x] [P3-T3] Append `'.claude/lib/blast-radius/BlastRadiusConflict.psm1'` to `CodeCoverage.Path` in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` immediately after the `'.claude/lib/blast-radius/BlastRadiusTokenShape.psm1'` entry at line 169, preceded by a comment naming issue #643 and stating that the list is an explicit per-file allow-list so the new module must be named to be measured. Acceptance: `git grep -c -F "BlastRadiusConflict.psm1" -- scripts/powershell/PoshQC/settings/pester.runsettings.psd1` reports a count of 1 for that path and the file still parses (`pwsh -NoProfile -Command "Import-PowerShellDataFile ./scripts/powershell/PoshQC/settings/pester.runsettings.psd1 | Out-Null; 'parsed'"` prints `parsed`).

- [x] [P3-T4] Insert `".claude/lib/blast-radius/BlastRadiusConflict.psm1",` into the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` immediately after the `BlastRadiusTokenShape.psm1` entry at line 136. Acceptance: `poetry run python -c "import json; p=json.load(open('extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json'))['paths']; print(p.count('.claude/lib/blast-radius/BlastRadiusConflict.psm1'), len(p)==len(set(p)))"` prints `1 True`.

- [x] [P3-T5] Copy `.claude/lib/blast-radius/BlastRadiusConflict.psm1` and `.claude/lib/blast-radius/BlastRadius.psm1` byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/` (creating the first, overwriting the second) with `pwsh -NoProfile -Command "Copy-Item -LiteralPath .claude/lib/blast-radius/BlastRadiusConflict.psm1 -Destination extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadiusConflict.psm1 -Force; Copy-Item -LiteralPath .claude/lib/blast-radius/BlastRadius.psm1 -Destination extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadius.psm1 -Force"` from the worktree root, not with the Write or Edit tool (constraint C9: a Write to either mirror path would be a fourth distinct production PowerShell file of the batch, after P3-T1, P3-T2, and P3-T3, and would be denied). Acceptance: `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 .claude/lib/blast-radius/BlastRadiusConflict.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadiusConflict.psm1, .claude/lib/blast-radius/BlastRadius.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadius.psm1).Hash"` prints four lines of which the first equals the second and the third equals the fourth (two equal pairs recorded in the task output).

- [x] [P3-T6] Create two parity fixtures under `tests/fixtures/blast_radius/`, each in the shape of `conflict-directory-vs-file.json` (`description`, `input.radius_a`, `input.radius_b`, `input.config`, `expected.conflict`, `expected.reasons`) with `source` `declared`, `computed_at` `2026-09-07T09-00`, empty `modules`, `shared_surfaces`, and `contracts`, and a `config` carrying `version` 1, empty `shared_surfaces` and `shared_surface_globs`, the five default `mergeable_paths`, `modules` `{ "config": ["config/**"] }`, and `over_breadth_fraction` 0.25: `conflict-mergeable-csproj-no-edge.json` (radius_a paths `QuickFiler.Test/QuickFiler.Test.csproj` and `QuickFiler.Test/A.cs`; radius_b paths the same `.csproj` and `QuickFiler.Test/B.cs`; expected `conflict` `false`, `reasons` `[]`) and `conflict-mergeable-glob-still-contends.json` (radius_a paths `["Proj/**"]`; radius_b paths `["Proj/*.csproj"]`; expected `conflict` `true` with one reason `{"kind": "path_overlap", "detail": "Proj/** ~ Proj/*.csproj"}`). Acceptance: `poetry run pytest tests/scripts/dev_tools/test_blast_radius_parity.py -v` prints `test_conflict_fixture_reproduces_the_expected_verdict[conflict-mergeable-csproj-no-edge] PASSED`, `test_conflict_fixture_reproduces_the_expected_reasons[conflict-mergeable-csproj-no-edge] PASSED`, and the same two node IDs for `conflict-mergeable-glob-still-contends`, and `test_discovered_fixture_count_equals_the_json_file_count PASSED`.

- [x] [P3-T7] Create `tests/scripts/claude-lib/blast-radius/BlastRadiusConflict.Tests.ps1` importing `BlastRadiusConflict.psm1` and `BlastRadius.psm1` (path resolution as in `BlastRadius.Conflict.Tests.ps1:25-27`), with these `It` blocks: under `Describe 'Get-ConfigMergeablePath'` — `returns the present entries sorted`, `returns an empty array for an absent key`, `throws for a non-list value`, `throws for a blank entry`; under `Describe 'Test-MergeablePath'` — `matches a concrete csproj against the double-star pattern`, `matches a root-level packages.config for a double-star prefix`, `never treats a glob entry as mergeable` (entry `Proj/*.csproj` is `$false`), `matches an entry equal to a configured pattern`; under `Describe 'Get-NonMergeablePathEntry'` — `drops matching entries and sorts the survivors`, `returns the input content when the mergeable list is empty`; under `Describe 'Relocated overlap helpers'` — `Get-SmallestPathOverlap renders the ordinally smallest pair`, `Get-SmallestCommonEntry returns the smallest shared element`; under `Describe 'Test-BlastRadiusConflict equivalence'` — `returns identical results for an absent key and an empty list` (compare `conflict` and the reason `kind`/`detail` sequence for the `.csproj` pair under a config without the key and a config with an empty list). Acceptance: `wc -l tests/scripts/claude-lib/blast-radius/BlastRadiusConflict.Tests.ps1` reports at most 320, and P3-T9 lists all thirteen names as passed.

- [x] [P3-T8] Before editing the suite, run the C9 PowerShell reset (this is the fourth distinct PowerShell test file of the session: `BlastRadius.KeyPartition.Tests.ps1`, `BlastRadius.TruthTable.Tests.ps1`, and `BlastRadiusConflict.Tests.ps1` already hold the three slots) and append its section to `<FEATURE>/evidence/other/batch-budget-resets.<ISO8601>.md`. Then extend `tests/scripts/claude-lib/blast-radius/BlastRadius.Conflict.Tests.ps1`: reword the comment at lines 29-30 so it states the relation reads only `mergeable_paths` and the minimal mapping omits it deliberately; append a `Context 'Mechanically-mergeable paths (issue #643)'` inside `Describe 'Test-BlastRadiusConflict disjuncts in isolation'` with three `It` blocks — `reports no conflict for a csproj-only overlap` (config carrying the five defaults; `conflict` `$false`; `reasons` count 0), `still reports path overlap for a declared glob entry` (paths `Proj/**` against `Proj/*.csproj`; one reason with detail `Proj/** ~ Proj/*.csproj`), and `keeps the csproj in both radii paths` (the two input hashtables still list the `.csproj` after the call). Acceptance: the reset artifact records the pre-reset `testFiles` array containing those three paths and a post-reset state-file count of `0`; `wc -l tests/scripts/claude-lib/blast-radius/BlastRadius.Conflict.Tests.ps1` reports at most 490; and P3-T9 lists the three names as passed.

- [x] [P3-T9] Run `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-lib/blast-radius, tests/scripts/claude-lib/ClaudeLibModuleConvention.Tests.ps1 -Output Detailed"` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase3-pester-blast-radius.<ISO8601>.md` with the four required fields and the verbatim `Tests Passed:` line. Acceptance: the `Tests Passed:` line reports `Failed: 0`; the detailed output lists as passed `reproduces the expected verdict for conflict-mergeable-csproj-no-edge`, `reproduces the expected verdict for conflict-mergeable-glob-still-contends`, `returns identical results for an absent key and an empty list`, `matches a root-level packages.config for a double-star prefix`, `reports no conflict for a csproj-only overlap`, `lists every discovered library module in core.json paths`, `ships a bundled counterpart for every library module`, and `sets the fail-fast error preference at module scope in every discovered module`.

- [x] [P3-T10] Run `poetry run pytest tests/scripts/dev_tools/test_blast_radius_parity.py tests/scripts/dev_tools/test_blast_radius_mergeable_paths.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase3-python-parity.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and the output contains `test_conflict_fixture_reproduces_the_expected_reasons[conflict-mergeable-glob-still-contends] PASSED`.

- [x] [P3-T11] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_end_to_end.py tests/scripts/dev_tools/test_push_down_claude_pack_selection.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase3-push-down-parity.<ISO8601>.md` with the four required fields and every failing node ID with its message (possibly none). Acceptance: `EXIT_CODE: 0`, or `EXIT_CODE: 1` under the C4 bounded exemption only; in both cases the output contains `test_push_down_no_arguments_publishes_full_tree PASSED` and `test_push_down_claude_repeated_generation_is_deterministic PASSED`.

- [x] [P3-T12] Run `wc -l .claude/lib/blast-radius/BlastRadius.psm1 .claude/lib/blast-radius/BlastRadiusConflict.psm1 extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadius.psm1 extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadiusConflict.psm1 tests/scripts/claude-lib/blast-radius/BlastRadiusConflict.Tests.ps1 tests/scripts/claude-lib/blast-radius/BlastRadius.Conflict.Tests.ps1` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase3-line-counts.<ISO8601>.md` with the four required fields and the six counts. Acceptance: every count is at most 500, and the two mirror counts equal their self-hosted counterparts.

- [x] [P3-T13] Check off `spec.md` S2, S3, S4, S5, S6, S9, and T1, citing P3-T9, P3-T10, and P1-T8. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `12` (the five from earlier phases plus these seven), `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `39`, and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` reports a removed count of `0` and an added count of `253`.

### Phase 4 — TypeScript manifest split, fallback suppression, and key carriage

- [x] [P4-T1] Create `extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-manifests.ts` (pure, no I/O, no clock) holding, moved unchanged together with their doc comments from `claude-blast-radius-derive-core.ts`: the `DirectoryObservation` interface (lines 44-61), `MANIFEST_FILENAMES` (67-76), `EXCLUDED_DIR_NAMES` (97-113), `compareOrdinal` (245-247, now exported), `isExcludedDirectoryName` (255-257), and `isManifestFileName` (266-273); plus the new exports `MODULE_MANIFEST_SUFFIXES: ReadonlyArray<string> = []`, `NON_MODULE_MANIFEST_SUFFIXES: ReadonlyArray<string> = [".csproj", ".fsproj", ".vbproj", ".sln", ".slnx"]`, `MANIFEST_SUFFIXES` as the readonly concatenation of the two families (so `isManifestFileName` keeps its behaviour; the existing five-member declaration at `claude-blast-radius-derive-core.ts:78-85` is superseded by this concatenation and is deleted from the core in P4-T2), `isModuleManifestFileName`, `isNonModuleManifestFileName`, the interface `ProjectDirectoryClassification { readonly modulePaths: ReadonlyArray<string>; readonly structureObserved: boolean }`, and `classifyProjectDirectories(observations): ProjectDirectoryClassification` (root skipped as before; a directory carrying an exact manifest name or a module-family suffix joins `modulePaths`; a directory carrying only a non-module-family suffix sets `structureObserved` and contributes no path). Each export carries a doc comment; the file header records that `.sln`/`.slnx` join the non-module family because ancestor pruning no longer removes a nested solution directory once project directories stop being modules. Acceptance: after P4-T2, `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run typecheck; $code = $LASTEXITCODE; Pop-Location; exit $code'` (from the worktree root) exits 0 with no `error TS` line, and `wc -l extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-manifests.ts` reports at most 200.

- [x] [P4-T2] Edit `extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts`. Apply the edits in descending line order (the output literal at line 462, then 449-451, then 430-434, then 275-297, then 266-273, then 255-257, then 245-247, then 169-175, then 87-113, then 78-85, then 67-76, then 44-61, then the import region and the module header), so no edit invalidates a later anchor: insert `mergeable_paths: source[CARRIED_KEYS[5]],` into the output literal immediately after the `mandate_reads: source[CARRIED_KEYS[4]],` line (line 462) and before the `modules` property; in `deriveDestinationModuleMap` replace lines 449-451 with `const classification = classifyProjectDirectories(observations); const modulePaths = pruneAncestors(classification.modulePaths); const derivedPaths = modulePaths.length > 0 ? modulePaths : classification.structureObserved ? [] : topLevelDirectories(observations);` with an intent comment stating that an observed .NET manifest is a structure signal that suppresses the top-level fallback; update the `@returns` doc (lines 430-434) to name the new key order and the suppression; delete the existing local `classifyProjectDirectories` function together with its doc comment (lines 275-297) so that the name is declared exactly once, by the manifests module, before it is re-exported here; delete `isManifestFileName` (266-273), `isExcludedDirectoryName` (255-257), and `compareOrdinal` (245-247) with their doc comments; append `"mergeable_paths"` to `CARRIED_KEYS` (lines 169-175) as index 5 and update its comment; delete `EXCLUDED_DIR_NAMES` with its comment (87-113), the five-member `MANIFEST_SUFFIXES` with its comment (78-85), `MANIFEST_FILENAMES` (67-76), and the `DirectoryObservation` interface with its doc comment (44-61); then, in the import region, add `import { classifyProjectDirectories, compareOrdinal } from "./claude-blast-radius-derive-manifests";` for local use and `export { MANIFEST_FILENAMES, MANIFEST_SUFFIXES, MODULE_MANIFEST_SUFFIXES, NON_MODULE_MANIFEST_SUFFIXES, EXCLUDED_DIR_NAMES, isExcludedDirectoryName, isManifestFileName, classifyProjectDirectories } from "./claude-blast-radius-derive-manifests";` plus `export type { DirectoryObservation, ProjectDirectoryClassification } from "./claude-blast-radius-derive-manifests";`; and update the module header to name the new key order and the suppression. `assembleModules`, `PAYLOAD_MODULES`, `assertNoForbiddenGlob`, and `FORBIDDEN_GLOBS` are not modified. Acceptance: `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run typecheck; $code = $LASTEXITCODE; Pop-Location; exit $code'` prints no `error TS` line and exits 0; `git grep -c -F "mergeable_paths" -- extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts` reports a count of at least 3 for that path; `git grep -c -F "function classifyProjectDirectories" -- extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts` reports no match (exit 1); and `wc -l extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts` reports at most 420.

- [x] [P4-T3] Add a `coverageThreshold` entry for `"./src/lib/push-down/claude-blast-radius-derive-manifests.ts"` with `lines: 85` and `branches: 75` to `extensions/drm-copilot/jest.config.cjs`, immediately after the existing `"./src/lib/push-down/claude-blast-radius-derive-core.ts"` entry (lines 206-209 on the current tree) and before the issue #596 comment at line 210, preceded by a comment naming issue #643 and stating that the map carries no `global` key so a new production file without its own entry is ungated (the comment names the issue and does not repeat the file name). Acceptance: `git grep -c -F "claude-blast-radius-derive-manifests.ts" -- extensions/drm-copilot/jest.config.cjs` reports a count of 1 for that path, and `wc -l extensions/drm-copilot/jest.config.cjs` reports at most 300 (267 today).

- [x] [P4-T4] Rewrite two cases in `extensions/drm-copilot/test/lib/push-down/blast-radius-derive-core.test.ts`: lines 118-124 become `it("treats a suffix-matched .NET manifest as structure rather than a module", ...)` asserting `Object.keys(deriveModules([observe(""), observe("Widget", "Widget.csproj")]))` equals `["config"]`; lines 193-207 become `it("derives both projects of a two-module Go layout", ...)` with `observe("Foo", "go.mod")` and `observe("Foo.Tests", "go.mod")` and the same two expectations. Extend the comment at lines 281-283 so it names `mergeable_paths` as a second optional key that is absent from `SOURCE_DOCUMENT` and therefore dropped, with its presence case pinned in `blast-radius-derive-mergeable.test.ts`. The `it.each([...MANIFEST_SUFFIXES])` case at lines 92-99 is left unchanged (every suffix still classifies as a manifest). Acceptance: `wc -l extensions/drm-copilot/test/lib/push-down/blast-radius-derive-core.test.ts` reports at most 490, and `git grep -c -F "Widget.csproj" -- extensions/drm-copilot/test/lib/push-down/blast-radius-derive-core.test.ts` reports a count of 1 for that path.

- [x] [P4-T5] Rewrite the two `App.csproj` layouts in `extensions/drm-copilot/test/lib/push-down/blast-radius-derive.test.ts` (line 193 and line 398) to `entry("go.mod", false)`, and adjust the adjacent comments (lines 188 and 391-393) to say a Go module rather than a project file; the `src/App` expectations are unchanged. Acceptance: `git grep -c -F "App.csproj" -- extensions/drm-copilot/test/lib/push-down/blast-radius-derive.test.ts` reports no match (exit 1), and `wc -l extensions/drm-copilot/test/lib/push-down/blast-radius-derive.test.ts` reports at most 480.

- [x] [P4-T6] Edit `extensions/drm-copilot/test/lib/push-down/config-carriage.test-helpers.ts`: in `SOURCE_BLAST_RADIUS` (lines 86-118) insert `mergeable_paths` with the five default entries, in the P1-T1 order, between `mandate_reads` and `modules`; extend the doc comment at lines 79-85 with one sentence stating that `mergeable_paths` carries the five-entry mechanically-mergeable path class; change `SRC_APP_LAYOUT` (lines 222-228) so `src/App` carries `{ name: "go.mod", isDir: false }` and reword its doc comment (216-221) to say one Go module. The `mergeable_paths` insertion is what keeps `claude-config-carriage.test.ts` `keeps SOURCE_BLAST_RADIUS in step with the committed bundled blast-radius resource` (lines 110-134; its assertion `expect(fixture).toEqual(committed)` is at line 133) green after P1-T2 changed the committed bundled copy: that test parses the bundled file and the fixture and compares them structurally, so the fixture must carry the same five entries in the same order. Acceptance: `git grep -c -F "App.csproj" -- extensions/drm-copilot/test/lib/push-down/config-carriage.test-helpers.ts` reports no match (exit 1), `git grep -c -F "mergeable_paths" -- extensions/drm-copilot/test/lib/push-down/config-carriage.test-helpers.ts` reports a count of at least 1 for that path, and `wc -l extensions/drm-copilot/test/lib/push-down/config-carriage.test-helpers.ts` reports at most 245.

- [x] [P4-T7] Reword the arrange comment at `extensions/drm-copilot/test/lib/push-down/claude-config-carriage.test.ts:295-297` so it says the destination carries one Go module rather than one C# project; the `"src/App"` and `"src/App/**"` assertions at 323-324 and 378 are unchanged. Acceptance: `git grep -c -F "one C# project" -- extensions/drm-copilot/test/lib/push-down/claude-config-carriage.test.ts` reports no match (exit 1), and `wc -l extensions/drm-copilot/test/lib/push-down/claude-config-carriage.test.ts` reports at most 470.

- [x] [P4-T8] Create `extensions/drm-copilot/test/lib/push-down/blast-radius-derive-manifests.test.ts` importing from `../../../src/lib/push-down/claude-blast-radius-derive-manifests` and from `../../../src/lib/push-down/claude-blast-radius-derive-core`, with the `observe` and `deriveModules` helpers copied from `blast-radius-derive-core.test.ts:54-81` (`observe` with its doc comment at 54-66, `deriveModules` at 68-81), and these cases: `splits the manifest suffixes into an empty module family and the five-member non-module family` (`MODULE_MANIFEST_SUFFIXES` is `[]`; `NON_MODULE_MANIFEST_SUFFIXES` equals the five; `MANIFEST_SUFFIXES` equals their concatenation); `classifies every non-module suffix as a manifest but not as a module manifest`; `returns no module path and a structure signal for a nine-project .NET layout` (nine sibling observations `Proj1`..`Proj9`, each carrying `ProjN.csproj`, plus the root; `classifyProjectDirectories` returns `modulePaths` `[]` and `structureObserved` `true`); `derives exactly the config payload module for a nine-project .NET layout` (`deriveModules` equals `{ config: ["config/**"] }`); `yields no module for a nested solution file` (`observe("src", "All.sln")` plus `observe("src/App", "App.csproj")` derives `{ config: ["config/**"] }`); `keeps the non-.NET module beside config in a mixed layout` (`observe("tools", "package.json")` plus two `.csproj` directories derives keys `["config", "tools"]`); `re-exports the manifest surface from the derivation core` (the core's `isManifestFileName` and `classifyProjectDirectories` are the same function objects as the manifests module's). Acceptance: `wc -l extensions/drm-copilot/test/lib/push-down/blast-radius-derive-manifests.test.ts` reports at most 220, and P4-T11 reports the file as a passed suite.

- [x] [P4-T9] Create `extensions/drm-copilot/test/lib/push-down/blast-radius-derive-mergeable.test.ts`, modelled on `blast-radius-derive.test.ts:421-472` (in-memory file system, `decorate`, `fakeLister({})`, a `SOURCE_DOCUMENT_WITH_MERGEABLE_PATHS` constant carrying `mandate_reads` and the five default `mergeable_paths`), with three cases: `carries mergeable_paths into the destination document verbatim`, `emits mergeable_paths between mandate_reads and modules` (key order `version`, `shared_surfaces`, `shared_surface_globs`, `mandate_reads`, `mergeable_paths`, `modules`, `over_breadth_fraction`), and `omits mergeable_paths when the source document declares none` (`not.toHaveProperty("mergeable_paths")`). Acceptance: `wc -l extensions/drm-copilot/test/lib/push-down/blast-radius-derive-mergeable.test.ts` reports at most 150, and P4-T11 reports the file as a passed suite.

- [x] [P4-T10] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run lint; $code = $LASTEXITCODE; Pop-Location; exit $code'` and `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run typecheck; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase4-typescript-lint-typecheck.<ISO8601>.md` with the four required fields for each command. Acceptance: both exit 0; ESLint prints no line containing `problems`; `tsc` prints no line containing `error TS`.

- [x] [P4-T11] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run test:unit; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase4-typescript-unit.<ISO8601>.md` with the four required fields, the verbatim `Test Suites:` and `Tests:` lines, and the `PASS` lines for `blast-radius-derive-manifests.test.ts`, `blast-radius-derive-mergeable.test.ts`, `blast-radius-derive-core.test.ts`, `blast-radius-derive.test.ts`, and `claude-config-carriage.test.ts`. Acceptance: `EXIT_CODE: 0`, the `Tests:` line contains no `failed`, and the passed test count is at least the P0-T11 count plus 10.

- [x] [P4-T12] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run test:coverage -- --coverageReporters=text; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Jest exits non-zero when any `coverageThreshold` entry is not met, so this run carries the P4-T3 gate for the new production file. Artifact: `<FEATURE>/evidence/qa-gates/phase4-typescript-scoped-coverage.<ISO8601>.md` with the four required fields and the verbatim rows for `claude-blast-radius-derive-manifests.ts` and `claude-blast-radius-derive-core.ts`. Acceptance: `EXIT_CODE: 0`; both rows show `% Lines` at or above 85 and `% Branch` at or above 75.

- [x] [P4-T13] Check off `spec.md` S11, S14, S15, S16, S17, T2, T4 and `user-story.md` U4, citing P4-T11 and P4-T12, and P1-T9/P1-T10 for T4. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `19` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `32`; `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `2` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `9`; and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` reports a removed count of `0` for both files with added counts of `253` and `70`.

### Phase 5 — Project-file merge library, entry script, fixtures, suites, publication, and orchestrator procedure

Directory conventions for this phase: production files under `.claude/lib/project-file-merge/`; Pester suites under `tests/scripts/claude-lib/project-file-merge/`; text fixtures under `tests/fixtures/project_file_merge/` named `<case>.conflicted.<ext>` (the worktree file as git leaves it after `git merge --no-commit origin/main`), `<case>.expected.<ext>` (the merged result), and for script-level cases `<case>.ours.<ext>`, `<case>.theirs.<ext>`, `<case>.base.<ext>` (the `:2:`, `:3:`, `:1:` stage texts). Fixtures are raw text and exempt from the 500-line cap; every fixture is kept under 60 lines. No test writes a file: `Write-MergedFile` is mocked in every suite that reaches it, except for the single `-WhatIf` invocation in the `Byte-level seams` block of P5-T10, which executes the real function up to its `ShouldProcess` guard and writes nothing; `Invoke-GitExe` is mocked rather than git executed, per `.claude/rules/powershell.md`.

- [x] [P5-T1] Create `.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1` (pure; header ends with the convention sentence; strict mode and the error-preference guard as in P3-T1; no sibling imports) exporting: `Get-ProjectFileKind -Path` (leaf name `packages.config` yields `packages`, leaf name `app.config` yields `appconfig`, extension `.csproj`, `.vbproj`, or `.props` yields `msbuild`, anything else yields `$null`); `Get-MergeableUnit -Line <string[]> -Kind` (parses a whole side of a hunk into an ordered array of units with `Key`, `Version`, `Lines`, and `Attributes`, or returns `$null` when any line is outside the grammar: for `msbuild` the single-line form `<Compile|Analyzer|None|Content|EmbeddedResource Include="..." />` and the paired open/close form whose only children are `DependentUpon`, `SubType`, `AutoGen`, `DesignTime`, `Link`, `CopyToOutputDirectory`, `Generator`, `LastGenOutput`, keyed `<ItemType>:<Include>`; for `packages` the `<package id="..." version="..." ... />` line keyed `package:<id>` with `Version` from the `version` attribute; for `appconfig` the `<dependentAssembly>` ... `</dependentAssembly>` block keyed `bindingRedirect:<assemblyIdentity name>` with `Version` from `newVersion`; blank and whitespace-only lines are skipped); and `Compare-UnitVersion -Ours -Theirs` returning `ours`, `theirs`, or `equal` when both parse as `[System.Version]`, and `escalate` otherwise. Every function has comment-based help and every loop and branch an intent comment. Acceptance: `pwsh -NoProfile -Command "Import-Module ./.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1 -Force; (Get-Command -Module ProjectFileMergeGrammar).Name -join ','"` prints `Compare-UnitVersion,Get-MergeableUnit,Get-ProjectFileKind` in any order, and `wc -l .claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1` reports at most 320.

- [x] [P5-T2] Create `.claude/lib/project-file-merge/ProjectFileMerge.psm1` (pure; header, strict mode, guard as above; one column-0 `Import-Module` of `ProjectFileMergeGrammar.psm1` with `-Force -ErrorAction Stop`) exporting: `Get-ConflictHunk -Line <string[]>` (returns the hunks of a conflicted text, each with `Start`, `End`, `Ours`, `Theirs`, and `Base` line arrays, accepting the `merge` style `<<<<<<<` / `=======` / `>>>>>>>` and the `diff3`/`zdiff3` style with a `|||||||` base section; an unterminated hunk returns `$null`); `Get-UnitKeySet -Line <string[]> -Kind` (the set of unit keys of a complete file text, skipping non-grammar lines); `Merge-ConflictedText -Line <string[]> -Kind` (for every hunk, parse both sides with `Get-MergeableUnit`; any `$null` side yields `Escalate` `$true` with `EscalateReason`; resolution is ours units in ours order followed by theirs units whose key is absent from ours in theirs order; the same key on both sides with differing `Version` keeps the unit `Compare-UnitVersion` selects and, for `appconfig`, rewrites the kept unit's `oldVersion` upper bound to the chosen `newVersion`; the same key with the same version but differing `Attributes` or child lines escalates; returns an object with `Lines`, `EntriesAddedFromOurs`, `EntriesAddedFromTheirs`, `VersionResolutions` (each `{ key, ours, theirs, chosen }`), `Escalate`, and `EscalateReason`); and `Test-NeverDropPostCondition -MergedLine -OursLine -TheirsLine -BaseLine -Kind` (returns `$true` only when the merged key set equals the union of the ours and theirs key sets and every key present in base and in both sides is present). Lines are treated as opaque strings apart from the grammar match; no XML API is used. Acceptance: `grep -r -c -E "System\.Xml|\[xml\]" .claude/lib/project-file-merge` prints `0` for every file it lists (plain `grep`, because the directory is untracked until the orchestrator commits and `git grep` would not read it), and `wc -l .claude/lib/project-file-merge/ProjectFileMerge.psm1` reports at most 400.

- [x] [P5-T3] Create `.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` with `param([string] $Worktree, [string] $ConfigPath)`, strict mode and the error-preference guard, `$PSScriptRoot`-relative `Import-Module ... -Force -ErrorAction Stop` of `ProjectFileMergeGrammar.psm1`, `ProjectFileMerge.psm1`, and `../blast-radius/BlastRadiusConflict.psm1`, and these functions: `Invoke-GitExe -GitArgs <string[]>` (the only executable seam: `git @GitArgs 2>&1`, throwing on a non-zero `$LASTEXITCODE`); `Read-ConflictedFile -Path` (returns `Bytes`, `HasBom` from the first three bytes, and `Line` split with each line's own terminator retained, decoding as UTF-8; escalates when the bytes are not valid UTF-8); `Write-MergedFile -Path -Line -HasBom` (`[CmdletBinding(SupportsShouldProcess = $true)]`, writes the BOM when `HasBom` and the UTF-8 bytes of the lines joined with their retained terminators through `[System.IO.File]::WriteAllBytes`; `Write-MergedFile` assembles the byte array before the `ShouldProcess` guard and places only the `[System.IO.File]::WriteAllBytes` call inside it, so a `-WhatIf` invocation executes every line but that one, which keeps the file's per-file line coverage above the P8-T12 floor that the blanket mocks of P5-T10 would otherwise put it below); and `Invoke-MergeableConflictResolution -Worktree -ConfigPath` which: reads `mergeable_paths` through `Get-ConfigMergeablePath` from `$ConfigPath` (default `Join-Path $Worktree 'config/blast-radius.json'`); lists conflicted paths with `Invoke-GitExe -GitArgs @('-C', $Worktree, 'diff', '--name-only', '--diff-filter=U')`; classifies each with `Test-MergeablePath`; when any path is non-mergeable returns `@{ result = 'escalate'; resolved = @(); escalate_paths = <those paths> }` without reading or writing any file; otherwise for each path reads the conflicted file, merges with `Merge-ConflictedText`, reads stages with `Invoke-GitExe -GitArgs @('-C', $Worktree, 'show', ":2:$path")`, `":3:$path"`, and `":1:$path"`, checks `Test-NeverDropPostCondition`, and accumulates; any escalation or post-condition failure returns `escalate` with that path in `escalate_paths` and writes nothing; only when every path resolved does it call `Write-MergedFile` for each and return `@{ result = 'resolved'; resolved = <one entry per path with path, entries_added_from_ours, entries_added_from_theirs, version_resolutions>; escalate_paths = @() }`. The entry-point body is guarded with `if ($MyInvocation.InvocationName -ne '.')` (the `scripts/dev-tools/Invoke-ReleaseReconciliation.ps1:162` precedent), throws when `$Worktree` is blank, writes `ConvertTo-Json -InputObject $result -Depth 6 -Compress` as the only stdout line, and exits 0; stderr carries any thrown message. The script never invokes `git add` or `git commit`. Acceptance (plain `grep`, because the file is untracked until the orchestrator commits): `grep -c -F "InvocationName -ne '.'" .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` prints `1`; `grep -c -E "git add|git commit|'add'|'commit'" .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` prints `0`; and `wc -l .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` reports at most 260.

- [x] [P5-T4] Create the ten MSBuild fixture pairs under `tests/fixtures/project_file_merge/`: for each item type in `compile`, `analyzer`, `none`, `content`, `embeddedresource`, a `<type>-single-line.conflicted.csproj` / `.expected.csproj` pair (one `merge`-style hunk in which ours inserts two `Include` lines and theirs inserts two different `Include` lines inside one `<ItemGroup>`) and a `<type>-paired.conflicted.csproj` / `.expected.csproj` pair (the open/close form with `DependentUpon` and `SubType` children). Each expected file lists ours entries in ours order followed by theirs entries in theirs order, with every unconflicted line unchanged. Acceptance: `pwsh -NoProfile -Command "(Get-ChildItem tests/fixtures/project_file_merge -Filter *.csproj | Measure-Object).Count"` prints a value of at least 20, and P5-T19 lists the ten `ProjectFileMerge.Tests.ps1` item-type cases as passed.

- [x] [P5-T5] Create the package and binding-redirect fixtures: `packages-disjoint.conflicted.config` / `.expected.config` (disjoint `id` values; union); `packages-version-differs.conflicted.config` / `.expected.config` (the same `id` at `13.0.1` ours and `13.0.3` theirs; expected keeps `13.0.3`); `packages-unparseable-version.conflicted.config` (the same `id` with `1.0.0-beta1` on one side; no expected file, because it escalates); `appconfig-disjoint.conflicted.config` / `.expected.config` (two `<dependentAssembly>` blocks with different `assemblyIdentity name`; union); `appconfig-newversion-differs.conflicted.config` / `.expected.config` (the same assembly with `newVersion` `4.0.0.0` ours and `4.0.2.0` theirs; expected keeps the theirs block and its `oldVersion` upper bound equals `4.0.2.0`). Acceptance: the nine files exist, and P5-T19 lists the packages and app.config cases as passed.

- [x] [P5-T6] Create the remaining module-level fixtures: `diff3-compile.conflicted.csproj` / `.expected.csproj` (one hunk carrying a `|||||||` base section); `non-grammar-line.conflicted.csproj` (a hunk side containing a `<PropertyGroup>` line; escalates); `same-key-different-attributes.conflicted.csproj` (the same `Compile` `Include` on both sides with differing `DependentUpon` children; escalates); `crlf-compile.conflicted.csproj` / `.expected.csproj` (every line terminated with CRLF); `bom-compile.conflicted.csproj` / `.expected.csproj` (UTF-8 BOM on both). Before creating the CRLF pair, append the line `tests/fixtures/project_file_merge/crlf-*.csproj -text` to the repository-root `.gitattributes` (which on the current tree holds the single line `* text=auto eol=lf`), preceded by a comment naming issue #643 and stating that the single existing rule `* text=auto eol=lf` would otherwise rewrite the fixture's CRLF terminators to LF on commit, which makes the `preserves CRLF terminators` case of P5-T9 pass on an uncommitted worktree and fail on every fresh checkout including CI. The BOM pair needs no exemption because `text=auto` does not strip a byte-order mark. Acceptance: `git check-attr text -- tests/fixtures/project_file_merge/crlf-compile.conflicted.csproj tests/fixtures/project_file_merge/crlf-compile.expected.csproj` reports `text: unset` for both paths (it reports `text: auto` for them on the current tree); `git grep -c -F "crlf-*.csproj -text" -- .gitattributes` reports a count of 1 for that path; `pwsh -NoProfile -Command "[System.IO.File]::ReadAllBytes('tests/fixtures/project_file_merge/bom-compile.conflicted.csproj')[0..2] -join ','"` prints `239,187,191`; and `pwsh -NoProfile -Command 'Set-Variable t ([System.IO.File]::ReadAllText("tests/fixtures/project_file_merge/crlf-compile.conflicted.csproj")); ([regex]::Matches($t, "\r\n")).Count -eq ([regex]::Matches($t, "\n")).Count'` prints `True` (the `-Command` payload is single-quoted so Bash passes `$t` and the regex escapes through unexpanded).

- [x] [P5-T7] Create the script-level fixtures: `script-resolved.conflicted.csproj`, `.ours.csproj`, `.theirs.csproj`, `.base.csproj`, `.expected.csproj` (a two-sided `Compile` insertion whose stage texts agree with the conflicted file); and `script-never-drop.ours.csproj` / `.theirs.csproj` / `.base.csproj` (stage texts in which theirs carries an additional `Compile` entry absent from `script-resolved.conflicted.csproj`, so the post-condition fails against them). Acceptance: the eight files exist, and P5-T19 lists the `Resolve-MergeableConflict.Tests.ps1` never-drop case as passed.

- [x] [P5-T8] Create `tests/scripts/claude-lib/project-file-merge/ProjectFileMergeGrammar.Tests.ps1` with `It` blocks: under `Describe 'Get-ProjectFileKind'` — one case per kind and one for an unrecognised leaf name returning `$null`; under `Describe 'Get-MergeableUnit'` — `parses a single-line <ItemType> item` as a `-ForEach` over the five item types, `parses the paired form with metadata children`, `parses a package line with its version`, `parses a dependentAssembly block keyed on the assembly name`, `returns null for a line outside the grammar`, `skips blank lines`; under `Describe 'Compare-UnitVersion'` — `selects the higher four-part version`, `reports equal versions`, `escalates an unparseable version on either side`. Acceptance: `wc -l tests/scripts/claude-lib/project-file-merge/ProjectFileMergeGrammar.Tests.ps1` reports at most 330, and P5-T19 lists every name above as passed.

- [x] [P5-T9] Create `tests/scripts/claude-lib/project-file-merge/ProjectFileMerge.Tests.ps1` reading fixtures with `Get-Content -Raw` and splitting with terminators retained, with `It` blocks: under `Describe 'Get-ConflictHunk'` — `parses a merge-style hunk`, `parses a diff3-style hunk and skips its base section`, `returns null for an unterminated hunk`; under `Describe 'Merge-ConflictedText keyed union'` — `resolves <case> to the expected union` as a `-ForEach` over the ten MSBuild cases plus `diff3-compile`, `packages-disjoint`, and `appconfig-disjoint` (merged lines equal the expected fixture lines); `keeps the higher package version and records the resolution` (`packages-version-differs`; `VersionResolutions` equals one entry `{ key = 'package:Newtonsoft.Json'; ours = '13.0.1'; theirs = '13.0.3'; chosen = '13.0.3' }` with the fixture's `id`), `keeps the higher newVersion and rewrites the oldVersion upper bound` (`appconfig-newversion-differs`), `escalates an unparseable package version`, `escalates a hunk line outside the grammar`, `escalates the same key at the same version with differing attributes`, `preserves CRLF terminators` (every merged line ends with CRLF), `preserves the byte prefix for a BOM fixture` (the merged lines equal the expected lines and the caller-supplied `HasBom` is carried through unchanged); under `Describe 'Test-NeverDropPostCondition'` — `passes when the merged key set is the union of both sides`, `fails when a theirs key is missing from the merged text`, `fails when a key present in base and both sides is missing`. Acceptance: `wc -l tests/scripts/claude-lib/project-file-merge/ProjectFileMerge.Tests.ps1` reports at most 420, and P5-T19 lists every name above as passed.

- [x] [P5-T10] Before creating the suite, run the C9 PowerShell reset (the reset before P3-T8 cleared both lists; `Resolve-MergeableConflict.Tests.ps1` is the fourth test file since then, after `BlastRadius.Conflict.Tests.ps1`, `ProjectFileMergeGrammar.Tests.ps1`, and `ProjectFileMerge.Tests.ps1`) and append its section to the same artifact. This reset also clears the production list, which is what allows P5-T12 to edit `pester.runsettings.psd1` after P5-T1, P5-T2, and P5-T3 filled it. Then create `tests/scripts/claude-lib/project-file-merge/Resolve-MergeableConflict.Tests.ps1` that dot-sources `.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1` in the file-level `BeforeAll` (the guarded entry point does not run) and holds two `Describe` blocks. The first, `Describe 'Invoke-MergeableConflictResolution'`, registers in its own `BeforeAll` `Mock Invoke-GitExe { param([string[]]$GitArgs) ... }` returning the conflicted-path list for the `diff --name-only --diff-filter=U` shape and the fixture stage texts for the `show :2:`, `:3:`, `:1:` shapes, `Mock Write-MergedFile { param([string]$Path, [string[]]$Line, [bool]$HasBom) }`, and `Mock Read-ConflictedFile` returning the `script-resolved.conflicted.csproj` bytes, then asserts with `-ConfigPath` set to the committed `config/blast-radius.json`: `resolves a csproj-only conflict set and reports the added entries` (`result` `resolved`; `resolved[0].path` is the `.csproj`; `entries_added_from_ours` and `entries_added_from_theirs` are the fixture's two entry keys; `Should -Invoke Write-MergedFile -Times 1 -Exactly`); `escalates when any conflicted path is outside mergeable_paths` (paths `Proj/Proj.csproj` and `Proj/Foo.cs`; `result` `escalate`; `escalate_paths` equals `@('Proj/Foo.cs')`; `Should -Invoke Write-MergedFile -Times 0 -Exactly`; `Should -Invoke Invoke-GitExe -Times 1 -Exactly` proving no stage read); `escalates and writes nothing when the never-drop post-condition fails` (stage mocks return the `script-never-drop` texts); `classifies paths with the shared blast-radius matcher` (a root-level `packages.config` alone is treated as mergeable); `never invokes a staging or commit git command` (every recorded `GitArgs` array contains neither `add` nor `commit`); and `returns exactly the three documented result keys and one JSON call site` (the returned hashtable has exactly the keys `result`, `resolved`, and `escalate_paths`, and the script text read with `Get-Content` contains exactly one `ConvertTo-Json` occurrence; the guarded entry point is not executed by any test, because it would run git). The second, `Describe 'Byte-level seams'`, registers none of the three mocks; its own `BeforeAll` calls the real `Read-ConflictedFile` on `tests/fixtures/project_file_merge/bom-compile.conflicted.csproj` once and stores the result in a `$script:`-scoped variable, because Pester `It` blocks do not share locals; it holds three `It` blocks: `reads the byte-order mark and the retained terminators from a BOM fixture` (on the `BeforeAll` result, `HasBom` is `$true` and every returned line retains its own terminator), `reads a CRLF fixture with its terminators retained` (the real `Read-ConflictedFile` on `tests/fixtures/project_file_merge/crlf-compile.conflicted.csproj`; every line ends with CRLF and `HasBom` is `$false`), and `assembles the merged bytes without writing under -WhatIf` (call the real `Write-MergedFile -Path tests/fixtures/project_file_merge/whatif-never-written.csproj -HasBom $true -WhatIf` with `-Line` set to the `Line` array returned by the real `Read-ConflictedFile` call on `tests/fixtures/project_file_merge/bom-compile.conflicted.csproj` made in this `Describe`'s `BeforeAll`, then assert `Test-Path tests/fixtures/project_file_merge/whatif-never-written.csproj` is `$false`). All three read committed fixtures only and create no file. Acceptance: the reset artifact records the pre-reset `prodFiles` and `testFiles` arrays and a post-reset state-file count of `0`; `wc -l tests/scripts/claude-lib/project-file-merge/Resolve-MergeableConflict.Tests.ps1` reports at most 320; and P5-T19 lists the nine names as passed.

- [x] [P5-T11] Create `tests/scripts/claude-lib/project-file-merge/ProjectFileMerge.Manifest.Tests.ps1` modelled on `tests/scripts/claude-lib/blast-radius/BlastRadius.Manifest.Tests.ps1`, discovering every `*.psm1` and `*.ps1` under `.claude/lib/project-file-merge` from disk, asserting each is listed exactly once in `core.json` `paths` and has a bundled counterpart under `extensions/drm-copilot/resources/claude-customizations/`, and additionally asserting each bundled counterpart's SHA-256 equals the self-hosted file's. Acceptance: `wc -l tests/scripts/claude-lib/project-file-merge/ProjectFileMerge.Manifest.Tests.ps1` reports at most 110, and P5-T19 lists `lists every discovered library file in core.json paths` and `ships a byte-identical bundled counterpart for every library file` as passed.

- [x] [P5-T12] Append `'.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1'`, `'.claude/lib/project-file-merge/ProjectFileMerge.psm1'`, and `'.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1'` to `CodeCoverage.Path` in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` after the entry added in P3-T3, preceded by a comment naming issue #643 and stating that the entry script's Pester suite dot-sources it behind a guarded entry-point body so line attribution is valid. Acceptance: `git grep -c -F "project-file-merge" -- scripts/powershell/PoshQC/settings/pester.runsettings.psd1` reports a count of 3 for that path (the comment names the issue and does not repeat the directory name), and the `Import-PowerShellDataFile` probe of P3-T3 prints `parsed`.

- [x] [P5-T13] Insert the three paths `".claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1"`, `".claude/lib/project-file-merge/ProjectFileMerge.psm1"`, and `".claude/lib/project-file-merge/Resolve-MergeableConflict.ps1"` into `core.json` `paths` immediately after the `BlastRadiusConflict.psm1` entry added in P3-T4. Acceptance: `poetry run python -c "import json; p=json.load(open('extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json'))['paths']; print([p.count(x) for x in ('.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1', '.claude/lib/project-file-merge/ProjectFileMerge.psm1', '.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1')], len(p)==len(set(p)))"` prints `[1, 1, 1] True`.

- [x] [P5-T14] Copy the three files byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/` (a directory absent from the current tree) with `pwsh -NoProfile -Command "New-Item -ItemType Directory -Path extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge -Force | Out-Null; Copy-Item -LiteralPath .claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1, .claude/lib/project-file-merge/ProjectFileMerge.psm1, .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1 -Destination extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge -Force"` from the worktree root, not with the Write or Edit tool (constraint C9: after the P5-T10 reset the production list holds only `pester.runsettings.psd1` from P5-T12, so Writes to the three mirror paths would reach a fourth distinct production PowerShell file at the third mirror and be denied). Acceptance: `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 .claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1, .claude/lib/project-file-merge/ProjectFileMerge.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/ProjectFileMerge.psm1, .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1).Hash"` prints six lines forming three equal adjacent pairs (lines 1 and 2, 3 and 4, 5 and 6), recorded in the task output.

- [x] [P5-T15] Add four entries to the `tools:` list of `.claude/agents/parallel-orchestrator.md` immediately after line 20 (`Bash(bash .claude/lib/bash/validate-parallel-manifest.sh*)`): `"Bash(pwsh -NoProfile -File .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1*)"`, `"Bash(dotnet tool restore*)"`, `"Bash(dotnet csharpier check *)"`, `"Bash(dotnet build *)"`; and, inside the existing `## Checkpoint Persistence` section (no new `##` heading), immediately after the paragraph that ends `artifact_type: "parallel-orchestrator-state"`. (the paragraph at lines 223-229 on the current tree, which this task's own four-line `tools:` insertion shifts to 227-233, and which carries the sentence `You consume that schema and add no field to it.` that P6-T6 later narrows; the content anchor governs), add one paragraph stating that the parent runs the project-file merge step of the `parallel-orchestrate` skill itself, that the four grants are scoped to that step, and that `mergeable_conflicts_resolved` is the one optional item field the rule file declares, with the token `mergeable_conflicts_resolved` appearing on exactly one line of that paragraph. Copy the file byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md`. Acceptance: `git grep -c -F "Bash(dotnet build *)" -- .claude/agents/parallel-orchestrator.md extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md` reports a count of 1 for each of the two paths, the same for `Bash(pwsh -NoProfile -File .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1*)`, `Bash(dotnet tool restore*)`, and `Bash(dotnet csharpier check *)`; `git grep -c -E "^## " -- .claude/agents/parallel-orchestrator.md` reports a count of 9 for that path; and the two files' SHA-256 hashes are equal.

- [x] [P5-T16] Edit `## Per-Item Merge-Conflict Handling` in `.claude/skills/parallel-orchestrate/SKILL.md` (lines 341-380): keep the intro paragraph and renumber nothing; insert, before the existing step 1, a block headed by the sentence `Project-file conflicts are resolved by the parent first (issue #643); the numbered steps below are the escalation path.` followed by four sub-steps stating verbatim: (a) on a conflicted `gh pr merge --merge` the parent runs `git -C <worktree_path> fetch origin main` and `git -C <worktree_path> merge --no-commit origin/main`; (b) the parent runs `pwsh -NoProfile -File .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1 -Worktree <worktree_path>` and parses its single JSON object; a `result` of `escalate` makes the parent run `git -C <worktree_path> merge --abort` and continue with step 1 below, including `escalate_paths` in the Blocking finding; (c) on `resolved` the parent stages and commits the resolved paths with a message body listing every entry added from each side and every version choice, then runs `dotnet tool restore --tool-manifest <worktree_path>/.config/dotnet-tools.json`, `dotnet csharpier check <worktree_path>`, and `dotnet build <worktree_path>/<solution>` with path arguments rather than `cd`; a failing check reverts with `git -C <worktree_path> reset --hard HEAD~1` and escalates with the tool output as the finding; (d) on success the parent pushes the item branch, records `mergeable_conflicts_resolved` on the item, sets `merge_status: pr_open`, regenerates `parallel-status.md`, and re-enters `## Per-Item Merge to Main (Merge-on-Green)` at the durable `gh pr checks` confirmation step. The existing steps 1-5 and the `Boundary with F8` paragraph are retained verbatim. Copy the file byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md`. Acceptance: `git grep -c -F "dotnet csharpier check" -- .claude/skills/parallel-orchestrate/SKILL.md` reports a count of at least 1 for that path, the same for `reset --hard HEAD~1`, `merge --abort`, `escalate_paths`, and `Resolve-MergeableConflict.ps1`; `git grep -c -F "Boundary with F8" -- .claude/skills/parallel-orchestrate/SKILL.md` reports a count of 1 for that path; the two files' SHA-256 hashes are equal.

- [x] [P5-T17] Edit `## Documentation Maintenance Boundaries` (lines 406-441) of the same skill: after the paragraph that begins `Read-only projections of F3-owned arrays` add one paragraph, without using the phrase `section \`## Mergeable Conflicts Resolved\``, stating that the `## Mergeable Conflicts Resolved` heading of the projection renders `items[].mergeable_conflicts_resolved` (path, resolved_at, merged_against, merge_commit_sha, entries added from each side, version resolutions), that the same resolution is cited in the resolution commit message body, in the pull-request body, and in an evidence artifact at `docs/features/parallel/<slug>/evidence/other/mergeable-conflicts.<yyyy-MM-ddTHH-mm>.md` carrying `Timestamp`, `Command`, `EXIT_CODE`, and the script's JSON output, and add `Every append to an item's \`mergeable_conflicts_resolved\`` as a new bullet of the regeneration-boundaries list. Do not alter the `Header block fields:` sentence or the `Cohort table:` sentence. Copy the file to the mirror. Acceptance: `git grep -c -F "## Mergeable Conflicts Resolved" -- .claude/skills/parallel-orchestrate/SKILL.md` reports a count of at least 1 for that path, `git grep -c -F "mergeable-conflicts." -- .claude/skills/parallel-orchestrate/SKILL.md` reports a count of at least 1 for that path, `git grep -c -E 'section `## Mergeable' -- .claude/skills/parallel-orchestrate/SKILL.md` reports no match (exit 1; the pattern is single-quoted because it carries a backtick), and the mirror hash equals the source hash.

- [x] [P5-T18] Append a `## Mergeable Conflicts Resolved` section to `docs/features/templates/parallel/parallel-status.md` after `## Drift Events`, with one sentence stating it is a projection of `items[].mergeable_conflicts_resolved` written by `parallel-orchestrator` after a parent-side project-file resolution and rendered empty when no item carries the field, and a table with header cells `issue_num`, `path`, `resolved_at`, `merged_against`, `merge_commit_sha`, `entries_added_from_ours`, `entries_added_from_theirs`, `version_resolutions` and one placeholder row. Acceptance: `git grep -c -F "## Mergeable Conflicts Resolved" -- docs/features/templates/parallel/parallel-status.md` reports a count of 1 for that path, and `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -v` prints `test_seam_status_template_realises_projections_prescribed_by_skill PASSED` and `test_status_template_begins_with_generated_file_banner PASSED`.

- [x] [P5-T19] Run `pwsh -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-lib/project-file-merge, tests/scripts/claude-lib/ClaudeLibModuleConvention.Tests.ps1 -Output Detailed"` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase5-pester-project-file-merge.<ISO8601>.md` with the four required fields, the verbatim `Tests Passed:` line, and the count of `It` results per suite file. Acceptance: the `Tests Passed:` line reports `Failed: 0`, the detailed output lists as passed every name enumerated in P5-T8, P5-T9, P5-T10, and P5-T11, and `grep -r -c -E "System\.Xml|\[xml\]" .claude/lib/project-file-merge` prints `0` for every file it lists (plain `grep`; the directory is untracked at this point).

- [x] [P5-T20] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py tests/scripts/dev_tools/test_parallel_orchestrator_permission_contracts.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_end_to_end.py -v` from the worktree root (`test_parallel_orchestrator_permission_contracts.py` binds the skill's prescribed commands to the agent's grants, and both change in this phase). Artifact: `<FEATURE>/evidence/qa-gates/phase5-surface-and-push-down.<ISO8601>.md` with the four required fields and every failing node ID with its message (possibly none). Acceptance: `EXIT_CODE: 0`, or `EXIT_CODE: 1` under the C4 bounded exemption only; the output contains `test_agent_body_contains_exactly_the_nine_required_headings PASSED`, `test_orchestrate_skill_first_thirteen_headings_match_required_layout PASSED`, `test_orchestrate_skill_section_states_its_required_obligations[merge-conflict-exhaustion-and-f8-handoff] PASSED`, `test_agent_tools_allowlist_excludes_pr_author_channel PASSED`, and `test_push_down_no_arguments_publishes_full_tree PASSED`.

- [x] [P5-T21] Run `wc -l` over the three production files, their three mirrors, the four Pester suites of this phase, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Artifact: `<FEATURE>/evidence/qa-gates/phase5-line-counts.<ISO8601>.md` with the four required fields and the eleven counts. Acceptance: every count is at most 500, the `tests/scripts/claude-lib/project-file-merge/Resolve-MergeableConflict.Tests.ps1` count is at most 320 (the P5-T10 bound), and each mirror count equals its self-hosted count.

- [x] [P5-T22] Check off `spec.md` S18 through S30, S33, and T3, and `user-story.md` U5, U6, U7, U8, U9, and U10, citing P5-T19 and P5-T20. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `34` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `17`; `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `8` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `3`; and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` reports a removed count of `0` for both files with added counts of `253` and `70`.

### Phase 6 — Rule-file doctrine, planner-side prose, and the narrowed "add no field" sentences

- [ ] [P6-T1] Insert a subsection headed `### Mechanically-mergeable path class (issue #643)` into `.claude/rules/parallel-orchestration.md` immediately after the `### Read-by-mandate classification` subsection (that is, before line 263 `### Placeholder-shape rejection (issue #502)`), stating: that `config/blast-radius.json` carries an optional `mergeable_paths` list of exact entries and `**` globs whose published default is the five-entry list; that a matching path stays in the declared `paths` and the exclusion is applied at exactly one place per runtime, inside `conflicts` and `Test-BlastRadiusConflict`, immediately before the path-overlap computation, so derivation, normalization, observed-radius construction, V1/V2/V3, and `detect_escaped_paths` are unchanged; that the key is optional and fail-closed; the matcher rule (exact equality, glob containment for concrete entries only, and the local `**/` root-level rule with the note that `_glob_to_regex_text` and its PowerShell mirror are unchanged and must be re-examined together if either changes); and the three constraints, numbered as in the mandate-read subsection: the planner remains obliged to enumerate a genuine write explicitly; the path stays in the declared radius so the audits still see it; `detect_escaped_paths` remains the backstop at execution time. Acceptance: `git grep -c -F "### Mechanically-mergeable path class (issue #643)" -- .claude/rules/parallel-orchestration.md` reports a count of 1 for that path, and `git grep -c -F "detect_escaped_paths" -- .claude/rules/parallel-orchestration.md` reports a count of at least 3 for that path.

- [ ] [P6-T2] Rewrite the key-partition sentence of `.claude/rules/parallel-orchestration.md` that begins `Only \`version\`, \`over_breadth_fraction\`, and \`mandate_reads\` are byte-equal` (inside `### The published truth table is not a copy of this one (issue #500)`; lines 369-370 on the current tree, shifted by the P6-T1 insertion, so the sentence text is the anchor) so it reads, with the four names on one line, `Only \`version\`, \`over_breadth_fraction\`, \`mandate_reads\`, and \`mergeable_paths\`` followed on the next line by `are byte-equal across the two copies.` Acceptance: `git grep -c -F '`version`, `over_breadth_fraction`, `mandate_reads`, and `mergeable_paths`' -- .claude/rules/parallel-orchestration.md` reports a count of 1 for that path, and `git grep -c -F 'and `mandate_reads` are byte-equal' -- .claude/rules/parallel-orchestration.md` reports no match (exit 1); both patterns are single-quoted because they carry backticks. (Verified baseline: the second fragment matches line 369 today and the first matches nothing.)

- [ ] [P6-T3] Add one paragraph to the `### The published truth table is not a copy of this one (issue #500)` subsection, immediately after the paragraph beginning `**A destination's module map is DERIVED` and ending `helper raises on an absent key. Nothing schedules on it.` (line 354 on the current tree, shifted by the P6-T1 insertion, so the paragraph text is the anchor), stating that the .NET manifest family (`.csproj`, `.fsproj`, `.vbproj`, `.sln`, `.slnx`) is a structure signal in `claude-blast-radius-derive-manifests.ts`: it marks a project root, contributes no module, and suppresses the top-level-directory fallback, so a .NET solution layout derives exactly `{ "config": ["config/**"] }`, because a per-assembly module matches the majority of work items and belongs nowhere under the granularity criterion (issue #643). Acceptance: `git grep -c -F "claude-blast-radius-derive-manifests.ts" -- .claude/rules/parallel-orchestration.md` reports a count of 1 for that path.

- [ ] [P6-T4] Add one paragraph at the end of the `### Mechanically-mergeable path class (issue #643)` subsection declaring that the parallel-orchestrator checkpoint item record may carry an optional `mergeable_conflicts_resolved` list, that the field is additive and tolerated-not-validated (no validator in Python, TypeScript, or bash reads it, following the `expected_conflict_components` diagnostic precedent), that its entries carry `path`, `resolved_at`, `merged_against`, `merge_commit_sha`, `entries_added_from_ours`, `entries_added_from_theirs`, and `version_resolutions` with entry keys `<ItemType>:<Include>`, `package:<id>`, and `bindingRedirect:<name>`, and that invariant 9 (the six `blast_radius` keys) and invariant 15 (the four edge reasons) are unchanged. Acceptance: `git grep -c -F "mergeable_conflicts_resolved" -- .claude/rules/parallel-orchestration.md` reports a count of at least 1 for that path, and `git diff c3ffb080 -- .claude/rules/parallel-orchestration.md` contains no removed line beginning `9. **Blast-radius shape.**` and no removed line beginning `15. **Conflict-edge shape.**`.

- [ ] [P6-T5] Copy `.claude/rules/parallel-orchestration.md` byte-for-byte to `extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md`. Acceptance: the two SHA-256 hashes are equal, and `git grep -c -F "### Mechanically-mergeable path class (issue #643)" -- extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md` reports a count of 1 for that path.

- [ ] [P6-T6] Narrow the two "add no field" sentences; both anchors are sentence text, because P5-T15 shifts the agent section by four lines and P5-T16/P5-T17 shift the skill section, and both sentences wrap across two lines on the current tree (agent 225-226; skill 447-448), so a search for either original sentence already returns nothing and is not an acceptance condition. In `.claude/agents/parallel-orchestrator.md`, inside `## Checkpoint Persistence`, replace the wrapped sentence `You consume that schema and add no field to it.` so the replacement carries the fragment `add no field to it that the rule file does not declare` on a single unwrapped line, followed by `` `mergeable_conflicts_resolved` is the one declared optional item field. `` with the token `mergeable_conflicts_resolved` on one line. In `.claude/skills/parallel-orchestrate/SKILL.md`, inside `## Parallel-Level Checkpoint`, replace the wrapped sentence `Consume that schema; add no field to it and extend no enum in it.` so the replacement carries the fragment `add no field to it that` on a single unwrapped line, names `` `.claude/rules/parallel-orchestration.md` `` as the declaring file and `mergeable_conflicts_resolved` as the one declared optional item field, and retains the words `extend no enum in it`. Copy both files to their mirrors. Acceptance: `git grep -c -F "add no field to it that the rule file does not declare" -- .claude/agents/parallel-orchestrator.md` reports a count of 1 for that path; `git grep -c -F "add no field to it that" -- .claude/skills/parallel-orchestrate/SKILL.md` reports a count of 1 for that path; `git grep -c -F "mergeable_conflicts_resolved" -- .claude/agents/parallel-orchestrator.md .claude/skills/parallel-orchestrate/SKILL.md` reports a count of exactly 2 for the agent path (the P5-T15 paragraph line and this sentence) and at least 4 for the skill path (the P5-T16 sub-step, the P5-T17 paragraph and bullet, and this sentence); and both mirror SHA-256 hashes equal their source hashes.

- [ ] [P6-T7] Add one sentence to each of the three procedural consumers stating that `Test-BlastRadiusConflict` now reads `mergeable_paths` from the truth table and contributes no `path_overlap` edge for a path matching it while the path stays in the declared radius: in `.claude/skills/parallel-plan/SKILL.md` after the sentence ending at line 197 (`... writes a single object.`) and inside step 1 of `### Seeding procedure` after line 314; in `.claude/skills/parallel-add/SKILL.md` inside step 3 after the sentence at lines 67-69 that names the third argument; in `.claude/agents/parallel-planner.md` after line 161. Copy each file to its mirror under `extensions/drm-copilot/resources/claude-customizations/.claude/`. Each added sentence carries the token `mergeable_paths` on exactly one line. Acceptance: `git grep -c -F "mergeable_paths" -- .claude/skills/parallel-plan/SKILL.md` reports a count of 2 for that path, `git grep -c -F "mergeable_paths" -- .claude/skills/parallel-add/SKILL.md` reports a count of 1 for that path, `git grep -c -F "mergeable_paths" -- .claude/agents/parallel-planner.md` reports a count of 1 for that path, and the three mirror hashes equal their source hashes.

- [ ] [P6-T8] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts.py tests/scripts/dev_tools/test_parallel_planner_surface_contracts_landed.py tests/scripts/dev_tools/test_claude_rules_frontmatter.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase6-documentation-contracts.<ISO8601>.md` with the four required fields and every failing node ID with its message (possibly none). Acceptance: `EXIT_CODE: 0`, or `EXIT_CODE: 1` under the C4 bounded exemption only; the output contains `test_agent_body_contains_exactly_the_nine_required_headings PASSED` and `test_checkpoint_section_states_the_four_arrays_never_written PASSED`.

- [ ] [P6-T9] Check off `spec.md` S10, S12, S13, and S32, citing P6-T8 and the grep counts of P6-T1, P6-T2, P6-T6, and P6-T7. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `38`, `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `13`, and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` reports a removed count of `0` and an added count of `253`.

### Phase 7 — Zero-edge single cohort, validator tolerance, and the unmodified-surface proof

- [ ] [P7-T1] Create `tests/fixtures/parallel_cohorts/cohorts_mergeable_only_overlaps.json` in the shape of `cohorts_disjoint_items.json`: `name` `cohorts_mergeable_only_overlaps`, `notes` stating that four items whose only path overlaps are `.csproj` files acquire no edge under `mergeable_paths` and therefore share cohort 0 (issue #643), `input.item_keys` `[796, 797, 798, 799]`, `input.conflict_edges` `[]`, `expected_cohorts` `[[796, 797, 798, 799]]`. The same corpus is consumed by `tests/shell/parallel_cohorts_parity.bats` (line 43 sets `FIXTURE_DIR` to that directory, line 46 sets the corpus floor `MINIMUM_FIXTURE_COUNT=20`, and line 70 globs `"$FIXTURE_DIR"/*.json`), so the bash lane asserts this fixture against `.claude/lib/bash/compute-cohorts.sh` in CI without a further task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_parallel_cohort_bash_parity.py -v` prints `test_reference_implementation_reproduces_the_fixture[cohorts_mergeable_only_overlaps] PASSED` and `test_corpus_meets_the_declared_floor PASSED`.

- [ ] [P7-T2] Before creating the module, run the C9 Python reset (this is the fourth distinct Python test file of the session, after `blast_radius_parity_test_support.py`, `test_blast_radius_mergeable_paths.py`, and `test_parallel_drift_detection_conflicts.py`) and append its section to the same artifact. Then create `tests/scripts/dev_tools/test_parallel_mergeable_cohort.py` building four `BlastRadius` records for keys 796-799, each listing `TaskMaster.Test/TaskMaster.Test.csproj` plus one distinct `.cs` file, with empty modules, surfaces, and contracts, and the committed `config/blast-radius.json` loaded from disk, with three tests: `test_csproj_only_overlaps_yield_no_edge_for_any_pair` (all six unordered pairs give `conflicts(...).conflict is False`), `test_csproj_only_overlaps_place_every_item_in_a_single_cohort` (`compute_cohorts([796, 797, 798, 799], [])` equals `[[796, 797, 798, 799]]`, the edge list being the empty list the first test justifies), and `test_cohort_fixture_for_mergeable_only_overlaps_exists_in_the_shared_corpus` (the P7-T1 fixture parses, carries those four keys and an empty edge list, and its `expected_cohorts` equals the value the second test computed). Acceptance: the reset artifact records the pre-reset `testFiles` array containing those three paths and a post-reset state-file count of `0`; `poetry run pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py` prints `3 passed`; and `wc -l tests/scripts/dev_tools/test_parallel_mergeable_cohort.py` reports at most 160.

- [ ] [P7-T3] Create `tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_mergeable.py` importing `build_valid_parallel_state`, `item_at`, and `validate` from `tests.scripts.dev_tools.test_validate_parallel_orchestrator_state`, with `test_item_carrying_mergeable_conflicts_resolved_yields_no_errors` (attach to item 0 a one-entry `mergeable_conflicts_resolved` list with `path` `TaskMaster.Test/TaskMaster.Test.csproj`, `resolved_at` `2026-09-07T10-15`, `merged_against` and `merge_commit_sha` forty-character lowercase hex strings, `entries_added_from_ours` `["Compile:Foo.cs"]`, `entries_added_from_theirs` `["Compile:Bar.cs"]`, and `version_resolutions` `[{"key": "package:Newtonsoft.Json", "ours": "13.0.1", "theirs": "13.0.3", "chosen": "13.0.3"}]`; assert `validate(state) == []`) and `test_item_omitting_mergeable_conflicts_resolved_yields_no_errors` (assert the key is absent and `validate(state) == []`). Acceptance: `poetry run pytest tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_mergeable.py` prints `2 passed`, and `wc -l tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_mergeable.py` reports at most 90.

- [ ] [P7-T4] Append a `describe("optional mergeable_conflicts_resolved item field", ...)` block to `extensions/drm-copilot/test/lib/validate/parallel-orchestrator-state-core.test.ts` after the invariant-19 block ending at line 417, using `buildValidParallelState`, `itemAt`, and `validateState` from `./parallel-state-test-support`, with `it("accepts an item carrying mergeable_conflicts_resolved in the documented shape", ...)` (the same record as P7-T3; `expect(validateState(state)).toEqual([])`) and `it("accepts an item omitting mergeable_conflicts_resolved", ...)`. Acceptance: `wc -l extensions/drm-copilot/test/lib/validate/parallel-orchestrator-state-core.test.ts` reports at most 460, and P7-T6 reports the suite as passed.

- [ ] [P7-T5] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_mergeable_cohort.py tests/scripts/dev_tools/test_parallel_cohort_bash_parity.py tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_mergeable.py tests/scripts/dev_tools/test_validate_parallel_orchestrator_state.py tests/scripts/dev_tools/test_validate_parallel_orchestrator_state_cohort_barrier.py -v` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase7-python-cohort-and-tolerance.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`, and the output contains `test_csproj_only_overlaps_place_every_item_in_a_single_cohort PASSED`, `test_reference_implementation_reproduces_the_fixture[cohorts_mergeable_only_overlaps] PASSED`, and `test_item_carrying_mergeable_conflicts_resolved_yields_no_errors PASSED`.

- [ ] [P7-T6] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run test:unit; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/phase7-typescript-unit.<ISO8601>.md` with the four required fields and the verbatim `Tests:` line. Acceptance: `EXIT_CODE: 0`, the `Tests:` line contains no `failed`, and the passed count equals the P4-T11 count plus 2.

- [ ] [P7-T7] Run `git diff c3ffb080 --name-only` and `git status --porcelain --untracked-files=all` from the worktree root, then `git diff c3ffb080 -- .claude scripts extensions tests config | grep -c -F PARALLEL_COHORT_BARRIER_VIOLATION`. Artifact: `<FEATURE>/evidence/qa-gates/phase7-unmodified-surfaces.<ISO8601>.md` with the four required fields and both listings verbatim. Acceptance: no listed path contains `enforce-parallel-cohort-barrier`, `cohort_barrier`, or `cohort-barrier`; no listed path is `scripts/dev_tools/_blast_radius_glob.py`, `.claude/lib/blast-radius/BlastRadiusGlob.psm1`, `scripts/dev_tools/_blast_radius_validation.py`, or `.claude/lib/blast-radius/BlastRadiusConfig.psm1`; and the grep count is `0`.

- [ ] [P7-T8] Check off `spec.md` S31, S34, S35, and T5, and `user-story.md` U1, U2, and U11, citing P7-T5, P7-T6, and P7-T7. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `42` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `9`; `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `11` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `0`; and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` reports a removed count of `0` for both files with added counts of `253` and `70`.

### Phase 8 — Final QA loop, coverage deltas, scope verification, and acceptance-criteria check-off

Loop rule for this phase: the three language loops each run in the stated order after the last source edit. If any step fails, or any write-mode step rewrites a tracked file (observed by the porcelain listings and digest pairs, not by exit code), the defect is fixed at source and that language's loop restarts from its first step; P8-T13 records every restart. No task in this phase has a skip branch.

- [ ] [P8-T1] Run `git diff c3ffb080 --name-only` and `git status --porcelain --untracked-files=all` from the worktree root, then `wc -l` on every listed path (tracked or untracked) whose extension is `.py`, `.ps1`, `.psm1`, `.psd1`, `.ts`, `.cjs`, or `.json` and that does not lie under `tests/fixtures/`. Artifact: `<FEATURE>/evidence/qa-gates/file-size-compliance.<ISO8601>.md` with the four required fields and one count per path. Acceptance: every count is at most 500, and the artifact names each file from the C2 table with its final count beside its plan-time count.

- [ ] [P8-T2] Run `git status --porcelain --untracked-files=all`, then `poetry run black .`, then `git status --porcelain --untracked-files=all` again from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-python-black.<ISO8601>.md` with the four required fields, both porcelain listings verbatim, and Black's verbatim summary lines. Acceptance: `EXIT_CODE: 0`, the summary carries a `files left unchanged.` line and no `reformatted` line, and the two porcelain listings are identical; a `reformatted` line or a porcelain difference restarts the Python loop from this task after the change is reviewed.

- [ ] [P8-T3] Run `poetry run ruff check .` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-python-ruff.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and the verbatim line `All checks passed!`.

- [ ] [P8-T4] Run `poetry run pyright` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-python-pyright.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and the verbatim line `0 errors, 0 warnings, 0 informations`.

- [ ] [P8-T5] Run `poetry run pytest --cov=scripts.dev_tools --cov-branch --cov-report=term-missing --cov-report=json:artifacts/python/coverage.json` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-python-pytest-coverage.<ISO8601>.md` with the four required fields; `Output Summary:` carries the verbatim pytest summary line, the verbatim `TOTAL` row, the two percentages computed from the JSON `totals` integers exactly as in P0-T7, the verbatim table rows for `_blast_radius_mergeable.py` and `_blast_radius_conflicts.py`, and every failing node ID with its message (possibly none). Acceptance: `EXIT_CODE: 0`, or `EXIT_CODE: 1` under the C4 bounded exemption only; statement coverage at or above 85 and branch coverage at or above 75; the passed count is at least the P0-T7 passed count plus 25; both named rows show `Cover` at or above 90.

- [ ] [P8-T6] Run `git status --porcelain --untracked-files=all`, then `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run format; $code = $LASTEXITCODE; Pop-Location; exit $code'`, then `git status --porcelain --untracked-files=all` again, all from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-typescript-prettier.<ISO8601>.md` with the four required fields, both porcelain listings verbatim, the count of output lines carrying `(unchanged)`, and every matched-file line that lacks that marker. Acceptance: `EXIT_CODE: 0`, no matched-file line lacks `(unchanged)`, and the two porcelain listings are identical; otherwise the TypeScript loop restarts from this task.

- [ ] [P8-T7] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run lint; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-typescript-eslint.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and no output line contains `problems`.

- [ ] [P8-T8] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run typecheck; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-typescript-typecheck.<ISO8601>.md` with the four required fields. Acceptance: `EXIT_CODE: 0` and no output line contains `error TS`.

- [ ] [P8-T9] Run `pwsh -NoProfile -Command 'Push-Location extensions/drm-copilot; npm run test:coverage -- --coverageReporters=text; $code = $LASTEXITCODE; Pop-Location; exit $code'` from the worktree root (Jest exits non-zero when any `coverageThreshold` entry, including the P4-T3 entry, is not met). Artifact: `<FEATURE>/evidence/qa-gates/final-typescript-jest-coverage.<ISO8601>.md` with the four required fields, the verbatim `Test Suites:` and `Tests:` lines, the verbatim `All files` row, and the verbatim rows for `claude-blast-radius-derive-manifests.ts` and `claude-blast-radius-derive-core.ts`. Acceptance: `EXIT_CODE: 0`; `All files` shows `% Lines` at or above 85 and `% Branch` at or above 75; both named rows show `% Lines` at or above 85 and `% Branch` at or above 75; the passed test count is at least the P0-T11 count plus 12.

- [ ] [P8-T10] Record the ten lines printed by `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 .claude/lib/blast-radius/BlastRadius.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadius.psm1, .claude/lib/blast-radius/BlastRadiusConflict.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/blast-radius/BlastRadiusConflict.psm1, .claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/ProjectFileMergeGrammar.psm1, .claude/lib/project-file-merge/ProjectFileMerge.psm1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/ProjectFileMerge.psm1, .claude/lib/project-file-merge/Resolve-MergeableConflict.ps1, extensions/drm-copilot/resources/claude-customizations/.claude/lib/project-file-merge/Resolve-MergeableConflict.ps1).Hash"` (the five PowerShell production files of this plan, each followed by its mirror) and `git status --porcelain --untracked-files=all`; invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the worktree root; run the same hash command and the porcelain listing again. Artifact: `<FEATURE>/evidence/qa-gates/final-powershell-poshqc-format.<ISO8601>.md` with the four required fields, the verbatim `ok` and `summary` values, the ten digest pairs, and both porcelain listings. Acceptance: `ok` is `true`, every digest pair is equal, and the two porcelain listings are identical; otherwise the PowerShell loop restarts from this task.

- [ ] [P8-T11] Invoke `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root` set to the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-powershell-poshqc-analyze.<ISO8601>.md` with the four required fields and the verbatim `ok` and `summary` values. Acceptance: `ok` is `true` and the summary begins `Ran bundled PoshQC analyze against`.

- [ ] [P8-T12] Invoke `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the worktree root, then run `pwsh -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest -Root (Get-Location).Path"` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/final-powershell-poshqc-test.<ISO8601>.md` with the four required fields; `Output Summary:` carries the MCP `ok` value, the verbatim `Tests Passed:` line, the verbatim `Covered` line, the report-level `LINE` counters and derived percentage from `artifacts/pester/powershell-coverage.xml`, and the per-`sourcefile` `LINE` counters and derived percentages for `BlastRadius.psm1`, `BlastRadiusConflict.psm1`, `ProjectFileMergeGrammar.psm1`, `ProjectFileMerge.psm1`, and `Resolve-MergeableConflict.ps1`, plus the statement that Pester measures no branch coverage. Acceptance: the `Tests Passed:` line reports `Failed: 0` and a passed count at least the P0-T14 count plus 45; the report-level line percentage is at or above 85 and not below the P0-T14 value minus 0.5; each of the five per-file line percentages is at or above 85; `BlastRadius.psm1` is not below its P0-T14 per-file value.

- [ ] [P8-T13] Write `<FEATURE>/evidence/qa-gates/final-qa-loop-outcome.<ISO8601>.md` with `Timestamp:`, one row per language listing its steps in order with each step's artifact path and exit code, the number of restarts per language with the cause of each, and the statement that the recorded pass of every step occurred after the last source change. Acceptance: every step P8-T2 through P8-T12 is listed with `EXIT_CODE: 0` (or the C4-exempted `1` for P8-T5 with the exemption conditions restated), and the restart count is recorded as an integer for each language.

- [ ] [P8-T14] Write `<FEATURE>/evidence/qa-gates/coverage-delta.<ISO8601>.md` with `Timestamp:` and a table per language: Python baseline (P0-T7) versus final (P8-T5) statement and branch percentages, plus the two new-module rows; TypeScript baseline (P0-T11) versus final (P8-T9) `All files` lines and branches, plus the two named rows; PowerShell baseline (P0-T14) versus final (P8-T12) report-level line percentage, plus the five per-file rows. Acceptance: every final value meets its threshold (85 line/statement, 75 branch where measured), every per-file new-code value is at or above 85, and no run-level value is below its baseline by more than 0.5 percentage points; all values are numeric.

- [ ] [P8-T15] Run `git diff c3ffb080 --name-only` and `git status --porcelain --untracked-files=all` from the worktree root. Artifact: `<FEATURE>/evidence/qa-gates/scope-verification.<ISO8601>.md` with the four required fields and both listings verbatim. Acceptance: every listed path is one of: a path under `docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/`; `docs/features/potential/promoted/2026-09-07-mergeable-project-file-overlaps.md` (in the change set since the promotion, as P0-T15 records); `extensions/drm-copilot/jest.config.cjs` (P4-T3); `.gitattributes` (P5-T6, the CRLF-fixture normalization exemption); `config/blast-radius.json`; `extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json`; `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`; `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; `docs/features/templates/parallel/parallel-status.md`; a path under `scripts/dev_tools/` named `_blast_radius_mergeable.py`, `_blast_radius_conflicts.py`, or `compute_blast_radius.py`; a path under `.claude/lib/blast-radius/`, `.claude/lib/project-file-merge/`, or their mirrors; `.claude/rules/parallel-orchestration.md`, `.claude/agents/parallel-orchestrator.md`, `.claude/agents/parallel-planner.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/skills/parallel-plan/SKILL.md`, `.claude/skills/parallel-add/SKILL.md`, or their mirrors; `extensions/drm-copilot/src/lib/push-down/claude-blast-radius-derive-core.ts` or `claude-blast-radius-derive-manifests.ts`; a path under `extensions/drm-copilot/test/lib/push-down/` or `extensions/drm-copilot/test/lib/validate/parallel-orchestrator-state-core.test.ts`; a path under `tests/scripts/dev_tools/`, `tests/scripts/claude-lib/blast-radius/`, `tests/scripts/claude-lib/project-file-merge/`, `tests/fixtures/blast_radius/`, `tests/fixtures/parallel_cohorts/`, or `tests/fixtures/project_file_merge/`. The artifact states that no other path is listed.

- [ ] [P8-T16] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -v` and, for every file this plan mirrors (the two blast-radius modules, the three project-file-merge files, the rule file, the two agent files, and the three skill files), record the ten lines of the P8-T10 hash command and the twelve lines printed by `pwsh -NoProfile -Command "(Get-FileHash -Algorithm SHA256 .claude/rules/parallel-orchestration.md, extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md, .claude/agents/parallel-orchestrator.md, extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md, .claude/agents/parallel-planner.md, extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-planner.md, .claude/skills/parallel-orchestrate/SKILL.md, extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md, .claude/skills/parallel-plan/SKILL.md, extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-plan/SKILL.md, .claude/skills/parallel-add/SKILL.md, extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-add/SKILL.md).Hash"` (each self-hosted file followed by its mirror). Artifact: `<FEATURE>/evidence/qa-gates/push-down-parity.<ISO8601>.md` with the four required fields and the eleven digest pairs. Acceptance: all eleven adjacent pairs are equal, and the pytest output contains `test_planner_review_resources_exist_and_are_byte_identical PASSED` and, for `test_bundled_claude_payload_contains_all_repo_runtime_contracts`, either `PASSED` or a failure whose sole message names a path under `.claude/state/` per C4.

- [ ] [P8-T17] Check off `spec.md` S38 and S39 (recording beside S39, in the task's progress output only, that its artifacts live under `evidence/qa-gates/` per the C1 override rejection), D1 through D7, and reconcile every previously checked item against its evidence artifact per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, leaving unchecked any item whose evidence is missing. Acceptance: `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `51` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md` prints `0`; `grep -c -F -e "- [x] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `11` and `grep -c -F -e "- [ ] " docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` prints `0`; and `git diff c3ffb080 --numstat -- docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/spec.md docs/features/active/2026-09-07-mergeable-project-file-overlaps-643/user-story.md` reports a removed count of `0` for both files with added counts of `253` and `70` (no text other than the checkboxes changed); if any item is left unchecked the checked counts are lower by that number and the artifact of P8-T18 names each one.

- [ ] [P8-T18] Write `<FEATURE>/evidence/issue-updates/ac-status-summary.<ISO8601>.md` in the tracking skill's required format (`Source:`, `Total AC items:`, `Checked off (delivered):`, `Remaining (unchecked):`, `Items remaining:`) for `spec.md` (51 checkbox items across its three checkbox sections) and `user-story.md` (11 items), naming the evidence artifact backing each checked item. Acceptance: `Checked off (delivered):` equals the `grep -c -F -e "- [x] "` count and `Remaining (unchecked):` equals the `grep -c -F -e "- [ ] "` count observed by P8-T17 for each file, the two sum to 51 and 11 respectively, and the artifact names one evidence path per checked item.

---

## Test Plan

- Unit: Python (`test_blast_radius_mergeable_paths.py`, `test_parallel_mergeable_cohort.py`, `test_validate_parallel_orchestrator_state_mergeable.py`, one case in `test_parallel_drift_detection_conflicts.py`); Pester (`BlastRadiusConflict.Tests.ps1`, three cases in `BlastRadius.Conflict.Tests.ps1`, one case each in `BlastRadius.TruthTable.Tests.ps1`, `ProjectFileMergeGrammar.Tests.ps1`, `ProjectFileMerge.Tests.ps1`, `Resolve-MergeableConflict.Tests.ps1`, `ProjectFileMerge.Manifest.Tests.ps1`); Jest (`blast-radius-derive-manifests.test.ts`, `blast-radius-derive-mergeable.test.ts`, one `describe` in `parallel-orchestrator-state-core.test.ts`, rewritten cases in three existing files).
- Parity: two new `tests/fixtures/blast_radius/` fixtures consumed by both parity suites; one new `tests/fixtures/parallel_cohorts/` fixture consumed by the Python lane locally and the bats lane in CI; key-partition registries in both languages.
- Contract: `test_parallel_orchestrator_surface_contracts.py`, the two planner surface-contract modules, `test_push_down_claude_resource_contracts.py`, `test_push_down_claude_pack_end_to_end.py`, `ClaudeLibModuleConvention.Tests.ps1`, the two manifest-membership suites.
- Coverage evidence: baselines P0-T7, P0-T11, P0-T14; post-change P8-T5, P8-T9, P8-T12; comparison P8-T14; scoped checks P2-T6 and P4-T12; per-file jest threshold for the new production file P4-T3.

## Open Questions / Notes

- The spec's final criterion names `evidence/qa/`; the canonical kind `evidence/qa-gates/` is used (constraint C1).
- The bats lane is not run locally (it requires bats under WSL per repository memory); the shared fixture corpus is the mechanism by which the bash lane asserts the zero-edge cohort case in CI.
