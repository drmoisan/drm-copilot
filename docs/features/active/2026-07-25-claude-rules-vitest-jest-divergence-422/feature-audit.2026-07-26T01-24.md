# Feature Audit: Claude/Codex Rule Mirror Vitest→Jest Correction (#422)

**Audit Date:** 2026-07-26
**Feature Folder:** `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422`
**Base Branch:** `origin/main`
**Head Branch:** `bug/claude-rules-typescript-vitest-jest-divergence`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

---

## Scope and Baseline

- **Base branch:** `origin/main` (commit `fb483b8468204e4385b5583c3b3ec4c0a987eede`)
- **Head branch/commit:** `bug/claude-rules-typescript-vitest-jest-divergence` (commit `042ed066b1350100513bc0a7e09c141b2f3ead12`)
- **Merge base:** `fb483b8468204e4385b5583c3b3ec4c0a987eede`
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by the reviewer against `origin/main`; the artifact pair was absent at review start)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/evidence/**` (26 artifacts across `baseline/`, `regression-testing/`, `qa-gates/`, `other/`)
  - Additional evidence: reviewer-executed commands (full pytest suite, Black/Ruff/Pyright, SHA-256 pair hashing, `git show` at merge base, lcov parse)
- **Feature folder used:** `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422`
- **Requirements source:** `spec.md` `## Acceptance Criteria` (14 checkbox items)
- **Work mode resolution note:** `issue.md` carries the explicit marker `- Work Mode: full-bug`, so `spec.md` is the sole authoritative AC source. `user-story.md` is intentionally absent for `full-bug` and is not a defect.
- **Scope note:** The audit scope is the full branch diff `fb483b84..042ed066` (43 files). PR context artifacts were regenerated by the reviewer because they were missing; base branch was supplied explicitly and matches the auto-detected merge base.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/spec.md` — only source (`full-bug`)

### Acceptance criteria

1. `.claude/rules/typescript.md` (lines 16, 42, 47, 51, 73) is corrected to Jest — framework name, `jest.spyOn`/`jest.mock`/`jest.resetAllMocks`, and `jest.useFakeTimers()` — and its bundled copy at `extensions/drm-copilot/resources/claude-customizations/.claude/rules/typescript.md` is byte-identical.
2. `.claude/rules/typescript.md` line 16 names `npm run test:unit` as the unit-test command (not `npm run test`, which maps to the `vscode-test` integration runner).
3. `.claude/rules/typescript.md` line 51 names `npm run test:unit:coverage` (not the non-existent `npm run test:coverage`), and the stale clause "(the script is wired in Prompt B1 alongside the Vitest dependency)" is removed or replaced with accurate text.
4. `.claude/rules/general-unit-test.md` (lines 40, 105) is corrected — `vitest.config.ts` → `jest.config.cjs` in the permitted coverage-exclude list; `vi.useFakeTimers()` → `jest.useFakeTimers()` — and its bundled copy at `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-unit-test.md` is byte-identical.
5. `.claude/rules/general-code-change.md` (line 39) names Jest in the TypeScript slot of the unit-test-stage runner list, and its bundled copy at `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-code-change.md` is byte-identical.
6. `.claude/agents/atomic-executor.md` line 18 allowlists `Bash(npx jest *)` in place of `Bash(npx vitest *)`, line 79 names `npx jest` in the TypeScript toolchain command list, and its bundled copy at `extensions/drm-copilot/resources/claude-customizations/.claude/agents/atomic-executor.md` is byte-identical.
7. `.agents/skills/general-unit-test/SKILL.md` (lines 45, 110) carries the same corrections as `.claude/rules/general-unit-test.md`, and its bundled copy at `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-unit-test/SKILL.md` is byte-identical.
8. `.agents/skills/general-code-change/SKILL.md` (line 44) carries the same correction as `.claude/rules/general-code-change.md`, and its bundled copy at `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-code-change/SKILL.md` is byte-identical.
9. Both parity tests pass: `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` and `tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts`.
10. The new regression test `tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py` exists, asserts framework-token absence in all six repo-root mirrors and command resolution plus the `test:unit`/`test:unit:coverage` semantic anchors, and passes against the fixed tree.
11. Fail-before evidence is recorded: the new regression test was run against the pre-fix tree and its failure output captured before the fix was applied.
12. Non-goals respected: no file under `.github/instructions/**` was modified; root `package.json`, `jest.config.cjs`, `run-jest.cjs`, `tsconfig*.json`, and `.vscode-test.*` are unmodified; `expert-react-frontend-engineer.agent.md` and `github-actions-ci-cd-best-practices.instructions.md` (originals and vendored copies) are unmodified; no Vitest migration was performed.
13. The adjacent `.dependency-cruiser.cjs` finding at `.claude/rules/typescript.md:57` was not folded into this fix and is recorded for separate filing.
14. Full toolchain pass completed for the changed test module (format → lint → type-check → test).

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | `typescript.md` corrected to Jest at lines 16/42/47/51/73; bundle byte-identical | PASS | Reviewer read of `.claude/rules/typescript.md`: line 16 "**Testing — Jest**", line 42 "Use **Jest**", line 47 `jest.spyOn`/`jest.mock`/`jest.resetAllMocks()`, line 73 `jest.useFakeTimers()`. SHA-256 of repo-root file equals bundled copy (`44574b98…`). | `git diff fb483b84..HEAD -- .claude/rules/typescript.md`; `sha256sum` of both files | Independent of executor evidence. |
| 2 | Line 16 names `npm run test:unit` | PASS | Line 16 reads "Command: `npm run test:unit`"; `package.json` maps `test:unit` → `node run-jest.cjs` and `test` → `vscode-test`. Regression test `test_typescript_rule_testing_line_names_the_unit_test_command` also asserts absence of `` `npm run test` `` on that line and passes. | File read; `python -c` dump of `package.json` scripts; `poetry run pytest …instruction_contracts.py` | |
| 3 | Line 51 names `npm run test:unit:coverage`; stale clause replaced accurately | PASS | Line 51 reads "Coverage command: `npm run test:unit:coverage` (the root `package.json` script runs `node run-jest.cjs --coverage`)." — the replacement text matches the actual script value. No `test:coverage` token remains; "Prompt B1" clause gone. | File read; `package.json` scripts dump | Replacement text verified factually accurate, not merely present. |
| 4 | `general-unit-test.md` lines 40/105 corrected; bundle byte-identical | PASS | Diff shows `vitest.config.ts` → `jest.config.cjs` (line 40) and `vi.useFakeTimers()` for Vitest → `jest.useFakeTimers()` for Jest (line 105). SHA-256 pair identical (`c2d5f069…`). | `git diff fb483b84..HEAD -- .claude/rules/general-unit-test.md`; `sha256sum` | |
| 5 | `general-code-change.md` line 39 names Jest; bundle byte-identical | PASS | Diff shows `(e.g., Pytest, Jest, MSTest, Pester)` in the unit-test stage. SHA-256 pair identical (`02eefec6…`). | `git diff`; `sha256sum` | |
| 6 | `atomic-executor.md` line 18 `Bash(npx jest *)`, line 79 `npx jest`; bundle byte-identical | PASS | Diff shows both replacements at the stated lines. SHA-256 pair identical (`55a5b37e…`). Consistency confirmed: hook regex at `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1:67` includes `npx … jest`; Jest `^30.4.2` devDependency and `jest.config.cjs` exist; executor `npx jest --listTests` exit 0. | `git diff`; `sha256sum`; grep of hook; `package.json` inspection | Runtime-affecting change verified against the enforcement hook and actual Jest setup. |
| 7 | `.agents/skills/general-unit-test/SKILL.md` lines 45/110 corrected; bundle byte-identical | PASS | Diff shows the same two corrections as criterion 4, in lockstep. SHA-256 pair identical (`92889171…`). | `git diff`; `sha256sum` | |
| 8 | `.agents/skills/general-code-change/SKILL.md` line 44 corrected; bundle byte-identical | PASS | Diff shows `(e.g., Pytest, Jest, MSTest, Pester)`. SHA-256 pair identical (`635dcdb6…`). | `git diff`; `sha256sum` | |
| 9 | Both parity tests pass | PASS | Reviewer run: both named parity tests pass (within the 28-passed targeted selection) and within the full suite (2138 passed). Executor artifact `evidence/qa-gates/final-parity-and-regression.2026-07-26T01-08.md` concurs (17/17). | `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py -q` | |
| 10 | New regression test exists with all required assertions and passes | PASS | Module read: 15 cases covering framework-token absence (six mirrors, two patterns), `npm run` command resolution, and both semantic anchors. Reviewer run: 15/15 pass against the fixed tree. | Read of the module; `poetry run pytest tests/scripts/dev_tools/test_typescript_toolchain_instruction_contracts.py -q` | |
| 11 | Fail-before evidence recorded | PASS | `evidence/regression-testing/fail-before.2026-07-26T00-58.md`: EXIT_CODE 1, `12 failed, 3 passed`, failures at exactly the spec-named lines, captured pre-fix. Reviewer corroborated the pre-fix defect content at the merge base (`git show fb483b84:.claude/rules/typescript.md` shows Vitest at lines 16/42/51/73, `npm run test`, `npm run test:coverage`), confirming the recorded failures are consistent with the historical tree. | `git show fb483b8468204e4385b5583c3b3ec4c0a987eede:.claude/rules/typescript.md` | The historical run itself cannot be re-executed; artifact completeness plus merge-base corroboration support PASS. |
| 12 | Non-goals respected | PASS | Reviewer scan of the full changed-file list (43 paths): none under `.github/instructions/**`; no `package.json`, `jest.config.cjs`, `run-jest.cjs`, `tsconfig*.json`, `.vscode-test.*`; neither NOT-A-DEFECT file; no `docs/features/completed/**`; no Vitest dependency or config added. | `git diff --name-only fb483b84..HEAD` | Independent of executor's `evidence/other/non-goal-verification.2026-07-26T00-58.md`, which concurs. |
| 13 | `.dependency-cruiser.cjs` finding not folded in, recorded for separate filing | PASS | `evidence/other/adjacent-finding-dependency-cruiser.2026-07-26T00-58.md` exists with the finding and follow-up recommendation; `.claude/rules/typescript.md:57` still names `.dependency-cruiser.cjs` unchanged; reviewer glob confirms the file does not exist repository-wide. | Read of artifact; read of `typescript.md:57`; glob `**/.dependency-cruiser.cjs` | |
| 14 | Full toolchain pass for the changed test module | PASS | Reviewer re-ran all four stages check-only: `black --check .` clean (333 files), `ruff check .` clean, `pyright` 0 errors, `pytest -q` 2138 passed. Executor artifacts `evidence/qa-gates/final-python-{black,ruff,pyright,pytest-coverage}.2026-07-26T01-08.md` record the same single-pass loop. | `poetry run black --check .`; `poetry run ruff check .`; `poetry run pyright`; `poetry run pytest -q` | |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 14 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. After merge, confirm the separate issue for the `.dependency-cruiser.cjs` stale reference is filed (spec `## Rollout & Follow-up`), and schedule the `README.md:303`/`README.md:318` Vitest-wording correction.
2. On the next extension release, confirm the corrected bundled resources ship through the existing push-down mechanism (no additional action expected; the parity tests gate this).

Supplementary verification beyond the AC set: Python coverage held exactly at baseline (91.00% line / 81.84% branch, floors 85% / 75%), verified by reviewer parse of `artifacts/python/lcov.info`; evidence-location validator exits 0; no scope-narrowing instruction was received (see policy audit `## Rejected Scope Narrowing`).

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- All 14 criteria were evaluated **PASS** and were already checked (`- [x]`) in `spec.md` by the executor's Phase 4/Phase 6 check-off tasks. The reviewer independently re-verified each criterion; no criterion required unchecking, and no new check-off edit was needed.
- No criterion is PARTIAL, FAIL, or UNVERIFIED, so no checkbox was reverted.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/spec.md`
- Total AC items: 14
- Checked off (delivered): 14
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-claude-rules-vitest-jest-divergence-422/spec.md` | 14 | 14 | 0 | Checkbox-backed; pre-checked by executor, independently re-verified by reviewer |

No source-file checkbox change was made by this audit: every PASS criterion was already checked, and the tracking rules prohibit redundant edits and phantom criteria.
