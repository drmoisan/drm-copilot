# Atomic Remediation Plan — Codex-Native Parallel Orchestration #467

## Objective

Remediate all findings in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-inputs.2026-08-12T01-42.md`: raise each of the five added Python production modules (`scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py`, `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py`, `scripts/dev_tools/parallel_codex_readiness_filesystem.py`, `scripts/dev_tools/push_down_codex_routing_merge.py`, and `scripts/dev_tools/validate_parallel_codex_readiness.py`) to at least 90% line coverage; restore each modified Python production file (`scripts/dev_tools/parallel_kickoff_contract.py`, `scripts/dev_tools/resolve_codex_deployment.py`, and `scripts/dev_tools/resolve_codex_topology.py`) to its own numeric baseline line coverage or higher; provide attributable coverage for the changed/new PowerShell runtime files; restore regressed TypeScript file coverage; align planner and orchestrator prompts with dedicated permissions; and add required Python documentation and intent comments.

## Execution contract

- This is a planning-only artifact. Planning and preflight must not implement any remediation.
- Execute phases and tasks in order; check a task only after its binary acceptance criterion is met.
- Use test-first bounded batches. A Python or PowerShell batch may own no more than three production files and three test files. TypeScript batches use the same conservative cap.
- Every changed production file, test file, or reusable script must remain at or below 500 lines.
- Do not modify `.claude/**`.
- Change canonical sources first, regenerate bundles with repository tooling, and validate source/bundle parity. Do not hand-edit generated output.
- Write evidence only below `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/<kind>/`.
- For each language, run formatting, linting/analysis, type checking where applicable, and tests in that order. If a step fails or changes files, correct it and restart that language loop from formatting until one uninterrupted pass succeeds.

## R1 Python batch ownership

The following inventory is exhaustive and authoritative for R1. Each of the eight production paths appears exactly once, each named test owner appears in only one batch, and the ordered batches remain within the three-production-file and three-test-file limits.

| Batch | Production path | Coverage contract | Test owner |
|---|---|---|---|
| 1 | `scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py` | Added file: at least 90% line coverage | `tests/scripts/dev_tools/test_parallel_completion_receipts.py` |
| 1 | `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py` | Added file: at least 90% line coverage | `tests/scripts/dev_tools/test_parallel_mutation_receipt_bound_runtime.py` |
| 1 | `scripts/dev_tools/parallel_codex_readiness_filesystem.py` | Added file: at least 90% line coverage | `tests/scripts/dev_tools/test_parallel_codex_readiness_filesystem.py` |
| 2 | `scripts/dev_tools/push_down_codex_routing_merge.py` | Added file: at least 90% line coverage | `tests/scripts/dev_tools/test_push_down_codex_routing_merge.py` |
| 2 | `scripts/dev_tools/validate_parallel_codex_readiness.py` | Added file: at least 90% line coverage | `tests/scripts/dev_tools/test_validate_parallel_codex_readiness.py` |
| 2 | `scripts/dev_tools/parallel_kickoff_contract.py` | Modified file: restore its own P0-T8 baseline line coverage or higher | `tests/scripts/dev_tools/test_parallel_kickoff_contract.py` |
| 3 | `scripts/dev_tools/resolve_codex_deployment.py` | Modified file: restore its own P0-T8 baseline line coverage or higher | `tests/scripts/dev_tools/test_resolve_codex_deployment.py` |
| 3 | `scripts/dev_tools/resolve_codex_topology.py` | Modified file: restore its own P0-T8 baseline line coverage or higher | `tests/scripts/dev_tools/test_resolve_codex_topology.py` |

Batch counts and order: batch 1 owns 3 production files and 3 test files; batch 2 owns 3 production files and 3 test files; batch 3 owns 2 production files and 2 test files. Execution order is batch 1, then batch 2, then batch 3.

### Phase 0 — Policy, inventory, and baselines

- [x] [P0-T1] Read `AGENTS.md`, `.agents/skills/policy-compliance-order/SKILL.md`, `.agents/skills/general-code-change/SKILL.md`, and `.agents/skills/general-unit-test/SKILL.md`; record applicable precedence, coverage thresholds, `.claude/**` immutability, evidence rules, and the 500-line gate in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/policy.md`, with acceptance defined as every constraint being cited to its canonical repository source.
- [x] [P0-T2] Read `.agents/skills/python/SKILL.md`, `.agents/skills/python-change-budget-router/SKILL.md`, `.agents/skills/python-qa-gate/SKILL.md`, `.agents/skills/python-suppressions/SKILL.md`, and `.agents/skills/self-explanatory-code-commenting/SKILL.md`; append exact Python toolchain commands and documentation rules to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/policy.md`, with acceptance defined as a complete executable Python QA sequence.
- [x] [P0-T3] Read `.agents/skills/powershell/SKILL.md`, `.agents/skills/powershell-change-budget-router/SKILL.md`, and `.agents/skills/powershell-qa-gate/SKILL.md`; append exact PowerShell toolchain and attributable-coverage commands to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/policy.md`, with acceptance defined as a complete executable PowerShell QA sequence.
- [x] [P0-T4] Read `.agents/skills/typescript/SKILL.md` and `.agents/skills/typescript-suppressions/SKILL.md`; append exact TypeScript toolchain and coverage commands to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/policy.md`, with acceptance defined as a complete executable TypeScript QA sequence.
- [x] [P0-T5] Read `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/remediation-inputs.2026-08-12T01-42.md`; enumerate every finding, production path, test owner, prompt source, generated bundle, and required command in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/scope.md`, with acceptance defined as every input finding mapping to at least one later task ID.
- [x] [P0-T6] Count lines in every scoped production, test, and reusable script path from `evidence/baseline/scope.md`; save results to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/line-counts.txt`, with acceptance defined as every path having a count at or below 500 or an explicit pre-edit split task.
- [x] [P0-T7] Run the canonical source/bundle parity and persona-permission validators without file modification; save commands, exit codes, and output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/parity-and-permissions.txt`, with acceptance defined as a reproducible baseline for each prompt contradiction and parity state.
- [x] [P0-T8] Run the repository Python format-check, Ruff, Pyright, and full Pytest with line and branch coverage in policy order without production edits; write command-specific receipts with `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` under `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/python/`, including numeric repository line and branch coverage and numeric per-file line coverage with exact uncovered lines for `scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py`, `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py`, `scripts/dev_tools/parallel_codex_readiness_filesystem.py`, `scripts/dev_tools/push_down_codex_routing_merge.py`, `scripts/dev_tools/validate_parallel_codex_readiness.py`, `scripts/dev_tools/parallel_kickoff_contract.py`, `scripts/dev_tools/resolve_codex_deployment.py`, and `scripts/dev_tools/resolve_codex_topology.py`; acceptance is a recorded numeric baseline for each of the eight paths, with all five added-file deficits attributable to exact uncovered lines and the three modified-file values retained as individual no-regression thresholds for P9-T4.
- [x] [P0-T9] Run the repository PoshQC format-check, PoshQC analysis, Pester, and attributable coverage commands in policy order without production edits; save exact commands, exit codes, total coverage, and attribution for all 25 changed/new PowerShell runtime files under `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/powershell/`, with acceptance defined as all 24 missing-attribution findings being reproduced or explicitly reconciled.
- [x] [P0-T10] Run the repository TypeScript format-check, lint, type-check, tests, and changed-file coverage commands in policy order without production edits; save exact commands, exit codes, total coverage, and line coverage for all five modified files under `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/typescript/`, with acceptance defined as every reviewed regression being reproduced or explicitly reconciled.
- [x] [P0-T11] Run the Bash baseline under WSL from the repository root in exact order with `bash scripts/bash/shell-qc.sh format`, `bash scripts/bash/shell-qc.sh check`, and `bash -lc "SHELL_QC_KCOV_OUT_DIR='docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/bash/kcov' bash scripts/bash/shell-qc.sh test --coverage"`; write one command receipt containing `Timestamp:`, the exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to each of `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/bash/format.md`, `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/bash/check.md`, and `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/bash/tests-coverage.md`; acceptance is all three commands and receipts being complete, the coverage receipt recording test counts and numeric `Bash coverage (lines)` with the Cobertura output retained below the named canonical `evidence/baseline/bash/kcov/` path, and missing `bats` or `kcov` being recorded as a blocking baseline failure rather than a passing skip.

### Phase 1 — Persona-permission tests first

- [x] [P1-T1] [expect-fail] Add and run a focused failing test in the planner persona test owner named by `evidence/baseline/scope.md` asserting that every tool and permission statement in the canonical planner prompt matches the dedicated planner profile; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/persona-planner-red.txt`, with acceptance defined as failure against the current contradiction with no production-source edit.
- [x] [P1-T2] [expect-fail] Add and run a focused failing test in the orchestrator persona test owner named by `evidence/baseline/scope.md` asserting that every tool and permission statement in the canonical orchestrator prompt matches the dedicated orchestrator profile; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/persona-orchestrator-red.txt`, with acceptance defined as failure against the current contradiction with no production-source edit.
- [x] [P1-T3] [expect-fail] Add and run a focused failing parity test in the exact parity test owner named by `evidence/baseline/scope.md` asserting that corrected canonical prompts and generated bundles agree while `.claude/**` remains outside writable targets; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/persona-parity-red.txt`, with acceptance defined as failure attributable only to current prompt or bundle drift.
- [x] [P1-T4] [expect-fail] Run the tests introduced by P1-T1 through P1-T3 and save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/persona-suite-red.txt`; acceptance is each reviewed contradiction producing an explicit failing assertion.

### Phase 2 — Persona-permission source and bundle correction

- [x] [P2-T1] Update the canonical planner persona source identified in `evidence/baseline/scope.md` so its prompt describes only tools and permissions granted by its dedicated profile; acceptance is P1-T1 passing without any change under `.claude/**`.
- [x] [P2-T2] Update the canonical orchestrator persona source identified in `evidence/baseline/scope.md` so its prompt describes only tools and permissions granted by its dedicated profile; acceptance is P1-T2 passing without any change under `.claude/**`.
- [x] [P2-T3] Run the canonical customization generation command for sources changed by P2-T1 and P2-T2; acceptance is regenerated tracked output matching the corrected sources, no hand-edited generated output, and no changed path under `.claude/**`.
- [x] [P2-T4] Run P1-T1 through P1-T3 plus the canonical source/bundle parity validator and save the green result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/persona-green.txt`; acceptance is zero test failures and zero parity drift.
- [x] [P2-T5] Count lines in every production, test, and reusable script file changed in Phases 1 and 2 and save results to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/persona.txt`; acceptance is every file at or below 500 lines.

### Phase 3 — Python test-first batch 1

- [x] [P3-T1] [expect-fail] Before any batch 1 production edit, add focused positive, negative, boundary, and error cases only to `tests/scripts/dev_tools/test_parallel_completion_receipts.py`, `tests/scripts/dev_tools/test_parallel_mutation_receipt_bound_runtime.py`, and `tests/scripts/dev_tools/test_parallel_codex_readiness_filesystem.py`, then run those exact targeted tests; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-1-tests-red.md`, with acceptance defined as each new test failing for its intended uncovered behavior and no production-source edit.
- [x] [P3-T2] [expect-fail] Run the P3-T1 tests with numeric per-file line coverage for `scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py`, `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py`, and `scripts/dev_tools/parallel_codex_readiness_filesystem.py`; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, numeric `Output Summary:`, and exact uncovered-line mappings to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-1-coverage-red.md`, with acceptance defined as every intended coverage deficit mapping to its owning production path and P3-T1 test owner before production edits.
- [x] [P3-T3] Add complete intent-first documentation and only the minimum behavior-preserving testability seams required by P3-T1 and P3-T2 to `scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py`, `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py`, and `scripts/dev_tools/parallel_codex_readiness_filesystem.py`; acceptance is no public behavior change, no unapproved suppression, and every callable, loop/comprehension, non-trivial branch, and multi-step block satisfying `.agents/skills/self-explanatory-code-commenting/SKILL.md`.
- [x] [P3-T4] Run Black, Ruff, Pyright, and targeted Pytest with per-file line coverage for the P3-T1 test owners and P3-T3 production paths in exact order, restarting from Black after any failure or file change; write `Timestamp:`, each exact `Command:`, integer `EXIT_CODE:`, and numeric `Output Summary:` values to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-batch-1-green.md`, with acceptance defined as an uninterrupted pass and each of the three added production files reaching at least 90% line coverage.
- [x] [P3-T5] Count lines and run the applicable documentation, suppression, test-location, and changed-file policy checks for exactly the three production paths and three test owners in batch 1; write `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/python-batch-1-policy.md`, with acceptance defined as every file at or below 500 lines and every applicable policy check passing.

### Phase 4 — Python test-first batch 2

- [x] [P4-T1] [expect-fail] After Phase 3 passes and before any batch 2 production edit, add focused positive, negative, boundary, and error cases only to `tests/scripts/dev_tools/test_push_down_codex_routing_merge.py`, `tests/scripts/dev_tools/test_validate_parallel_codex_readiness.py`, and `tests/scripts/dev_tools/test_parallel_kickoff_contract.py`, then run those exact targeted tests; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-2-tests-red.md`, with acceptance defined as each new test failing for its intended uncovered behavior and no batch 2 production-source edit.
- [x] [P4-T2] [expect-fail] Run the P4-T1 tests with numeric per-file line coverage for `scripts/dev_tools/push_down_codex_routing_merge.py`, `scripts/dev_tools/validate_parallel_codex_readiness.py`, and `scripts/dev_tools/parallel_kickoff_contract.py`; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, numeric `Output Summary:`, and exact uncovered-line mappings to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-2-coverage-red.md`, with acceptance defined as every intended coverage deficit mapping to its owning production path and P4-T1 test owner before batch 2 production edits.
- [x] [P4-T3] Add complete intent-first documentation and only the minimum behavior-preserving testability seams required by P4-T1 and P4-T2 to `scripts/dev_tools/push_down_codex_routing_merge.py`, `scripts/dev_tools/validate_parallel_codex_readiness.py`, and `scripts/dev_tools/parallel_kickoff_contract.py`; acceptance is no public behavior change, no unapproved suppression, and every callable, loop/comprehension, non-trivial branch, and multi-step block satisfying `.agents/skills/self-explanatory-code-commenting/SKILL.md`.
- [x] [P4-T4] Run Black, Ruff, Pyright, and targeted Pytest with per-file line coverage for the P4-T1 test owners and P4-T3 production paths in exact order, restarting from Black after any failure or file change; write `Timestamp:`, each exact `Command:`, integer `EXIT_CODE:`, and numeric `Output Summary:` values to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-batch-2-green.md`, with acceptance defined as an uninterrupted pass, both added production files reaching at least 90% line coverage, and `scripts/dev_tools/parallel_kickoff_contract.py` restoring its own P0-T8 baseline line coverage or higher.
- [x] [P4-T5] Count lines and run the applicable documentation, suppression, test-location, and changed-file policy checks for exactly the three production paths and three test owners in batch 2; write `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/python-batch-2-policy.md`, with acceptance defined as every file at or below 500 lines and every applicable policy check passing.

### Phase 5 — Python test-first batch 3

- [x] [P5-T1] [expect-fail] After Phase 4 passes and before any batch 3 production edit, add focused positive, negative, boundary, and error cases only to `tests/scripts/dev_tools/test_resolve_codex_deployment.py` and `tests/scripts/dev_tools/test_resolve_codex_topology.py`, then run those exact targeted tests; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-3-tests-red.md`, with acceptance defined as each new test failing for its intended uncovered behavior and no batch 3 production-source edit.
- [x] [P5-T2] [expect-fail] Run the P5-T1 tests with numeric per-file line coverage for `scripts/dev_tools/resolve_codex_deployment.py` and `scripts/dev_tools/resolve_codex_topology.py`; write `Timestamp:`, exact `Command:`, integer nonzero `EXIT_CODE:`, numeric `Output Summary:`, and exact uncovered-line mappings to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/python-batch-3-coverage-red.md`, with acceptance defined as every intended coverage deficit mapping to its owning production path and P5-T1 test owner before batch 3 production edits.
- [x] [P5-T3] Add complete intent-first documentation and only the minimum behavior-preserving testability seams required by P5-T1 and P5-T2 to `scripts/dev_tools/resolve_codex_deployment.py` and `scripts/dev_tools/resolve_codex_topology.py`; acceptance is no public behavior change, no unapproved suppression, and every callable, loop/comprehension, non-trivial branch, and multi-step block satisfying `.agents/skills/self-explanatory-code-commenting/SKILL.md`.
- [x] [P5-T4] Run Black, Ruff, Pyright, and targeted Pytest with per-file line coverage for the P5-T1 test owners and P5-T3 production paths in exact order, restarting from Black after any failure or file change; write `Timestamp:`, each exact `Command:`, integer `EXIT_CODE:`, and numeric `Output Summary:` values to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-batch-3-green.md`, with acceptance defined as an uninterrupted pass and each of `scripts/dev_tools/resolve_codex_deployment.py` and `scripts/dev_tools/resolve_codex_topology.py` restoring its own P0-T8 baseline line coverage or higher.
- [x] [P5-T5] Count lines and run the applicable documentation, suppression, test-location, and changed-file policy checks for exactly the two production paths and two test owners in batch 3; write `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/python-batch-3-policy.md`, with acceptance defined as every file at or below 500 lines and every applicable policy check passing.

### Phase 6 — PowerShell attributable coverage, test-first batches

- [x] [P6-T1] Partition all PowerShell runtime files lacking attributable coverage into ordered batches of no more than three production files and three Pester test files in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/powershell-batches.md`; acceptance is every deficient runtime file appearing exactly once with its test owner and uncovered behaviors.
- [x] [P6-T2] [expect-fail] For PowerShell batch 1, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-1-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T3] For PowerShell batch 1, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-1-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T4] [expect-fail] For PowerShell batch 2, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-2-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T5] For PowerShell batch 2, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-2-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T6] [expect-fail] For PowerShell batch 3, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-3-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T7] For PowerShell batch 3, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-3-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T8] [expect-fail] For PowerShell batch 4, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-4-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T9] For PowerShell batch 4, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-4-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T10] [expect-fail] For PowerShell batch 5, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-5-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T11] For PowerShell batch 5, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-5-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T12] [expect-fail] For PowerShell batch 6, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-6-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T13] For PowerShell batch 6, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-6-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T14] [expect-fail] For PowerShell batch 7, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-7-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T15] For PowerShell batch 7, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-7-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T16] [expect-fail] For PowerShell batch 8, add focused failing Pester cases before production edits and run the targeted cases with attributable coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/powershell-batch-8-red.txt`, with acceptance defined as each failure being attributable to an uncovered batch behavior and no production edit.
- [x] [P6-T17] For PowerShell batch 8, add only required testability seams; run PoshQC format, PoshQC analyze, and targeted Pester with attributable coverage in exact order and restart from format after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-batch-8-green.txt`, with acceptance defined as every batch runtime file carrying attributable coverage and no regression.
- [x] [P6-T18] Count lines in every PowerShell production and test path changed in Phase 6 and save results to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/powershell.txt`; acceptance is every file at or below 500 lines.

### Phase 7 — TypeScript coverage, test-first batches

- [x] [P7-T1] Partition the five regressed TypeScript files into two ordered batches of no more than three production files and three test owners in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/baseline/typescript-batches.md`; acceptance is every file appearing exactly once with uncovered lines and its test owner.
- [x] [P7-T2] [expect-fail] For TypeScript batch 1, add focused failing tests before production edits and run the targeted tests with per-file coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/typescript-batch-1-red.txt`, with acceptance defined as each failure being attributable to a reviewed coverage gap and no production edit.
- [x] [P7-T3] For TypeScript batch 1, add only required behavior-preserving testability seams; run formatter, linter, type checker, and targeted tests with per-file coverage in exact order and restart from formatting after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-batch-1-green.txt`, with acceptance defined as restored baseline line coverage or higher for every batch file and no changed-line regression.
- [x] [P7-T4] [expect-fail] For TypeScript batch 2, add focused failing tests before production edits and run the targeted tests with per-file coverage; save the expected-red result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/typescript-batch-2-red.txt`, with acceptance defined as each failure being attributable to a reviewed coverage gap and no production edit.
- [x] [P7-T5] For TypeScript batch 2, add only required behavior-preserving testability seams; run formatter, linter, type checker, and targeted tests with per-file coverage in exact order and restart from formatting after any failure or file change; save the uninterrupted-green output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-batch-2-green.txt`, with acceptance defined as restored baseline line coverage or higher for every batch file and no changed-line regression.
- [x] [P7-T6] Count lines in every TypeScript production and test path changed in Phase 7 and save results to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/typescript.txt`; acceptance is every file at or below 500 lines.

### Phase 8 — Source/bundle parity and `.claude` immutability

- [x] [P8-T1] Run the canonical customization generation command after all canonical-source edits and save the resulting tracked diff to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/regeneration-diff.txt`; acceptance is either no diff or only expected generated paths mapped in `evidence/baseline/scope.md`.
- [x] [P8-T2] Run the canonical source/bundle parity validator and save command, exit code, and output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/source-bundle-parity.txt`; acceptance is exit code zero for all scoped sources and bundles.
- [x] [P8-T3] Diff `.claude/**` against the remediation start commit and save the result to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/claude-immutability.txt`; acceptance is no remediation-authored change under `.claude/**`.
- [x] [P8-T4] Run planner/orchestrator profile, manifest, topology, prompt, and permission validation and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/runtime-validation.txt`; acceptance is zero contradictory permission claims and zero validation failures.

### Phase 9 — Full Python QA loop

- [x] [P9-T1] Run repository Black over the full Python scope and save a receipt containing `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-format.md`; acceptance is success with no file changes, otherwise correct and restart Phase 9 at P9-T1.
- [x] [P9-T2] Run repository Ruff over the full Python scope and save a receipt containing `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-lint.md`; acceptance is zero errors, otherwise correct and restart Phase 9 at P9-T1.
- [x] [P9-T3] Run repository Pyright over the full Python scope and save a receipt containing `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-types.md`; acceptance is zero errors, otherwise correct and restart Phase 9 at P9-T1.
- [x] [P9-T4] Run full Pytest with repository line and branch coverage, changed-line coverage, and numeric per-file line coverage for `scripts/dev_tools/_parallel_orchestrator_state_completion_receipts.py`, `scripts/dev_tools/_parallel_orchestrator_state_mutation_receipts.py`, `scripts/dev_tools/parallel_codex_readiness_filesystem.py`, `scripts/dev_tools/push_down_codex_routing_merge.py`, `scripts/dev_tools/validate_parallel_codex_readiness.py`, `scripts/dev_tools/parallel_kickoff_contract.py`, `scripts/dev_tools/resolve_codex_deployment.py`, and `scripts/dev_tools/resolve_codex_topology.py`; save a receipt containing `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and numeric `Output Summary:` comparisons to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-tests-coverage.md`; acceptance is all tests passing, repository line coverage at least 85%, repository branch coverage at least 75%, each of the five added files at least 90% line coverage, each of the three modified files individually at or above its own P0-T8 baseline line coverage, and no changed-line regression, otherwise correct and restart Phase 9 at P9-T1.

### Phase 10 — Full PowerShell QA loop

- [x] [P10-T1] Run repository PoshQC formatting over the full PowerShell scope and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-format.txt`; acceptance is success with no file changes, otherwise correct and restart Phase 10 at P10-T1.
- [x] [P10-T2] Run repository PoshQC analysis over the full PowerShell scope and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-analysis.txt`; acceptance is zero errors and no new warnings, otherwise correct and restart Phase 10 at P10-T1.
- [x] [P10-T3] Run full Pester with repository, changed-line, and per-runtime-file attributable line coverage and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/powershell-tests-coverage.txt`; acceptance is all tests passing, repository line coverage at least 85%, the evidence including numeric repository and per-runtime-file line coverage values, each new PowerShell file at least 90% line coverage, all 25 runtime files having attributable line coverage, and no line-coverage regression, while the evidence explicitly records that branch coverage is unsupported by the configured PowerShell tooling and is not treated as PASS; otherwise correct and restart Phase 10 at P10-T1.

### Phase 11 — Full TypeScript QA loop

- [x] [P11-T1] Run the repository formatter over the full TypeScript scope and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-format.txt`; acceptance is success with no file changes, otherwise correct and restart Phase 11 at P11-T1.
- [x] [P11-T2] Run the repository linter over the full TypeScript scope and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-lint.txt`; acceptance is zero errors, otherwise correct and restart Phase 11 at P11-T1.
- [x] [P11-T3] Run the repository TypeScript type checker over the full scope and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-types.txt`; acceptance is zero errors, otherwise correct and restart Phase 11 at P11-T1.
- [x] [P11-T4] Run the full TypeScript test suite with repository, changed-line, and per-file coverage and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/typescript-tests-coverage.txt`; acceptance is all tests passing, repository line coverage at least 85% and branch coverage at least 75%, all five modified files restored to baseline or higher, and no changed-line regression, otherwise correct and restart Phase 11 at P11-T1.

### Phase 12 — Full Bash QA loop

- [x] [P12-T1] Run `bash scripts/bash/shell-qc.sh format` under WSL from the repository root and save a receipt containing `Timestamp:`, the exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/bash-format.md`; acceptance is exit code zero with no file rewrite in the final pass, and any failure or file rewrite requires correction and a restart of the Bash loop from P12-T1.
- [x] [P12-T2] After P12-T1 succeeds without a file rewrite, run `bash scripts/bash/shell-qc.sh check` under WSL from the repository root and save a receipt containing `Timestamp:`, the exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/bash-check.md`; acceptance is exit code zero with shfmt diff-clean and zero shellcheck errors in the same uninterrupted pass, and any failure or file change requires correction and a restart of the Bash loop from P12-T1.
- [x] [P12-T3] After P12-T2 succeeds, run `bash -lc "SHELL_QC_KCOV_OUT_DIR='docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/bash-kcov' bash scripts/bash/shell-qc.sh test --coverage"` under WSL from the repository root and save a receipt containing `Timestamp:`, the exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/bash-tests-coverage.md`; acceptance is exit code zero, all Bats tests passing, numeric `Bash coverage (lines)` at least 85% with no regression from P0-T11, the receipt explicitly recording that configured kcov branch coverage is unsupported and is not treated as passing, and Cobertura output retained below the named canonical `evidence/qa-gates/bash-kcov/` path, while any failure or file change requires correction and a restart of the Bash loop from P12-T1.

## R5 documentation correction contract

The R5 correction uses two ordered batches and no new production or test file. Batch A owns `scripts/dev_tools/_parallel_orchestrator_state_resume_truth.py` (285 pre-edit lines; 11 incomplete callable contracts including the missing nested `ordering@120` docstring; 14 missing immediate loop/comprehension intent comments), `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py` (318 pre-edit lines; 12 incomplete callable contracts; 17 missing immediate loop/comprehension intent comments), `tests/scripts/dev_tools/test_parallel_resume_truth.py` (243 lines, read-only), and `tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py` (286 lines, read-only). Batch B owns `scripts/dev_tools/validate_parallel_codex_readiness.py` (493 pre-edit lines; 12 incomplete callable contracts; missing immediate comments for the list comprehensions at audit lines 160 and 271), and `tests/scripts/dev_tools/test_validate_parallel_codex_readiness.py` (447 lines, read-only). The generator guard at readiness audit line 231 is a policy false positive: it must be reported through the exact adjudication below and must not be changed solely for R5.

Both red and green policy checks must execute the following exact inline, read-only checker body through `poetry run python -`; writing this checker to disk or creating a temporary file is prohibited. The invocation supplies production paths as positional arguments, focused test owners as `--owner=<path>`, and `--allow-readiness-guard-adjudication` only for Batch B. Each receipt must record the complete expanded command, not only the `$R5Checker` variable name.

```powershell
$R5Checker = @'
import ast
import hashlib
import sys
from pathlib import Path

CONTRACT_FIELDS = ("Args:", "Returns:", "Raises:", "Side Effects:")
ADJACENCY_NODES = (
    ast.For,
    ast.AsyncFor,
    ast.While,
    ast.ListComp,
    ast.DictComp,
    ast.SetComp,
    ast.GeneratorExp,
)
READINESS_PATH = "scripts/dev_tools/validate_parallel_codex_readiness.py"
READINESS_GENERATOR = '(part in (".", "..") for part in path.parts)'


def is_docstring_statement(node: ast.stmt) -> bool:
    return (
        isinstance(node, ast.Expr)
        and isinstance(node.value, ast.Constant)
        and isinstance(node.value.value, str)
    )


class StripDocstrings(ast.NodeTransformer):
    def strip(self, node: ast.AST) -> ast.AST:
        self.generic_visit(node)
        body = getattr(node, "body", None)
        if isinstance(body, list) and body and is_docstring_statement(body[0]):
            body.pop(0)
        return node

    visit_Module = strip
    visit_ClassDef = strip
    visit_FunctionDef = strip
    visit_AsyncFunctionDef = strip


paths: list[str] = []
owners: list[str] = []
allow_guard = False
for argument in sys.argv[1:]:
    if argument.startswith("--owner="):
        owners.append(argument.removeprefix("--owner="))
    elif argument == "--allow-readiness-guard-adjudication":
        allow_guard = True
    else:
        paths.append(argument)

findings: list[str] = []
guard_seen = False
for path_text in paths:
    path = Path(path_text)
    source = path.read_text(encoding="utf-8")
    source_lines = source.splitlines()
    tree = ast.parse(source, filename=path_text)
    parents = {
        child: parent
        for parent in ast.walk(tree)
        for child in ast.iter_child_nodes(parent)
    }
    callables = sorted(
        (
            node
            for node in ast.walk(tree)
            if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))
        ),
        key=lambda node: (node.lineno, node.col_offset),
    )
    contract_failures = 0
    for node in callables:
        docstring = ast.get_docstring(node, clean=False) or ""
        missing = [field for field in CONTRACT_FIELDS if field not in docstring]
        if missing:
            contract_failures += 1
            findings.append(
                f"CALLABLE {path_text}:{node.name}@{node.lineno} missing={','.join(missing)}"
            )

    adjacency_nodes = sorted(
        (node for node in ast.walk(tree) if isinstance(node, ADJACENCY_NODES)),
        key=lambda node: (node.lineno, node.col_offset, type(node).__name__),
    )
    adjacency_failures = 0
    adjudicated = 0
    for node in adjacency_nodes:
        segment = " ".join((ast.get_source_segment(source, node) or "").split())
        parent = parents.get(node)
        is_guard = (
            path.as_posix() == READINESS_PATH
            and isinstance(node, ast.GeneratorExp)
            and segment == READINESS_GENERATOR
            and isinstance(parent, ast.Call)
            and isinstance(parent.func, ast.Name)
            and parent.func.id == "any"
            and len(parent.args) == 1
            and parent.args[0] is node
            and not parent.keywords
        )
        if is_guard and allow_guard:
            guard_seen = True
            adjudicated += 1
            print(
                "ADJUDICATED "
                f"{path_text}:audit-line-231/current-line-{node.lineno} GeneratorExp "
                "reason=trivial generator guard is a policy false positive; no R5 change authorized"
            )
            continue
        previous = source_lines[node.lineno - 2].strip() if node.lineno > 1 else ""
        if not previous.startswith("#") or not previous.removeprefix("#").strip():
            adjacency_failures += 1
            findings.append(
                f"ADJACENCY {path_text}:{type(node).__name__}@{node.lineno} missing=immediate-intent-comment"
            )

    semantic_tree = StripDocstrings().visit(ast.parse(source, filename=path_text))
    semantic_dump = ast.dump(ast.fix_missing_locations(semantic_tree), include_attributes=False)
    semantic_sha = hashlib.sha256(semantic_dump.encode("utf-8")).hexdigest().upper()
    print(
        f"SUMMARY {path_text} callables={len(callables)} "
        f"contract_failures={contract_failures} adjacency_nodes={len(adjacency_nodes)} "
        f"adjacency_failures={adjacency_failures} adjudicated={adjudicated}"
    )
    print(f"SEMANTIC_SHA256 {path_text} {semantic_sha}")

for owner_text in owners:
    owner = Path(owner_text)
    owner_sha = hashlib.sha256(owner.read_bytes()).hexdigest().upper()
    print(f"OWNER_SHA256 {owner_text} {owner_sha}")

if allow_guard and not guard_seen:
    findings.append("ADJUDICATION readiness audit-line-231 guard was not found exactly")
for finding in findings:
    print(f"FAIL {finding}")
raise SystemExit(1 if findings else 0)
'@
```

R5 execution authorization is exact. P13-T8 through P13-T24 may change only the three production paths named above and the following evidence: `evidence/regression-testing/r5-documentation-batch-a-red.md`, `evidence/regression-testing/r5-documentation-batch-b-red.md`, `evidence/qa-gates/r5-documentation-batch-a-green.md`, `evidence/qa-gates/r5-documentation-batch-b-green.md`, `evidence/qa-gates/r5-documentation-batch-a-line-counts.md`, `evidence/qa-gates/r5-documentation-batch-b-line-counts.md`, `evidence/other/r5-documentation-batch-a-diff.md`, `evidence/other/r5-documentation-batch-b-diff.md`, `evidence/qa-gates/python-format-r5-refresh.md`, `evidence/qa-gates/python-lint-r5-refresh.md`, `evidence/qa-gates/python-types-r5-refresh.md`, `evidence/qa-gates/python-tests-coverage-r5-refresh.md`, `evidence/qa-gates/python-coverage-r5-refresh.json`, and currency updates to `evidence/qa-gates/index.md`, `evidence/qa-gates/line-counts.txt`, and `evidence/qa-gates/validators.txt`. P13-T25 through P13-T28 additionally authorize only `evidence/qa-gates/remediation-traceability.md`, `issue.md`, `spec.md`, `user-story.md`, `evidence/qa-gates/final-diff-inventory.txt`, and `evidence/other/executor-to-orchestrator-handback.md`, and each acceptance-criteria source remains subject to P13-T26's proof requirement. All abbreviated paths in this paragraph are relative to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/`. The three focused test owners are read-only and must remain byte-identical to the `OWNER_SHA256` values captured by their batch red receipt. No suppression, dependency, public behavior/API change, semantic test rewrite, or production/test-file creation is authorized. The sole temporary-file exception is the exact repo-root coverage-tool data file `.coverage-python-r5-refresh`, authorized only during P13-T18 through P13-T21; it does not authorize a test-created temporary file or any other temporary path. Before the restarted loop, resolve the exception to an absolute path, prove its parent is the repository root, and require pre-absence; a present file may be deleted only when the P13-T21 receipt proves it is the exact file created by the same R5 loop attempt, while an unrelated or unproven pre-existing file fails closed without deletion. The coverage data path and canonical `evidence/qa-gates/python-coverage-r5-refresh.json` path must remain distinct after absolute-path normalization. Record the exact `COVERAGE_FILE` value and every command that creates, reads, reports from, or deletes the coverage data. After report and JSON reconciliation, or immediately after any failure, delete only the proven same-attempt `.coverage-python-r5-refresh` and verify its absence before checkoff, restart, or advancement; no temporary coverage data is an evidence artifact. If Black leaves any owned production or test file above 500 lines, execution fails closed; extracting a module requires a separately authorized fresh plan revision.

### Phase 13 — Integration evidence and orchestrator handback

- [x] [P13-T1] Run the complete repository QA command set for Python, PowerShell, TypeScript, and Bash in policy order and write exact commands, exit codes, timestamps, and artifact paths to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/index.md`, explicitly indexing `evidence/qa-gates/bash-format.md`, `evidence/qa-gates/bash-check.md`, `evidence/qa-gates/bash-tests-coverage.md`, and `evidence/qa-gates/bash-kcov/`; acceptance is one uninterrupted green pass per language after its last file change, numeric line-coverage results for every language, numeric aggregate branch-coverage results for Python and TypeScript, and explicit unsupported/not-PASS branch-coverage status for PowerShell and Bash in the integration index.
- [x] [P13-T2] Run the final line-count gate across every changed production, test, and reusable script file and save output to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/line-counts.txt`; acceptance is every file at or below 500 lines.
- [x] [P13-T3] Create `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/research/2026-08-10T20-10-codex-native-parallel-orchestration-research.md` as an exact-byte copy of the legacy source file named `2026-08-10T20-10-codex-native-parallel-orchestration-research.md` directly under `artifacts/research/`, without modifying or deleting the source; acceptance is both files existing with SHA-256 `A4C3BAF2C50ADFDF59228C24CF3912D7E6C0CACC7A356D12D8CF54F0B3E669EA` and byte-for-byte equality.
- [x] [P13-T4] Update exactly the four old-path references: the Markdown link target and displayed path in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/plan.2026-08-10T20-25.md`, that plan's PC-06 prose reference, and the `research` and `skill_research_completion.evidence` values in `artifacts/orchestration/orchestrator-state.json`; acceptance is all four values naming `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/research/2026-08-10T20-10-codex-native-parallel-orchestration-research.md`, with the Markdown link using the valid relative target `research/2026-08-10T20-10-codex-native-parallel-orchestration-research.md`, and no other checkpoint field or plan content changing.
- [x] [P13-T5] Search the complete tracked and ignored repository scope for the exact legacy reference formed by concatenating `artifacts/research/` and `2026-08-10T20-10-codex-native-parallel-orchestration-research.md`, verify the P13-T3 target still has SHA-256 `A4C3BAF2C50ADFDF59228C24CF3912D7E6C0CACC7A356D12D8CF54F0B3E669EA`, and verify the four P13-T4 references resolve to the target; acceptance is zero old-path references and all target/reference checks passing before deletion is authorized.
- [x] [P13-T6] Delete only the legacy source file defined in P13-T3 after P13-T5 passes; acceptance is the old ignored source absent, the canonical target present with SHA-256 `A4C3BAF2C50ADFDF59228C24CF3912D7E6C0CACC7A356D12D8CF54F0B3E669EA`, no unrelated file changed by P13-T3 through P13-T6, and no `.claude/**` change.
- [x] [P13-T7] Rerun only the evidence-location validator that failed P13-T3, both original-plan and remediation-plan artifact validators, strict orchestrator-checkpoint topology/model-routing validation, and the remaining P13-T3 repository-policy validators that have not already passed; append exact commands, timestamps, integer exit codes, and output summaries to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/validators.txt`; acceptance is every named validator exiting successfully without rerunning already-green source/bundle parity, topology, prompt/permission, or language QA gates.
- [x] [P13-T8] [expect-fail] Before any Batch A edit, run the exact R5 inline checker for `scripts/dev_tools/_parallel_orchestrator_state_resume_truth.py` and `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py` with `tests/scripts/dev_tools/test_parallel_resume_truth.py` and `tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py` supplied as owners; write a receipt containing `Timestamp:`, the complete exact `Command:`, integer `EXIT_CODE: 1`, and an attributable `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/r5-documentation-batch-a-red.md`; acceptance is an exact inventory of 11 and 12 callable-contract failures respectively, explicit identification of missing nested `ordering@120` documentation, 14 and 17 adjacency failures respectively, production semantic digests, both owner byte digests, and no file modification.
- [x] [P13-T9] Change only docstrings and immediate intent comments in `scripts/dev_tools/_parallel_orchestrator_state_resume_truth.py` and `scripts/dev_tools/_parallel_orchestrator_state_receipt_cohort.py`: give all 23 audited callables, including the nested `ordering` callable, explicit purpose plus `Args:`, `Returns:`, `Raises:`, and `Side Effects:` contracts, and add exactly the 31 audited immediate intent comments; acceptance is no executable statement, signature, public API, focused test, suppression, dependency, or other file changing.
- [x] [P13-T10] Run Black, Ruff, Pyright, the focused Pytest owners `tests/scripts/dev_tools/test_parallel_resume_truth.py` and `tests/scripts/dev_tools/test_parallel_receipt_bound_cohort.py`, then the exact R5 inline checker for Batch A in that order, restarting from Black after any failure or file change; write each exact command, integer exit code, numeric test result, checker summary, semantic digests, and owner byte digests to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/r5-documentation-batch-a-green.md`; acceptance is one uninterrupted pass, zero contract failures, zero adjacency failures, unchanged production semantic digests from P13-T8, and unchanged focused-test owner digests from P13-T8.
- [x] [P13-T11] Count lines in the two Batch A production paths and two focused test owners after the clean Black pass; write `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and all four numeric counts to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/r5-documentation-batch-a-line-counts.md`; acceptance is every file at or below 500 lines.
- [x] [P13-T12] Inspect the Batch A diff and compare the P13-T8/P13-T10 semantic and owner digests; write the exact diff command, changed-path inventory, and digest comparison to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/r5-documentation-batch-a-diff.md`; acceptance is only the two Batch A production files changing, every executable AST remaining identical after docstring removal, both focused tests remaining byte-identical, and every added comment explaining intent rather than narrating syntax.
- [x] [P13-T13] [expect-fail] After Batch A passes and before any Batch B edit, run the exact R5 inline checker for `scripts/dev_tools/validate_parallel_codex_readiness.py` with `tests/scripts/dev_tools/test_validate_parallel_codex_readiness.py` supplied as owner and `--allow-readiness-guard-adjudication`; manually record the checker adjudication for the audit-line-231 generator guard and write `Timestamp:`, the complete exact `Command:`, integer `EXIT_CODE: 1`, and an attributable `Output Summary:` to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/regression-testing/r5-documentation-batch-b-red.md`; acceptance is exactly 12 callable-contract failures, 16 total iteration nodes, exactly two actionable adjacency failures for the list comprehensions at audit lines 160 and 271, and exactly one non-failing false-positive adjudication for the exact audit-line-231 GeneratorExp only after its parent is verified as a sole-argument `any(...)` call, plus a production semantic digest, the owner byte digest, and no file modification.
- [x] [P13-T14] In `scripts/dev_tools/validate_parallel_codex_readiness.py`, replace exactly the 12 audited one-line docstrings line-for-line with compact complete contract text containing purpose plus `Args:`, `Returns:`, `Raises:`, and `Side Effects:`, and add only two immediate intent-comment lines for the list comprehensions at audit lines 160 and 271; acceptance is no change to the audit-line-231 generator guard, no other added line, no executable statement, signature, public API, focused test, suppression, dependency, or other file changing, and a pre-Black line count of 495.
- [x] [P13-T15] Run Black, Ruff, Pyright, focused Pytest `tests/scripts/dev_tools/test_validate_parallel_codex_readiness.py`, then the exact R5 inline checker with the readiness guard adjudication in that order, restarting from Black after any failure or file change; write each exact command, integer exit code, numeric test result, checker summary, manual audit-line-231 adjudication, semantic digest, and owner byte digest to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/r5-documentation-batch-b-green.md`; acceptance is one uninterrupted pass, zero contract failures, 16 total iteration nodes, zero actionable adjacency failures, exactly one parent-verified sole-argument-`any(...)` GeneratorExp adjudication, the production semantic digest matching P13-T13, and the focused-test owner digest matching P13-T13.
- [x] [P13-T16] Count lines in the Batch B production path and focused test owner after the clean Black pass; write `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and both numeric counts to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/r5-documentation-batch-b-line-counts.md`; acceptance is both files at or below 500 lines, with any over-limit result failing closed before P13-T17 and without module extraction.
- [x] [P13-T17] Inspect the Batch B diff and compare the P13-T13/P13-T15 semantic and owner digests; write the exact diff command, changed-path inventory, line-delta inventory, and digest comparison to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/r5-documentation-batch-b-diff.md`; acceptance is only `scripts/dev_tools/validate_parallel_codex_readiness.py` changing, its executable AST remaining identical after docstring removal, its focused test remaining byte-identical, exactly 12 line-neutral docstring replacements plus two immediate intent comments, the audit-line-231 generator guard remaining unchanged, and both Batch B checker receipts containing exactly one parent-verified sole-argument-`any(...)` GeneratorExp adjudication.
- [ ] [P13-T18] Because Python changed after completed P9, restart the full Python loop by resolving the repository root and exact `.coverage-python-r5-refresh` path, enforcing the R5 exception's containment, provenance, and pre-absence rules, then running repository Black over the complete Python scope; replace the prior receipt with `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-format-r5-refresh.md`; acceptance is the coverage data absent before Black and exit code zero with no file change in the accepted pass, otherwise correct within the R5 allowlist, enforce exact-file cleanup when authorized, and restart at P13-T18.
- [ ] [P13-T19] After P13-T18 passes without a file change, run repository Ruff over the complete Python scope and replace the prior receipt with `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-lint-r5-refresh.md`; acceptance is zero errors, otherwise correct within the R5 allowlist, enforce exact-file cleanup when authorized, and restart at P13-T18.
- [ ] [P13-T20] After P13-T19 passes, run repository Pyright over the complete Python scope and replace the prior receipt with `Timestamp:`, exact `Command:`, integer `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-types-r5-refresh.md`; acceptance is zero errors, otherwise correct within the R5 allowlist, enforce exact-file cleanup when authorized, and restart at P13-T18.
- [ ] [P13-T21] Retain in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-tests-coverage-r5-refresh.md` the invalid-attempt facts that 3,963 tests passed with 5 skipped and terminal coverage printed, but `COVERAGE_FILE` collided with the canonical JSON target, the report failed with `no such table: tracer`, the terminal process exit code zero was rejected, the authorized JSON was absent, and no extra artifact remained. After P13-T18 through P13-T20 pass again, set `COVERAGE_FILE` only to the absolute repo-root `.coverage-python-r5-refresh`, prove it is distinct from the canonical JSON path, and run full Pytest with repository line and branch coverage, changed-line coverage, and numeric per-file coverage; record the exact environment, every command and individual exit code, and output summary in that receipt, and write machine-readable results only to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/python-coverage-r5-refresh.json`. Parse the canonical JSON and reconcile its numeric results with the terminal report before deleting only the proven same-attempt coverage data file; on any command, report, parse, reconciliation, or gate failure, treat the attempt as failed regardless of a later zero process status, delete only that exact proven same-attempt data file, verify post-absence, and restart at P13-T18. Acceptance is every command exiting zero without a masked earlier failure, all tests passing, the JSON existing and parsing, terminal and JSON results agreeing, repository line coverage at least 85%, repository branch coverage at least 75%, the five original added Python coverage owners remaining at least 90%, `parallel_kickoff_contract.py`, `resolve_codex_deployment.py`, and `resolve_codex_topology.py` remaining at or above their P0-T8 per-file baselines, each of the three R5 documentation owners remaining at or above its pre-R5 full-coverage value in `evidence/qa-gates/python-coverage.json`, no changed-line regression, no data/report path collision, and `.coverage-python-r5-refresh` absent before checkoff or P13-T22.
- [ ] [P13-T22] Refresh `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/index.md` to supersede the completed P13-T1 Python entries with P13-T18 through P13-T21 while retaining the already-green PowerShell, TypeScript, and Bash command/evidence entries unchanged; acceptance is current Python commands, exit codes, coverage values, and artifact paths being indexed without rerunning or altering PowerShell, TypeScript, or Bash QA evidence.
- [ ] [P13-T23] Rerun the final line-count gate after R5 across every changed production, test, and reusable script file and update `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/line-counts.txt`; acceptance is every file at or below 500 lines, the three R5 production owners and three read-only focused test owners being listed numerically, and no module extraction or new production/test file.
- [ ] [P13-T24] Rerun only the remediation-plan validator, evidence-location validator, Python documentation/suppression/test-location policy checks, and repository-policy validators made stale by P13-T8 through P13-T23, appending exact commands, timestamps, integer exit codes, and output summaries to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/validators.txt`; acceptance is every named validator exiting successfully with the original plan, strict checkpoint topology/model-routing, source/bundle parity, prompt/permission, and PowerShell/TypeScript/Bash QA gates not rerun because R5 does not invalidate them.
- [ ] [P13-T25] Map every finding from `remediation-inputs.2026-08-12T01-42.md`, including the exact R5 callable/comment inventories and audit-line-231 adjudication, to changed paths, focused and full tests, coverage, and passing evidence in `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/remediation-traceability.md`; acceptance is all findings having complete current evidence and none remaining open.
- [ ] [P13-T26] Update only acceptance-criteria source checkboxes identified in `evidence/baseline/scope.md` that are proven by P13-T1 through P13-T25, leaving every criterion that requires feature re-review or exact-current-head hosted CI unchecked and explicitly deferred to the orchestrator; acceptance is each checked criterion linked to current local evidence, each deferred criterion remaining unchecked, and no unsupported completion claim.
- [ ] [P13-T27] Save the authorized final diff inventory to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/final-diff-inventory.txt`; acceptance is no unauthorized production or test path, no new production/test file, no `.claude/**` change, and no evidence outside the canonical feature evidence tree.
- [ ] [P13-T28] Write `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/other/executor-to-orchestrator-handback.md` and return control to the orchestrator without staging, committing, pushing, refreshing PR context, invoking feature review, updating deferred acceptance criteria, opening or updating a PR, or running hosted CI; acceptance is the handback identifying completed P13-T1 through P13-T27 evidence and explicitly assigning the orchestrator the pre-R4 deterministic staging, configured `drm-copilot` MCP commit-context collection, exact generated commit-steward commit, refreshed PR context, full feature re-review and remediation disposition, PR and exact-current-head hosted CI, deferred acceptance-criteria reconciliation, final strict validation, clean index/worktree/untracked-state verification, and fail-closed adjudication of unsupported PowerShell and Bash branch-coverage evidence.

P13-T3 through P13-T7 authorize changes only to `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/research/2026-08-10T20-10-codex-native-parallel-orchestration-research.md`, `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/plan.2026-08-10T20-25.md`, `artifacts/orchestration/orchestrator-state.json`, the legacy source file named `2026-08-10T20-10-codex-native-parallel-orchestration-research.md` directly under `artifacts/research/`, and `docs/features/active/2026-08-10-codex-native-parallel-orchestration-467/evidence/qa-gates/validators.txt`. If exact-byte equality, the required SHA-256, the exact four-reference update, zero-old-reference search, `.claude/**` invariance, or any named validator cannot be proven, execution fails closed before P13-T8; no deletion, acceptance-criteria update, or handback may be reported as complete.

## Completion condition

Execution under this plan is complete only after every task is checked in order, every reviewed finding is closed by current local evidence, each language has one uninterrupted green full QA and supported coverage pass current to its last in-scope change, all changed files pass the 500-line gate, source/bundle parity and `.claude/**` immutability are verified, canonical validators pass, locally provable acceptance criteria are reconciled, and P13-T28 returns control to the orchestrator. Feature re-review, remediation disposition, PR and exact-current-head hosted CI, deferred acceptance-criteria reconciliation, final strict validation, and clean-tree verification remain orchestrator-owned and incomplete at executor handback.
