Timestamp: 2026-08-28T17-11

# Policy Compliance Audit — issue #575 (release-poll-budgets-unpinned-and-isolation-evidence-proxy-level)

Branch: `bug/release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575-r2`
Work mode: `full-bug` (confirmed by `issue.md:12` — `Work Mode: full-bug`).
AC source: `spec.md` AC1-AC17 only, per work-mode routing.

## Scope Resolution

Local `main` in this worktree is stale: it does not include PR #579 (issue #573), which is already
merged into `origin/main` (`e546e814`). A naive `git diff main` therefore over-attributes 40+ files
from issue #573 to this review. The branch's own merge commit (`f5e64abe`, "Merge remote-tracking
branch 'origin/main'") confirms `origin/main` (`e546e814`) is the correct merge-base; `git
merge-base origin/main HEAD` returns exactly `e546e814`, matching the branch's parent chain. All
findings below are scoped to `git diff origin/main`, which resolves to the 17 files matching the
task description exactly (4 commits, no #573 spillover). This resolution is recorded here rather
than silently applied because it materially changes the diff surface a naive reviewer would audit.

## Rejected Scope Narrowing

None observed. No caller instruction in this task attempted to narrow scope to a plan/task/phase
subset, mark a language "out of scope," or skip a toolchain/coverage check. The task description's
explicit call-outs (Gap 1 test-only, Gap 2 doc-only, AC17 file-untouched verification) are
restatements of the branch's own design constraints, not scope-narrowing instructions, and each was
independently verified against the diff rather than taken on faith.

## Evidence Location Compliance

- `git diff origin/main --name-only | grep -E "^artifacts/(baselines|qa|evidence|coverage)/"` — no matches.
- `python scripts/dev_tools/validate_evidence_locations.py --root .` — exit code 0, no violations reported.
- All evidence in this feature's diff is written under `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/evidence/{baseline,regression-testing,qa-gates}/`, matching the canonical scheme in `evidence-and-timestamp-conventions`.

**Verdict: PASS.**

## Coverage Verification (PowerShell — sole language with changed files in the branch diff)

Changed files in scope for coverage: one new test file
(`tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1`). No production `.ps1`
file was added or modified by this branch (`git diff origin/main --name-status` shows zero entries
under `scripts/dev-tools/` or `scripts/powershell/`). Per `.claude/rules/general-unit-test.md`
("Configure coverage tooling to exclude test files ... so metrics reflect application code, not
tests"), the new file is correctly outside the coverage denominator, and there is no "new production
file" or "modified production file" coverage gate to apply — only the repo-wide gate is live for
this diff.

- **Coverage artifact:** `artifacts/pester/powershell-coverage.xml` exists locally (gitignored, regenerated by the toolchain run; confirmed present at time of this audit).
- **Repo-wide line coverage:** independently re-derived from the XML's root `<counter type="LINE" missed="403" covered="7236" />` = 7236/7639 = 94.72%. This matches the evidence artifact `evidence/qa-gates/final-test-coverage.2026-08-28T21-25.md` digit-for-digit. 94.72% >= 85% (uniform threshold, `.claude/rules/quality-tiers.md`) and >= 80% (Verification Procedure floor). **PASS.**
- **`scripts/dev-tools/Invoke-ReleaseTagPush.ps1` (the file the new test exercises):** independently re-derived from the XML's sourcefile-level `<counter type="LINE" missed="2" covered="75" />` = 75/77 = 97.40%, identical to both the recorded baseline (P0-T8, before this fix) and the post-change figure — zero regression. **PASS.**
- **Branch coverage:** not evaluated for PowerShell. Pester measures command (instruction) and line coverage only; per `.claude/rules/powershell.md` and the coverage-verification instructions, no branch threshold applies to PowerShell and an absent branch figure is not a FAIL condition.

**PowerShell coverage verdict: PASS** (repo-wide 94.72%, changed-file 97.40%, no regression, artifact present and independently cross-checked against raw XML rather than taken on the evidence file's word).

No other language has changed files in this branch diff (`git diff origin/main --name-status` shows only `.ps1`/`.Tests.ps1` and Markdown files). Python, TypeScript, and C# coverage gates are not applicable and are not evaluated — this is a legitimate `zero changed files` exclusion, not a narrowing.

## Policy Reading Order and Rule Applicability

Applicable per files in scope: `CLAUDE.md`, `general-code-change.md`, `general-unit-test.md`,
`powershell.md`. No Python/TypeScript/C# rule applies (no changed files in those languages). No
`ci-workflows.md` finding applies — `.github/workflows/publish-mcp-npm.yml` is confirmed untouched
(see AC17 section below). No `benchmark-baselines.md` finding applies — no benchmark baseline files
are touched. `orchestrator-state.md` and `parallel-orchestration.md` do not apply — this diff (scoped
to `origin/main`) touches no orchestrator-state or parallel-manifest artifact.

### General Code Change Policy (`.claude/rules/general-code-change.md`)

- **File size limit (500 lines):** new file `tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` is 79 lines (`wc -l`, independently counted). **PASS.**
- **I/O boundaries / design seams:** the new test uses the established wrapper-function seam pattern (`Invoke-GitExe`, `Invoke-NpmExe`) already present in the sibling file; no new seam introduced. **PASS.**
- **No production `.ps1` file changed:** confirmed by `git diff origin/main --name-status` — zero entries under `scripts/dev-tools/`. **PASS.**

**Verdict: PASS.**

### General Unit Test Policy (`.claude/rules/general-unit-test.md`)

- **Independence/Isolation:** three `It` blocks, each self-contained via `BeforeEach` mock reset; no shared mutable state across tests. **PASS.**
- **Fast execution / determinism:** `Invoke-Sleep` is mocked as a no-op; no `Start-Sleep`, no real network, no real `npm`/`git`/`gh` process (all behind the existing wrapper seams). Regression evidence (`evidence/regression-testing/gap1-new-test-pass.2026-08-28T20-55.md`) shows the 3-test file completing in 1.17s. **PASS.**
- **Readability:** `It` names are descriptive and map to check (a)/(b)/(c); a `Context`-level comment explains why `Invoke-TagPublishVerification` is deliberately left unmocked. **PASS.**
- **Coverage Exclusion Policy:** the new file is a test file under `tests/`, not a production path; it is correctly outside coverage measurement, and no production file was excluded. **PASS.**
- **Test File Location:** `tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` mirrors `scripts/dev-tools/Invoke-ReleaseTagPush.ps1`'s location, matching the mandated `tests/<mirror>` layout; filename carries the `.Tests.ps1` suffix. **PASS.**
- **External Dependencies:** `Test-Path`, `Get-NpmVersion`, `Invoke-GitExe`, `Invoke-Sleep`, and `Invoke-NpmExe` are all mocked; `Invoke-TagPublishVerification`, `Wait-ForWorkflowRun` (per-`It` filtered), `Test-PublishStepConclusion` (per-`It` filtered), and `Test-NpmVersionResolved` are left real, consistent with "mock sparingly; prefer real code paths" (`.claude/rules/powershell.md`). No temporary files used. **PASS.**

**Verdict: PASS.**

### PowerShell Rule (`.claude/rules/powershell.md`)

- **Mock signature parity:** `Test-Path -MockWith { param($LiteralPath) ... }` and `Get-NpmVersion -MockWith { param([string]$ManifestPath) ... }` match the production call sites' named parameters (`Test-Path -LiteralPath ...`, `Get-NpmVersion -ManifestPath ...`, confirmed by reading `scripts/dev-tools/Invoke-ReleaseTagPush.ps1:78-106,170-176`). This mocking pattern is byte-identical to the established convention already used 10+ times in the sibling file `Invoke-ReleaseTagPush.Tests.ps1`. **PASS.**
- **External executable mocking:** no direct mock of `git`, `npm`, or `gh`; only wrapper functions are mocked. **PASS.**
- **Toolchain order (format → analyze → test):** evidence shows format (`final-format.2026-08-28T21-15.md`, exit 0, 0 rewrites across 422 files), analyze (`final-analyze.2026-08-28T21-17.md`, exit 0, zero findings), and test-with-coverage (`final-test-coverage.2026-08-28T21-25.md`, exit 0, 3840 passed / 0 failed) run in that order, each converging in a single pass with no auto-fixes. **PASS.**
- **Deterministic test requirements:** no network access, no ambient PATH/profile dependency introduced; the raw-socket probe (Gap 2, `P2-T1`) is evidence-collection tooling run outside the Pester suite, not a unit test, so it is not subject to the Pester determinism rules. **PASS.**
- **Change budget:** this batch adds one test file and zero production files — well within the 2-production/3-test-file batch caps; no `powershell-orchestrator` routing was required. **PASS.**

**Verdict: PASS.**

### Quality Tiers (`.claude/rules/quality-tiers.md`)

`quality-tiers.yml` does not exist at the repository root in this worktree (confirmed: `quality-tiers.yml: No such file or directory`). This is a pre-existing, previously-documented gap (recorded in the #526 policy-audit as "the absent `quality-tiers.yml` remains unfiled here" and explicitly called out as investigated-and-excluded in this feature's own `spec.md` "Explicitly excluded systems" list). It is not introduced or worsened by this branch, and this branch adds no new project requiring classification. **Not a Blocking finding for this review; recorded as a known, pre-existing, out-of-scope gap.**

### CI Workflow / Benchmark-Baseline Rules

Not applicable — no workflow file and no benchmark-baseline file is touched by this diff.

## AC17 File-Untouched Verification (independent re-check)

Re-ran independently of the evidence artifact:
```
git diff origin/main --name-only -- scripts/dev-tools/Invoke-ReleaseVerification.ps1 scripts/dev-tools/Invoke-ReleaseVerificationHelpers.ps1 .github/workflows/publish-mcp-npm.yml
```
Empty output — confirms all three AC17-protected files are untouched. The feature's own evidence artifact (`evidence/qa-gates/diff-scope-verification.2026-08-28T21-30.md`) used local `main` for the same check; local `main` differs from `origin/main` only by files unrelated to these three paths (the #573 PR), so both bases agree on this specific result. **PASS.**

## Gap 2 (append-only correction) — Policy Check

- `git diff origin/main -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` shows a pure addition (`+` lines only) appended after the file's existing final line; no `-` lines. **Genuinely append-only. PASS.**
- Line 417 (AC21's original sentence and checkbox) read directly from the post-change file: byte-for-byte identical to the pre-change text quoted in the correction section itself and to the #526 feature-audit's own quotation. **PASS.**
- The correction's factual claims are backed by a freshly recorded, independently reproducible probe (`evidence/qa-gates/proxy-level-isolation-correction.2026-08-28T21-05.md`), which in turn cross-references and reproduces the #526 reaudit's own R4 probe finding (`docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/policy-audit.2026-08-26T04-33.md:395-406`, confirmed to exist and contain the matching `RAW_SOCKET_CONNECTED=TRUE` finding at that exact location). The correction is not an unsupported assertion. **PASS.**

**Verdict: PASS.**

## Overall Policy Audit Verdict

**PASS.** Zero Blocking findings. One informational, pre-existing, out-of-scope note (absent `quality-tiers.yml`, not introduced by this branch).
