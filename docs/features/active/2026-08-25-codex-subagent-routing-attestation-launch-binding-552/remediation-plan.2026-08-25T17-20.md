# Issue #552 — CI Remediation Plan

- **Requirements source:** `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/remediation-inputs.2026-08-25T17-20.md`
- **Confirmed blocking CI finding:** GitHub Actions job `97971527109` failed `tests/scripts/dev_tools/test_poshqc_bundled_parity.py::test_poshqc_bundled_module_files_match_repo_root_sources` because `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` is not identical to `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`.
- **Permitted production change:** Synchronize only `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` from `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` by an encoding-preserving byte copy.
- **Prohibited changes:** Do not modify `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, any test, policy, routing data, Issue #552 implementation file, unrelated PoshQC file, or `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/plan.2026-08-25T14-58.md`. Do not delete, edit, or bypass `.codex/state/**` data except the two P0-T6-verified ignored batch counters and the then-empty `.codex/state` directory.
- **Evidence rule:** Write each artifact below `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/<kind>/` with an ISO-8601 `yyyy-MM-ddTHH-mm` filename timestamp. Every command artifact must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Pester artifacts must additionally record Pester counts and numeric line coverage.
- **Automation boundary:** The executor must perform every task without manual validation. This plan does not authorize commit, push, CI monitoring, source changes, test changes, policy changes, or `.codex/state/**` changes other than the evidenced P0-T6 counter transition.

### Phase 0 — Policy Reads and Remediation Baseline

- [x] [P0-T1] Read `AGENTS.md` (standing instructions), `AGENTS.md` (General Code Change Policy), `AGENTS.md` (General Unit Test Policy), `.agents/skills/powershell/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/issue.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/spec.md`, and `.agents/skills/acceptance-criteria-tracking/SKILL.md` in that order; record the actual order and every path read in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/phase0-instructions-read.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Policy Order:`, and the complete ordered path list; it identifies `spec.md` as the full-bug acceptance-criteria source and reports every listed acceptance criterion as already complete without modifying the requirement files.
- [x] [P0-T2] Capture the pre-remediation SHA-256 values, byte lengths, and `git diff --no-index` exit codes for `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, `scripts/powershell/PoshQC/settings/pssa.settings.psd1`, and `extensions/drm-copilot/resources/powershell/PoshQC/settings/pssa.settings.psd1`. In the same artifact, capture a reproducible baseline manifest of every file under `tests/`, `.agents/skills/`, and `.codex/state/`, plus `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/plan.2026-08-25T14-58.md`, recording each path’s SHA-256 and byte length; also record `git status --porcelain=v1 --untracked-files=all`. Write the result to `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/poshqc-settings-parity-before.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; records the Pester source/mirror difference and missing Issue #552 coverage-path entry, records baseline hashes and comparison exit codes for both protected PSSA settings files, and contains the complete prohibited-path and worktree-status baseline required by P1-T3 and P2-T5.
- [x] [P0-T3] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_poshqc_bundled_parity.py::test_poshqc_bundled_module_files_match_repo_root_sources`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/poshqc-bundled-parity-fail-before.<yyyy-MM-ddTHH-mm>.md` with `ExpectedExitCode: 1`, CI job `97971527109`, and the failure diagnosis. Acceptance: the recorded failing assertion names the source and mirror settings files; no tracked, production, test-source, or protected-path file is modified. The test may create or refresh only the ignored generated bytecode file `tests/scripts/dev_tools/__pycache__/test_poshqc_bundled_parity.cpython-313-pytest-9.0.2.pyc`; when present, record its SHA-256 and byte length in this evidence, and require SHA-256 `6E9EF900125750B31D1E9E83584CE465DF2912629E50925EC5070A308B032CC5` and byte length `4400`; any other path change or bytecode value is `REMEDIATION_REQUIRED`.
- [x] [P0-T4] Run `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the active worktree and `scan_folders` set only to `extensions/drm-copilot/resources/powershell/PoshQC/settings`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/powershell-format.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` is recorded and the mirror file is reported unchanged; if the command changes the mirror or fails, do not mark this task complete and rerun this task before P0-T5.
- [x] [P0-T5] Run `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root` set to the active worktree and `scan_folders` set only to `extensions/drm-copilot/resources/powershell/PoshQC/settings`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/powershell-analyze.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the analyzer diagnostic count are recorded.
- [x] [P0-T6] Immediately before the next full Pester gate, re-resolve and reverify only `.codex/state/powershell-batch-budget.01a03a40-eb5b-7e63-9c04-94ca4f0590d0.json` (SHA-256 `C936BE085A62DC4C5567A766486B5CBC436A85794B2F36899F4A8A5B74AE50C9`; one production path `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; one test path `tests/scripts/codex-hooks/model-profile-attestation.Tests.ps1`) and `.codex/state/python-batch-budget.01a03a40-eb5b-7e63-9c04-94ca4f0590d0.json` (SHA-256 `915C1DC9B190656F8617CEF0373C545986F6B7EE17502A07ADBD3CCF2657CD7B`; no production paths; test paths `tests/scripts/dev_tools/test_resolve_codex_deployment.py` and `tests/scripts/dev_tools/test_codex_agent_wrapper_contracts.py`). Verify `.codex/state` contains no authority-store, attestation, or routing-authority file. Only when every path, JSON content, and hash match, delete exactly those two files with `Remove-Item -LiteralPath`; remove `.codex/state` only when a post-delete `Get-ChildItem -Force` proves it is empty. Record the complete transition in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/batch-counter-transition.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the evidence contains `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, both pre-delete paths/content/hash values, the authority-store search scope/result, the two deletions, and the empty-directory result; any mismatch, additional state file, or authority-store fails closed without deleting a file.
- [x] [P0-T7] Retain the two existing failed baseline Pester artifacts and do not run another pre-reset baseline. Immediately after P0-T6, run `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the active worktree and no `scan_folders` override so the repository Pester configuration is used; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/powershell-test-after-counter-reset.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0`, Pester pass/fail/skip counts, and numeric line coverage are recorded; if the command fails or changes a tracked or prohibited-path file, restart from P0-T4 formatting without repeating the completed counter transition. Expected ignored Pester outputs below `artifacts/pester/` do not reset the loop; record every such output path and its SHA-256 value in the command evidence.

### Phase 1 — Bounded Mirror Synchronization and Regression Proof

- [x] [P1-T1] Copy `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` directly to `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` using `Copy-Item -LiteralPath` and then compare the two files with SHA-256, byte length, and `git diff --no-index`; record the command and result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/other/poshqc-settings-mirror-sync.<yyyy-MM-ddTHH-mm>.md`. Acceptance: both files have identical SHA-256 and byte length, `git diff --no-index` exits `0`, and the mirror is the only production file written by this task.
- [x] [P1-T2] Run `poetry run pytest tests/scripts/dev_tools/test_poshqc_bundled_parity.py::test_poshqc_bundled_module_files_match_repo_root_sources`; record the passing result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/poshqc-bundled-parity-pass-after.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` is recorded and the test reports the required source-to-mirror parity; no tracked, production, test-source, or protected-path file is modified. The test may create or refresh only the ignored generated bytecode file `tests/scripts/dev_tools/__pycache__/test_poshqc_bundled_parity.cpython-313-pytest-9.0.2.pyc`; when present, record its SHA-256 and byte length in this evidence, and require SHA-256 `6E9EF900125750B31D1E9E83584CE465DF2912629E50925EC5070A308B032CC5` and byte length `4400`; any other path change or bytecode value is `REMEDIATION_REQUIRED`.
- [x] [P1-T3] Compare the Pester source SHA-256 from P0-T2 with its current SHA-256. Recreate and compare the P0-T2 protected-settings manifest for `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `scripts/powershell/PoshQC/settings/pssa.settings.psd1`, and `extensions/drm-copilot/resources/powershell/PoshQC/settings/pssa.settings.psd1`, plus the P0-T2 prohibited-path manifest for `tests/`, `.agents/skills/`, `.codex/state/`, and `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/plan.2026-08-25T14-58.md`; also compare the normalized `git status --porcelain=v1 --untracked-files=all` snapshot, excluding only `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, this remediation plan, files below this feature’s canonical `evidence/` hierarchy, and the P0-T6-authorized removal of the two named counter files plus the then-empty `.codex/state` directory. Treat exactly one additional manifest entry as an allowed generated-test exception only when it is `tests/scripts/dev_tools/__pycache__/test_poshqc_bundled_parity.cpython-313-pytest-9.0.2.pyc` with SHA-256 `6E9EF900125750B31D1E9E83584CE465DF2912629E50925EC5070A308B032CC5` and byte length `4400`; its provenance must be the P0-T3/P1-T2 parity-test commands, and it must remain ignored by Git. Record the comparison in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/other/remediation-scope-check.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the Pester source hash, both protected PSSA settings manifests, and every prohibited-path manifest entry are unchanged from baseline except removal of exactly the two P0-T6-verified counter files and the then-empty directory, plus only the exact generated-test exception above; the normalized worktree-status snapshot contains no other new or modified path and no authority-store exists; otherwise record `REMEDIATION_REQUIRED` and do not proceed to final QA.

### Phase 2 — Final PowerShell QA Loop and End-State Evidence

- [x] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the active worktree and `scan_folders` set only to `extensions/drm-copilot/resources/powershell/PoshQC/settings`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/powershell-format.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` is recorded and the mirror is unchanged; if it changes the mirror or fails, rerun P1-T1 to restore exact source bytes and restart the QA loop at P2-T1.
- [x] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root` set to the active worktree and `scan_folders` set only to `extensions/drm-copilot/resources/powershell/PoshQC/settings`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/powershell-analyze.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the analyzer diagnostic count are recorded; if the command fails, restart the QA loop at P2-T1.
- [x] [P2-T3] Run `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the active worktree and no `scan_folders` override so the repository Pester configuration is used; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/powershell-test-coverage.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0`, Pester pass/fail/skip counts, and numeric line coverage are recorded; if the command fails or changes a tracked or prohibited-path file, restart from P2-T1 formatting. Expected ignored Pester outputs below `artifacts/pester/` do not reset the loop; record every such output path and its SHA-256 value in the command evidence.
- [x] [P2-T4] After the clean P2-T1 through P2-T3 sequence, rerun `poetry run pytest tests/scripts/dev_tools/test_poshqc_bundled_parity.py::test_poshqc_bundled_module_files_match_repo_root_sources` and a SHA-256/byte-length/`git diff --no-index` comparison of the source and mirror; record both checks in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/poshqc-bundled-parity-final.<yyyy-MM-ddTHH-mm>.md`. Acceptance: every check exits `0`, source and mirror remain byte-for-byte identical, and the evidence records CI job `97971527109` as remediated by the bounded mirror synchronization.
- [x] [P2-T5] Re-run the P0-T2 protected-settings and prohibited-path manifest comparison from P1-T3, including both root and mirror `pssa.settings.psd1` files, and record the final result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/remediation-scope-final.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the only production-file delta attributable to this remediation is `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`; both root and mirror PSSA settings files match their P0-T2 protected baseline values; the only permitted runtime-state removal is the two P0-T6-verified counter files and the then-empty `.codex/state` directory; the only permitted additional prohibited-path manifest entry is the ignored P0-T3/P1-T2/P2-T4 generated bytecode file `tests/scripts/dev_tools/__pycache__/test_poshqc_bundled_parity.cpython-313-pytest-9.0.2.pyc` with SHA-256 `6E9EF900125750B31D1E9E83584CE465DF2912629E50925EC5070A308B032CC5` and byte length `4400`; no authority-store exists; all evidence files are under the feature's canonical `evidence/` hierarchy; and no command outcome is recorded as `SKIPPED`.

## 2026-08-25 Commit-Steward Routing Revision

The completed P0-T1 through P2-T5 PoshQC remediation remains authoritative and must not be repeated, altered, or reverted. This revision addresses the later Issue #552 acceptance defect: the orchestration workflow requires the existing logical `commit-steward` agent, but the C3 standalone resolver rejects it because the generated-family inventory omits it. The prior prohibition on routing-data changes is superseded only for the explicit paths in P4-T1 through P4-T3. No task authorizes a commit, push, CI monitoring, policy change, test weakening, or manual validation.

### Phase 3 — Commit-Steward Routing Baseline

- [x] [P3-T1] Read `AGENTS.md` (standing instructions, General Code Change Policy, and General Unit Test Policy), `.agents/skills/python/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/issue.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/spec.md`, and this plan in that order; record the actual order and every path read in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/commit-steward-routing-instructions-read.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Policy Order:`, and the complete ordered path list; it identifies `spec.md` as the full-bug acceptance-criteria source and states that prior completed P0-T1 through P2-T5 are preserved.
- [x] [P3-T2] Capture SHA-256 values and byte lengths for `config/orchestration-routing.json`, `extensions/drm-copilot/resources/config/orchestration-routing.json`, `scripts/dev_tools/resolve_codex_deployment.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, `.codex/agents/commit-steward.toml`, `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/agents/commit-steward.toml`, and `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json`; record absence of the five `commit-steward-{c1,c2,c3,c3-elevated,c4}.toml` profiles in both the repo root and bundled `.codex/agents/` directories. In the same artifact, capture SHA-256 and byte-length manifests for the staged PoshQC mirror and all existing files below this feature’s `evidence/` hierarchy, plus `git status --porcelain=v1 --untracked-files=all`. Write the result to `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/commit-steward-routing-before.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; it proves both routing-config surfaces and both base-agent surfaces are byte-identical, records the absent generated profiles, and establishes the immutable staged remediation/evidence manifest required by P5-T5.
- [x] [P3-T3] [expect-fail] Run `poetry run python -m scripts.dev_tools.resolve_codex_deployment --logical-agent commit-steward --complexity-band C3 --execution-context standalone --orchestration-complexity-ceiling C3`; record the rejected resolution in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/commit-steward-c3-fail-before.<yyyy-MM-ddTHH-mm>.md` with `ExpectedExitCode: 1`. Acceptance: the evidence contains the exact unsupported-logical-agent diagnostic, the requested standalone C3 inputs, and confirms that no file changed.
- [x] [P3-T4] Run `poetry run pytest tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py --cov=scripts.dev_tools.resolve_codex_deployment --cov=scripts.dev_tools.generate_codex_agent_variants --cov-branch --cov-report=term-missing`; record the baseline in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/commit-steward-routing-python-test-coverage.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, test counts, numeric line and branch coverage for both changed Python modules, and the repository coverage headline; a non-zero result is recorded as a baseline defect and requires the Phase 5 loop to restart from P5-T1.

### Phase 4 — Generated Commit-Steward Profile Support

- [x] [P4-T1] Update `config/orchestration-routing.json` and its byte-identical bundled mirror `extensions/drm-copilot/resources/config/orchestration-routing.json` so `codex_model_policy.generated_agent_families` includes exactly one `commit-steward` entry, retaining order and every unrelated value. Acceptance: both JSON files parse, have identical bytes, and differ from P3-T2 only by the one generated-family entry.
- [x] [P4-T2] Update `scripts/dev_tools/resolve_codex_deployment.py` so `commit-steward` is an accepted generated family and resolves `C3` / `standalone` / `C3` exactly to `commit-steward-c3`, `gpt-5.6-terra`, and `high`, with no overlay. Acceptance: all existing agent-family behavior and unsupported-agent failure behavior remain unchanged except that `commit-steward` is accepted under the generated-family rules.
- [x] [P4-T3] Update `scripts/dev_tools/generate_codex_agent_variants.py` so `commit-steward` belongs to the core generated family set, then run its write mode to synchronize the root and bundled base aliases, all five root and bundled `commit-steward-{c1,c2,c3,c3-elevated,c4}.toml` profiles, and `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json`. Acceptance: every generated profile preserves the canonical commit-steward instructions, has the exact configured model/reasoning fields, the standalone C3 files are Terra/high, and the bundled core manifest lists the base alias and all five generated paths.
- [x] [P4-T4] Extend `tests/scripts/dev_tools/test_resolve_codex_deployment.py` and `tests/scripts/dev_tools/test_codex_model_policy_config_parity.py` with deterministic assertions for the exact standalone `commit-steward-c3` receipt, resolver/config family parity, and byte-identical root/bundled routing configuration. Acceptance: the assertions target the P3-T2 unsupported-family and absent-config-entry defects and pass only when P4-T1 and P4-T2 are complete; no test accepts an alias, fallback model, or elevated profile for standalone C3.
- [x] [P4-T5] Extend `tests/scripts/dev_tools/test_generate_codex_agent_variants.py` with direct assertions that `commit-steward` is a core family and that its base plus all five generated profile files equal the generator output in both root and bundle surfaces and are listed in the core manifest. Acceptance: the regression detects any missing profile, source/bundle drift, stale profile content, or absent manifest entry without writing a temporary file.
- [x] [P4-T6] Run `poetry run python -m scripts.dev_tools.resolve_codex_deployment --logical-agent commit-steward --complexity-band C3 --execution-context standalone --orchestration-complexity-ceiling C3`; record the exact pass-after receipt in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/commit-steward-c3-pass-after.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the JSON receipt identifies logical agent `commit-steward`, deployment agent `commit-steward-c3`, model `gpt-5.6-terra`, reasoning `high`, `c3_overlay_applied: false`, and `c3_overlay_reason: null`.
- [x] [P4-T7] Run `poetry run pytest tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/commit-steward-routing-focused-pytest.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the test result covers exact resolution, configuration parity, generated root/bundle profile parity, and core-manifest membership.
- [x] [P4-T8] Run `poetry run python -m scripts.dev_tools.generate_codex_agent_variants --check`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/commit-steward-routing-generator-drift.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and `Output Summary:` reports no generated profile or core-manifest drift.

### Phase 5 — Final Python QA and Scope Evidence

- [x] [P5-T1] Run `poetry run black scripts/dev_tools/resolve_codex_deployment.py scripts/dev_tools/generate_codex_agent_variants.py tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/commit-steward-routing-python-black.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` are recorded; if formatting changes a file, restart the final loop at P5-T1 after retaining the artifact.
- [x] [P5-T2] Run `poetry run ruff check scripts/dev_tools/resolve_codex_deployment.py scripts/dev_tools/generate_codex_agent_variants.py tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/commit-steward-routing-python-ruff.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` are recorded; a failure restarts the final loop at P5-T1 after correction.
- [x] [P5-T3] Run `poetry run pyright scripts/dev_tools/resolve_codex_deployment.py scripts/dev_tools/generate_codex_agent_variants.py tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/commit-steward-routing-python-pyright.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` record zero Pyright errors; a failure restarts the final loop at P5-T1 after correction.
- [x] [P5-T4] Run `poetry run pytest tests/scripts/dev_tools/test_resolve_codex_deployment.py tests/scripts/dev_tools/test_generate_codex_agent_variants.py tests/scripts/dev_tools/test_codex_model_policy_config_parity.py --cov=scripts.dev_tools.resolve_codex_deployment --cov=scripts.dev_tools.generate_codex_agent_variants --cov-branch --cov-report=term-missing`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/commit-steward-routing-python-test-coverage.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, test counts, numeric line and branch coverage for both changed modules, and the repository coverage headline are recorded; coverage does not regress from P3-T4, remains at least 80% repository-wide, and preserves at least 90% coverage for any newly added executable unit; a failure restarts the final loop at P5-T1.
- [x] [P5-T5] Compare the current worktree against P3-T2 and record the final manifest in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/commit-steward-routing-scope-final.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` prove that the P3-T2 staged PoshQC mirror and every pre-existing feature-evidence artifact retain their baseline SHA-256 values; the only non-evidence changes introduced by this revision are `config/orchestration-routing.json`, `extensions/drm-copilot/resources/config/orchestration-routing.json`, `scripts/dev_tools/resolve_codex_deployment.py`, `scripts/dev_tools/generate_codex_agent_variants.py`, `tests/scripts/dev_tools/test_resolve_codex_deployment.py`, `tests/scripts/dev_tools/test_generate_codex_agent_variants.py`, `tests/scripts/dev_tools/test_codex_model_policy_config_parity.py`, the root and bundled base aliases plus five generated `commit-steward` profile files, and the bundled core manifest; all new evidence is below this feature’s canonical `evidence/` hierarchy; and no command outcome is `SKIPPED`.

## 2026-08-25 MCP Validator Parity Revision

The completed P0-T1 through P5-T5 remediation remains authoritative and must not be repeated, altered, or reverted. This bounded revision corrects the TypeScript source validator so that a persisted standalone C3 `commit-steward` receipt resolves and validates as `commit-steward-c3`, `gpt-5.6-terra`, and `high`. The existing `feature-review` logical alias to `feature-reviewer` must remain accepted. The only permitted source and test changes are `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`, `extensions/drm-copilot/test/lib/validate/codex-deployment.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing.test.ts`. No package publication, external release, published-runtime bootstrap, or `@danmoisan/drm-copilot-mcp@1.1.2` runtime-update claim is authorized. Evidence from a currently installed MCP runtime is fail-before diagnostic evidence only; source-level Jest evidence is the permitted post-change verification.

### Phase 6 — TypeScript MCP Validator Baseline

- [x] [P6-T1] Read `AGENTS.md` (standing instructions, General Code Change Policy, and General Unit Test Policy), `.agents/skills/typescript/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/issue.md`, `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/spec.md`, `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`, `extensions/drm-copilot/test/lib/validate/codex-deployment.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing.test.ts` in that order; record the actual order and every path read in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/mcp-validator-parity-instructions-read.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Policy Order:`, and the complete ordered path list; it identifies `spec.md` as the full-bug acceptance-criteria source and states that P0-T1 through P5-T5 remain preserved.
- [x] [P6-T2] Capture SHA-256 values, byte lengths, and `git diff --no-index` results for `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`, `extensions/drm-copilot/test/lib/validate/codex-deployment.test.ts`, `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing.test.ts`, and `extensions/drm-copilot/package.json`; in the same artifact capture the package version, the SHA-256 and byte lengths of `config/orchestration-routing.json`, `extensions/drm-copilot/resources/config/orchestration-routing.json`, the root and bundled `.codex/agents/commit-steward*.toml` profile inventories, and the current `git status --porcelain=v1 --untracked-files=all`. Write the result to `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/mcp-validator-parity-before.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; it establishes the immutable prior-remediation manifest, records package version `1.1.2`, proves root/bundled routing and generated profile parity before this source-only revision, and identifies no TypeScript source mirror requiring publication.
- [x] [P6-T3] [expect-fail] Invoke `mcp__drm-copilot__validate_orchestration_artifacts` against `artifacts/orchestration/orchestrator-state.json` with `artifact_type: "orchestrator-state"`, `require_codex_model_routing: true`, and the active worktree as `workspace_root`; record the complete current-MCP result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/mcp-validator-commit-steward-fail-before.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and `Output Summary:`; it records the `Unsupported Codex logical agent: 'commit-steward'` validation diagnostic and package/runtime identity when reported, and explicitly states that this invocation neither updates nor proves an update to `@danmoisan/drm-copilot-mcp@1.1.2`.
- [x] [P6-T4] Run `npm run test:coverage -- test/lib/validate/codex-deployment.test.ts test/lib/validate/orchestrator-state-codex-model-routing.test.ts test/lib/validate/orchestration-artifacts.test.ts` from `extensions/drm-copilot`; record the baseline result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/mcp-validator-parity-typescript-test-coverage.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, Jest pass/fail/skip counts, and numeric line coverage; it records the pre-change source-level validator behavior and does not modify production, test, package, or bundled customization files.

### Phase 7 — Source Validator and Orchestration-Parity Regression

- [x] [P7-T1] Update `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts` so `resolveCodexDeployment("commit-steward", "C3", "standalone", "C3")` resolves exactly to deployment agent `commit-steward-c3`, model `gpt-5.6-terra`, and reasoning effort `high`. Acceptance: no other resolver profile, C3-overlay rule, forced persona, or logical alias mapping is changed.
- [x] [P7-T2] Add Arrange-Act-Assert coverage in `extensions/drm-copilot/test/lib/validate/codex-deployment.test.ts` for the exact standalone C3 `commit-steward` resolution, and retain or extend the existing `feature-review` alias assertion in that same file. Acceptance: the test proves the `commit-steward-c3` Terra/high receipt and the `feature-review` to `feature-reviewer` mapping without modifying any production path.
- [x] [P7-T3] Add an orchestration-checkpoint regression in `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing.test.ts` that validates a persisted `commit-steward` C3 standalone receipt against a corresponding delegated `commit-steward-c3` or logical `commit-steward` entry. Acceptance: the test proves that the source validator accepts the exact receipt while the existing feature-review alias checkpoint test remains present and passing.
- [x] [P7-T4] Run `npm run test:unit -- test/lib/validate/codex-deployment.test.ts test/lib/validate/orchestrator-state-codex-model-routing.test.ts test/lib/validate/orchestration-artifacts.test.ts` from `extensions/drm-copilot`; record the source-level pass-after result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/mcp-validator-parity-source-pass-after.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0`, Jest pass/fail/skip counts, and the exact passing `commit-steward` and `feature-review` regression names are recorded; the evidence states that it validates workspace source only and makes no claim about the installed `@danmoisan/drm-copilot-mcp@1.1.2` runtime.
- [x] [P7-T5] Compare the current root and bundled routing/profile surfaces captured in P6-T2 and record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/other/mcp-validator-parity-root-bundle-scope.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` prove `config/orchestration-routing.json` remains byte-identical to `extensions/drm-copilot/resources/config/orchestration-routing.json`, each root `commit-steward*.toml` remains byte-identical to its bundled counterpart, and this revision has not invoked customization publication or modified a bundled runtime payload.

### Phase 8 — TypeScript QA Loop and Plan Validation

- [x] [P8-T1] Run `npm run format` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-typescript-format.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; if formatting changes any file, restart this phase at P8-T1 and record the restarted clean pass.
- [x] [P8-T2] Run `npm run lint` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-typescript-lint.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; a failure restarts the QA loop at P8-T1 after correction.
- [x] [P8-T3] Run `npm run typecheck` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-typescript-typecheck.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with zero TypeScript errors; a failure restarts the QA loop at P8-T1 after correction.
- [x] [P8-T4] Retain the completed historical `npm run test:coverage` result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-typescript-unit-coverage.2026-08-25T22-34.md`. Acceptance: the artifact retains the exact failed command result, coverage values, and `claude-config-carriage` root/bundle diagnosis; it is verified as historical diagnostic evidence only, does not satisfy final QA, and is superseded for final TypeScript coverage closure by P9-T7.
- [x] [P8-T5] Run `npm run build` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-typescript-build.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; a failure restarts the QA loop at P8-T1 after correction.
- [x] [P8-T6] Compare the current worktree against P6-T2 and record the final manifest in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-scope-final.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` prove that the only new non-evidence paths from this revision are `extensions/drm-copilot/src/lib/validate/orchestrator-state-codex-model-routing.ts`, `extensions/drm-copilot/test/lib/validate/codex-deployment.test.ts`, and `extensions/drm-copilot/test/lib/validate/orchestrator-state-codex-model-routing.test.ts`; all prior staged remediation paths preserve their P6-T2 values; all new evidence is under this feature’s canonical `evidence/` hierarchy; no command outcome is `SKIPPED`; no publication or external release occurred; and no evidence claims that `@danmoisan/drm-copilot-mcp@1.1.2` is updated.
- [x] [P8-T7] Run `mcp__drm-copilot__validate_orchestration_artifacts` with `artifact_type: "plan"`, `artifact_path: "docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/remediation-plan.2026-08-25T17-20.md"`, and the active worktree as `workspace_root`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/mcp-validator-parity-plan-validator.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the MCP validator result confirm this exact plan path has canonical phases and sequential task IDs.

## 2026-08-25 Claude Configuration Carriage Revision

The completed P0-T1 through P8-T7 work, including the failed P8-T4 coverage evidence, remains authoritative and must not be repeated, altered, or reverted. This corrective phase is limited to synchronizing the authoritative root routing file to the missing Claude-customizations routing mirror, executing the directly relevant existing carriage regression, and recording the required TypeScript QA and scope evidence. It does not authorize source-code changes, test-source changes, policy changes, configuration changes other than the named Claude routing mirror, commits, pushes, publication, PR review, CI monitoring, external state changes, or any claim that `@danmoisan/drm-copilot-mcp@1.1.2` is updated.

### Phase 9 — Claude Routing Mirror Repair and TypeScript QA Closure

- [x] [P9-T1] Capture SHA-256 values, byte lengths, and `git diff --no-index` exit codes for `config/orchestration-routing.json`, `extensions/drm-copilot/resources/config/orchestration-routing.json`, and `extensions/drm-copilot/resources/claude-customizations/config/orchestration-routing.json`; also capture `git status --porcelain=v1 --untracked-files=all`. Record the baseline in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/remediation-baseline/claude-config-carriage-before.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; it proves the root and `resources/config` files are byte-identical, identifies the Claude-customizations file as the only non-identical routing copy, and records the complete worktree-status baseline for P9-T9.
- [x] [P9-T2] Copy `config/orchestration-routing.json` directly to `extensions/drm-copilot/resources/claude-customizations/config/orchestration-routing.json` using `Copy-Item -LiteralPath`, then compare all three routing files with SHA-256, byte lengths, and pairwise `git diff --no-index` commands. Record the synchronization in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/other/claude-config-carriage-mirror-sync.<yyyy-MM-ddTHH-mm>.md`. Acceptance: every routing copy has identical SHA-256 and byte length, every pairwise diff exits `0`, and the only non-evidence file written by this task is `extensions/drm-copilot/resources/claude-customizations/config/orchestration-routing.json`.
- [x] [P9-T3] Run `npm run test:unit -- test/lib/push-down/claude-config-carriage.test.ts` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/regression-testing/claude-config-carriage-pass-after.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, and Jest pass/fail/skip counts; it records the passing `pins the bundled routing source byte-identical to the repo-root file` regression, does not modify test source, and makes no runtime-update or publication claim.
- [x] [P9-T4] Restart the TypeScript QA loop by running `npm run format` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-typescript-format.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; if formatting changes a file or fails, restore the P9-T2 byte-identical routing state when needed and restart at P9-T4.
- [x] [P9-T5] Run `npm run lint` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-typescript-lint.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; a failure restarts the QA loop at P9-T4 after correction.
- [x] [P9-T6] Run `npm run typecheck` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-typescript-typecheck.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` with zero TypeScript errors; a failure restarts the QA loop at P9-T4 after correction.
- [x] [P9-T7] Run `npm run test:coverage` from `extensions/drm-copilot`; record the clean P8-T4 closure result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-typescript-unit-coverage.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, Jest pass/fail/skip counts, repository numeric line coverage, and numeric coverage for `orchestrator-state-codex-model-routing.ts`; coverage does not regress from P6-T4, remains at least 80% repository-wide, and remains at least 90% for each newly added executable branch or unit; a failure restarts the QA loop at P9-T4 after correction.
- [x] [P9-T8] Run `npm run build` from `extensions/drm-copilot`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-typescript-build.<yyyy-MM-ddTHH-mm>.md`. Acceptance: the artifact contains `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:`; a failure restarts the QA loop at P9-T4 after correction.
- [x] [P9-T9] Compare the three routing files against the P9-T1 manifest, repeat their SHA-256/byte-length/pairwise-`git diff --no-index` checks, and compare the normalized current `git status --porcelain=v1 --untracked-files=all` output with the P9-T1 baseline. Record the end-state result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-scope-final.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` prove all three routing files are byte-identical; the only new non-evidence path from P9-T1 is `extensions/drm-copilot/resources/claude-customizations/config/orchestration-routing.json`; all P0-T8 staged paths retain their P9-T1 values; no command outcome is `SKIPPED`; no publication, external state change, commit, push, or PR review occurred; and no evidence claims that `@danmoisan/drm-copilot-mcp@1.1.2` is updated.
- [x] [P9-T10] Run `mcp__drm-copilot__validate_orchestration_artifacts` with `artifact_type: "plan"`, `artifact_path: "docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/remediation-plan.2026-08-25T17-20.md"`, and the active worktree as `workspace_root`; record the result in `docs/features/active/2026-08-25-codex-subagent-routing-attestation-launch-binding-552/evidence/qa-gates/claude-config-carriage-plan-validator.<yyyy-MM-ddTHH-mm>.md`. Acceptance: `EXIT_CODE: 0` and the MCP validator confirms this exact plan path has canonical phases and sequential task IDs.
