{
  "name": "cohorts_error_unknown_endpoint_negative",
  "notes": "A negative unknown endpoint renders its sign inside the tuple repr.",
  "input": {
    "item_keys": [
      1
    ],
    "conflict_edges": [
      [
        1,
        -4
      ]
    ]
  },
  "expected_error": "Conflict edge (1, -4) names item key -4, which is not a member of item_keys; every edge endpoint must be a declared item key."
}
