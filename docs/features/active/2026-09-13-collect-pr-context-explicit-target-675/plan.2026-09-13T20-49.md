# 2026-09-13-collect-pr-context-explicit-target (Plan)

- **Issue:** #675
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-13T20-49
- **Status:** Ready for preflight (revision round 2)
- **Revision history:** 1.1 — revision round 1. Thirteen blocking and six advisory preflight deltas applied: acceptance conditions that could not fail or could not pass replaced with re-derived ones; the coverage command given a second `lcov` reporter so per-file percentages have a source; a file-move step added to P1-T5 for the 500-line cap; the `index.ts` barrel named as an explicit threshold-map exemption; the `buildService` diff payload parameterized; `--untracked-files=all` adopted in Phase 7; and acceptance-criteria check-off timing aligned with `.claude/skills/acceptance-criteria-tracking/SKILL.md`. Phase and task counts are unchanged at 10 phases and 65 tasks.
- **Version:** 1.2 — revision round 2. Two blocking and two advisory preflight deltas applied: the Phase 9 preamble reconciled with per-task check-off timing and P9-T1 through P9-T4 converted from whole-file checked counts to group-scoped `awk` counts plus the invariant 35-line total; the `SF:` record spellings in P4-T5 and P8-T5 corrected to the Windows path separator the LCOV reporter actually writes; P7-T7's `ls -1 artifacts` expectation restated as one line naming `orchestration` rather than a byte-exact string, because the repository shell aliases `ls` to `ls -F`; and P7-T4, P7-T5, and P7-T7 amended to name which command their single `EXIT_CODE:` field records, with the non-zero success-path exit codes recorded in `Output Summary:`. Phase and task counts are unchanged at 10 phases and 65 tasks.
- **Epic:** `worktree-scoped-state-resolution`, feature F6 (wave 0, complexity C2), scope row 3.5
- **Work Mode:** `full-bug` — `spec.md` is the sole acceptance-criteria source. `user-story.md` does not exist for this feature and must not be created or referenced.
- **Spec (AC source):** `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md`
- **Research record:** `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/research/2026-09-13T21-30-collect-pr-context-explicit-target-research.md`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

## Execution conventions

**Working directories.** Every `git`, `grep`, and `wc` command in this plan runs from the worktree root `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a41d019edfae43cb3`, and every path it names is repository-relative. Every `npm`, `npx`, and `node run-jest.cjs` command runs from `extensions/drm-copilot/`, and every path it names is relative to that directory. Each task states which of the two it uses.

**Toolchain commands.** The four stage commands are fixed for this plan and are never substituted:

1. `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"`
2. `npx eslint --no-error-on-unmatched-pattern src test`
3. `npx tsc -p ./ --noEmit`
4. `node run-jest.cjs --coverage --coverageReporters=text-summary --coverageReporters=lcov`

`npm run format` is `prettier --write` and mutates tracked source; it is not used as a gate anywhere in this plan. A bare `npx prettier --check .` would scan `node_modules`, `out/`, and `coverage/` and is not this repository's scope. Tests run through `node run-jest.cjs`, never through `npx jest`. `npm ci` is a hard prerequisite for stages 1 through 4: `node_modules` was absent in this fresh worktree at baseline capture, and without it `npx eslint` exits 2 with `ERR_MODULE_NOT_FOUND` and `npx tsc` exits 1 because `npx` resolves a different `tsc` package.

The `--coverageReporters` CLI flag REPLACES the `coverageReporters` array in `jest.config.cjs` rather than adding to it. Supplying `--coverageReporters=text-summary` alone was measured on 2026-09-13 to create no `coverage/` directory and no `coverage/lcov.info`, so the per-file percentages P8-T5 requires would have no source. The flag is therefore repeated: `text-summary` prints the four headline percentages and `lcov` writes `coverage/lcov.info`, whose per-file `LF`/`LH` and `BRF`/`BRH` records are the only place a per-file percentage can be read. `coverage/` is gitignored at line 59 of the repository `.gitignore`, so the added reporter contributes nothing to any diff or porcelain listing.

**Diff anchor.** Every diff-based assertion in this plan is anchored to the fixed commit `499e288a` ("docs(epic): add worktree-scoped-state-resolution epic manifest"), this branch's merge base with `origin/epic/worktree-scoped-state-resolution-integration`. `main` (at `d93e2916`) is not used: this branch was created from the epic integration branch rather than from `main`, so a `main`-anchored diff sweeps in the epic-manifest commit, and `main` is a moving ref, so a diff anchored to it is not reproducible between two runs of the same condition.

**Tracked versus untracked.** `git diff` and `git diff --name-only` enumerate tracked changes only. Nothing is committed during plan execution, so every file this plan creates is untracked and never appears in an anchored diff. Every condition that derives a file set from a diff pairs the anchored diff with a `git status --porcelain` companion and asserts over the union. The two are complementary and each alone is wrong in one state: the anchored diff is blind to untracked files, and porcelain status goes empty once the change is committed. Where a condition inspects a file this plan creates, it uses plain `grep` against the path; `git grep` is never used, because it cannot see an untracked file.

**Evidence location (non-overridable).** All evidence artifacts are written under `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/`, using the canonical sub-paths `evidence/baseline/`, `evidence/regression-testing/`, and `evidence/qa-gates/`. The baseline sub-path is singular `baseline`. The research record writes `evidence/baselines/` at lines 473 and 590; that spelling is incorrect and is not propagated. No evidence file is written to `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/coverage/`, or `artifacts/evidence/`. Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; baseline and final-QC test artifacts carry numeric coverage headline values, never placeholders. One artifact per command step; no aggregate-only artifact.

**Evidence filename stamp.** Every evidence artifact this plan produces carries the fixed plan-cycle stamp `2026-09-13T20-49` in its filename, so each acceptance condition can name a concrete path with no placeholder token. The `Timestamp:` field inside each artifact records the actual wall-clock time of that command's run. The single exception is `phase0-instructions-read.md`, whose filename is fixed by the atomic plan contract and carries no stamp.

**Literals this plan instructs the executor to create.** These exact tokens do not exist in the tree today and are created by the tasks below, so they are quoted here verbatim outside every command span: `target_ref`, `targetRef`, `export function classifyPrContextDiffState`, `diff-emptiness`, `target_resolution`, `targetResolution`, `session-fallback`, `resolved_head_ref`, `resolved_head_sha`, `readonly changedFileCount`, `readonly resolvedHeadRef`, `commits ahead`, and the summary-line prefix `Head ref (source):`.

**Scope boundary.** This feature touches no file under `.claude/` and no file under `extensions/drm-copilot/resources/`. All `.claude/**` prose for `target_ref` is deferred to epic feature F7 per Decision 7 of `spec.md`. No Codex mirror of this TypeScript surface exists, so this plan carries no Codex-parity task. No task calls the live `mcp__drm-copilot__collect_pr_context` tool: MCP tools resolve their resources from the installed VS Code extension rather than from the repository tree, so such a call would measure the old build. The single exception is `mcp__drm-copilot__validate_orchestration_artifacts`, which is used only to validate this plan document and asserts nothing about the feature's behaviour.

**Constraint budget.** File-size cap is 500 lines for production, test, and reusable script files; Markdown is exempt. Coverage gates are line >= 85% and branch >= 75%, per-file, because `extensions/drm-copilot/jest.config.cjs` carries no `global` key and no glob entries. No production path under `src/` may be added to any coverage exclusion, and `jest.config.cjs` must not gain a `coveragePathIgnorePatterns` key. The untyped-escape-hatch budget for this change is 0. No `setTimeout`, no `Date.now()` outside an injected clock, and no temporary file in any test.

---

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read the repository policy files in the order defined by the `policy-compliance-order` skill and write the read record to `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-instructions-read.md`. The files, in order, are `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, and `.claude/rules/plan-acceptance-gates.md`. Acceptance: the artifact exists at that path and contains the field `Timestamp:`, the field `Policy Order:`, and one line naming each of the seven files above.

- [x] [P0-T2] From `extensions/drm-copilot/`, run `npm ci` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-npm-ci.2026-09-13T20-49.md`. This task must run before every other toolchain task in this plan. Acceptance: `npm ci` exits 0; from `extensions/drm-copilot/`, `npx tsc --version` exits 0 and `npx eslint --version` exits 0, which is the pair that fails when `node_modules` is absent; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` stating the installed-package count `npm ci` reported on stdout together with the two version strings the two probe commands printed.

- [x] [P0-T3] From `extensions/drm-copilot/`, run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-prettier-check.2026-09-13T20-49.md`. This invocation is check-mode and non-mutating; because a write-mode formatter exits 0 both on a clean run and after repairing files, the tree is observed independently. Acceptance: the command exits 0; from the worktree root, `git status --porcelain -- extensions/drm-copilot/src extensions/drm-copilot/test` produces no output after the run, confirming the check rewrote nothing; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording both the exit code and the verbatim final stdout line the command printed.

- [x] [P0-T4] From `extensions/drm-copilot/`, run `npx eslint --no-error-on-unmatched-pattern src test` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-eslint.2026-09-13T20-49.md`. Acceptance: the command exits 0; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording that the command wrote zero bytes to stdout and zero bytes to stderr, which is this ESLint invocation's clean-run signal, together with the exit code. No error or warning count is recorded: a clean run of this command prints no output at all, so no such count is available from it.

- [x] [P0-T5] From `extensions/drm-copilot/`, run `npx tsc -p ./ --noEmit` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-tsc.2026-09-13T20-49.md`. Acceptance: the command exits 0; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording that no diagnostic was printed.

- [x] [P0-T6] From `extensions/drm-copilot/`, run `node run-jest.cjs --coverage --coverageReporters=text-summary --coverageReporters=lcov` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-jest-coverage.2026-09-13T20-49.md`. The `text-summary` reporter is passed explicitly so the four coverage percentages are printed and can be read from the run. Acceptance: the command exits 0 with zero failed suites and zero failed tests; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording the passed-suite count, the passed-test count, and four numeric percentages for Statements, Branches, Functions, and Lines transcribed from the printed summary. Placeholder values are not permitted.

- [x] [P0-T7] From the worktree root, run `wc -l extensions/drm-copilot/src/repo-automation-service.ts extensions/drm-copilot/src/lib/pr-context/collector-output.ts extensions/drm-copilot/src/mcp-tool-inputs.ts extensions/drm-copilot/src/lib/pr-context/collector-core.ts extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts extensions/drm-copilot/src/lib/pr-context/index.ts extensions/drm-copilot/src/mcp-tools.ts extensions/drm-copilot/src/repo-automation-service-contract.ts extensions/drm-copilot/src/mcp-tool-definitions.ts extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts extensions/drm-copilot/jest.config.cjs extensions/drm-copilot/test/mcp-tool-inputs.test.ts extensions/drm-copilot/test/mcp-repo-automation-tool-definitions.test.ts extensions/drm-copilot/test/lib/pr-context/pr-context-service-call.test.ts extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts extensions/drm-copilot/test/repo-automation-dispatch.test.ts extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts extensions/drm-copilot/test/extension.collect-pr-context.test.ts extensions/drm-copilot/test/extension.integration.test.ts` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-file-line-counts.2026-09-13T20-49.md`. Acceptance: the command exits 0; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` transcribing the per-file counts, and it records the pre-change headroom to the 500-line cap for `extensions/drm-copilot/src/repo-automation-service.ts` and for `extensions/drm-copilot/src/lib/pr-context/collector-output.ts` as explicit integers, and it records the pre-change headroom to the 500-line cap for `extensions/drm-copilot/test/mcp-tool-inputs.test.ts`, `extensions/drm-copilot/test/repo-automation-dispatch.test.ts`, `extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts`, and `extensions/drm-copilot/test/extension.collect-pr-context.test.ts` as explicit integers.

- [ ] [P0-T8] From the worktree root, confirm the diff anchor by running `git merge-base HEAD origin/epic/worktree-scoped-state-resolution-integration` and `git rev-parse 499e288a`, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline/phase0-diff-anchor.2026-09-13T20-49.md`. Acceptance: both commands exit 0 and the first command's output shares its leading eight characters with the second command's output; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` quoting both full SHAs.

---

### Phase 1 — Tool Schema and Input Resolution

- [x] [P1-T1] In `extensions/drm-copilot/src/mcp-tool-definitions.ts`, add a `target_ref` string property to the `collect_pr_context` entry's `inputSchema.properties` (the entry begins at line 38 and its schema block currently spans lines 41 to 53), leaving `required` as the two-element array naming `workspace_root` and `base` and leaving `additionalProperties: false` in place. Acceptance: from the worktree root, `grep -c "target_ref" extensions/drm-copilot/src/mcp-tool-definitions.ts` reports at least 1, and `grep -F -c 'required: ["workspace_root", "base"]' extensions/drm-copilot/src/mcp-tool-definitions.ts` reports 1. That fixed string occurs exactly once in the file today and belongs to the `collect_pr_context` entry, so the count drops to 0 if `target_ref` is added to the `required` array.

- [x] [P1-T2] In `extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts`, apply the identical `target_ref` property addition to the `collect_pr_context` entry (which begins at line 51), leaving `required` and `additionalProperties` unchanged. Acceptance: from the worktree root, `grep -c "target_ref" extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` reports at least 1, and `grep -F -c 'required: ["workspace_root", "base"]' extensions/drm-copilot/src/mcp-repo-automation-tool-definitions.ts` reports 1. That fixed string occurs exactly once in the file today and belongs to the `collect_pr_context` entry, so the count drops to 0 if `target_ref` is added to the `required` array.

- [x] [P1-T3] In `extensions/drm-copilot/src/mcp-tool-inputs.ts`, extend the `CollectPrContextToolInput` type with an optional `targetRef` field and extend `resolveCollectPrContextToolInput` (lines 140 to 152) so that an absent `target_ref` key omits the field from the resolved input, a non-empty non-whitespace string is carried as `targetRef`, and an empty string, a whitespace-only string, or a non-string value is rejected with an error whose message contains the literal `target_ref`. Empty is rejected rather than treated as absent, because treating it as absent restores the silent fallback the epic forbids. Acceptance: from the worktree root, `grep -c "targetRef" extensions/drm-copilot/src/mcp-tool-inputs.ts` reports at least 1; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [x] [P1-T4] From the worktree root, run `wc -l extensions/drm-copilot/src/mcp-tool-inputs.ts`; if the count exceeds 500, extract the `collect_pr_context` input resolution into a new sibling module under `extensions/drm-copilot/src/` following the existing split precedent set by `mcp-tool-inputs-discovery.ts`, re-export it from `mcp-tool-inputs.ts`, and add a matching `coverageThreshold` entry for the new file in `extensions/drm-copilot/jest.config.cjs`. Acceptance: `wc -l extensions/drm-copilot/src/mcp-tool-inputs.ts` reports 500 or fewer; if a sibling module was created, `grep -c "coverageThreshold" extensions/drm-copilot/jest.config.cjs` reports at least 1 and the new file's path appears in the threshold map; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [x] [P1-T5] `extensions/drm-copilot/test/mcp-tool-inputs.test.ts` measures 496 lines and has 4 lines of headroom against the 500-line cap, so first move the existing `describe("resolvePushDownCodexAndAgentsCustomizationsToolInput", ...)` block (lines 288 to 333, three tests, ending at the `});` on line 333 immediately before the blank line 334 and the next `describe` on line 335) verbatim into a new sibling `extensions/drm-copilot/test/mcp-tool-inputs.push-down-codex.test.ts`, carrying the imports it needs and deleting no test. This follows the split precedent already set by `mcp-tool-inputs.workspace-root.test.ts` and `mcp-tool-inputs.codex-native-converter.test.ts`. Then, in `extensions/drm-copilot/test/mcp-tool-inputs.test.ts`, inside the existing `describe("resolveCollectPrContextToolInput", ...)` block that begins at line 261, add the test `resolves target_ref when supplied and omits it when absent` and the test `rejects an empty target_ref instead of treating it as absent`. The second test covers an empty string, a whitespace-only string, and a non-string value, and asserts that the raised error message contains `target_ref`. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/mcp-tool-inputs.test.ts -t "resolves target_ref when supplied and omits it when absent"` exits 0 reporting 1 passed test, and `node run-jest.cjs test/mcp-tool-inputs.test.ts -t "rejects an empty target_ref instead of treating it as absent"` exits 0 reporting 1 passed test; from `extensions/drm-copilot/`, `node run-jest.cjs test/mcp-tool-inputs.test.ts test/mcp-tool-inputs.push-down-codex.test.ts` exits 0 reporting 45 passed tests across 2 suites, which is the 43 that `test/mcp-tool-inputs.test.ts` reports today plus the two added here; from the worktree root, `wc -l extensions/drm-copilot/test/mcp-tool-inputs.test.ts` and `wc -l extensions/drm-copilot/test/mcp-tool-inputs.push-down-codex.test.ts` each report 500 or fewer.

- [x] [P1-T6] In `extensions/drm-copilot/test/mcp-repo-automation-tool-definitions.test.ts`, which already imports `toolDefinitions` at line 3 and `REPO_AUTOMATION_TOOL_DEFINITIONS` at line 4, add the test `declares the same input-schema properties and required arrays on both tool-definition surfaces`. The assertion iterates the tool names present in both arrays and compares dynamically derived `properties` key sets and `required` arrays; it must not hard-code a property list, or it would not catch the next parameter's drift. Its scope is deliberately limited to those two aspects: a blanket deep equality over the two arrays fails today, because `mcp-tool-definitions.ts` carries `enum` constraints on the `run_codex_native_converter` entry that the repo-automation copy omits. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/mcp-repo-automation-tool-definitions.test.ts -t "declares the same input-schema properties and required arrays on both tool-definition surfaces"` exits 0 reporting 1 passed test.

- [x] [P1-T7] From `extensions/drm-copilot/`, run `node run-jest.cjs test/mcp-tool-inputs.test.ts test/mcp-repo-automation-tool-definitions.test.ts test/extension.list-mcp-tools.test.ts` and confirm no pre-existing assertion in those three suites regressed from the schema addition. Acceptance: the command exits 0 with zero failed tests.

---

### Phase 2 — Thread the Target Ref to the Collector Head

- [x] [P2-T1] In `extensions/drm-copilot/src/repo-automation-service-contract.ts`, add a named exported input type for `collectPrContext` carrying `workspaceRoot`, `base`, and an optional `targetRef`, change the `collectPrContext` member of the `RepoAutomationService` interface (lines 71 to 73) to use it, and add the three optional provenance fields `targetResolution`, `resolvedHeadRef`, and `resolvedHeadSha` to `RepoAutomationExecutionResult` (declared at line 32). This file is interface-only and remains deliberately omitted from the `coverageThreshold` map for the reason already documented in `jest.config.cjs`. Acceptance: from the worktree root, `grep -c "targetResolution" extensions/drm-copilot/src/repo-automation-service-contract.ts` reports at least 1 and `wc -l extensions/drm-copilot/src/repo-automation-service-contract.ts` reports 500 or fewer.

- [x] [P2-T2] In `extensions/drm-copilot/src/repo-automation-service.ts`, change the `collectPrContext` method signature (lines 157 to 159) to the named input type added in P2-T1 and forward `targetRef` into the `collectPrContextServiceCall` argument object (lines 162 to 168), omitting the property when the value is `undefined`. Then run `wc -l extensions/drm-copilot/src/repo-automation-service.ts` from the worktree root; the file measured 498 lines at baseline with two lines of headroom, so if the count now exceeds 500, reduce it without changing the file's exported surface — for example by removing the two-line implementation comment at lines 160 to 161, whose content is preserved in `pr-context-service-call.ts`. Acceptance: `wc -l extensions/drm-copilot/src/repo-automation-service.ts` reports 500 or fewer; `grep -c "targetRef" extensions/drm-copilot/src/repo-automation-service.ts` reports at least 1; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [x] [P2-T3] In `extensions/drm-copilot/src/lib/pr-context/collector-output.ts`, widen `CollectAndWriteResult` (declared at lines 341 to 346, currently carrying only `summaryText` and `appendixText`) so `collectAndWrite` (lines 365 to 396) also returns the collected merge base, the collected head SHA, the resolved head ref, and the changed-file count derived from the buckets already present on the collected record. No additional git invocation is introduced: every value is already on `collected.contextResult`. Name the four added fields `mergeBase`, `headSha`, `resolvedHeadRef`, and `changedFileCount`. Acceptance: from the worktree root, `grep -c "readonly changedFileCount" extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports at least 1 and `grep -c "readonly resolvedHeadRef" extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports at least 1. A bare `mergeBase` search is not used: that token already occurs once in this file today, at line 173 in the `Merge base:` summary line, so it reports at least 1 before any edit and cannot fail; `wc -l extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports 500 or fewer; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0. If `wc -l` reports more than 500, move the `Base/Head` `summarySections` construction into `extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts`, which measures 388 lines with 112 of headroom and already carries a `coverageThreshold` entry at `jest.config.cjs` lines 41 to 44, and call it from `buildSummaryText`. Do not create a new module for this: a new module would be ungated and would require its own threshold entry.

- [x] [P2-T4] In `extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, add an optional `targetRef` to `CollectPrContextServiceCallInput` (lines 70 to 81), forward it as the `head` option in the `collectAndWrite` call (lines 121 to 131), and add the three provenance fields to `CollectPrContextServiceCallResult` (lines 84 to 89) and to the returned record (lines 136 to 141). `target_resolution` is `explicit` when a `targetRef` was supplied and `session-fallback` otherwise; `resolvedHeadRef` and `resolvedHeadSha` are the values the collector actually used. The `head` plumbing below this layer already exists and is already honoured at `collector-core.ts:119` and `render.ts:193`, so no change is required in `collector-core.ts` or `render.ts`. Acceptance: from the worktree root, `grep -c "session-fallback" extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports at least 1; `wc -l extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports 500 or fewer; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [x] [P2-T5] In `extensions/drm-copilot/src/mcp-tools.ts`, declare the three optional snake_case provenance fields on `RepoAutomationMcpToolResult` (lines 61 to 86) and project them in `toMcpToolResult` (lines 103 to 127) using the same omit-when-undefined spread form the surrounding fields already use. Acceptance: from the worktree root, `grep -c "resolved_head_sha" extensions/drm-copilot/src/mcp-tools.ts` reports at least 1; `wc -l extensions/drm-copilot/src/mcp-tools.ts` reports 500 or fewer; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [x] [P2-T6] From `extensions/drm-copilot/`, run `node run-jest.cjs` and confirm the threading change alone regressed no existing suite. The empty-diff guard has not landed yet, so every suite that currently computes an empty diff must still pass at this point. Acceptance: the command exits 0 with zero failed suites and zero failed tests.

---

### Phase 3 — Fallback Observability in the Summary Artifact

- [x] [P3-T1] In `extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts`, add a pure exported renderer that takes the explicit-target ref or its absence plus the workspace root and returns one line beginning with the literal `Head ref (source):`, rendering the explicit case and the session-fallback case as distinguishable text. This file is the repository's existing home for summary-text helpers and is already gated at 85 lines / 75 branches in `jest.config.cjs` at lines 41 to 44, so it needs no new threshold entry. Acceptance: from the worktree root, `grep -c "Head ref (source):" extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts` reports at least 1 and `wc -l extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts` reports 500 or fewer.

- [x] [P3-T2] In `extensions/drm-copilot/src/lib/pr-context/collector-output.ts`, emit the P3-T1 renderer's line into the `summarySections` array immediately after the `Head ref (resolved):` entry at line 172 and before the `Merge base:` entry at line 173, so a fallback cannot be read without the head it produced being read in the same glance. Acceptance: from the worktree root, `grep -n "Head ref (source):" extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports a line number greater than the line number `grep -n "Head ref (resolved):" extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports; `wc -l extensions/drm-copilot/src/lib/pr-context/collector-output.ts` reports 500 or fewer. If `wc -l` reports more than 500, move the `Base/Head` `summarySections` construction into `extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts`, which measures 388 lines with 112 of headroom and already carries a `coverageThreshold` entry at `jest.config.cjs` lines 41 to 44, and call it from `buildSummaryText`. Do not create a new module for this: a new module would be ungated and would require its own threshold entry.

- [x] [P3-T3] Create `extensions/drm-copilot/test/lib/pr-context/collector-output-head-source.test.ts` as a sibling of `collector-output.test.ts`, which is 485 lines and effectively full, matching the existing split precedent set by `collector-output-freshness.test.ts`. Add the test `renders Head ref (source) naming the explicit target in the Base/Head block` and the test `renders Head ref (source) naming the session fallback in the Base/Head block`. Each asserts that the rendered line begins with the literal `Head ref (source):`, that its index in the rendered text is greater than the index of the `Head ref (resolved):` line, and that the two cases render distinguishable text. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/lib/pr-context/collector-output-head-source.test.ts` exits 0 reporting 2 passed tests; from the worktree root, `wc -l extensions/drm-copilot/test/lib/pr-context/collector-output-head-source.test.ts` reports 500 or fewer.

- [x] [P3-T4] Update any assertion in `extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts` or `extensions/drm-copilot/test/lib/pr-context/collector-output-freshness.test.ts` that depends on the exact line sequence of the `Base/Head` block, so those two suites reflect the inserted line. No test is deleted. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/lib/pr-context/collector-output.test.ts test/lib/pr-context/collector-output-freshness.test.ts` exits 0 with zero failed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts extensions/drm-copilot/test/lib/pr-context/collector-output-freshness.test.ts` produces no removed line containing `it(`.

- [x] [P3-T5] From `extensions/drm-copilot/`, run `node run-jest.cjs` and confirm the inserted summary line regressed no other suite. Acceptance: the command exits 0 with zero failed suites and zero failed tests.

---

### Phase 4 — The Pure Diff-State Classifier

- [ ] [P4-T1] Create `extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` exporting the pure function `classifyPrContextDiffState` over a record carrying the merge base, the head SHA, the resolved head ref, the resolved base, and the changed-file count, returning a discriminated union distinguishing the refs-unresolved state (merge base or head SHA is `null`) from the refs-resolved-no-change state and from the populated state, together with the distinct failure message for each failing state. The refs-unresolved message names the requested base, the head ref that was attempted, and the underlying git failure text. The refs-resolved-no-change message names the resolved head ref, the head SHA, the merge base, and the resolved base. No `any` annotation and no `as any` assertion is used; `unknown` plus narrowing and the discriminated union are the required alternatives. The module's doc comment records that the guard cannot detect a wrong target whose branch has commits ahead of the base, and that the working-tree sections and the current-PR lookup remain scoped to `workspace_root` even when `target_ref` is supplied. Acceptance: from the worktree root, `grep -c "export function classifyPrContextDiffState" extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` reports 1; `grep -c "commits ahead" extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` reports at least 1; `grep -c "workspace_root" extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` reports at least 1; `wc -l extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` reports 500 or fewer.

- [ ] [P4-T2] In `extensions/drm-copilot/src/lib/pr-context/index.ts`, add an export block for the new module alongside the existing collector export blocks at lines 100 to 115. Acceptance: from the worktree root, `grep -c "diff-emptiness" extensions/drm-copilot/src/lib/pr-context/index.ts` reports at least 1 and `wc -l extensions/drm-copilot/src/lib/pr-context/index.ts` reports 500 or fewer; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0. `extensions/drm-copilot/src/lib/pr-context/index.ts` is a re-export barrel. Measured against `coverage/lcov.info` on 2026-09-13 it records `LF:115` and `LH:0`, because no module under `extensions/drm-copilot/src` or `extensions/drm-copilot/test` imports the barrel path. It therefore remains deliberately omitted from the `coverageThreshold` map on the same basis as the two omissions already documented in `jest.config.cjs`, and it stays inside `collectCoverageFrom` so it remains measured. Record that omission as a comment in `extensions/drm-copilot/jest.config.cjs` alongside the `diff-emptiness.ts` entry P4-T3 adds. Do not add a threshold entry for it: at 0% measured lines such an entry would fail the gate at P8-T4.

- [ ] [P4-T3] In `extensions/drm-copilot/jest.config.cjs`, add the `coverageThreshold` entry for the path `./src/lib/pr-context/diff-emptiness.ts` with `lines: 85` and `branches: 75`, matching every other entry in the map. Without this entry the new module lands completely ungated, because the map carries no `global` key and no glob entries. Do not add a `coveragePathIgnorePatterns` key and do not change `collectCoverageFrom`, which is the two-element array at line 17. Acceptance: from the worktree root, `grep -c "diff-emptiness" extensions/drm-copilot/jest.config.cjs` reports at least 1; `grep -c "coveragePathIgnorePatterns" extensions/drm-copilot/jest.config.cjs` reports 0; `grep -c "^  collectCoverageFrom:" extensions/drm-copilot/jest.config.cjs` reports 1, and that line's text is exactly `  collectCoverageFrom: ["src/**/*.ts", "!src/**/*.d.ts"],`. The anchored pattern is required: a bare `collectCoverageFrom` search reports 4 in this file today, because the comment lines at 100, 180 and 276 also name the key.

- [ ] [P4-T4] Create `extensions/drm-copilot/test/lib/pr-context/diff-emptiness.test.ts` with table-driven tests over `classifyPrContextDiffState`, covering at minimum one case for refs unresolved, one for refs resolved with the merge base equal to the head SHA and no changed file, one for refs resolved and unequal with no changed file, and one for refs resolved with at least one changed file. This is where the classifier's branch coverage is earned. No `setTimeout`, no `Date.now()`, and no temporary file is used. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/lib/pr-context/diff-emptiness.test.ts` exits 0 with zero failed tests and reports at least 4 passed tests; from the worktree root, `wc -l extensions/drm-copilot/test/lib/pr-context/diff-emptiness.test.ts` reports 500 or fewer.

- [ ] [P4-T5] From `extensions/drm-copilot/`, run `node run-jest.cjs --coverage --coverageReporters=text-summary --coverageReporters=lcov` and confirm the new module's threshold entry is satisfied. The `text-summary` reporter is passed explicitly so the coverage percentages are printed and can be read from the run, and `lcov` is passed alongside it so `coverage/lcov.info` is written. Acceptance: `coverage/lcov.info` contains the record `SF:src\lib\pr-context\diff-emptiness.ts`, the `LH`/`LF` ratio on the lines following that record is at or above 0.85, the `BRH`/`BRF` ratio is at or above 0.75, and no line of the run's output names `diff-emptiness` as failing a coverage threshold. The backslash spelling is the one the reporter writes: the barrel's record was measured on 2026-09-13 in this worktree as `SF:src\lib\pr-context\index.ts`, so a search for the forward-slash spelling returns nothing. The run's exit code is recorded but is not the gate at this point, and the reason is stated so it is not read as a waiver: every coverage run evaluates the whole per-file threshold map, and `./src/lib/pr-context/pr-context-service-call.ts` (measured `BRF:8 BRH:7` before this change) and `./src/mcp-tools.ts` each gain explicit-target branches in Phase 2 whose non-`undefined` side no test drives until P6-T1 and P6-T5. Those two files are gated unconditionally at P8-T4, which runs after both of those tasks.

---

### Phase 5 — Repair the Suites That Currently Compute an Empty Diff

This phase runs before the guard is wired in Phase 6. That ordering is deliberate: scripting a non-empty diff into these fakes is harmless while the guard is absent, whereas landing the guard first would make every gate in this phase unsatisfiable. Approximately twenty existing tests across the five files below currently run against a completely empty git diff, because each suite's command-runner fake returns an empty string for every unmatched git subcommand. Each affected test is updated to script a non-empty diff using the pattern already established in `collector-core.test.ts` and `collector-integration.test.ts` — a name-status line of the form `M` followed by a tab and a path for `diff --name-status`, and a numstat line of the form `1`, tab, `0`, tab, path for `diff --numstat`. No test is deleted: deletion would silently drop the path-identity and freshness coverage added by issue #574.

- [ ] [P5-T1] In `extensions/drm-copilot/test/lib/pr-context/pr-context-service-call.test.ts`, extend the `ScriptRunner.dispatch` method (lines 45 to 66, whose final `return ok("")` at line 65 is what makes every diff empty today) to return a non-empty name-status and numstat diff, leaving the `gh`-unavailable branch at lines 46 to 51 and every existing assertion intact. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/lib/pr-context/pr-context-service-call.test.ts` exits 0 reporting 8 passed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/lib/pr-context/pr-context-service-call.test.ts` produces no removed line containing `it(`.

- [ ] [P5-T2] In `extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts`, give `buildService` a second parameter selecting the git diff payload, with a non-empty name-status and numstat diff as the value both existing tests use, and an empty-diff value available for later use. The parameter is required rather than deferred: `buildService` is the single shared runner factory in this file, P6-T5 adds a test to this same file that needs the empty-diff payload, and after this task the factory would otherwise no longer be able to produce one. Leave the `gh`-failure response and both existing tests intact. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/repo-automation-dispatch-pr-context-verification.test.ts` exits 0 reporting 2 passed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts` produces no removed line containing `it(`.

- [ ] [P5-T3] In `extensions/drm-copilot/test/repo-automation-dispatch.test.ts`, extend the object-literal runner used by the `collect_pr_context` test so its git responses include a non-empty name-status and numstat diff, leaving the existing assertion on the returned summary string intact. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/repo-automation-dispatch.test.ts` exits 0 with zero failed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/repo-automation-dispatch.test.ts` produces no removed line containing `it(`.

- [ ] [P5-T4] In `extensions/drm-copilot/test/extension.collect-pr-context.test.ts`, extend the `spawnSync` script in `setGitBranchDiscoveryState` so the git diff subcommands return a non-empty name-status and numstat payload instead of the blanket empty result, leaving the six existing success-path tests intact. Note that `SubprocessRunner` reads the completed process's stdout only when it is a Buffer, so the scripted value must be supplied in the form the existing script already uses for the other subcommands it serves. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/extension.collect-pr-context.test.ts` exits 0 with zero failed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/extension.collect-pr-context.test.ts` produces no removed line containing `it(`.

- [ ] [P5-T5] In `extensions/drm-copilot/test/extension.integration.test.ts`, extend the `spawnSync` mock in the same way so the three command-handler tests drive a non-empty diff, leaving those three tests intact. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/extension.integration.test.ts` exits 0 with zero failed tests; from the worktree root, `git diff 499e288a -- extensions/drm-copilot/test/extension.integration.test.ts` produces no removed line containing `it(`.

- [ ] [P5-T6] From the worktree root, confirm no test was deleted from any of the five suites by running `git diff 499e288a -- extensions/drm-copilot/test/lib/pr-context/pr-context-service-call.test.ts extensions/drm-copilot/test/extension.collect-pr-context.test.ts extensions/drm-copilot/test/extension.integration.test.ts extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts extensions/drm-copilot/test/repo-automation-dispatch.test.ts` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/regression-testing/no-test-deleted.2026-09-13T20-49.md`. All five files are tracked at `499e288a`, so a removed line and a whole-file deletion are both visible to the anchored diff, and an untracked file cannot contain a pre-existing test; no porcelain companion is required for this observation and that reasoning is recorded in the artifact. Acceptance: the diff produces no removed line containing `it(` and no line containing `deleted file mode`; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording both counts as 0.

- [ ] [P5-T7] From `extensions/drm-copilot/`, run `node run-jest.cjs` and confirm the whole suite still passes with the guard still absent. Acceptance: the command exits 0 with zero failed suites and zero failed tests.

---

### Phase 6 — Wire the Empty-Diff Guard and Add the Target Test Matrix

- [ ] [P6-T1] Create `extensions/drm-copilot/test/lib/pr-context/pr-context-service-call-target.test.ts` as a sibling of `pr-context-service-call.test.ts`, table-driven over the cross product of explicit target versus absent target and populated diff versus empty-with-refs-resolved versus empty-with-refs-unresolved. Drive it with a local `CommandRunner` fake dispatching on the joined subcommand prefix and a `TreeFileSystem` seeded with a `.git` marker so `GitClient.resolveRoot()` short-circuits, matching the seams `pr-context-service-call.test.ts` already uses at lines 35 to 76. Capture the full argv the fake runner received, extending the capture technique `repo-automation-dispatch.test.ts` already uses for the run options. The file contains exactly these six tests, whose names are normative because acceptance conditions cite them: `passes the explicit target ref to git rather than the session HEAD`; `reports target_resolution explicit and the resolved head ref and sha when a target ref is supplied`; `reports target_resolution session-fallback when no target ref is supplied`; `raises naming the resolved head ref, head sha, merge base and base when the refs resolve and no file changed`; `raises naming the requested base when the base or head could not be resolved`; `writes both artifacts and then raises when the diff is empty`. No `setTimeout`, no `Date.now()`, and no temporary file is used. Acceptance: from the worktree root, `grep -c "passes the explicit target ref to git rather than the session HEAD" extensions/drm-copilot/test/lib/pr-context/pr-context-service-call-target.test.ts` reports 1, and `wc -l extensions/drm-copilot/test/lib/pr-context/pr-context-service-call-target.test.ts` reports 500 or fewer.

- [ ] [P6-T2] [expect-fail] From `extensions/drm-copilot/`, run `node run-jest.cjs test/lib/pr-context/pr-context-service-call-target.test.ts` before the guard is wired, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/regression-testing/fail-before-empty-diff-guard.2026-09-13T20-49.md`. The three raise-expecting tests must fail at this point, because no code path yet distinguishes an empty result from a populated one. Acceptance: the command exits non-zero and its output names all three of `raises naming the resolved head ref, head sha, merge base and base when the refs resolve and no file changed`, `raises naming the requested base when the base or head could not be resolved`, and `writes both artifacts and then raises when the diff is empty` as failing; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, and an `Output Summary:` naming the three failing tests and the passed-test count.

- [ ] [P6-T3] In `extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, call `classifyPrContextDiffState` on the values `collectAndWrite` now returns and raise on the two failing states. The call is placed after both `verifyWrittenArtifact` invocations (currently lines 133 and 134) and before the success record is constructed (currently lines 136 to 141), so both artifacts are written and read-back-verified first. That ordering preserves the `writtenPaths` equals `result.artifacts` invariant, keeps the summary available as the operator's primary diagnostic for why the diff was empty, and ensures a discarded write is reported as a verification failure rather than being masked by an empty-diff error. The existing errors for a missing workspace root, a missing base, and an artifact read-back mismatch are unchanged in text and in ordering, and the two `Wrote context ...` log lines are unchanged. Acceptance: from the worktree root, `grep -c "classifyPrContextDiffState" extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports at least 1; the LARGEST line number `grep -n "classifyPrContextDiffState(" extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports is greater than the largest line number `grep -n "verifyWrittenArtifact(input.fileSystem" extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports. The trailing `(` and the largest-of qualifier are both required, because the import statement also names the symbol and sits above both verification calls; `wc -l extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts` reports 500 or fewer; from `extensions/drm-copilot/`, `npx tsc -p ./ --noEmit` exits 0.

- [ ] [P6-T4] From `extensions/drm-copilot/`, run `node run-jest.cjs test/lib/pr-context/pr-context-service-call-target.test.ts` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/regression-testing/pass-after-empty-diff-guard.2026-09-13T20-49.md`. Acceptance: the command exits 0 reporting 6 passed tests and zero failed tests; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording the passed-test count and naming the three tests that failed at P6-T2 as now passing.

- [ ] [P6-T5] In `extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts`, add the test `returns ok false with the empty-diff failure text when the collected diff is empty` and the test `projects target_resolution and the resolved head onto the dispatch result`, matching that file's existing `ok: false` precedent at line 90. The empty-diff test selects the empty payload through the `buildService` diff-payload parameter added by P5-T2; it does not reintroduce a blanket empty git response. The second test asserts the three snake_case fields on the result returned by `dispatchRepoAutomationTool`. Neither test invokes the live MCP tool. Acceptance: from `extensions/drm-copilot/`, `node run-jest.cjs test/repo-automation-dispatch-pr-context-verification.test.ts -t "returns ok false with the empty-diff failure text when the collected diff is empty"` exits 0 reporting 1 passed test, and `node run-jest.cjs test/repo-automation-dispatch-pr-context-verification.test.ts -t "projects target_resolution and the resolved head onto the dispatch result"` exits 0 reporting 1 passed test; from the worktree root, `wc -l extensions/drm-copilot/test/repo-automation-dispatch-pr-context-verification.test.ts` reports 500 or fewer.

- [ ] [P6-T6] From `extensions/drm-copilot/`, run `node run-jest.cjs test/lib/pr-context/pr-context-service-call.test.ts -t "writes exactly the paths it reports in result.artifacts"`, then `node run-jest.cjs test/lib/pr-context/pr-context-service-call.test.ts -t "writes both artifacts and succeeds when the GitHub CLI is unavailable"`, then `node run-jest.cjs test/lib/pr-context/pr-context-service-call.test.ts -t "raises when a stale file is present and the write is discarded"`, then `node run-jest.cjs test/lib/pr-context/pr-context-service-call.test.ts -t "raises naming the appendix when the summary write succeeds and the appendix write fails"`, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/regression-testing/preexisting-guards-service-call.2026-09-13T20-49.md`. These four are the currently-passing regression guards the epic requires be retained rather than omitted as redundant. Acceptance: each of the four commands exits 0 reporting 1 passed test; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` naming all four tests with their pass results.

- [ ] [P6-T7] From `extensions/drm-copilot/`, run `node run-jest.cjs test/repo-automation-dispatch-pr-context-verification.test.ts -t "reports ok false with the failure text when the service call raises"`, then `node run-jest.cjs test/repo-automation-dispatch-pr-context-verification.test.ts -t "reports ok true with artifacts equal to the paths written in the same run"`, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/regression-testing/preexisting-guards-dispatch.2026-09-13T20-49.md`. Acceptance: both commands exit 0 reporting 1 passed test each; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` naming both tests with their pass results.

- [ ] [P6-T8] From `extensions/drm-copilot/`, run `node run-jest.cjs` and confirm the guard regressed no suite. Acceptance: the command exits 0 with zero failed suites and zero failed tests.

---

### Phase 7 — Policy-Gate Verification

- [ ] [P7-T1] From the worktree root, derive the source set as the union of the paths reported by `git diff --name-only 499e288a -- extensions/drm-copilot/src` and the paths reported by `git status --porcelain --untracked-files=all -- extensions/drm-copilot/src`, and confirm every production path in that union has a `coverageThreshold` entry in `extensions/drm-copilot/jest.config.cjs`. Exactly two paths are exempt and both are named here rather than left to the executor's judgment: `extensions/drm-copilot/src/repo-automation-service-contract.ts`, which is interface-only and whose omission is already documented in `jest.config.cjs`; and `extensions/drm-copilot/src/lib/pr-context/index.ts`, which is a re-export barrel measuring `LF:115` / `LH:0` and whose omission P4-T2 documents in `jest.config.cjs`. Every other path in the union must have an entry. The union is required because `extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` is created by this change, is untracked until commit, and therefore never appears in the anchored diff. Write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/coverage-threshold-coverage.2026-09-13T20-49.md`. Acceptance: `grep -c "diff-emptiness" extensions/drm-copilot/jest.config.cjs` reports at least 1; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` listing every path in the union together with its threshold-map status, with no path other than those two named exemptions marked as unentered.

- [ ] [P7-T2] From the worktree root, confirm no coverage exclusion was introduced. Acceptance: `grep -c "coveragePathIgnorePatterns" extensions/drm-copilot/jest.config.cjs` reports 0, and `grep -n "^  collectCoverageFrom:" extensions/drm-copilot/jest.config.cjs` reports exactly one line, that line is line 17, and its text is exactly `  collectCoverageFrom: ["src/**/*.ts", "!src/**/*.d.ts"],` with no third array entry. The anchored pattern is required: a bare `collectCoverageFrom` search reports 4 lines in this file today, so a condition demanding exactly one match could never pass.

- [ ] [P7-T3] From the worktree root, derive the change set as the union of the paths reported by `git diff --name-only 499e288a` and the paths reported by `git status --porcelain --untracked-files=all`, run a line count over every non-Markdown path in that union, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/file-size-cap.2026-09-13T20-49.md`. Check explicitly, in addition to the union, by running `wc -l extensions/drm-copilot/src/repo-automation-service.ts extensions/drm-copilot/src/lib/pr-context/collector-output.ts extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts extensions/drm-copilot/test/lib/pr-context/diff-emptiness.test.ts extensions/drm-copilot/test/lib/pr-context/pr-context-service-call-target.test.ts extensions/drm-copilot/test/lib/pr-context/collector-output-head-source.test.ts`: the first two each had fewer than 20 lines of headroom before the change, and the last four are created by this change and are invisible to the anchored diff until they are committed. The `--untracked-files=all` form is required: plain porcelain collapses a wholly-untracked directory to a single entry, and a line count cannot be run against a directory path. Markdown documentation is exempt from the cap. Acceptance: every non-Markdown path in the union reports 500 or fewer lines; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` listing each path with its line count and the maximum observed count.

- [ ] [P7-T4] From the worktree root, confirm no untyped escape hatch was introduced under `extensions/drm-copilot/src`, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/no-any-budget.2026-09-13T20-49.md`. Both mechanisms are required, because neither alone covers both states: the anchored diff is the only mechanism that sees modifications to files that already existed at `499e288a`, and the direct grep is the only mechanism that sees a file this change creates. Acceptance: `git diff 499e288a -- extensions/drm-copilot/src` contains no added line matching `: any` and no added line matching `as any`; `grep -nE ": any|as any" extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts` reports no match, and the same pattern reports no match for every additional untracked path listed by `git status --porcelain --untracked-files=all -- extensions/drm-copilot/src`; the artifact records the anchored-diff command in `Command:` and its exit code in `EXIT_CODE: 0`, and carries `Timestamp:` and an `Output Summary:` recording both results, the enumerated untracked path list, and the exit code each `grep -nE` invocation returned. A `grep` that matches nothing exits 1, which is this check's success signal, so that value is recorded in the summary rather than in the `EXIT_CODE:` field.

- [ ] [P7-T5] From the worktree root, confirm no banned determinism construct was introduced under `extensions/drm-copilot/src` or `extensions/drm-copilot/test`, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/determinism.2026-09-13T20-49.md`. Acceptance: `git diff 499e288a -- extensions/drm-copilot/src extensions/drm-copilot/test` contains no added line matching `setTimeout`, `Date.now(`, `mkdtemp`, or `tmpdir`; `grep -nE "setTimeout|Date\.now\(|mkdtemp|tmpdir" extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts extensions/drm-copilot/test/lib/pr-context/diff-emptiness.test.ts extensions/drm-copilot/test/lib/pr-context/pr-context-service-call-target.test.ts extensions/drm-copilot/test/lib/pr-context/collector-output-head-source.test.ts` reports no match, and the same pattern reports no match for every additional untracked path listed by `git status --porcelain --untracked-files=all -- extensions/drm-copilot/src extensions/drm-copilot/test`; the artifact records the anchored-diff command in `Command:` and its exit code in `EXIT_CODE: 0`, and carries `Timestamp:` and an `Output Summary:` recording both results and the exit code the `grep -nE` invocation returned. A `grep` that matches nothing exits 1, which is this check's success signal, so that value is recorded in the summary rather than in the `EXIT_CODE:` field.

- [ ] [P7-T6] From the worktree root, confirm the Decision 7 deferral held and no bundled-payload mirroring obligation was activated, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/scope-boundary.2026-09-13T20-49.md`. The porcelain companion is required because the anchored diff enumerates tracked changes only and would not report a newly added, still-uncommitted file under either tree. Acceptance: `git diff --name-only 499e288a -- .claude extensions/drm-copilot/resources` produces no output, and `git status --porcelain --untracked-files=all -- .claude extensions/drm-copilot/resources` produces no output; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording that both commands produced zero lines.

- [ ] [P7-T7] From the worktree root, confirm no evidence file was written outside the canonical feature evidence tree, and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/evidence-location.2026-09-13T20-49.md`. Acceptance: from the worktree root, `ls -1 artifacts` exits 0 and prints exactly one line, and that line names `orchestration`, confirming that none of `baselines`, `baseline`, `qa`, `qa-gates`, `coverage`, or `evidence` was created under it. The observed line on 2026-09-13 was `orchestration/`: the repository shell aliases `ls` to `ls -F`, which appends a directory marker. That trailing separator is part of the same single entry and is not a second one. A `git status --porcelain -- artifacts` check is not used and must not be substituted: `/artifacts` is listed at line 6 of the repository `.gitignore`, so porcelain status reports nothing for that tree whatever is written into it; `ls docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baseline` exits 0; `ls docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/baselines` exits non-zero, confirming the incorrect plural spelling was not created; the artifact records the `ls -1 artifacts` command in `Command:` and its exit code in `EXIT_CODE: 0`, and carries `Timestamp:` and an `Output Summary:` recording all three results, including the non-zero exit code the `evidence/baselines` probe returned, which is that probe's success signal.

---

### Phase 8 — Final QC Loop

Run the four stages in the order below. If any stage fails, or rewrites any tracked file, correct the cause and restart the loop from stage 1. A phase is complete only when all four stages pass consecutively in a single uninterrupted pass, with the four artifacts below produced from that one pass.

- [ ] [P8-T1] From `extensions/drm-copilot/`, run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/final-prettier-check.2026-09-13T20-49.md`. This invocation is check-mode and non-mutating, so the tree is observed independently of the exit code: a write-mode formatter exits 0 both on a clean run and after repairing files, whereas this run must leave the tree byte-identical. Acceptance: the command exits 0; from the worktree root, the output of `git status --porcelain -- extensions/drm-copilot/src extensions/drm-copilot/test` is identical before and after the run; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording the exit code, the verbatim final stdout line the command printed, and the before-and-after porcelain comparison result.

- [ ] [P8-T2] From `extensions/drm-copilot/`, run `npx eslint --no-error-on-unmatched-pattern src test` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/final-eslint.2026-09-13T20-49.md`. No autofix flag is passed, so this run rewrites nothing. Acceptance: the command exits 0; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording that the command wrote zero bytes to stdout and zero bytes to stderr, which is this ESLint invocation's clean-run signal, together with the exit code. No error or warning count is recorded: a clean run of this command prints no output at all, so no such count is available from it.

- [ ] [P8-T3] From `extensions/drm-copilot/`, run `npx tsc -p ./ --noEmit` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/final-tsc.2026-09-13T20-49.md`. Acceptance: the command exits 0; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording that no diagnostic was printed.

- [ ] [P8-T4] From `extensions/drm-copilot/`, run `node run-jest.cjs --coverage --coverageReporters=text-summary --coverageReporters=lcov` and write `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/final-jest-coverage.2026-09-13T20-49.md`. The `text-summary` reporter is passed explicitly so the four coverage percentages are printed and can be read from the run; the whole per-file threshold map is evaluated on every coverage run, so any per-file threshold failure surfaces as a non-zero exit. Acceptance: the command exits 0 with zero failed suites and zero failed tests; the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` recording the passed-suite count, the passed-test count, and four numeric percentages for Statements, Branches, Functions, and Lines transcribed from the printed summary. Placeholder values are not permitted.

- [ ] [P8-T5] Write the coverage delta record to `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/qa-gates/coverage-delta.2026-09-13T20-49.md`, comparing the P0-T6 baseline against the P8-T4 post-change run and reporting new-and-changed-code coverage for this change's source set. Acceptance: the artifact carries `Timestamp:` and three labelled numeric blocks — baseline Statements, Branches, Functions, and Lines percentages copied from the P0-T6 artifact; post-change Statements, Branches, Functions, and Lines percentages copied from the P8-T4 artifact; and per-file line and branch percentages for `extensions/drm-copilot/src/lib/pr-context/diff-emptiness.ts`, `extensions/drm-copilot/src/lib/pr-context/pr-context-service-call.ts`, `extensions/drm-copilot/src/lib/pr-context/collector-output.ts`, `extensions/drm-copilot/src/lib/pr-context/summary-helpers.ts`, `extensions/drm-copilot/src/mcp-tool-inputs.ts`, and `extensions/drm-copilot/src/mcp-tools.ts` read from the LCOV data the coverage run wrote, where each of those six files appears in `coverage/lcov.info` as an `SF:` record spelled with the Windows path separator — for example `SF:src\mcp-tools.ts` and `SF:src\lib\pr-context\collector-output.ts` — and each percentage is computed as `LH`/`LF` and `BRH`/`BRF` from the lines that follow its record — each at or above 85 for lines and at or above 75 for branches. The artifact states explicitly whether post-change Lines and Branches percentages are at or above their baseline values, and no value is recorded as a placeholder.

---

### Phase 9 — Acceptance-Criteria Check-Off and Record Updates

Acceptance criteria are checked off individually in `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` as the corresponding work is verified. The criterion text is never edited; only the checkbox marker changes from unchecked to checked. `spec.md` is the sole acceptance-criteria source for this `full-bug` feature. Before plan execution begins the file carries 31 lines beginning with an unchecked list marker, 4 beginning with a checked one, and 35 checkbox lines in total; on completion those counts are 4 unchecked and 31 checked. Because each criterion is checked off at the point its named task passes rather than in this phase, the whole-file checked count at any intermediate point is not a fixed number, so P9-T1 through P9-T4 assert group-scoped counts together with the invariant total of 35, and only P9-T5 and P9-T6 assert the whole-file checked and unchecked counts, which are fixed at that terminal point. Each criterion is checked off individually at the point the task named against it passes verification, not deferred to this phase; the tasks below are the reconciliation and count-verification pass over check-offs already made, and any criterion still unchecked when its named task has passed is checked off here.

- [ ] [P9-T1] In `spec.md`, confirm the four criteria under the **Schema and input resolution** heading are checked off, having verified each against P1-T1, P1-T2, P1-T5, and P1-T6, and check off any that is still unchecked. Acceptance: from the worktree root, `awk '/^\*\*Schema and input resolution\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[ \] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 0; `awk '/^\*\*Schema and input resolution\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[[x ]\] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 4, confirming no criterion line in that group was removed or rewritten; and `grep -c "^- \[[x ]\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 35. The group-scoped form is required and a whole-file checked count must not be substituted: this plan checks each criterion off at the point its named task passes, so the whole-file checked count when this task runs depends on how many earlier check-offs have already been made and is not a fixed number. Both group counters report 4 against the file as it stands today, so the first condition fails until the four check-offs are made. An anchored diff against `spec.md` is not used and must not be substituted: the whole feature folder is untracked at `499e288a`, so a diff taken against that commit is empty whatever the executor does.

- [ ] [P9-T2] In `spec.md`, confirm the two criteria under the **Explicit target reaches git** heading and the three under the **Fallback is observable, not silent** heading are checked off, having verified each against P6-T4, P3-T3, and P6-T5, and check off any that is still unchecked. Acceptance: from the worktree root, `awk '/^\*\*Explicit target reaches git\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[ \] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 0; `awk '/^\*\*Explicit target reaches git\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[[x ]\] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 2; `awk '/^\*\*Fallback is observable, not silent\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[ \] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 0; `awk '/^\*\*Fallback is observable, not silent\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[[x ]\] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 3; and `grep -c "^- \[[x ]\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 35. The group-scoped form is required for the reason recorded in P9-T1.

- [ ] [P9-T3] In `spec.md`, confirm the six criteria under the **Empty diff fails loudly** heading are checked off, having verified each against P4-T1, P4-T4, P6-T4, and P6-T5, and check off any that is still unchecked. Acceptance: from the worktree root, `awk '/^\*\*Empty diff fails loudly\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[ \] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 0; `awk '/^\*\*Empty diff fails loudly\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[[x ]\] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 6; and `grep -c "^- \[[x ]\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 35. The group-scoped form is required for the reason recorded in P9-T1.

- [ ] [P9-T4] In `spec.md`, confirm the four criteria under the **No regression** heading are checked off, having verified each against P5-T6, P6-T6, P6-T7, and P7-T6, and check off any that is still unchecked. Acceptance: from the worktree root, `awk '/^\*\*No regression\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[ \] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 0; `awk '/^\*\*No regression\*\*$/{f=1;next} /^(\*\*|## )/{f=0} f&&/^- \[[x ]\] /{c++} END{print c+0}' docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 4; and `grep -c "^- \[[x ]\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 35. The group-scoped form is required for the reason recorded in P9-T1.

- [ ] [P9-T5] In `spec.md`, check off the seven criteria under the **Policy gates** heading and the one under the **Documented limitation** heading, having confirmed each against P7-T1, P7-T2, P7-T3, P7-T4, P7-T5, P8-T1 through P8-T4, P7-T7, and P4-T1. Acceptance: from the worktree root, `grep -c "^- \[x\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 31, and `grep -c "^- \[ \] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` reports 4.

- [ ] [P9-T6] Update the `- **Status:**` and `- **Last Updated:**` metadata lines of `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` to record delivery, and append an outcomes note to `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/issue.md` recording the two new failure modes, the optional `target_ref` parameter, and the F7 deferral of all `.claude/**` prose. No acceptance-criterion text is edited in either file. Acceptance: from the worktree root, `grep -c "target_ref" docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/issue.md` reports at least 1, and, after the metadata edit, `grep -c "^- \[x\] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` still reports 31 and `grep -c "^- \[ \] " docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/spec.md` still reports 4, confirming no criterion line was rewritten. An anchored diff against `spec.md` is not used and must not be substituted, for the reason recorded in P9-T1.

- [ ] [P9-T7] Write the issue-update mirror to `docs/features/active/2026-09-13-collect-pr-context-explicit-target-675/evidence/issue-updates/issue-675.2026-09-13T20-49.md`. Acceptance: the artifact exists and carries `Timestamp:`, the exact text intended for issue #675, and a `PostedAs:` line.
