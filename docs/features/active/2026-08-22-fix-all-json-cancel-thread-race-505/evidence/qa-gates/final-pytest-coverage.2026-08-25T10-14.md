# Final QC — Pytest with Coverage (Phase 6, [P6-T4])

Timestamp: 2026-08-25T10-14

Command: `poetry run pytest --cov --cov-branch --cov-report=term-missing --cov-report=json:artifacts/python/coverage.json`

EXIT_CODE: 0

Output Summary:
- Passed: 4121
- Failed: 0
- Skipped: 5
- Raw terminal line: `4121 passed, 5 skipped in 28.17s`
- Repository-wide line coverage: **92.6302414231258** percent, read from `totals.percent_statements_covered` in `artifacts/python/coverage.json`. Threshold is at least 85 — satisfied.
- Repository-wide branch coverage: **85.21485797523671** percent, read from `totals.percent_branches_covered` in `artifacts/python/coverage.json`. Threshold is at least 75 — satisfied.
- The terminal `TOTAL` row's `Cover` column reads `91%`. That figure is `totals.percent_covered` (90.63829787234043), the combined statements-plus-branches ratio, and is deliberately **not** recorded as either the line or the branch figure. Both figures above come only from the two named JSON keys.
- All five skips are pre-existing parametrized skips in `tests/scripts/dev_tools/test_parallel_manifest_bash_parity.py` (fixtures that declare no accessor expectation). None is related to this change.

## Precondition applied before this run

`.claude/state/python-batch-budget.default.json` is gitignored local hook state (`.gitignore` line 68, `.claude/state/`). It is untracked, has no git history, and is regenerated by the `enforce-python-batch-budget` PreToolUse hook whenever an agent writes a Python file. While it exists,
`tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` fails, because that test enumerates `.claude` files from the filesystem rather than from git and therefore trips on any gitignored local file under `.claude/`. A clean checkout and CI never carry that file.

The file was deleted and its absence verified immediately before this run:

- `rm -f .claude/state/python-batch-budget.default.json` — EXIT_CODE 0
- `ls -la .claude/state/` — directory present and empty (no entries beyond `.` and `..`)

Deleting restores the clean-checkout condition the test assumes. No test file, no `.gitignore` entry, and no repository file was modified to obtain this result. The same correction is recorded in `../baseline/pytest-coverage.2026-08-25T09-17.md`.

## Loop state

- Loop iteration: 1
- No file was modified by this stage. `git status --porcelain` after the run reports only the three Phase 6 evidence artifacts written by [P6-T1] through [P6-T3] as untracked, and no modification to any tracked source or test file. `artifacts/python/` is gitignored tool output and is not part of the diff.
- Stages [P6-T1] through [P6-T4] therefore completed consecutively in a single pass with no stage failing and no stage modifying a file.
