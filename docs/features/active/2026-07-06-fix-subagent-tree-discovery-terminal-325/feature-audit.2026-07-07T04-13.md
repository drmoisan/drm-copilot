# Feature Audit — fix-subagent-tree-discovery-terminal (Issue #325)

**Timestamp:** 2026-07-07T04-13
**Work mode:** `minor-audit` (per `issue.md` marker) — AC source is `issue.md`'s `## Acceptance Criteria` section only.
**Scope:** Full branch diff, `origin/main @ 4db27ebed2bde1919eda5991ff0de938204aef03` vs. `bug/fix-subagent-tree-discovery-terminal-325 @ 79ebc52950dfce8782e51faae1795eee3b528f92`.
**Nature of this audit:** Re-audit after remediation (`79ebc52`) of the two Blocking findings raised in the prior review cycle. All ten AC checkboxes were already `[x]` in `issue.md` before this audit began; each is independently re-verified below against the current head-of-branch code, not merely against the checkbox state.

## Acceptance Criteria Evaluation

| # | Criterion (verbatim) | Verdict | Evidence |
|---|---|---|---|
| 1 | Transcript discovery resolves the user-global Claude projects directory (`~/.claude/projects/`, honoring a home-dir / CLAUDE config dir override) rather than globbing `<repo>/.claude/projects/`. | **PASS** | `getClaudeProjectsRoot()` in `src/command-runtime.ts` (lines 152-168): resolves `env["CLAUDE_CONFIG_DIR"]` first (trimmed, non-empty check), then falls back to `env["HOME"]` then `env["USERPROFILE"]`, joining with `.claude/projects` (or `<configDir>/projects`). `subagent-tree-command.ts` calls `getClaudeProjectsRoot()`, not a repo-relative glob. Covered by 5 passing unit tests in `test/command-runtime.test.ts`. |
| 2 | Candidate discovery is narrowed to the encoded directory name for the current workspace path (separators and `:` replaced by `-`), verified against on-disk examples, and includes per-worktree sibling folders. | **PASS** | `encodeWorkspacePath()` replaces `[\\/:]` with `-`; `matchEncodedDirectories()` matches an exact (case-insensitive) name or a `<name>-wt-` prefix, covering per-worktree siblings including nested worktree-of-worktree names. Case-insensitivity is explicitly documented and tested (drive-letter case variance `c--Users-...` vs `C--Users-...`) in `test/lib/subagent-tree/workspace-encoding.test.ts` (120 lines). On-disk verification is recorded in `evidence/other/encoding-rule-confirmation.2026-07-07T02-50.md`. |
| 3 | Existing selection behavior is preserved: flattened `/subagents/` transcripts are excluded, a single candidate auto-selects, multiple candidates prompt via quick-pick. | **PASS** | `discoverRootSessionCandidates()` filters out any path containing `/subagents/`; `selectRootSession()` auto-returns the sole candidate when `candidates.length === 1`, otherwise calls `vscode.window.showQuickPick`. Exercised by `test/subagent-tree-command.test.ts` (364 lines) including the new multi-candidate and single-candidate cases. |
| 4 | The zero-candidates error message names the real user-global search location. | **PASS** | `selectRootSession()`: `` `No root session transcripts found under ${claudeProjectsRoot}.` `` — names the resolved user-global path, not a repo-relative string, and reports via both `output.appendLine` and `showErrorMessage`. |
| 5 | The rendered tree (existing header line plus full `formatTree` output) is written to an integrated VS Code terminal, and the terminal is revealed. | **PASS** | `registerSubagentTreeCommand()`'s command handler calls `terminalWriter.write(header, rendered)` then `terminalWriter.reveal()`, where `rendered = formatTree(tree)` and `header` is the same `` `[${COMMAND_ID}] subagent tree for ${selected}:` `` line used previously with the `OutputChannel`. The default `createTerminalWriter` is `createSubagentTreeTerminalWriter`, which backs onto a real `vscode.Pseudoterminal`-driven `vscode.Terminal`. `reveal()` calls `this.terminal?.show()`. |
| 6 | The terminal uses a stable, recognizable name and repeated runs reuse/replace a single named terminal rather than accumulating terminals. | **PASS** | `SUBAGENT_TREE_TERMINAL_NAME = "drm-copilot: Subagent Tree"` is used for every `vscode.window.createTerminal(...)` call. `PseudoterminalTerminalWriter.write()` reuses the existing terminal when `this.terminal !== undefined && this.terminal.exitStatus === undefined`, and only creates a replacement when the previous terminal has exited. `terminalWriter` is constructed once at command-registration time (not per invocation), and `test/terminal-writer.test.ts` directly asserts both the reuse case ("reuses the same terminal across repeated writes while it remains open") and the replacement case ("creates a replacement terminal once the previous terminal has exited"), plus `test/subagent-tree-command.test.ts`'s "reuses the same terminal-writer instance across two consecutive invocations". |
| 7 | Genuine errors (failures, zero-candidates, user-cancel) still route to the error path (`showErrorMessage` / diagnostic sink), not solely to the terminal. | **PASS** | The command handler's top-level `try/catch` routes any thrown error to `output.appendLine` + `showErrorMessage`; `selectRootSession()` routes both the zero-candidates case and the user-cancel case (`selectedItem === undefined`) through `output.appendLine` (and, for zero-candidates, `showErrorMessage`) and returns `undefined` without ever calling `terminalWriter.write`/`reveal`. |
| 8 | The pure module boundary is preserved: `extensions/drm-copilot/src/lib/subagent-tree/` contains no `vscode` imports and `formatTree` remains a pure string renderer; filesystem-root resolution and terminal wiring live in the host-bound command file or behind injectable seams. | **PASS** | `grep -rn "vscode" src/lib/subagent-tree/` returns only a doc-comment stating the absence of such an import, zero actual import statements. Enforced continuously by `test/lib/subagent-tree/module-boundary.test.ts`, which scans every `.ts` file under the module for `vscode` import syntax and asserts none exist. Filesystem-root resolution (`getClaudeProjectsRoot`, `getWorkspaceRoot`) and terminal wiring (`terminal-writer.ts`) both live outside `lib/subagent-tree/`. |
| 9 | The command remains testable without a live VS Code host: the terminal factory is injected the same way the `FileSystem` seam is, and unit tests assert on captured terminal output. | **PASS** | `registerSubagentTreeCommand(options)` accepts `createFileSystem?` and `createTerminalWriter?` as parallel, symmetrically-defaulted optional factories. `test/subagent-tree-command.test.ts` injects a `FakeTerminalWriter` (capturing `writes` and `revealCallCount`) in every test, asserting on `write.header`/`write.body` and reveal-call counts without touching a real VS Code host. |
| 10 | The extension toolchain passes: `npm run format`, `lint`, `typecheck`, `test:coverage`, `build`. Per-file coverage meets lines >= 85% and branches >= 75%; no production file is excluded from coverage. | **PASS** | Independently re-run during this audit from `extensions/drm-copilot/`: `format` exit 0 (all files unchanged), `lint` exit 0, `typecheck` exit 0, `test:coverage` exit 0 (134 suites / 1531 tests passed; repo-wide 96.58% lines / 88.52% branches), `build` exit 0. Per-file coverage for every file touched by this branch, parsed directly from `coverage/lcov.info`: `command-runtime.ts` 93.21%/89.74%, `runtime-detection.ts` 93.36%/82.22%, `subagent-tree-command.ts` 100%/94.74%, `terminal-writer.ts` 99%/100%, `workspace-encoding.ts` 100%/100% — all comfortably above 85%/75%. `jest.config.cjs`'s `collectCoverageFrom` excludes only `**/*.d.ts`; no production `src/**` path is excluded. |

## Acceptance Criteria Status

- Source: `docs/features/active/2026-07-06-fix-subagent-tree-discovery-terminal-325/issue.md`
- Total AC items: 10
- Checked off (delivered): 10 (all were already `[x]` before this audit; each independently re-verified PASS above — no check-off action was needed or taken by this review)
- Remaining (unchecked): 0
- Items remaining: none

## Baseline / Prior-Review Cross-Reference

The prior feature audit (`feature-audit.2026-07-07T03-30.md`) found all ten AC items PASS on their literal wording even before remediation, noting that the two Blocking findings were code-quality/policy issues outside the literal AC text (the file-size limit and the CRLF-normalization defect are both general code-change/unit-test-policy concerns, not phrased as acceptance criteria in `issue.md`). This audit confirms that assessment remains accurate post-remediation: no AC wording changed, and the remediation did not alter the functional behavior verified against any of the ten criteria — it only fixed the file-size violation and the terminal line-break defect, the latter of which is implicitly covered by AC #5's "full `formatTree` output" requirement (a staircased multi-line render would not faithfully reproduce `formatTree`'s output). AC #5 is therefore evaluated PASS with additional confidence post-remediation, since the CRLF defect could have been read as a partial violation of "full formatTree output" faithfulness before the fix.

## Verdict

**PASS — 10/10 acceptance criteria verified, 0 gaps.** No remediation-inputs artifact is produced for this feature audit; no AC-related deficiency was found.
