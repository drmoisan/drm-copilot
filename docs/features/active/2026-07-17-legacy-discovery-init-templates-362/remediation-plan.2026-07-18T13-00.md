# Remediation Plan — legacy-discovery-init-templates (#362), Cycle 1

- Timestamp: 2026-07-18T13-00
- Issue: #362
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/issue.md`)
- Branch: `feature/legacy-discovery-init-templates-362` (remediating committed `48d16f6f`)
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/remediation-inputs.2026-07-18T13-00.md` (Blocking findings R1–R4 and its Re-Verification Checklist). No scope beyond that document is authorized.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-init-templates-362`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`, `evidence/regression-testing/`, `evidence/other/`). No `artifacts/`-rooted evidence path is permitted. Do not overwrite the prior cycle's `evidence/qa-gates/phase6-*` files.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root `C:\Users\DanMoisan\repos\drm-copilot\.claude\worktrees\agent-ae68d14e27bb8727e` unless the task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- Feature #360 loader: `scripts/dev_tools/discovery/domain_profile.py`, `scripts/dev_tools/discovery/domain_profile_models.py` (nested profile shape; strict unknown-key rejection; `technology_stack.legacy` is a required non-empty string list).
- Feature #359 schemas: `schemas/discovery/v1/feature-contract.schema.json`, `schemas/discovery/v1/coverage-ledger.schema.json`, `schemas/discovery/v1/runtime-characterization-scenario.schema.json`, `schemas/discovery/v1/parity-matrix.schema.json`, `schemas/discovery/v1/unspecified-behavior-record.schema.json`, `schemas/discovery/v1/product-decision-record.schema.json`, `schemas/discovery/v1/evidence-reference.schema.json` (all use `additionalProperties: false`, require `schema_version` matching `^1\.\d+\.\d+$`, identifier fields matching `^[a-z0-9][a-z0-9._-]*$`, and timestamp fields matching the schema's ISO-8601 regex).
- `$schema` resolution rule: `scripts/dev_tools/validate_json.py` `_load_schema` no-scheme branch resolves `base_path.parent / uri`; from `docs/discovery/templates/artifacts/<name>.template.json` the correct relative path to the real schemas is `../../../../schemas/discovery/v1/<name>.schema.json`.
- Placeholder-value constraints: identifier-pattern fields must use lowercase hyphenated placeholder identifiers (for example `placeholder-feature-contract`); timestamp-pattern fields must use `1970-01-01T00:00:00Z`; enum fields must use a value from the schema's enum (for example `draft`); no value may contain any token the domain-neutrality test (`tests/scripts/dev_tools/discovery/test_domain_neutrality.py`) disallows.

### Phase 0 — Remediation Baseline Capture (Cycle 1)

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r1c1-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated.
- [x] [P0-T2] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r1c1-phase0-black-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T3] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r1c1-phase0-ruff-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T4] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r1c1-phase0-pyright-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T5] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r1c1-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records the numeric baseline line-coverage and branch-coverage percentages and the pass/fail/skip counts (including the currently-skipped `test_schema_conformance_pending_issue_9002`). Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid).

### Phase 1 — R1: Make the Discovery Templates Trackable in Git

- [x] [P1-T1] Edit `.gitignore` line 6, changing the bare token `artifacts` to the root-anchored `/artifacts`, so the pattern no longer matches nested path segments such as `docs/discovery/templates/artifacts/`. Acceptance (both must hold): `git check-ignore -q -- docs/discovery/templates/artifacts/feature-contract.template.json` exits 1 (not ignored), and `git check-ignore -q -- artifacts/orchestration/orchestrator-state.json` exits 0 (root orchestration dir remains ignored).
- [x] [P1-T2] Verify the anchor change has no unintended side effects and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/other/r1c1-gitignore-anchor-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Commands: `git status --porcelain -- docs/discovery/templates/` (must list all seven `docs/discovery/templates/artifacts/*.template.json` files as untracked `??` entries) and full `git status --porcelain` (must show no newly visible path outside `.gitignore`, `docs/discovery/templates/artifacts/`, and this feature's own `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/` files). Acceptance: the artifact records both command outputs and confirms exactly the seven template files became visible.

### Phase 2 — R4: Restore the `scripts/dev_tools/discovery/__init__.py` Re-Export Surface

- [x] [P2-T1] Restore the public re-export surface in `scripts/dev_tools/discovery/__init__.py`: recover the pre-existing feature-#360 content via `git show "$(git merge-base HEAD epic/legacy-discovery-and-parity-integration):scripts/dev_tools/discovery/__init__.py"`, reinstate every re-exported name from that version (at minimum `DomainProfile`, `DomainProfileError`, `load_domain_profile`, `parse_domain_profile_text`, `DEFAULT_PROFILE_FILENAME`, plus any additional names in that version's `__all__`), and merge the current one-line `dev.discovery.*` namespace docstring into the restored module docstring (one combined docstring; do not drop either statement). Acceptance: `poetry run python -c "from scripts.dev_tools.discovery import DomainProfile, DomainProfileError, load_domain_profile, parse_domain_profile_text, DEFAULT_PROFILE_FILENAME"` exits 0.
- [x] [P2-T2] Add regression test file `tests/scripts/dev_tools/discovery/test_package_exports.py` with a test that imports `DomainProfile`, `DomainProfileError`, `load_domain_profile`, `parse_domain_profile_text`, and `DEFAULT_PROFILE_FILENAME` from `scripts.dev_tools.discovery` and asserts each is identical (`is`) to its counterpart in `scripts.dev_tools.discovery.domain_profile` / `scripts.dev_tools.discovery.domain_profile_models`, so the package-level import surface cannot silently regress again. The test must be deterministic, use no filesystem I/O, and carry a descriptive docstring. Acceptance: `poetry run pytest tests/scripts/dev_tools/discovery/test_package_exports.py -q` exits 0.

### Phase 3 — R2: Rewrite the Domain-Profile Template to the Real #360 Nested Shape

- [x] [P3-T1] Rewrite `docs/discovery/templates/domain-profile/domain-profile.yaml` from the flat key:value document to the nested shape required by `parse_domain_profile_text` in `scripts/dev_tools/discovery/domain_profile.py`, using exactly this content (confirm optional-field spelling against `scripts/dev_tools/discovery/domain_profile_models.py` before finalizing; only keys in the loader's allowed key sets may appear):

  ```yaml
  profile_version: 1
  legacy_source:
    root: "<legacy-source-path>"
  target:
    root: "<target-path>"
  technology_stack:
    legacy:
      - "<technology-stack>"
  artifacts:
    root: "<artifact-output-dir>"
  ```

  Acceptance: `poetry run python -c "from pathlib import Path; from scripts.dev_tools.discovery.domain_profile import parse_domain_profile_text; parse_domain_profile_text(Path('docs/discovery/templates/domain-profile/domain-profile.yaml').read_text(encoding='utf-8'))"` exits 0.
- [x] [P3-T2] Add a regression test `test_domain_profile_template_parses_with_real_loader` to `tests/scripts/dev_tools/discovery/test_init_flow.py` that reads the real template text at `resolve_default_template_root() / DOMAIN_PROFILE_RELATIVE_PATH` (imports from `scripts.dev_tools.discovery.init_models`), passes it through `substitute_placeholders` (both with `tokens=None` and with a representative domain-neutral token mapping for `<legacy-source-path>`, `<target-path>`, `<technology-stack>`, `<artifact-output-dir>`), calls the real `parse_domain_profile_text` on each rendered text, and asserts the returned `DomainProfile.profile_version == 1`. No temporary files; the only filesystem access is reading the in-repo template file. Acceptance: `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -q` exits 0.

### Phase 4 — R3: Make All Seven Artifact Templates Resolve and Validate Against the Real #359 Schemas

- [x] [P4-T1] Rewrite `docs/discovery/templates/artifacts/feature-contract.template.json`: set `"$schema"` to `../../../../schemas/discovery/v1/feature-contract.schema.json`; replace `"version"` with `"schema_version": "1.0.0"`; provide all fields required by `schemas/discovery/v1/feature-contract.schema.json` (`$schema`, `schema_version`, `id`, `title`, `description`, `status`, `acceptance_criteria` with at least one item containing `id`, `statement`, `verification`), using pattern-compliant, domain-neutral placeholder values per the Ground-Truth Contracts section (identifier fields lowercase-hyphenated, `status`/`verification` from the schema enums, e.g. `draft`/`manual`). Acceptance: `poetry run python -c "import json; from pathlib import Path; import jsonschema; jsonschema.validate(json.loads(Path('docs/discovery/templates/artifacts/feature-contract.template.json').read_text(encoding='utf-8')), json.loads(Path('schemas/discovery/v1/feature-contract.schema.json').read_text(encoding='utf-8')))"` exits 0.
- [x] [P4-T2] Rewrite `docs/discovery/templates/artifacts/coverage-ledger.template.json` the same way against `schemas/discovery/v1/coverage-ledger.schema.json` (required: `$schema`, `schema_version`, `id`, `generated_at`, `subject`, `entries`, `summary`; consult the schema for the nested shapes of `subject`, `entries`, `summary`; `generated_at` = `1970-01-01T00:00:00Z`). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T3] Rewrite `docs/discovery/templates/artifacts/runtime-characterization-scenario.template.json` the same way against `schemas/discovery/v1/runtime-characterization-scenario.schema.json` (required: `$schema`, `schema_version`, `id`, `feature_id`, `title`, `preconditions`, `stimulus`, `observed_behavior`, `observation_method`, `evidence_refs`; consult the schema for enums and array item shapes). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T4] Rewrite `docs/discovery/templates/artifacts/parity-matrix.template.json` the same way against `schemas/discovery/v1/parity-matrix.schema.json` (required: `$schema`, `schema_version`, `id`, `generated_at`, `source_label`, `target_label`, `rows`; consult the schema for the `rows` item shape; `generated_at` = `1970-01-01T00:00:00Z`). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T5] Rewrite `docs/discovery/templates/artifacts/unspecified-behavior-record.template.json` the same way against `schemas/discovery/v1/unspecified-behavior-record.schema.json` (required: `$schema`, `schema_version`, `id`, `title`, `description`, `category`, `discovery_context`, `resolution_status`, `evidence_refs`; consult the schema for enums). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T6] Rewrite `docs/discovery/templates/artifacts/product-decision-record.template.json` the same way against `schemas/discovery/v1/product-decision-record.schema.json` (required: `$schema`, `schema_version`, `id`, `title`, `decision`, `status`, `rationale`; consult the schema for the `status` enum). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T7] Rewrite `docs/discovery/templates/artifacts/evidence-reference.template.json` the same way against `schemas/discovery/v1/evidence-reference.schema.json` (required: `$schema`, `schema_version`, `id`, `kind`, `location`, `captured_at`, `description`; consult the schema for the `kind` enum; `captured_at` = `1970-01-01T00:00:00Z`). Acceptance: the equivalent `jsonschema.validate` one-liner for this template/schema pair exits 0.
- [x] [P4-T8] In `tests/scripts/dev_tools/discovery/test_init_flow.py`, remove the `@pytest.mark.skip` marker from `test_schema_conformance_pending_issue_9002`, rename the test to `test_generated_artifacts_conform_to_real_schemas`, rewrite its docstring to state it validates against the merged `schemas/discovery/v1/` files (removing the factually inaccurate "no schema files exist" premise), and implement its body: seed `FakeFileSystem` with the real template texts read from `resolve_default_template_root()` for each entry in `EXPECTED_TEMPLATE_RELATIVE_PATHS`, run `create_discovery_workspace`, then for each of the seven artifact entries in `OUTPUT_RELATIVE_PATHS` `json.loads` the rendered instance and `jsonschema.validate` it against its real schema file loaded from `schemas/discovery/v1/<name>.schema.json`. No temporary files; the only filesystem access is reading in-repo template and schema files. Acceptance: `poetry run pytest tests/scripts/dev_tools/discovery/test_init_flow.py -q` exits 0 with zero skipped tests in that file.
- [x] [P4-T9] Run `poetry run dev.validate-json` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/other/r1c1-validate-json.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` confirming the governed-glob validation passes with the seven `docs/discovery/templates/artifacts/*.template.json` files now resolvable via `_load_schema`. Acceptance: the command exits 0 and the artifact exists with all four fields.

### Phase 5 — Re-Check Reverted Acceptance Criteria in spec.md and user-story.md

- [x] [P5-T1] In `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md`: (a) revise acceptance criterion 3's text to describe the real nested domain-profile shape required by the merged feature-#360 loader (replacing the stale "flat single-level `key: value`" wording), and update the corresponding stale prose in "Upstream Dependencies (planned contracts)" and the "Template location" layout comment to record that #360's nested contract is merged and adopted; (b) after confirming the Phase 1–4 acceptance commands passed, mark acceptance criteria 1, 3, 4, and 5 as `[x]`. Do not mark criterion 9 in this task. Acceptance: criteria 1, 3, 4, 5 are `[x]`, criterion 3's text no longer claims a flat document, and criterion 9 remains unchanged pending Phase 7.
- [x] [P5-T2] In `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md`: apply the same criterion-3 text revision and, after confirming the Phase 1–4 acceptance commands passed, mark acceptance criteria 1, 3, 4, and 5 as `[x]`. Do not mark criterion 9 in this task. Acceptance: criteria 1, 3, 4, 5 are `[x]` and criterion 9 remains unchanged pending Phase 7.

### Phase 6 — Commit and Clean-Checkout Re-Verification (R1)

- [x] [P6-T1] Stage and commit all remediation changes on `feature/legacy-discovery-init-templates-362` (at minimum: `.gitignore`, the seven `docs/discovery/templates/artifacts/*.template.json` files, `docs/discovery/templates/domain-profile/domain-profile.yaml`, `scripts/dev_tools/discovery/__init__.py`, `tests/scripts/dev_tools/discovery/test_init_flow.py`, `tests/scripts/dev_tools/discovery/test_package_exports.py`, updated `spec.md`/`user-story.md`, this plan, and all new evidence files) with a professional, factual commit message referencing issue #362 remediation cycle 1. Acceptance: `git status --porcelain` is empty and `git ls-tree -r HEAD --name-only` lists all 8 files under `docs/discovery/templates/`.
- [x] [P6-T2] Perform the clean-checkout re-verification from the remediation-inputs checklist: run `git worktree add --detach "$TEMP/r1c1-clean-check-362" HEAD`, list all files under `$TEMP/r1c1-clean-check-362\docs\discovery` (must be exactly the 8 template files), then run the discovery test package from the clean checkout — with the clean checkout as cwd, run `poetry run pytest tests/scripts/dev_tools/discovery -q`; if poetry provisions a different environment for that directory, instead invoke the root project's interpreter (`poetry env info --path` from the repository root) as `<env-path>\Scripts\python.exe -m pytest tests/scripts/dev_tools/discovery -q` from the clean-checkout cwd — and confirm `test_domain_neutrality` raises no `FileNotFoundError`. Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/regression-testing/r1c1-clean-checkout-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` listing the 8 files and the pytest result. Acceptance: all 8 files are present in the detached checkout and the pytest run exits 0.
- [x] [P6-T3] Remove the temporary detached worktree: `git worktree remove --force "$TEMP/r1c1-clean-check-362"`. Acceptance: `git worktree list` no longer shows the `r1c1-clean-check-362` path.

### Phase 7 — Final QA Loop (Python)

- [x] [P7-T1] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r1c1-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P7-T1. Acceptance: exit 0 with no files changed, recorded in the artifact.
- [x] [P7-T2] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r1c1-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P7-T1. Acceptance: exit 0 recorded in the artifact.
- [x] [P7-T3] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r1c1-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P7-T1. Acceptance: exit 0 with 0 errors recorded in the artifact.
- [x] [P7-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r1c1-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-change line-coverage and branch-coverage percentages, the pass count, and explicit confirmation that `test_generated_artifacts_conform_to_real_schemas`, `test_domain_profile_template_parses_with_real_loader`, and the `test_package_exports.py` test all passed with zero skips in `tests/scripts/dev_tools/discovery/`. If this stage fails, fix, then restart the loop from P7-T1. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid).
- [x] [P7-T5] Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r1c1-coverage-delta.<TS>.md` comparing the Phase 0 baseline coverage (from `evidence/remediation-baseline/r1c1-phase0-pytest-baseline.<TS>.md`) against the P7-T4 post-change coverage, reporting: baseline line/branch %, post-change line/branch %, and the coverage of changed/new code (`scripts/dev_tools/discovery/__init__.py` and the new/changed tests' production targets), and verifying line >= 85%, branch >= 75%, and no regression on changed lines. Acceptance: the artifact contains all numeric values and every threshold check passes.
- [x] [P7-T6] After P7-T4/P7-T5 confirm the coverage thresholds and zero-skip test state, mark acceptance criterion 9 as `[x]` and update its schema-conformance sub-clause wording (the test is now implemented against the merged `schemas/discovery/v1/` files, no longer "tracked as dependent on feature 9002") in both `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/spec.md` and `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/user-story.md`. Acceptance: criterion 9 is `[x]` in both files with accurate wording.
- [x] [P7-T7] Commit all Phase 7 evidence artifacts, the criterion-9 document updates, and any file changes produced by QA-loop restarts. If any production, test, or template file changed during the Phase 7 loop (beyond evidence and the two AC documents), re-run the P6-T2 clean-checkout verification against the new HEAD and write a fresh `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/regression-testing/r1c1-clean-checkout-verification.<TS>.md`; if only evidence and AC documents changed, record that determination in the commit message and this re-run is not required. Acceptance: `git status --porcelain` is empty and `git ls-tree -r HEAD --name-only` still lists all 8 files under `docs/discovery/templates/`.

## Exit Condition

All Phase 0–7 tasks complete with their acceptance criteria satisfied; R1–R4 fixes verified by the commands recorded above; fresh QA evidence exists under `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/` without overwriting the prior cycle's `phase6-*` files. Per the remediation-inputs Re-Verification Checklist item 6, PR creation remains blocked until a subsequent feature-review cycle reports zero Blocking findings; that re-review is outside this plan's scope.
