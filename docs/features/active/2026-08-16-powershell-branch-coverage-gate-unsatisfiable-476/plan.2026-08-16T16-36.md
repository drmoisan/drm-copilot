# 2026-08-16-powershell-branch-coverage-gate-unsatisfiable (Plan)

- **Issue:** #476
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-16T16-36
- **Status:** Ready for Preflight
- **Version:** 1.0
- **Work Mode:** full-bug (`spec.md` is the sole acceptance-criteria source; no `user-story.md` exists)
- **Spec:** `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/spec.md` (AC1-AC16)
- **Research:** `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/research/2026-08-16T17-30-powershell-branch-coverage-gate-research.md`
- **Branch:** `bug/powershell-branch-coverage-gate-unsatisfiable-476` (base `main` at `687380a6`)

## Plan Conventions

- `FEATURE` denotes `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476`. Every evidence path below is written in full; the abbreviation appears only in prose.
- `<ts>` in an artifact filename denotes the execution-time ISO-8601 timestamp `yyyy-MM-ddTHH-mm` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`.
- Every command-bearing evidence artifact must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Test artifacts for coverage-bearing runs must record numeric coverage values in `Output Summary:`.
- **Fail-closed evidence rule:** if any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or has incomplete fields, the corresponding task remains unchecked and the outcome is BLOCKED or INCOMPLETE, never PASS.
- **No-SKIPPED rule:** every command task in this plan is unconditional. `EXIT_CODE: SKIPPED` is not a valid outcome for any task.
- **Mirror-pairing rule:** every task that edits a shipped root file edits its bundle mirror in the same task. No intermediate commit or task boundary may leave a root/mirror pair divergent.

## Scope Summary

Prose-only policy alignment: repository policy currently requires PowerShell branch coverage `>= 75%`, but Pester 5.6.1 cannot measure branch coverage in any output format (the branch denominator is 0, so the threshold is unevaluable). The fix adds a carve-out to `.claude/rules/powershell.md` structurally parallel to the bash precedent at `.claude/rules/shell.md:68-70` and qualifies the branch clause at every other site that binds it to PowerShell. The coverage hook `.claude/hooks/validate-feature-review-coverage.ps1` already implements the target policy and is not modified (AC10).

Closed edit surface — exactly 17 files (AC14):

| Pair | Root file | Bundle mirror |
| --- | --- | --- |
| 1 | `.claude/rules/powershell.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/rules/powershell.md` |
| 2 | `.claude/rules/general-unit-test.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-unit-test.md` |
| 3 | `.claude/rules/quality-tiers.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/rules/quality-tiers.md` |
| 4 | `.claude/skills/feature-review-workflow/SKILL.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/skills/feature-review-workflow/SKILL.md` |
| 5 | `.claude/agents/feature-review.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/agents/feature-review.md` |
| 6 | `.claude/skills/powershell-qa-gate/SKILL.md` | `extensions/drm-copilot/resources/claude-customizations/.claude/skills/powershell-qa-gate/SKILL.md` |
| 7 | `.agents/skills/general-unit-test/SKILL.md` | `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-unit-test/SKILL.md` |
| 8 | `.agents/skills/quality-tiers/SKILL.md` | `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/quality-tiers/SKILL.md` |
| — | `README.md` (non-shipped consistency edit, no mirror) | — |

Non-goals (fixed by spec; do not re-litigate): no AST branch collector, no command-coverage gate, no change to `.claude/hooks/validate-feature-review-coverage.ps1` or its Pester suite, no `.github/**` edit, no change to `.claude/rules/shell.md`, no change to `pester.runsettings.psd1`, no change to Python/TypeScript/C# branch gates or rule files.

Invariant guards (spec failure conditions):
1. No wording may lower, remove, or condition the `>= 85%` line-coverage threshold (AC2).
2. No wording may read as excluding PowerShell files from coverage *measurement*; at least one amended statement must make the threshold-versus-measurement distinction explicit (AC3).

## AC Coverage Map

| AC | Tasks |
| --- | --- |
| AC1 | P1-T1, P4-T1 |
| AC2 | P1-T1, P4-T2 |
| AC3 | P1-T1, P1-T2, P4-T3 |
| AC4 | P1-T2, P1-T3, P2-T1, P2-T2, P4-T2 |
| AC5 | P4-T5 |
| AC6 | P2-T3 |
| AC7 | P3-T1, P3-T2 |
| AC8 | P1-T1..T3, P2-T1..T3, P3-T1..T2, P4-T7 |
| AC9 | P5-T1, P5-T3 |
| AC10 | P4-T6 |
| AC11 | P4-T6 |
| AC12 | P3-T3 |
| AC13 | P2-T3, P4-T4 |
| AC14 | P4-T8 |
| AC15 | P5-T2, P5-T3 |
| AC16 | P4-T9 |

### Phase 0 — Policy Compliance and Baseline Capture

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, and the structural precedent `.claude/rules/shell.md` (lines 60-70) — and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields and lists all seven files.
- [x] [P0-T2] Record the git baseline: run `git rev-parse HEAD` and `git status --porcelain` on branch `bug/powershell-branch-coverage-gate-unsatisfiable-476` and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/git-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (commit SHA and clean/dirty state). Acceptance: the artifact exists with all four fields and records the HEAD SHA.
- [x] [P0-T3] Capture the baseline for the binding parity and completeness suites: run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py` from the repository root and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/pytest-parity-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: the artifact exists with all four fields and `EXIT_CODE: 0`.
- [x] [P0-T4] Capture the full Python test baseline with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/pytest-full-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric line-coverage and branch-coverage percentages and the pass count. Acceptance: the artifact exists with all four fields and numeric coverage values.
- [x] [P0-T5] Capture the extension Jest baseline with coverage: run `npm run test:coverage` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/jest-coverage-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric coverage summary values and the suite pass count. Acceptance: the artifact exists with all four fields and numeric coverage values.
- [x] [P0-T6] Capture the pre-change inventory grep: run a case-insensitive grep for `branch coverage|branch-coverage` across `.claude/`, `.agents/`, `.codex/`, `README.md`, `extensions/drm-copilot/resources/claude-customizations/`, and `extensions/drm-copilot/resources/codex-and-agents-customizations/` (excluding `docs/features/**` and `docs/research/**`) and write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/baseline/branch-coverage-grep-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing every file:line that currently binds the branch `>= 75%` threshold to PowerShell (expected: the 8 root sites of research R1 plus their mirrors and `README.md:298`). Acceptance: the artifact exists with all four fields and the pre-change match list.
- [x] [P0-T7] Write the fail-before exception dossier `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/regression-testing/fail-before-exception.<ts>.md` containing `Timestamp:`, `WhyFailingRunImpossible:` (this is a prose-only policy defect; research R5 verified that no test in `tests/**` or `extensions/drm-copilot/test/**` asserts on the branch-coverage wording of any affected file, so no deterministic regression test can fail before the fix), and an alternative-proof section referencing the P0-T6 grep artifact as the pre-change defect evidence, with the P4-T9 sweep as the post-change counterpart. Acceptance: the dossier exists with `Timestamp:`, `WhyFailingRunImpossible:`, and the alternative-proof section.

### Phase 1 — Root Rule-File Edits (root and mirror together)

- [x] [P1-T1] In `.claude/rules/powershell.md` (Testing Standards list, lines 63-65), replace the branch-coverage bullet at line 64 with a carve-out structurally parallel to `.claude/rules/shell.md:68-70` containing all four parts in order: (1) name Pester and state the metrics it actually measures — command (instruction) coverage and line coverage; (2) preserve the uniform line threshold explicitly with the `.claude/rules/quality-tiers.md` cross-reference (`>= 85%`); (3) state that branch coverage is not measurable by Pester for PowerShell; (4) state that there is no PowerShell branch-coverage gate. Add one sentence making the threshold-versus-measurement distinction explicit (PowerShell production files remain in the coverage denominator per the Coverage Exclusion Policy in `.claude/rules/general-unit-test.md`; this carve-out removes an unevaluable threshold, not a measurement obligation). Leave the line-coverage bullet (line 63) and the changed-lines no-regression bullet (line 65) byte-unchanged. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/rules/powershell.md` in this same task. Acceptance: both files are byte-identical, contain all four parts and the measurement-distinction sentence, and lines 63 and 65 content is unmodified. (AC1, AC2, AC3, AC8)
- [x] [P1-T2] In `.claude/rules/general-unit-test.md` (Coverage Requirements, line 24), qualify the branch-coverage threshold so it applies only to languages whose tooling measures branch coverage, naming PowerShell (Pester) and bash (kcov) as the two exceptions where only the line threshold applies; do not modify the line-coverage bullet or the no-regression bullet, and do not attach any wording that excludes PowerShell from coverage measurement. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/rules/general-unit-test.md` in this same task. Acceptance: both files are byte-identical, only the branch clause is qualified, and the line and no-regression clauses remain unconditional. (AC3, AC4, AC8)
- [x] [P1-T3] In `.claude/rules/quality-tiers.md`, apply the same branch-capable-language qualification at the three sites: line 25 (uniform-thresholds statement), line 34 (uniform-thresholds branch bullet), and line 51 (rationale paragraph); leave the line-coverage row and the no-regression row unconditional. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/rules/quality-tiers.md` in this same task. Acceptance: both files are byte-identical, all three sites carry the qualification attached to the branch clause only, and the line row and no-regression row are unmodified. (AC4, AC8)

### Phase 2 — Review-Surface and QA-Gate Skill Edits (root and mirror together)

- [x] [P2-T1] In `.claude/skills/feature-review-workflow/SKILL.md` (coverage threshold bullets, lines 111-114), qualify the branch `>= 75%` clauses (new files, modified files, repo-wide per language) so they apply only to branch-capable languages; the line `>= 85%` clause and the no-regression clause remain unconditional for all coverage languages including PowerShell, and PowerShell remains enumerated as a coverage language. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/feature-review-workflow/SKILL.md` in this same task. Acceptance: both files are byte-identical, the qualification attaches only to the branch clauses, and PowerShell's enumeration and line-clause obligations are unchanged. (AC4, AC8)
- [x] [P2-T2] In `.claude/agents/feature-review.md` (coverage threshold bullets, lines 112-114), apply the same branch-clause qualification as P2-T1; the line clause and no-regression clause remain unconditional and the PowerShell coverage-artifact table row is unchanged. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/agents/feature-review.md` in this same task. Acceptance: both files are byte-identical and only the branch clauses are qualified. (AC4, AC8)
- [x] [P2-T3] In `.claude/skills/powershell-qa-gate/SKILL.md` (line 45), remove or qualify the `branch coverage >= 75%` clause so no branch threshold binds to PowerShell; retain the `>= 85%` line-coverage requirement at that site; attach no numeric threshold to command (instruction) coverage. Apply the identical edit to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/powershell-qa-gate/SKILL.md` in this same task. Acceptance: both files are byte-identical, no branch threshold remains at the site, the line threshold is retained, and no command-coverage threshold is introduced. (AC6, AC8, AC13)

### Phase 3 — Codex Surface and README Edits

- [x] [P3-T1] In `.agents/skills/general-unit-test/SKILL.md` (line 29), apply the same branch-clause qualification as P1-T2 (branch threshold qualified to branch-capable languages, naming PowerShell and bash as exceptions; line clause and no-regression clause unconditional). Apply the identical edit to `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/general-unit-test/SKILL.md` in this same task. Acceptance: both files are byte-identical and carry AC4-equivalent semantics. (AC7, AC8)
- [x] [P3-T2] In `.agents/skills/quality-tiers/SKILL.md` (lines 30, 39, 56), apply the same three-site qualification as P1-T3. Apply the identical edit to `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/quality-tiers/SKILL.md` in this same task. Acceptance: both files are byte-identical, all three sites qualified on the branch clause only, line and no-regression statements unconditional. (AC7, AC8)
- [x] [P3-T3] In `README.md` (line 298), amend the toolchain summary so it no longer states a uniform branch-coverage `>= 75%` requirement covering PowerShell, while preserving the `>= 85%` line-coverage statement. `README.md` is non-shipped and has no bundle mirror. Acceptance: the amended line preserves the line threshold and binds no branch threshold to PowerShell. (AC12)

### Phase 4 — Guard and Scope Verification

- [x] [P4-T1] Verify AC1: read the amended carve-out in `.claude/rules/powershell.md` side-by-side with `.claude/rules/shell.md:68-70` and match each of the four structural parts. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac1-carveout-structure.<ts>.md` quoting the carve-out and mapping each part 1-4 to its sentence. Acceptance: the artifact maps all four parts with quotations.
- [x] [P4-T2] Verify AC2 and AC4: run `git diff` over the eight edited root files plus `README.md` and confirm (a) no `>= 85%` line-coverage statement was lowered, removed, or made conditional (`.claude/rules/powershell.md:63` textually unchanged), and (b) in every shared file the qualification attaches to the branch clause and to no other clause, with the no-regression clause unconditional. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac2-ac4-line-threshold-guard.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` and a per-file verdict. Acceptance: the artifact records a pass verdict for every edited file on both checks.
- [x] [P4-T3] Verify AC3: read every amended passage in all 17 files and confirm none states or implies that PowerShell files are excluded from coverage measurement, then quote the explicit threshold-versus-measurement distinction added in P1-T1. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac3-measurement-obligation.<ts>.md` with the per-passage read-through result and the quoted distinction sentence with its file and line. Acceptance: the artifact records zero measurement-exclusion readings and locates the explicit distinction.
- [x] [P4-T4] Verify AC13: read every amended passage and confirm no numeric threshold attaches to command or instruction coverage anywhere; command coverage appears only descriptively. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac13-no-command-gate.<ts>.md` with the per-passage result. Acceptance: the artifact records zero command-coverage thresholds.
- [x] [P4-T5] Verify AC5: grep the amended shared files for the `>= 75%` branch statement scoped to branch-capable languages (Python, TypeScript, C#) and run `git diff --name-only` to confirm `.claude/rules/python.md`, `.claude/rules/typescript.md`, and `.claude/rules/csharp.md` are unmodified. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac5-branch-capable-unweakened.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact shows the retained `>= 75%` branch statement and the three unmodified rule files.
- [x] [P4-T6] Verify AC10 and AC11: run `git diff --name-only` and confirm no change to `.claude/hooks/validate-feature-review-coverage.ps1`, to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-feature-review-coverage.ps1`, or to any path under `.github/`. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac10-ac11-untouched-surfaces.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact shows zero matches for the hook, its mirror, and `.github/**` in the changed-file list.
- [x] [P4-T7] Verify AC8: compute file hashes (`Get-FileHash -Algorithm SHA256`) for each of the 8 root/mirror pairs in the Scope Summary table and confirm each pair is byte-identical. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac8-byte-parity.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the 8 hash pairs. Acceptance: all 8 pairs have matching hashes.
- [x] [P4-T8] Verify AC14: run `git diff --name-only` against base `main` and reconcile the output against the closed 17-file list (8 root files, 8 bundle mirrors, `README.md`) plus this feature's `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/**` artifacts; no other file may appear. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac14-edit-surface.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` and the reconciliation table. Acceptance: the reconciliation shows exactly the 17 enumerated files plus feature-folder evidence/documentation and nothing else.
- [x] [P4-T9] Verify AC16 (late inventory sweep): re-run the P0-T6 case-insensitive grep for `branch coverage|branch-coverage` across `.claude/`, `.agents/`, `.codex/`, `README.md`, and both bundle payload trees (excluding `docs/features/**` and `docs/research/**`) and confirm no remaining statement binds an unqualified branch `>= 75%` requirement to PowerShell; additionally confirm via `git diff` that `.claude/rules/shell.md` is unchanged. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/ac16-inventory-sweep.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` comparing the post-change match list against the P0-T6 baseline list. Acceptance: zero remaining unqualified PowerShell branch-threshold bindings and an unchanged `shell.md`.

### Phase 5 — Final QA Loop

The change set is Markdown-only: no Python, TypeScript, PowerShell, or C# source file is modified, so the formatting, linting, and type-checking stages have no in-scope inputs and the binding QA surface is the test stage below (spec Test Strategy; research R5). If any task in this phase fails or changes files, remediate and re-run the phase from P5-T1 until all tasks pass in a single sequence.

- [x] [P5-T1] Run the binding parity and completeness suites: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py` from the repository root. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/final-pytest-parity.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: `EXIT_CODE: 0`. (AC9)
- [x] [P5-T2] Run the full Python suite with coverage: `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/final-pytest-full.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric line-coverage and branch-coverage percentages and the pass count. Acceptance: `EXIT_CODE: 0` with numeric coverage values recorded. (AC15)
- [x] [P5-T3] Run the extension Jest suite with coverage: `npm run test:coverage` from `extensions/drm-copilot/` (includes the pack-completeness twin `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts`). Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/final-jest-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric coverage summary and suite pass count. Acceptance: `EXIT_CODE: 0` with numeric coverage values recorded. (AC9, AC15)
- [x] [P5-T4] Compare coverage against baseline: reconcile the numeric values in `evidence/qa-gates/final-pytest-full.<ts>.md` and `evidence/qa-gates/final-jest-coverage.<ts>.md` against `evidence/baseline/pytest-full-baseline.<ts>.md` and `evidence/baseline/jest-coverage-baseline.<ts>.md` (paths under `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/`). Because no production or test code changes, the expected delta is zero; any regression is a blocking defect requiring remediation and a Phase 5 restart. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/coverage-delta.<ts>.md` recording baseline, post-change, and delta values per suite. Acceptance: the artifact records numeric baseline and post-change values with no regression.
- [x] [P5-T5] Confirm a clean single pass: verify P5-T1 through P5-T4 completed in one uninterrupted sequence with no failures and no file modifications; if not, the phase was restarted and this task records the final clean sequence. Write `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/evidence/qa-gates/final-qa-clean-pass.<ts>.md` with `Timestamp:` and the list of the four final-QA artifacts from the clean pass. Acceptance: the artifact lists all four artifacts from a single clean sequence.

### Phase 6 — Documentation and Status

- [x] [P6-T1] Update `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/spec.md`: check each of AC1-AC16 whose verification artifact exists, citing the evidence artifact path per criterion, and update `Last Updated` and `Status`. Acceptance: all 16 AC checkboxes are checked and each is traceable to a named evidence artifact.
- [x] [P6-T2] Update `docs/features/active/2026-08-16-powershell-branch-coverage-gate-unsatisfiable-476/issue.md`: record the fix outcome (policy aligned with Pester capability; hook unchanged; edit surface closed at 17 files), confirm the two out-of-scope follow-ups remain recorded (AST-instrumentation branch collector; `CodeCoverage.Path` allow-list conflict), and note any deviations from scope (expected: none). Acceptance: `issue.md` contains the outcome record and both follow-up entries.
