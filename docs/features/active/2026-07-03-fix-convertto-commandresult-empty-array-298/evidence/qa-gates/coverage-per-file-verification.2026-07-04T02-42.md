Timestamp: 2026-07-04T02-42
Command: Select-String -Path artifacts/pester/powershell-coverage.xml -Pattern 'sourcefilename="Invoke-FullReleaseFlow.ps1"' followed by manual extraction of the enclosing <class>'s top-level <counter type="LINE" missed="..." covered="..."> attributes; percentage computed as covered / (covered + missed) * 100
EXIT_CODE: 0
Output Summary: The `<class name=".../Invoke-FullReleaseFlow" sourcefilename="Invoke-FullReleaseFlow.ps1">` element's top-level `<counter type="LINE" missed="6" covered="90" />` (confirmed identically at the `<sourcefile name="Invoke-FullReleaseFlow.ps1">` level). Computed line coverage = 90 / (90 + 6) * 100 = 93.75%. This is non-zero and >= 85%.

Provenance note: this coverage.xml was generated by a fresh-process direct invocation of `Invoke-PoshQCTest` (the same underlying function `mcp__drm-copilot__run_poshqc_test` wraps) after `Import-Module ./scripts/powershell/PoshQC -Force`, because the MCP tool's long-running session had cached settings from before the P2-T1 edit and did not pick up the new `CodeCoverage.Path` entry across repeated calls (see `evidence/qa-gates/test-final.<timestamp>.md` for full detail on this session-staleness finding). The settings file on disk is confirmed correct independently via `Import-PowerShellDataFile` and `Select-String` match count = 1 (P2-T2).
