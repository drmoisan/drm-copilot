# Definition of Done Reconciliation

Timestamp: 2026-08-20T13-54
Task: [P12-T14]
Issue: #486
Source: the `## Definition of Done` section of `docs/features/active/2026-08-17-reject-unfalsifiable-acceptance-gates-in-atomic-plans-486/spec.md` (lines 175-182)

Command: per-item verification against the evidence artifacts named in each row below.

EXIT_CODE: 0

Output Summary: 6 of 8 Definition-of-Done items checked off. Two items are left `[ ]`: item 1, because spec `AC7` is not checked off, and item 2, because it restates the same `G5_SEVERITY`-versus-false-positive-count divergence recorded in `evidence/qa-gates/ac-reconciliation.2026-08-20T13-50.md`. `[P12-T14]` is therefore NOT checked off in the plan.

| # | Item | Result |
| --- | --- | --- |
| 1 | Every acceptance criterion below is checked off, with the named test or observable command recorded. | **NOT SATISFIED — left `[ ]`.** 17 of 18 criteria are `[x]`. Spec `AC7` is `[ ]` for the reasons recorded in `evidence/qa-gates/ac-reconciliation.2026-08-20T13-50.md`. The naming half of this item is satisfied for all 18 criteria by that same artifact. |
| 2 | The G5 corpus measurement artifact exists under `<FEATURE>/evidence/qa-gates/` and G5's shipped severity matches the recorded false-positive count. | **NOT SATISFIED — left `[ ]`.** The artifact exists: `evidence/qa-gates/g5-corpus-measurement.2026-08-20T12-02.md`. The second clause is the same substance as `AC7`: the recorded false-positive count is 0 and the shipped severity is `warning`, whereas the spec's one-conjunct rule pairs a false-positive count of 0 with Blocking. The severity matches the artifact's own two-conjunct determination but not the rule as the spec states it. Left unchecked for consistency with the `AC7` disposition rather than resolved by the executor. |
| 3 | Python and TypeScript ship in the same change and produce identical finding strings for the shared fixture set. | **SATISFIED — `[x]`.** Both runtimes are in this branch's change set (`evidence/qa-gates/branch-diff-file-list.2026-08-20T14-48.md`). Parity: `evidence/qa-gates/parity-fixture-run.2026-08-20T14-32.md` records 8 fixtures compared with 0 mismatches, asserted by `tests/scripts/dev_tools/test_plan_gate_parity.py::test_parity_findings_match_expected_strings` and `plan-gate-parity.test.ts` `produces the same finding strings as the Python runtime`. |
| 4 | `.claude/hooks/validate-planner-output.ps1` is unmodified. | **SATISFIED — `[x]`.** Re-verified in this pass: a porcelain status query scoped to that path produced 0 lines and a name-only committed-diff query against `main` scoped to that path produced 0 lines. Recorded in `evidence/qa-gates/branch-diff-file-list.2026-08-20T14-48.md`. |
| 5 | `.claude/rules/plan-acceptance-gates.md` exists and records the G1-G6 rule set, both severity decisions, and the scope-of-invocation argument against a grandfathering mechanism; `.claude/skills/atomic-plan-contract/SKILL.md` cross-references it. | **SATISFIED — `[x]`.** The file exists and carries the G1-G6 rule table, the `### G5 — fixed by the corpus measurement and by nothing else` and `### G6 — ships as a Warning` sections, and the `## Scope of Invocation — no grandfathering or exemption mechanism` section. Counted matches: `G6` 13, `grandfathering` 2, `pythonRepr` 4, `g5-corpus-measurement` 1. The cross-reference is at `.claude/skills/atomic-plan-contract/SKILL.md:160`, with the wrap-tolerant authoring guidance at lines 164 and 169. |
| 6 | Unit tests cover every row of the `--cov` classification table, every literal-checkability branch, both attribution-window boundaries, and the graceful-degradation path. | **SATISFIED — `[x]`.** `--cov` table: `test_plan_gate_discrimination_cov.py::test_cov_value_classification` is parametrized over 10 rows (placeholder, `.py`, `.py::TestFoo`, slash-with-no-suffix, tracked directory, dotted module, `.`, empty value, space-separated, neighbouring `--cov-branch`/`--cov-report` flags), with G2 and G3 resolution covered by `test_plan_gate_discrimination_context.py::test_g2_tracked_module_path_is_blocking`, `::test_g3_unresolvable_path_is_warning`, and `::test_tracked_directory_produces_no_finding`. Literal-checkability branches: `::test_checkable_literal_skips_regex_metacharacters`, `::test_checkable_literal_accepts_fixed_string_flag`, `::test_checkable_literal_skips_placeholder_operand`. Attribution-window boundaries: `test_plan_gate_commands.py::test_extract_plan_commands_attributes_continuation_bullet_to_task` (inside the window) against `::test_extract_plan_commands_skips_document_preamble`, `::test_extract_plan_commands_skips_phase_preamble`, and `::test_extract_plan_commands_skips_span_after_intervening_heading` (outside it), plus the G6 line-window boundary at `::test_window_boundary_five_lines_apart_is_not_joined`. Graceful degradation: `::test_context_free_call_skips_context_rules` and `::test_failing_git_adapter_produces_no_findings`. Each has a named TypeScript counterpart. |
| 7 | Line coverage >= 85% and branch coverage >= 75% for the new modules in both runtimes, measured with the dotted-module form on the Python side. | **SATISFIED — `[x]`.** Numeric values, from `evidence/qa-gates/coverage-delta.2026-08-20T13-36.md`: Python `scripts/dev_tools/plan_gate_commands.py` line 100.00 / branch 100.00, `scripts/dev_tools/plan_gate_discrimination.py` line 98.21 / branch 90.54; TypeScript `src/lib/validate/plan-gate-commands.ts` line 96.24 / branch 84.93, `src/lib/validate/plan-gate-discrimination.ts` line 100 / branch 97.91, `src/lib/validate/plan-gate-rules.ts` line 97.71 / branch 89.39. The Python measurement used the dotted-module form `--cov=scripts.dev_tools.plan_gate_commands` and `--cov=scripts.dev_tools.plan_gate_discrimination`, with the non-vacuous-measurement proof recorded in `evidence/qa-gates/python-test.2026-08-20T13-21.md`. |
| 8 | Full toolchain pass in both runtimes (format, lint, type-check, test) with no stage auto-fixing files on the final pass. | **SATISFIED — `[x]`.** Python: `evidence/qa-gates/python-format.2026-08-20T13-18.md` (0 reformatted of 433), `python-lint.2026-08-20T13-19.md` (all checks passed, 0 fixes), `python-typecheck.2026-08-20T13-20.md` (0 errors over 433 files analyzed), `python-test.2026-08-20T13-21.md` (3998 passed). TypeScript: `typescript-format.2026-08-20T13-25.md` (398 files, all `(unchanged)`), `typescript-lint.2026-08-20T13-27.md` (390 files linted, 0 errors, 0 warnings), `typescript-typecheck.2026-08-20T13-29.md` (0 diagnostics over a 475-file program), `typescript-test.2026-08-20T13-32.md` (193 suites, 2621 tests passed). No stage rewrote a file, so the loop completed in one pass and no restart was triggered. |

## Note on the coverage-item citation requirement

`[P12-T14]`'s acceptance clause requires that "the coverage item cites the numeric values recorded in `evidence/qa-gates/coverage-delta.<ts>.md`". Item 7 above cites those numeric values and names the artifact. The citation is recorded here rather than appended to the spec's Definition-of-Done line, because appending it would modify the item text; the same text-preservation reasoning recorded in `evidence/qa-gates/ac-reconciliation.2026-08-20T13-50.md` applies.
