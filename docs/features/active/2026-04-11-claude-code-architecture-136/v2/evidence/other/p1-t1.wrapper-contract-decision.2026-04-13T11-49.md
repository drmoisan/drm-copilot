Timestamp: 2026-04-13T12:22:00-04:00
Decision: Use one JSON-encoded transport field for multi-folder `scan_folders` at the live MCP wrapper boundary.

Encoding Rule:
- `extensions/drm-copilot/src/repo-automation-service.ts` must serialize `scanFolders` as a JSON array string and pass it once as `-ScanFoldersJson <json>`.

Decoding Rule:
- Each live bundled wrapper must accept `-ScanFoldersJson` as a single string parameter.
- Each wrapper must decode `ScanFoldersJson` with `ConvertFrom-Json` to a `string[]`.
- Each wrapper must pass the decoded `string[]` once to the underlying `Invoke-PoshQC*` command as `-ScanFolders $scanFolders`.

Files That Must Honor This Contract:
- `extensions/drm-copilot/src/repo-automation-service.ts`
- `extensions/drm-copilot/resources/templates/run-poshqc-format.ps1`
- `extensions/drm-copilot/resources/templates/run-poshqc-analyze.ps1`
- `extensions/drm-copilot/resources/templates/run-poshqc-test.ps1`

Out of Scope:
- The resolved settings/schema, coverage-output-path, and stale-token findings remain closed.
- The contract decision does not change the approved live MCP verification commands in final QA.
