# Push-Down Payload Contract Test — [P5-T8]

Timestamp: 2026-08-28T12-46

Command: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts`

EXIT_CODE: 1

ExpectedExitCode: 1

This task's acceptance admits two branches. Branch **(b)** applies: the test failed, and the failure
message names exactly the path the branch permits.

## Verbatim Assertion Message

```
    for relative_path in repo_runtime_files:
>       assert (
            relative_path in bundled_files
        ), f"Repo file missing from bundle: {relative_path}"
E       AssertionError: Repo file missing from bundle: .claude\state\powershell-batch-budget.default.json
E       assert WindowsPath('.claude/state/powershell-batch-budget.default.json') in [WindowsPath('.claude/agent-memory/epic-orchestrator/feedback_commit_push_memory_before_pr.md'), ...]

tests\scripts\dev_tools\test_push_down_claude_resource_contracts.py:120: AssertionError
=========================== short test summary info ===========================
FAILED tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts
============================== 1 failed in 0.13s ==============================
```

## The Path the Message Names

```
.claude\state\powershell-batch-budget.default.json
```

This is exactly the default PowerShell batch-budget state file under .claude/state, spelled
`powershell-batch-budget.default.json`. It is the only path the message names. The branch (b)
restriction — that any other path fails this task — is therefore satisfied.

## Branch (b) Conditions, Each Substantiated

| Condition | Evidence |
| --- | --- |
| The path lies outside this feature's declared file scope | The plan's File Scope section declares seven production paths and three test paths. None is under .claude/state. This change writes no file under that directory. |
| It is gitignored by the .gitignore entry `.claude/state/` | `git check-ignore -v .claude/state/powershell-batch-budget.default.json` exits 0 and reports `.gitignore:68:.claude/state/`, naming line 68 of `.gitignore` as the matching rule. |
| It is therefore not a repository file | `git ls-files .claude/state/` produces empty output: no file under that directory is tracked. |
| It is the pre-existing local-only condition of repository issue #510 | Issue #510 records that the bundle-parity test fails locally on gitignored `.claude` state while passing in CI. The specification's Assumptions section records the same condition, and Risk 2 of the specification directs that the path named in the failure be read before treating it as a regression from this change. |

No remediation of that path is attempted, as this task's text directs.

## Why This Failure Is Absent From the [P0-T10] Baseline

The [P0-T10] full-suite baseline reported 4195 passed and 0 failed, so this node ID passed at
baseline. The reason is timing, not a change in this branch: the `.claude/state/` directory did not
exist when the baseline ran. Directory and file modification times observed in this worktree place
the directory's creation after the baseline run, and the two state files it holds are written by the
batch-budget hooks as this session's own PowerShell and Python tool invocations proceed.

The file is untracked, gitignored, machine-local session state generated by the tooling of this
execution run. It is not produced by any edit this change makes. This is recorded here because
[P6-T4] is judged against the [P0-T10] baseline failing set, which is empty; the reconciliation is
carried out in the [P6-T4] artifact.

## Addendum — Confirming Run After Environment-Parity Restoration ([P6-T4])

This section is an addendum recorded after [P6-T4]. It does not alter the [P5-T8] result above, which
stands as branch (b) with `EXIT_CODE: 1` and no remediation attempted.

At [P6-T4] the untracked, gitignored `.claude/state/` directory — session state created by the
batch-budget hooks after the [P0-T10] baseline was captured — was removed to restore the environment
the baseline was taken in. The same node ID was then re-run:

```
Command: poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts
EXIT_CODE: 0

tests\scripts\dev_tools\test_push_down_claude_resource_contracts.py .    [100%]

============================== 1 passed in 0.09s ==============================
```

The bundle itself was never changed between the two runs. The confirming run establishes the
substantive claim the acceptance criterion makes: every edited file under `.claude` matches its
bundled counterpart, and the earlier failure was caused solely by session-generated local state
outside this change's file scope.

Output Summary: `EXIT_CODE: 1` with `ExpectedExitCode: 1`, taking branch (b) of this task's
acceptance. The run reports `1 failed`. The assertion message is `Repo file missing from bundle:
.claude\state\powershell-batch-budget.default.json`, which names exactly the default PowerShell
batch-budget state file under .claude/state spelled `powershell-batch-budget.default.json` and no
other path. That path lies outside this feature's declared file scope; it is gitignored by the
`.gitignore` entry `.claude/state/` at line 68, confirmed by `git check-ignore -v`; it is therefore
not a repository file, confirmed by an empty `git ls-files .claude/state/`; and it is the
pre-existing local-only condition recorded as repository issue #510, which is green in CI. No
remediation of that path is attempted. This task discharges AC11.
