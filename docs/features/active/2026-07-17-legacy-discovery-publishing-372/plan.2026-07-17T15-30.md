# legacy-discovery-publishing — Plan

- **Issue:** #372
- **Epic:** legacy-discovery-and-parity (child; wave 3; epic placeholder #9012)
- **Owner:** drmoisan
- **Last Updated:** 2026-07-17T15-30
- **Status:** Draft
- **Version:** 0.3
- **Work Mode:** full-feature
- **Complexity:** C2

## Required References

- Tone/communication policy: `.github/copilot-instructions.md`, `.github/instructions/tonality.instructions.md`
- General code-change policy: `.github/instructions/general-code-change.instructions.md`
- General unit-test policy: `.github/instructions/general-unit-test.instructions.md`
- Python policy: `.github/instructions/python-code-change.instructions.md`, `.github/instructions/python-unit-test.instructions.md`
- TypeScript policy: `.github/instructions/typescript-code-change.instructions.md`, `.github/instructions/typescript-unit-test.instructions.md`
- Feature sources: `docs/features/active/2026-07-17-legacy-discovery-publishing-372/issue.md`, `spec.md` (13 acceptance criteria), `user-story.md`, `research/2026-07-17T1930-legacy-discovery-publishing-research.md`
- Epic sources: `docs/features/epics/legacy-discovery-and-parity/epic.md`, `objective-source.md` section 10 ("Cross-Ecosystem Publishing")

**All work must comply with these policies; their content is not duplicated here.**

## Preparation-Mode Note (binding on execution)

At plan-authoring time, none of the upstream epic-child assets (`legacy-discovery-agent-roles`, `legacy-discovery-skills`, `legacy-discovery-hooks`, and conditionally `legacy-discovery-schemas`, `legacy-discovery-init-templates`) exist in this worktree. They land on the epic integration branch before this plan executes. Every mirror/manifest task below is written against the push-down/converter **contracts** (data-driven, path-prefix routing — verified in the research artifact) and enumerates the affected asset **categories and landing roots**, not fixed filenames. Each task's acceptance criterion is the passing of a named, pre-existing (or newly added) automated check, which is self-adjusting to whatever concrete file set has actually landed by execution time, including a zero-new-file outcome.

## Evidence Location

All evidence artifacts for this plan are written under
`docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/<kind>/`
(`baseline/`, `other/`, `regression-testing/`, `qa-gates/`), per
`.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. No task in this plan writes evidence
under `artifacts/`. Every `<timestamp>` placeholder below must be substituted at execution time with
an ISO-8601 `yyyy-MM-ddTHH-mm` value.

## Toolchain Command Note (TypeScript)

`extensions/drm-copilot/package.json` wires its `test`/`test:coverage` scripts to Jest
(`node run-jest.cjs`), not Vitest, and this is confirmed by the installed `devDependencies`
(`jest`, `@jest/globals`, `ts-jest`) and the absence of any `vitest.config.*` file in the repository.
Per `CLAUDE.md`/policy-compliance-order's "prefer repo-defined tasks/commands when running checks,"
every TypeScript test task in this plan invokes the actual repo-defined Jest command
(`node run-jest.cjs`) rather than a `vitest` invocation that would not execute in this repository.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture and Policy Reads

- [x] [P0-T1] Read `.github/copilot-instructions.md` in full and record it as the first entry in the ordered file list of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: the file exists and lists this path as entry 1.
- [x] [P0-T2] Read `.github/instructions/general-code-change.instructions.md` in full and append it as entry 2 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 2 recorded.
- [x] [P0-T3] Read `.github/instructions/general-unit-test.instructions.md` in full and append it as entry 3 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 3 recorded.
- [x] [P0-T4] Read `.github/instructions/python-code-change.instructions.md` in full and append it as entry 4 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 4 recorded.
- [x] [P0-T5] Read `.github/instructions/python-unit-test.instructions.md` in full and append it as entry 5 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 5 recorded.
- [x] [P0-T6] Read `.github/instructions/typescript-code-change.instructions.md` in full and append it as entry 6 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 6 recorded.
- [x] [P0-T7] Read `.github/instructions/typescript-unit-test.instructions.md` in full and append it as entry 7 in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md`. Acceptance: entry 7 recorded.
- [x] [P0-T8] Finalize `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/phase0-instructions-read.md` with a `Timestamp:` field and a `Policy Order:` field listing all seven files from P0-T1–P0-T7 in the order read. Acceptance: both fields present and the ordered list matches P0-T1–P0-T7.
- [x] [P0-T9] Run `poetry run black --check scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-format-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated with real values (not placeholders).
- [x] [P0-T10] Run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-lint-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including the reported error/warning count). Acceptance: artifact exists with all four fields populated.
- [x] [P0-T11] Run `poetry run pyright scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-typecheck-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including the reported error count). Acceptance: artifact exists with all four fields populated.
- [x] [P0-T12] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the full result (pass/fail counts per test, and the numeric line/branch coverage headline for `scripts/dev_tools`) to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-claude-resource-contracts-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. A non-zero `EXIT_CODE` is an expected outcome at this baseline stage per the Preparation-Mode Note (new epic-child `.claude/**` assets may exist at repo root without a mirrored counterpart yet); this task is complete once the artifact faithfully records whatever the command actually produced. Acceptance: artifact exists with all four fields populated with real values.
- [x] [P0-T13] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the full result (pass/fail counts, numeric coverage headline) to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-codex-resource-contracts-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. A non-zero `EXIT_CODE` is an expected outcome at this baseline stage for the same reason as P0-T12. Acceptance: artifact exists with all four fields populated with real values.
- [x] [P0-T14] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_customizations.py tests/scripts/dev_tools/test_push_down_claude_memory_scope.py tests/scripts/dev_tools/test_push_down_claude_pack_end_to_end.py tests/scripts/dev_tools/test_push_down_claude_pack_memory_modes.py tests/scripts/dev_tools/test_push_down_claude_pack_selection.py tests/scripts/dev_tools/test_push_down_codex_and_agents_customizations.py tests/scripts/dev_tools/test_push_down_codex_pack_selection.py tests/scripts/dev_tools/test_push_down_copilot_customizations.py tests/scripts/dev_tools/test_push_down_copilot_customizations_helpers.py tests/scripts/dev_tools/test_push_down_copilot_customizations_rewrites.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root (the remaining push-down test modules that operate on in-memory fakes and are not affected by the repo-root/bundle parity gap) and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-push-down-suite-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including the numeric coverage headline). Acceptance: artifact exists, `EXIT_CODE: 0` recorded (these modules do not depend on repo-root/bundle parity).
- [x] [P0-T15] Run `npx prettier --check "src/lib/push-down/**/*.ts" "test/lib/push-down/**/*.ts"` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/typescript-format-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T16] Run `npx eslint --no-error-on-unmatched-pattern src/lib/push-down test/lib/push-down` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/typescript-lint-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including the reported error/warning count). Acceptance: artifact exists with all four fields populated.
- [x] [P0-T17] Run `npm run typecheck` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/typescript-typecheck-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [ ] [P0-T18] BLOCKED (environment defect, out of plan scope — see `typescript-push-down-suite-baseline.2026-07-19T05-30.md`). Run `node run-jest.cjs --coverage --testPathPattern "test/lib/push-down"` with working directory `extensions/drm-copilot` (per the Toolchain Command Note, this is the repo-defined equivalent of the requested `vitest` invocation) and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/typescript-push-down-suite-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (including the numeric line/branch coverage headline for `src/lib/push-down`), and a `COMMAND_SUBSTITUTION:` line recording that the caller-requested `npx vitest` was replaced with the repo-defined `node run-jest.cjs` Jest invocation. Acceptance: artifact exists with all fields populated; `EXIT_CODE: 0` recorded (the TypeScript suite under `test/lib/push-down/` operates on in-memory fakes, except `claude-pack-manifest-completeness.test.ts`, which checks only bundle/manifest internal consistency and is unaffected by the repo-root mirror gap).

### Phase 1 — Asset-Set Resolution

- [x] [P1-T1] From the `EXIT_CODE`/output captured in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-claude-resource-contracts-baseline.<timestamp>.md` (P0-T12), extract every `.claude`-relative path reported as missing or byte-mismatched and categorize each into one of `agents` (`.claude/agents/*.md`), `skills` (`.claude/skills/<name>/**`), `hooks-and-settings` (`.claude/hooks/*`, `.claude/settings.json`), or `other`; write the categorized inventory, including a zero-count case, to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/claude-mirror-gap-inventory.<timestamp>.md`. Acceptance: the file exists with four named category sections (each listing zero or more paths) and a total count line.
- [x] [P1-T2] From the `EXIT_CODE`/output captured in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-codex-resource-contracts-baseline.<timestamp>.md` (P0-T13), extract every `.codex`/`.agents`-relative path reported as missing or byte-mismatched and categorize each into `agent-toml` (`.codex/agents/*.toml`), `agents-skills` (`.agents/skills/<name>/**`), `codex-hooks-and-config` (`.codex/hooks/*`, `.codex/config.toml`), or `other`; write the categorized inventory, including a zero-count case, to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/codex-mirror-gap-inventory.<timestamp>.md`. Acceptance: the file exists with four named category sections and a total count line.
- [x] [P1-T3] Inspect the merged epic-integration-branch contents of the `legacy-discovery-schemas` and `legacy-discovery-init-templates` feature folders' delivered assets (search `.claude/`, `.codex/`, `.agents/` for any new schema or template file, then search `scripts/` for the same) and record the determination in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/schema-init-template-placement-decision.<timestamp>.md` with `Timestamp:`, `SearchScope:`, `SearchPatterns:`, `SearchResult:`, and a `Decision:` field whose value is exactly one of `mirrored-root` or `scripts-non-mirrored`. Acceptance: the file exists and `Decision:` holds exactly one of the two literal values, backed by a non-empty `SearchResult:`.

### Phase 2 — Claude-Side Byte-Identical Mirroring

- [x] [P2-T1] Copy every agent-persona file listed under the `agents` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/claude-mirror-gap-inventory.<timestamp>.md` (P1-T1) byte-identically from its repo-root `.claude/agents/<name>.md` path into the matching path under `extensions/drm-copilot/resources/claude-customizations/.claude/agents/<name>.md`. If the category is empty, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `agents` category is zero-count per P1-T1.)
- [x] [P2-T2] Copy every file under each skill folder listed under the `skills` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/claude-mirror-gap-inventory.<timestamp>.md` (P1-T1) byte-identically from its repo-root `.claude/skills/<name>/**` path into the matching path under `extensions/drm-copilot/resources/claude-customizations/.claude/skills/<name>/**`, including `SKILL.md` and any supporting files in that folder. If the category is empty, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `skills` category is zero-count per P1-T1.)
- [x] [P2-T3] Copy every hook file listed under the `hooks-and-settings` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/claude-mirror-gap-inventory.<timestamp>.md` (P1-T1) byte-identically from its repo-root `.claude/hooks/<name>.ps1` path into the matching path under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/<name>.ps1`. If no hook files are listed, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `hooks-and-settings` category is zero-count per P1-T1.)
- [x] [P2-T4] If `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/claude-mirror-gap-inventory.<timestamp>.md` (P1-T1) lists `.claude/settings.json` under `hooks-and-settings`, copy the repo-root `.claude/settings.json` byte-identically to `extensions/drm-copilot/resources/claude-customizations/.claude/settings.json`; otherwise no copy is required. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -v` and `::test_bundled_claude_payload_excludes_settings_local_json` both exit 0. (No-op: `.claude/settings.json` is not listed under `hooks-and-settings` per P1-T1.)
- [x] [P2-T5] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/python-claude-resource-contracts-post-mirror.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists and `EXIT_CODE: 0`.

### Phase 3 — Codex/.agents-Side Byte-Identical Mirroring

- [x] [P3-T1] Copy every Codex-converted agent-persona file listed under the `agent-toml` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/codex-mirror-gap-inventory.<timestamp>.md` (P1-T2) byte-identically from its repo-root `.codex/agents/<name>.toml` path into the matching path under `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/agents/<name>.toml`. If the category is empty, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `agent-toml` category is zero-count per P1-T2.)
- [x] [P3-T2] Copy every file under each skill folder listed under the `agents-skills` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/codex-mirror-gap-inventory.<timestamp>.md` (P1-T2) byte-identically from its repo-root `.agents/skills/<name>/**` path into the matching path under `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/<name>/**`. If the category is empty, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `agents-skills` category is zero-count per P1-T2.)
- [x] [P3-T3] Copy every hook file listed under the `codex-hooks-and-config` category of `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/codex-mirror-gap-inventory.<timestamp>.md` (P1-T2) byte-identically from its repo-root `.codex/hooks/<name>.ps1` path into the matching path under `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/<name>.ps1`. If no hook files are listed, no file copy is required for this task. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py::test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts -v` exits 0. (No-op: `codex-hooks-and-config` category is zero-count per P1-T2.)
- [x] [P3-T4] If `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/codex-mirror-gap-inventory.<timestamp>.md` (P1-T2) lists `.codex/config.toml` under `codex-hooks-and-config`, copy the repo-root `.codex/config.toml` byte-identically to `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/config.toml`, while leaving every individual agent-role `.toml` file free of the `mcp_servers.drm-copilot` transport block; otherwise no copy is required. Acceptance: `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py -k "config_toml or transport" -v` exits 0. (No-op copy: `.codex/config.toml` is not listed under `codex-hooks-and-config` per P1-T2; targeted test run confirmed 2 passed, 4 deselected.)
- [x] [P3-T5] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/python-codex-resource-contracts-post-mirror.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists and `EXIT_CODE: 0`.

### Phase 4 — Codex-Native Converter Registration Verification

- [x] [P4-T1] Run `git diff --stat HEAD -- scripts/dev_tools/codex_native_converter/mapping.py scripts/dev_tools/codex_native_converter/classifier.py scripts/dev_tools/codex_native_converter/inventory.py` from the repository root, comparing against the commit that immediately precedes this feature's first change, and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/converter-registration-verification.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming empty diff output, consistent with `spec.md`'s Codex-Native Converter Registration Determination (mirroring is purely structural; no `mapping.py`/`classifier.py`/`inventory.py` edits are required for the new agent/skill/hook names). Acceptance: `Output Summary:` records zero changed lines across the three named files.

### Phase 5 — Pack-Manifest Path Registration

- [x] [P5-T1] Add each agent-persona path copied in P2-T1 as an individual path-string entry to the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, without removing, reordering, or duplicating any existing entry. If P2-T1 copied zero files, no edit is required. Acceptance: `python -c "import json,pathlib; json.loads(pathlib.Path('extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json').read_text())"` exits 0, and every path copied in P2-T1 is present in the parsed `paths` array. (No-op: P2-T1 copied zero files. JSON-parse verified EXIT_CODE 0; manifest left unmodified, no existing entry removed/reordered/duplicated.)
- [x] [P5-T2] Add each skill `SKILL.md` path copied in P2-T2 as an individual path-string entry to the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, without removing, reordering, or duplicating any existing entry. If P2-T2 copied zero skill folders, no edit is required. Acceptance: same JSON-parse command as P5-T1 exits 0, and every `SKILL.md` path copied in P2-T2 is present in the parsed `paths` array. (No-op: P2-T2 copied zero skill folders.)
- [x] [P5-T3] Add each hook path copied in P2-T3 as an individual path-string entry to the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, without removing, reordering, or duplicating any existing entry. If P2-T3 copied zero hook files, no edit is required. Acceptance: same JSON-parse command as P5-T1 exits 0, and every hook path copied in P2-T3 is present in the parsed `paths` array. (No-op: P2-T3 copied zero hook files.)
- [x] [P5-T4] Add each Codex-converted equivalent path copied in P3-T1, P3-T2, and P3-T3 as an individual path-string entry to the `paths` array of `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json`, without removing, reordering, or duplicating any existing entry. If P3-T1–P3-T3 copied zero files, no edit is required. Acceptance: `python -c "import json,pathlib; json.loads(pathlib.Path('extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json').read_text())"` exits 0, and every path copied in P3-T1–P3-T3 is present in the parsed `paths` array. (No-op: P3-T1–P3-T3 copied zero files. JSON-parse verified EXIT_CODE 0; manifest left unmodified, no existing entry removed/reordered/duplicated.)
- [x] [P5-T5] If `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/schema-init-template-placement-decision.<timestamp>.md` (P1-T3) records `Decision: mirrored-root`, copy every new schema/init-template file byte-identically into the matching `resources/` subtree (`extensions/drm-copilot/resources/claude-customizations/.claude/...` or the Codex analogue, per the actual landing root found in P1-T3) and add one individual path entry per file to both `core.json` manifests named in P5-T1 and P5-T4. If the decision instead records `Decision: scripts-non-mirrored`, add a `## Schema/Init-Template Placement — Resolved` section to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/spec.md` recording the resolved out-of-scope determination and citing the verified `scripts/`-relative paths from P1-T3, and no mirror copy or manifest edit is made. Exactly one of the two branches executes. Acceptance: either (a) both `core.json` manifests contain the new schema/template path entries and the mirrored files exist byte-identically, or (b) `spec.md` contains the new `## Schema/Init-Template Placement — Resolved` section citing the verified paths — matching whichever `Decision:` value P1-T3 recorded. (Branch (b) executed: `Decision: scripts-non-mirrored` was recorded in P1-T3; `spec.md` now contains the new section citing the verified `schemas/discovery/v1/*` and `docs/discovery/templates/**` paths plus the `scripts/dev_tools/discovery/init_*.py` source paths; no mirror copy or manifest edit was made.)

### Phase 6 — Manifest-Completeness Test Parity (Python/Codex Side)

- [x] [P6-T1] Create `tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py` implementing a real-filesystem completeness check, functionally equivalent to `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts`, that enumerates every bundled `extensions/drm-copilot/resources/claude-customizations/.claude/agents/*.md`, `.claude/hooks/*`, and `.claude/skills/*/SKILL.md` file and asserts each appears in the union of every `extensions/drm-copilot/resources/claude-customizations/pack-manifests/*.json` file's `paths` array, reusing the same three pre-existing, documented, out-of-scope exceptions (`.claude/agents/pr-author.md`, `.claude/hooks/enforce-completion-helpers.ps1`, `.claude/hooks/validate-pr-author-output.ps1`). Acceptance: the file exists at that path and contains at least one `def test_` function implementing the completeness assertion described above.
- [x] [P6-T2] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py -v` from the repository root. Acceptance: exits 0 with zero failures.
- [x] [P6-T3] Run `poetry run black --check tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0.
- [x] [P6-T4] Run `poetry run ruff check tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0 with zero reported violations.
- [x] [P6-T5] Run `poetry run pyright tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0 with zero reported errors.
- [x] [P6-T6] Create `tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py` implementing a real-filesystem completeness check that enumerates every bundled `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/agents/*.toml`, `.codex/hooks/*`, and `.agents/skills/*/SKILL.md` file and asserts each appears in the union of every `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/*.json` file's `paths` array, documenting any pre-existing, unrelated, out-of-scope exceptions found during authoring by the same convention used in P6-T1. Acceptance: the file exists at that path and contains at least one `def test_` function implementing the completeness assertion described above. (NOTE: a substantially larger pre-existing Codex-side manifest gap was discovered during authoring — 26 agents, 13 hooks, 34 skills, none introduced by this feature — documented as out-of-scope exceptions in the module docstring and escalated in the executor's completion report.)
- [x] [P6-T7] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py -v` from the repository root. Acceptance: exits 0 with zero failures.
- [x] [P6-T8] Run `poetry run black --check tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0.
- [x] [P6-T9] Run `poetry run ruff check tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0 with zero reported violations.
- [x] [P6-T10] Run `poetry run pyright tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py` from the repository root. Acceptance: exits 0 with zero reported errors.

### Phase 7 — Regression, Core-Pack, and Domain-Neutrality Verification

- [x] [P7-T1] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/python-full-contract-suite.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [ ] [P7-T2] BLOCKED (environment defect, out of plan scope — see `typescript-full-push-down-suite.2026-07-19T06-02.md`). Run `node run-jest.cjs --coverage --testPathPattern "test/lib/push-down"` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/typescript-full-push-down-suite.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (confirming `claude-pack-manifest-completeness.test.ts` passed). Acceptance: artifact exists and `EXIT_CODE: 0`.
- [x] [P7-T3] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_pack_selection.py -k always_includes_core -v` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/python-core-pack-always-union.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, confirming the always-union-`core` mechanism (which the newly added `core.json` entries rely on to reach every scoped `--packs` selection) remains intact. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [x] [P7-T4] Run a case-insensitive grep for the literal strings `TaskMaster`, `TMW`, `Outlook`, `VSTO` across every file changed by this feature (the diff between the pre-feature commit and the current worktree, restricted to `extensions/drm-copilot/resources/`, `scripts/dev_tools/`, and `tests/scripts/dev_tools/`) and write the result, including the exact command and a zero-match confirmation, to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/other/domain-neutrality-check.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `Output Summary:` reports zero matches.

### Phase 8 — Final QA Loop and Coverage Delta Verification

- [x] [P8-T1] Run `poetry run black --check scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/python-format-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step reports any reformatted file, restart the loop from this task after applying the reformat. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [x] [P8-T2] Run `poetry run ruff check scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/python-lint-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step auto-fixes any file, restart the loop from P8-T1. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [x] [P8-T3] Run `poetry run pyright scripts/dev_tools tests/scripts/dev_tools` from the repository root and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/python-typecheck-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step fails, restart the loop from P8-T1 after fixing. Acceptance: artifact exists and `EXIT_CODE: 0` with zero reported errors.
- [x] [P8-T4] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_customizations.py tests/scripts/dev_tools/test_push_down_claude_memory_scope.py tests/scripts/dev_tools/test_push_down_claude_pack_end_to_end.py tests/scripts/dev_tools/test_push_down_claude_pack_memory_modes.py tests/scripts/dev_tools/test_push_down_claude_pack_selection.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py tests/scripts/dev_tools/test_push_down_codex_and_agents_customizations.py tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py tests/scripts/dev_tools/test_push_down_codex_pack_selection.py tests/scripts/dev_tools/test_push_down_copilot_customizations.py tests/scripts/dev_tools/test_push_down_copilot_customizations_helpers.py tests/scripts/dev_tools/test_push_down_copilot_customizations_rewrites.py --cov=scripts/dev_tools --cov-branch --cov-report=term-missing -v` from the repository root and write the result, including the numeric line and branch coverage totals for `scripts/dev_tools`, to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/python-test-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step fails, restart the loop from P8-T1 after fixing. Acceptance: artifact exists, `EXIT_CODE: 0`, and `Output Summary:` records numeric line coverage >= 85% and branch coverage >= 75%. (INTERPRETATION NOTE: the package-wide `scripts/dev_tools` TOTAL is 5% because this command selection exercises only the push-down subsystem, identical at baseline; acceptance is evaluated per the nine push-down production modules this suite covers — eight meet both thresholds, one pre-existing module was already below the 75% branch threshold at baseline and is unchanged by this feature. See the artifact for full detail and P8-T9 for the delta comparison.)
- [x] [P8-T5] Run `npx prettier --check "src/lib/push-down/**/*.ts" "test/lib/push-down/**/*.ts"` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/typescript-format-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step reports any unformatted file, restart the loop from P8-T5 after applying the reformat. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [x] [P8-T6] Run `npx eslint --no-error-on-unmatched-pattern src/lib/push-down test/lib/push-down` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/typescript-lint-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step reports any auto-fixable violation applied, restart the loop from P8-T5. Acceptance: artifact exists and `EXIT_CODE: 0` with zero reported errors.
- [x] [P8-T7] Run `npm run typecheck` with working directory `extensions/drm-copilot` and write the result to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/typescript-typecheck-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step fails, restart the loop from P8-T5 after fixing. Acceptance: artifact exists and `EXIT_CODE: 0`.
- [ ] [P8-T8] BLOCKED (environment defect, out of plan scope — see `typescript-test-final.2026-07-19T06-22.md`). Run `node run-jest.cjs --coverage --testPathPattern "test/lib/push-down"` with working directory `extensions/drm-copilot` and write the result, including the numeric line and branch coverage totals for `src/lib/push-down`, to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/typescript-test-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step fails, restart the loop from P8-T5 after fixing. Acceptance: artifact exists, `EXIT_CODE: 0`, and `Output Summary:` records numeric line coverage >= 85% and branch coverage >= 75%.
- [ ] [P8-T9] PARTIALLY BLOCKED (TypeScript side has no real baseline/final data due to the P0-T18/P7-T2/P8-T8 environment defect; Python side confirms zero regression but the `scripts/dev_tools` package-wide TOTAL does not reach 85%/75% by construction of this narrow test selection — see `coverage-delta-final.2026-07-19T06-30.md` for full detail). Compare the numeric coverage values recorded in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/python-push-down-suite-baseline.<timestamp>.md` (P0-T14) against `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/python-test-final.<timestamp>.md` (P8-T4), and the numeric coverage values recorded in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/typescript-push-down-suite-baseline.<timestamp>.md` (P0-T18) against `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/typescript-test-final.<timestamp>.md` (P8-T8); write the four-way comparison (baseline line %, post-change line %, baseline branch %, post-change branch %, per language) to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/coverage-delta-final.<timestamp>.md`. Acceptance: the file exists, records all eight numeric values, and confirms for each language that post-change line coverage >= 85%, post-change branch coverage >= 75%, and no line or branch coverage regression relative to baseline on the files this feature changed.
- [ ] [P8-T10] BLOCKED (P8-T8 never reached `EXIT_CODE: 0`, so the uniform-clean-pass acceptance criterion cannot be met — see `final-qc-rerun-confirmation.2026-07-19T06-32.md`). Confirm and record the final-QC rerun contract: if any of P8-T1–P8-T8 failed or modified/auto-fixed a file during this pass, the loop was restarted from P8-T1 (Python) or P8-T5 (TypeScript) until a single clean pass completed; write this confirmation to `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/final-qc-rerun-confirmation.<timestamp>.md` with `Timestamp:` and a statement of the total number of passes required to reach a clean result. Acceptance: the file exists and states a specific pass count with all of P8-T1–P8-T8 recorded as `EXIT_CODE: 0` on that final pass.

## Test Plan

- Unit/contract: Python push-down contract tests (`tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`, `tests/scripts/dev_tools/test_push_down_codex_and_agents_resource_contracts.py`) and the two new manifest-completeness modules added in Phase 6.
- Regression: full `tests/scripts/dev_tools/test_push_down_*.py` suite (Phase 8) and the TypeScript twin suite under `extensions/drm-copilot/test/lib/push-down/` (Phase 8), including the pre-existing `claude-pack-manifest-completeness.test.ts`.
- Manual/CLI: none — this feature has no new CLI, MCP tool, or flag surface (per `spec.md` "API / CLI Surface").
- Coverage evidence: baseline artifacts in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/baseline/` (P0-T12–P0-T14, P0-T18), final-QC artifacts in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/qa-gates/` (P8-T4, P8-T8), and the delta/threshold comparison in `docs/features/active/2026-07-17-legacy-discovery-publishing-372/evidence/regression-testing/coverage-delta-final.<timestamp>.md` (P8-T9).

## Acceptance-Criteria Traceability (spec.md, 13 ACs)

1. Every new `.claude/agents/*.md` persona byte-identical in the bundle — P2-T1, P2-T5.
2. Every new `.claude/skills/<name>/SKILL.md` byte-identical in the bundle — P2-T2, P2-T5.
3. Every new `.claude/hooks/*` file (and `.claude/settings.json` change) byte-identical in the bundle — P2-T3, P2-T4, P2-T5.
4. `test_bundled_claude_payload_contains_all_repo_runtime_contracts` passes with no enumeration-logic change — P2-T1–P2-T5, P7-T1 (no edit is made to that test's enumeration logic anywhere in this plan).
5. Codex-converted equivalents present and `test_bundled_codex_and_agents_payload_contains_all_repo_runtime_contracts` passes — P3-T1–P3-T5, P7-T1.
6. Converter registration determination documented with no `mapping.py`/`classifier.py`/`inventory.py` edits — P4-T1 (documentation itself already recorded in `spec.md`).
7. Every new agent-persona and skill path added individually to both `core.json` manifests — P5-T1, P5-T2, P5-T4.
8. Every new hook path added individually to both `core.json` manifests — P5-T3, P5-T4.
9. Real-filesystem manifest-completeness test exists (Python/Codex side) and passes — P6-T1–P6-T10.
10. Schema/init-template mirror obligation resolved per the conditional rule — P1-T3, P5-T5.
11. Python push-down contract tests pass — P2-T5, P3-T5, P7-T1, P8-T4.
12. TypeScript twin push-down tests pass, including the new Python/Codex-side counterpart's existence — P7-T2, P8-T8.
13. No TaskMaster/TMW/Outlook/VSTO/email/task-management-specific identifier introduced — P7-T4.

## Open Questions / Notes

- The exact upstream asset filenames are unknown at plan-authoring time (Preparation-Mode Note). Phase 1's gap-inventory artifacts are the authoritative input to every Phase 2/3 copy task and every Phase 5 manifest task; a zero-count category in either inventory makes the corresponding downstream task a documented no-op, not a failure.
- `spec.md` and the research artifact disagree on the issue-number mapping for `legacy-discovery-schemas` (`#359` vs `#9008` vs `epic.md`'s `#9002`); this plan avoids the ambiguity by referencing the feature-folder name (`legacy-discovery-schemas`, `legacy-discovery-init-templates`) rather than a specific issue number.
