# root-vscode-test-entrypoint-unrunnable (Plan)

- **Issue:** #421
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-26T03-00
- **Status:** Ready for Preflight
- **Version:** 1.0
- **Work Mode:** full-bug (`spec.md` is the sole authoritative acceptance-criteria source; `user-story.md` is intentionally absent)
- **AC Source:** `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/spec.md` (AC1–AC11)
- **Branch:** `bug/vscode-test-integration-entrypoint` (base `fb483b8468204e4385b5583c3b3ec4c0a987eede`)

**Fail-closed evidence rule:** Explicit baseline artifact tasks, final-QA artifact tasks, and a coverage-comparison task are included for TypeScript (the sole coverage-bearing language in scope). If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or records placeholder values, the outcome is BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Every evidence-producing task names its artifact path. Do not mark an evidence-backed task complete without the artifact on disk containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (command-bearing tasks). Substitute `<timestamp>` in artifact filenames with the actual ISO-8601 `yyyy-MM-ddTHH-mm` value at execution time.

## Global Constraints (apply to every task)

1. **Forbidden files — MUST NOT be modified by any task:** `run-jest.cjs`, `jest.config.cjs`, `.claude/rules/**`, `.agents/skills/**`, `extensions/drm-copilot/resources/claude-customizations/**`, the root `package.json` `dependencies`/`devDependencies`/`overrides`/manifest fields, and `package-lock.json`. If any task appears to require touching one of these files, STOP, leave the task unchecked, and report the conflict; do not improvise a workaround. The devDependency cleanup (`@vscode/test-cli`, `@vscode/test-electron`, `@types/mocha`) is explicitly out of scope (owned by a sibling orchestration).
2. **Verification constraint (spec Test Strategy):** Root `npm test` CANNOT pass inside this worktree — the path contains the dot-directory `.claude`, triggering the pre-existing jest `<rootDir>` glob-escape artifact (#414 Condition 3, `No tests found, exiting with code 1`). All local jest verification MUST use the path-independent rootDir-free invocation: `node run-jest.cjs --testMatch "**/tests/unit/**/*.test.ts" --testMatch "**/extensions/drm-copilot/test/**/*.test.ts"` (add `--coverage` where coverage is required). The authoritative verification of a passing root `npm test` is a green CI run of the new workflow against the branch head (Phase 5). No task's acceptance requires plain `npm test` to exit 0 in this worktree.
3. **Evidence locations (non-overridable):** All evidence artifacts resolve to `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/<kind>/`. Never `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or any other non-canonical path.
4. **AC tracking:** Acceptance criteria live only in `spec.md`. Check off each AC (`- [ ]` → `- [x]`) individually, evidence-first, per the `acceptance-criteria-tracking` protocol; final check-off reconciliation is Phase 6.

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read the policy files in this exact order: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/typescript.md`, (5) `.claude/rules/typescript-suppressions.md`, (6) `.claude/rules/quality-tiers.md`, (7) `.claude/rules/tonality.md`, (8) `.claude/rules/ci-workflows.md` (workflow files are in scope). Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the numbered order above), and the explicit list of files read. Acceptance: artifact exists with all three fields and lists all eight files.
- [x] [P0-T2] Record the branch/commit baseline: run `git branch --show-current` and `git rev-parse HEAD` in the worktree and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/phase0-branch-baseline.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording branch `bug/vscode-test-integration-entrypoint`, the HEAD SHA, base commit `fb483b8468204e4385b5583c3b3ec4c0a987eede`, and a note that the worktree path contains the `.claude` dot-directory (triggering #414 Condition 3). Acceptance: artifact exists with all four schema fields and both SHAs.
- [x] [P0-T3] Install root dependencies: run `npm ci` at the repository root (worktree root). Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-npm-ci-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 and artifact exists. `package-lock.json` must be unchanged afterward (`git status --porcelain package-lock.json` empty; record in the artifact).
- [x] [P0-T4] [expect-fail] Capture the fail-before state of root `npm test`: run `npm test` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/regression-testing/fail-before-npm-test.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the verbatim `@vscode/test-cli` error text (`Error: Could not find a .vscode-test file in this directory or any parent...`). Acceptance: EXIT_CODE is non-zero and the verbatim error is captured (this is the fail-before evidence for AC8(a)).
- [x] [P0-T5] [expect-fail] Capture the fail-before state of root `npm run test:integration`: run `npm run test:integration` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/regression-testing/fail-before-npm-test-integration.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the verbatim `@vscode/test-cli` error text. Acceptance: EXIT_CODE is non-zero and the verbatim error is captured.
- [x] [P0-T6] Baseline formatting: run `npm run format:check` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-format-check-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 and artifact exists.
- [x] [P0-T7] Baseline linting: run `npm run lint` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-lint-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 and artifact exists.
- [x] [P0-T8] Baseline type checking: run `npm run typecheck` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-typecheck-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 and artifact exists.
- [x] [P0-T9] Baseline TypeScript coverage (path-independent invocation per Global Constraint 2): run `node run-jest.cjs --coverage --testMatch "**/tests/unit/**/*.test.ts" --testMatch "**/extensions/drm-copilot/test/**/*.test.ts"` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-test-coverage-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including NUMERIC baseline values: line coverage %, branch coverage %, suite count, and test count. Acceptance: EXIT_CODE 0 and numeric line and branch percentages are recorded (no placeholders).
- [x] [P0-T10] Baseline CI inventory proving no workflow runs the root TypeScript toolchain: grep `.github/workflows/` for `jest`, `run-jest`, `test:unit`, `vscode-test`, and root `npm test` invocations and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/baseline/baseline-ci-inventory.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording zero matches for root-toolchain invocations. The grep is expected to return zero matches; separately record that `_drm-copilot-extension-tests.yml` runs `npm --prefix extensions/drm-copilot run test`, which is the extension's own suite and not the root TypeScript toolchain. Acceptance: artifact records that no workflow invokes the root TypeScript toolchain at baseline (AC8(b) evidence).
- [x] [P0-T11] Retrieve the prior-art regression guard: run `git show 2f67b888:tests/unit/vscode-test-removal.test.ts` and write its verbatim content to `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/other/prior-art-vscode-test-removal-2f67b888.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 and the full file content is captured for adaptation in P2-T1.

### Phase 1 — Root package.json Scripts-Block Repair

Constraint for this phase: every edit is confined to the `scripts` object of root `package.json`. No other key (`devDependencies`, `dependencies`, `overrides`, manifest fields) may change, and `package-lock.json` must not change.

- [x] [P1-T1] Edit root `package.json` scripts block: change `"test": "vscode-test"` to `"test": "node run-jest.cjs"`. Acceptance: `scripts.test` equals `node run-jest.cjs` exactly.
- [x] [P1-T2] Edit root `package.json` scripts block: delete the `"test:integration": "vscode-test"` entry. Acceptance: the `scripts` object contains no `test:integration` key.
- [x] [P1-T3] Edit root `package.json` scripts block: delete the `"compile:integration-tests"` entry. Acceptance: the `scripts` object contains no `compile:integration-tests` key.
- [x] [P1-T4] Edit root `package.json` scripts block: change `"pretest"` from `"npm run compile && npm run compile:integration-tests && npm run lint"` to `"npm run compile && npm run lint"`. Acceptance: `scripts.pretest` equals `npm run compile && npm run lint` exactly.
- [x] [P1-T5] Edit root `package.json` scripts block: remove the `".vscode-test.mjs"` glob entry from both the `format` and `format:check` script values; all other glob entries in both scripts remain unchanged. Acceptance: neither script value contains the substring `.vscode-test.mjs`.
- [x] [P1-T6] Verify the scripts-block-only change set: run `git diff -- package.json` and confirm every changed hunk lies inside the `scripts` object; run a search of all root `package.json` script values for the substring `vscode-test` and confirm zero matches. Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/scripts-block-verification.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the diff hunk inventory and the zero-match grep result. Acceptance: diff touches the scripts block only and the grep of script values returns zero matches (AC1, AC2 evidence).

### Phase 2 — Regression Guard Test

- [x] [P2-T1] Create `tests/unit/vscode-test-removal.test.ts`, adapted from the prior art captured in P0-T11. The test must: (a) Arrange by reading and parsing the committed root `package.json` (a versioned repository file; no temporary files, no mocks, no wall-clock or RNG use); (b) Assert that no root npm script value contains the substring `vscode-test`; (c) Assert that none of `.vscode-test.mjs`, `.vscode-test.js`, `.vscode-test.cjs`, `.vscode-test.json`, `tsconfig.vscode-test.json` exists at the repository root; (d) follow Arrange–Act–Assert with descriptive test names; (e) require NO change to `jest.config.cjs`, `run-jest.cjs`, or any jest configuration (its path already matches the existing `tests/unit/**/*.test.ts` `testMatch`). Acceptance: the file exists at exactly `tests/unit/vscode-test-removal.test.ts`, compiles under `npm run typecheck`, and no jest/runner config file was modified (AC3).
- [x] [P2-T2] Run the full root jest suite via the path-independent invocation `node run-jest.cjs --testMatch "**/tests/unit/**/*.test.ts" --testMatch "**/extensions/drm-copilot/test/**/*.test.ts"` at the repository root, then prove the guard suite by name with BOTH: (a) `node run-jest.cjs --listTests --testMatch "**/tests/unit/**/*.test.ts"`, whose output must include the absolute path of `tests/unit/vscode-test-removal.test.ts`; and (b) `node run-jest.cjs --testMatch "**/tests/unit/**/*.test.ts" --testPathPatterns "vscode-test-removal"`, whose output must read `Test Suites: 1 passed, 1 total` and `Ran all test suites matching vscode-test-removal.`. Do NOT require a per-suite `PASS <path>` line locally: jest emits those with ANSI cursor control and they do not survive this environment's output capture (verified — even `--verbose` yields only the summary block). Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/guard-test-local-run.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the suite/test totals, and record all three command outputs in the artifact. Acceptance: the full-suite run exits 0 with all suites passing, and both name-proof outputs are recorded (AC3, AC4-local, AC7 evidence).

### Phase 3 — CI Wiring (Root TypeScript Toolchain)

Note per `.claude/rules/ci-workflows.md`: the new workflow contains no deliberately-failing nested commands, so the `$LASTEXITCODE` reset pattern is not triggered; use plain steps with default failure propagation.

- [x] [P3-T1] Create `.github/workflows/_root-typescript-tests.yml`, mirroring the structure of `.github/workflows/_drm-copilot-extension-tests.yml`: `name: Root TypeScript Tests (reusable)`; `on:` declaring BOTH `workflow_call:` and `workflow_dispatch:`; one job (`root-typescript-tests`) with `strategy.matrix.os: [windows-latest, ubuntu-latest]` and `runs-on: ${{ matrix.os }}`; steps: `actions/checkout@v7`, `actions/setup-node@v7` with `node-version: "20"`, `cache: npm`, and `cache-dependency-path: package-lock.json` (the root lockfile), then `npm ci` at the repository root, then `npm test` at the repository root (which via `pretest` runs compile + lint, then the root jest suite). No inline `steps:` beyond these; no deliberately-failing nested commands. Acceptance: the file exists, declares both triggers, and its steps run `npm ci` and root `npm test` (AC5 structural evidence).
- [x] [P3-T2] Edit `.github/workflows/ci.yml`: add one additive job `root-typescript-tests:` with `uses: ./.github/workflows/_root-typescript-tests.yml`, consistent with the existing reusable-workflow jobs. Do not rename, reorder semantics of, or otherwise modify any existing job. Acceptance: `ci.yml` diff consists of exactly one added job block referencing the new reusable workflow; all existing job names are unchanged (AC5 wiring evidence).
- [x] [P3-T3] Update `.github/workflows/README.md`: add a row for `_root-typescript-tests.yml` (`workflow_call`, `workflow_dispatch`, `gh workflow run _root-typescript-tests.yml`) to the Per-Stage Dispatch table, and update the introductory "seven reusable per-stage workflows" count to eight; leave the historical "Scope of This Refactor" section unchanged (it documents the state at the time of that refactor); if a reviewer flags the stale count, that is an expected, documented decision. Acceptance: the dispatch table lists the new workflow and the intro count is accurate (AC5 documentation evidence).

### Phase 4 — Final QA Loop (Local, Seven-Stage)

Loop discipline: stages run in order — formatting (P4-T1), linting (P4-T2), type checking (P4-T3), architecture/contract/integration not-applicable determinations (P4-T4), tests with coverage (P4-T5). If ANY stage fails or modifies files, remediate and restart the loop from P4-T1, re-executing and re-capturing every stage artifact, until all stages pass in one consecutive clean pass (confirmed by P4-T8). `EXIT_CODE: SKIPPED` is not a passing outcome for any task in this phase.

- [x] [P4-T1] Final QC formatting: run `npm run format:check` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-format-check-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 in the final clean pass.
- [x] [P4-T2] Final QC linting: run `npm run lint` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-lint-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 in the final clean pass.
- [x] [P4-T3] Final QC type checking: run `npm run typecheck` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-typecheck-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0 in the final clean pass.
- [x] [P4-T4] Record the not-applicable determinations for toolchain stages 4, 6, and 7 with command evidence: verify no root TypeScript architecture-boundary gate is configured (e.g., list the repository root for `.dependency-cruiser.*` config files and record the absence), and record the spec Test Strategy rationales — stage 4 (no root architecture gate configured), stage 6 (no contract/schema surface at the root), stage 7 (no root integration suite after this change; extension integration-style jest tests run under stage 5 and in `_drm-copilot-extension-tests.yml`). Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-stages-4-6-7-na-root.<timestamp>.md` with `Timestamp:`, `Command:` (the verification command executed), `EXIT_CODE:`, `Output Summary:` containing the three per-stage rationales. Acceptance: artifact exists with command evidence for the architecture-gate absence and explicit rationale for each of stages 4, 6, 7 (AC10 n/a coverage).
- [x] [P4-T5] Final QC tests with coverage (path-independent invocation per Global Constraint 2): run `node run-jest.cjs --coverage --testMatch "**/tests/unit/**/*.test.ts" --testMatch "**/extensions/drm-copilot/test/**/*.test.ts"` at the repository root and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-test-coverage-root.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including NUMERIC post-change line coverage %, branch coverage %, suite count, test count, and the guard-suite name proof captured as the output of `node run-jest.cjs --testMatch "**/tests/unit/**/*.test.ts" --testPathPatterns "vscode-test-removal"` (`Test Suites: 1 passed, 1 total`), not a per-suite `PASS` line (see P2-T2 for the rationale). Acceptance: EXIT_CODE 0, line coverage >= 85%, branch coverage >= 75%, and the guard suite passes (AC4-local, AC7, AC10 stage-5 evidence).
- [x] [P4-T6] Coverage delta/threshold verification: compare the P0-T9 baseline against the P4-T5 post-change run and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/coverage-comparison-root.<timestamp>.md` reporting (a) baseline numeric line/branch coverage, (b) post-change numeric line/branch coverage, (c) new/changed-code coverage: record that no production source file changed (changed files are `package.json` scripts, workflow YAML, README, and one new test file — none coverage-measured as production code; the coverage denominator is unchanged), and (d) confirmation of no regression against thresholds (line >= 85%, branch >= 75%). Include `Timestamp:` and an `Output Summary:`. Acceptance: all values are numeric (no placeholders), post-change coverage is >= baseline or any delta is explained with the denominator analysis, and both thresholds pass (AC8 supporting evidence).
- [x] [P4-T7] Boundary inventory: run `git diff --name-only fb483b8468204e4385b5583c3b3ec4c0a987eede` (and `git status --porcelain` for uncommitted state) and verify the change set contains ONLY: `package.json`, `tests/unit/vscode-test-removal.test.ts`, `.github/workflows/_root-typescript-tests.yml`, `.github/workflows/ci.yml`, `.github/workflows/README.md`, and files under `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/` — and specifically NO modification to `run-jest.cjs`, `jest.config.cjs`, `.claude/rules/**`, `.agents/skills/**`, `extensions/drm-copilot/resources/claude-customizations/**`, `package-lock.json`, or any non-`scripts` key of `package.json`. Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/boundary-inventory.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing every changed path. Acceptance: the inventory matches the allowed set exactly (AC9 evidence).
- [x] [P4-T8] Confirm the clean pass: verify that the artifacts for P4-T1 through P4-T5 were produced in one consecutive pass with EXIT_CODE 0 and no file modified by any stage (if any stage failed or changed files, the loop was restarted from P4-T1 and all artifacts re-captured before this task). Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/final-qa-clean-pass.<timestamp>.md` with `Timestamp:` and `Output Summary:` listing the five stage artifacts of the clean pass and the number of loop iterations executed. Acceptance: a single consecutive clean pass is documented (AC10 evidence).

### Phase 5 — Commit, Push, and Authoritative CI Verification

- [x] [P5-T1] Commit the change set on branch `bug/vscode-test-integration-entrypoint` with a conventional, neutral message referencing #421 (e.g., `fix(scripts): repoint root npm test at jest and remove dead vscode-test entry points (#421)`), including `package.json`, `tests/unit/vscode-test-removal.test.ts`, the three `.github/workflows/` files, and the feature-folder evidence/doc updates. Acceptance: `git status --porcelain` shows no uncommitted in-scope changes and the commit exists on the branch.
- [x] [P5-T2] Push the branch: run `git push -u origin bug/vscode-test-integration-entrypoint` (or `git push` if upstream exists). Acceptance: EXIT_CODE 0 and the remote branch head equals the local HEAD SHA.
- [x] [P5-T3] Produce the authoritative green run against the branch head by dispatching the orchestrator workflow: run `gh workflow run ci.yml --ref bug/vscode-test-integration-entrypoint`, then poll with `gh run list --workflow=ci.yml --branch bug/vscode-test-integration-entrypoint` and `gh run watch <run-id>` until completion. `ci.yml` is dispatched rather than `_root-typescript-tests.yml` because GitHub's `workflow_dispatch` resolves only workflow files present on the default branch; a newly added callable workflow is not directly dispatchable until it lands on `main` (repo precedent: `docs/features/completed/npm-audit-gate-and-dependabot/policy-audit.2026-06-20T00-43.md`). Because a dispatched run executes the workflow files from the supplied `--ref`, this run exercises the branch's `ci.yml` and its local `uses: ./.github/workflows/_root-typescript-tests.yml` callee against the pushed head SHA. Acceptance: the `root-typescript-tests` jobs conclude `success` on both `windows-latest` and `ubuntu-latest` against the pushed head SHA. If the run fails, remediate (restarting the Phase 4 loop for any code change), re-push, and re-dispatch; do not proceed with a red run.
- [x] [P5-T4] Record the green-run evidence: write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/ci-green-run-root-typescript-tests.<timestamp>.md` with `Timestamp:`, `Command:` (the P5-T3 dispatch/watch commands), `EXIT_CODE:`, `Output Summary:` including the P5-T3 `ci.yml` run URL, the head SHA, per-OS job conclusions, and a log excerpt showing root `npm test` executing the jest suite with `tests/unit/vscode-test-removal.test.ts` listed as passing. If the CI log's per-suite `PASS` lines are unavailable, substitute the `Test Suites: N passed` summary plus the `root-typescript-tests` job conclusion as the guard-suite evidence. Acceptance: artifact contains the run URL, `success` conclusions, and the guard-suite log excerpt or its documented substitute (AC4-CI, AC6, and `modified-workflow-needs-green-run` evidence for the workflow diff).
- [x] [P5-T5] Verify the `ci.yml` wiring end-to-end from the SAME run produced in P5-T3 (do not dispatch a second time): confirm the run's job list includes the new `root-typescript-tests` job and that the run concludes `success` overall. Write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/qa-gates/ci-orchestrator-run.<timestamp>.md` with `Timestamp:`, `Command:` (the `gh run view <run-id>` / `gh run watch` commands), `EXIT_CODE:`, `Output Summary:` including the run URL and per-job conclusions, and an explicit note that direct `gh workflow run _root-typescript-tests.yml --ref <branch>` is unavailable until the workflow lands on the default branch and is therefore recorded as a post-merge verification follow-up (AC5's `workflow_dispatch` requirement is satisfied structurally by the declared trigger, verified by P3-T1). Acceptance: the full `ci.yml` run is green with the new job present (AC5 end-to-end and green-run evidence for the `ci.yml` diff). If a job unrelated to this change fails, record the failure, STOP, and report the blocked state; do not mark this task complete.

### Phase 6 — Acceptance-Criteria Check-Off and Closeout

- [x] [P6-T1] Verify each acceptance criterion AC1–AC11 in `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/spec.md` individually against the evidence artifacts (AC1/AC2: P1-T6; AC3: P2-T1 and P2-T2; AC4: P2-T2 and P5-T4; AC5: P3-T1/P3-T2/P3-T3 and P5-T5; AC6: P5-T3/P5-T4; AC7: P4-T5; AC8: P0-T4/P0-T5/P0-T10/P4-T6/P5-T4; AC9: P4-T7; AC10: P4-T1..P4-T5 and P4-T8; AC11: confirm the spec's Scope Decision section records the selected option (a), the decisive evidence including the `git log --all` correction and the `2f67b888` prior art, the rejected option (b) with reasons, the strongest counterargument, and the deferred devDependency/lockfile follow-up). Check off each satisfied criterion in `spec.md` (`- [ ]` → `- [x]`, text unchanged) per the `acceptance-criteria-tracking` protocol, and write `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/other/ac-verification.<timestamp>.md` with `Timestamp:` and a per-AC table mapping each AC to its verifying artifact path(s) and verdict. Acceptance: all eleven ACs are individually verified with cited evidence and checked off in `spec.md`; any unmet AC is left unchecked with the gap documented.
- [x] [P6-T2] Record the final AC Status Summary (source file, total 11, checked-off count, remaining items) in `docs/features/active/2026-07-25-root-vscode-test-entrypoint-unrunnable-421/evidence/other/ac-status-summary.<timestamp>.md` with `Timestamp:`, and commit and push the spec check-offs plus Phase 5/6 evidence artifacts on the same branch. Acceptance: the summary artifact exists showing 11/11 checked (or documents any remainder as a blocked state), and the branch head includes all evidence and the updated `spec.md`.
