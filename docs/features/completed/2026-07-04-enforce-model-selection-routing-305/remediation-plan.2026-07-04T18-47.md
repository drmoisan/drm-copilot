# Remediation Plan: enforce-model-selection-routing (Issue #305)

**Generated:** 2026-07-04T18-47
**Feature Folder:** `docs/features/active/2026-07-04-enforce-model-selection-routing-305`
**Primary Input:** `docs/features/active/2026-07-04-enforce-model-selection-routing-305/remediation-inputs.2026-07-04T14-34.md`
**Scope:** Bounded remediation of two BLOCKING findings only. No scope expansion.
**Canonical evidence root:** `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/<kind>/`

## Scope Statement

This plan remediates exactly two BLOCKING findings and nothing more:

- BLOCKING-1: `extensions/drm-copilot/src/repo-automation-service.ts` is 502 lines, exceeding the 500-line hard limit in `.claude/rules/general-code-change.md`.
- BLOCKING-2: No TypeScript coverage artifact exists; the extension has no `test:coverage` script and no Jest coverage configuration.

Non-blocking observations from the inputs (500-line `validate_orchestrator_state.py`, pre-existing tracked `coverage.xml`) require no action and are out of scope. Do not reimplement `compute_complexity_floor` or `resolve_delegation_model`. Keep all changes minimal and confined to the two blockers.

All commands that target the extension run from `extensions/drm-copilot`. Repo root is `C:\Users\DanMoisan\repos\drm-copilot-wt-2026-07-04-13-37`.

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read the following policy files in required order and record them in a Phase 0 policy-read evidence artifact at `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/remediation-baseline/phase0-instructions-read.md` (fields: `Timestamp:`, `Policy Order:`, explicit list of files read): `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`, `.claude/rules/architecture-boundaries.md`. Binary outcome: the artifact exists with all three required fields populated.
- [x] [P0-T2] Capture the current line count of the target file. Command: `wc -l extensions/drm-copilot/src/repo-automation-service.ts`. Write the result to `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/remediation-baseline/linecount-baseline.md` (fields: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Binary outcome: artifact records the baseline count (expected 502).
- [x] [P0-T3] Capture the extension TypeScript QA baseline. Commands (run in `extensions/drm-copilot`): `npm run format`, `npm run lint`, `npm run typecheck`, `npm run test`. Write one artifact `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/remediation-baseline/typescript-baseline.md` (fields: `Timestamp:`, `Command:` for each, `EXIT_CODE:` for each, `Output Summary:` with pass/fail and Jest test counts). Binary outcome: artifact records the four exit codes and the current pass state (coverage numbers are expected ABSENT at baseline and this is recorded).
- [x] [P0-T4] Capture the cross-language regression baseline so no-regression can be verified after the change. Commands: run the repo Python test suite and the PowerShell PoshQC/Pester quick suite as currently invoked by the repo. Write `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/remediation-baseline/cross-language-baseline.md` (fields: `Timestamp:`, `Command:` for each, `EXIT_CODE:` for each, `Output Summary:` with pass counts and coverage headline where reported). Binary outcome: artifact records Python and PowerShell baseline exit codes and summaries.

---

### Phase 1 — BLOCKING-1: Reduce repo-automation-service.ts below 500 lines

- [x] [P1-T1] Create the sibling helper module `extensions/drm-copilot/src/lib/validate/build-validate-orchestration-service-call-input.ts` that exports a single pure function assembling the `ValidateOrchestrationServiceCallInput` object currently constructed inline at `extensions/drm-copilot/src/repo-automation-service.ts` lines ~454–466 (the request-shaping block that maps `fileSystem`, `workspaceRoot`, `artifactType`, `artifactPath`, and the optional `requireComplete`/`requireModelRouting` fields). The function must reproduce the existing optional-field spread semantics exactly (omit a key when its source value is `undefined`). Binary outcome: the new file exists, exports the helper, imports `ValidateOrchestrationServiceCallInput` from `./validate-orchestration-service-call`, and is ≤ 500 lines (`wc -l` on the new file).
- [x] [P1-T2] Update `extensions/drm-copilot/src/repo-automation-service.ts` to import the new helper and replace the inline argument-shaping block inside `validateOrchestrationArtifacts` with a call to the helper, preserving the `return validateOrchestrationServiceCall(...)` behavior and the method signature unchanged. Add the import alongside the existing `validateOrchestrationServiceCall` import (line ~34). Binary outcome: the inline object literal is removed, the helper is invoked, and no other method is modified.
- [x] [P1-T3] Confirm the target file is at or under the hard limit. Command: `wc -l extensions/drm-copilot/src/repo-automation-service.ts`. Binary outcome: line count ≤ 500. Record in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/linecount-postchange.md` (fields: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` with the numeric count for both the target file and the new sibling file, both ≤ 500).
- [x] [P1-T4] Verify behavior is unchanged after extraction. Commands (in `extensions/drm-copilot`): `npm run typecheck` then `npm run test`. Binary outcome: both exit 0 and the Jest pass count is not lower than the P0-T3 baseline. Record in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/blocking1-verification.md` (fields: `Timestamp:`, `Command:` each, `EXIT_CODE:` each, `Output Summary:`).
- [x] [P1-T5] Confirm the extraction touched no `.claude/**` file, so no bundle re-mirror is required. Command: `git status --porcelain` filtered for `.claude/`. Binary outcome: no `.claude/**` path is modified; record the result and the `EVIDENCE that bundle parity is unaffected` in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/bundle-parity-remediation.md` (fields: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). If any `.claude/**` file was in fact changed, re-mirror the bundle copy and record byte-identical parity before proceeding.

---

### Phase 2 — BLOCKING-2: Wire TypeScript coverage and produce the artifact

- [x] [P2-T1] Add a `test:coverage` script to `extensions/drm-copilot/package.json` `scripts` block: `"test:coverage": "node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary"`. Binary outcome: the script exists and `npm run` lists it. Do not alter existing `test`/`test:unit` scripts.
- [x] [P2-T2] Add Jest coverage configuration to `extensions/drm-copilot/jest.config.cjs`: set `collectCoverage: false` (enabled via CLI flag), `collectCoverageFrom: ["src/**/*.ts", "!src/**/*.d.ts"]`, `coverageReporters: ["lcov", "text-summary"]`, `coverageDirectory: "<rootDir>/coverage"`, and a `coverageThreshold` requiring `lines: 85` and `branches: 75`. The `coverageProvider: "v8"` entry is retained. Do not add any `coveragePathIgnorePatterns`/`collectCoverageFrom` exclusion that matches a production `src/**` runtime path — per `.claude/rules/general-unit-test.md`, permitted excludes are build output, tests, config, and `node_modules` only. Binary outcome: `jest.config.cjs` contains the coverage config and remains valid JavaScript (loads without error under `node -e "require('./jest.config.cjs')"`).
- [x] [P2-T3] Run the coverage suite. Command (in `extensions/drm-copilot`): `npm run test:coverage`. Binary outcome: the command completes and produces `extensions/drm-copilot/coverage/lcov.info`. Record the run in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/typescript-coverage.md` (fields: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` with the text-summary line/branch totals).
- [x] [P2-T4] Extract and record the numeric line and branch coverage for each changed TypeScript file from `extensions/drm-copilot/coverage/lcov.info`: `src/lib/validate/orchestrator-state-core.ts`, `src/lib/validate/orchestration-artifacts.ts`, `src/lib/validate/validate-orchestration-service-call.ts`, `src/lib/validate/build-validate-orchestration-service-call-input.ts` (the P1-T1 sibling), `src/repo-automation-service.ts`, and each changed `src/mcp-*.ts`. Append the per-file numbers to `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/typescript-coverage.md` under an `Output Summary:` per-file table. Binary outcome: each listed changed file has a recorded line% and branch% value (type-only files with no executable behavior are noted as such and excluded from the threshold check per policy).
- [x] [P2-T5] Verify each changed TypeScript file meets ≥85% line and ≥75% branch and shows no regression on changed lines. Binary outcome: every non-type-only changed file in P2-T4 is at or above both thresholds. If any changed file is below threshold, add Jest tests under the extension `test/` tree that mirrors the source path (for example `test/lib/validate/build-validate-orchestration-service-call-input.test.ts` for the P1-T1 sibling), then re-run `npm run test:coverage` and update `typescript-coverage.md`. Record the final pass/fail decision line `COVERAGE_GATE: PASS` (or the remediation actions taken) in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/typescript-coverage.md`.

---

### Phase 3 — Final QA Loop and Closure Gate

- [x] [P3-T1] Run the full extension TypeScript toolchain loop in order and restart from step 1 if any step changes files or fails. Commands (in `extensions/drm-copilot`): `npm run format`, then `npm run lint`, then `npm run typecheck`, then `npm run test`. Binary outcome: a single clean pass with all four exit codes 0. Record one artifact `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/typescript-final-qa.md` (fields: `Timestamp:`, `Command:` each, `EXIT_CODE:` each, `Output Summary:` with test counts).
- [x] [P3-T2] Run the coverage suite once more as the final coverage gate. Command (in `extensions/drm-copilot`): `npm run test:coverage`. Binary outcome: exit 0, `coverage/lcov.info` regenerated, and text-summary totals recorded. Append the final numeric line/branch totals to `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/typescript-final-qa.md` under `Output Summary:`.
- [x] [P3-T3] Confirm no cross-language regression. Commands: rerun the repo Python test suite and the PowerShell PoshQC/Pester quick suite from P0-T4. Binary outcome: both green with pass counts and coverage headline not lower than the P0-T4 baseline. Record in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/regression-testing/cross-language-postchange.md` (fields: `Timestamp:`, `Command:` each, `EXIT_CODE:` each, `Output Summary:`).
- [x] [P3-T4] Confirm bundle parity for any touched `.claude/**` file remains byte-identical (expected: none touched). Command: `git status --porcelain` filtered for `.claude/`. Binary outcome: no `.claude/**` change, or byte-identical parity confirmed for any change. Record in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/bundle-parity-final.md` (fields: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`).
- [x] [P3-T5] Verify all five closure-gate conditions hold and record the consolidated result in `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/qa-gates/closure-gate.md` (fields: `Timestamp:`, `Output Summary:`): (1) `wc -l extensions/drm-copilot/src/repo-automation-service.ts` ≤ 500 and the extracted sibling ≤ 500; (2) TS coverage artifact `extensions/drm-copilot/coverage/lcov.info` exists showing ≥85% line / ≥75% branch on changed TS with no regression on changed lines; (3) `npm run format && npm run lint && npm run typecheck && npm run test` plus `npm run test:coverage` all pass; (4) bundle parity byte-identical for any touched `.claude/**` (expected none); (5) Python and PowerShell toolchains remain green (no regression). Binary outcome: all five conditions recorded as satisfied, or the specific failing condition identified for another remediation iteration.

---

## Evidence Location Compliance

All evidence artifacts in this plan resolve to `docs/features/active/2026-07-04-enforce-model-selection-routing-305/evidence/<kind>/` (`remediation-baseline/`, `qa-gates/`, `regression-testing/`). No `artifacts/` evidence path is used. Any upstream instruction supplying a non-canonical evidence path is rejected in favor of the canonical scheme per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`.

DIRECTIVE: PREFLIGHT VALIDATION ONLY
