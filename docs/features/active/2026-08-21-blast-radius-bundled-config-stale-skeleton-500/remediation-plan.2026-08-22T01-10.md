# Remediation Plan: blast-radius bundled truth-table correction (#500)

**Timestamp:** 2026-08-22T01-10
**Authored by:** atomic-planner
**Feature Folder:** `docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500`
**Base Branch:** `main` @ `fb30a9a58b8422e610a09b07361421e97367807a`
**Head:** `bug/blast-radius-bundled-config-stale-skeleton-500` @ `59425465`
**Work Mode:** `full-bug`; acceptance-criteria source is `spec.md` only
**Remediation Cycle:** 1
**Source:** `remediation-inputs.2026-08-22T00-52.md`, items R1 through R6, all six in scope for this
cycle per the delegating instruction ("Include them anyway").

---

## Scope Statement

This cycle implements all six enumerated items:

- R1, R2 (Blocking) — correct the AC9 and AC10 criterion text in `spec.md`.
- R3 (Minor) — correct the Class 2 mitigation sentence in `spec.md`.
- R4 (delivered, not deferred) — add the directional separator-free invariant to the Python and
  PowerShell parity gates, with a falsifiability demonstration, and record it in the governing rule.
- R5 (Minor) — correct the `post-fix-conflict-graph` evidence artifact's recorded command.
- R6 (Minor) — replace the pipeline `ConvertTo-Json` in the PowerShell Class 1 mirror.

**Languages touched this cycle:** Python (one test module edited) and PowerShell (one test file
edited). Markdown is touched (`spec.md`, one evidence artifact, one rule file and its bundled
mirror) but Markdown carries no toolchain or coverage obligation.

**Languages explicitly NOT re-baselined, and why:** TypeScript. No file under
`extensions/drm-copilot/src/**` or `extensions/drm-copilot/test/**` is touched by any of R1
through R6; the original branch's TypeScript delivery (`PAYLOAD_MODULES`, the two Jest cases) is
unaffected by this remediation cycle, so a TypeScript baseline recapture would measure a state this
cycle does not change. C# and shell are likewise untouched, matching the original branch.

**Do Not Do (binding, from `remediation-inputs.2026-08-22T00-52.md`):**

- Do not amend the substance of any acceptance criterion; change only file/command identifiers in
  AC9 and the PD-1 reason, and the file identifier in AC10.
- Do not add, remove, or renumber acceptance criteria (count stays at 17).
- Do not move the gate into `tests/scripts/dev_tools/test_blast_radius_config.py` (499 of 500
  lines; do not edit this file).
- Do not change `PAYLOAD_MODULES`, either truth table's data, or the rule text beyond the single
  permitted R4 amendment.
- Do not widen the bundled `shared_surfaces` set or relax Class 2 to a subset relation.
- Do not weaken any assertion, delete any test, or add any suppression.
- Do not add a coverage `exclude`/`omit` entry.
- Do not author, import, or read a JSON Schema.
- Do not open a new remediation cycle for findings already listed in the inputs.
- Do not write evidence anywhere but `<FEATURE>/evidence/<kind>/`.
- Do not modify any file under `.claude/rules/` or `.github/instructions/` except
  `.claude/rules/parallel-orchestration.md` and its byte-identical bundled mirror (R4 only).

---

### Phase 0 — Policy Reads and Toolchain Baselines (Python, PowerShell)

- [x] [P0-T1] Read `CLAUDE.md` in full.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md` in full.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` in full.
- [x] [P0-T4] Read `.claude/rules/python.md` in full.
- [x] [P0-T5] Read `.claude/rules/python-suppressions.md` in full.
- [x] [P0-T6] Read `.claude/rules/powershell.md` in full.
- [x] [P0-T7] Read `.claude/rules/quality-tiers.md` in full.
- [x] [P0-T8] Read `.claude/rules/parallel-orchestration.md` in full.
- [x] [P0-T9] Read `.claude/rules/plan-acceptance-gates.md` in full.
- [x] [P0-T10] Read `.claude/rules/tonality.md` in full.
- [x] [P0-T11] Write the Phase 0 policy-read evidence artifact to
      `docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/evidence/remediation-baseline/phase0-instructions-read.<timestamp>.md`
      (using the `yyyy-MM-ddTHH-mm` timestamp form), recording `Timestamp:`, `Policy Order:`, and
      the ten files read in P0-T1 through P0-T10, in that order.
      Acceptance: the artifact file exists and lists all ten files in order.
- [x] [P0-T12] Capture the Python formatting baseline: run `poetry run black --check .` from the
      worktree root and record the artifact at
      `evidence/remediation-baseline/python-black.<timestamp>.md` with `Timestamp:`, `Command:`,
      `EXIT_CODE:`, and `Output Summary:` (files unchanged count).
      Acceptance: `EXIT_CODE: 0` is recorded.
- [x] [P0-T13] Capture the Python lint baseline: run `poetry run ruff check .` and record the
      artifact at `evidence/remediation-baseline/python-ruff.<timestamp>.md` with the same four
      fields.
      Acceptance: `EXIT_CODE: 0` is recorded.
- [x] [P0-T14] Capture the Python type-check baseline: run `poetry run pyright` and record the
      artifact at `evidence/remediation-baseline/python-pyright.<timestamp>.md` with the same four
      fields, `Output Summary:` naming the error and warning counts.
      Acceptance: `EXIT_CODE: 0` and `0 errors, 0 warnings` are recorded.
- [x] [P0-T15] Capture the Python test-and-coverage baseline: run
      `poetry run pytest --cov=scripts.dev_tools --cov-branch --cov-report=term-missing --cov-report=json:artifacts/python/coverage.json`
      and record the artifact at
      `evidence/remediation-baseline/python-pytest-coverage.<timestamp>.md` with `Timestamp:`,
      `Command:`, `EXIT_CODE:`, and `Output Summary:` naming the pass/fail/skip counts, the
      statement percentage computed as `(Stmts - Miss) / Stmts` from the `TOTAL` row, and the
      branch percentage read from `totals.percent_branches_covered` in
      `artifacts/python/coverage.json`.
      Acceptance: `EXIT_CODE: 0` and both numeric coverage figures are recorded, each >= the
      thresholds in `.claude/rules/quality-tiers.md` (85% statements, 75% branches).
- [x] [P0-T16] Capture the PowerShell formatting baseline: run
      `mcp__drm-copilot__run_poshqc_format` and record the artifact at
      `evidence/remediation-baseline/powershell-poshqc-format.<timestamp>.md` with `Timestamp:`,
      `Command:`, `EXIT_CODE:`, and `Output Summary:`.
      Acceptance: `EXIT_CODE: 0` and zero files rewritten are recorded.
- [x] [P0-T17] Capture the PowerShell lint baseline: run `mcp__drm-copilot__run_poshqc_analyze` and
      record the artifact at `evidence/remediation-baseline/powershell-poshqc-analyze.<timestamp>.md`
      with the same four fields.
      Acceptance: `EXIT_CODE: 0` and zero findings are recorded.
- [x] [P0-T18] Capture the PowerShell test-and-coverage baseline: run
      `mcp__drm-copilot__run_poshqc_test` and record the artifact at
      `evidence/remediation-baseline/powershell-poshqc-test.<timestamp>.md` with `Timestamp:`,
      `Command:`, `EXIT_CODE:`, and `Output Summary:` naming the pass/fail/skip counts and the line
      coverage percentage computed from the JaCoCo root `LINE` counter as
      `covered / (covered + missed)`.
      Acceptance: `EXIT_CODE: 0` and a recorded line coverage >= 85%.
- [x] [P0-T19] Write a scope note at `evidence/remediation-baseline/typescript-out-of-scope.<timestamp>.md`
      stating that no file under `extensions/drm-copilot/src/**` or `extensions/drm-copilot/test/**`
      is touched by R1 through R6 in this cycle, so no TypeScript baseline is recaptured.
      Acceptance: the artifact file exists and names the reason given in the Scope Statement above.

---

### Phase 1 — R1, R2: Correct the AC9 and AC10 criterion text in `spec.md` (Blocking)

- [x] [P1-T1] In
      `docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`,
      `## Acceptance Criteria`, rewrite the ninth checkbox item (AC9) so it names
      `tests/scripts/dev_tools/test_blast_radius_config_parity.py` as the file carrying the
      three-class key-partition gate, states that it imports the shared helpers from
      `tests/scripts/dev_tools/test_blast_radius_config.py` and holds its declared constants and
      accessors in `tests/scripts/dev_tools/blast_radius_parity_test_support.py`, records the
      literal `PD-1` and the 499-of-500-line ceiling in `.claude/rules/general-code-change.md` as
      the reason for the sibling module, and cites
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py` as the
      verification command. The three-class content (Class 1, Class 2, Class 3) is preserved
      verbatim. Leave the checkbox as `- [ ]` for now; it is checked in Phase 6 after
      reverification.
      Acceptance: `git grep -c -F "blast_radius_parity_test_support.py" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports a count of at least 1.
- [x] [P1-T2] In the same file, rewrite the tenth checkbox item (AC10) so its line begins
      `- [ ] \`tests/scripts/dev_tools/test_blast_radius_config_parity.py\` asserts` (naming the
      file explicitly, matching AC9's leading code span, rather than relying on the anaphor "the
      same file") and cites
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py` explicitly
      (rather than "the same pytest invocation"). The umbrella-denylist, separator-free, and
      non-vacuity content is preserved verbatim. Leave the checkbox as `- [ ]` for now.
      Acceptance: `git grep -c -F "test_blast_radius_config_parity.py" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports a count of at least 2 (one occurrence from P1-T1's AC9 rewrite, one from this
      task's AC10 rewrite).
- [x] [P1-T3] Confirm the corrected command collects the gate as stated: run
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py` from the
      worktree root.
      Acceptance: `EXIT_CODE: 0` and `14 passed` (the count before Phase 2 adds the fifteenth
      case).

---

### Phase 2 — R4: Add the directional separator-free invariant (Python + PowerShell)

- [x] [P2-T1] Add a new test function
      `test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle` to
      `tests/scripts/dev_tools/test_blast_radius_config_parity.py`. The function computes the
      separator-free entries of `SELF_HOSTED_CONFIG` and `BUNDLED_CONFIG` via the existing
      `shared_surfaces` accessor (already imported in this module) filtered on `"/" not in entry`,
      and asserts the self-hosted separator-free set is a subset of the bundled separator-free set,
      with a failure message naming the missing entries and both `SELF_HOSTED_CONFIG_LABEL` and
      `BUNDLED_CONFIG_LABEL` (both already imported). No new import and no new accessor in
      `blast_radius_parity_test_support.py` is required.
      Acceptance:
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py::test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle`
      exits 0 with `1 passed`.
- [x] [P2-T2] Confirm the file size after P2-T1: run
      `wc -l tests/scripts/dev_tools/test_blast_radius_config_parity.py` (or the equivalent
      PowerShell `(Get-Content <path>).Count`).
      Acceptance: the reported line count is less than 500.
- [x] [P2-T3] Add a mirrored Pester case to
      `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`, inside the existing
      `Cross-copy key partition` `Context`, titled
      `requires every separator-free self-hosted shared surface to reach the bundled copy`. The
      case filters `$script:CommittedConfig['shared_surfaces']` and
      `$script:BundledConfig['shared_surfaces']` on entries with no `/`, using
      `Where-Object { -not $_.Contains('/') }`, then asserts (via `-cnotcontains` accumulation) that
      no self-hosted separator-free entry is absent from the bundled separator-free set.
      Acceptance: a targeted `Invoke-Pester` run against
      `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1` reports the new
      `It` block passing with 0 failed.
- [x] [P2-T4] Confirm the file size after P2-T3: `(Get-Content tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1).Count`.
      Acceptance: the reported line count is less than 500.
- [x] [P2-T5] [expect-fail] Demonstrate falsifiability in both languages by reverting the bundled
      copy to its pre-fix, merge-base state and observing both the new Python case and the new
      Pester case fail in that same reverted state. Run:
      `git show fb30a9a58b8422e610a09b07361421e97367807a:extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json > extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json`,
      then
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py::test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle`.
      Record the Python fail-before evidence artifact at
      `evidence/regression-testing/python-directional-invariant-fail-before.<timestamp>.md` with
      `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and `Output Summary:` quoting
      the assertion message naming the missing entries.
      Acceptance (Python): `EXIT_CODE: 1` and `1 failed` are recorded, with the failure naming at
      least one of `poetry.lock`, `package-lock.json`, or `quality-tiers.yml` as missing from the
      reverted bundle.

      In the same reverted state, run the mirrored Pester case in isolation using a name filter,
      because an unfiltered run of the file also fails the umbrella, payload-module, Class 1, and
      bundled-separator-free cases once the bundled copy is reverted, which would bury the new
      case's signal. Build and run a scoped configuration:
      ```powershell
      $pesterConfig = New-PesterConfiguration
      $pesterConfig.Run.Path = 'tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1'
      $pesterConfig.Run.PassThru = $true
      $pesterConfig.Filter.FullName = '*requires every separator-free self-hosted shared surface to reach the bundled copy*'
      $result = Invoke-Pester -Configuration $pesterConfig
      ```
      Record the PowerShell fail-before evidence artifact at
      `evidence/regression-testing/powershell-directional-invariant-fail-before.<timestamp>.md`
      with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and `Output Summary:`
      quoting `$result.FailedCount` and the failure message naming the missing entries.
      Acceptance (PowerShell): the filtered run reports `Passed=0 Failed=1`, with the failure
      naming at least one of `poetry.lock`, `package-lock.json`, or `quality-tiers.yml` as missing
      from the reverted bundle.
- [x] [P2-T6] Restore the bundled copy and confirm both new cases pass again: run
      `git checkout -- extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json`,
      then `git status --short -- extensions/drm-copilot/resources/claude-customizations/config/blast-radius.json`
      (expect no output), then rerun
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py::test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle`.
      Record the Python pass-after evidence artifact at
      `evidence/qa-gates/python-directional-invariant-pass-after.<timestamp>.md` with the same four
      required fields.
      Acceptance (restore + Python): the `git status --short` command produces no output for that
      path, and `EXIT_CODE: 0` with `1 passed` is recorded for the rerun.

      Then rerun the same filtered Pester configuration used in P2-T5 (identical `Run.Path` and
      `Filter.FullName`) against the restored tree. Record the PowerShell pass-after evidence
      artifact at `evidence/qa-gates/powershell-directional-invariant-pass-after.<timestamp>.md`
      with the same four required fields, `Output Summary:` quoting `$result.PassedCount` and
      `$result.FailedCount`.
      Acceptance (PowerShell): the filtered rerun reports `Passed=1 Failed=0`.
- [x] [P2-T7] Amend `.claude/rules/parallel-orchestration.md`, in the
      `### The published truth table is not a copy of this one (issue #500)` section, appending a
      paragraph titled "A directional invariant closes the residual Class 2 gap (issue #500
      remediation)" that records: portable-set equality and the `bundled <= self_hosted` subset
      relation together do not observe the self-hosted copy gaining a portable separator-free
      surface that never reaches the bundle; and that
      `test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle` in
      `tests/scripts/dev_tools/test_blast_radius_config_parity.py`, mirrored in
      `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`, closes that gap
      structurally by asserting the reverse containment for separator-free entries.
      Acceptance: `git grep -n -F "closes the residual Class 2 gap" -- .claude/rules/parallel-orchestration.md`
      reports at least one match.
- [x] [P2-T8] Mirror the P2-T7 amendment byte-identically into
      `extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md`
      in the same commit.
      Acceptance: `diff .claude/rules/parallel-orchestration.md extensions/drm-copilot/resources/claude-customizations/.claude/rules/parallel-orchestration.md`
      produces no output and exits 0.

---

### Phase 3 — R6: Replace the pipeline `ConvertTo-Json` in the Class 1 Pester mirror

- [x] [P3-T1] In `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`, in the
      `It 'declares equal values for the runtime-describing keys in both copies'` case, replace
      `$script:CommittedConfig[$key] | ConvertTo-Json -Depth 10 -Compress` with
      `ConvertTo-Json -InputObject $script:CommittedConfig[$key] -Depth 10 -Compress`, and replace
      `$script:BundledConfig[$key] | ConvertTo-Json -Depth 10 -Compress` with
      `ConvertTo-Json -InputObject $script:BundledConfig[$key] -Depth 10 -Compress`, on both
      assignment lines inside the `foreach ($key in $byteEqualKey)` loop.
      Acceptance: `git grep -n -F "ConvertTo-Json -InputObject" -- tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`
      reports exactly 2 matches.
- [x] [P3-T2] Confirm the corrected form is discriminating: a targeted `Invoke-Pester` run against
      `tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1` reports the
      `Cross-copy key partition` `Context` passing with 0 failed.
      Acceptance: 0 failed reported for that `Context`.

---

### Phase 4 — R3: Correct the Class 2 mitigation sentence in `spec.md`

- [x] [P4-T1] In `spec.md`, `## Risks & Mitigations`, replace the bullet reading "Class 1
      byte-equality and Class 2 portable-set equality both fail loudly when a future self-hosted
      change does not reach the bundle." with a bullet stating: Class 1 byte-equality fails loudly
      when a `mandate_reads`, `version`, or `over_breadth_fraction` change reaches only one copy;
      Class 2 portable-set equality fails on any unilateral change to the bundled set or to the
      declared constant, and does not by itself observe the self-hosted set growing away from it;
      the catastrophic form of the fail-open defect, a published table with no separator-free
      shared surface, is independently blocked by the non-empty precondition in
      `test_every_separator_free_bundled_shared_surface_is_wildcard_free`; and the residual gap is
      closed structurally by `test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle`
      (added in Phase 2). Keep the phrase "Class 2 portable-set equality" contiguous on a single
      line so a line-oriented search still matches it.
      Acceptance, all three run from the worktree root against
      `docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`:
      (a) `git grep -c -F "byte-equality and Class 2 portable-set equality both fail loudly" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports `0` after the edit (this substring lies entirely on the pre-edit line 581 and reports
      `1` before the edit, so the condition is false now and true only after the bullet is rewritten);
      (b) `git grep -c -F "test_every_separator_free_self_hosted_shared_surface_reaches_the_bundle" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports at least `1` after the edit (this literal is absent from `spec.md` before the edit);
      (c) `git grep -c -F "test_every_separator_free_bundled_shared_surface_is_wildcard_free" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports at least `1` after the edit (also absent before the edit). All three conjuncts are
      false against the unedited file and can only become true by performing the rewrite. The
      repo-relative pathspec is required in every command: a bare `-- spec.md` resolves relative to
      the current directory and matches no tracked file when run from the worktree root, since
      `spec.md` does not exist at the repository root.

---

### Phase 5 — R5: Correct the `spec_text` omission in the conflict-graph evidence artifact

- [x] [P5-T1] In
      `evidence/other/post-fix-conflict-graph.2026-08-22T00-20.md`, the `Command:` block at line 7
      (the file's only `Command:` block), amend the command description to name all four positional arguments passed
      to `derive_blast_radius`: `plan_text` (each folder's `plan*.md` content), `spec_text` (each
      folder's `spec.md` content, supplied as the second positional argument), `feature_folder`
      (the bare folder name), and `config` (the parsed truth table), plus the fixed `computed_at`
      constant used as the keyword argument.
      Acceptance: `git grep -n -F "spec_text" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/evidence/other/post-fix-conflict-graph.2026-08-22T00-20.md`
      reports at least one match inside the command description.

---

### Phase 6 — Final QA Loop (Python + PowerShell) and Acceptance-Criteria Reconciliation

- [x] [P6-T1] Run `poetry run black --check .` from the worktree root.
      Acceptance: `EXIT_CODE: 0`.
- [x] [P6-T2] Run `poetry run ruff check .` from the worktree root.
      Acceptance: `EXIT_CODE: 0`, zero new `noqa` present.
- [x] [P6-T3] Run `poetry run pyright` from the worktree root.
      Acceptance: `EXIT_CODE: 0`, `0 errors, 0 warnings`.
- [x] [P6-T4] Run
      `poetry run pytest --cov=scripts.dev_tools --cov-branch --cov-report=term-missing --cov-report=json:artifacts/python/coverage.json`
      and record the final-QC artifact at
      `evidence/qa-gates/final-python-pytest-coverage.<timestamp>.md` with `Timestamp:`,
      `Command:`, `EXIT_CODE:`, and `Output Summary:` naming the pass/fail/skip counts, the
      statement percentage from the `TOTAL` row, and the branch percentage from
      `totals.percent_branches_covered` in `artifacts/python/coverage.json`.
      Acceptance: `EXIT_CODE: 0` for the full run, with both coverage figures at or above the
      P0-T15 baseline figures (no regression). The per-file count for the gate module is checked
      separately in P6-T12.
- [x] [P6-T5] Run `mcp__drm-copilot__run_poshqc_format`.
      Acceptance: `EXIT_CODE: 0`, zero files rewritten (a clean tree after the run).
- [x] [P6-T6] Run `mcp__drm-copilot__run_poshqc_analyze`.
      Acceptance: `EXIT_CODE: 0`, zero findings.
- [x] [P6-T7] Run `mcp__drm-copilot__run_poshqc_test` and record the final-QC artifact at
      `evidence/qa-gates/final-powershell-poshqc-test.<timestamp>.md` with the same four required
      fields, `Output Summary:` naming the pass/fail/skip counts and the line coverage percentage
      computed from the JaCoCo root `LINE` counter.
      Acceptance: `EXIT_CODE: 0` and the recorded line coverage at or above the P0-T18 baseline
      figure (no regression).
- [x] [P6-T8] Write the coverage delta-verification artifact at
      `evidence/qa-gates/coverage-delta-verification.<timestamp>.md` reporting, for Python and for
      PowerShell: baseline coverage (from Phase 0), post-change coverage (from P6-T4 / P6-T7), and
      new/changed-code coverage. Because no production file is touched by R1 through R6, the
      new/changed-code coverage figure for both languages is 100% of changed production lines, of
      which there are zero.
      Acceptance: the artifact records both baseline and post-change figures for both languages
      with a delta of 0.00 or better on every figure.
- [x] [P6-T9] Confirm no coverage `exclude`/`omit` entry was added: run
      `git diff fb30a9a58b8422e610a09b07361421e97367807a...HEAD -- pyproject.toml`.
      Acceptance: the diff produces no output for `pyproject.toml`.
- [x] [P6-T10] Confirm the file-size ceiling holds for every file touched this cycle: run
      `wc -l tests/scripts/dev_tools/test_blast_radius_config_parity.py tests/scripts/claude-lib/blast-radius/BlastRadius.TruthTable.Tests.ps1`.
      Acceptance: both reported counts are less than 500.
- [x] [P6-T11] Confirm `tests/scripts/dev_tools/test_blast_radius_config.py` is untouched, per the
      Do Not Do constraint: run
      `git diff fb30a9a58b8422e610a09b07361421e97367807a...HEAD -- tests/scripts/dev_tools/test_blast_radius_config.py`.
      Acceptance: the diff produces no output.
- [x] [P6-T12] Re-verify the AC9/AC10 gate command collects the full set including the Phase 2
      addition: run
      `poetry run pytest tests/scripts/dev_tools/test_blast_radius_config_parity.py`.
      Acceptance: `EXIT_CODE: 0` and `15 passed`.
- [x] [P6-T13] In `spec.md`, `## Acceptance Criteria`, change the AC9 checkbox from `- [ ]` to
      `- [x]` (text unchanged from Phase 1). This task depends on P6-T12 passing.
      Acceptance:
      `git grep -n -P "^- \[x\] \`tests/scripts/dev_tools/test_blast_radius_config_parity\.py\`" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports exactly 1 match (AC9 only, before P6-T14 runs).
- [x] [P6-T14] In the same file, change the AC10 checkbox from `- [ ]` to `- [x]` (text unchanged
      from Phase 1, and beginning with the same code-span-led marker as AC9, per Phase 1). This
      task depends on P6-T12 passing.
      Acceptance:
      `git grep -c -P "^- \[x\] \`tests/scripts/dev_tools/test_blast_radius_config_parity\.py\`" -- docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md`
      reports a count of exactly 2 (AC9 and AC10 both checked).
- [x] [P6-T15] Confirm the acceptance-criteria count is unchanged at 17: run
      `sed -n '/^## Acceptance Criteria$/,/^## Out of Scope$/p' docs/features/active/2026-08-21-blast-radius-bundled-config-stale-skeleton-500/spec.md | grep -c -E "^- \[[ x]\]"`,
      which scopes the count to the `## Acceptance Criteria` section only (the file also carries
      unrelated checkbox lines in `## Repro & Evidence` and `## Test Strategy` that must not be
      counted).
      Acceptance: the reported count is 17.
- [x] [P6-T16] Write the final single-pass confirmation artifact at
      `evidence/qa-gates/remediation-toolchain-single-pass.<timestamp>.md` recording that P6-T1
      through P6-T7 executed in one uninterrupted sequence with no restart and no file rewritten by
      any stage.
      Acceptance: the artifact records all seven exit codes as 0 with no intervening restart.
- [x] [P6-T17] Write the updated AC status summary at
      `evidence/issue-updates/ac-status-summary.<timestamp>.md` stating 17 of 17 acceptance
      criteria checked, with a one-line note that AC9 and AC10 were unchecked by the prior review
      cycle and re-checked in this cycle after the corrected verification command passed.
      Acceptance: the artifact states `17 of 17 checked`.

---

## Exit Condition for This Cycle

This cycle exits when all of the following hold, matching `remediation-inputs.2026-08-22T00-52.md`:

1. `spec.md` AC9 names `tests/scripts/dev_tools/test_blast_radius_config_parity.py` and cites a
   pytest invocation that collects the gate's cases, and its checkbox is `[x]`.
2. `spec.md` AC10 resolves to the same file and command, and its checkbox is `[x]`.
3. All seventeen acceptance criteria are checked and each is independently verifiable from its own
   stated command.
4. The full toolchain passes in a single pass for Python and PowerShell (the two languages touched
   this cycle), and coverage is unchanged or improved in both.
5. `blocking_count == 0`.
