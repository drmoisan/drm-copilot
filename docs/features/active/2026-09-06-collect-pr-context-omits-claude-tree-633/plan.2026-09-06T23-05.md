# collect-pr-context-omits-claude-tree (Plan)

- **Issue:** #633
- **Parent:** epic `cleanup-merged-worktrees-hardening` (child H), non-blocking preparation-mode child
- **Owner:** drmoisan
- **Last Updated:** 2026-09-06T23-05
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** full-bug (spec.md is the sole acceptance-criteria source; `## Acceptance Criteria` in `spec.md` carries 8 checkbox items, AC1-AC8 below)
- **Feature root:** `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633`

**Scope boundary (do not exceed):** this plan touches only
`extensions/drm-copilot/src/lib/pr-context/collector-core.ts`,
`extensions/drm-copilot/jest.config.cjs`, and the three test files under
`extensions/drm-copilot/test/lib/pr-context/`. No task in this plan modifies
`scripts/bash/cleanup_worktrees_*`, `.claude/hooks/*`, or any Python file
(`scripts/dev_tools/pr_context/collector.py` is explicitly verified unmodified, not
edited).

**Fail-closed evidence rule:** every baseline, regression, and final-QA artifact named
below must exist with all required schema fields before the task producing it may be
checked off. Missing or incomplete evidence blocks the task, not just the phase.

**Evidence location:** all evidence in this plan is written under
`docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/<kind>/`
(`baseline/`, `regression-testing/`, `qa-gates/`, `other/`). No task in this plan writes
to `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or any other
non-canonical path.

---

### Phase 0 — Policy Reads and Baseline Capture

- [ ] [P0-T1] Read `CLAUDE.md` in full and note its Policy Compliance Reading Order section for later reference.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md` in full.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md` in full.
- [ ] [P0-T4] Read `.claude/rules/typescript.md` in full (the sole in-scope language for this fix).
- [ ] [P0-T5] Write the Phase 0 policy-read evidence artifact at `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/phase0-instructions-read.<timestamp>.md` (ISO-8601 `yyyy-MM-ddTHH-mm` timestamp) containing `Timestamp:`, `Policy Order:` (list the four files from P0-T1 through P0-T4 in that exact order), and an explicit bullet list of the four file paths read. The task is complete only when this file exists with all three fields present.
- [ ] [P0-T6] Run `cd extensions/drm-copilot && npm ci` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (the final npm summary line, e.g. package count/install time) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/npm-ci.<timestamp>.md`. `EXIT_CODE` must be `0` before any later task in this plan runs `npx`/`npm run` inside `extensions/drm-copilot`, because `node_modules` is gitignored and not populated by `git worktree add`.
- [ ] [P0-T7] Run the read-only baseline format check `cd extensions/drm-copilot && npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` (no `--write`, so no file is modified by this task) and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (the literal success or failure message Prettier prints, and, on failure, the list of non-conforming files) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/prettier-check.<timestamp>.md`.
- [ ] [P0-T8] Run the baseline lint check `cd extensions/drm-copilot && npm run lint` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (problem count, or "no output" when clean) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/eslint-check.<timestamp>.md`.
- [ ] [P0-T9] Run the baseline type-check `cd extensions/drm-copilot && npm run typecheck` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/tsc-check.<timestamp>.md`.
- [ ] [P0-T10] Run the full baseline coverage suite `cd extensions/drm-copilot && npm run test:coverage` and record `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (the Jest pass/fail totals line and the aggregate Statements/Branches/Functions/Lines percentages printed by the `text-summary` coverage reporter configured in `jest.config.cjs`) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/jest-coverage-full.<timestamp>.md`. This run produces `extensions/drm-copilot/coverage/lcov.info`, consumed by P0-T11.
- [ ] [P0-T11] Read `extensions/drm-copilot/coverage/lcov.info` produced by P0-T10. Locate the three per-file records via the fixed-string anchors `SF:src\lib\pr-context\collector-core.ts`, `SF:src\lib\pr-context\collector-output.ts`, and `SF:src\lib\pr-context\summary-helpers.ts` (backslash path separators, matching the Windows/Jest/lcov output convention confirmed in prior evidence artifacts, e.g. `docs/features/active/2026-08-07-parallel-enforcement-hooks-440/evidence/qa-gates/final-qc-ts-tests-coverage.2026-08-08T23-15.md:57`); for each record, read its `LF:`/`LH:` (lines found/hit) and `BRF:`/`BRH:` (branches found/hit) fields and compute the line and branch percentages. Record all three files' baseline `LF`, `LH`, `BRF`, `BRH`, and computed percentages in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/baseline/lcov-per-file-baseline.<timestamp>.md`. Note explicitly that `jest.config.cjs`'s `coverageThreshold` map (confirmed by direct read: lines 33-40 gate `collector-output.ts` and `summary-helpers.ts` today) carries no entry for `collector-core.ts` at this point in the plan, so `collector-core.ts`'s baseline percentages are descriptive only and not yet gated by a threshold.

### Phase 1 — Fail-Before Regression Test (Bucket-Partition Gap)

- [ ] [P1-T1] [expect-fail] In `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, inside the existing `describe("collectPrContext classification and buckets", ...)` block (opens at line 242 as of this plan's authoring), add a new sibling test immediately after the existing `it("classifies pull/invalid refs and partitions core/rename buckets", ...)` test (which ends at line 337) and before that describe block's closing `});` (line 338), named exactly `it("routes previously-dropped paths into bucketDocs (fail-before: current code drops them)", () => { ... })`. Mirror the existing test's construction pattern (a local `ScriptRunner` built the same way as lines 299-314, reusing `richGh()` or `ghHandler({ ghAvailable: true })` for gh dispatch and `gitHandler` as the fallback): stub `diff --name-status` to return `` `M\t.claude/skills/example/SKILL.md\nM\tsrc/example.ts\nM\t${CHANGED}` `` and `diff --numstat` to return `` `1\t0\t.claude/skills/example/SKILL.md\n1\t0\tsrc/example.ts\n1\t0\t${CHANGED}` ``. Call `collectPrContext(...)` with the same options shape used by the neighboring test, then assert `result.bucketDocs.map(([p]) => p)` contains both `.claude/skills/example/SKILL.md` and `src/example.ts`. Task is complete when `git status --porcelain -- extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` reports the file as modified (`M ` prefix) and `grep -F "routes previously-dropped paths into bucketDocs" extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts` returns exactly one match.
- [ ] [P1-T2] [expect-fail] Against the pre-fix `collector-core.ts` (before any task in Phase 2 runs), run `cd extensions/drm-copilot && node run-jest.cjs --testPathPatterns=test/lib/pr-context/collector-core.test.ts --testNamePattern="routes previously-dropped paths into bucketDocs"` and confirm the command's `EXIT_CODE` is non-zero (the new test fails because `.claude/skills/example/SKILL.md` and `src/example.ts` match none of the three existing predicates and are silently dropped from all three buckets). Record `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero), and `Output Summary:` (the Jest failure output naming the failed assertion) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/regression-testing/fail-before-bucket-partition.<timestamp>.md`. This is the fail-before evidence required by AC2.

### Phase 2 — Production Fix

- [ ] [P2-T1] In `extensions/drm-copilot/src/lib/pr-context/collector-core.ts`, edit the bucket-partition loop (the `if`/`else if`/`else if` chain confirmed by direct read at lines 320-333, immediately preceding the function's `return` statement at line 335) to add a terminal `else` branch between the existing `else if (path.startsWith("docs/") || ...) { bucketDocs.push([path, stats]); }` block's closing `}` and the `for` loop's closing `}`, in exactly this form:
  ```
    } else {
      // Route every unmatched, non-renamed path into the docs/tooling bucket.
      bucketDocs.push([path, stats]);
    }
  }
  ```
  Do not change the rename-status check, the `.py`/`.ps1` check, or the `docs/`/`.github`/`AGENTS` check. Task is complete when `git diff main -- extensions/drm-copilot/src/lib/pr-context/collector-core.ts` shows a change, and `grep -F "// Route every unmatched, non-renamed path into the docs/tooling bucket." extensions/drm-copilot/src/lib/pr-context/collector-core.ts` returns exactly one match.
- [ ] [P2-T2] Re-run the same scoped test from P1-T2 — `cd extensions/drm-copilot && node run-jest.cjs --testPathPatterns=test/lib/pr-context/collector-core.test.ts --testNamePattern="routes previously-dropped paths into bucketDocs"` — against the now-fixed `collector-core.ts` and confirm `EXIT_CODE` is `0`. Record `Timestamp:`, `Command:`, `EXIT_CODE:` (`0`), and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/regression-testing/pass-after-bucket-partition.<timestamp>.md`. This is the pass-after evidence required by AC2 and AC1.

### Phase 3 — Non-Regression Coverage, Render Assertion, and Threshold Wiring

- [ ] [P3-T1] In `extensions/drm-copilot/jest.config.cjs`, add a new entry to the `coverageThreshold` object for `./src/lib/pr-context/collector-core.ts` with `{ lines: 85, branches: 75 }`, placed adjacent to the existing entries for `./src/lib/pr-context/collector-output.ts` and `./src/lib/pr-context/summary-helpers.ts` (confirmed by direct read at lines 33-40 as of this plan's authoring). The new entry must read exactly, matching the existing entries' current multi-line formatting (confirmed by direct read of `extensions/drm-copilot/jest.config.cjs` lines 33-36 in this revision pass):
  ```
      "./src/lib/pr-context/collector-core.ts": {
        lines: 85,
        branches: 75,
      },
  ```
  Task is complete when `grep -F "\"./src/lib/pr-context/collector-core.ts\": {" extensions/drm-copilot/jest.config.cjs` returns exactly one match.
- [ ] [P3-T2] In `extensions/drm-copilot/test/lib/pr-context/collector-core.test.ts`, extend the existing `it("classifies pull/invalid refs and partitions core/rename buckets", ...)` test's assertion block (immediately after its existing three `expect` lines that check `bucketRenames`/`bucketCore`/`bucketDocs` membership, confirmed by direct read at lines 334-336) with three additional negative assertions: `expect(result.bucketDocs.map(([p]) => p)).not.toContain("new.py")`, `expect(result.bucketRenames.map(([p]) => p)).not.toContain("src/app.py")`, and `expect(result.bucketCore.map(([p]) => p)).not.toContain(CHANGED)`. These confirm the new terminal `else` branch does not double-bucket a rename, a `.py` file, or a `docs/`-prefixed file that already matched an earlier predicate (AC5, no double-counting). Task is complete when `cd extensions/drm-copilot && node run-jest.cjs --testPathPatterns=test/lib/pr-context/collector-core.test.ts --testNamePattern="classifies pull/invalid refs and partitions core/rename buckets"` reports `EXIT_CODE` `0`.
- [ ] [P3-T3] In `extensions/drm-copilot/test/lib/pr-context/collector-output.test.ts`, add a new test inside the existing `describe("buildSummaryText", ...)` block named exactly `it("renders a previously-dropped non-core/non-docs-prefixed path in the Changed files overview section", () => { ... })`, that calls `buildSummaryText` using the file's existing `collected(overrides)` builder (confirmed by direct read at lines 55-88) with `overrides: { bucketDocs: [[".claude/skills/example/SKILL.md", [1, 0]], ["src/example.ts", [1, 0]]] }`, and asserts the returned text contains both `.claude/skills/example/SKILL.md` and `src/example.ts` at an index strictly between the index of the literal `"===== Changed files overview ====="` (confirmed present at line 115 of this same test file) and the index of the literal `"===== Issue digests ====="` (confirmed present at line 116). Task is complete when `cd extensions/drm-copilot && node run-jest.cjs --testPathPatterns=test/lib/pr-context/collector-output.test.ts --testNamePattern="renders a previously-dropped non-core/non-docs-prefixed path"` reports `EXIT_CODE` `0`. This satisfies AC3 without modifying `collector-output.ts` itself.
- [ ] [P3-T4] Run the existing, unmodified test file `cd extensions/drm-copilot && node run-jest.cjs --testPathPatterns=test/lib/pr-context/collector-output-freshness.test.ts` and confirm `EXIT_CODE` is `0`, verifying that all three tests in this file — which build their fixture record with `bucketCore: []`, `bucketRenames: []`, `bucketDocs: []` (confirmed present at lines 74-76 of this file as of this plan's authoring) — continue to pass unmodified after the Phase 2 fix, i.e., the new terminal `else` branch adds nothing when the upstream `statusMap` is empty. Record `Timestamp:`, `Command:`, `EXIT_CODE:` (`0`), and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/regression-testing/freshness-fixture-unchanged.<timestamp>.md`. This is the AC4 evidence.

### Phase 4 — Full TypeScript QA Loop

Run these four steps in order; if any step reports a non-zero `EXIT_CODE`, or P4-T1's before/after tree observation shows a change, restart from P4-T1 and re-record all four artifacts before proceeding to Phase 5.

- [ ] [P4-T1] Run `cd extensions/drm-copilot && git status --porcelain -- src test` (before-snapshot), then `cd extensions/drm-copilot && npm run format` (write-mode Prettier), then `cd extensions/drm-copilot && git status --porcelain -- src test` again (after-snapshot). Record `Timestamp:`, `Command:`, `EXIT_CODE:`, both porcelain snapshots, and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/format.<timestamp>.md`. If the after-snapshot differs from the before-snapshot, this step rewrote tracked files; the loop restarts from this task per the phase preamble.
- [ ] [P4-T2] Run `cd extensions/drm-copilot && npm run lint` and record `Timestamp:`, `Command:`, `EXIT_CODE:` (must be `0`), and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/lint.<timestamp>.md`.
- [ ] [P4-T3] Run `cd extensions/drm-copilot && npm run typecheck` and record `Timestamp:`, `Command:`, `EXIT_CODE:` (must be `0`), and `Output Summary:` in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/typecheck.<timestamp>.md`.
- [ ] [P4-T4] Run `cd extensions/drm-copilot && npm run test:coverage` and record `Timestamp:`, `Command:`, `EXIT_CODE:` (must be `0`, meaning every entry in `jest.config.cjs`'s `coverageThreshold` map — including the `collector-core.ts` entry added in P3-T1 — is satisfied), and `Output Summary:` (Jest pass/fail totals and the aggregate `text-summary` coverage percentages) in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/test-coverage.<timestamp>.md`. This run refreshes `extensions/drm-copilot/coverage/lcov.info`, consumed by Phase 5.

### Phase 5 — Coverage Delta Reconciliation

- [ ] [P5-T1] Read `extensions/drm-copilot/coverage/lcov.info` as refreshed by P4-T4. Locate the same three per-file records via the fixed-string anchors `SF:src\lib\pr-context\collector-core.ts`, `SF:src\lib\pr-context\collector-output.ts`, and `SF:src\lib\pr-context\summary-helpers.ts` (backslash path separators, matching the Windows/Jest/lcov output convention confirmed in prior evidence artifacts, e.g. `docs/features/active/2026-08-07-parallel-enforcement-hooks-440/evidence/qa-gates/final-qc-ts-tests-coverage.2026-08-08T23-15.md:57`), read each record's `LF:`/`LH:`/`BRF:`/`BRH:` fields, and compute the post-change line and branch percentages for all three files. Record the post-change numbers in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/lcov-per-file-post-change.<timestamp>.md`.
- [ ] [P5-T2] Compare the P5-T1 post-change numbers against the P0-T11 baseline numbers for all three files. Confirm `collector-core.ts`'s post-change line percentage is `>= 85` and branch percentage is `>= 75` (it had no threshold and only descriptive baseline numbers before this fix); confirm `collector-output.ts` and `summary-helpers.ts` show no line/branch percentage regression relative to their P0-T11 baseline. Record baseline, post-change, and delta values for all three files, plus a pass/fail verdict per file, in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/qa-gates/coverage-delta.<timestamp>.md`. If any file fails its check, the plan outcome is remediation-required and must not be reported as complete. This is the AC8 evidence.

### Phase 6 — Acceptance Criteria Reconciliation

- [ ] [P6-T1] AC1 ("every non-renamed changed path routes into exactly one bucket, no silent drop"): confirm this is satisfied by citing the P2-T1 fix and the P2-T2 pass-after result (`EXIT_CODE 0`) together with the P3-T2 no-double-bucketing result (`EXIT_CODE 0`). Record the citation in `docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/evidence/other/ac-reconciliation.<timestamp>.md` under an `AC1:` heading.
- [ ] [P6-T2] AC2 ("collector-core.test.ts regression test demonstrates the previously-dropped path lands in `bucketDocs`, and fails pre-fix"): confirm by citing the P1-T1 test addition, the P1-T2 fail-before artifact (`EXIT_CODE` non-zero), and the P2-T2 pass-after artifact (`EXIT_CODE 0`). Append an `AC2:` section to the same `ac-reconciliation.<timestamp>.md` artifact.
- [ ] [P6-T3] AC3 ("collector-output.test.ts confirms the previously-dropped path appears in the rendered overview"): confirm by citing the P3-T3 test result (`EXIT_CODE 0`). Append an `AC3:` section.
- [ ] [P6-T4] AC4 ("the existing empty-bucket fixture in `collector-output-freshness.test.ts` continues to pass unmodified"): confirm by citing the P3-T4 result (`EXIT_CODE 0`, file unmodified). Append an `AC4:` section.
- [ ] [P6-T5] AC5 ("rename precedence, `.py`/`.ps1` core routing, and `docs/`/`.github`/`AGENTS` docs routing unchanged, no double-bucketing"): confirm by citing the P3-T2 result (`EXIT_CODE 0`). Append an `AC5:` section.
- [ ] [P6-T6] AC6 ("`scripts/dev_tools/pr_context/collector.py` explicitly left unmodified; Known Limitation section documents the identical defect and a follow-up recommendation"): run `git diff main -- scripts/dev_tools/pr_context/collector.py` and confirm the command produces no output; run `grep -F "## Known Limitation (Python Parity Module)" docs/features/active/2026-09-06-collect-pr-context-omits-claude-tree-633/spec.md` and confirm exactly one match. Append an `AC6:` section citing both results.
- [ ] [P6-T7] AC7 ("full TypeScript toolchain pass completed — Prettier, ESLint, `tsc`, Jest with coverage — with `npm ci` run first"): confirm by citing the P0-T6 `npm ci` result (`EXIT_CODE 0`) and the Phase 4 results (P4-T1 through P4-T4, all `EXIT_CODE 0` in the same pass with no file changes from P4-T1). Append an `AC7:` section.
- [ ] [P6-T8] AC8 ("line coverage >= 85% and branch coverage >= 75% maintained on changed files, no production file excluded from coverage measurement"): confirm by citing the P5-T2 coverage-delta artifact and by confirming `extensions/drm-copilot/jest.config.cjs`'s `collectCoverageFrom` array still reads exactly `["src/**/*.ts", "!src/**/*.d.ts"]` (no new production-path exclusion added by this plan). Append an `AC8:` section citing both. This is the final task of the plan; when it is checked off, all 8 acceptance criteria in `spec.md` are reconciled against recorded evidence.
