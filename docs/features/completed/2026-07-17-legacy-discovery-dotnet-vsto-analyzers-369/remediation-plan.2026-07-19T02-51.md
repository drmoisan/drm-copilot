# Remediation Plan — legacy-discovery-dotnet-vsto-analyzers (#369), Remediation Cycle 2

- Timestamp: 2026-07-19T02-51
- Issue: #369
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/issue.md`)
- Branch: `feature/legacy-discovery-dotnet-vsto-analyzers-369`
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-inputs.2026-07-19T02-51.md` (single Blocking finding — the contract test `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` fails because two repo `.claude` hook files have no byte-identical counterpart in the bundled extension payload). No scope beyond that document is authorized.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`). No `artifacts/`-rooted evidence path is permitted. Prior full-feature-cycle evidence (`phase0-*`, `final-qc-*`, `coverage-delta.2026-07-18T21-15`) and prior remediation cycle-1 evidence (`r1c1-*`) MUST NOT be overwritten. All new artifacts for this cycle use the `r2c2-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root unless a task states otherwise. Shell is PowerShell unless a task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- The two repo-root hook files exist and are the authoritative source bytes; they MUST NOT be modified by this plan:
  - `.claude/hooks/enforce-discovery-artifact-gate.ps1`
  - `.claude/hooks/validate-discovery-artifact-gate.ps1`
- The required byte-identical bundle destinations (currently absent) are:
  - `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1`
  - `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1`
- Acceptance for the push-down is byte-identical mirroring (repo bytes == bundle bytes), enforced by `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts`. These are PowerShell resource files copied verbatim as bundle payload; no PowerShell authored-logic change is in scope, so the QA loop is the Python toolchain only.
- The defect pre-exists on `origin/epic/legacy-discovery-and-parity-integration` and was inherited via the cycle-1 merge. It is not introduced by the #369 analyzer changes but blocks CI-green for PR #384. Repository precedent: commit `f17f1af0` ("fix(bundled-resources): push-down four missing agent personas to extension bundle").
- Push-down mechanism note: the repository push-down tool `python -m scripts.dev_tools.push_down_claude_customizations` publishes the entire `.claude` tree into a destination workspace, not individual files. To keep this remediation minimal and scoped strictly to the two missing files (and avoid touching unrelated bundle payload), this plan copies the two files byte-verbatim and verifies byte-identity by hash, which reproduces the exact byte-identical semantics the contract test enforces.

### Phase 0 — Policy Read and Python Baseline Capture

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated.
- [x] [P0-T2] Capture the fail-before state of the blocking contract test by running `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -v` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/regression-testing/r2c2-fail-before-contract-test.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records the assertion message naming the two missing-from-bundle files. This task is `[expect-fail]`: the recorded exit code is expected to be non-zero. Acceptance: the artifact exists, the recorded exit code is non-zero, and the `Output Summary:` names both `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1` as missing from the bundle.
- [x] [P0-T3] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase0-black-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T4] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase0-ruff-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T5] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase0-pyright-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T6] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records the numeric baseline line-coverage and branch-coverage percentages and the pass/fail/skip counts (the failing contract test is expected to appear in the failure count at baseline). Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid).

### Phase 1 — Push Down the Two Missing Hook Files and Re-Verify the Contract Test

- [x] [P1-T1] Copy `.claude/hooks/enforce-discovery-artifact-gate.ps1` byte-verbatim to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1` using `Copy-Item -LiteralPath '.claude/hooks/enforce-discovery-artifact-gate.ps1' -Destination 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1'`. Acceptance: the destination file exists after the copy.
- [x] [P1-T2] Verify byte-identity of the first pushed-down file: compute `(Get-FileHash -LiteralPath '.claude/hooks/enforce-discovery-artifact-gate.ps1' -Algorithm SHA256).Hash` and `(Get-FileHash -LiteralPath 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1' -Algorithm SHA256).Hash` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase1-hash-enforce-hook.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording both hashes. Acceptance: the two SHA256 hashes are identical.
- [x] [P1-T3] Copy `.claude/hooks/validate-discovery-artifact-gate.ps1` byte-verbatim to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1` using `Copy-Item -LiteralPath '.claude/hooks/validate-discovery-artifact-gate.ps1' -Destination 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1'`. Acceptance: the destination file exists after the copy.
- [x] [P1-T4] Verify byte-identity of the second pushed-down file: compute `(Get-FileHash -LiteralPath '.claude/hooks/validate-discovery-artifact-gate.ps1' -Algorithm SHA256).Hash` and `(Get-FileHash -LiteralPath 'extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1' -Algorithm SHA256).Hash` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase1-hash-validate-hook.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording both hashes. Acceptance: the two SHA256 hashes are identical.
- [x] [P1-T5] Confirm the repo-root originals were not modified: run `git status --porcelain -- .claude/hooks/enforce-discovery-artifact-gate.ps1 .claude/hooks/validate-discovery-artifact-gate.ps1` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-phase1-originals-unmodified.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the porcelain output. Acceptance: the command produces no output for either repo-root file (both originals remain unmodified). Only the two new bundle-destination paths under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/` may appear as changes elsewhere in the working tree.
- [x] [P1-T6] Re-run the specific contract test module until it passes: run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -v` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/regression-testing/r2c2-pass-after-contract-test.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the pass counts and explicit confirmation that `test_bundled_claude_payload_contains_all_repo_runtime_contracts` passed with no missing-from-bundle assertion. Acceptance: exit code 0 and the named test passes. If it still fails, correct the byte-identical copy in Phase 1 and re-run this task until it passes; `EXIT_CODE: SKIPPED` is not a valid outcome.

### Phase 2 — Final QA Loop (Python)

- [x] [P2-T1] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P2-T1. Acceptance: exit 0 with no files changed, recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T2] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P2-T1. Acceptance: exit 0 recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T3] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with 0 errors recorded in the artifact. `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-change line-coverage and branch-coverage percentages and the pass/fail/skip counts, and explicit confirmation that the full suite (including `test_bundled_claude_payload_contains_all_repo_runtime_contracts`) is green. If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid). `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T5] Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-coverage-delta.<TS>.md` comparing the Phase 0 baseline coverage (from `evidence/remediation-baseline/r2c2-phase0-pytest-baseline.<TS>.md`) against the P2-T4 post-change coverage, reporting: baseline line/branch %, post-change line/branch %, and confirmation that line coverage >= 85% and branch coverage >= 75% hold with no regression. Acceptance: the artifact contains all numeric values and both threshold checks pass. (The change adds only two byte-identical PowerShell resource files to the bundle and no Python production code, so no coverage regression is expected.)

### Phase 3 — Commit, Push, and Confirm Mergeability

- [x] [P3-T1] Stage and commit the two pushed-down bundle files as a single change: `git add extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-discovery-artifact-gate.ps1 extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-discovery-artifact-gate.ps1`, then `git commit` with a professional, factual message referencing issue #369 remediation cycle 2 and the bundle push-down of the two discovery-artifact-gate hooks. Acceptance: `git log -1 --name-only` lists exactly the two bundle files (and no repo-root `.claude/hooks/*` original) as changed.
- [x] [P3-T2] Stage and commit this cycle's plan-checklist updates and all Phase 0–2 evidence artifacts (`docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-plan.2026-07-19T02-51.md` and every new file under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r2c2-*`, `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/regression-testing/r2c2-*`, and `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-*`) in a follow-up commit with a professional, factual commit message referencing issue #369 remediation cycle 2. Acceptance: `git status --porcelain` is empty.
- [x] [P3-T3] Push the updated branch: `git push origin feature/legacy-discovery-dotnet-vsto-analyzers-369`. Acceptance: the command exits 0 and the remote branch HEAD matches the local branch HEAD (verify with `git rev-parse HEAD` and `git rev-parse origin/feature/legacy-discovery-dotnet-vsto-analyzers-369` after the push).
- [x] [P3-T4] Confirm PR #384 remains mergeable after the push: run `gh pr view 384 --json mergeable,mergeStateStatus` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r2c2-pr-mergeable-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the recorded `mergeable` value is `MERGEABLE` (not `CONFLICTING` or `UNKNOWN`).

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; both missing repo `.claude` hook files are present as byte-identical copies at their bundle destinations under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/` (verified by SHA256 hash equality) with the repo-root originals unmodified; `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` passes; the full Python toolchain (Black, Ruff, Pyright, Pytest with coverage) passes with line coverage >= 85% and branch coverage >= 75% and no regression; fresh evidence exists under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/` with the `r2c2-` prefix without overwriting prior-cycle evidence; the branch is pushed; and `gh pr view 384 --json mergeable` reports `MERGEABLE`.
