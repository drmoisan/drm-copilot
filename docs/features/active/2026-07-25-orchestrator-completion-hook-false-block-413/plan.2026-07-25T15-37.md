# Orchestrator Completion Hook False-Block on Successful Validation (Plan)

- **Issue:** #413
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-25T15-37
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug (`spec.md` is the sole acceptance-criteria source; `user-story.md` is intentionally absent)
- **Branch:** `bug/orchestrator-completion-hook-false-block-413` (based on `origin/main`; review base `main`)

## Authoritative Inputs

- Requirements and acceptance criteria (13 items): `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/spec.md` (`## Acceptance Criteria`)
- Reproduction and work-mode marker: `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/issue.md`
- Completed research (conclusions are established inputs; do not relitigate): `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/research/2026-07-25T10-15-orchestrator-completion-hook-false-block-413-research.md`

## Conventions Used in This Plan

- `<FEATURE>` = `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413`
- `<WORKSPACE>` = `C:/Users/DanMoisan/repos/drm-copilot/.claude/worktrees/agent-a0fcdf306557436df` (pass as `workspace_root` to every `mcp__drm-copilot__run_poshqc_*` call)
- `<ts>` = an ISO-8601 timestamp of the form `yyyy-MM-ddTHH-mm` captured at task execution time
- Every evidence artifact for a command step MUST record: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Test-step artifacts MUST additionally record numeric coverage headline values (overall covered % and per-file covered % for the changed hook `.claude/hooks/validate-orchestrator-output.ps1`).
- Evidence locations are non-overridable: `<FEATURE>/evidence/baseline/`, `<FEATURE>/evidence/regression-testing/`, `<FEATURE>/evidence/qa-gates/`, `<FEATURE>/evidence/other/`, and `<FEATURE>/evidence/issue-updates/` only. No `artifacts/`-rooted evidence path is permitted for any artifact produced by this plan. The only permitted `artifacts/` paths in this plan are the orchestration checkpoint itself, `artifacts/orchestration/orchestrator-state.json` (a subject of the fix, not an evidence store), and the read-only Pester run outputs `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml` (toolchain outputs that are read for numeric values, never written as evidence).
- Toolchain environment fact and authority split: `mcp__drm-copilot__run_poshqc_test` executes the published npx-cached MCP bundle, not the working tree's PoshQC engine, and its return payload carries no coverage numbers. The MCP forms are authoritative for pass/fail status; the direct repo-root run `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC; Invoke-PoshQCTest -Root ."` is authoritative for coverage numbers, which are read from the run outputs `artifacts/pester/pester-junit.xml` (pass/fail counts) and `artifacts/pester/powershell-coverage.xml` (Pester `CoverageGutters`/JaCoCo counters: INSTRUCTION/LINE/METHOD only; no BRANCH counter is emitted). The direct repo-root forms (`Invoke-PoshQCFormat -Root .`, `Invoke-PoshQCAnalyze -Root .`) remain the documented fallback for format/analyze if any doubt arises about whether an edit was exercised.
- Coverage gates (uniform, `.claude/rules/quality-tiers.md`): line >= 85%, branch >= 75%, no regression on changed lines. The hook and both portable modules are already instrumented in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` (hook at line 48); no runsettings change is in scope.
- Python coverage note: no Python production code is modified by this plan. The parity pytest (`tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`) is a gate over PowerShell file content, not a Python code change, so PowerShell is the only language requiring coverage-bearing baseline and final-QC evidence.

## Approved Fix (established scope — sequence only, do not redesign)

In `Invoke-RoutingContractValidation` (`.claude/hooks/validate-orchestrator-output.ps1`, line 224) change:

```powershell
$hasErrors = ($exitCode -ne 0) -or (-not [string]::IsNullOrWhiteSpace($outputText))
```

to:

```powershell
$hasErrors = ($exitCode -ne 0)
```

retaining `ErrorText = $outputText`, correcting the `.DESCRIPTION` docstring (approximately lines 167-170, which documents the two-disjunct rule) and the inline decision comment (approximately lines 222-223). The bundled copy `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1` is resynced byte-identically by hand in the same change (no regeneration script exists); parity is locked by `test_bundled_claude_payload_contains_all_repo_runtime_contracts` in `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`.

Files that change (complete list):

1. `.claude/hooks/validate-orchestrator-output.ps1`
2. `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1`
3. `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1`

## Non-Goals (record; a diff touching any of these is out of scope)

- No change to `scripts/dev_tools/validate_orchestrator_state.py`, `scripts/dev_tools/validate_orchestration_artifacts.py`, or any complexity-floor implementation (owned by a parallel orchestration; a change here would collide).
- No change to the `step9_status: "passed"` documentation/validator divergence.
- No work on the repo-wide `npm audit` GHSA-mh99-v99m-4gvg advisory.
- No change to `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` or `OrchestratorState.psm1` (either copy) — verify-only per Phase 1.
- No `.codex/hooks/` work: research verified no Codex mirror of this hook exists (confirmed again in Phase 1).
- No change to `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`.

## Test-File Line Budget Decision (binding)

`tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` is currently 449 lines against the hard 500-line cap (`.claude/rules/general-code-change.md`). Per the spec Test Strategy: the defect-asserting test at approximately lines 266-276 is replaced in place (approximately net-zero), and the two added It blocks (the end-to-end ALLOW test, ~20 lines, and the P2-T3 exit-2 fail-closed unit test, ~8 lines) land the file at approximately 477 lines, still under the 500-line cap. The binding decision to keep all test changes in the existing file is unaffected. Decision: all test changes go in the existing file; no sibling file is created. Contingency (only if actual post-edit count would exceed 500): move the new issue-413 regression tests to a sibling file `tests/scripts/claude-hooks/validate-orchestrator-output.routing-contract.Tests.ps1` in the same directory. Phase 4 verifies the final count.

**Fail-closed evidence rule:** if any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the outcome is BLOCKED or INCOMPLETE, never PASS.

**No-SKIPPED rule:** every command-bearing task below must execute its stated command; `EXIT_CODE: SKIPPED` is invalid except where a task's own text explicitly authorizes a skip branch.

### Phase 0 — Policy Reading and Baseline Capture

- [x] [P0-T1] Read the policy files in this exact order: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/powershell.md`, (5) `.claude/rules/quality-tiers.md`. Write `<FEATURE>/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields and lists all five files in order.
- [x] [P0-T2] Record the branch/commit baseline. Commands: `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD` at `<WORKSPACE>`. Evidence: `<FEATURE>/evidence/baseline/branch-baseline.<ts>.md` with `Timestamp:`, `Command:` (the artifact MUST record both command strings), `EXIT_CODE:`, `Output Summary:` (branch name `bug/orchestrator-completion-hook-false-block-413` and the HEAD SHA). Acceptance: artifact exists with all four fields, both command strings, and both values.
- [x] [P0-T3] Capture the PowerShell format baseline. Command: `mcp__drm-copilot__run_poshqc_format` with `workspace_root = <WORKSPACE>` (fallback: `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC; Invoke-PoshQCFormat -Root ."` at repo root). Evidence: `<FEATURE>/evidence/baseline/poshqc-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields and EXIT_CODE 0 with no files changed; confirm with `git status --porcelain` before and after the format run (the MCP return payload does not report file changes). A dirty baseline is a blocker to report, not to fix silently.
- [x] [P0-T4] Capture the PowerShell analyze baseline. Command: `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root = <WORKSPACE>` (fallback: direct `Invoke-PoshQCAnalyze -Root .`). Evidence: `<FEATURE>/evidence/baseline/poshqc-analyze.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields and EXIT_CODE 0.
- [x] [P0-T5] Capture the PowerShell test-and-coverage baseline. Command: `mcp__drm-copilot__run_poshqc_test` with `workspace_root = <WORKSPACE>`, followed by the authoritative coverage run `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC; Invoke-PoshQCTest -Root ."` at repo root. Read numeric values from the run outputs: pass/fail counts from `artifacts/pester/pester-junit.xml` and coverage from `artifacts/pester/powershell-coverage.xml` (Pester `CoverageGutters`/JaCoCo). Evidence: `<FEATURE>/evidence/baseline/poshqc-test.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording pass/fail counts, the numeric overall covered percentage, and the numeric per-file covered percentage for `.claude/hooks/validate-orchestrator-output.ps1` (covered/total commands). Record `Branch coverage: not emitted — this toolchain's Pester CoverageGutters output provides INSTRUCTION/LINE/METHOD counters only and no BRANCH counter (precedent: docs/features/completed/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-test-baseline.md)`. Acceptance: artifact exists with all four fields, numeric overall and per-file values (no placeholders), and the branch-metric availability statement. The absent branch metric is a documented tooling limitation and does not trigger the fail-closed evidence rule. Derive the per-file value by summing the INSTRUCTION counters under that file's `<sourcefile>` element under `<package name=".../.claude/hooks">` in `artifacts/pester/powershell-coverage.xml` (covered / (covered + missed)); do not read the report-level total as the per-file value.
- [x] [P0-T6] Capture the bundle-parity pytest baseline. Command: `poetry run python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q` at repo root. Evidence: `<FEATURE>/evidence/baseline/parity-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass count). Acceptance: artifact exists with all four fields and EXIT_CODE 0.

### Phase 1 — Scope Verification (verify-only files and negative claims)

- [x] [P1-T1] Verify the repo portable fallback carries no defect: read `Test-OrchestratorStateCompletionReadiness` in `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` and confirm (a) the success return is literally `@{ ExitCode = 0; Output = '' }` and (b) every error path (load failure, base-presence error, gate error) sets `ExitCode = 1`. Evidence: `<FEATURE>/evidence/baseline/portable-fallback-verification.<ts>.md` quoting the three return sites with line numbers and a `Timestamp:`. Acceptance: artifact exists and confirms both properties; no edit is made to the file.
- [x] [P1-T2] Verify the bundled portable fallback identically: repeat the P1-T1 confirmation against `extensions/drm-copilot/resources/claude-customizations/.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` and record the result in the same artifact `<FEATURE>/evidence/baseline/portable-fallback-verification.<ts>.md` under a separate heading. Acceptance: the artifact confirms both properties for the bundled copy; no edit is made to the file.
- [x] [P1-T3] Verify no Codex mirror of the hook exists: search `.codex/hooks/` for `validate-orchestrator-output.ps1`. Evidence: append to `<FEATURE>/evidence/baseline/portable-fallback-verification.<ts>.md` (or a sibling `codex-mirror-check.<ts>.md` in the same folder) a negative-claim record with `SearchScope:` (`.codex/hooks/`), `SearchPatterns:` (`validate-orchestrator-output.ps1`), `SearchResult:` (`none` expected). Acceptance: negative-evidence record exists with all three fields.
- [x] [P1-T4] Record the test-file line budget: measure the current line count of `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` (expected 449) and record the binding decision from `## Test-File Line Budget Decision` above (edits stay in the existing file; sibling file `tests/scripts/claude-hooks/validate-orchestrator-output.routing-contract.Tests.ps1` only if the post-edit count would exceed 500). Evidence: `<FEATURE>/evidence/baseline/test-file-line-budget.<ts>.md` with `Timestamp:`, the measured count, the projected post-edit count, and the decision. Acceptance: artifact exists with the measured count and the explicit decision.

### Phase 2 — Regression Tests (fail-before, ahead of the production edit)

- [x] [P2-T1] [expect-fail] In `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1`, `Context 'Invoke-RoutingContractValidation'`, replace the defect-asserting test `It 'reports HasErrors when the seam returns error text with exit 0'` (approximately lines 266-276) with `It 'reports no errors when the seam returns exit 0 with the validator success line (issue #413)'`: stub `-Invoker { param($Path, $Type) [pscustomobject]@{ ExitCode = 0; Output = 'orchestrator-state validation passed: artifacts/orchestration/orchestrator-state.json' } }` and assert `$result.HasErrors | Should -BeFalse`. In-memory scriptblock stub only; no temporary files; mirror the precedent test at `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` (approximately lines 241-245). Acceptance: the old exit-0-with-text-blocks assertion no longer exists anywhere in the suite and the new It block compiles.
- [x] [P2-T2] [expect-fail] In the same file, `Context 'routing-contract validation (Gap 1)'`, add `It 'allows DONE when the validator exits 0 and prints its success line (issue #413)'`: reuse the context's existing `Get-CheckpointFileContent` mock, pass a `-RoutingInvoker` stub returning `ExitCode = 0` with the success-line `Output`, and assert `$result.Ok | Should -BeTrue` and `$result.Message | Should -BeNullOrEmpty`. No temporary files; no external processes. Acceptance: the new It block exists in that context and compiles.
- [x] [P2-T3] In `Context 'Invoke-RoutingContractValidation'`, add `It 'reports HasErrors when the seam returns exit code 2 (argparse misuse / crash path stays fail-closed)'`: stub `{ param($Path, $Type) [pscustomobject]@{ ExitCode = 2; Output = 'usage: validate_orchestration_artifacts ...' } }` and assert `$result.HasErrors | Should -BeTrue`. This test is expected to PASS both before and after the fix, so it is deliberately NOT tagged `[expect-fail]`. In-memory scriptblock stub only; no temporary files. Acceptance: the It block exists and passes in both the P2 fail-before run and the P4 pass-after run, providing the AC4 exit-2/crash evidence.
- [x] [P2-T4] [expect-fail] Run the fail-before evidence pass against the still-unfixed hook. Command: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1 -Output Detailed"` at repo root. Expected result: exactly the two issue-413 ALLOW tests from P2-T1 and P2-T2 FAIL (proving they detect the defect), the new exit-2 fail-closed test from P2-T3 PASSES, and all pre-existing tests in the file pass. Evidence: `<FEATURE>/evidence/regression-testing/fail-before.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero expected), and an `Output Summary:` naming both failing test titles, confirming the exit-2 test passed, and confirming no other failures. Acceptance: artifact exists with all four fields and shows both issue-413 ALLOW tests failing before the fix.

### Phase 3 — Production Fix and Bundled Resync

- [x] [P3-T1] In `.claude/hooks/validate-orchestrator-output.ps1`, change line 224 to `$hasErrors = ($exitCode -ne 0)` keeping the following line's `ErrorText = $outputText` return unchanged, and correct the inline comment above the decision (approximately lines 222-223) to describe exit-code-only discrimination. Do not alter the `$Invoker` seam signature, the `{ HasErrors, ErrorText }` return contract, the `2>&1` capture, or the `MODEL_ROUTING_BLOCKED:`/`ROUTING_CONTRACT_BLOCKED:` discrimination in `Invoke-OrchestratorOutputValidation` (approximately lines 316-326). Acceptance: the two-disjunct expression no longer exists in the file and the seam/return contracts are textually unchanged.
- [x] [P3-T2] In the same file, correct the `Invoke-RoutingContractValidation` `.DESCRIPTION` docstring (approximately lines 167-170) so `HasErrors` is documented as `$true` only when the validator reports a non-zero exit code (removing the "or produced any error text" clause) while `ErrorText` remains documented as the captured combined output. Acceptance: the docstring documents exit-code-only discrimination and no longer documents the two-disjunct rule.
- [x] [P3-T3] Resync the bundled copy by hand: copy `.claude/hooks/validate-orchestrator-output.ps1` byte-for-byte over `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1` (for example `Copy-Item -Force`). No regeneration script exists; the copy is the sync mechanism. Acceptance: the bundled file content equals the repo file content.
- [x] [P3-T4] Verify byte parity deterministically. Command (single-quoted outer string so neither bash nor the calling PowerShell expands `$a`/`$b`; run with the workspace root as the working directory): `pwsh -NoLogo -NoProfile -Command '$a = (Get-FileHash .claude/hooks/validate-orchestrator-output.ps1 -Algorithm SHA256).Hash; $b = (Get-FileHash extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1 -Algorithm SHA256).Hash; Write-Output "repo=$a"; Write-Output "bundled=$b"; Write-Output "equal=$($a -eq $b)"'` (must print `equal=True`). Evidence: `<FEATURE>/evidence/qa-gates/bundle-byte-parity.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including both SHA256 hashes and the `equal=` line. Acceptance: artifact exists with matching hashes and `equal=True`.
- [x] [P3-T5] Run the bundle-parity pytest gate. Command: `poetry run python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q` at repo root. Evidence: `<FEATURE>/evidence/qa-gates/parity-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with EXIT_CODE 0.

### Phase 4 — Regression Verification (pass-after)

- [x] [P4-T1] Run the pass-after evidence run for the fixed hook. Command: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1 -Output Detailed"` at repo root. Evidence: `<FEATURE>/evidence/regression-testing/pass-after.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (0 required), and an `Output Summary:` confirming the two issue-413 ALLOW tests now pass, the P2-T3 exit-2 fail-closed test still passes, and zero tests fail. Acceptance: artifact exists pairing with the P2-T4 fail-before artifact (fail-before/pass-after proven).
- [x] [P4-T2] Verify the fail-closed BLOCK tests are unmodified and passing: confirm via `git diff -- tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` that no hunk modifies the body of `It 'reports HasErrors when the seam returns a non-zero exit code'` or `It 'blocks DONE with ROUTING_CONTRACT_BLOCKED when the validator reports errors'` (identify by test title, not line number), and that both passed in the P4-T1 run. Evidence: append the diff-scope confirmation and both test titles with pass status to `<FEATURE>/evidence/regression-testing/pass-after.<ts>.md`. Acceptance: both tests are diff-untouched and passing; no blocking assertion was removed or relaxed.
- [x] [P4-T3] Run the block-reason discrimination suite unmodified. Command: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-hooks/validate-orchestrator-output.model-routing.Tests.ps1 -Output Detailed"` at repo root; also confirm via `git diff` the file has no changes and the `model_routing_receipts|complexity_assessments` regex in `Invoke-OrchestratorOutputValidation` is unchanged in the hook diff. Evidence: `<FEATURE>/evidence/regression-testing/model-routing-discrimination.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists, EXIT_CODE 0, file diff-untouched, regex unchanged.
- [x] [P4-T4] Run the portable-fallback suite unmodified. Command: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCompletion.Tests.ps1 -Output Detailed"` at repo root; confirm via `git diff` that `OrchestratorStateCompletion.psm1` (both repo and bundled copies) and its test file have no changes. Evidence: `<FEATURE>/evidence/regression-testing/portable-fallback-tests.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists, EXIT_CODE 0, all three files diff-untouched.
- [x] [P4-T5] Verify the post-edit line count of `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` is <= 500 (projected ~477). Evidence: append the measured post-edit count to `<FEATURE>/evidence/baseline/test-file-line-budget.<ts>.md`. Acceptance: recorded count <= 500. If the count exceeds 500 this task fails; execute the P1-T4 contingency (sibling file `tests/scripts/claude-hooks/validate-orchestrator-output.routing-contract.Tests.ps1`) and re-run P4-T1 before marking this task complete.

### Phase 5 — Primary Acceptance Evidence (end-to-end hook execution)

- [x] [P5-T1] Determine whether the live checkpoint currently satisfies the DONE gate. Command: `poetry run python -m scripts.dev_tools.validate_orchestration_artifacts orchestrator-state artifacts/orchestration/orchestrator-state.json --require-complete --require-model-routing` at repo root. Evidence: `<FEATURE>/evidence/qa-gates/live-checkpoint-precheck.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists recording the exit code either way (0 or non-zero are both valid recordings; this task gates the branch in P5-T2).
- [x] [P5-T2] Exercise the fixed hook end-to-end (primary acceptance evidence for issue #413). Branch (authorized by this task text): if P5-T1 recorded EXIT_CODE 0, run `CLAUDE_HOOK_INPUT='{"output":"DONE: issue #413 fix complete."}' pwsh -File .claude/hooks/validate-orchestrator-output.ps1` at repo root against the live checkpoint as-is; required outcome exit code 0. If P5-T1 recorded non-zero, do NOT modify `artifacts/orchestration/orchestrator-state.json` (it is owned by the enclosing orchestration). Instead build a completion-passing fixture at `<FEATURE>/evidence/other/completion-passing-checkpoint.<ts>.json` using this concrete recipe: start from the shape returned by `_build_complete_small_state()` (`tests/scripts/dev_tools/test_validate_orchestrator_state.py`, line 590), then add (a) `ci_gate` as an object with `conclusion: "success"`, a non-empty `head_sha`, and `verified_at`; (b) one `model_routing_receipts` entry for each delegated agent (`atomic-planner`, `atomic-executor`, `feature-review`) using the field shape of `_receipt(...)` (`tests/scripts/dev_tools/test_validate_orchestrator_state_model_routing_gate.py`, line 52); and (c) one matching `complexity_assessments` entry per receipt phase using the shape of `_assessment(...)` (same file, line 94). Prove the fixture passes with `poetry run python -m scripts.dev_tools.validate_orchestration_artifacts orchestrator-state <FEATURE>/evidence/other/completion-passing-checkpoint.<ts>.json --require-complete --require-model-routing` at exit 0, then run `CLAUDE_HOOK_INPUT='{"output":"DONE: issue #413 fix complete."}' pwsh -File .claude/hooks/validate-orchestrator-output.ps1 -CheckpointPath <FEATURE>/evidence/other/completion-passing-checkpoint.<ts>.json`; required outcome exit code 0. Evidence: `<FEATURE>/evidence/qa-gates/hook-e2e-allow.<ts>.md` with `Timestamp:`, `Command:` (including the payload and the checkpoint path), `EXIT_CODE:` (0 required), `Output Summary:`, the branch taken, the fixture-validation exit code, and an explicit statement that the live checkpoint was not modified. Acceptance: artifact exists showing hook exit code 0 against a checkpoint proven to pass `--require-complete --require-model-routing` through the real Python subprocess seam. This env-var prefix form requires a POSIX shell; when invoking from PowerShell instead, set `$env:CLAUDE_HOOK_INPUT = '{"output":"DONE: issue #413 fix complete."}'` as a separate preceding statement, then invoke the hook on the following line. PowerShell single-quoted strings are literal, so the inner double quotes MUST NOT be backslash-escaped: a backslash-escaped payload reaches the hook verbatim, fails `ConvertFrom-Json`, and the hook returns `orchestrator hook: failed to parse CLAUDE_HOOK_INPUT as JSON` and exits 1, which would falsely read as a fix failure. Record the form actually used in the evidence `Command:` field.

### Phase 6 — Final QA Loop (full PowerShell toolchain, coverage-bearing)

- [x] [P6-T1] Final QA: format. Command: `mcp__drm-copilot__run_poshqc_format` with `workspace_root = <WORKSPACE>` (fallback: direct `Invoke-PoshQCFormat -Root .`). If this step fails or changes any file, fix, then restart the loop at this task. Evidence: `<FEATURE>/evidence/qa-gates/final-poshqc-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with EXIT_CODE 0 and no files changed on the final pass; confirm with `git status --porcelain` before and after the format run (the MCP return payload does not report file changes).
- [x] [P6-T2] Final QA: analyze. Command: `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root = <WORKSPACE>` (fallback: direct `Invoke-PoshQCAnalyze -Root .`). On any failure, fix and restart the loop at P6-T1. Evidence: `<FEATURE>/evidence/qa-gates/final-poshqc-analyze.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with EXIT_CODE 0. (Type checking is not applicable to PowerShell per `.claude/rules/general-code-change.md`.)
- [x] [P6-T3] Final QA: test with coverage. Command: `mcp__drm-copilot__run_poshqc_test` with `workspace_root = <WORKSPACE>`, followed by the authoritative coverage run `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC; Invoke-PoshQCTest -Root ."` at repo root. Read numeric values from the run outputs: pass/fail counts from `artifacts/pester/pester-junit.xml` and coverage from `artifacts/pester/powershell-coverage.xml` (Pester `CoverageGutters`/JaCoCo). On any failure, fix and restart the loop at P6-T1. Evidence: `<FEATURE>/evidence/qa-gates/final-poshqc-test.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording pass/fail counts, the numeric post-change overall covered percentage, and the numeric post-change per-file covered percentage for `.claude/hooks/validate-orchestrator-output.ps1` (covered/total commands). Record `Branch coverage: not emitted — this toolchain's Pester CoverageGutters output provides INSTRUCTION/LINE/METHOD counters only and no BRANCH counter (precedent: docs/features/completed/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-test-baseline.md)`. Acceptance: artifact exists with all four fields, EXIT_CODE 0, numeric post-change overall and per-file values (no placeholders), and the branch-metric availability statement. The absent branch metric is a documented tooling limitation and does not trigger the fail-closed evidence rule. Derive the per-file value by summing the INSTRUCTION counters under that file's `<sourcefile>` element under `<package name=".../.claude/hooks">` in `artifacts/pester/powershell-coverage.xml` (covered / (covered + missed)); do not read the report-level total as the per-file value.
- [x] [P6-T4] Final QA: bundle-parity pytest. Command: `poetry run python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q` at repo root; must run after the last PowerShell file write of the loop. Evidence: `<FEATURE>/evidence/qa-gates/final-parity-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with EXIT_CODE 0.
- [x] [P6-T5] Coverage delta and threshold verification: compare the baseline values from `<FEATURE>/evidence/baseline/poshqc-test.<ts>.md` (P0-T5) against the post-change values from `<FEATURE>/evidence/qa-gates/final-poshqc-test.<ts>.md` (P6-T3) and confirm (a) overall covered percentage >= 85%, (b) per-file covered percentage for `.claude/hooks/validate-orchestrator-output.ps1` is >= its baseline value (no regression on changed lines), (c) the 75% branch-coverage gate is recorded as not measurable by this toolchain, citing the INSTRUCTION/LINE/METHOD-only counter set and the #272 precedent artifact. Evidence: `<FEATURE>/evidence/qa-gates/coverage-delta.<ts>.md` with `Timestamp:`, baseline values, post-change values, deltas, and a verdict per check. Acceptance: artifact exists with numeric values for (a) and (b) and an explicit PASS/NOT-MEASURABLE verdict for each of the three checks; missing numeric values for (a) or (b) are remediation-required.

### Phase 7 — Acceptance Tracking, Scope Audit, and Handoff

- [x] [P7-T1] Diff-scope audit. Commands (all at repo root): `git status --porcelain`, `git diff --name-only`, `git diff --name-only --cached`, and `git diff --name-only main...HEAD` (the last may be empty when no commit has been made; it is recorded, not relied upon). The union of the four outputs is the audited file list. Confirm the code diff touches only the three in-scope files (`.claude/hooks/validate-orchestrator-output.ps1`, `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1`, `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1`) plus files under `<FEATURE>/` (docs and evidence) and, only if the P4-T5 contingency fired, the sibling test file. Confirm no Python validator, complexity-floor, `.codex/`, `OrchestratorStateCompletion.psm1`, or `pester.runsettings.psd1` file appears. Evidence: `<FEATURE>/evidence/qa-gates/diff-scope-audit.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the union file list and the source command for each entry). Acceptance: artifact exists and the file list contains no out-of-scope path.
- [x] [P7-T2] Late live-checkpoint verification: at the end of execution, when `artifacts/orchestration/orchestrator-state.json` genuinely satisfies `--require-complete --require-model-routing` (re-run the P5-T1 validator command to prove it), set `CLAUDE_HOOK_INPUT` to a DONE-claiming payload and run `pwsh -File .claude/hooks/validate-orchestrator-output.ps1`; required outcome is exit code 0. Authorized conditional (explicit skip branch): if the live checkpoint is owned by the enclosing orchestration and cannot reach a completion-passing state within this execution, record `EXIT_CODE: SKIPPED` with the validator's non-zero precheck output as proof of the constraint, cite the P5-T2 fixture-path run as the standing acceptance evidence, and record that the live checkpoint was never overwritten. Evidence: `<FEATURE>/evidence/qa-gates/hook-e2e-live-checkpoint.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the branch taken. Acceptance: artifact exists showing either hook exit 0 against the live checkpoint or the documented constraint with the precheck proof. This env-var prefix form requires a POSIX shell; when invoking from PowerShell instead, set `$env:CLAUDE_HOOK_INPUT = '{"output":"DONE: issue #413 fix complete."}'` as a separate preceding statement, then invoke the hook on the following line. PowerShell single-quoted strings are literal, so the inner double quotes MUST NOT be backslash-escaped: a backslash-escaped payload reaches the hook verbatim, fails `ConvertFrom-Json`, and the hook returns `orchestrator hook: failed to parse CLAUDE_HOOK_INPUT as JSON` and exits 1, which would falsely read as a fix failure. Record the form actually used in the evidence `Command:` field.
- [x] [P7-T3] Acceptance-criteria check-off: per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, walk all 13 items in the `## Acceptance Criteria` section of `<FEATURE>/spec.md`, check off each satisfied criterion only after citing the specific evidence artifact that satisfies it (evidence-before-check-off), and produce an AC status summary (satisfied/unsatisfied counts with artifact citations). Evidence: the updated `<FEATURE>/spec.md` checkboxes plus the summary recorded in `<FEATURE>/evidence/qa-gates/ac-status-summary.<ts>.md`. Acceptance: every checked AC cites an existing artifact; any unchecked AC has a stated reason.
- [x] [P7-T4] Update feature documentation for handoff: record the outcome, any deviations from scope (expected: none), and links to the fail-before/pass-after and primary-acceptance artifacts in `<FEATURE>/spec.md` (Status field) and, if the issue body requires mirroring, follow the issue-update mirroring convention (`<FEATURE>/evidence/issue-updates/issue-413.<ts>.md`). Acceptance: `spec.md` Status is updated and every recorded link resolves to an existing file.
