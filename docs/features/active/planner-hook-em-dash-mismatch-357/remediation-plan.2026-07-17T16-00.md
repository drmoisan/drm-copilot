# Remediation Plan: planner-hook-em-dash-mismatch-357 (Issue #357) — Cycle 2

**Plan Timestamp:** 2026-07-17T16-00
**Feature Folder:** `docs/features/active/planner-hook-em-dash-mismatch-357`
**Triggering Input:** `docs/features/active/planner-hook-em-dash-mismatch-357/remediation-inputs.2026-07-17T16-00.md`
**Supporting Audits:** `policy-audit.2026-07-17T16-00.md`, `code-review.2026-07-17T16-00.md`, `feature-audit.2026-07-17T16-00.md`

**Scope of this cycle:** Close the two remaining conditions from cycle 1: (1) the bundled `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` copy does not include `.claude/hooks/validate-planner-output.ps1` in its `CodeCoverage.Path` allowlist, so `mcp__drm-copilot__run_poshqc_test` (which reads this bundled copy) still does not measure the file in the canonical `artifacts/pester/powershell-coverage.xml` artifact; (2) ad hoc line coverage for the file is 73.72% (115/156), below the uniform 85% line-coverage threshold. This plan implements Enumerated Fix List items 1–3 from `remediation-inputs.2026-07-17T16-00.md` (all required/blocking).

**Change budget for this cycle:** `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1` (production, settings), `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` (test), `.claude/hooks/validate-planner-output.ps1` (only if a covering test requires a change there — none is expected).

**Explicitly out of scope for this cycle:** `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` (a distinct vendored copy of the hook itself, not the settings file; Informational finding, tracked separately — do not touch). No lowering of the 85% line / 75% branch coverage threshold. No coverage-exclusion of `.claude/hooks/validate-planner-output.ps1`. No temporary files created in tests. No widening of the change budget beyond syncing the one settings-file entry and adding test coverage.

---

### Phase 0 — Remediation Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/powershell.md` in that policy-read order, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/phase0-instructions-read-remediation2.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record the baseline formatting result in `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-format-baseline-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T3] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to the same four files as P0-T2, and record the baseline analyzer result in `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-analyze-baseline-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T4] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` using the pre-fix (unmodified) `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-test-baseline-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` restating this cycle's starting numeric coverage state for `.claude/hooks/validate-planner-output.ps1`: 73.72% ad hoc line coverage (115/156 commands, carried forward from `evidence/qa-gates/coverage-delta-remediation1.md`) and confirming the canonical artifact still does not measure this file at the start of this cycle.
- [x] [P0-T5] Run `grep -n "sourcefilename=\"validate-planner-output" artifacts/pester/powershell-coverage.xml` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/coverage-xml-baseline-check-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming zero matches (no `<class>`/`<sourcefile>` entry for `validate-planner-output.ps1`) prior to this cycle's settings-file edit.
- [x] [P0-T6] Run `diff extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1 scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/settings-diff-baseline-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming the only difference between the two files is the missing `.claude/hooks/validate-planner-output.ps1` allowlist entry (and its comment) in the bundled copy.

---

### Phase 1 — Sync Bundled Settings Copy and Close Remaining Coverage Gap

- [x] [P1-T1] In `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, add `'.claude/hooks/validate-planner-output.ps1'` as a new entry in the `CodeCoverage.Path` array, in the same array position (immediately after the `scripts/powershell/PoshQC/PoshQC.ScanConfig.psm1` entry, before the closing `)`), with the exact matching one-line comment referencing issue #357 already present in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`'s corresponding entry. Acceptance: `diff extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1 scripts/powershell/PoshQC/settings/pester.runsettings.psd1` produces no output (the two files are byte-identical).
- [x] [P1-T2] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanFileContent` directly (no mock) with a path guaranteed not to exist on disk (e.g., `"$PSCommandPath.does-not-exist"`), asserting the returned hashtable has `Exists = $false` and `Lines` is an empty array. Do not create any file at this path.
- [x] [P1-T3] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanFileContent` directly (no mock) with `$PSCommandPath` (the test file itself, which already exists on disk), asserting the returned hashtable has `Exists = $true` and `Lines` is a non-empty array. Do not create any new file for this test.
- [x] [P1-T4] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Invoke-PlannerOutputValidation` with a raw payload whose `output` contains a valid `PREFLIGHT: ALL CLEAR` signal but no plan-path token of any recognized form, asserting `Ok` is `$false` and `Message` matches `'does not advertise a plan-path'`.
- [x] [P1-T5] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Invoke-PlannerOutputValidation` with a raw payload whose `output` contains the markdown-link plan-path form `[plan-path](docs/features/active/foo/plan.md)` plus `PREFLIGHT: ALL CLEAR`, with `Get-PlanFileContent` mocked to return `Exists = $false`, asserting `Ok` is `$false` and `Message` matches both `'no file exists'` and the literal path `docs/features/active/foo/plan.md`.
- [x] [P1-T6] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Invoke-PlannerOutputValidation` with a raw payload whose `output` contains the markdown-link plan-path form with a whitespace-only captured value (e.g., `[plan-path]( )`) plus `PREFLIGHT: ALL CLEAR`, asserting `Ok` is `$false` and `Message` matches `'does not advertise a plan-path'`.
- [x] [P1-T7] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture containing a line that starts with `- [` but does not match the full `- [ ] [P#-T#] <description>` task pattern (e.g., `'- [ ] not a real task line'`) under a valid Phase 0 heading, asserting the returned error array contains an entry matching `'task line must match'`.
- [x] [P1-T8] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture containing a `### Phase 1 — <Title>` heading followed by a task line whose own `[P#-T#]` token names a phase number that has never appeared as a heading (e.g., `- [ ] [P2-T1] ...` under the `Phase 1` heading), asserting the returned error array contains an entry matching `'does not match current phase'`.
- [x] [P1-T9] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture where a phase's first task line is numbered `T2` instead of `T1` (skipping the expected first task number), asserting the returned error array contains an entry matching `'expected task number'`.
- [x] [P1-T10] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture containing only canonical `### Phase N — <Title>` headings and no `- [` task lines at all, asserting the returned error array contains an entry matching `'canonical task lines'`.
- [x] [P1-T11] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture where Phase 0's task text mentions `baseline` but contains none of `policy`, `instruction`, or `read`, asserting the returned error array contains an entry matching `'policy-read task'`.
- [x] [P1-T12] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture where Phase 0's task text mentions `policy` but does not mention `baseline`, asserting the returned error array contains an entry matching `'baseline task'`.
- [x] [P1-T13] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Get-PlanStructureValidationReport` with a `Lines` fixture whose final phase's task text contains none of `qa`, `quality`, `toolchain`, `format`, `lint`, `type`, `test`, or `coverage`, asserting the returned error array contains an entry matching `'QA or toolchain validation'`.
- [x] [P1-T14] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Invoke-PlannerOutputValidation` with a raw payload string that is not valid JSON (e.g., `'{not valid json'`), asserting `Ok` is `$false` and `Message` matches `'failed to parse'`.
- [x] [P1-T15] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case that calls `Invoke-PlannerOutputValidation` with a raw payload that is valid JSON but whose `output` property is absent or whitespace-only (e.g., `'{}'`), asserting `Ok` is `$false` and `Message` matches `'agent output is empty'`.

---

### Phase 2 — Final QA and Coverage Verification

- [x] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-format-remediation2-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero or the run modifies any file, restart the full toolchain loop from P2-T1.
- [x] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to the same four files as P2-T1, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-analyze-remediation2-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero, remediate the reported finding(s) and restart the full toolchain loop from P2-T1.
- [ ] [P2-T3] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` using the now-synced `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-test-remediation2-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including the pass/fail test count and the numeric post-change line coverage percentage for `.claude/hooks/validate-planner-output.ps1` as reported in `artifacts/pester/powershell-coverage.xml`. If any test fails, the run modifies files, or the reported line coverage for `.claude/hooks/validate-planner-output.ps1` is below 85%, add further covering tests to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` targeting any lines still reported uncovered in the canonical artifact (excluding the documented dead-code ternary null-arm at line 217 and the top-level script-invocation lines 282–285/288, which require subprocess execution prohibited by the external-process restriction in `.claude/rules/general-unit-test.md`), then restart the full toolchain loop from P2-T1.
- [ ] [P2-T4] Run `grep -n "sourcefilename=\"validate-planner-output" artifacts/pester/powershell-coverage.xml` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/coverage-xml-post-check-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming a non-empty match now exists for `validate-planner-output.ps1` with a `line-rate`/`LINE` counter, and stating the numeric line-rate value.
- [x] [P2-T5] Run `diff extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1 scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/settings-diff-post-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming the two files remain byte-identical after this cycle's edit (no unrelated drift introduced).
- [x] [P2-T6] Record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/coverage-delta-remediation2.md` comparing this cycle's starting coverage state (73.72% ad hoc line coverage, 115/156 commands, canonical artifact absent for this file, from `evidence/remediation-baseline/poshqc-test-baseline-remediation2.md`) against this cycle's post-change coverage state (the canonical-artifact numeric line coverage from `evidence/qa-gates/poshqc-test-remediation2-final.md` and `evidence/qa-gates/coverage-xml-post-check-remediation2.md`), stating the numeric delta, an explicit pass/fail statement against the 85% uniform line-coverage threshold in `.claude/rules/quality-tiers.md`, and a restated note that Pester 5.6.1's coverage engine does not emit a `BRANCH` counter for any file in this repository (documented, accepted tooling limitation, not attributable to this change).
- [x] [P2-T7] Run `git diff --stat` against the merge-base for `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and this cycle's `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/` additions, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/change-scope-check-remediation2.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming no file outside the declared change budget — in particular `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` — was modified.
