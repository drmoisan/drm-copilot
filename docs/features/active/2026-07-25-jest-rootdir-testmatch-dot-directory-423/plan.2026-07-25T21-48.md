# 2026-07-25-jest-rootdir-testmatch-dot-directory (Plan)

- **Issue:** #423
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-26
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug (spec.md is the sole acceptance-criteria source; user-story.md intentionally absent)
- **Branch:** `bug/jest-no-tests-found-dot-directory-worktree` (base `origin/main` = `fb483b84`)
- **Spec:** `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/spec.md` (17 acceptance criteria)
- **Research:** `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423/research/2026-07-25T22-15-jest-rootdir-testmatch-dot-directory-research.md` (recommendation accepted; do not re-open the design)

## Plan Conventions

- `<FEATURE>` = `docs/features/active/2026-07-25-jest-rootdir-testmatch-dot-directory-423`. All evidence artifacts MUST be written under `<FEATURE>/evidence/<kind>/` (`baseline`, `regression-testing`, `qa-gates`, `other`). No `artifacts/` evidence paths are permitted.
- `<ts>` = ISO-8601 timestamp `yyyy-MM-ddTHH-mm` captured at task execution time.
- Every command-step evidence artifact MUST contain: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- **Fail-closed evidence rule:** if any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or incomplete, the outcome is BLOCKED/INCOMPLETE, never PASS. Do not mark a task complete without its artifact.
- **No-SKIPPED rule:** every command-bearing task below is unconditional. `EXIT_CODE: SKIPPED` is not a valid outcome for any task in this plan.
- **Fail-before semantics:** the CURRENT worktree state (dot-prefixed checkout, unfixed configs) is the fail-before witness. Phase 0 baseline runs are EXPECTED to exit 1 with zero discovered tests; those exits are successful task outcomes, tagged `[expect-fail]`.

## File Ownership (hard constraint)

Only these six files may be modified, plus feature-folder docs/evidence under `<FEATURE>/`:

1. `jest.config.cjs` (root)
2. `run-jest.cjs` (root)
3. `extensions/drm-copilot/jest.config.cjs`
4. `extensions/drm-copilot/run-jest.cjs`
5. `tests/unit/jest-config-resolution.test.ts` (new)
6. `extensions/drm-copilot/test/jest-config-resolution.test.ts` (new)

FORBIDDEN (owned by parallel orchestrations): root `package.json`, `tsconfig*.json`, `.vscode-test.*`, `.claude/rules/**`, `.agents/skills/**`, `extensions/drm-copilot/resources/claude-customizations/**`. If any task appears to require one of these files, stop and report the conflict; do not plan or perform the edit. No new helper modules: the `run-jest.cjs` guards stay inline. No `roots` key change in either Jest config.

## Coverage Note

The extension config carries per-file `coverageThreshold` entries and `collectCoverageFrom: ["src/**/*.ts", "!src/**/*.d.ts"]`. The new test files live under `test/`, not `src/`, so they add no coverage-threshold obligations. Numeric baseline coverage is structurally unobtainable pre-fix because the defect prevents all test discovery (`No tests found, exiting with code 1`); Phase 0 records this as an expect-fail baseline coverage artifact, and Phase 4 records post-change numeric coverage plus the per-file threshold gate result (Jest exits non-zero if any configured threshold regresses). The root package has no coverage thresholds configured and its Jest entry point is not CI-wired (known gap recorded in spec.md; out of scope).

### Phase 0 — Policy Reads and Baseline Capture (fail-before witness)

- [ ] [P0-T1] Read policy files in the required order — `CLAUDE.md` (auto-loaded), `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md` — and write `<FEATURE>/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, the explicit list of files read, and a `Known Documentation Discrepancy` section recording that `.claude/rules/typescript.md` names Vitest while both packages install and run Jest 30.4.2, so the new regression tests use Jest with `@jest/globals` (discrepancy recorded, not a blocker; `.claude/rules/**` is forbidden to modify). Acceptance: the artifact exists with all four sections populated.
- [ ] [P0-T2] Record the git baseline in `<FEATURE>/evidence/baseline/baseline-git.<ts>.md`: current branch name, `HEAD` SHA, base SHA `fb483b84`, and `git status --porcelain` output showing a clean tree (feature-folder files excepted if present). Include `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists and names branch `bug/jest-no-tests-found-dot-directory-worktree` and base `fb483b84`.
- [ ] [P0-T3] [expect-fail] Run `node run-jest.cjs` at the repository root against the UNFIXED config and capture the full output to `<FEATURE>/evidence/regression-testing/fail-before-root-jest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: `EXIT_CODE: 1`, output contains `No tests found, exiting with code 1` and `0 matches` for the `testMatch` patterns (spec AC3 fail-before witness; observed files-checked count recorded as-is, expected ~435).
- [ ] [P0-T4] [expect-fail] Run `npm --prefix extensions/drm-copilot run test` against the UNFIXED config and capture the full output to `<FEATURE>/evidence/regression-testing/fail-before-extension-jest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: non-zero exit, output contains `No tests found, exiting with code 1` and `0 matches` (spec AC3; observed files-checked count recorded as-is, expected ~368).
- [ ] [P0-T5] [expect-fail] Run `npm --prefix extensions/drm-copilot run test:coverage` against the UNFIXED config and capture the output to `<FEATURE>/evidence/baseline/baseline-extension-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, plus a `WhyNumericBaselineUnavailable:` line stating that zero test discovery (this defect) makes numeric baseline coverage structurally unobtainable; the pre-fix coverage baseline is therefore this expect-fail artifact. Acceptance: non-zero exit recorded with the explanation line present.
- [ ] [P0-T6] Run `npm run format:check` at the repository root and capture the result to `<FEATURE>/evidence/baseline/baseline-root-format.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: artifact exists with the observed exit code (expected 0 on a clean base).
- [ ] [P0-T7] Run `npm run lint` at the repository root and capture the result to `<FEATURE>/evidence/baseline/baseline-root-lint.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: artifact exists with the observed exit code (expected 0).
- [ ] [P0-T8] Run `npm run typecheck` at the repository root and capture the result to `<FEATURE>/evidence/baseline/baseline-root-typecheck.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: artifact exists with the observed exit code (expected 0).
- [ ] [P0-T9] Run `npm --prefix extensions/drm-copilot run format` followed by `git status --porcelain -- extensions/drm-copilot` and capture both results to `<FEATURE>/evidence/baseline/baseline-extension-format.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including whether the formatter produced a diff). Acceptance: artifact exists; formatter exit code 0 and no unexpected diff on the clean base.
- [ ] [P0-T10] Run `npm --prefix extensions/drm-copilot run lint` and capture the result to `<FEATURE>/evidence/baseline/baseline-extension-lint.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: artifact exists with the observed exit code (expected 0).
- [ ] [P0-T11] Run `npm --prefix extensions/drm-copilot run typecheck` and capture the result to `<FEATURE>/evidence/baseline/baseline-extension-typecheck.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: artifact exists with the observed exit code (expected 0).

### Phase 1 — testMatch Fix (both Jest configs)

- [ ] [P1-T1] Edit `jest.config.cjs` (repo root): replace the `testMatch` value with exactly `["**/tests/unit/**/*.test.ts", "**/extensions/drm-copilot/test/**/*.test.ts"]`. Change nothing else in the file — no `roots` key, and `testPathIgnorePatterns`, `moduleFileExtensions`, `transform` (including the ts-jest `tsconfig: "<rootDir>/tsconfig.jest.json"` reference), and `coverageProvider` remain byte-identical. Acceptance: the file exports the exact new `testMatch` array; no entry contains `<rootDir>` or a backslash; every entry starts with `**/` (spec AC1).
- [ ] [P1-T2] Edit `extensions/drm-copilot/jest.config.cjs`: replace the `testMatch` value with exactly `["**/test/**/*.test.ts"]`. Change nothing else — no `roots` key, and `testPathIgnorePatterns`, `collectCoverage`, `collectCoverageFrom`, `coverageReporters`, `coverageDirectory`, `coverageThreshold`, `transform`, and all comments remain byte-identical. Acceptance: the file exports the exact new `testMatch` array; no entry contains `<rootDir>` or a backslash; the entry starts with `**/` (spec AC1).
- [ ] [P1-T3] Run `git diff fb483b84 -- jest.config.cjs extensions/drm-copilot/jest.config.cjs` and capture the full diff to `<FEATURE>/evidence/other/config-diff.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: the diff shows only the `testMatch` value change in each file — no `roots` key added, and `testPathIgnorePatterns`, `collectCoverageFrom`, `coverageThreshold`, `coverageDirectory`, and ts-jest `tsconfig` references unchanged (spec AC2).

### Phase 2 — Inline Prohibited-Flag Guards (both run-jest.cjs entry points)

- [ ] [P2-T1] Edit `run-jest.cjs` (repo root): add an inline pre-spawn guard (no helper module) that scans `process.argv.slice(2)` for the exact arguments `--passWithNoTests`, `--onlyChanged`, and `--lastCommit`; on a hit, write a stderr message that names the rejected flag, states that zero discovered tests must fail, and cites `issue #423` (reference wording: `--passWithNoTests is prohibited in this repository: zero discovered tests must fail (issue #423).`, analogous for the other flags), then `process.exit(1)` before `runNodeTool(...)` is invoked. Preserve the existing `--testPathPattern` → `--testPathPatterns` rewrite and the existing exit-code propagation unchanged. Acceptance: the guard is inline in this file, executes before spawn, covers all three flags, and the rewrite/exit-propagation lines are unmodified (spec AC5, AC7, AC8).
- [ ] [P2-T2] Edit `extensions/drm-copilot/run-jest.cjs`: add the same inline pre-spawn guard for `--passWithNoTests`, `--onlyChanged`, and `--lastCommit` with the same stderr message content citing `issue #423`, exiting 1 before `cp.spawnSync(...)` is invoked. Preserve the existing `--testPathPattern` → `--testPathPatterns` rewrite, the `result.error` handling, and the `result.status ?? 1` exit-code propagation unchanged. Acceptance: the guard is inline in this file, executes before spawn, covers all three flags, and existing behavior for non-rejected arguments is unmodified (spec AC6, AC7, AC8).
- [ ] [P2-T3] Run `git diff fb483b84 -- run-jest.cjs extensions/drm-copilot/run-jest.cjs` and capture the full diff to `<FEATURE>/evidence/other/run-jest-diff.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: the diff shows only the inline guard additions — no new module files, no changes to the rewrite or exit-code propagation lines (spec AC8 review evidence).
- [ ] [P2-T4] From the repository root, run `node run-jest.cjs --passWithNoTests`, `node run-jest.cjs --onlyChanged`, and `node run-jest.cjs --lastCommit`, capturing for each the exit code and full stderr to `<FEATURE>/evidence/regression-testing/guard-root.<ts>.md` (`Timestamp:`, `Command:` per invocation, `EXIT_CODE:` per invocation, `Output Summary:`). Acceptance: all three invocations exit 1, each stderr message names the rejected flag and cites `issue #423`, and no Jest output (no test-run or no-tests diagnostic) appears, demonstrating Jest was not spawned (spec AC5, AC7).
- [ ] [P2-T5] From `extensions/drm-copilot/`, run `node run-jest.cjs --passWithNoTests`, `node run-jest.cjs --onlyChanged`, and `node run-jest.cjs --lastCommit`, capturing for each the exit code and full stderr to `<FEATURE>/evidence/regression-testing/guard-extension.<ts>.md` (`Timestamp:`, `Command:` per invocation, `EXIT_CODE:` per invocation, `Output Summary:`). Acceptance: all three invocations exit 1, each stderr message names the rejected flag and cites `issue #423`, and no Jest output appears (spec AC6, AC7).

### Phase 3 — Regression Tests and Pass-After Evidence

- [ ] [P3-T1] Create `tests/unit/jest-config-resolution.test.ts` as a Jest test using `@jest/globals` imports. Unit under test: `require`d config object from the root `jest.config.cjs` plus `globsToMatcher` from `jest-util`; all matcher inputs are synthetic path STRINGS only — no filesystem reads/writes other than the config `require`, no temporary files or directories, no materialised dot-prefixed checkout, no spawned processes. Implement spec assertion groups 1–6: (1) shape guard — every `testMatch` entry is a string, contains no `<rootDir>`, contains no `\`, starts with `**/`; (2) per-pattern positive match against synthetic dot-prefixed Windows paths — root pattern 1 via `C:\Users\x\repos\drm-copilot\.claude\worktrees\wt-1\tests\unit\sample.test.ts` and root pattern 2 via `C:\Users\x\repos\drm-copilot\.claude\worktrees\wt-1\extensions\drm-copilot\test\lib\sample.test.ts`, each asserted individually via `globsToMatcher([pattern])`; (3) per-pattern positive match for POSIX paths (`/home/runner/work/drm-copilot/drm-copilot/tests/unit/sample.test.ts` and analogous); (4) defect witness — hard-coded broken pattern `"C:/Users/x/repos/drm-copilot\\.claude/worktrees/wt-1/tests/unit/**/*.test.ts"` asserted NOT to match the corresponding group-2 path; (5) negative flow — matcher returns `false` for a non-test path such as `...\src\extension.ts`; (6) loudness config guard — `config.passWithNoTests` strictly falsy and `config.testPathIgnorePatterns` contains `"/node_modules/"` and `"/out/"`. Acceptance: the file exists at the stated path with all six assertion groups present and per-pattern individual assertions (spec AC9, AC11, AC12, AC13, AC14).
- [ ] [P3-T2] Create `extensions/drm-copilot/test/jest-config-resolution.test.ts` as a Jest test using `@jest/globals` imports, same constraints as P3-T1 (synthetic path strings only; no fs/temp/process usage beyond the config `require`). Implement assertion groups 1–6 for the extension config: shape guard; positive match of the single pattern via `globsToMatcher([pattern])` against `C:\Users\x\repos\drm-copilot\.claude\worktrees\wt-1\extensions\drm-copilot\test\sample.test.ts`; POSIX positive match against the analogous `/home/runner/...` extension path; defect witness using the analogous hard-coded broken `\.claude` pattern asserted NOT to match; negative flow against a `src/` production path; loudness config guard (`passWithNoTests` strictly falsy, `testPathIgnorePatterns` contains `"/node_modules/"` and `"/out/"`). Acceptance: the file exists at the stated path with all six assertion groups present (spec AC10, AC11, AC12, AC13, AC14).
- [ ] [P3-T3] Review both new test files against spec AC11–AC14 and record the checklist to `<FEATURE>/evidence/other/regression-test-review.<ts>.md`: per-pattern individual assertions present (AC11); defect-witness hard-coded `\.` pattern present in both files (AC12); `@jest/globals` imports, no filesystem access beyond config `require`, no temp files/directories, no dot-prefixed checkout materialisation (AC13); group-6 assertions present in both files (AC14). Include `Timestamp:` and cite file paths and line references. Acceptance: artifact exists with an explicit pass/fail entry for each of AC11–AC14, all pass.
- [ ] [P3-T4] Run `node run-jest.cjs` at the repository root with the fix applied and capture the full output to `<FEATURE>/evidence/regression-testing/pass-after-root-jest.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the discovered suite count). Acceptance: `EXIT_CODE: 0`, all tests pass, and the discovered suite count is non-zero and matches the on-disk inventory (expected at spec time: 171 test suites — 2 under `tests/unit/`, 169 under `extensions/drm-copilot/test/`; record the observed count) (spec AC4, AC9).
- [ ] [P3-T5] Run `npm --prefix extensions/drm-copilot run test` with the fix applied and capture the full output to `<FEATURE>/evidence/regression-testing/pass-after-extension-jest.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the discovered suite count). Acceptance: `EXIT_CODE: 0`, all tests pass, and the discovered suite count is non-zero (expected at spec time: 169 test suites; record the observed count) (spec AC4, AC10).
- [ ] [P3-T6] Run the in-process spot check from the repository root (research Q2 script, creates no files): `node -e` invoking `readConfig` from `jest-config` and `globsToMatcher` from `jest-util`, printing `projectConfig.testMatch` and the matcher result for this worktree's real `tests\unit\hello-typescript.test.ts` path. Capture the output to `<FEATURE>/evidence/regression-testing/spot-check-readconfig.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: `projectConfig.testMatch` equals `["**/tests/unit/**/*.test.ts", "**/extensions/drm-copilot/test/**/*.test.ts"]` and the matcher returns `true` for the real dot-prefixed worktree path.

### Phase 4 — Final QC Loop (both packages), Coverage, and Scope Verification

Loop rule for P4-T1 through P4-T9: these commands are unconditional; if any command fails or modifies files, fix the cause and restart the loop from P4-T1 until every command passes in a single clean pass. Each rerun overwrites nothing — append or re-timestamp artifacts.

- [ ] [P4-T1] Run `npm run format:check` at the repository root; capture to `<FEATURE>/evidence/qa-gates/final-root-format.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). If it reports unformatted files, run `npm run format` at the repository root, record that invocation and the subsequent `git status --porcelain` in the same artifact, confirm the write touched no forbidden file (the root prettier globs include `tsconfig*.json` and `.vscode-test.mjs`, which are byte-identical to base and must remain so), then restart the loop at P4-T1. Acceptance: `npm run format:check` exits 0 in the final clean pass (spec AC15).
- [ ] [P4-T2] Run `npm run lint` at the repository root; capture to `<FEATURE>/evidence/qa-gates/final-root-lint.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: exit 0 in the final clean pass (spec AC15).
- [ ] [P4-T3] Run `npm run typecheck` at the repository root; capture to `<FEATURE>/evidence/qa-gates/final-root-typecheck.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: exit 0 in the final clean pass (spec AC15).
- [ ] [P4-T4] Run `node run-jest.cjs` at the repository root; capture to `<FEATURE>/evidence/qa-gates/final-root-test.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including suite/test counts). Acceptance: exit 0 with all suites passing in the final clean pass (spec AC15).
- [ ] [P4-T5] Capture `git status --porcelain -- extensions/drm-copilot` **before** running the formatter, then run `npm --prefix extensions/drm-copilot run format`, then capture `git status --porcelain -- extensions/drm-copilot` again; record all three invocations to `<FEATURE>/evidence/qa-gates/final-extension-format.<ts>.md` (`Timestamp:`, `Command:` per invocation, `EXIT_CODE:` per invocation, `Output Summary:`). Acceptance: formatter exit 0 and the before/after `git status` captures are identical, i.e. the formatter modified no file. The expected in-scope entries (` M extensions/drm-copilot/jest.config.cjs`, ` M extensions/drm-copilot/run-jest.cjs`, `?? extensions/drm-copilot/test/jest-config-resolution.test.ts`) appear in both captures and are not a failure; a difference between the captures is a failure and requires restarting the loop at P4-T1 (spec AC16).
- [ ] [P4-T6] Run `npm --prefix extensions/drm-copilot run lint`; capture to `<FEATURE>/evidence/qa-gates/final-extension-lint.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: exit 0 in the final clean pass (spec AC16).
- [ ] [P4-T7] Run `npm --prefix extensions/drm-copilot run typecheck`; capture to `<FEATURE>/evidence/qa-gates/final-extension-typecheck.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`). Acceptance: exit 0 in the final clean pass (spec AC16).
- [ ] [P4-T8] Run `npm --prefix extensions/drm-copilot run test`; capture to `<FEATURE>/evidence/qa-gates/final-extension-test.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including suite/test counts). Acceptance: exit 0 with all suites passing in the final clean pass (spec AC16).
- [ ] [P4-T9] Run `npm --prefix extensions/drm-copilot run test:coverage`; capture to `<FEATURE>/evidence/qa-gates/final-extension-coverage.<ts>.md` (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` with the numeric coverage headline values from the text-summary reporter — statements, branches, functions, lines percentages). Acceptance: exit 0, which confirms every configured per-file `coverageThreshold` entry passed (Jest exits non-zero on any threshold regression), with numeric values recorded.
- [ ] [P4-T10] Write the coverage delta/threshold verification artifact `<FEATURE>/evidence/qa-gates/coverage-delta.<ts>.md` containing: (a) baseline coverage — reference `<FEATURE>/evidence/baseline/baseline-extension-coverage.<ts>.md` and its `WhyNumericBaselineUnavailable:` statement (zero test discovery pre-fix); (b) post-change numeric coverage values copied from the P4-T9 artifact; (c) new/changed-code coverage statement — the six changed files are Jest config/entry-point scaffolding and test files outside `collectCoverageFrom` (`src/**`), so no new per-file threshold obligation exists and no production coverage denominator changed; (d) verdict that no per-file threshold regressed (P4-T9 exit 0). Include `Timestamp:`. Acceptance: artifact exists with sections (a)–(d) populated and a PASS verdict; if any numeric value is unavailable, the verdict is remediation-required, not PASS.
- [ ] [P4-T11] From the repository root, run `git diff --name-only fb483b84` and `git status --porcelain --untracked-files=all`, and capture both outputs to `<FEATURE>/evidence/qa-gates/scope-check.<ts>.md` (`Timestamp:`, `Command:` per invocation, `EXIT_CODE:` per invocation, `Output Summary:` stating the combined changed-file inventory). The two commands together enumerate committed changes since base, uncommitted tracked modifications, and untracked additions; `HEAD`-only diffs are not sufficient because this plan performs no commit. Acceptance: the combined inventory contains no entry matching root `package.json`, `tsconfig*.json`, `.vscode-test.*`, `.claude/rules/**`, `.agents/skills/**`, or `extensions/drm-copilot/resources/claude-customizations/**`; it contains all six in-scope files (four modified, two untracked additions); every remaining entry is under `<FEATURE>/**`. Gitignored build/coverage output (for example `extensions/drm-copilot/coverage/`) does not appear and needs no exception (spec AC17).
- [ ] [P4-T12] Write the single-clean-pass loop summary `<FEATURE>/evidence/qa-gates/final-loop-summary.<ts>.md` recording, for the final pass of P4-T1 through P4-T9: each command, its artifact path, its exit code, and the number of loop restarts performed with the restart reasons (0 if none). Include `Timestamp:`. Acceptance: artifact exists showing all nine commands at exit 0 within one uninterrupted pass (spec AC15, AC16).
- [ ] [P4-T13] Reconcile all 17 acceptance criteria in `<FEATURE>/spec.md` against the evidence artifacts: write `<FEATURE>/evidence/other/ac-verification.<ts>.md` mapping each AC (AC1–AC17) to its verifying artifact path(s) and verdict, then check the corresponding AC checkboxes in `<FEATURE>/spec.md` for every AC whose evidence exists and passes. Include `Timestamp:`. Acceptance: the mapping artifact exists with all 17 ACs at PASS and the spec checkboxes updated; any non-PASS AC leaves its checkbox unchecked and the plan outcome is remediation-required.
