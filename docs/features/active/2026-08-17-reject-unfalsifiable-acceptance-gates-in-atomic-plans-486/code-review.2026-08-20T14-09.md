# Code Review: reject-unfalsifiable-acceptance-gates-in-atomic-plans (Issue #486)

**Review Date:** 2026-08-20 (artifact timestamp 2026-08-20T14-09, host clock; see clock-variance note in the sibling policy audit)
**Branch:** `feature/reject-unfalsifiable-acceptance-gates-in-atomic-plans-486` @ `cdf85294e713d08185aecf68f8869bed2975a723` vs `main` (merge-base `71aebdb9`)
**Scope:** full branch diff, 67 files (+7652/-21)

## Executive Summary

The change adds acceptance-gate rules G1–G6 to the plan validator in both runtimes with a two-channel blocking/warnings report, a repository seam with graceful degradation, byte-identical cross-runtime finding strings enforced by a shared parity fixture set, and a new rule file plus authoring guidance. Code quality is high: modules are small, pure logic is separated from the git seam, severities are constants rather than parsed text, typing is complete in both runtimes, and the two-channel design cleanly preserves byte identity of the pre-change failure output. The full toolchain (format, lint, type-check, tests, coverage) passes in a single pass in both languages, re-run in this review session.

Two Blocking findings, both already disclosed by the executor's evidence rather than discovered by this review: a changed-line coverage regression in `validate-orchestration-service-call.ts` (the combined error-plus-warning message path is untested), and the spec AC7 text defect that leaves AC7, two Definition-of-Done items, and plan tasks [P12-T13]/[P12-T14] unreconciled. The second is a documentation defect, not a code defect; the shipped `G5_SEVERITY = "warning"` is the correct outcome under the approved plan's pre-declared rule and the spec's own rationale. Two Minor findings and two Info observations follow.

No silent fixes were made during this review.

## Findings Table

| Severity | File | Location | Finding | Recommendation | Rationale | Evidence |
|----------|------|----------|---------|----------------|-----------|----------|
| Blocking | extensions/drm-copilot/src/lib/validate/validate-orchestration-service-call.ts | lines 117-118 | Added lines building the `warningBlock` `.map()` body are uncovered; file coverage regressed 100.00→98.51 line, 84.61→81.25 branch. The combined path (errors.length > 0 AND warnings.length > 0 in one call) has no test; the three tests in `validate-orchestration-service-call-plan-gates.test.ts` cover warning-only, warning-surfacing, and no-warning paths. | Add one named test driving a stub validation that returns >= 1 error and >= 1 warning; assert the thrown message ends with the `PLAN GATE WARNING: `-prefixed block appended after the error list. Restores 100% line and >= 84.61% branch on the file. | Uniform gate "No regression on changed lines" (`.claude/rules/quality-tiers.md`); modified-file coverage rule in the feature-review procedure. The untested path is also the exact behavior the code comment promises ("appended after the error block"). | `coverage/lcov.info` regenerated this session: `DA:117,0` and `DA:118,0`; executor's `evidence/qa-gates/coverage-delta.2026-08-20T13-36.md` records the same FAIL. |
| Blocking | docs/features/active/2026-08-17-reject-unfalsifiable-acceptance-gates-in-atomic-plans-486/spec.md | AC7 (line 192), pre-declared rule (line 64), DoD items 1-2 (lines 176-177) | Spec AC7 states a one-conjunct biconditional ("Blocking iff FP count == 0") that does not handle the zero-finding measurement actually obtained (0 findings over 166 plans / 100 candidates). Shipped severity is Warning per the approved plan's two-conjunct rule ([P5-T3]), which pre-declared and terminally disposed this exact branch. AC7 is FAIL as literally written; AC7, DoD 1-2, [P12-T13], [P12-T14] are all unchecked in consequence. | Amend spec.md AC7, the line-64 rule paragraph, and DoD item 2 to the two-conjunct form already recorded in `.claude/rules/plan-acceptance-gates.md`; record the deviation; then check off AC7, DoD 1-2, and the two plan tasks. No code change. | The defect is in the spec text: a vacuous measurement (0 FP over 0 findings) measures nothing, and shipping Blocking on it would contradict the spec's own stated rationale (line 62) and Constraints section (line 147). Implementation, plan, rule file, and measurement artifact are mutually consistent; only the spec sentence is not. | `evidence/qa-gates/g5-corpus-measurement.2026-08-20T12-02.md`; `G5_SEVERITY` = warning in `plan_gate_discrimination.py:53` and `plan-gate-discrimination.ts:69`; plan [P5-T3] task text; `.claude/rules/plan-acceptance-gates.md` "G5 — fixed by the corpus measurement". |
| Minor | scripts/dev_tools/validate_orchestration_artifacts.py | line 359 | The added defensive short-circuit `if args.artifact_type == "plan": return _plan_channels(args)[0]` inside `_validate_from_args` is uncovered. It is unreachable through `main()` (which routes plan through `_validate_from_args_with_warnings` first) and guards only direct callers of `_validate_from_args`. | Add a direct unit test calling `_validate_from_args` with a plan namespace, or accept with a comment; fold into the remediation cycle as a one-test item. | New line with zero coverage; file-level coverage improved on both axes so this is not a gate failure, but the branch is silent dead weight until a caller exists. | pytest `term-missing` this session: `Missing: 72, 359, 406, 408, 410, 433->437`; baseline miss set was `66, 117-121, 132, 147, 314, 316, 318` — 359 is the only new-line miss. |
| Minor | tests (both runtimes) | integration scenario coverage | No single synthetic plan carries all three confirmed failure modes (G1 `.py`-path coverage, G5 interpolated literal, G6 wrapped phrase) producing three distinct findings at their specified severities in one end-to-end run. Each mode is covered individually; the parity fixtures are one-command-per-plan. | Add one integration test per runtime with a three-task synthetic plan asserting one G1 Blocking plus G5 and G6 Warnings in a single evaluation; then check off spec seeded-condition 2. | Spec `## Seeded Test Conditions` item 2 states the combined scenario explicitly; a combined run also exercises channel ordering across rules, which no current test does. | `test_plan_gate_parity.py` `_parity_plan()` (single-task fixtures); grep over the new test modules found no multi-mode plan fixture. |
| Info | extensions/drm-copilot/src/lib/validate/plan-gate-rules.ts | whole file | Third TypeScript module; the spec's "Scope of change" prose says two new modules per runtime. The split is authorized by plan [P8-T14] (500-line ceiling contingency) with the per-file jest threshold added per [P9-T12] and the `G5_SEVERITY` declaration relocated per [P10-T5]. | None required; deviation is documented here and in the policy audit. | Plan-authorized contingency, not an unplanned deviation; file is under the ceiling (437 lines) and behind the same coverage gate. | plan [P8-T14]/[P9-T12] task text; `jest.config.cjs` diff. |
| Info | docs/features/active/.../plan.2026-08-17T15-00.md | [P2-T2] acceptance text | Self-validation of the feature's own plan emits two Warnings (G4 space-separated `--cov tests.foo`, G3 unresolvable value) against the fixture command the plan itself quotes; exit stays 0. | None; this is the designed warning behavior and a live demonstration that Warnings do not fail the gate. | Expected self-referential detection; the fixture is a deliberate negative example. | CLI run this session: `PLAN GATE WARNING: [P2-T2] ...` twice, exit 0. |

## Typed-Python Review

Both new Python modules are fully typed: frozen dataclasses (`PlanGateCommand`, `PlanGateReport`), explicit `list[str]` channels, a structural context type for the repository seam, and `tuple[list[str], list[str]]` on the new two-channel entry point. Pyright passes with zero errors and the diff introduces no suppression comments. The graceful-degradation catch is intentionally broad at the seam boundary and is a specified rule-set behavior (findings are dropped rather than exceptions escaping the validator), documented in the spec and the rule file rather than a silent-ignore violation.

## Cross-Runtime Parity Review

The eight shared parity fixtures (including apostrophe-bearing `--cov` value and search literal) assert identical expected strings in both runtimes; `test_g5_severity_constant_matches_typescript` reads the TypeScript constant from source and compares it to the Python constant; companion tests assert the absence of `repr(`/`!r` in the Python gate module and of `pythonRepr(` in the three TypeScript gate modules. This closes the known quote-selection divergence class recorded in `.claude/rules/parallel-orchestration.md` for the older validators. Verified by running both suites this session.

## Review Constraints Honored

- No code, test, or policy files were modified by this review. The only repository writes were the four review artifacts and the two spec seeded-condition check-offs authorized by `acceptance-criteria-tracking` (evidence-backed PASS items).
