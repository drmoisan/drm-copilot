# core-pack-manifest-missing-epic-orchestrate-files (Plan)

- **Issue:** #279
- **Parent (optional):** none (references issue #275, which added the six missing files)
- **Owner:** drmoisan
- **Last Updated:** 2026-07-03T14-30
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** minor-audit (bug)
- **Requirements source:** `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/issue.md` (sole source; `## Acceptance Criteria` AC1-AC5, added by this planning pass). `spec.md` and `user-story.md` intentionally do not exist for this minor-audit folder and are not required.
- **Evidence root:** `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/<kind>/`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QC artifact tasks, and coverage-comparison tasks for the in-scope language (TypeScript) where policy requires coverage. If any required baseline artifact, QC artifact, or coverage-comparison artifact is missing or has incomplete fields, the verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path in each evidence-producing task. Do not mark evidence-backed work complete without the artifact present and field-complete (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`).

**Toolchain note (verified against `extensions/drm-copilot/package.json`):** This package uses Jest (via `node run-jest.cjs`), not Vitest, and has no `test:coverage` script. The actual commands, run from `extensions/drm-copilot/`, are:
- format: `npm run format`
- lint: `npm run lint`
- type-check: `npm run typecheck`
- test (coverage): `npm test -- --coverage` (forwards `--coverage` to Jest; `coverageProvider: v8` is set in `jest.config.cjs`)
Each task below records the exact command actually executed in its evidence artifact.

**Scope note:** One production JSON edit (`extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`) plus one new unit-test file. `packages/mcp-server/resources/claude-customizations/pack-manifests/core.json` must NOT be hand-edited: `packages/mcp-server/.gitignore` contains `resources/` and `packages/mcp-server/prepack.cjs` copies `extensions/drm-copilot/resources` into `packages/mcp-server/resources` via `cpSync` at pack time (verified by reading both files during planning), so the edit in Phase 1 is the only source-of-truth change needed.

**Verified bundle state (during planning):** All six files named in the issue exist on disk under `extensions/drm-copilot/resources/claude-customizations/.claude/` (`agents/epic-orchestrator.md`, `skills/epic-orchestrate/SKILL.md`, `hooks/enforce-epic-merge-gate.ps1`, `hooks/enforce-epic-wave-barrier.ps1`, `hooks/enforce-epic-worktree-removal-gate.ps1`, `hooks/enforce-pr-author-skill.epic-base-branch.ps1`) and none of the six appear in the current `pack-manifests/core.json` `paths` array.

---

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read the policy files in the required order and record them in a single evidence artifact at `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/baseline/phase0-instructions-read.md`. The artifact must contain `Timestamp:`, `Policy Order:`, and an explicit list of files read, covering: `CLAUDE.md`, `.github/copilot-instructions.md`, `.github/instructions/general-code-change.instructions.md`, `.github/instructions/general-unit-test.instructions.md`, `.github/instructions/typescript-code-change.instructions.md`, `.github/instructions/typescript-unit-test.instructions.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, and `.claude/rules/typescript-suppressions.md`. Acceptance: artifact exists and lists every file above.
- [x] [P0-T2] Capture baseline formatting state. Run `npm run format` from `extensions/drm-copilot/` (this repo's `format` script runs `prettier --write`, not `--check`; if a no-op check is required instead, run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` from `extensions/drm-copilot/` and record whichever command was actually run). Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/baseline/baseline-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T3] Capture baseline lint state. Run `npm run lint` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/baseline/baseline-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T4] Capture baseline type-check state. Run `npm run typecheck` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/baseline/baseline-typecheck.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields populated.
- [x] [P0-T5] Capture baseline test + coverage state. Run `npm test -- --coverage` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/baseline/baseline-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that includes the numeric baseline coverage headline (overall line % and branch %) and the pass/fail test count. Acceptance: artifact exists with all four fields populated and numeric line and branch coverage values recorded (no placeholders).

### Phase 1 — Constrained Small-Path Implementation (handoff)

- [x] [P1-T1] Edit `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` to add the six missing paths, preserving the file's existing alphabetical/grouped ordering (agents block, hooks block, skills block) and valid JSON. Specifically: insert `.claude/agents/epic-orchestrator.md` immediately before `.claude/agents/epic-review.md`; insert `.claude/hooks/enforce-epic-merge-gate.ps1`, then `.claude/hooks/enforce-epic-wave-barrier.ps1`, then `.claude/hooks/enforce-epic-worktree-removal-gate.ps1` (in that order) immediately before `.claude/hooks/enforce-evidence-locations.ps1`; insert `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1` immediately before `.claude/hooks/enforce-pr-author-skill.ps1`; insert `.claude/skills/epic-orchestrate/SKILL.md` immediately before `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Do not touch `packages/mcp-server/resources/claude-customizations/pack-manifests/core.json` (gitignored/regenerated, per the Scope note above). Acceptance: `core.json` parses as valid JSON, contains all six paths in the positions specified, and no existing path was removed or reordered outside the six insertions.
- [x] [P1-T2] Add a new test file at `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts` (Jest, `@jest/globals`) that reads the real filesystem (via `node:fs`/`node:path`, resolved from `__dirname`, not the in-memory fake used by sibling tests in this directory) to: (a) enumerate every `extensions/drm-copilot/resources/claude-customizations/.claude/agents/*.md`, `.claude/skills/*/SKILL.md`, and `.claude/hooks/*.ps1` file on disk as a `.claude`-relative path; (b) parse every `extensions/drm-copilot/resources/claude-customizations/pack-manifests/*.json` file and union their `paths` arrays; (c) assert every enumerated path from (a) is present in the union from (b), reporting any missing paths in the assertion failure message. Acceptance: the test file exists, does not use `buildInMemoryFileSystem` or any hardcoded expected-file list, and passes against the post-fix `core.json` from P1-T1.
- [x] [P1-T3] Add explicit assertions in `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts` (from P1-T2) that directly check each of the six paths named in issue AC1 (`.claude/agents/epic-orchestrator.md`, `.claude/skills/epic-orchestrate/SKILL.md`, `.claude/hooks/enforce-epic-merge-gate.ps1`, `.claude/hooks/enforce-epic-wave-barrier.ps1`, `.claude/hooks/enforce-epic-worktree-removal-gate.ps1`, `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`) is present in the union computed in P1-T2, in addition to the generic completeness assertion. Acceptance: the test file contains an explicit assertion naming each of the six paths, and all six assertions pass.
- [x] [P1-T4] [expect-fail] Confirm the regression value of the new test: temporarily revert the six insertions from P1-T1 in a local diff (do not commit), re-run `npm test -- --testPathPatterns claude-pack-manifest-completeness` from `extensions/drm-copilot/`, and record the failing output (listing the six paths as missing) at `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/regression-testing/fail-before.2026-07-03T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Then restore the P1-T1 edit before proceeding. Acceptance: the fail-before artifact exists, records a non-zero `EXIT_CODE`, and its `Output Summary:` names at least one of the six missing paths; the P1-T1 edit is restored afterward.
- [x] [P1-T5] Record the AC-to-task mapping for issue AC1-AC5 at `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/other/implementation-ac-map.md` with `Timestamp:`, mapping: AC1 -> P1-T1; AC2 -> Scope note (no task; verified during planning, re-confirm no diff touches `packages/mcp-server/resources/`); AC3 -> P1-T2; AC4 -> P1-T3 and P1-T4; AC5 -> Phase 2. Acceptance: artifact exists and maps each of AC1-AC5 to its implementing task(s) or phase.

### Phase 2 — Final QC Loop

- [x] [P2-T1] Run formatting. Execute `npm run format` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/qa-gates/final-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this step changes files, restart the loop from this task after re-running. Acceptance: artifact exists with all four fields; formatting reports no remaining changes.
- [x] [P2-T2] Run linting. Execute `npm run lint` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/qa-gates/final-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0` and zero lint errors.
- [x] [P2-T3] Run type checking. Execute `npm run typecheck` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/qa-gates/final-typecheck.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0` and zero type errors.
- [x] [P2-T4] Run tests with coverage. Execute `npm test -- --coverage` from `extensions/drm-copilot/`. Write the result to `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/qa-gates/final-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records numeric post-change overall line % and branch % and the pass/fail test count, and explicitly confirms `claude-pack-manifest-completeness.test.ts` passed. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0`; numeric post-change line and branch coverage recorded (no placeholders).
- [x] [P2-T5] Verify coverage thresholds and no changed-line regression. Compare the baseline coverage from `evidence/baseline/baseline-test-coverage.md` (P0-T5) against the post-change coverage from `evidence/qa-gates/final-test-coverage.md` (P2-T4). Write `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/evidence/qa-gates/coverage-comparison.md` with `Timestamp:`, baseline line %/branch %, post-change line %/branch %, and an explicit statement that overall line >= 85% and branch >= 75% and that no changed line lost coverage. Acceptance: artifact exists; thresholds met; no changed-line coverage regression. If any required value is unavailable, mark the outcome remediation-required (not PASS).
- [x] [P2-T6] Check off satisfied acceptance criteria in `docs/features/active/2026-07-03-core-pack-manifest-missing-epic-orchestrate-files-279/issue.md` (`## Acceptance Criteria` AC1-AC5), changing `- [ ]` to `- [x]` only for criteria whose supporting evidence artifact exists and passed, per `acceptance-criteria-tracking`. Acceptance: each AC item's checkbox state matches its verified evidence; no AC is checked without a corresponding passing artifact.
