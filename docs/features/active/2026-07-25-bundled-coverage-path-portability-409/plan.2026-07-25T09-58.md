# 2026-07-25-bundled-coverage-path-portability (Plan)

- **Issue:** #409
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-25T12-05
- **Status:** Ready for Preflight (revision 1 — executor preflight deltas applied)
- **Version:** 1.1
- **Work Mode:** full-bug (AC source: `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md` only; `user-story.md` is intentionally absent and is not a blocker)
- **Branch:** `bug/bundled-coverage-path-portability-409`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Timestamp convention:** `<ts>` in artifact filenames denotes the ISO-8601 run timestamp `yyyy-MM-ddTHH-mm` captured at execution time, per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`.

**Evidence root (non-overridable):** every evidence artifact in this plan resolves under `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/<kind>/`. `artifacts/pester/` is referenced only as a permitted tool-output location; copies placed in the evidence tree are the evidence artifacts.

**Approved production surface (do not widen):** exactly two byte-identical files — `scripts/powershell/PoshQC/PoshQC.Testing.psm1` and `extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1`. One new test file. Out of scope: the latent `Run.Path` portability risk (spec SD1), any change to `pester.runsettings.psd1` (either copy), any version bump, any publish action.

**No-SKIPPED rule:** every command-bearing task below is unconditional. `EXIT_CODE: SKIPPED` is not a valid outcome for any task in this plan.

### Phase 0 — Policy Compliance and Baseline Capture

- [x] [P0-T1] Read the policy files in the required order: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/powershell.md`, (5) `.claude/rules/python.md`, (6) `.claude/rules/python-suppressions.md`. Record the read in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of the six files read. Acceptance: the artifact exists with all three fields and all six file paths listed in order.
- [x] [P0-T2] Capture the PowerShell format baseline: run MCP tool `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-poshqc-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `Output Summary:` states whether any files were reformatted.
- [x] [P0-T3] Capture the PowerShell lint baseline: run MCP tool `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-poshqc-analyze.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (diagnostic count). Acceptance: artifact exists with all four fields.
- [x] [P0-T4] Capture the PowerShell test-and-coverage baseline: run MCP tool `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-poshqc-test.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; `Output Summary:` MUST include the numeric baseline line-coverage percent from the replayed coverage summary and note that branch coverage is not separately measurable in the PowerShell toolchain (documented limitation, see spec.md Test Strategy). Acceptance: artifact exists with all four fields and a numeric coverage value.
- [x] [P0-T5] Preserve the baseline coverage artifact for the invariance proof (spec AC 4), sourced from a **direct repo-root module run** rather than the MCP tool: the configured MCP server is `npx -y @danmoisan/drm-copilot-mcp` (`.mcp.json`), whose npx-cached 1.0.18 bundle contains its own pre-fix copy of `PoshQC.Testing.psm1` that will not change when this branch edits the repository, so an MCP-sourced post-change artifact would not exercise the fix and the AC-4 comparison would be confounded by the harness. Run `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest"` from the repository root (omit `-Root` so it defaults to the absolute `$PWD.ProviderPath`), then copy the tool output `artifacts/pester/powershell-coverage.xml` to `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/powershell-coverage.baseline.xml` and extract the distinct per-file entry set (sorted `filename`/`sourcefile` values) into `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/coverage-file-set.baseline.<ts>.md` with `Timestamp:`, `Command:` (both the direct run and the extraction), `EXIT_CODE:`, `Output Summary:` (file count, and an explicit statement that the run log contains zero prune messages because the pre-fix module has no pruning code). Command for the copy and extraction: one `pwsh -NoLogo -NoProfile -Command` invocation performing `Copy-Item` plus `Select-Xml`/regex extraction of distinct file entries. Acceptance: both files exist, the entry-set artifact lists the measured files with a count, and the artifact records that the coverage XML came from the repo-root module, not the MCP tool.
- [x] [P0-T6] Capture the Python format baseline: run `poetry run black --check .` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-python-black.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T7] Capture the Python lint baseline: run `poetry run ruff check .` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-python-ruff.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Capture the Python type-check baseline: run `poetry run pyright` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-python-pyright.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error count). Acceptance: artifact exists with all four fields.
- [x] [P0-T9] Capture the Python test-and-coverage baseline: run `poetry run pytest --cov --cov-branch --cov-report=term-missing` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/baseline-python-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; `Output Summary:` MUST include the numeric baseline line-coverage and branch-coverage percentages and the pass/fail test counts. Acceptance: artifact exists with all four fields and both numeric coverage values.

### Phase 1 — Regression Test Authoring and Fail-Before Evidence (spec SD3)

- [x] [P1-T1] Create the new test file `tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1` (new file, under 500 lines) containing seam-injected tests for the fixed behavior of `Invoke-PoshQCTest` covering the four required scenarios from spec.md Test Strategy: (1) all configured coverage paths exist → `$InvokePester` receives the full resolved set and no prune log lines are emitted; (2) mixed set → only existing paths survive and each pruned path is logged with its resolved value via `$Logger`; (3) no path exists → `CodeCoverage.Enabled` is false at the `$InvokePester` boundary, the run proceeds (summary replay still occurs), the disable message is logged exactly once, and `$CopyCoverage` is not invoked; (4) rooted absolute entry → kept or pruned by the same `$TestPathExists` predicate without re-joining to `-Root`. Requirements: use the module-collision-guard `BeforeAll` pattern from `tests/scripts/powershell/PoshQC/PoshQC.TestingInvokeConfigPaths.Tests.ps1` (lines 6-20); fake `$LoadSettings`, `$BuildConfiguration`, `$EnsureModule`, `$ResolveScanConfig`, `$EnumerateTests` per the existing pattern; capture `$Logger` messages in a list; capture the final config at the injected `$InvokePester`; the injected `$TestPathExists` predicate MUST be path-discriminating, not a blanket `{ $false }`: it must return `$true` for the `-SettingsPath` value (the same seam gates the settings-existence check at `PoshQC.Testing.psm1:296` and a blanket false throws `Settings not found:` before the coverage block is reached) and return the scenario-specific verdict only for the resolved coverage-path values, for example `-TestPathExists { param([string] $Path) if ($Path -eq '/coverage-settings.psd1') { $true } else { $Path -like '*present*' } }`; override `-ExpandCoveragePaths` with a pass-through (`{ param($Config, [string] $RootPath) [void] $RootPath; $Config }`) and place each scenario that enters the coverage-enabled block inside `InModuleScope PoshQC` with `Mock -CommandName New-Item -MockWith { }`, following `tests/scripts/powershell/PoshQC/PoshQC.TestingInvokeConfigPaths.Tests.ps1` lines 115-157; both the default `$ExpandCoveragePaths` seam (`PoshQC.Testing.psm1:229-231`) and the inline coverage block (`349-355`) call `New-Item -ItemType Directory` whenever `CodeCoverage.OutputPath` is set, and scenario 3 MUST set a non-null `CodeCoverage.OutputPath` so that its `$CopyCoverage`-not-invoked assertion is caused by `$coverageEnabled = $false` rather than by a null `$coverageOutputPath` at the line-383 gate; no temp files, no filesystem access, no live Pester subprocess, no `Thread.Sleep`/timing waits. Acceptance: the file exists at the stated path with all four scenarios and no filesystem or temp-file usage.
- [x] [P1-T2] [expect-fail] Run the new test file against the pre-fix production code: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1 -Output Detailed"`. Expected result: the mixed-set, empty-set, and rooted-absolute scenarios FAIL because pre-fix `Invoke-PoshQCTest` forwards nonexistent coverage paths (as reported absent by the injected `$TestPathExists` seam) to the injected `$InvokePester` seam; the all-exist pass-through scenario passes. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/regression-testing/fail-before.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` naming each failing scenario and quoting the assertion output that shows nonexistent paths were forwarded. Acceptance: artifact exists with a non-zero `EXIT_CODE:` and the named failing scenarios.

### Phase 2 — Mirrored Production Fix and Parity

- [x] [P2-T1] Edit the coverage-enabled block of `scripts/powershell/PoshQC/PoshQC.Testing.psm1` (currently lines 338-347), immediately after `$resolvedCoveragePaths` is computed and assigned: (1) filter the resolved set through the existing injectable `$TestPathExists` seam (declared at line 167), evaluating rooted absolute entries as-is without re-joining to `-Root`; (2) log each pruned path individually, with its resolved value, through the existing `$Logger` seam; (3) when the surviving set is empty, set `$config.CodeCoverage.Enabled = $false` and `$coverageEnabled = $false`, log a single explanation that code coverage is disabled for this invocation because no configured coverage path exists under the resolved root, and proceed with the run (spec SD2); (4) assign the surviving set to `$config.CodeCoverage.Path` when non-empty. No parameter-signature changes; no changes outside `Invoke-PoshQCTest`; no changes to `settings/pester.runsettings.psd1`. Acceptance: the edit is confined to the coverage-enabled block of `Invoke-PoshQCTest` and, when every path exists, the set handed to `$InvokePester` is unchanged from today.
- [x] [P2-T2] Verify the file-size cap: run `pwsh -NoLogo -NoProfile -Command "(Get-Content scripts/powershell/PoshQC/PoshQC.Testing.psm1).Count"` and confirm the count is <= 500 (pre-change count 443; the pruning block adds approximately 15-25 lines). Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/other/file-size-check.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the line count). Explicit contingency: if the count exceeds 500, do NOT exceed the cap and do NOT widen the approved two-file production surface — instead re-author the pruning block more compactly within `Invoke-PoshQCTest` (the measured budget is 443 pre-change lines plus roughly 15-25 added lines, so the cap is reachable without extraction). If the cap still cannot be met, extract the pruning logic into a helper in an appropriate existing PoshQC module (or a new mirrored module pair if no existing module fits), mirror it under P2-T3, record the production-surface deviation in this artifact under a `SURFACE_DEVIATION:` heading, continue executing the remainder of the plan, and escalate the deviation in the completion report. Do not halt mid-plan. Acceptance: artifact exists recording a count <= 500, or the `SURFACE_DEVIATION:` record with plan execution continuing.
- [x] [P2-T3] Mirror the fix byte-identically: run `pwsh -NoLogo -NoProfile -Command "Copy-Item scripts/powershell/PoshQC/PoshQC.Testing.psm1 extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1 -Force; (Get-FileHash scripts/powershell/PoshQC/PoshQC.Testing.psm1).Hash; (Get-FileHash extensions/drm-copilot/resources/powershell/PoshQC/PoshQC.Testing.psm1).Hash"` and confirm both hashes are equal. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/other/mirror-hash.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (both hash values). Acceptance: artifact records two identical hashes.
- [x] [P2-T4] Run the bundled-parity test: `poetry run pytest tests/scripts/dev_tools/test_poshqc_bundled_parity.py`. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/parity-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: `EXIT_CODE: 0` and all parity assertions pass.
- [x] [P2-T5] Check off spec acceptance criterion 5 (bundled mirror byte-identical and parity test passes) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`: change only `- [ ]` to `- [x]` for that criterion, per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, citing the P2-T3 and P2-T4 artifacts. Acceptance: exactly that one checkbox is checked and the criterion text is unmodified.

### Phase 3 — Regression Pass-After and Consumer-Scenario Verification

- [x] [P3-T1] Run the new test file against the fixed code: `pwsh -NoLogo -NoProfile -Command "Invoke-Pester -Path tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1 -Output Detailed"`. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/regression-testing/pass-after.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (all four scenario results). Acceptance: `EXIT_CODE: 0` and all four scenarios pass.
- [x] [P3-T2] Verify the consumer-repository scenario (spec AC 7) using the research reproduction protocol: from the repository root run `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -File extensions/drm-copilot/resources/templates/run-poshqc-test.ps1 -WorkspaceRoot tests -ScanFoldersJson '["scripts/powershell/PoshQC"]'`. The workspace root `tests/` contains Pester tests (scan folder resolves to `tests/scripts/powershell/PoshQC`) but none of the configured coverage paths exist under it. Assert: the run proceeds past Pester RunStart and completes test execution instead of aborting; every configured coverage path is logged as pruned and/or the single coverage-disable message appears; the process exit reflects the test results, not a RunStart abort. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/regression-testing/consumer-scenario.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (RunStart passed, prune/disable log lines observed, test counts). Explicit note (research Reproduction Protocol): this invocation unconditionally creates tool output directories under `tests/artifacts/` before coverage handling (`PoshQC.Testing.psm1` output-directory creation); this is repository-tree tool-output churn, not a test-created temp file under `.claude/rules/general-unit-test.md`, and a fully side-effect-free faithful reproduction is not achievable because directory creation precedes the previously-aborting code path; cleanup is P3-T3. Acceptance: artifact records run completion past RunStart with the logged prune/disable evidence.
- [x] [P3-T3] Clean up the consumer-scenario tool output: run `pwsh -NoLogo -NoProfile -Command "Remove-Item -Recurse -Force tests/artifacts -ErrorAction Stop; git status --porcelain tests/"` from the repository root and confirm no untracked residue remains under `tests/` other than the intended new test file `tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1`. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/other/consumer-scenario-cleanup.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (git status output). Acceptance: artifact shows `tests/artifacts/` removed and no unexpected residue.
- [x] [P3-T4] Check off spec acceptance criterion 1 (nonexistent paths pruned via `$TestPathExists`; existing paths pass through unchanged) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, citing scenarios 1, 2, and 4 in the P3-T1 artifact. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P3-T5] Check off spec acceptance criterion 2 (each pruned path logged individually via `$Logger`; pruning never silent) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, citing the scenario-2 log assertions in the P3-T1 artifact. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P3-T6] Check off spec acceptance criterion 3 (empty surviving set → coverage disabled at the `$InvokePester` boundary with a logged explanation; run proceeds; never enabled-but-empty) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, citing the scenario-3 assertions in the P3-T1 artifact. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P3-T7] Check off spec acceptance criterion 6 (deterministic seam-injected unit tests for the four scenarios, no temp files, fail-before evidence captured) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, after verifying both `evidence/regression-testing/fail-before.<ts>.md` (P1-T2) and `evidence/regression-testing/pass-after.<ts>.md` (P3-T1) exist with complete fields. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P3-T8] Check off spec acceptance criterion 7 (consumer-repository scenario completes instead of aborting at RunStart, output captured under evidence/regression-testing/) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, after verifying the P3-T2 artifact exists with complete fields. Acceptance: exactly that one checkbox is checked; text unmodified.

### Phase 4 — Final QA Loop, Coverage Invariance, and Closeout

- [x] [P4-T1] Final-QC PowerShell format: run MCP tool `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-poshqc-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If the tool fails or reformats any file, fix, then restart the PowerShell loop from P4-T1 and record the rerun in the same artifact series. Acceptance: final recorded pass has `EXIT_CODE: 0` with zero files changed.
- [x] [P4-T2] Final-QC PowerShell lint: run MCP tool `mcp__drm-copilot__run_poshqc_analyze` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-poshqc-analyze.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (diagnostic count). If it fails, fix and restart from P4-T1. Acceptance: final recorded pass has `EXIT_CODE: 0` and zero errors.
- [x] [P4-T3] Final-QC PowerShell test with coverage: run MCP tool `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-poshqc-test.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; `Output Summary:` MUST include the numeric post-change line-coverage percent, the test pass/fail counts, and an explicit statement that the new test file `tests/scripts/powershell/PoshQC/PoshQC.TestingCoveragePruning.Tests.ps1` was discovered and passed inside this run. Note: this stage runs the npx-cached published MCP bundle, not the edited repository module, so it is the mandated toolchain gate and the coverage denominator for the changed file, but it is not the AC-4 invariance harness (see P4-T4). If it fails, fix and restart from P4-T1. Acceptance: final recorded pass has `EXIT_CODE: 0`, a numeric coverage value, and the new pruning test file recorded as discovered and passing.
- [x] [P4-T4] Produce the authoritative post-change coverage artifact from a **direct repo-root module run** of the fixed code, matching the P0-T5 baseline harness exactly: run `pwsh -NoLogo -NoProfile -Command "Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest"` from the repository root (omit `-Root` so it defaults to the absolute `$PWD.ProviderPath`), then copy the tool output `artifacts/pester/powershell-coverage.xml` to `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/powershell-coverage.post-change.xml`. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/direct-module-post-change-run.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the numeric line-coverage percent, the test pass/fail counts, and the prune-message count observed in the run log (which MUST be zero: every configured coverage path exists in this repository). Acceptance: the copy exists in the evidence tree, the artifact records zero prune messages from the fixed module, and both this run and the P0-T5 baseline run used the identical direct-module invocation so the P4-T5 comparison isolates the code change.
- [x] [P4-T5] Coverage-invariance proof (spec AC 4): extract the distinct per-file entry set from `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/powershell-coverage.post-change.xml` and compare it against the baseline set in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/baseline/powershell-coverage.baseline.xml` (one `pwsh -NoLogo -NoProfile -Command` invocation using `Select-Xml`/regex extraction and `Compare-Object`). Assert: the per-file entry sets are identical; the post-change covered percent is equal to or better than baseline; the P4-T4 direct-module run log contains zero prune messages. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/coverage-file-set-delta.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (identical-set assertion result, both file counts, baseline vs post-change percent, prune-message count = 0 from the P4-T4 fixed-module run). Acceptance: artifact records an identical file set and no coverage reduction.
- [x] [P4-T6] Final-QC Python format: run `poetry run black .` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-python-black.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If any file is reformatted or the command fails, fix, then restart the Python loop from P4-T6. Acceptance: final recorded pass has `EXIT_CODE: 0` with zero files reformatted.
- [x] [P4-T7] Final-QC Python lint: run `poetry run ruff check .` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-python-ruff.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If it fails, fix and restart from P4-T6. Acceptance: final recorded pass has `EXIT_CODE: 0`.
- [x] [P4-T8] Final-QC Python type check: run `poetry run pyright` at the repository root. Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-python-pyright.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error count). If it fails, fix and restart from P4-T6. Acceptance: final recorded pass has `EXIT_CODE: 0` and zero errors.
- [x] [P4-T9] Final-QC Python test with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing` at the repository root (this includes `tests/scripts/dev_tools/test_poshqc_bundled_parity.py`). Write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/final-python-pytest.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; `Output Summary:` MUST include numeric post-change line-coverage and branch-coverage percentages and pass/fail counts. If it fails, fix and restart from P4-T6. Acceptance: final recorded pass has `EXIT_CODE: 0` with both numeric coverage values.
- [x] [P4-T10] Coverage delta/threshold verification (Coverage Evidence Contract): compare baseline (P0-T4, P0-T9) against final-QC (P4-T3, P4-T9) coverage and write `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/qa-gates/coverage-delta.<ts>.md` reporting, per language: baseline coverage, post-change coverage, and changed-line coverage for the new pruning lines in `scripts/powershell/PoshQC/PoshQC.Testing.psm1` (extracted from `evidence/qa-gates/powershell-coverage.post-change.xml`), with the threshold checks: line coverage >= 85% (both languages), branch coverage >= 75% (Python; PowerShell branch coverage not separately measurable — documented limitation per spec.md Test Strategy), and no regression on changed lines. Include `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If any required numeric value is unavailable, the outcome is remediation-required and MUST NOT be reported as PASS. Acceptance: artifact contains all numeric values and every threshold check passes.
- [x] [P4-T11] Check off spec acceptance criterion 4 (behavior unchanged when every configured path exists; per-file coverage entry set identical baseline vs post-change; zero prune messages) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, after verifying the P4-T5 artifact records an identical set and that both compared XMLs came from the direct repo-root module runs in P0-T5 and P4-T4. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P4-T12] Check off spec acceptance criterion 8 (full toolchain pass: `run_poshqc_format` → `run_poshqc_analyze` → `run_poshqc_test` plus the parity pytest, all clean in a single pass) in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, after verifying P4-T1 through P4-T3 and P4-T6 through P4-T9 all recorded `EXIT_CODE: 0` in a single uninterrupted pass. Acceptance: exactly that one checkbox is checked; text unmodified.
- [x] [P4-T13] Record the acceptance-criteria status summary required by `.claude/skills/acceptance-criteria-tracking/SKILL.md` in `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/evidence/other/ac-status-summary.<ts>.md`: source `docs/features/active/2026-07-25-bundled-coverage-path-portability-409/spec.md`, total AC items 8, checked-off count, remaining count, and the list of any unchecked criterion texts, plus `Timestamp:`. Acceptance: artifact exists and its counts match the checkbox state in `spec.md`.
