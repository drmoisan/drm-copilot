# Policy Compliance Audit: legacy-discovery-mcp-vscode (Issue #370)

---

**Audit Date:** 2026-07-19
**Code Under Test:** TypeScript under `extensions/drm-copilot/` — new production files `src/repo-automation-execute-discovery.ts`, `src/mcp-tool-inputs-discovery.ts`, `src/mcp-handlers/discovery-handlers.ts`, `src/mcp-discovery-tool-definitions.ts`, `src/discovery-command-registration.ts`, `src/repo-automation-service-contract.ts`; modified production files `src/runtime-detection.ts`, `src/repo-automation-service.ts`, `src/repo-automation-tool-names.ts`, `src/mcp-tools.ts`, `src/mcp-repo-automation-tool-definitions.ts`, `src/mcp-tool-definitions.ts`, `src/extension.ts`; config `jest.config.cjs` (new), `package.json` (modified); new/extended test files under `extensions/drm-copilot/test/` (10 files). Docs and evidence under `docs/features/active/2026-07-17-legacy-discovery-mcp-vscode-370/`.

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|----------|--------------|-------|-------------|-------------------|---------------------|-------------------|
| TypeScript | 15 files (13 src + 2 config) | 2006 tests (165 suites) | PASS 2006 pass, 0 fail | 96.74% lines, 89.28% branches | 96.30% lines, 89.22% branches | All new files >= 90.66% lines, >= 78.18% branches (see 1.2.1) |

Languages with zero changed files on this branch (Python, PowerShell, C#, Bash): N/A — no changed files, no coverage verdict required.

### Coverage Evidence Checklist

- TypeScript baseline coverage artifact: `docs/features/active/2026-07-17-legacy-discovery-mcp-vscode-370/evidence/baseline/baseline-test-coverage.2026-07-19T00-40.md`
- TypeScript post-change coverage artifact: `docs/features/active/2026-07-17-legacy-discovery-mcp-vscode-370/evidence/qa-gates/final-qc-test-coverage.2026-07-19T02-20.md`; lcov data at `extensions/drm-copilot/coverage/lcov.info` (regenerated by this reviewer, 2026-07-19T02-33)
- PowerShell baseline coverage artifact: `N/A - out of scope` (zero PowerShell files changed on branch)
- PowerShell post-change coverage artifact: `N/A - out of scope`
- Per-language comparison summary: Section 1.2.1 below; per-file delta table in `evidence/qa-gates/coverage-delta.2026-07-19T02-20.md`

**Non-negotiable verdict rule:** satisfied — numeric baseline and post-change coverage metrics for the single in-scope language (TypeScript) are recorded above and in 1.2.1, with per-new-file coverage.

**Fail-closed rule:** no required baseline, QA, or coverage-comparison artifact is missing.

**Evidence rule:** all numbers below come from executor evidence artifacts cross-verified by this reviewer's independent re-run (jest exit 0, identical totals: 165 suites / 2006 tests / 96.30% lines / 89.22% branches).

---

## Rejected Scope Narrowing

None. The caller's delegation prompt supplied the resolved base branch, merge-base SHA, and feature folder, and stated factually that changed code is entirely under `extensions/drm-copilot/` — a statement this reviewer independently confirmed against the full branch diff (`git diff --stat a6dd7d45..9525dd0a`). No instruction attempted to narrow the audit below the full feature-vs-base scope, mark any language "out of scope," or skip a toolchain or coverage check. The audit scope is the full branch diff `a6dd7d4591ef80f4d351cea4b0488ce08568286e..9525dd0a529ff833f13c0c0bec8076794492d16e`.

---

## Evidence Location Compliance

- Scan command: `python -m scripts.dev_tools.validate_evidence_locations --root .` → EXIT_CODE 0 (no violations).
- Branch-diff scan for forbidden evidence paths (`artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/`, `artifacts/post-change/`): `git diff --name-only a6dd7d45..HEAD | grep -E "^artifacts/(baselines|baseline|qa|qa-gates|evidence|coverage|regression-testing|post-change)/"` → no matches.
- All feature evidence resides under the canonical `docs/features/active/2026-07-17-legacy-discovery-mcp-vscode-370/evidence/{baseline,qa-gates,other}/`.
- No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` events: no caller instruction supplied a non-canonical evidence path.

Result: **PASS** — zero findings.

---

## Executive Summary

This audit reviews the full branch diff of `feature/legacy-discovery-mcp-vscode-370` (head `9525dd0a`) against base `epic/legacy-discovery-and-parity-integration` (merge-base `a6dd7d45`). The change exposes seven `dev.discovery.*` Python CLI operations as MCP tools and VS Code commands in the `drm-copilot` extension: 4,422 insertions / 303 deletions across 45 files, all production code in TypeScript.

**Policy documents evaluated:**
- [x] `general-code-change.instructions.md` (via `.claude/rules/general-code-change.md`)
- [x] `general-unit-test.instructions.md` (via `.claude/rules/general-unit-test.md`)

**Language-specific policies evaluated:**
- N/A `python-code-change.instructions.md` + `python-unit-test.instructions.md` (no Python files changed)
- N/A `powershell-code-change.instructions.md` + `powershell-unit-test.instructions.md` (no PowerShell files changed)
- [x] TypeScript: `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`
- N/A Bash, C#

All four toolchain stages were independently re-executed by this reviewer in check-only mode and passed: Prettier `--check` (clean), ESLint (0 findings), `tsc --noEmit` (0 errors), Jest coverage suite (165 suites / 2006 tests passed, exit 0, all per-file thresholds satisfied). Coverage is 96.30% lines / 89.22% branches repo-wide for the extension, above the 85%/75% uniform floors. Every new production file meets the per-file 85/75 gate except the type-only `repo-automation-service-contract.ts`, which is omitted from the threshold gate under the interface-only clarification in `.claude/rules/general-unit-test.md` (justification recorded inline in `jest.config.cjs`).

Environment note (verification method, not a defect): Jest 30 on Windows cannot discover test files when the repository sits under the dotted `.claude/worktrees/...` path (`node run-jest.cjs --listTests` returns zero matches; root cause documented in `evidence/baseline/baseline-test-coverage.2026-07-19T00-40.md`). This reviewer verified tests and coverage by re-syncing the executor's non-dotted mirror at `C:\Users\DanMoisan\AppData\Local\Temp\drm370\extensions\drm-copilot` from branch head `9525dd0a` (`src/`, `test/`, `jest.config.cjs`, `package.json`, `tsconfig*.json`, `run-jest.cjs` copied; `node_modules/`, `resources/` junctioned) and running the identical `node run-jest.cjs --coverage ...` invocation there. Results matched the executor evidence exactly.

**Temporary artifacts cleanup:**
- [x] No temporary or one-time scripts were committed on the branch (diff inspection).
- [x] The non-dotted mirror is outside the repository (user temp directory) and is not part of the branch.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Independence** - Tests run in any order | PASS | Jest runs suites in parallel workers; new discovery tests build per-test fakes (mock service builders, faked spawn boundary) with no shared mutable state; full 165-suite run passes. |
| **Isolation** - Each test targets single behavior | PASS | Per-tool describe blocks; resolver tests separate valid/missing/wrong-type cases; service-call tests assert one argv/parse/error behavior each (`test/repo-automation-execute-discovery.test.ts`, 21 tests). |
| **Fast Execution** - Tests complete quickly | PASS | Full suite: 8.7 s for 2006 tests (reviewer run, mirror). |
| **Determinism** - Consistent results | PASS | Spawn boundary faked (no real subprocess); no wall-clock, RNG, or network use in new tests; VS Code API faked via existing extension test harness. |
| **Readability & Maintainability** - Clear structure | PASS | Descriptive test names per behavior; Arrange-Act-Assert structure; files grouped by module mirroring `src/`. |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Baseline Coverage Documented** | PASS | Baseline: 96.74% lines, 89.28% branches, 158 suites / 1886 tests. Command: `npm run test:coverage`. Timestamp: 2026-07-19T00-40. Artifact: `evidence/baseline/baseline-test-coverage.2026-07-19T00-40.md`. |
| **No Coverage Regression** | PASS | Post-change: 96.30% lines, 89.22% branches (-0.44 / -0.06 pts) — denominator growth from ~1,600 new production lines, not regression on changed lines; all changed/new lines covered at or above floors (per-file table below). |
| **New Code Coverage** | PASS | All new production files >= 85% lines / >= 75% branches (lowest: `discovery-command-registration.ts` 90.66%/78.18%); type-only `repo-automation-service-contract.ts` legitimately reports 0% executable coverage (interface-only clarification). Verified from `coverage/lcov.info` regenerated at head. |
| **Comprehensive Coverage** | PASS | Every handler (7), every resolver (7), every service method (7), the dispatch switch (7 cases), the mapping table (all module:function entries), and the Python runtime probe are covered by named tests (Appendix A). |
| **Positive Flows** | PASS | Per-tool success round-trips in `test/mcp-server.discovery.test.ts` and `test/mcp-tools.discovery.test.ts`; argv-composition happy paths per tool in `test/repo-automation-execute-discovery.test.ts`. |
| **Negative Flows** | PASS | Missing/wrong-type/out-of-enum resolver rejections (23 tests in `test/mcp-tool-inputs-discovery.test.ts`); invalid-enum dispatch rejection before spawn; unsupported artifact_type/report_type errors in the mapping table. |
| **Edge Cases** | PASS | Omitted `workspace_root` fallback, optional-flag omission/inclusion (`--force`, `--check`, `--output`), empty stdout, malformed JSON stdout, PATHEXT behavior on Windows probe. |
| **Error Handling** | PASS | Non-zero CLI exit propagates as `CommandExecutionError` carrying stderr; dispatch maps thrown errors to `ok: false` + `stderr_excerpt`; interpreter-not-found probe error message asserted. |
| **Concurrency** | N/A | Wrapper is stateless per invocation; no concurrent state introduced. |
| **State Transitions** | N/A | No stateful component added; extension state unchanged. |

### 1.2.1 Per-Language Coverage Comparison

- TypeScript: Baseline: 96.74% lines / 89.28% branches -> Post-change: 96.30% lines / 89.22% branches. Change: -0.44 pts lines / -0.06 pts branches (denominator growth; no changed-line regression). New/changed-code coverage: 90.66% lines / 78.18% branches at minimum across gated new files (per-file table below, reviewer-verified from `extensions/drm-copilot/coverage/lcov.info` at head `9525dd0a`). Disposition: PASS. Evidence: `evidence/qa-gates/coverage-delta.2026-07-19T02-20.md`; reviewer lcov parse 2026-07-19T02-33.

| File | Kind | Lines | Branches | Gate |
|---|---|---|---|---|
| `src/repo-automation-execute-discovery.ts` | new | 97.14% (441/454) | 83.87% (52/62) | PASS |
| `src/mcp-tool-inputs-discovery.ts` | new | 100% (246/246) | 96.88% (31/32) | PASS |
| `src/mcp-handlers/discovery-handlers.ts` | new | 100% (69/69) | 100% (7/7) | PASS |
| `src/mcp-discovery-tool-definitions.ts` | new | 100% (210/210) | 100% (0 branches) | PASS |
| `src/discovery-command-registration.ts` | new | 90.66% (330/364) | 78.18% (43/55) | PASS |
| `src/repo-automation-service-contract.ts` | new, type-only | 0% (0/175) | — | PASS (interface-only omission per policy) |
| `src/mcp-tools.ts` | modified | 92.77% (295/318) | 83.33% (45/54) | PASS |
| `src/runtime-detection.ts` | modified | 95.05% (269/283) | 84.75% (50/59) | PASS |
| `src/repo-automation-service.ts` | modified | 98.18% (431/439) | 93.33% (42/45) | PASS |
| `src/repo-automation-tool-names.ts` | modified | 100% (32/32) | — | PASS |
| `src/mcp-repo-automation-tool-definitions.ts` | modified | 100% (490/490) | — | PASS |
| `src/mcp-tool-definitions.ts` | modified | 100% (437/437) | — | PASS |
| `src/extension.ts` | modified | 97.38% (483/496) | 90.91% (60/66) | PASS |

- Python / PowerShell / C# / Bash: N/A — zero changed files on the branch diff.

### 1.3 Test Structure and Diagnostics

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clear Failure Messages** | PASS | Resolver errors carry the field name (`Field 'target_dir' is required...` style via `normalizeRequiredText`); argv assertions compare full expected arrays so a mismatch prints both argvs. |
| **Arrange-Act-Assert Pattern** | PASS | New tests build fakes (Arrange), invoke the resolver/handler/service call (Act), and assert results/throws (Assert); consistent with existing suite conventions. |
| **Document Intent** | PASS | Behavior-per-test naming (e.g. found via workspace `.venv`, per-kind validate entry selection, two-input completion report). |

### 1.4 External Dependencies and Environment

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Avoid External Dependencies** | PASS | The spawn boundary is faked in every test; no real subprocess, network, or database access. |
| **Use Mocks/Stubs** | PASS | `runCommandWithOutput` faked in service-call tests; `RepoAutomationService` mocked in dispatch/server tests; VS Code API via extension test harness; `findExecutableOnPath` probed with fake filesystem helpers in runtime tests. |
| **Environment Stability** | PASS | No temporary files created by tests (repository policy); no reliance on mutable global state; PATH/PATHEXT behavior tested via injected fakes. |

### 1.5 Policy Audit Requirement

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Pre-submission Review** | PASS | This document, plus `code-review.2026-07-19T02-34.md` and `feature-audit.2026-07-19T02-34.md` in the same folder. |

---

## 2. General Code Change Policy Compliance

### 2.1 Before Making Changes

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clarify the objective** | PASS | Issue #370; `spec.md` v0.3 and `user-story.md` reconciled against the landed discovery CLI (`evidence/other/plan-reconciliation.2026-07-19T00-15.md`). |
| **Read existing change plans** | PASS | `plan.2026-07-17T15-08.md` v1.1 executed; preflight validation recorded (`evidence/qa-gates/preflight-validation.2026-07-19T01-30.md`). |
| **Document the plan** | PASS | Plan file with phased tasks P0–P8; policy-read record at `evidence/baseline/phase0-instructions-read.md`. |

### 2.2 Design Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Simplicity first** | PASS | Thin-wrapper design: resolvers validate, handlers delegate, one generic executor (`executeDiscoveryServiceCall`) composes argv and parses stdout; no discovery logic re-authored in TypeScript. |
| **Reusability** | PASS | Shared analyzer arg builder and shared `registerAnalyzerCommand` factory for the three analyzers; `DISCOVERY_TOOL_DEFINITIONS` spread into both definition files eliminates duplication; enum constants single-sourced in `mcp-tool-inputs-discovery.ts`. |
| **Extensibility** | PASS | Central mapping table makes an upstream CLI rename a one-line change; adding a tool follows the established five-touch-point pattern with an exhaustive switch producing compile errors on omission. |
| **Separation of concerns** | PASS | Input validation (resolvers) / dispatch (handlers) / subprocess I/O (executor) / VS Code UI (registration module) are in separate modules; the service contract is extracted to a type-only module. |

### 2.3 Module & File Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Cohesive modules** | PASS | One module per concern (definitions, inputs, handlers, executor/mapping, command registration, contract). |
| **Under 500 lines** | PASS | Largest touched production file: `extension.ts` 496; `mcp-repo-automation-tool-definitions.ts` 490; largest test file `test/mcp-server.test.ts` 493 (round-trip suite extracted to `test/mcp-server.discovery.test.ts` at 303 to respect the cap). Full table: `evidence/qa-gates/file-size-audit.2026-07-19T02-10.md`; spot-verified by reviewer. |
| **Public vs internal** | PASS | Per-tool exports are the public surface; mapping-table constants and the request type are module-private; `repo-automation-service.ts` re-exports moved contract symbols so existing imports remain valid. |
| **No circular dependencies** | PASS | `tsc --noEmit` and Jest module resolution clean; contract module type-imports only; enum constants flow one way (inputs -> definitions/registration). |

### 2.4 Naming, Docs, and Comments

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Descriptive names** | PASS | `camelCase` functions, `PascalCase` types, kebab-case filenames, snake_case MCP fields per existing conventions. |
| **Docs/docstrings** | PASS | Header doc comment on the mapping/executor module records the reconciliation and the `-c` mechanism justification; JSDoc on exported resolvers/functions with `@throws`. |
| **Comment why, not what** | PASS | Comments explain the `py`-then-`python` probe order rationale, the per-file-threshold-only jest policy, and the type-only omissions. |

### 2.5 After Making Changes - Toolchain Execution

| Requirement | Status | Evidence |
|------------|--------|----------|
| **1. Formatting** | PASS | Reviewer: `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` → "All matched files use Prettier code style!", exit 0. Executor: `npm run format` exit 0 (`evidence/qa-gates/final-qc-format.2026-07-19T02-20.md`). |
| **2. Linting** | PASS | Reviewer: `npm run lint` (`eslint --no-error-on-unmatched-pattern src test`) → exit 0, no findings. |
| **3. Type checking** | PASS | Reviewer: `npm run typecheck` (`tsc -p ./ --noEmit`) → exit 0, no errors. |
| **4. Testing** | PASS | Reviewer: `node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary` in the non-dotted mirror synced to head `9525dd0a` → 165 suites / 2006 tests passed, exit 0. |
| **Full toolchain loop** | PASS | Executor loop completed in one iteration (`evidence/qa-gates/final-qc-loop.2026-07-19T02-20.md`); reviewer re-run confirms all stages clean in a single pass. |
| **Explicit reporting** | PASS | Commands and exit codes recorded in evidence artifacts and this audit. |

Stages 4 (architecture-boundary), 6 (contract/schema), and 7 (integration) of the seven-stage loop: the extension package defines no dependency-cruiser stage (no `.dependency-cruiser.cjs` in `extensions/drm-copilot/`); the MCP contract is exercised by the definitions-parity and `mcp-server` list/dispatch round-trip tests, which serve as the contract check for this surface; integration with the live Python CLI is an explicit non-goal (spec `## Non-Goals`, faked spawn boundary). No pre-existing stage was skipped.

### 2.6 Summarize and Document

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Summarize changes** | PASS | Commit `9525dd0a` "feat(discovery): expose discovery CLI as MCP tools and VS Code commands"; Section 9 below. |
| **Design choices explained** | PASS | `-c` invocation mechanism, per-kind validate entries, enum-dispatched report tool, and contract extraction documented in spec and module header comments. |
| **Update supporting documents** | PASS | `spec.md`, `user-story.md`, and plan reconciled and updated on-branch. |
| **Provide next steps** | PASS | Feature ready for PR flow per this review (see Section 10). |

---

## 3. Language-Specific Code Change Policy Compliance

Only TypeScript is in scope; Python/PowerShell/Bash/JSON-pipeline sections are not applicable (no such files changed; `package.json` is extension-owned and Prettier-formatted, not part of the repo `format_json` governed set).

### Section 3E: TypeScript Code Change Policy Compliance

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Formatting — Prettier** | PASS | `npx prettier --check ...` exit 0 (reviewer). |
| **Linting — ESLint** | PASS | `npm run lint` exit 0 (reviewer); zero suppressions added in the diff (`grep` for `eslint-disable` / `@ts-` in changed files: none). |
| **Type checking — TSC** | PASS | `npm run typecheck` exit 0 (reviewer). No `any`; raw MCP input handled as `unknown` and narrowed by resolvers. |
| **Strong typing** | PASS | Exported inputs/results are explicit interfaces; discriminated `as const` enum tuples with derived union types; no type assertions added except `as const`/`candidate is string` narrowing predicate. |
| **ES modules** | PASS | ESM `import`/`export` throughout new code (`jest.config.cjs` is the pre-existing CommonJS config-file convention, a permitted config exception). |
| **Naming / file naming** | PASS | Kebab-case filenames; no `I` interface prefix; snake_case only at the MCP JSON boundary. |
| **Separation of concerns** | PASS | Pure arg/parse logic separate from subprocess I/O and VS Code UI wiring (Section 2.2). |
| **Error handling** | PASS | Fail-fast resolver errors before spawn; `CommandExecutionError` propagation with context; no catch-all swallowing (dispatch `catch` converts to structured failure result by design). |
| **Dependencies** | PASS | Zero new npm dependencies (`package.json` diff adds only `contributes.commands` entries). |
| **Test framework note** | PASS (pre-existing convention) | `.claude/rules/typescript.md` names Vitest, but the `extensions/drm-copilot` package has used Jest 30 + ts-jest since before this feature (158 pre-existing suites); the feature follows the package's established framework, as its spec mandates. Recorded in Section 8, not attributable to this change. |

---

## 4. Language-Specific Unit Test Policy Compliance

### Section 4E: TypeScript Unit Test Policy Compliance

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Framework** | PASS (pre-existing Jest convention) | Jest 30 + ts-jest, v8 coverage — the extension package standard. Test files named `*.test.ts`. |
| **Coverage expectation** | PASS | Repo-wide (extension) 96.30% lines / 89.22% branches; per-changed-file table in 1.2.1 all above 85/75. |
| **Test location mirroring** | PASS (package convention) | Tests live under `extensions/drm-copilot/test/` mirroring `src/` module names — the established mirrored test-tree for this package (`test/` vs the repo-generic `tests/` naming); not colocated with production sources. The feature's own AC mandates this exact location. |
| **No host runtime required** | PASS | VS Code API faked via the extension test harness; no Outlook/host runtime involved. |
| **AAA structure, one behavior per test** | PASS | Section 1.1/1.3. |
| **Mock hygiene** | PASS | Targeted fakes at the spawn and service boundaries; mocks rebuilt per test. |
| **No external dependencies / temp files** | PASS | Faked boundaries only; no temp files. |
| **Snapshot tests** | PASS | None added. |

---

## 5. Test Coverage Detail

### `repo-automation-execute-discovery.ts` (21 tests in `test/repo-automation-execute-discovery.test.ts`)

| Test area | Scenario Type | Status |
|-----------|--------------|--------|
| Per-tool `-c` argv composition (validate per-kind, init, inventory, dotnet, vsto, scenario, coverage/parity/completion reports) | Positive | PASS |
| `cwd === workspaceRoot` on every spawn | Positive | PASS |
| Artifact parsing: `written_paths` JSON (analyzers), `ok: wrote <path>` (scenario), none (validate/report) | Positive/Edge | PASS |
| Malformed/empty stdout returns no artifacts | Edge | PASS |
| Unsupported `artifact_type`/`report_type` throws before spawn | Negative | PASS |
| Non-zero exit propagates `CommandExecutionError` with stderr | Error Handling | PASS |

**Coverage:** 97.14% lines / 83.87% branches. Not covered: 13 lines on rarely-hit defensive branches — above gate.

### `mcp-tool-inputs-discovery.ts` (23 tests) — 100% lines / 96.88% branches. Valid, missing, wrong-type, out-of-enum, and report_type-conditional required-field cases per resolver.

### `mcp-handlers/discovery-handlers.ts` + dispatch (11 tests in `test/mcp-tools.discovery.test.ts`) — 100%/100%. Success routing and thrown-error mapping (`ok: false`, `stderr_excerpt`) per tool.

### `discovery-command-registration.ts` (16 tests in `test/extension.discovery-commands.test.ts`) — 90.66% lines / 78.18% branches. Direct-args and interactive (prompt/cancel) paths per command.

### `runtime-detection.ts` Python probe (7 tests in `test/runtime-detection.test.ts`) — 95.05%/84.75%. `.venv` hit, `py`/`python` PATH fallback incl. PATHEXT, not-found error message; existing PowerShell probe cases unchanged.

### MCP server round-trips (10 tests in `test/mcp-server.discovery.test.ts`; `listTools` union in `test/mcp-server.test.ts`) — dispatch round-trips over `InMemoryTransport`, invalid-enum rejection, no-terminal invariant preserved on the MCP path.

### Definitions contract (15 tests in `test/mcp-repo-automation-tool-definitions.test.ts`) — union order parity, cross-file alignment (single `DISCOVERY_TOOL_DEFINITIONS` spread), `additionalProperties: false`, enum equality with resolver constants.

---

## 6. Test Execution Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Total Tests | 2006 | PASS |
| Tests Passed | 2006 (100%) | PASS |
| Tests Failed | 0 | PASS |
| Execution Time | 8.7 s total (reviewer run) | PASS Fast |
| Average Time per Test | ~4 ms | PASS Fast |
| Test Suites | 165 passed / 165 | PASS |
| Code Coverage | 96.30% lines, 89.22% branches | PASS |

---

## 7. Code Quality Checks

**For TypeScript (`extensions/drm-copilot/`):**

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Prettier (check-only) | `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` | All matched files use Prettier code style; exit 0 | PASS |
| ESLint | `npm run lint` | 0 errors, 0 warnings; exit 0 | PASS |
| TSC | `npm run typecheck` | 0 errors; exit 0 | PASS |
| Jest + coverage | `node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary` (non-dotted mirror at head `9525dd0a`) | 165/165 suites, 2006/2006 tests, thresholds satisfied; exit 0 | PASS |

**Notes:**
Jest test discovery returns zero matches inside the dotted `.claude/worktrees` checkout (jest-util `replacePathSepForGlob` + picomatch escaped-dot interaction, documented in `evidence/baseline/baseline-test-coverage.2026-07-19T00-40.md`). This is an environment path quirk, not a code defect; format/lint/typecheck ran directly in the worktree and the test/coverage stage ran in a byte-identically configured non-dotted mirror synced to the branch head.

---

## 8. Gaps and Exceptions

### Identified Gaps
**None.** All applicable policy requirements are met.

### Approved Exceptions / Pre-existing Deviations (recorded, not introduced by this change)

- **Jest instead of Vitest:** `.claude/rules/typescript.md` names Vitest, but `extensions/drm-copilot` has been Jest 30 + ts-jest since prior features (158 pre-existing suites on the base branch). The feature's spec/AC mandate Jest for this package. Recorded as a pre-existing package-level convention; changing frameworks is out of scope for this feature.
- **`test/` (singular) test tree:** the package's established mirrored test tree is `extensions/drm-copilot/test/` rather than the repo-generic `tests/` name. Tests mirror `src/` structure and are not colocated with production files; the AC text mandates this exact location.
- **Type-only files omitted from per-file threshold gate:** `src/repo-automation-service-contract.ts` (this feature) and the pre-existing `src/lib/subagent-tree/types.ts` — both permitted by the interface-only clarification in `.claude/rules/general-unit-test.md`; both remain in `collectCoverageFrom` (not excluded from measurement) with justification comments in `jest.config.cjs`. No production path is excluded from coverage measurement.

### Removed/Skipped Tests
**None.** All planned tests implemented; zero skipped tests in the run output.

---

## 9. Summary of Changes

### Commits in This PR/Branch

1. **9525dd0a** - feat(discovery): expose discovery CLI as MCP tools and VS Code commands (single commit over merge-base `a6dd7d45`)

### Files Modified

1. **`extensions/drm-copilot/src/repo-automation-execute-discovery.ts`** (NEW, 454 lines) — central landed `dev.discovery.*` mapping table + Python `-c` subprocess executor with stdout artifact parsing.
2. **`extensions/drm-copilot/src/mcp-tool-inputs-discovery.ts`** (NEW, 246) — seven input resolvers; single source of the `artifact_type`/`report_type` enum literals; report_type-aware required-field validation before spawn.
3. **`extensions/drm-copilot/src/mcp-handlers/discovery-handlers.ts`** (NEW, 69) — seven thin handlers (resolve then delegate to service).
4. **`extensions/drm-copilot/src/mcp-discovery-tool-definitions.ts`** (NEW, 210) — seven `ToolDefinition`s (`additionalProperties: false`, shared `workspaceRootProperty`), spread into both definition files for guaranteed alignment.
5. **`extensions/drm-copilot/src/discovery-command-registration.ts`** (NEW, 364) — seven VS Code commands with direct-args and interactive paths.
6. **`extensions/drm-copilot/src/repo-automation-service-contract.ts`** (NEW, 175, type-only) — extracted `RepoAutomationService` contract; re-exported from the service module.
7. **`extensions/drm-copilot/src/runtime-detection.ts`** (MODIFIED) — `RuntimeKind` widened to `"powershell" | "python"`; Python probe (`.venv` → `py` → `python`).
8. **`extensions/drm-copilot/src/repo-automation-service.ts`** (MODIFIED) — seven service methods; contract extraction keeps file at 439 lines.
9. **`extensions/drm-copilot/src/repo-automation-tool-names.ts`, `mcp-tools.ts`, `mcp-repo-automation-tool-definitions.ts`, `mcp-tool-definitions.ts`, `extension.ts`, `package.json`** (MODIFIED) — union entries, dispatch cases, definition spreads, activation wiring, `contributes.commands`.
10. **`extensions/drm-copilot/jest.config.cjs`** (NEW) — per-file coverage thresholds incl. the six gated new/modified discovery files.
11. **Tests** (10 new/extended files under `extensions/drm-copilot/test/`) — 120 new tests (1886 → 2006).
12. **Docs/evidence** — spec/user-story/plan reconciliation and baseline/QA evidence under the feature folder.

---

## 10. Compliance Verdict

### Overall Status: ✅ FULLY COMPLIANT

All toolchain stages pass in independent reviewer re-execution; coverage exceeds the uniform 85%/75% floors repo-wide and per changed file; file-size, domain-neutrality, evidence-location, suppression, and dependency policies are satisfied; no Blocking findings.

### Policy-by-Policy Summary

#### General Code Change Policy (Section 2)
- ✅ Before Making Changes: plan, spec, reconciliation, and baselines present.
- ✅ Design Principles: thin wrapper, central mapping table, clean layering.
- ✅ Module & File Structure: all files <= 500 lines; contract extracted proactively.
- ✅ Naming, Docs, Comments: conventions followed; rationale-bearing comments.
- ✅ Toolchain Execution: single clean pass (executor) + independent reviewer re-run.
- ✅ Summarize & Document: docs reconciled on-branch.

#### Language-Specific Code Change Policy (Section 3 — TypeScript)
- ✅ Tooling & Baseline: Prettier/ESLint/TSC clean.
- ✅ Typing & Design: no `any`; `unknown`+narrowing at the MCP boundary; zero suppressions.
- ✅ Error Handling: fail-fast resolvers; contextual error propagation.

#### General Unit Test Policy (Section 1)
- ✅ Core Principles; ✅ Coverage & Scenarios; ✅ Test Structure; ✅ External Dependencies; ✅ Policy Audit.

#### Language-Specific Unit Test Policy (Section 4 — TypeScript)
- ✅ Framework & Scope (Jest per package convention); ✅ Style & Structure; ✅ Naming & Readability; ✅ Toolchain.

### Metrics Summary

- ✅ 2006/2006 tests passing (100%), 165/165 suites
- ✅ 96.30% line / 89.22% branch coverage (extension-wide)
- ✅ All new production files >= 90.66% lines / >= 78.18% branches (type-only file exempt per policy)
- ✅ All code quality checks passing; zero suppressions added
- ✅ Test execution time 8.7 s (fast)

### Recommendation

**Ready for merge** — no remediation required. Proceed to PR authoring against `epic/legacy-discovery-and-parity-integration`.

---

## Appendix A: Test Inventory

New and extended discovery test files (full 2006-test inventory omitted; totals verified by reviewer run):

- `test/repo-automation-execute-discovery.test.ts` — 21 tests: per-tool argv composition (per-kind validate, init, inventory, dotnet, vsto, scenario, coverage/parity/completion), cwd, artifact parsing, error propagation.
- `test/mcp-tool-inputs-discovery.test.ts` — 23 tests: per-resolver valid/missing/wrong-type/out-of-enum, report_type-conditional required inputs, workspace-root fallback.
- `test/mcp-tools.discovery.test.ts` — 11 tests: dispatch routing per tool; thrown-error → `ok: false` + `stderr_excerpt` mapping.
- `test/extension.discovery-commands.test.ts` — 16 tests: seven commands, direct-args and interactive/cancel paths, disposables pushed.
- `test/runtime-detection.test.ts` — 7 tests: Python probe `.venv`/`py`/`python`/PATHEXT/not-found.
- `test/mcp-server.discovery.test.ts` — 10 tests: MCP list/dispatch round-trips, invalid-enum rejection, no-terminal invariant.
- `test/repo-automation-service.discovery.test.ts` — 8 tests: service methods delegate to executor helpers.
- `test/mcp-repo-automation-tool-definitions.test.ts` — 15 tests (extended): union order, cross-file alignment, `additionalProperties: false`, enum equality.
- `test/mcp-server.test.ts` (extended) — `listTools` exact-array including the seven discovery tools.
- `test/mcp-server-epic-validation.test.ts`, `test/mcp-tools.codex-native-converter.test.ts`, `test/mcp-tools.push-down-claude.test.ts` (extended) — mock-service builders widened for the new interface methods.

---

## Appendix B: Toolchain Commands Reference

**For TypeScript (run in `extensions/drm-copilot/`):**
```bash
# Formatting (check-only variant used by this audit; repo script is --write)
npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"
npm run format          # repo script (prettier --write)

# Linting
npm run lint            # eslint --no-error-on-unmatched-pattern src test

# Type checking
npm run typecheck       # tsc -p ./ --noEmit

# Testing / coverage (Jest 30 + ts-jest, v8)
npm run test            # node run-jest.cjs
npm run test:coverage   # node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary
# Note: under the dotted .claude/worktrees path, run the identical invocation in a
# non-dotted mirror (see Section 7 notes) — jest glob matching fails on the dotted path.
```

**Repository validators used by this audit:**
```bash
python -m scripts.dev_tools.validate_evidence_locations --root .
python -m scripts.dev_tools.pr_context.collector --base a6dd7d4591ef80f4d351cea4b0488ce08568286e --head HEAD
python -m scripts.dev_tools.validate_orchestration_artifacts policy-audit <path>
```

---

**Audit Completed By:** feature-review agent (Claude)
**Audit Date:** 2026-07-19
**Policy Version:** Current (as of audit date)
