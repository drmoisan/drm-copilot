# Feature Audit: npm-audit-brace-expansion-dos (#414)

**Audit Date:** 2026-07-25
**Feature Folder:** `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414`
**Base Branch:** `main`
**Head Branch:** `bug/npm-audit-brace-expansion`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

---

## Scope and Baseline

- **Base branch:** `main` (resolved per `pr-base-branch-merge-base`: merge-base with `main` is commit `73b3f2a29c5f6519ae61738dd60f171c0640159d`, timestamp 2026-07-25 14:34:07 -0500 [epoch 1785008047]; competing candidate `development` merge-base `2745d23d` at 2026-05-25 [epoch 1779726190] — `main` selected as most-recent common ancestor)
- **Head branch/commit:** `bug/npm-audit-brace-expansion` (commit `31392e00f94ab3502f9ad6bb194f80bc7c36a24d`, matching the caller-stated head; `origin/bug/npm-audit-brace-expansion` also at `31392e00`)
- **Merge base:** `73b3f2a29c5f6519ae61738dd60f171c0640159d`
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer against `main` at review time; the artifact pair was absent at review start)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/evidence/**` (45 artifacts: `baseline/` 15, `qa-gates/` 21, `regression-testing/` 4, `other/` 9 as committed; 45 total)
  - Additional evidence: reviewer-executed commands recorded per criterion below; CI run <https://github.com/drmoisan/drm-copilot/actions/runs/30176168742>
- **Feature folder used:** `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414` (sole active folder matching issue #414; suffix matches the canonical issue number)
- **Requirements source:** `spec.md` only
- **Work mode resolution note:** Explicit marker `- Work Mode: full-bug` present in `issue.md` (line 12). Per the acceptance-criteria-tracking contract, `full-bug` resolves the AC source to `spec.md` only. `user-story.md` is intentionally absent for this mode; its absence is not a finding.
- **Scope note:** The audit scope is the full branch diff `73b3f2a2..31392e00` (53 changed paths: 4 dependency files, 49 Markdown documentation/evidence files). The spec's own AC 11 constrains the non-documentation change set to exactly four files, which this audit verifies rather than assumes. No caller-supplied scope narrowing was detected or applied.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/spec.md` (`## Acceptance Criteria`) — only source

All 11 criteria are markdown checkboxes and were already checked `[x]` by the executor (per `evidence/other/ac-status-summary.2026-07-25T22-27.md`). Criterion text below is copied from the source file; criteria 7 and 8 are abbreviated in the evaluation table but quoted in full here.

### Acceptance criteria

1. `npm audit --audit-level=moderate` exits 0 in the repository root (`.`).
2. `npm audit --audit-level=moderate` exits 0 in `extensions/drm-copilot`.
3. `npm audit --audit-level=moderate` exits 0 in `packages/mcp-server` (regression guard — it passes today and must continue to pass).
4. No `brace-expansion` node resolving to a version `<=5.0.7` (specifically `2.1.2` or `5.0.7`) remains in `package-lock.json` or `extensions/drm-copilot/package-lock.json` (verify by grep for `node_modules/brace-expansion` entries and their `"version"` fields).
5. No `minimatch@9.x` node remains in `package-lock.json` or `extensions/drm-copilot/package-lock.json` (verify by grep for `"version": "9.` under `minimatch` nodes).
6. The `"c8": { "brace-expansion": "^5.0.7" }` override block is removed from both `package.json` and `extensions/drm-copilot/package.json`, and both manifests carry unscoped `"brace-expansion": "^5.0.8"` and `"minimatch": "^10.2.5"` overrides.
7. Root toolchain passes with baseline-parity qualifications: `npm run lint` and `npm run typecheck` exit 0 in the repository root; `npm run format:check` either exits 0 or produces output identical to the recorded pre-edit baseline (the same two pre-existing fixture failures — `tests/fixtures/discovery_schemas/v1/runtime-characterization-scenario.invalid.json` and `tests/fixtures/discovery_schemas/v1/unspecified-behavior-record.invalid.json` — which are byte-identical to `origin/main`), proving the change introduced no new formatting failure; root unit-test coverage meets >=85% line / >=75% branch measured via the rootDir-free jest invocation that avoids the worktree `.claude` glob-escape artifact (the `npm run test:unit:coverage` script reports `No tests found` under this worktree path — an artifact of the worktree location, not a repository defect). `npm run test:integration` (`vscode-test`) is not a runnable gate in this repository — no `.vscode-test.{json,js,cjs,mjs}` config exists, so `@vscode/test-cli` exits 1 before starting a runner, both locally and on CI, and no workflow invokes it. For that command it is sufficient that the post-change output is identical to the recorded pre-edit baseline failure, and that mocha's forced `minimatch` consumption path is verified directly instead.
8. Extension toolchain passes: `npm run lint`, `npm run typecheck`, and `npm run compile` exit 0 in `extensions/drm-copilot`; extension unit-test coverage meets >=85% line / >=75% branch measured via the rootDir-free jest invocation that avoids the worktree `.claude` glob-escape artifact, with the worktree-path artifact documented in the evidence (the `npm run test:coverage` script reports `No tests found` under this worktree path — an artifact of the worktree location, not a repository defect).
9. All three `NPM Audit Gate` jobs succeed on the branch head SHA, verified via `gh run view <id> --json jobs`.
10. `packages/mcp-server/package.json` and `packages/mcp-server/package-lock.json` are unmodified (`git diff --name-only main...HEAD` lists no `packages/mcp-server` paths).
11. The change set (`git diff --name-only main...HEAD`), after excluding paths under `docs/features/` and `artifacts/orchestration/` (feature documentation and the orchestration checkpoint), contains exactly four files: `package.json`, `package-lock.json`, `extensions/drm-copilot/package.json`, `extensions/drm-copilot/package-lock.json`.

**Note on criteria 7 and 8 wording:** the baseline-parity qualifications were written into the criterion text by the orchestrator disposition of the Phase 0 gate-baseline escalation (plan Conventions; `evidence/other/phase0-gate-baseline-escalation.2026-07-25T17-18.md`). This reviewer independently re-verified all three underlying pre-existing conditions (see criterion 7 evaluation and the policy audit, Section 8) and finds the reworded criteria sound: each qualification covers only a condition demonstrably present on `main` before this change, and every other gate retained absolute exit-0 acceptance.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Root `npm audit` exits 0 | PASS | Reviewer re-ran at head `31392e00`: exit 0, `found 0 vulnerabilities`. Executor: `evidence/regression-testing/npm-audit-pass-after-root.2026-07-25T21-42.md`. Fail-before: exit 1, 22 high (`evidence/baseline/npm-audit-fail-before-root.2026-07-25T17-01.md`). | `npm audit --audit-level=moderate` (repo root) | Independently verified against the live advisory database on 2026-07-25. |
| 2 | Extension `npm audit` exits 0 | PASS | Reviewer re-ran: exit 0. Executor: `evidence/regression-testing/npm-audit-pass-after-extension.2026-07-25T21-45.md`. Fail-before: exit 1, 20 high (`evidence/baseline/npm-audit-fail-before-extension.2026-07-25T17-02.md`). | `npm audit --audit-level=moderate` (extensions/drm-copilot) | Independently verified. |
| 3 | mcp-server `npm audit` exits 0 (regression guard) | PASS | Reviewer re-ran: exit 0, `found 0 vulnerabilities`. Executor: `evidence/regression-testing/npm-audit-post-change-mcp-server.2026-07-25T21-54.md`; baseline control `evidence/baseline/npm-audit-baseline-mcp-server.2026-07-25T17-02.md`. | `npm audit --audit-level=moderate` (packages/mcp-server) | Tree contains zero `brace-expansion`/`minimatch`/`glob` nodes (reviewer grep count 0 for each). |
| 4 | No `brace-expansion` node `<=5.0.7` in either affected lockfile | PASS | Reviewer grep: exactly one `node_modules/brace-expansion` node per lockfile, resolved `5.0.8` with registry integrity hash; the only `"version": "2.1.2"` match in each file is `safer-buffer` (unrelated); zero `brace-expansion-5.0.7.tgz` / `brace-expansion-2.1.2.tgz` references. Executor: `evidence/other/lockfile-assertions.2026-07-25T21-48.md`. | `grep -n 'node_modules/brace-expansion' package-lock.json` (both roots); context inspection of all `"version": "2.1.2"`/`"5.0.7"` matches | Reviewer verified the near-miss matches belong to other packages rather than accepting zero-hit greps alone. |
| 5 | No `minimatch@9.x` node in either affected lockfile | PASS | Reviewer grep: zero `minimatch-9*.tgz` references in either lockfile; exactly one `minimatch-10.2.5.tgz` reference per lockfile (the hoisted node). Executor: `evidence/other/lockfile-assertions.2026-07-25T21-48.md`. | `grep -n 'minimatch-9' package-lock.json extensions/drm-copilot/package-lock.json` (exit 1, no matches) | The branch diff shows the removal of hoisted root `minimatch@9.0.9` and both nested 9.0.9 nodes. |
| 6 | `c8` override removed; unscoped `brace-expansion ^5.0.8` and `minimatch ^10.2.5` in both manifests | PASS | Reviewer grep: `"c8"` absent from both manifests (exit 1); `"brace-expansion": "^5.0.8"` and `"minimatch": "^10.2.5"` present in both `overrides` blocks (root lines 20–21; extension lines 222–223). Diff shows exactly the two added entries and the removed `c8` block per manifest. Executor: `evidence/other/manifest-assertions.2026-07-25T21-50.md`. | `grep -n '"c8"' package.json extensions/drm-copilot/package.json`; `grep -n '"brace-expansion"\|"minimatch"' ...`; `git diff 73b3f2a2 HEAD -- package.json extensions/drm-copilot/package.json` | |
| 7 | Root toolchain passes with baseline-parity qualifications (full text above) | PASS | Lint: exit 0 (executor `final-lint-root.2026-07-25T21-59.md`; reviewer re-ran, exit 0). Typecheck: exit 0 (executor `final-typecheck-root.2026-07-25T22-00.md`; reviewer re-ran, exit 0). Format:check: reviewer re-ran — flags exactly the two named fixtures and no other file, identical to baseline `format-check-root-baseline.2026-07-25T17-09.md`; fixtures byte-identical to `origin/main` (reviewer: empty `git diff`). Coverage: 97.00% line / 89.06% branch via rootDir-free invocation (`final-test-unit-coverage-root.2026-07-25T22-02.md`); reviewer re-parsed `coverage/lcov.info`: 97.01% (39673/40896) line, 89.06% (5514/6191) branch; zero delta vs baseline (`coverage-comparison-root.2026-07-25T22-05.md`). Worktree artifact reproduced by reviewer (`npx jest --listTests` returns zero tests here). test:integration: post-change output identical to pre-edit baseline failure (`final-test-integration-root.2026-07-25T22-04.md` vs `test-integration-root-baseline.2026-07-25T17-12.md`); reviewer glob-verified no `.vscode-test.{json,js,cjs,mjs}` exists and grep-verified no workflow invokes the script. Mocha path verified directly: reviewer re-executed the check — `minimatch@10.2.5` resolves from mocha's root, brace pattern returns `true`, no `TypeError` (`mocha-minimatch-brace-path.2026-07-25T22-25.md`). | `npm run lint`; `npm run typecheck`; `npm run format:check`; `git diff --name-only origin/main -- tests/fixtures/discovery_schemas/`; lcov parse of `coverage/lcov.info`; `npx jest --listTests`; node one-liner resolving `minimatch` from mocha's resolution root | Every branch of this compound criterion satisfied on the terms its own text states; all three qualifications re-verified as genuinely pre-existing on `main`. |
| 8 | Extension toolchain passes; coverage >= 85%/75% via rootDir-free invocation | PASS | Lint exit 0 (`final-lint-extension.2026-07-25T22-07.md`); typecheck exit 0 (executor artifact; reviewer re-ran, exit 0); compile exit 0 (`final-compile-extension.2026-07-25T22-09.md`). Coverage: 96.33% line / 89.21% branch (`final-test-coverage-extension.2026-07-25T22-11.md`); reviewer re-parsed `extensions/drm-copilot/coverage/lcov.info`: 96.34% (37643/39074) line, 89.21% (5201/5830) branch; zero delta with identical absolute counts (`coverage-comparison-extension.2026-07-25T22-12.md`). Worktree-path artifact documented in the evidence as the criterion requires. | `npm run typecheck` (extension); lcov parse of `extensions/drm-copilot/coverage/lcov.info` | 2031/2031 tests across 168 suites. |
| 9 | All three `NPM Audit Gate` jobs succeed on the branch head SHA via `gh run view` | PASS | Reviewer re-queried run 30176168742: conclusion `success`; legs `npm-audit / npm audit (.)`, `(extensions/drm-copilot)`, `(packages/mcp-server)` all `success`; run `headSha` `478f40b8` matches the head recorded and pushed at verification time ([P6-T1], `evidence/other/branch-head.2026-07-25T22-17.md`; `evidence/qa-gates/npm-audit-gate-ci.2026-07-25T22-22.md`). | `gh run view 30176168742 --json headSha,conclusion,jobs` | Qualification: the branch head subsequently advanced to `31392e00` by a docs-only commit (reviewer: `git diff --name-only 478f40b8 31392e00` lists only `docs/features/**`; audit inputs byte-identical). The criterion was satisfied on the branch head as it stood at verification; reviewer additionally re-ran all three audits locally at `31392e00`, exit 0. The orchestrator S9 CI gate re-verifies on the PR head. Recorded as an Info finding in the code review; non-blocking. |
| 10 | `packages/mcp-server` manifest and lockfile unmodified | PASS | Reviewer: `git diff --name-only 73b3f2a2 31392e00` contains zero paths beginning `packages/mcp-server` (grep count 0). Executor: `evidence/other/committed-change-set-assertion.2026-07-25T22-18.md` assertion (a). Install/build control: `mcp-server-install-build.2026-07-25T22-14.md` (both commands exit 0). | `git diff --name-only 73b3f2a2 31392e00 \| grep -c '^packages/mcp-server'` | |
| 11 | Change set after exclusions is exactly the four dependency files | PASS | Reviewer: after excluding `docs/features/` and `artifacts/orchestration/`, the diff list is exactly `extensions/drm-copilot/package-lock.json`, `extensions/drm-copilot/package.json`, `package-lock.json`, `package.json` — no more, no fewer. Executor: `evidence/other/committed-change-set-assertion.2026-07-25T22-18.md` assertion (b). | `git diff --name-only 73b3f2a2 31392e00 \| grep -v '^docs/features/' \| grep -v '^artifacts/orchestration/'` | No workflow, source, or configuration path present; `artifacts/orchestration/` contributes zero paths to this range. |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 11 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. At PR time, confirm the three `NPM Audit Gate` legs report `success` on the PR head SHA (orchestrator S9 CI gate; closes the `478f40b8` vs `31392e00` head-advance qualification on criterion 9).
2. File the three pre-existing defects recorded in `evidence/other/preexisting-defects-for-filing.2026-07-25T22-28.md` as separate issues (root `format:check` fixtures, absent root `.vscode-test` configuration, jest dot-directory worktree discovery artifact).

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- Criteria evaluated as **PASS** may be checked off in the authoritative source file if not already checked.
- All 11 criteria were already checked `[x]` in `spec.md` by the executor (one at a time, evidence-before-check-off, per `evidence/other/ac-status-summary.2026-07-25T22-27.md`). This audit independently re-verified each criterion and confirms every existing check-off is justified. No source-file checkbox change was made by this review because no unchecked PASS criterion and no unjustified checked criterion exists.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/spec.md`
- Total AC items: 11
- Checked off (delivered): 11
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-npm-audit-brace-expansion-dos-414/spec.md` | 11 | 11 | 0 | Checkbox-backed; sole authoritative source for `full-bug` mode; all check-offs re-verified by this audit |
