# Feature Audit: orchestrator-completion-hook-false-block (#413)

**Audit Date:** 2026-07-25
**Feature Folder:** `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413`
**Base Branch:** `main`
**Head Branch:** `bug/orchestrator-completion-hook-false-block-413`
**Work Mode:** `full-bug`
**Audit Type:** Initial acceptance review

**Template source note:** Created from the bundled asset `extensions/drm-copilot/resources/templates/policy_audit/feature-audit.yyyy-MM-ddTHH-mm.md` — the exact file the MCP `feature-audit-template` selector resolves — because the MCP tool surface is unavailable in this delegated environment.

---

## Scope and Baseline

- **Base branch:** `main` (resolved `origin/main @ 72126592b127cdd9429ed7b8ae921554bebe20c9`)
- **Head branch/commit:** `bug/orchestrator-completion-hook-false-block-413` (commit `60994855d8e0ef11402b7ce2b33af33debe94d6b`)
- **Merge base:** `72126592b127cdd9429ed7b8ae921554bebe20c9` (equals the base tip; the branch is a strict descendant of `main`)
- **Evidence sources:**
  - Primary: `artifacts/pr_context.summary.txt` (regenerated by this reviewer at 2026-07-25T17-48 against base `main`; the artifact pair supplied by the caller was absent on disk)
  - Secondary baseline diff: `artifacts/pr_context.appendix.txt`
  - Feature evidence: `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/evidence/**` (23 artifacts across baseline, regression-testing, qa-gates, other)
  - Additional evidence: reviewer-executed verification runs (Pester re-runs, parity pytest, SHA-256 parity, coverage XML re-parse, end-to-end hook executions in both directions)
- **Feature folder used:** `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413`
- **Requirements source:** `spec.md` only
- **Work mode resolution note:** Explicit marker `- Work Mode: full-bug` at `issue.md` line 12. Per the work-mode contract, `spec.md` is the sole AC source; `user-story.md` is intentionally absent and its absence is correct for this mode, not a finding.
- **Scope note:** Full feature-vs-base audit of `72126592..60994855`. Languages with changed files: PowerShell only. The caller supplied no scope narrowing.

---

## Acceptance Criteria Inventory

**Authoritative AC source files for this run:**
- `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/spec.md` (`## Acceptance Criteria`, 13 checkbox items) — only source

### Acceptance criteria

1. The hook ALLOWS a DONE claim when the validator exits 0 and prints its success line: a regression test exists at the `Invoke-RoutingContractValidation` unit level in `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` stubbing `ExitCode = 0` with `Output = 'orchestrator-state validation passed: artifacts/orchestration/orchestrator-state.json'` and asserting `HasErrors` is `$false`, and it passes.
2. The hook ALLOWS a DONE claim end-to-end: a regression test exists in `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` exercising `Invoke-OrchestratorOutputValidation` with a `-RoutingInvoker` stub returning `ExitCode = 0` and the success-line output, asserting `Ok` is `$true` and `Message` is null or empty, and it passes.
3. The hook still BLOCKS when the validator exits non-zero: the existing non-zero-exit regression tests (unit test at approximately lines 255-264 and end-to-end `ROUTING_CONTRACT_BLOCKED` test at approximately lines 180-199 of `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1`) pass unmodified.
4. The gate still fails closed with no weakening: no existing test assertion that a genuine validator failure (non-zero exit) blocks DONE is removed or relaxed, and the fixed decision blocks on every non-zero exit code including exit 2 and crash paths.
5. The `MODEL_ROUTING_BLOCKED:` block reason still fires for model-routing/complexity errors and `ROUTING_CONTRACT_BLOCKED:` for other failures: `tests/scripts/claude-hooks/validate-orchestrator-output.model-routing.Tests.ps1` passes unmodified, and the `model_routing_receipts|complexity_assessments` discrimination logic in `Invoke-OrchestratorOutputValidation` is unchanged in the diff.
6. The existing test `'reports HasErrors when the seam returns error text with exit 0'` (approximately lines 266-276 of `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1`) is revised, because it asserts the defect; the exit-0-with-text-blocks assertion no longer exists in the suite.
7. The `{ HasErrors, ErrorText }` return contract and the `$Invoker` seam signature of `Invoke-RoutingContractValidation` are unchanged, `ErrorText` still carries the captured output, and the `.DESCRIPTION` docstring is corrected to document exit-code-only discrimination.
8. The bundled pushed-down copy `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-orchestrator-output.ps1` is byte-identical to `.claude/hooks/validate-orchestrator-output.ps1`, and `python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q` passes.
9. The portable fallback path is verified unchanged and still fails closed: no diff touches `OrchestratorStateCompletion.psm1` (either copy), and `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCompletion.Tests.ps1` passes unmodified.
10. `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` remains at or under the 500-line cap; if a sibling test file was required, it lives under `tests/scripts/claude-hooks/` per the test-location rule.
11. The PowerShell toolchain loop passes cleanly in a single pass: PoshQC format, PSScriptAnalyzer analyze, and Pester test all succeed, with line coverage >= 85% and branch coverage >= 75% and no coverage regression on changed lines.
12. Primary acceptance evidence: the hook itself is exercised end-to-end after the fix — `pwsh -File .claude/hooks/validate-orchestrator-output.ps1` with a DONE-claiming `CLAUDE_HOOK_INPUT` against a real completion-passing `artifacts/orchestration/orchestrator-state.json` — and exits 0.
13. No unintended changes outside the defined scope: the diff touches only the three files in the Files In Scope table, and no Python validator, complexity-floor, or `.codex/` file is modified.

---

## Acceptance Criteria Evaluation

| # | Criterion | Status | Evidence | Verification command(s) | Notes |
|---|-----------|--------|----------|--------------------------|-------|
| 1 | Unit-level ALLOW regression exists and passes | PASS | Diff hunk adds `It 'reports no errors when the seam returns exit 0 with the validator success line (issue #413)'` with the exact stub and assertion required; fail-before artifact shows it failing pre-fix | `git diff 72126592..HEAD -- tests/...`; reviewer Pester re-run (40/40) | Fail-before/pass-after pair present (`evidence/regression-testing/`) |
| 2 | End-to-end ALLOW regression exists and passes | PASS | Diff hunk adds `It 'allows DONE when the validator exits 0 and prints its success line (issue #413)'` asserting `Ok -BeTrue` and `Message -BeNullOrEmpty` | Same as #1 | Failed pre-fix per fail-before artifact |
| 3 | Existing non-zero-exit BLOCK tests pass unmodified | PASS | Diff contains no change to either test; both pass in reviewer re-run | `git diff 72126592..HEAD -- tests/...`; `Invoke-Pester` re-run | |
| 4 | Fail-closed with no weakening, incl. exit 2 / crash | PASS | No blocking assertion removed or relaxed (diff inspected); new exit-2 test added; `$hasErrors = ($exitCode -ne 0)` blocks all non-zero by construction; validator source verified: no exit-0-with-error-text path exists (`main()` lines 344-352); live run blocks with exit 1 | Reviewer read of `validate_orchestration_artifacts.py`; live hook run against mid-run checkpoint (exit 1, `ROUTING_CONTRACT_BLOCKED:`) | The load-bearing discriminator claim was independently verified |
| 5 | Block-reason discrimination unchanged | PASS | model-routing test file absent from diff; regex block at hook lines 330-333 unchanged; 6/6 tests pass in reviewer re-run; live block message shows `ROUTING_CONTRACT_BLOCKED:` for non-routing errors | `git diff --name-status`; `Invoke-Pester` re-run | |
| 6 | Defect-asserting test revised; old assertion gone | PASS | Diff replaces the test in place; no `ExitCode = 0` + error-text blocking assertion remains in the suite | `git diff 72126592..HEAD -- tests/...`; grep of the test file | |
| 7 | Contracts unchanged; docstring corrected | PASS | `return @{ HasErrors...; ErrorText = $outputText }` and `param($Path, $Type)` seam unchanged (context lines); `.DESCRIPTION` now documents exit-code-only discrimination; live block message carries validator stderr in `ErrorText` | Hook file read at head; live hook run | |
| 8 | Bundled copy byte-identical; parity pytest passes | PASS | Both copies hash `5e4bfa47c748c4e2e44262141e1f543b1ade1a19ed43005855735ab422d3183b`; parity pytest 7/7 | `sha256sum` both copies; `poetry run python -m pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q` (reviewer re-run) | Hash matches the spec's recorded value |
| 9 | Portable fallback unchanged and fail-closed | PASS | `git diff 72126592..HEAD -- .claude/lib/ extensions/.../.claude/lib/` empty; module returns `ExitCode = 0; Output = ''` on success (line 240) and `ExitCode = 1` on error paths (lines 227, 237); 7/7 tests pass | Reviewer diff check, module read, Pester re-run | |
| 10 | Test file at or under 500-line cap | PASS | 486 lines measured at head (was 449); no sibling file needed | `wc -l tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` | 14 lines of headroom |
| 11 | Toolchain single clean pass; line >= 85%; branch >= 75%; no changed-line regression | PASS (with adjudicated exception on the branch clause) | Format EXIT 0 / no changes; analyze 0 findings (reviewer re-ran PSSA: 0); Pester 1,347/0/9; line coverage 90.22% repo-wide and 92.16% on the changed hook (reviewer re-parsed XML); changed line 232 `mi=0 ci=2`; no missed line in changed regions. Branch clause: the emitted JaCoCo XML contains zero BRANCH counters (reviewer-verified grep: INSTRUCTION/LINE/METHOD/CLASS only; per-line `mb`/`cb` uniformly 0) — genuinely not measurable by this toolchain; repository precedent #272 | Reviewer XML parse; `Invoke-ScriptAnalyzer` re-run; evidence artifacts `final-poshqc-*.2026-07-25T17-24.md`, `coverage-delta.2026-07-25T17-24.md` | Reviewer ruling on escalated item 1: the branch-coverage clause is accepted as not-measurable (pre-existing toolchain limitation, not caused or worsened by this change); all measurable clauses pass with margin. AC accepted and checked off; exception recorded in policy audit section 8 |
| 12 | Primary acceptance evidence: hook end-to-end exit 0 | PASS | Hook run with real default `$Invoker` (genuine Python subprocess, no mock) exits 0 against the fixture checkpoint independently proven to pass `--require-complete --require-model-routing` at exit 0; reviewer reproduced the run (exit 0) and the fail-closed converse against the live checkpoint (exit 1) | Reviewer executions recorded in policy audit Appendix B; `evidence/qa-gates/hook-e2e-allow.2026-07-25T17-19.md`, `live-checkpoint-precheck.2026-07-25T17-19.md` | Reviewer ruling on escalated item 2: the fixture-based evidence satisfies the criterion's substance. The live checkpoint is owned by the enclosing orchestration, was mid-run (failing for unrelated reasons), and modifying it was forbidden; the approved plan's [P5-T2] fixture branch anticipated exactly this. The path substitution does not alter the code path exercised |
| 13 | No unintended changes outside scope | PASS | Diff contains exactly: the three Files-In-Scope code/test files, the feature folder content, and the promotion record `docs/features/potential/promoted/2026-07-25-orchestrator-completion-hook-false-block.md` (standard promotion-lifecycle artifact from the docs commit `a587c0c6`). No Python validator, complexity-floor, `ModelRouting.psm1`, `.codex/`, `OrchestratorStateCompletion.psm1`, runsettings, lockfile, or policy file appears | `git diff --name-status 72126592..HEAD` (reviewer-run) | The promotion record is intended lifecycle content, noted for transparency; not scope creep |

---

## Summary

**Overall Feature Readiness:** PASS

**Criteria summary:**
- **PASS:** 13 criteria
- **PARTIAL:** 0 criteria
- **UNVERIFIED:** 0 criteria
- **FAIL:** 0 criteria

**Rulings on the two escalated items:**

1. **AC11 (branch coverage sub-clause): accepted as not measurable; AC11 evaluated PASS and checked off.** Verified independently against the emitted report: `artifacts/pester/powershell-coverage.xml` contains no `BRANCH` counter at any level (counter census: 265 INSTRUCTION, 265 LINE, 265 METHOD, 70 CLASS, 0 BRANCH), and per-line `mb`/`cb` attributes are uniformly zero. The executor's claim is accurate, the limitation predates this change (precedent: completed feature #272), and every measurable sub-clause passes with margin (line 90.22% repo-wide, 92.16% per-file, changed line fully covered, single clean toolchain pass). Recorded as an approved exception in `policy-audit.2026-07-25T17-54.md` section 8, with a non-blocking recommendation to pursue branch-capable PowerShell coverage tooling.
2. **AC12 (fixture-based primary evidence): accepted; AC12 remains checked.** The criterion's substance — the fixed hook exercised end-to-end through its real subprocess seam, exiting 0 against a checkpoint independently proven to pass `--require-complete --require-model-routing` — is fully met and was reproduced by this reviewer. The literal-path deviation was sanctioned by the approved plan's [P5-T2] fixture branch and forced by the ownership constraint on the live mid-run checkpoint, which this execution correctly treated as read-only. The converse (fail-closed against the genuinely failing live checkpoint) was also reproduced by this reviewer.

**Top gaps preventing PASS:**

1. None.

**Recommended follow-up verification steps:**

1. Non-blocking: file a tooling issue to evaluate a branch-coverage-capable PowerShell coverage path so the 75% branch gate becomes measurable.
2. At PR time, standard CI green gate on the branch head (no workflow files changed on this branch, so `modified-workflow-needs-green-run` does not apply).

---

## Acceptance Criteria Check-off

Per the acceptance-criteria tracking rules:
- Criteria evaluated as **PASS** may be checked off in the authoritative source file(s) if not already checked.
- No criterion was evaluated PARTIAL, FAIL, or UNVERIFIED.

**Check-off performed by this reviewer:** AC11 (the toolchain/coverage criterion) was changed from `- [ ]` to `- [x]` in `spec.md` following the PASS ruling above. All other 12 criteria were already checked by the executor and were verified rather than re-checked.

### AC Status Summary

- Source: `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/spec.md`
- Total AC items: 13
- Checked off (delivered): 13
- Remaining (unchecked): 0
- Items remaining: None.

| Source File | Total AC | Checked (PASS) | Unchecked | Notes |
|-------------|----------|----------------|-----------|-------|
| `docs/features/active/2026-07-25-orchestrator-completion-hook-false-block-413/spec.md` | 13 | 13 | 0 | Checkbox-backed; AC11 checked off by this reviewer per the adjudication above |
