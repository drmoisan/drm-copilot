# Remediation Plan — legacy-discovery-init-templates (#362), Remediation Cycle 4

- Timestamp: 2026-07-18T19-30
- Issue: #362
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/issue.md`)
- Branch: `feature/legacy-discovery-init-templates-362`
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/remediation-inputs.2026-07-18T19-30.md` (single Blocking finding R1 — PR #380 `mergeable: CONFLICTING` again against base `epic/legacy-discovery-and-parity-integration`, conflicted file `pyproject.toml`, this time colliding with newly fanned-in sibling feature #363). No scope beyond that document is authorized.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-init-templates-362`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`). No `artifacts/`-rooted evidence path is permitted. Do not overwrite prior cycles' `evidence/qa-gates/phase6-*`, `evidence/qa-gates/r1c1-*`, `evidence/qa-gates/r2c1-*`, `evidence/qa-gates/r3c1-*` files, or `evidence/remediation-baseline/r1c1-*`, `evidence/remediation-baseline/r2c1-*` files. All new artifacts for this cycle use the `r4c1-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- All commands run from the repository root `C:\Users\DanMoisan\repos\drm-copilot\.claude\worktrees\agent-ae68d14e27bb8727e` unless the task states otherwise. Shell is PowerShell unless a task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- Conflicted region: `pyproject.toml`, `[tool.poetry.scripts]` block, the contiguous run of `"dev.discovery.*"` alias lines that begins immediately after `"dev.collect-commit-context" = "scripts.dev_tools.collect_commit_context:main"`.
- Confirmed-so-far divergent lines (per `remediation-inputs.2026-07-18T19-30.md`): this feature branch (HEAD) carries `"dev.discovery.init" = "scripts.dev_tools.discovery.init_cli:main"`; the integration branch (`origin/epic/legacy-discovery-and-parity-integration`, via fan-in of sibling feature #363 commit `054eaa06`) added `"dev.discovery.inventory" = "scripts.dev_tools.discovery.analyzer.cli:main"` at the same insertion point. Both are independently valid additions; neither line may be dropped.
- Per the remediation-inputs document, this is the second consecutive cycle in which the integration branch advanced with a new sibling and reopened this same conflict shape (cycle 2's conflicting sibling was #364, which has since been fanned into this feature branch itself and is no longer a source of conflict). Phase 0 of this plan therefore requires re-enumerating the full divergent-line set directly from `origin/epic/legacy-discovery-and-parity-integration`'s current `pyproject.toml` rather than relying solely on the `dev.discovery.init` / `dev.discovery.inventory` pair named in the headline finding, because further siblings may have added additional `dev.discovery.*` (or other) lines to the same region, or elsewhere in `pyproject.toml`, since the remediation-inputs capture was taken.
- No line already present in either HEAD's or the integration branch's `[tool.poetry.scripts]` block (or any other block in `pyproject.toml`) may be dropped, edited, or duplicated by this remediation. The only permitted structural change within `[tool.poetry.scripts]` is removing the conflict-marker lines (`<<<<<<<`, `=======`, `>>>>>>>`) and reordering the contiguous `"dev.discovery.*"` run into strict alphabetical order by full quoted key string; no other line in `[tool.poetry.scripts]` (or elsewhere in the file) may be reordered, edited, or removed.
- Because the epic branch has advanced with a new sibling fan-in twice in a row since this plan's cycle-2 predecessor, Phase 1 includes an immediately-pre-commit re-check of `gh pr view 380 --json mergeable` state relative to the fetched tip, to reduce (not eliminate) the chance of a third recurrence caused by a further sibling landing between plan approval and merge-commit creation.

### Phase 0 — Baseline Capture and Divergent-Line Enumeration

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated.
- [x] [P0-T2] Run `git fetch origin epic/legacy-discovery-and-parity-integration` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-fetch.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the resulting SHA of `origin/epic/legacy-discovery-and-parity-integration`. Acceptance: exit code 0 and the artifact records the fetched SHA.
- [x] [P0-T3] Enumerate the true full set of divergent `[tool.poetry.scripts]` lines (and confirm no other block in `pyproject.toml` diverges outside the `[tool.coverage.report] exclude_lines` addition already known to merge cleanly): run `git merge-base HEAD origin/epic/legacy-discovery-and-parity-integration` to obtain `<merge-base>`, then run `git show <merge-base>:pyproject.toml`, `git show HEAD:pyproject.toml`, and `git show origin/epic/legacy-discovery-and-parity-integration:pyproject.toml`, each piped to `Select-String -Pattern '"dev\.discovery\.'` to isolate the `dev.discovery.*` run, and separately run `git diff <merge-base> origin/epic/legacy-discovery-and-parity-integration -- pyproject.toml` to confirm the full set of hunks the integration side has added since the merge base (not just the headline pair). Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-pyproject-scripts-enumeration.<TS>.md` with `Timestamp:`, the four commands and their verbatim output, and an explicit derived list of (a) lines present on HEAD but absent at the merge base, (b) lines present on the integration branch but absent at the merge base, and (c) any line present in both derived sets with an identical key (expected: none). Acceptance: the artifact's derived list (a) includes `"dev.discovery.init"`, the artifact's derived list (b) includes `"dev.discovery.inventory"`, and the artifact explicitly states whether any further lines beyond those two were found in either derived list or elsewhere in the file's diff.
- [x] [P0-T4] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-black-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T5] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-ruff-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T6] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-pyright-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T7] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that records the numeric pre-merge baseline line-coverage and branch-coverage percentages and the pass/fail/skip counts. Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid).

### Phase 1 — Merge the Integration Branch and Resolve the `pyproject.toml` Conflict

- [x] [P1-T1] Run `git merge --no-commit --no-ff origin/epic/legacy-discovery-and-parity-integration` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase1-merge-attempt.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the conflict result. Follow with `git diff --name-only --diff-filter=U` and record its output in the same artifact. Acceptance: the conflicted-file list is exactly `pyproject.toml` (no other file). If any other file is also conflicted, stop and do not proceed to P1-T2; that outcome is out of this plan's authorized scope and must be escalated as a new finding rather than resolved ad hoc.
- [x] [P1-T2] Resolve the `pyproject.toml` conflict by editing the conflicted region directly: remove the conflict-marker lines (`<<<<<<<`, `=======`, `>>>>>>>`); keep every `"dev.discovery.*"` line identified in P0-T3's derived lists (a) and (b) with no line dropped and no line duplicated; reorder the resulting contiguous `"dev.discovery.*"` run into strict alphabetical order by full quoted key string (for example `"dev.discovery.init"` sorts before `"dev.discovery.inventory"`, which sorts before `"dev.discovery.profile"`); leave every other line in `[tool.poetry.scripts]` unedited and in its pre-merge relative position. Acceptance (all must hold): `Select-String -Pattern '<<<<<<<|=======|>>>>>>>' pyproject.toml` returns no matches; the contiguous `"dev.discovery.*"` line block, when extracted via `Select-String -Pattern '"dev\.discovery\.' pyproject.toml`, is identical to the same list sorted alphabetically by key; every key recorded in P0-T3's derived lists (a) and (b) appears exactly once in the resolved file.
- [x] [P1-T3] Verify no duplicate keys exist anywhere in the resolved `[tool.poetry.scripts]` block (not only the `"dev.discovery.*"` run): extract every key in the block (all lines between the `[tool.poetry.scripts]` heading and the next `[tool.` heading that assign a value) and confirm no key string appears more than once. Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase1-duplicate-key-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact records zero duplicate keys.
- [x] [P1-T4] Confirm the sibling's clean (non-conflicting) `[tool.coverage.report] exclude_lines` addition (`"^\\s*\\.\\.\\.\\s*$"`, from commit `054eaa06`) is present in the resolved file: run `Select-String -Pattern '\\.\\.\\.\\\\s\\*\\$' pyproject.toml` (or an equivalent literal search for the exact exclude-line string) against the resolved `[tool.coverage.report]` block. Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase1-coverage-exclude-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact confirms the entry is present exactly once.
- [x] [P1-T5] Run `poetry check` against the resolved `pyproject.toml` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase1-poetry-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit code 0.

### Phase 2 — Final QA Loop (Python, Post-Merge)

- [x] [P2-T1] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P2-T1. Acceptance: exit 0 with no files changed, recorded in the artifact.
- [x] [P2-T2] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P2-T1. Acceptance: exit 0 recorded in the artifact.
- [x] [P2-T3] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with 0 errors recorded in the artifact.
- [x] [P2-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-merge line-coverage and branch-coverage percentages and the pass/fail/skip counts for the merged tree (this feature's code plus the integration branch's code, including sibling feature #363). If this stage fails, fix, then restart the loop from P2-T1. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid).
- [x] [P2-T5] Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-coverage-delta.<TS>.md` comparing the Phase 0 pre-merge baseline coverage (from `evidence/remediation-baseline/r4c1-phase0-pytest-baseline.<TS>.md`) against the P2-T4 post-merge coverage, reporting: baseline line/branch %, post-merge line/branch %, and confirmation that line coverage >= 85% and branch coverage >= 75% hold on the merged tree. Acceptance: the artifact contains all numeric values and both threshold checks pass.

### Phase 3 — Commit the Merge, Push, and Confirm Mergeability

- [x] [P3-T1] Immediately before committing, re-verify the integration branch tip has not advanced past the SHA fetched in P0-T2: run `git fetch origin epic/legacy-discovery-and-parity-integration` again and compare the resulting SHA to the SHA recorded in `r4c1-phase0-fetch.<TS>.md`. Write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-phase3-pre-commit-tip-recheck.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: if the SHA is unchanged, proceed to P3-T2. If the SHA has advanced, stop, abort the in-progress merge (`git merge --abort`), and escalate as a new finding rather than proceeding with a merge base that is already stale.
- [x] [P3-T2] Stage the resolved `pyproject.toml` and any other files the merge auto-resolved, then commit the merge as a genuine two-parent merge commit: `git add pyproject.toml`, then `git add -A` to pick up any remaining auto-merged files, then `git commit --no-edit` (do not use `--squash`, `git merge --squash`, or `git rebase`; the commit must record both HEAD and `origin/epic/legacy-discovery-and-parity-integration` as parents). Acceptance: `git log -1 --format=%P` on the new commit lists exactly two parent hashes, and `git diff --name-only --diff-filter=U` is empty.
- [x] [P3-T3] Stage and commit this cycle's plan-checklist updates and all Phase 0–2 evidence artifacts (`docs/features/active/2026-07-17-legacy-discovery-init-templates-362/remediation-plan.2026-07-18T19-30.md` and every new file under `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/remediation-baseline/r4c1-*` and `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-*`) in a follow-up commit with a professional, factual commit message referencing issue #362 remediation cycle 4. Acceptance: `git status --porcelain` is empty.
- [ ] [P3-T4] Push the updated branch: `git push origin feature/legacy-discovery-init-templates-362`. Acceptance: the command exits 0 and the remote branch's HEAD matches the local branch's HEAD (verify with `git rev-parse HEAD` and `git rev-parse origin/feature/legacy-discovery-init-templates-362` after the push).
- [ ] [P3-T5] Confirm PR #380 is no longer conflicting: run `gh pr view 380 --json mergeable` and write `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/r4c1-pr-mergeable-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the recorded `mergeable` value is `MERGEABLE` (not `CONFLICTING` or `UNKNOWN`).

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; the `pyproject.toml` conflict is resolved with every divergent `"dev.discovery.*"` line from both branches retained (including `"dev.discovery.init"` and `"dev.discovery.inventory"` and any further lines discovered during P0-T3's re-enumeration), alphabetically ordered within that contiguous run, and no other line altered; the merge is recorded as a genuine two-parent merge commit; fresh QA evidence exists under `docs/features/active/2026-07-17-legacy-discovery-init-templates-362/evidence/qa-gates/` with the `r4c1-` prefix without overwriting `phase6-*`, `r1c1-*`, `r2c1-*`, or `r3c1-*` evidence; the branch is pushed; and `gh pr view 380 --json mergeable` reports `MERGEABLE`.
