# 2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level (Plan)

- **Issue:** #575
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-28T09-30
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** full-bug (AC source: `spec.md` AC1-AC17 only; `user-story.md` intentionally absent)
- **Research:** `research/2026-08-28T15-00-release-poll-budgets-and-isolation-research.md`

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for PowerShell (the sole in-scope language). If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the outcome must be reported as remediation-required, never PASS.

**Evidence accounting rule:** Every evidence-producing task below names its canonical `evidence/<kind>/` destination under this feature folder. Do not mark evidence-backed work complete without the artifact.

**Scope summary (from `spec.md`):** Gap 1 is test-only (no production `.ps1` file changes): a new sibling Pester file pins the three call-site polling budgets that `Invoke-ReleaseTagPushGuarded` forwards to `Invoke-TagPublishVerification`. Gap 2 is documentation/evidence-only: an append-only, dated correction section is added to issue #526's `spec.md` near its existing AC21 (never rewriting AC21's original sentence or checkbox), plus a fresh evidence artifact in this feature's own `evidence/` tree. No file under `scripts/dev-tools/Invoke-ReleaseVerification.ps1`, `scripts/dev-tools/Invoke-ReleaseVerificationHelpers.ps1`, or `.github/workflows/publish-mcp-npm.yml` is touched (AC17).

All commands below assume the current working directory is the repository root.

---

### Phase 0 — Baseline

- [x] [P0-T1] Read `CLAUDE.md` at the repository root in full. Acceptance: `Test-Path -Path CLAUDE.md` returns `True`.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md` in full. Acceptance: `Test-Path -Path .claude/rules/general-code-change.md` returns `True`.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` in full. Acceptance: `Test-Path -Path .claude/rules/general-unit-test.md` returns `True`.
- [x] [P0-T4] Read `.claude/rules/powershell.md` in full (sole language in scope; no Python, TypeScript, or C# rule file applies). Acceptance: `Test-Path -Path .claude/rules/powershell.md` returns `True`.
- [x] [P0-T5] Write the Phase 0 policy-read evidence artifact at `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/evidence/baseline/phase0-instructions-read.<execution-timestamp>.md` (ISO-8601 timestamp per `evidence-and-timestamp-conventions`), containing `Timestamp:`, a `Policy Order:` field listing the four files read in P0-T1 through P0-T4 in that order, and an explicit confirmation line for each file. Acceptance: `git grep --no-index -c -F "Policy Order:" -- "docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/evidence/baseline/phase0-instructions-read.*.md"` returns `1`. (Quoted token: `Policy Order:`.)
- [x] [P0-T6] Run the self-hosted PoshQC format baseline, scoped to the two folders this fix touches, to confirm no pre-existing drift: `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCFormat -Root (Get-Location).Path -ScanFolders @('scripts/dev-tools','tests/scripts/dev-tools') *>&1`. Write the captured output to `evidence/baseline/poshqc-format-baseline.<execution-timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: `EXIT_CODE` is `0`, and the captured output contains zero lines matching the pattern `^Formatted: ` (no file was rewritten — every scanned file reports the literal per-file line `Already formatted: `, which the source of `Invoke-PoshQCFormat` in `scripts/powershell/PoshQC/PoshQC.Analyzer.psm1:64` prints for an unchanged file). (Quoted token: `Already formatted: `.)
- [x] [P0-T7] Run the self-hosted PoshQC analyze baseline, same scope: `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCAnalyze -Root (Get-Location).Path -ScanFolders @('scripts/dev-tools','tests/scripts/dev-tools') *>&1`. Write output to `evidence/baseline/poshqc-analyze-baseline.<execution-timestamp>.md` with the four required fields. Acceptance: `EXIT_CODE` is `0`, and the output contains a line matching the prefix `PSScriptAnalyzer passed: no findings under` (the literal success message printed by `Invoke-PoshQCAnalyze` at `scripts/powershell/PoshQC/PoshQC.Analyzer.psm1:185`). (Quoted token: `PSScriptAnalyzer passed: no findings under`.)
- [x] [P0-T8] Run the self-hosted PoshQC test-with-coverage baseline, full repository (per the known MCP-runner settings constraint recorded in the research, prefer the self-hosted invocation over the MCP tool for this coverage-bearing step): `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest -Root (Get-Location).Path *>&1`. Write output to `evidence/baseline/poshqc-test-baseline.<execution-timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording (a) the `Tests Passed: X, Failed: Y, Skipped: Z, Inconclusive: W, NotRun: V` line printed by `Invoke-PoshQCTest` (`scripts/powershell/PoshQC/PoshQC.Testing.psm1:423-428`) verbatim, (b) the numeric overall repository line-coverage percentage from the coverage report, and (c) the numeric line-coverage percentage specifically for `scripts/dev-tools/Invoke-ReleaseTagPush.ps1`. Acceptance: `EXIT_CODE` is `0`, and the captured `Failed:` value in the counts line is `0`. (Quoted token: `Tests Passed:`.)
- [x] [P0-T9] Confirm the current line count of `tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1`, which establishes the file-size constraint driving the new-sibling-file design (spec.md "Dependencies or blocked work"): `(Get-Content -Path tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1).Count`. Acceptance: the printed value is exactly `497`.

---

### Phase 1 — Gap 1: call-site polling-budget test (test-only)

- [x] [P1-T1] Create the new file `tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1`. It must: (a) in a `BeforeAll`, dot-source `scripts/dev-tools/Invoke-ReleaseTagPush.ps1` with `-ConfirmToken 'no'`, mirroring `tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1:4-11`; (b) in a `BeforeEach`, mock exactly `Test-Path` (return `$true`), `Get-NpmVersion` (mcp-server manifest path → `"0.0.2"`, extension manifest path → `"0.0.3"`, matching `Invoke-ReleaseTagPush.Tests.ps1:116-120`'s pattern), `Invoke-GitExe` (return `@{ Output = @(); ExitCode = 0 }` for every git argument vector), `Invoke-Sleep` (no-op), and `Invoke-NpmExe` (return `@{ Output = @('npm error code E404'); ExitCode = 1 }` on every call, so the real, unmocked `Test-NpmVersionResolved` always reports `$false` — this both satisfies the pre-push guard's "not yet consumed" requirement and drives check (c)'s full budget when a test reaches it); (c) contain **no** line `Mock -CommandName Invoke-TagPublishVerification` anywhere in the file, so the real composition/forwarding logic in `Invoke-TagPublishVerification` executes; (d) declare `Describe "Invoke-ReleaseTagPush.ps1 - call-site polling budget forwarding"` containing `Context "call-site budget forwarding to Invoke-TagPublishVerification"` with exactly three `It` blocks, named exactly:
  - `"forwards the check (a) interval and attempt budget to Wait-ForWorkflowRun"` — registers `Mock -CommandName Wait-ForWorkflowRun -ParameterFilter { $IntervalSeconds -eq 10 -and $MaxAttempts -eq 18 } -MockWith { 'NO_RUN' }`, invokes `Invoke-ReleaseTagPushGuarded -ConfirmToken 'yes' -RepoRoot '/repo'`, and asserts `Should -Invoke -CommandName Wait-ForWorkflowRun -Times 1 -Exactly -ParameterFilter { $IntervalSeconds -eq 10 -and $MaxAttempts -eq 18 }`.
  - `"forwards the check (b) interval and attempt budget to Test-PublishStepConclusion"` — registers a permissive `Mock -CommandName Wait-ForWorkflowRun -MockWith { '4242' }` plus `Mock -CommandName Test-PublishStepConclusion -ParameterFilter { $IntervalSeconds -eq 20 -and $MaxAttempts -eq 60 } -MockWith { 'STEP_SKIPPED' }`, invokes the guarded function the same way, and asserts `Should -Invoke -CommandName Test-PublishStepConclusion -Times 1 -Exactly -ParameterFilter { $IntervalSeconds -eq 20 -and $MaxAttempts -eq 60 }`.
  - `"polls the registry with the check (c) interval and attempt budget"` — registers `Mock -CommandName Wait-ForWorkflowRun -MockWith { '4242' }` and `Mock -CommandName Test-PublishStepConclusion -MockWith { 'SUCCESS' }`, invokes the guarded function the same way (`Invoke-ReleaseTagPushGuarded`, not `Invoke-TagPublishVerification` directly), and asserts `Should -Invoke -CommandName Invoke-NpmExe -Times 41 -Exactly` and `Should -Invoke -CommandName Invoke-Sleep -Times 39 -Exactly -ParameterFilter { $Seconds -eq 15 }`.

  Note on the 41 count: unlike the directly-invoked `Invoke-TagPublishVerification` scenario at `tests/scripts/dev-tools/Invoke-ReleaseVerification.Tests.ps1:350-362` (which asserts 40 calls), this `It` block invokes `Invoke-ReleaseTagPushGuarded`, whose pre-push guard at `scripts/dev-tools/Invoke-ReleaseTagPush.ps1` line 198 (`if (Test-NpmVersionResolved -Version $mcpVersion) { ... }`) runs unconditionally before the tag-and-verification path. Because this task's `BeforeEach` (item (b) above) deliberately leaves `Test-NpmVersionResolved` real and unmocked while mocking only `Invoke-NpmExe` beneath it, that guard call contributes exactly one `Invoke-NpmExe` call before the check-(c) loop inside `Invoke-TagPublishVerification` runs its own full 40-attempt budget. The total is therefore 41, not 40. `Invoke-Sleep` stays at 39 because the guard call performs no sleep — only the check-(c) loop sleeps between attempts.

  This task creates the three `It` block names verbatim (each carries the literal `It` keyword required by Pester syntax immediately before its name string, so the full name text searched for below includes that prefix). None of the following three strings exists anywhere in the tracked tree before this task writes the new file; the task's job is to author a file containing all three: `It forwards the check (a) interval and attempt budget to Wait-ForWorkflowRun`, `It forwards the check (b) interval and attempt budget to Test-PublishStepConclusion`, and `It polls the registry with the check (c) interval and attempt budget`.

  Acceptance (run from the repository root after the file is written):
  - `Test-Path -LiteralPath tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` returns `True`.
  - `git grep --no-index -F "Mock -CommandName Invoke-TagPublishVerification" -- tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` exits `1` (no match — constraint (c) above). (Quoted token: `Mock -CommandName Invoke-TagPublishVerification`.)
  - `git grep --no-index -c -F "It ""forwards the check (a) interval and attempt budget to Wait-ForWorkflowRun""" -- tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` returns `1`. (Quoted token: `forwards the check (a) interval and attempt budget to Wait-ForWorkflowRun`.)
  - `git grep --no-index -c -F "It ""forwards the check (b) interval and attempt budget to Test-PublishStepConclusion""" -- tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` returns `1`. (Quoted token: `forwards the check (b) interval and attempt budget to Test-PublishStepConclusion`.)
  - `git grep --no-index -c -F "It ""polls the registry with the check (c) interval and attempt budget""" -- tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1` returns `1`. (Quoted token: `polls the registry with the check (c) interval and attempt budget`.)
  - `(Get-Content -Path tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1).Count` is less than or equal to `500`.

- [x] [P1-T2] Run the new test file in isolation: `Invoke-Pester -Path './tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1' -PassThru | Select-Object TotalCount, PassedCount, FailedCount | Format-List`. Write the captured output to `evidence/regression-testing/gap1-new-test-pass.<execution-timestamp>.md` with the four required fields. Acceptance: `TotalCount` is `3`, `PassedCount` is `3`, `FailedCount` is `0`.
- [x] [P1-T3] Confirm `tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1` (21 `It` blocks) still passes unmodified after the new sibling file is added (AC5), and that the file itself carries zero edits: `Invoke-Pester -Path './tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1' -PassThru | Select-Object TotalCount, PassedCount, FailedCount | Format-List` followed by `git diff main -- tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1` and, as the paired porcelain-status companion, `git status --porcelain -- tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1`. Write the captured output of all three commands to `evidence/regression-testing/existing-suite-unmodified-green.<execution-timestamp>.md` with the four required fields. Acceptance: `TotalCount` is `21`, `PassedCount` is `21`, `FailedCount` is `0`; both the `git diff main` and `git status --porcelain` invocations produce empty output.
- [x] [P1-T4] Check off AC1 through AC6 in `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` (change `- [ ] AC<N> —` to `- [x] AC<N> —` for N in 1..6, preserving all criterion text unchanged per `acceptance-criteria-tracking`). Acceptance: each of the following returns `1`: `git grep --no-index -c -F "- [x] AC1 —" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md`; the same command with `AC2 —` through `AC6 —` substituted in turn.

---

### Phase 2 — Gap 2: AC21 isolation-evidence correction (documentation/evidence-only)

- [x] [P2-T1] Reproduce the raw-socket-vs-proxy-isolation probe from inside a proxy-redirected session, in one `pwsh` invocation:
  ```powershell
  $env:HTTP_PROXY = 'http://127.0.0.1:9'
  $env:HTTPS_PROXY = 'http://127.0.0.1:9'
  $env:ALL_PROXY = 'http://127.0.0.1:9'
  $env:NPM_CONFIG_REGISTRY = 'http://127.0.0.1:9'
  $connected = $false
  try {
      $client = [System.Net.Sockets.TcpClient]::new()
      $client.Connect('registry.npmjs.org', 443)
      $connected = $client.Connected
      $client.Close()
  } catch {
      $connected = $false
  }
  Write-Output "RAW_SOCKET_CONNECTED=$connected"
  ```
  This reproduces the `#526` reaudit's R4 probe (`policy-audit.2026-08-26T04-33.md:395-406`), which recorded a successful raw-socket connection from inside the same proxy-isolated session class on this same Windows 11 workstation environment. Acceptance: the command prints a line beginning `RAW_SOCKET_CONNECTED=`. If the printed value is `RAW_SOCKET_CONNECTED=True`, proceed to P2-T3/P2-T4 as written. If it is instead `RAW_SOCKET_CONNECTED=False` (registry unreachable from this execution environment), stop Gap 2 work, record the discrepancy in the evidence artifact from P2-T3, and flag it as a blocking finding for review before authoring the P2-T4 correction text — do not author a correction claiming raw-socket reachability that was not observed in this run. (Quoted token: `RAW_SOCKET_CONNECTED=True`.)
- [x] [P2-T2] Reaffirm AC21 clause 2 (no test added or modified by #526 invokes `npm`, `gh`, or `git` as a real external process) across all four release-verification test files, including the new one from Phase 1: `git grep -nE '&\s+(npm|gh|git)\s' -- tests/scripts/dev-tools/Invoke-ReleaseVerification.Tests.ps1 tests/scripts/dev-tools/Invoke-ReleaseVerificationHelpers.Tests.ps1 tests/scripts/dev-tools/Invoke-ReleaseTagPush.Tests.ps1 tests/scripts/dev-tools/Invoke-ReleaseTagPushCallSiteBudgets.Tests.ps1`. Acceptance: the command exits `1` (no match across any of the four files).
- [x] [P2-T3] Write the Gap 2 evidence artifact at `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/evidence/qa-gates/proxy-level-isolation-correction.<execution-timestamp>.md`, containing `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` blocks for both P2-T1 and P2-T2, plus a stated corrected claim: "the complete Pester suite passes with all proxy-aware network clients redirected to an unreachable discard endpoint; a raw socket is not blocked by this mechanism," and the reaffirmed clause-2 result from P2-T2. Acceptance: `Get-ChildItem -Path 'docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/evidence/qa-gates/proxy-level-isolation-correction.*.md' | Select-String -Pattern 'RAW_SOCKET_CONNECTED='` returns at least one match.
- [x] [P2-T4] Append the correction section to `docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md`, after the document's existing final line (currently line 486; do not insert at or before line 417, where AC21 lives). Append exactly the heading `## Correction — 2026-08-28 (issue #575)` followed by prose stating: the original claim ("no network access available"), the mechanism actually verified ("proxy-aware network clients ... redirected to an unreachable discard endpoint; a raw socket is not blocked by this mechanism"), the literal token `#575`, and a pointer to the evidence artifact written in P2-T3 — following the append-only pattern of `docs/features/completed/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/documentation-corrections.2026-08-20T20-02.md` (P5-T3). Do not modify the existing AC21 line or its checkbox. Acceptance (run from the repository root):
  - `git status --porcelain -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` shows the file as modified (a line beginning ` M `).
  - `git diff main -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` produces at least one line beginning with a single `+` (not `+++`).
  - The same anchored `git diff main -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` produces zero lines beginning with a single `-` (not `---`) — confirming the edit is append-only.
  - `git grep --no-index -n -F "## Correction — 2026-08-28 (issue #575)" -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` returns exactly one match. (Quoted token: `## Correction — 2026-08-28 (issue #575)`.)
  - `git grep --no-index -c -F "redirected to an unreachable discard endpoint" -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` returns `1`. (Quoted token: `redirected to an unreachable discard endpoint`.)
  - `git grep --no-index -c -F "a raw socket is not blocked by this mechanism" -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` returns `1`. (Quoted token: `a raw socket is not blocked by this mechanism`.)
  - `git grep --no-index -n -F "- [x] AC21 — The complete Pester suite passes with no network access available" -- docs/features/active/2026-08-23-tag-push-can-silently-skip-npm-publish-526/spec.md` returns a match at line `417` and no other line, confirming the original AC21 line and its `[x]` checkbox are byte-for-byte unchanged. (Quoted token: `- [x] AC21 — The complete Pester suite passes with no network access available`.)
- [x] [P2-T5] Verify AC16's required follow-up content already exists in this feature's own `spec.md` "Rollout & Follow-up" section (no new authoring needed — it was captured at spec-authoring time), then check off AC16. The section must contain the literal phrase `Test-PublishStepConclusion's own default parameter values`; if that exact phrase is not already present verbatim, this task adds it to the "Rollout & Follow-up" section as part of the verification, so the phrase exists in `spec.md` when this task completes. Acceptance: `git grep --no-index -c -F "Test-PublishStepConclusion's own default parameter values" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` returns `1`, followed by `git grep --no-index -c -F "- [x] AC16 —" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` returning `1` after the checkbox edit.
- [x] [P2-T6] Check off AC11 through AC15 in `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` (change `- [ ] AC<N> —` to `- [x] AC<N> —` for N in 11..15). Acceptance: each of the following returns `1`: `git grep --no-index -c -F "- [x] AC11 —" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md`; the same command with `AC12 —` through `AC15 —` substituted in turn.

---

### Phase 3 — Final QC (full PowerShell toolchain loop)

Per `.claude/rules/powershell.md` and `.claude/rules/general-code-change.md`, run format → analyze → test (with coverage) in order, restarting from format if any stage fails or auto-fixes files, until a single consecutive clean pass completes. Type checking does not apply to PowerShell. These tasks are unconditional; `EXIT_CODE: SKIPPED` is not a valid outcome for any of them.

- [ ] [P3-T1] Full-repository PoshQC format pass: `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCFormat -Root (Get-Location).Path *>&1`. If the output contains any line matching `^Formatted: ` (a file was rewritten), restart the loop from this task after re-running analyze and test, per the Mandatory Toolchain Loop, until a pass with zero rewrites is captured. Write the final clean-pass output to `evidence/qa-gates/final-format.<execution-timestamp>.md` with the four required fields. Acceptance: `EXIT_CODE` is `0` and the captured output contains zero lines matching `^Formatted: `, and at least one line matching `^Already formatted: .*Invoke-ReleaseTagPushCallSiteBudgets\.Tests\.ps1$` (the new file converged to already-formatted). (Quoted token: `Already formatted: `.)
- [ ] [P3-T2] Full-repository PoshQC analyze pass: `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCAnalyze -Root (Get-Location).Path *>&1`. Write output to `evidence/qa-gates/final-analyze.<execution-timestamp>.md` with the four required fields. Acceptance: `EXIT_CODE` is `0` and the output contains a line matching the prefix `PSScriptAnalyzer passed: no findings under`. (Quoted token: `PSScriptAnalyzer passed: no findings under`.)
- [ ] [P3-T3] Full-repository PoshQC test-with-coverage pass: `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force; Invoke-PoshQCTest -Root (Get-Location).Path *>&1`. Write output to `evidence/qa-gates/final-test-coverage.<execution-timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording (a) the `Tests Passed: X, Failed: Y, ...` line verbatim, (b) the post-change numeric overall repository line-coverage percentage, and (c) the post-change numeric line-coverage percentage for `scripts/dev-tools/Invoke-ReleaseTagPush.ps1`, compared against the P0-T8 baseline values. Acceptance: `EXIT_CODE` is `0`; `Failed:` is `0`; the overall line-coverage percentage is `>= 85`; and `scripts/dev-tools/Invoke-ReleaseTagPush.ps1`'s line-coverage percentage is not lower than its P0-T8 baseline value.
- [ ] [P3-T4] Verify AC17 (the diff modifies no line of `scripts/dev-tools/Invoke-ReleaseVerification.ps1`, `scripts/dev-tools/Invoke-ReleaseVerificationHelpers.ps1`, or `.github/workflows/publish-mcp-npm.yml`): `git diff main --name-only -- scripts/dev-tools/Invoke-ReleaseVerification.ps1 scripts/dev-tools/Invoke-ReleaseVerificationHelpers.ps1 .github/workflows/publish-mcp-npm.yml`, paired with `git status --porcelain -- scripts/dev-tools/Invoke-ReleaseVerification.ps1 scripts/dev-tools/Invoke-ReleaseVerificationHelpers.ps1 .github/workflows/publish-mcp-npm.yml`. Write both outputs to `evidence/qa-gates/diff-scope-verification.<execution-timestamp>.md`. Acceptance: both commands produce empty output.
- [ ] [P3-T5] Check off AC7, AC8, AC9, AC10, and AC17 in `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md`. Acceptance: each of the following returns `1`: `git grep --no-index -c -F "- [x] AC7 —" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md`; the same command with `AC8 —`, `AC9 —`, `AC10 —`, and `AC17 —` substituted in turn.
- [ ] [P3-T6] Confirm all 17 acceptance criteria in `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` are checked off. Acceptance: `git grep --no-index -c -F "- [ ] AC" -- docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` returns `0`. (Quoted token: `- [ ] AC`.)

---

### Acceptance Criteria Status (to be reported at completion, per `acceptance-criteria-tracking`)

- Source: `docs/features/active/2026-08-28-release-poll-budgets-unpinned-and-isolation-evidence-proxy-level-575/spec.md` (AC1-AC17; sole AC source under `full-bug` work mode).
- Total AC items: 17.
- Checked off across Phase 1 (AC1-AC6), Phase 2 (AC11-AC16), and Phase 3 (AC7-AC10, AC17) as each phase's tasks pass verification.
