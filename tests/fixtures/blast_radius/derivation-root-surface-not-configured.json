{
  "description": "Gap 1 negative (issue #452). A separator-free token that is NOT a member of the config shared_surfaces list stays rejected: the plan cites `README.md` in inline code and the derived radius must not gain it. This pins the false-positive guard, because admitting any bare filename would widen extraction to every filename appearing in prose.",
  "input": {
    "plan_text": "### Phase 1 — Dependencies\n- [ ] [P1-T1] Update `README.md` and edit `scripts/dev_tools/a.py`.\n",
    "spec_text": "",
    "feature_folder": "2026-08-07-example-452",
    "computed_at": "2026-08-07T12-00",
    "tracked_file_count": 100,
    "config": {
      "version": 1,
      "shared_surfaces": ["config/orchestration-routing.json", "poetry.lock"],
      "shared_surface_globs": ["scripts/dev_tools/validate_*.py"],
      "modules": {
        "config": ["config/**"],
        "docs": ["docs/**"],
        "python-dev-tools": ["scripts/dev_tools/**"],
        "tests": ["tests/**"]
      },
      "over_breadth_fraction": 0.25
    }
  },
  "expected": {
    "radius": {
      "paths": [
        "docs/features/active/2026-08-07-example-452/**",
        "scripts/dev_tools/a.py"
      ],
      "modules": ["docs", "python-dev-tools"],
      "shared_surfaces": [],
      "contracts": [],
      "source": "derived",
      "computed_at": "2026-08-07T12-00"
    },
    "findings": []
  }
}
