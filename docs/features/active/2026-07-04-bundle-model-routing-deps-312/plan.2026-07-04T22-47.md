# bundle-model-routing-deps (Plan)

- **Issue:** #312
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-05T00-00
- **Status:** Draft
- **Version:** 1.0
- **Architecture:** B-mirror (PowerShell ports; Python retained as validator authority) per `research/2026-07-04T2300-bundle-model-routing-deps-powershell-research.md` Sections 3, 9.

**Fail-closed evidence rule:** This plan includes explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for every in-scope language where policy requires coverage (PowerShell and Python). If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or incomplete, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Each evidence-producing task records the exact artifact path. Do not mark evidence-backed work complete without the artifact present with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

**Evidence location invariant:** All evidence artifacts resolve under `docs/features/active/2026-07-04-bundle-model-routing-deps-312/evidence/<kind>/` (canonical scheme from `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`). Baseline evidence -> `evidence/baseline/`; final QA evidence -> `evidence/qa-gates/`. Non-canonical paths such as `artifacts/baselines/`, `artifacts/qa/`, or `artifacts/coverage/` are forbidden and must be rejected.

**Timestamp format:** All artifact filenames use ISO-8601 `yyyy-MM-ddTHH-mm`, shown below as `<TS>`. The executor substitutes the actual capture timestamp.

**Line-number caveat:** Offsets shift as edits are applied. Every cited line number below was verified at plan time against the repository; the executor MUST re-confirm the exact citation offset at edit time before each Markdown edit (`research/2026-07-04T2300-...powershell-research.md` Section 4.4).

**Scope guard (non-goals, enforced by P8-T5):** No change to Python modules (`scripts/dev_tools/compute_complexity_floor.py`, `scripts/dev_tools/resolve_delegation_model.py`, the three `_orchestrator_state_*` helpers, `scripts/dev_tools/validate_orchestrator_state.py`), the TypeScript validator port, `pyproject.toml`, `config/orchestration-routing.json`, or any file under `tests/scripts/dev_tools/`. `config/orchestration-routing.json` stays JSON in place. Python and PowerShell coexist pinned to the same config.

---

### Phase 0 — Baseline capture & policy reads

- [x] [P0-T1] Read policy files in required order and write the policy-read evidence artifact to `docs/features/active/2026-07-04-bundle-model-routing-deps-312/evidence/baseline/phase0-instructions-read.<TS>.md`. Required order: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/powershell.md`, (5) `.claude/rules/python.md`, (6) `.claude/rules/python-suppressions.md`, (7) `.claude/rules/self-explanatory-code-commenting.md`, (8) `.claude/rules/quality-tiers.md`, (9) `.claude/rules/orchestrator-state.md`. Also read skills `.claude/skills/atomic-plan-contract/SKILL.md` and `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Artifact MUST include `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: artifact exists with all three fields populated.
- [x] [P0-T2] Capture PowerShell format baseline. Command: `mcp__drm-copilot__run_poshqc_format` (Invoke-Formatter, check/report mode). Write `evidence/baseline/poshqc-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail and count of files needing formatting). Acceptance: artifact exists with all four fields.
- [x] [P0-T3] Capture PowerShell analyze baseline. Command: `mcp__drm-copilot__run_poshqc_analyze` (PSScriptAnalyzer, settings `scripts/powershell/PoshQC/settings/pssa.settings.psd1`). Write `evidence/baseline/poshqc-analyze.<TS>.md` with the four required fields; `Output Summary:` records the finding count. Acceptance: artifact exists with all four fields.
- [x] [P0-T4] Capture PowerShell test+coverage baseline. Command: `mcp__drm-copilot__run_poshqc_test` (Pester 5.x, settings `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, coverage enabled). Write `evidence/baseline/poshqc-test.<TS>.md` with the four required fields; `Output Summary:` MUST include the numeric baseline line-coverage % and branch-coverage % headline and the passed/failed test counts. Acceptance: artifact exists with numeric coverage headline values (not placeholders).
- [x] [P0-T5] Capture Python format baseline. Command: `poetry run black --check .`. Write `evidence/baseline/python-black.<TS>.md` with the four required fields. Acceptance: artifact exists with all four fields.
- [x] [P0-T6] Capture Python lint baseline. Command: `poetry run ruff check .`. Write `evidence/baseline/python-ruff.<TS>.md` with the four required fields; `Output Summary:` records the error count. Acceptance: artifact exists with all four fields.
- [x] [P0-T7] Capture Python type-check baseline. Command: `poetry run pyright`. Write `evidence/baseline/python-pyright.<TS>.md` with the four required fields; `Output Summary:` records the error/warning count. Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Capture Python test+coverage baseline. Command: `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `evidence/baseline/python-pytest.<TS>.md` with the four required fields; `Output Summary:` MUST include the numeric baseline line-coverage % and branch-coverage % headline, the passed/failed counts, and an explicit note that `tests/scripts/dev_tools/test_orchestration_routing_config_parity.py`, `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`, and `tests/scripts/dev_tools/test_push_down_claude_pack_selection.py` currently pass. Acceptance: artifact exists with numeric coverage headline values.

### Phase 1 — Create the PowerShell ModelRouting module

- [x] [P1-T1] Create `.claude/lib/model-routing/ModelRouting.psm1` and implement the advanced function `Get-ComplexityFloor` as a faithful port of `scripts/dev_tools/compute_complexity_floor.py` (lines 65-108). Requirements: `[CmdletBinding()]`, `[OutputType([string])]`, parameter `[Parameter(Mandatory = $true)] [AllowEmptyCollection()] [string[]] $SignalsPresent`; module-scope constants matching the Python module — `$script:BAND_ORDER = @('C1','C2','C3','C4')`, `$script:LOWEST_BAND = 'C1'` (line 56), `$script:FLOOR_CANDIDATE_BAND = 'C3'` (line 59), `$script:FLOOR_CEILING_BAND = 'C3'` (line 62); behavior — empty input returns `C1`, any present floor signal contributes candidate rank of `C3`, floor is the max triggered candidate clamped to at most `C3`, `C4` is never returned, order-independent; reads no file; comment-based help with `.SYNOPSIS`, `.DESCRIPTION`, `.PARAMETER`, `.OUTPUTS` per `.claude/rules/self-explanatory-code-commenting.md`; `Export-ModuleMember -Function Get-ComplexityFloor`. Acceptance (manual verify): `Import-Module` the module, then `Get-ComplexityFloor -SignalsPresent @()` returns `'C1'` and `Get-ComplexityFloor -SignalsPresent @('classifier_or_model_logic')` returns `'C3'`.
- [x] [P1-T2] Add the advanced function `Resolve-DelegationModel` to `.claude/lib/model-routing/ModelRouting.psm1` as a faithful port of `scripts/dev_tools/resolve_delegation_model.py` (lines 79-140). Requirements: `[CmdletBinding()]`, `[OutputType([hashtable])]`, parameters `[Parameter(Mandatory = $true)] [string] $Agent`, `[Parameter(Mandatory = $true)] [string] $Band`, `[Parameter(Mandatory = $true)] [string] $FablePolicy`; module-scope constants matching the Python module — `$script:BASE_COMPLEXITY_TO_MODEL = @{ C1='haiku'; C2='sonnet'; C3='opus'; C4='fable' }` (lines 62-67), `$script:DISABLED_POLICY='disabled'` (line 51), `$script:PREFERRED_POLICY='preferred'` (line 52), `$script:FABLE_MODEL='fable'` (line 55), `$script:DISABLED_CLAMP_MODEL='opus'` (line 57), `$script:DISABLED_CLAMP_REASON='fable_disabled'` (line 59), `$script:PREFERRED_OVERLAY_AGENTS = @('atomic-planner','prd-feature','feature-review','task-researcher')` (lines 71-73), `$script:PREFERRED_OVERLAY_BAND='C3'` (line 75), `$script:PREFERRED_OVERLAY_MODEL='fable'` (line 76); return a `[hashtable]` with keys `table_model`, `model`, `clamped_from`, `clamp_reason` mirroring the Python dict (lines 127-140); preferred overlay redirects only the `C3` cell to `fable` and only for the four overlay agents; disabled-mode clamp of a `fable` table cell returns `model='opus'`, `clamped_from='fable'`, `clamp_reason='fable_disabled'`; a band not present in the base table fails fast via `throw` (PowerShell analog of the Python `KeyError`, line 105); no clamp/overlay case returns `table_model` verbatim with `clamped_from=$null`, `clamp_reason=$null`; reads no file; comment-based help; keep the file under the 500-line limit; `Export-ModuleMember -Function Get-ComplexityFloor, Resolve-DelegationModel`. Acceptance (manual verify): `Resolve-DelegationModel -Agent 'atomic-executor' -Band 'C1' -FablePolicy 'available'` returns `model='haiku'`; `-Agent 'atomic-planner' -Band 'C3' -FablePolicy 'preferred'` returns `model='fable'`; `-Agent 'atomic-executor' -Band 'C4' -FablePolicy 'disabled'` returns `table_model='fable'`, `model='opus'`, `clamped_from='fable'`, `clamp_reason='fable_disabled'`.
- [x] [P1-T3] Run the PowerShell toolchain loop against the new module: `mcp__drm-copilot__run_poshqc_format` -> `mcp__drm-copilot__run_poshqc_analyze`. Restart from format if either stage changes files or reports findings. Acceptance: format reports 100% pass and analyze reports 0 findings for `.claude/lib/model-routing/ModelRouting.psm1` in a single pass (Pester deferred until tests exist in Phase 3).

### Phase 2 — Manifest entry and byte-mirror copy

- [x] [P2-T1] Append `".claude/lib/model-routing/ModelRouting.psm1"` to the `paths` array in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` (currently 71 entries, lines 4-73). Preserve JSON formatting (2-space indent, trailing entry ordering consistent with the existing list). Acceptance: the file parses as valid JSON and `paths` contains the new entry exactly once.
- [x] [P2-T2] Create the byte-mirror copy `extensions/drm-copilot/resources/claude-customizations/.claude/lib/model-routing/ModelRouting.psm1` byte-for-byte identical to `.claude/lib/model-routing/ModelRouting.psm1`. This is mechanically required by `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` (byte-identical mirror assertion, exempting only `.claude/agent-memory/**`). Acceptance: both files are byte-identical (verify with a byte comparison).

### Phase 3 — Pester tests (behavioral, parity, manifest membership) and full PowerShell toolchain

- [x] [P3-T1] Create `tests/scripts/claude-lib/model-routing/Get-ComplexityFloor.Tests.ps1` translating the pytest cases from `tests/scripts/dev_tools/test_compute_complexity_floor.py`. Import the module via `Import-Module (Resolve-Path "$PSScriptRoot/../../../../.claude/lib/model-routing/ModelRouting.psm1").Path -Force` (four levels: `model-routing` -> `claude-lib` -> `scripts` -> `tests` -> repo root). Cases (use `Describe`/`Context`/`It`, one behavior per `It`, `-ForEach` for matrices): empty signals -> `C1` (mirrors `test_no_floor_signals_yields_c1`); each single floor signal (`classifier_or_model_logic`, `auth_or_token_handling`, `concurrency_or_ordering`, `cross_module_contract_change`) -> `C3` (mirrors `test_each_floor_signal_contributes_c3`); all floor signals together -> `C3` (mirrors `test_max_of_multiple_floor_signals_is_c3`); floor signals repeated many times still -> `C3` and never `C4` (mirrors `test_floor_never_exceeds_c3`); repeated calls deterministic (mirrors `test_determinism_across_repeated_calls`); reversed input order yields identical result (mirrors `test_determinism_independent_of_input_ordering`). No temp files, no external processes. Acceptance: file exists and every `It` targets a single behavior with Arrange-Act-Assert structure.
- [x] [P3-T2] Create `tests/scripts/claude-lib/model-routing/Resolve-DelegationModel.Tests.ps1` translating the pytest cases from `tests/scripts/dev_tools/test_resolve_delegation_model.py`. Import the module by resolved path as in P3-T1. Cases: base-table per band under `available` — `C1->haiku`, `C2->sonnet`, `C3->opus`, `C4->fable` with `clamped_from=$null` (mirrors `test_base_table_per_band_under_available`); `available` leaves a `fable` cell intact (mirrors `test_available_leaves_fable_cell_intact`); `disabled` clamps a `C4` `fable` cell to `opus` with `clamped_from='fable'`, `clamp_reason='fable_disabled'` for both overlay and non-overlay agents (mirrors `test_disabled_clamps_fable_cell_to_opus`); `disabled` does not apply the preferred overlay so an overlay agent at `C3` stays `opus` (mirrors `test_disabled_does_not_apply_preferred_overlay`); `preferred` resolves each of the four overlay agents' `C3` cell to `fable` (mirrors `test_preferred_resolves_overlay_agent_c3_to_fable`); `preferred` leaves `atomic-executor`/`pr-author` `C3` at `opus` (mirrors `test_preferred_leaves_non_overlay_agent_c3_at_opus`); determinism across repeated calls (mirrors `test_determinism_across_repeated_calls`); negative/error case — an out-of-table band (for example `'C9'`) causes `Resolve-DelegationModel` to `throw` (the PowerShell analog of the Python `KeyError`; not covered by the pytest suite but required by spec Test Strategy edge cases). Acceptance: file exists; positive, negative, boundary, and disabled-clamp cases are all present.
- [x] [P3-T3] Create `tests/scripts/claude-lib/model-routing/ModelRouting.Parity.Tests.ps1` — a static config-parity test modeled on `tests/scripts/dev_tools/test_orchestration_routing_config_parity.py`. Read `config/orchestration-routing.json` via `Get-Content ... | ConvertFrom-Json` (resolve the repo-root path from `$PSScriptRoot`); read the module's script-scope constants with Pester `InModuleScope 'ModelRouting' { ... }`. Assert: module `$script:BASE_COMPLEXITY_TO_MODEL` equals `model_policy.complexity_to_model` (config lines 146-151); module `$script:PREFERRED_OVERLAY_AGENTS` equals `model_policy.preferred_overlay.agents` (config lines 154-159); module `$script:PREFERRED_OVERLAY_BAND` equals `model_policy.preferred_overlay.band` (`C3`, line 160) and `$script:PREFERRED_OVERLAY_MODEL` equals `model_policy.preferred_overlay.model` (`fable`, line 161); module `$script:FLOOR_CANDIDATE_BAND` and `$script:FLOOR_CEILING_BAND` both equal `C3`; module `$script:DISABLED_POLICY` equals `model_budget.fable_policy` default (`disabled`, line 166). File-read only; no cross-process invocation. Acceptance: file exists and asserts each embedded constant against the authoritative config value.
- [x] [P3-T4] Create `tests/scripts/claude-lib/model-routing/ModelRouting.Manifest.Tests.ps1` — a manifest-membership test asserting `.claude/lib/model-routing/ModelRouting.psm1` appears in the `paths` array of `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`. Read the manifest via `Get-Content ... | ConvertFrom-Json` (resolve path from `$PSScriptRoot`) and assert membership. This satisfies AC "manifest-membership test" within the PowerShell toolchain and honors the non-goal that no `tests/scripts/dev_tools/` file is modified. Acceptance: file exists and asserts the path is present in `core.json.paths`.
- [x] [P3-T5] Run the full PowerShell toolchain loop with coverage: `mcp__drm-copilot__run_poshqc_format` -> `mcp__drm-copilot__run_poshqc_analyze` -> `mcp__drm-copilot__run_poshqc_test` (coverage enabled). Restart from format if any stage changes files or fails. Acceptance: format 100% pass, analyze 0 findings, Pester all new tests pass, and the new module `.claude/lib/model-routing/ModelRouting.psm1` reports line coverage >= 85% and branch coverage >= 75% — all in a single clean pass. (This is an intermediate gate; the authoritative final coverage evidence is captured in Phase 8.)

### Phase 4 — Repoint orchestrator-runs-the-reference skill citations (source + byte-mirror)

- [x] [P4-T1] In `.claude/skills/orchestrate/SKILL.md`, repoint the two orchestrator-runs-the-reference citations at the resume-step block: line 32 (`compute_complexity_floor(signals_present)` / `scripts/dev_tools/compute_complexity_floor.py`) and line 34 (`resolve_delegation_model(agent, complexity_band, fable_policy)` / `scripts/dev_tools/resolve_delegation_model.py`). Re-confirm offsets at edit time. Repoint the runnable-reference path to `.claude/lib/model-routing/ModelRouting.psm1` and the function names `Get-ComplexityFloor` / `Resolve-DelegationModel`, preserving the instruction "do not reimplement the formula." Acceptance: lines 32 and 34 name the PowerShell module and functions; no other line changes.
- [x] [P4-T2] In `.claude/skills/orchestrate/SKILL.md`, repoint the two Model-Selection reference-implementation bullets: line 86 (`scripts/dev_tools/compute_complexity_floor.py` / `compute_complexity_floor`) and line 87 (`scripts/dev_tools/resolve_delegation_model.py` / `resolve_delegation_model`) to `.claude/lib/model-routing/ModelRouting.psm1` and `Get-ComplexityFloor` / `Resolve-DelegationModel`. Leave the validator-authority citations unchanged: line 96 (`scripts/dev_tools/validate_orchestrator_state.py`) and line 100 (`scripts/dev_tools/_orchestrator_state_model_routing_gate.py`, "the Python validator is authoritative for per-receipt correctness"). Optionally add one sentence to the Model Selection section clarifying that the destination runtime reference is the PowerShell module while the repo validator remains the Python authority. Acceptance: lines 86-87 name the PowerShell module/functions; lines 96 and 100 still reference the Python validator paths.
- [x] [P4-T3] In `.claude/skills/epic-orchestrate/SKILL.md`, repoint the two reference-implementation citations at lines 118-119 (`scripts/dev_tools/compute_complexity_floor.py` (`compute_complexity_floor`) and `scripts/dev_tools/resolve_delegation_model.py` (`resolve_delegation_model`)) to `.claude/lib/model-routing/ModelRouting.psm1` and `Get-ComplexityFloor` / `Resolve-DelegationModel`. Re-confirm offsets at edit time. Acceptance: lines 118-119 name the PowerShell module/functions with no broken references.
- [x] [P4-T4] Mirror the P4-T1/P4-T2 edits into the byte-mirror `extensions/drm-copilot/resources/claude-customizations/.claude/skills/orchestrate/SKILL.md` so it stays byte-identical to the source. Acceptance: the mirror file is byte-identical to `.claude/skills/orchestrate/SKILL.md`.
- [x] [P4-T5] Mirror the P4-T3 edit into the byte-mirror `extensions/drm-copilot/resources/claude-customizations/.claude/skills/epic-orchestrate/SKILL.md`. Acceptance: the mirror file is byte-identical to `.claude/skills/epic-orchestrate/SKILL.md`.

### Phase 5 — Verify no dangling references remain

- [x] [P5-T1] Confirm every delivered `.claude/` reference the orchestrator is instructed to run resolves to a delivered file. Grep both source and byte-mirror trees for `Get-ComplexityFloor`, `Resolve-DelegationModel`, and `ModelRouting.psm1`, and confirm the cited path `.claude/lib/model-routing/ModelRouting.psm1` exists in both `.claude/` and `extensions/drm-copilot/resources/claude-customizations/.claude/`. Confirm the retained Python validator-authority citations (`orchestrate/SKILL.md` lines 96, 100; `.claude/rules/orchestrator-state.md` lines 47, 61) still point at the intended `scripts/dev_tools/*.py` paths. Write the check result to `evidence/qa-gates/reference-resolution.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact shows no dangling orchestrator-runs reference and the validator-authority split is intact.

### Phase 6 — Final PowerShell QA loop (coverage-enabled)

- [x] [P6-T1] Run `mcp__drm-copilot__run_poshqc_format`. Write `evidence/qa-gates/poshqc-format.<TS>.md` with the four required fields. If files change, apply and restart the full PowerShell loop (P6-T1 -> P6-T3). Acceptance: `EXIT_CODE: 0` and `Output Summary:` reports 100% format pass.
- [x] [P6-T2] Run `mcp__drm-copilot__run_poshqc_analyze`. Write `evidence/qa-gates/poshqc-analyze.<TS>.md` with the four required fields. If findings appear, fix and restart from P6-T1. Acceptance: `EXIT_CODE: 0` and `Output Summary:` reports 0 findings.
- [x] [P6-T3] Run `mcp__drm-copilot__run_poshqc_test` (coverage enabled). Write `evidence/qa-gates/poshqc-test.<TS>.md` with the four required fields; `Output Summary:` MUST include numeric post-change line-coverage % and branch-coverage % for the new module and the passed/failed test counts. If any test fails or files change, fix and restart from P6-T1. Acceptance: `EXIT_CODE: 0`, all Pester tests pass, new module line coverage >= 85% and branch coverage >= 75%, recorded numerically.

### Phase 7 — Final Python QA loop (confirm untouched suite stays green)

- [x] [P7-T1] Run `poetry run black --check .`. Write `evidence/qa-gates/python-black.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`.
- [x] [P7-T2] Run `poetry run ruff check .`. Write `evidence/qa-gates/python-ruff.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`, 0 errors.
- [x] [P7-T3] Run `poetry run pyright`. Write `evidence/qa-gates/python-pyright.<TS>.md` with the four required fields. Acceptance: `EXIT_CODE: 0`, 0 errors.
- [x] [P7-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `evidence/qa-gates/python-pytest.<TS>.md` with the four required fields; `Output Summary:` MUST include numeric post-change line-coverage % and branch-coverage % and explicitly confirm that `tests/scripts/dev_tools/test_orchestration_routing_config_parity.py`, `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`, and `tests/scripts/dev_tools/test_push_down_claude_pack_selection.py` pass (the byte-mirror and config-parity contract tests exercise the new mirror file and manifest entry). Acceptance: `EXIT_CODE: 0`, all pass, coverage headline recorded numerically.

### Phase 8 — Coverage delta, scope-guard verification, and acceptance-criteria checkoff

- [x] [P8-T1] Compute and record the PowerShell coverage delta. Compare baseline (`evidence/baseline/poshqc-test.<TS>.md`) against final (`evidence/qa-gates/poshqc-test.<TS>.md`). Write `evidence/qa-gates/coverage-delta-powershell.<TS>.md` reporting baseline line/branch %, post-change line/branch %, and new/changed-code (new module) line/branch %. Acceptance: artifact reports all three coverage figures; new-module line >= 85% and branch >= 75%; no regression on changed lines.
- [x] [P8-T2] Compute and record the Python coverage delta. Compare baseline (`evidence/baseline/python-pytest.<TS>.md`) against final (`evidence/qa-gates/python-pytest.<TS>.md`). Write `evidence/qa-gates/coverage-delta-python.<TS>.md` reporting baseline vs post-change line/branch %. Acceptance: artifact shows no coverage regression (Python production untouched, so figures are expected to be equal).
- [x] [P8-T3] Verify byte-mirror parity for all new and edited `.claude/**` files. Confirm `.claude/lib/model-routing/ModelRouting.psm1`, `.claude/skills/orchestrate/SKILL.md`, and `.claude/skills/epic-orchestrate/SKILL.md` are byte-identical to their `extensions/drm-copilot/resources/claude-customizations/.claude/...` mirrors. Write `evidence/qa-gates/byte-mirror-parity.<TS>.md` with the four required fields. Acceptance: artifact confirms byte-identical parity for all three files (backed by the passing `test_push_down_claude_resource_contracts.py` in P7-T4).
- [x] [P8-T4] Verify push-down delivery membership. Confirm `.claude/lib/model-routing/ModelRouting.psm1` is present in `core.json.paths` (delivered under `--packs core`) and physically present in both `.claude/` and the byte-mirror (delivered under no-selection full-tree publish). Write `evidence/qa-gates/pushdown-delivery.<TS>.md` with the four required fields. Acceptance: artifact confirms both delivery paths are satisfied.
- [x] [P8-T5] Verify scope guard (non-goals). Confirm via `git diff --name-only` that no file was modified under `scripts/dev_tools/` (the two ported modules, the three `_orchestrator_state_*` helpers, and `validate_orchestrator_state.py`), no change to `pyproject.toml`, `config/orchestration-routing.json`, `extensions/drm-copilot/src/lib/validate/orchestrator-state-core.ts`, or any file under `tests/scripts/dev_tools/`. Write `evidence/qa-gates/scope-guard.<TS>.md` with the four required fields listing the full changed-file set. Acceptance: artifact shows the changed-file set is limited to `.claude/lib/model-routing/ModelRouting.psm1`, its byte-mirror, `core.json`, the two source skills and their two mirrors, and the four new Pester test files — and no forbidden path appears.
- [x] [P8-T6] Acceptance-criteria checkoff. Map each spec Acceptance Criterion (`spec.md` lines 148-154) to its verifying evidence artifact and mark PASS/FAIL. Write `evidence/qa-gates/acceptance-criteria-checkoff.<TS>.md` covering: (AC1) ports exist in `ModelRouting.psm1` and match Python references -> P3-T5, P8-T1; (AC2) Pester tests exist, translate pytest cases, pass -> P3-T1/T2, P6-T3; (AC3) config-parity test pins constants and passes -> P3-T3, P6-T3; (AC4) module listed in `core.json.paths` and present in byte-mirror -> P2-T1/T2, P8-T3/T4, and manifest-membership Pester test P3-T4; (AC5) `orchestrate`/`epic-orchestrate` references resolve to PowerShell module/functions with no broken references, mirrored in the bundle -> P4-T1..T5, P5-T1; (AC6) PowerShell toolchain single clean pass with coverage thresholds -> P6-T1..T3; (AC7) no Python/config/TS/`pyproject`/`tests/scripts/dev_tools` changes and the existing dev_tools suite passes unchanged -> P7-T4, P8-T5. Acceptance: every AC maps to a passing artifact; if any AC lacks passing evidence, the verdict is BLOCKED/INCOMPLETE, not PASS.
