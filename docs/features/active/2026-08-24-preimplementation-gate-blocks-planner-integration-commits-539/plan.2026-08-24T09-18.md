# preimplementation-gate-blocks-planner-integration-commits (Plan)

- **Issue:** #539
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-24T09-18
- **Status:** Ready for preflight validation
- **Version:** 1.0
- **Work Mode:** full-bug (AC source: `spec.md` only; no `user-story.md` exists and none is required)

**Feature folder (FEATURE):** `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539`. All evidence paths below are absolute repo-relative paths that resolve under `FEATURE/evidence/<kind>/` per `evidence-and-timestamp-conventions`. Replace `<timestamp>` in evidence filenames with the actual ISO-8601 `yyyy-MM-ddTHH-mm` value at write time. No evidence artifact may be written under any `artifacts/` sub-path; `artifacts/pester/powershell-coverage.xml` is a runner-generated coverage output that tasks READ for numeric extraction, never an evidence artifact.

**Fail-closed evidence rule:** Baseline artifacts, final-QA artifacts, and the coverage-comparison artifact are mandatory. If any required artifact is missing or lacks numeric coverage values, the outcome is remediation-required, never PASS.

**Evidence accounting rule:** Every evidence-producing task names its expected artifact path. Do not mark evidence-backed work complete without the artifact. Each command-step artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.

**Language scope:** PowerShell toolchain plus a targeted pytest leg for the two push-down contract suites that gate the bundle mirrors (`tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and `tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`). PowerShell toolchain per `.claude/rules/powershell.md`, via MCP tools only: format via `mcp__drm-copilot__run_poshqc_format`, analyze via `mcp__drm-copilot__run_poshqc_analyze`, test via `mcp__drm-copilot__run_poshqc_test` (Pester v5, coverage-enabled). Restart the loop from format whenever any stage fails or changes files. No type-check stage and no branch-coverage gate for PowerShell; the uniform line-coverage threshold is >= 85%.

**Coverage mechanism (from the #535 precedent):** the MCP test tool exposes no per-invocation coverage parameter; instrumentation comes from the standing `CodeCoverage.Path` allow-list in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` (`CoveragePercentTarget = 0`, so the threshold is enforced by this plan's tasks, not by the runner). Every coverage-bearing task extracts the numeric per-file line-coverage percentage from the runner-generated coverage artifact `artifacts/pester/powershell-coverage.xml` (CoverageGutters/JaCoCo per-file counters), not from the aggregate console summary, and records the extracted numeric value in `Output Summary:`. Both canonical hook copies are already registered in that allow-list (`.codex` copy and `.claude` copy); the two new canonical helper files are added to it by P2-T3 and P3-T3 so the changed production surface never leaves the coverage denominator.

**Change budget and batching:** this fix touches eight production PowerShell files (four hook copies plus four helper copies) plus the coverage-settings `.psd1` pair, exceeding the direct-mode cap of 2, so implementation routes through `powershell-typed-engineer` delegation in four batches, each at most 3 production files and 3 test files. Batch boundaries are phase boundaries:

- **Batch 1 = Phase 2:** canonical Claude helper (new) + canonical Claude hook (edit) + self-hosted runsettings (Claude-helper coverage entry) — 3 production, 0 test.
- **Batch 2 = Phase 3:** canonical Codex helper (new) + canonical Codex hook (edit) + self-hosted runsettings (Codex-helper coverage entry) — 3 production, 0 test.
- **Batch 3 = Phase 4:** Claude bundle hook mirror + Claude bundle helper mirror + mirror runsettings — 3 production, 0 test (plus the Claude pack manifest, a JSON file outside the PowerShell budget).
- **Batch 4 = Phase 5:** Codex bundle hook mirror + Codex bundle helper mirror — 2 production, 1 test (`$script:SharedModuleNames` one-line append; plus the Codex pack manifest JSON).

Phase 1 authors the two new test files (0 production, 2 test). Phase 6 touches only Markdown skill files (exempt from the PowerShell budget and the 500-line cap). A batch-budget state reset task closes each production batch so the per-session cap enforced by `.claude/hooks/enforce-powershell-batch-budget.ps1` does not block the next batch. Rationale for this split: each canonical side lands with its own coverage registration so pass-after coverage is measurable immediately; bundle mirrors land after their canonicals so byte-identity is a copy, not a re-edit; the Codex-side registrations (`$script:SharedModuleNames`, Codex pack manifest) land together with the Codex bundle mirror because the codex contract suite's byte-identity and pack-manifest checks fail unless all three exist simultaneously.

**Production PowerShell files in scope (eight, from research sections 1 and 6):**

1. `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` (canonical Claude, 340 lines pre-change)
2. `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` (new canonical Claude helper)
3. `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` (canonical Codex, 336 lines pre-change)
4. `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` (new canonical Codex helper)
5. `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` (Claude bundle mirror; content-equal to 1)
6. `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` (new Claude bundle helper mirror; content-equal to 2)
7. `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` (Codex bundle mirror; byte-identical to 3)
8. `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` (new Codex bundle helper mirror; byte-identical to 4)

**Registration and settings files in scope:** `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json`.

**Skill prose files in scope (Markdown, D7):** `.claude/skills/epic-plan/SKILL.md`, `.claude/skills/parallel-plan/SKILL.md`, `extensions/drm-copilot/resources/claude-customizations/.claude/skills/epic-plan/SKILL.md`, `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-plan/SKILL.md`.

**Test files in scope:**

- `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.CommandExemption.Tests.ps1` (new sibling; the existing Claude suite is at 461/500 lines and is not extended)
- `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate-command-exemption.Tests.ps1` (new sibling; the existing Codex contract suite is at 494/500 lines)
- `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` (one-line append to `$script:SharedModuleNames` only; no other edit)

**Five exempt orchestration-bookkeeping trees (D2, exact set):** `docs/features/epics/`, `docs/features/parallel/`, `docs/features/active/`, `docs/features/potential/`, `artifacts/orchestration/`.

**Naming pinned by this plan (the executor creates these identifiers):** the helper filename `enforce-orchestration-preimplementation-gate-helpers.ps1` on all four sides; the script-scoped constant `$script:OrchestrationBookkeepingTrees` holding exactly the five prefixes above; the entry predicate `Test-ExemptOrchestrationStagingCommand` (`[CmdletBinding()]`, `[OutputType([bool])]`); the Pester `Context` names `issue #539 orchestration-tree staging exemption allow cases`, `issue #539 mixed pathspec deny cases`, and `issue #539 fail-closed rule table deny cases` in each new test file; and the skill-prose token `pathspec-bearing` (the additive skill sections describe the pathspec-bearing commit form). Internal helper decomposition below the entry predicate is the engineer's choice within approved-verb naming.

**Command-text hazard (binding on the executor):** the live gate regex-matches the ENTIRE text of every Bash invocation. No task in this plan embeds the two-word staging or integration invocation literals, or the promotion tool names, in a shell command line. Every fixture string that contains those literals lives inside a PowerShell test file and is exercised in-process through the pure decision seam. Version-control tasks below are stated in prose ("stage and commit"); the executor performs them only while the session's ready checkpoint at `artifacts/orchestration/orchestrator-state.json` is in place, exactly as the #535 execution did.

**Behavioral contract:** the D4 nineteen-row fail-closed rule table in `spec.md` is normative. The exemption is allow-side only: it is consulted only after the unchanged trigger regex matches, it never suppresses the trigger, the four non-git implementation patterns are evaluated unchanged (a chained line containing any non-git implementation segment stays classified as implementation regardless of the git-segment verdict), and every parse ambiguity denies. The deny reason keeps the `PREIMPLEMENTATION_GATE_BLOCKED` prefix and the phrases `route metadata` and `lifecycle readiness`. `Test-OrchestrationReady`, `Test-ImplementationPath`, the delegation classifiers, the decision-JSON schema, and the hook registrations are unmodified. No file under `.github/instructions/` or `.claude/rules/` is modified. No Python file is added to any hook directory.

### Phase 0 — Policy Reads and Baseline Capture

- [ ] [P0-T1] Read `CLAUDE.md` at repo root. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [ ] [P0-T4] Read `.claude/rules/powershell.md`. Acceptance: file read in full; recorded in the P0-T5 artifact.
- [ ] [P0-T5] Write the policy-read evidence artifact to `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the P0-T1 through P0-T4 sequence), and the explicit list of files read. Acceptance: artifact exists with all three fields.
- [ ] [P0-T6] Baseline format check: run `mcp__drm-copilot__run_poshqc_format` over the repository PowerShell sources. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/baseline-poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (state whether any file was reformatted; expected: none). Acceptance: artifact exists with all four fields.
- [ ] [P0-T7] Baseline lint: run `mcp__drm-copilot__run_poshqc_analyze` with repo settings. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/baseline-poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count; expected: 0). Acceptance: artifact exists with all four fields.
- [ ] [P0-T8] Baseline test with coverage (Claude suite): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1`. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/baseline-pester-claude-hooks.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and the numeric per-file line-coverage percentage for `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`, extracted from `artifacts/pester/powershell-coverage.xml` per the coverage mechanism in the preamble. Acceptance: artifact exists with a numeric coverage headline.
- [ ] [P0-T9] Baseline test with coverage (Codex suite): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1`. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/baseline-pester-codex-hooks.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and the numeric per-file line-coverage percentage for `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`, extracted from `artifacts/pester/powershell-coverage.xml` per the coverage mechanism in the preamble. Acceptance: artifact exists with a numeric coverage headline.
- [ ] [P0-T10] Baseline line counts and pair hashes: count lines in each of the four hook copies (paths 1, 3, 5, 7 of the production-file list) and the two existing suites (`tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1`, `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1`), and compute `Get-FileHash -Algorithm SHA256` for each of the four hook copies. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/baseline/baseline-linecounts-and-hashes.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` stating that the two Claude copies share one SHA-256 and the two Codex copies share another. Do NOT assert or record any cross-pair equality claim; the two pairs are deliberately divergent implementations. Acceptance: artifact exists; within-pair hash equality holds for both pairs.

### Phase 1 — Regression Suites and Fail-Before Evidence

Batch content: 0 production files, 2 test files (within the per-batch cap). Both new suites are authored against the UNFIXED hooks so the allow cases produce a genuine failing baseline.

- [ ] [P1-T1] Delegate to `powershell-typed-engineer`: create `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.CommandExemption.Tests.ps1`, dot-sourcing the canonical `.claude` hook exactly as the existing suite does and defining its own file-scoped payload/checkpoint builders in the existing suite's style. Every decision test drives `Invoke-OrchestrationPreimplementationGateDecision -ToolInputRaw ... -CheckpointRaw ...` with an explicitly not-ready checkpoint (route id empty, lifecycle readiness false), no disk I/O, no child processes, no temporary files. Required structure: (a) `Context` `issue #539 orchestration-tree staging exemption allow cases` with eight named `It` cases — epic.md staging under `docs/features/epics/`; a two-operand parallel manifest plus kickoff staging under `docs/features/parallel/`; a quoted operand under `docs/features/active/`; a backslash-spelled operand (D4 row 18 allow case); a kickoff `.md` under `artifacts/orchestration/`; the pathspec-bearing commit form with a message option and a double-dash separator naming one exempt path; a chained two-segment line whose every segment is independently exempt; and a `docs/features/potential/` operand — each asserting decision `allow`; (b) `Context` `issue #539 mixed pathspec deny cases` with four named `It` cases, each pairing one exempt operand with one production operand ending `.ps1`, `.py`, `.ts`, and `.cs` respectively, each asserting `deny` with a reason carrying the `PREIMPLEMENTATION_GATE_BLOCKED` prefix and the phrases `route metadata` and `lifecycle readiness`; (c) `Context` `issue #539 fail-closed rule table deny cases` as a `-ForEach` table with at least one named case per D4 row 1 through 17 and row 19 (row numbers embedded in the case names), using the row exemplars from `spec.md` D4 — including bare staging with zero operands; the tree-wide flags; the dot and colon-slash whole-tree operands; the pathless message-only commit; the content-widening and history-rewriting commit options; the pathspec-from-file options; double-dash with nothing after it; an unknown dash-leading option; colon-magic operands; a leading-dash operand without a preceding double-dash; an unbalanced quote; a dollar-sign operand, a backtick operand, and a redirection-bearing segment; a chained line with one non-exempt segment and an unsplittable text case; the subcommand-relocating options; a glob whose literal prefix escapes the exempt trees including the `docs/features/*` case; absolute operands in slash, drive-letter, and UNC spellings; a parent-directory segment; and the mixed operand set — each asserting `deny`; plus (d) one named deny `It` asserting that a here-string/message-body payload merely containing the two-word staging literal is denied (D3/D8 residual behavior). Acceptance: file exists with the three named `Context` blocks and case (d); file is at most 500 lines.
- [ ] [P1-T2] [expect-fail] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.CommandExemption.Tests.ps1` against the unmodified canonical Claude hook. Expected: every allow case from P1-T1(a) fails (the unfixed hook denies) and every deny case passes. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/regression-testing/fail-before-pester-claude.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode:` (the observed non-zero value), `Output Summary:` listing the failing allow-case names and counts. Acceptance: artifact exists and shows all eight allow cases failing against the unfixed hook.
- [ ] [P1-T3] Delegate to `powershell-typed-engineer`: create `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate-command-exemption.Tests.ps1`, dot-sourcing the canonical `.codex` hook copy exactly as the existing codex contract suite does for its pure-decision assertions, honoring the Codex copy's decision-entry idiom. Scenario obligations are identical to P1-T1: the same three named `Context` blocks, the same eight allow cases, the same four mixed-deny extension cases, at least one named case per D4 row 1 through 17 and row 19, and the here-string deny case. Acceptance: file exists with the three named `Context` blocks; file is at most 500 lines.
- [ ] [P1-T4] [expect-fail] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate-command-exemption.Tests.ps1` against the unmodified canonical Codex hook. Expected: every allow case fails; every deny case passes. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/regression-testing/fail-before-pester-codex.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode:` (the observed non-zero value), `Output Summary:` listing the failing allow-case names and counts. Acceptance: artifact exists and shows all eight allow cases failing against the unfixed hook.

### Phase 2 — Implementation Batch 1: Canonical Claude Hook, Helper, and Coverage Registration

Batch content: 3 production files (`.claude` helper, `.claude` hook, self-hosted runsettings), 0 test files. Delegated to `powershell-typed-engineer`.

- [ ] [P2-T1] Delegate to `powershell-typed-engineer`: create `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` containing the pathspec classifier as pure string functions — no disk, process, network, or environment access. Required contents: the script-scoped constant `$script:OrchestrationBookkeepingTrees` holding exactly the five exempt prefixes from the plan preamble, and the entry predicate `Test-ExemptOrchestrationStagingCommand` (`[CmdletBinding()]`, `[OutputType([bool])]`, mandatory string parameter) returning `$true` if and only if every trigger-matching segment of the supplied command text parses as a complete, recognized staging or integration invocation with at least one pathspec operand and every operand — after balanced-quote stripping and backslash-to-forward-slash normalization — resolves inside one of the five exempt trees. The implementation must realize every D4 row: segment splitting outside quotes for chained lines with deny on unsplittable text (row 13); a modeled option table with deny on any unmodeled dash-leading token (rows 2, 5, 6, 8); double-dash semantics (row 7); deny on zero operands for both subcommands (rows 1, 4); deny on whole-tree, colon-magic, leading-dash, interpolation, redirection, subcommand-relocating, glob-escaping, absolute, and parent-directory operand forms (rows 3, 9, 10, 12, 14, 15, 16, 17); quote handling with deny on unbalanced quoting (row 11); backslash normalization before the prefix test (row 18); and the all-operands-exempt invariant (row 19). Every ambiguity returns `$false`. Advanced functions with approved verbs; internal decomposition is the engineer's choice. Acceptance: file exists, defines the two pinned identifiers, and is at most 500 lines.
- [ ] [P2-T2] Delegate to `powershell-typed-engineer`: edit `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` to dot-source the sibling helper (following the `enforce-pr-author-skill.ps1` headroom-split precedent) and integrate the exemption into `Test-ImplementationCommand` allow-side only: when, and only when, the git trigger pattern matched and no other implementation pattern matched, consult `Test-ExemptOrchestrationStagingCommand`; a `$true` verdict classifies the command as not-implementation, any other outcome leaves the pre-change classification. The trigger regex line is byte-unchanged; the four non-git implementation patterns, `Test-OrchestrationReady`, `Test-ImplementationPath`, the delegation classifiers, the anomaly path, the block-reason text, and the decision-JSON shape are unmodified. Acceptance: hook dot-sources the helper, exemption reachable only from the git-trigger leg, file at most 500 lines.
- [ ] [P2-T3] Edit `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`: add the entry `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` to the `CodeCoverage.Path` allow-list adjacent to the existing canonical Claude hook entry, with a one-line comment citing issue #539 and the Coverage Exclusion Policy, following the existing dot-sourced-helper-sibling precedent entries. Acceptance: the entry is present in the allow-list.
- [ ] [P2-T4] Batch 1 toolchain hygiene: run `mcp__drm-copilot__run_poshqc_format` then `mcp__drm-copilot__run_poshqc_analyze` covering the files edited in P1-T1, P2-T1, P2-T2, and P2-T3; restart from format if either stage fails or changes files, until both pass clean in a single pass. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/batch1-format-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each stage. Acceptance: artifact exists; both stages clean.
- [ ] [P2-T5] Pass-after run (Claude side): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.CommandExemption.Tests.ps1` and `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1`. Expected: every test in both files passes — all eight #539 allow cases, all deny cases, and the entire pre-existing suite without modification to its assertions. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/regression-testing/pass-after-claude.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass counts and the numeric per-file line-coverage percentages for `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` and `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1`, extracted from `artifacts/pester/powershell-coverage.xml` per the coverage mechanism in the preamble. Acceptance: exit code 0, zero failed tests, two numeric coverage values recorded.
- [ ] [P2-T6] Commit batch 1: stage and commit the four batch-1 paths (canonical Claude helper, canonical Claude hook, self-hosted runsettings, new Claude test file) in one commit. Acceptance: a single commit contains exactly those paths and no other production files.
- [ ] [P2-T7] Reset the PowerShell batch-budget state before Phase 3. Run `Remove-Item -LiteralPath (Join-Path '.claude/state' ("powershell-batch-budget." + ($(if ($env:CLAUDE_SESSION_ID) { $env:CLAUDE_SESSION_ID } else { 'default' })) + ".json")) -Force -ErrorAction SilentlyContinue`, the reset mechanism named in the budget hook's own deny message. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/batch-budget-reset-1.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (state file absent after the reset). Acceptance: artifact exists; the state file does not exist when Phase 3 begins.

### Phase 3 — Implementation Batch 2: Canonical Codex Hook, Helper, and Coverage Registration

Batch content: 3 production files (`.codex` helper, `.codex` hook, self-hosted runsettings), 0 test files. Delegated to `powershell-typed-engineer`. Do NOT run `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` in this phase: its byte-identity check compares the canonical `.codex` copy against a bundle that is updated only in Phase 5.

- [ ] [P3-T1] Delegate to `powershell-typed-engineer`: create `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` with the same pinned identifiers (`$script:OrchestrationBookkeepingTrees`, `Test-ExemptOrchestrationStagingCommand`) and the same D4 semantics as P2-T1. The classifier is pure string logic, so byte-identical content with the `.claude` helper is permitted and preferred to minimize the divergence surface; no contract requires it. Acceptance: file exists, defines the two pinned identifiers, and is at most 500 lines.
- [ ] [P3-T2] Delegate to `powershell-typed-engineer`: edit `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` in that file's own idiom (the #535 precedent): dot-source the sibling helper alongside the existing `codex-pretooluse-file-mapping.ps1` dot-source, and integrate the exemption into the Codex copy's `Test-ImplementationCommand` on the git-trigger leg only, preserving the apply_patch marker parsing, the permissive decision-entry behavior, the Codex stdin entrypoint, and the exit-code contract unchanged. The trigger regex line is byte-unchanged. Do not add any cross-runtime import of `.claude/lib/hook-payload/HookPayload.psm1`. Acceptance: hook dot-sources the helper, exemption reachable only from the git-trigger leg, apply_patch leg unmodified, file at most 500 lines.
- [ ] [P3-T3] Edit `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`: add the entry `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` to the `CodeCoverage.Path` allow-list adjacent to the existing canonical Codex hook entry, with a one-line comment citing issue #539. Acceptance: the entry is present in the allow-list.
- [ ] [P3-T4] Batch 2 toolchain hygiene: run `mcp__drm-copilot__run_poshqc_format` then `mcp__drm-copilot__run_poshqc_analyze` covering the files edited in P1-T3, P3-T1, P3-T2, and P3-T3; restart from format if either stage fails or changes files, until both pass clean in a single pass. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/batch2-format-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each stage. Acceptance: artifact exists; both stages clean.
- [ ] [P3-T5] Pass-after run (Codex side): run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate-command-exemption.Tests.ps1`. Expected: every test passes. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/regression-testing/pass-after-codex.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass counts and the numeric per-file line-coverage percentages for `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1` and `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1`, extracted from `artifacts/pester/powershell-coverage.xml` per the coverage mechanism in the preamble. Acceptance: exit code 0, zero failed tests, two numeric coverage values recorded.
- [ ] [P3-T6] Commit batch 2: stage and commit the four batch-2 paths (canonical Codex helper, canonical Codex hook, self-hosted runsettings, new Codex test file) in one commit. Acceptance: a single commit contains exactly those paths and no other production files.
- [ ] [P3-T7] Reset the PowerShell batch-budget state before Phase 4, using the same command and mechanism as P2-T7. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/batch-budget-reset-2.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; the state file does not exist when Phase 4 begins.

### Phase 4 — Implementation Batch 3: Claude Bundle Mirrors and Claude-Side Registrations

Batch content: 3 production files (Claude bundle hook, Claude bundle helper, mirror runsettings), 0 test files; plus the Claude pack manifest (JSON, outside the PowerShell budget).

- [ ] [P4-T1] Copy the updated canonical Claude hook content-equal to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`. Acceptance: `Get-FileHash -Algorithm SHA256` of the two paths is equal.
- [ ] [P4-T2] Create `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` content-equal to the canonical `.claude` helper. Acceptance: `Get-FileHash -Algorithm SHA256` of the two paths is equal.
- [ ] [P4-T3] Edit `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`: apply the identical two coverage-entry hunks from P2-T3 and P3-T3. If the two runsettings copies were byte-identical at baseline (verify with `Get-FileHash -Algorithm SHA256` against the baseline observation), they must be byte-identical after this edit. Acceptance: both helper entries present in the mirror; the pre-change equality relation between the two runsettings copies, whichever it was, is preserved.
- [ ] [P4-T4] Edit `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`: add the entry `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` in the manifest's existing ordering convention, adjacent to the listed gate hook and the existing helper-sibling entries. Acceptance: the entry is present and the JSON parses.
- [ ] [P4-T5] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py`. Expected: pass — every repo `.claude/**` file, including the edited hook, the new helper, and the manifests, is content-equal to its bundled counterpart. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/pushdown-claude-parity.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: exit code 0, zero failed tests.
- [ ] [P4-T6] Record recomputed Claude pair-hash parity evidence in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/claude-pair-hash.<timestamp>.md` following the #535 artifact shape: SHA-256 per pair member for the hook pair (paths 1 and 5) and the helper pair (paths 2 and 6), line counts, and method (same content landed on both members, verified by hash). Include `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Do NOT record any cross-pair (Claude versus Codex) equality claim. Acceptance: artifact exists; within-pair hashes equal for both pairs.
- [ ] [P4-T7] Commit batch 3: stage and commit the four batch-3 paths (Claude bundle hook, Claude bundle helper, mirror runsettings, Claude pack manifest) in one commit. Acceptance: a single commit contains exactly those paths.
- [ ] [P4-T8] Reset the PowerShell batch-budget state before Phase 5, using the same command and mechanism as P2-T7. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/batch-budget-reset-3.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; the state file does not exist when Phase 5 begins.

### Phase 5 — Implementation Batch 4: Codex Bundle Mirrors and Codex-Side Registrations

Batch content: 2 production files (Codex bundle hook, Codex bundle helper), 1 test file (`$script:SharedModuleNames` one-line append); plus the Codex pack manifest (JSON). The byte-identical Codex pair must land in the same commit.

- [ ] [P5-T1] Copy the updated canonical Codex hook byte-identically to `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`. Acceptance: `Get-FileHash -Algorithm SHA256` of the two paths is equal.
- [ ] [P5-T2] Create `extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` byte-identical to the canonical `.codex` helper. Acceptance: `Get-FileHash -Algorithm SHA256` of the two paths is equal.
- [ ] [P5-T3] Edit `extensions/drm-copilot/resources/codex-and-agents-customizations/pack-manifests/core.json`: add the entry `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` in the manifest's existing ordering convention. Acceptance: the entry is present and the JSON parses.
- [ ] [P5-T4] Edit `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1`: append the string `enforce-orchestration-preimplementation-gate-helpers.ps1` to the `$script:SharedModuleNames` array (currently line 30). This single-line change subjects the Codex helper to the suite's parse, 500-line, byte-identity, no-legacy-env, and pack-manifest checks. No other edit to this file. Acceptance: the array contains the new name; the file is at most 500 lines.
- [ ] [P5-T5] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/codex-hooks/legacy-codex-hook-contracts.Tests.ps1` and `tests/scripts/codex-hooks/enforce-orchestration-preimplementation-gate-command-exemption.Tests.ps1`. Expected: every test passes, including the byte-identity check over the canonical and bundle Codex copies (hook and helper), the pack-manifest listing check for the helper, and the unchanged in-process classification table in which the pathless message-only commit fixture still classifies as an implementation command. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/codex-contract-suite.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: exit code 0, zero failed tests.
- [ ] [P5-T6] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`. Expected: pass — the Codex pack manifest is complete over the bundled `.codex` files including the new helper. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/pushdown-codex-manifest.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: exit code 0, zero failed tests.
- [ ] [P5-T7] Record recomputed Codex pair-hash parity evidence in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/codex-pair-hash.<timestamp>.md` following the #535 artifact shape: SHA-256 per pair member for the hook pair (paths 3 and 7) and the helper pair (paths 4 and 8), line counts, and method. Include `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Do NOT record any cross-pair equality claim. Acceptance: artifact exists; within-pair hashes equal for both pairs.
- [ ] [P5-T8] Commit batch 4: stage and commit the four batch-4 paths (Codex bundle hook, Codex bundle helper, Codex pack manifest, codex contract suite) in one commit so the byte-identical pair lands together. Acceptance: a single commit contains exactly those paths, including both `.codex`-side bundle copies.
- [ ] [P5-T9] Reset the PowerShell batch-budget state before Phase 6, using the same command and mechanism as P2-T7. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/other/batch-budget-reset-4.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; the state file does not exist when Phase 6 begins.

### Phase 6 — Planner Skill Prose (D7)

Markdown files only; exempt from the PowerShell change budget and the 500-line cap. The pinned preparation-mode marker literals `Preparation mode: true.` and `route_id: preparation.` are byte-unchanged; the additions are purely additive prose. All skill edits are made with the file-editing tool, never via shell command text.

- [ ] [P6-T1] Edit `.claude/skills/epic-plan/SKILL.md`: add an additive subsection instructing planner integration commits to use the pathspec-bearing commit form — a message option plus a double-dash separator followed by the exempt paths, or the exempt paths named directly before the message option — with every operand inside the five exempt orchestration-bookkeeping trees, because a pathless integration invocation is denied by the preimplementation gate (D4 row 4). The subsection body must contain the token `pathspec-bearing`. No existing line is modified or removed; the pinned marker literals are untouched. Acceptance: subsection present; no deletion or modification of pre-existing lines.
- [ ] [P6-T2] Edit `.claude/skills/parallel-plan/SKILL.md`: add the equivalent additive subsection with the same content obligations as P6-T1, covering the manifest, per-item plan, and durable kickoff commits. Acceptance: subsection present; no deletion or modification of pre-existing lines; pinned marker literals untouched.
- [ ] [P6-T3] Mirror both edited skill files content-equal into `extensions/drm-copilot/resources/claude-customizations/.claude/skills/epic-plan/SKILL.md` and `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-plan/SKILL.md`. Acceptance: `Get-FileHash -Algorithm SHA256` equality for each of the two file pairs.
- [ ] [P6-T4] Verify the pinned markers survived: run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-orchestration-preimplementation-gate.Tests.ps1` (the #535 marker tests and the verbatim-kickoff allow-case prompts live there) and rerun `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` for the skill mirrors. Expected: both pass without modification to any assertion. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/skill-prose-verification.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each command. Acceptance: both exit codes 0, zero failed tests.
- [ ] [P6-T5] Presence verification: run `Select-String -SimpleMatch -Pattern 'pathspec-bearing'` against each of the four skill files listed in the preamble and confirm at least one match per file. Record the four match counts in the P6-T4 artifact or a sibling entry within the same `evidence/qa-gates/` folder. Acceptance: match count at least 1 in each of the four files.
- [ ] [P6-T6] Commit the four skill files in one commit. Acceptance: a single commit contains exactly the four skill paths.

### Phase 7 — Final QA Loop, Coverage Verification, and Closure

Full PowerShell QA loop: format, analyze, test with coverage, in that order, via the MCP tools. If any stage fails or changes any file, restore the four pair identities and restart the loop from P7-T1, repeating until all stages pass clean in a single pass. Every command task below is unconditional; `SKIPPED` is not a valid outcome for any of them.

- [ ] [P7-T1] Final format: run `mcp__drm-copilot__run_poshqc_format` over the repository PowerShell sources. If any file changes, re-establish the hook and helper pair identities (P4-T1, P4-T2, P5-T1, P5-T2) and restart the loop from this task. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/final-poshqc-format.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: clean pass recorded (no file changed on the final iteration).
- [ ] [P7-T2] Final lint: run `mcp__drm-copilot__run_poshqc_analyze` with repo settings. On failure, remediate and restart the loop from P7-T1. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/final-poshqc-analyze.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count 0). Acceptance: zero findings on the final iteration.
- [ ] [P7-T3] Final test with coverage: run `mcp__drm-copilot__run_poshqc_test` over the folders `tests/scripts/claude-hooks` and `tests/scripts/codex-hooks` (all affected suites, including the transport and schema contract suites that dot-source the changed hooks). On any failure, remediate and restart the loop from P7-T1. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/final-pester.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including total pass/fail counts and the numeric per-file line-coverage percentage for each of the four canonical files — `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1`, `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1`, `.codex/hooks/enforce-orchestration-preimplementation-gate.ps1`, `.codex/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` — extracted from `artifacts/pester/powershell-coverage.xml` per the coverage mechanism in the preamble. Acceptance: exit code 0, zero failed tests, four numeric coverage values recorded, each at least 85.
- [ ] [P7-T4] Coverage delta and threshold verification: write `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/coverage-delta.<timestamp>.md` recording (a) baseline coverage from P0-T8 and P0-T9, (b) post-change coverage from P7-T3, (c) the changed-code result: numeric line coverage at least 85 on each of the four canonical changed or added production files with no regression versus baseline on the two hook files, and (d) mirror-inheritance evidence for the four bundle copies that no suite executes directly — each bundle member inherits its canonical measurement via the SHA-256 hash equality recorded in P4-T6 and P5-T7. If any numeric value is unavailable or below threshold, the outcome is remediation-required, never PASS. Acceptance: artifact exists with all four sections and every threshold met.
- [ ] [P7-T5] Scope and size verification: (a) count lines in each of the eight production PowerShell files from the preamble list, the two runsettings files, and the three test files in scope, confirming every count is at most 500; (b) confirm via the repository diff against the merge base with `main` that no changed path is under `.github/instructions/` or `.claude/rules/`, and that no `.py` file was added under `.claude/hooks/` or `.codex/hooks/`; (c) confirm the trigger line is byte-unchanged by running `Select-String -SimpleMatch` for the literal `(^|\s)git\s+(add|commit)\b` against each of the four hook copies and asserting exactly one match per file; (d) confirm no alternative readiness source was added: the `Test-OrchestrationReady` function bodies in the canonical Claude and Codex hooks are byte-unchanged from baseline. Record all counts and confirmations in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/scope-and-size.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists; all counts at most 500; all scope confirmations recorded.
- [ ] [P7-T6] Final push-down parity: run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_codex_and_agents_pack_manifest_completeness.py`. On failure, restore the affected pair identity and restart the loop from P7-T1. Record in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/evidence/qa-gates/pushdown-parity-final.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts). Acceptance: exit code 0, zero failed tests.
- [ ] [P7-T7] Check off spec.md acceptance criteria 1 through 7 (exemption if-and-only-if contract; the eight allow scenarios; the mixed-pathspec denials with unchanged reason text; the per-row rule-table cases; unchanged existing behavior; four-copy delivery with pair contracts passing; recomputed pair-hash evidence) individually in `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/spec.md`, each only after confirming its verifying evidence exists (P2-T5, P3-T5, P5-T5, P4-T5, P4-T6, P5-T7, P7-T3). Change only `- [ ]` to `- [x]`; criterion text unchanged. Acceptance: those seven checkboxes are checked.
- [ ] [P7-T8] Check off spec.md acceptance criteria 8 through 14 (helper extraction and 500-line cap; registration surfaces; coverage threshold; skill prose with unchanged markers; fail-before evidence; clean toolchain pass; no out-of-scope changes) individually, each only after confirming its verifying evidence exists (P7-T5, P5-T4 through P5-T6, P7-T3, P7-T4, P6-T4, P6-T5, P1-T2, P1-T4, P7-T1, P7-T2, P7-T6). Change only `- [ ]` to `- [x]`; criterion text unchanged. Acceptance: those seven checkboxes are checked.
- [ ] [P7-T9] Report the Acceptance Criteria Status summary (source `docs/features/active/2026-08-24-preimplementation-gate-blocks-planner-integration-commits-539/spec.md`, total 14, checked-off count, remaining items) in the executor's final completion report per `acceptance-criteria-tracking`. Acceptance: summary present in the completion report; all 14 items checked or each remaining item documented with its gap.
