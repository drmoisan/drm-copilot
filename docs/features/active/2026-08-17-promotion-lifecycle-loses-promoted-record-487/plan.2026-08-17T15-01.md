# 2026-08-17-promotion-lifecycle-loses-promoted-record (Plan)

- **Issue:** #487
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-17T15-01
- **Status:** Approved for preflight
- **Version:** 0.3 (revision 1 — deltas D1 through D7 from the `atomic-executor` preflight applied in place per the Plan-Path Continuity Contract)
- **Work Mode:** full-bug (marker `- Work Mode: full-bug` in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/issue.md`)
- **Requirements source:** `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` is the SOLE acceptance-criteria source (AC-1 through AC-12). `user-story.md` does not exist and must not be created or required.

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Evidence location invariant:** Every evidence artifact named in this plan resolves to `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/<kind>/`. The `artifacts/` sub-paths `baselines/`, `baseline/`, `qa/`, `qa-gates/`, `evidence/`, `coverage/`, `regression-testing/`, and `post-change/` are forbidden output locations. This clause is non-overridable.

`EVIDENCE_LOCATION_OVERRIDE_REJECTED: <FEATURE>/evidence/coverage/ and <FEATURE>/evidence/qa/ (named in spec.md Test Strategy, AC-7, AC-11, and Rollout & Follow-up) replaced with docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/`. `coverage/` and `qa/` are not canonical evidence kinds; the canonical kinds are `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`, and `remediation-baseline/`. All coverage and post-fix QA evidence in this plan is written under `evidence/qa-gates/`.

**Recorded deviations from `spec.md` (auditability record; no plan behavior changes as a result):**

1. **Fail-before evidence location.** `spec.md` AC-7 and the "Fail-before regression (required)" subsection name `<FEATURE>/evidence/baseline/` as the location for the pre-fix failing run. This plan writes fail-before evidence to `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/` (P1-T10, P1-T11, P1-T12), which is correct per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`: `regression-testing/` is the canonical fail-before search scope and the canonical location for a fail-before exception dossier. The plan's evidence path is not changed and AC-7's text is not changed; the divergence is recorded here so it is not reported as a discrepancy at P6-T4 or P7-T12.
2. **Line-count corrections applied to `spec.md`.** `flow.ts` is 444 lines, not 445, and `io.ts` is 396 lines, not 397. The numeric values in `spec.md` INV-5, the implementation-strategy table, AC-10, and the `flow.ts` risk entry were corrected to match. Only the numerals were changed; no acceptance-criterion text was altered and every AC checkbox remains unchecked.

**Timestamp convention:** `<ISO>` in an artifact filename means the executor's actual capture time in `yyyy-MM-ddTHH-mm` format (for example `2026-08-17T16-42`). Do not write the literal string `<ISO>`.

**Spec checkbox rule:** Every AC checkbox in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` remains UNCHECKED at the end of this planning cycle. Check-off is an execution-phase action, not a planning action.

**Worktree prohibition:** No task in this plan removes, prunes, or deletes a git worktree. `.claude/worktrees/agent-afc9f4fd25ec235a5/` must not be touched. Where a check would benefit from a clean worktree list, this plan performs diagnosis-and-report only (P6-T7).

**Design decisions adopted from research (recorded so they are not re-litigated):**

1. Disposition rule: COPY when the resolved source is under `docs/features/potential/promoted/`; MOVE otherwise. Decision is taken from the resolved source path at placement time, never from the requested feature name.
2. Emitted-line wording: the copy branch emits `Copied potential file to <path>`; the move branch keeps `Moved potential file to <path>`. Mirrored in Python.
3. Receipt post-condition layer: the service-call layer of each cluster, not the workflow layer. TypeScript only; the Python CLI emits no receipt so no Python analogue exists.
4. Receipt post-condition scope: `destination_path` for both tools, plus every filesystem-path `artifacts` entry. `potential_to_issue`'s `artifacts` carries a GitHub issue URL and is therefore excluded from the existence check.
5. Retroactive repair of previously lost promoted records is OUT OF SCOPE. The record removed during this run stays absent as evidence.

---

### Phase 0 — Policy Reading and Baseline Capture

- [ ] [P0-T1] Read `CLAUDE.md` at the workspace root and record its tone policy, policy-compliance reading order, and architecture sections in the Phase 0 evidence artifact.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md` and record the 500-line file-size limit, the seven-stage toolchain loop order, and the I/O-boundary rules in the Phase 0 evidence artifact.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md` and record the uniform coverage thresholds (line >= 85%, branch >= 75%), the coverage exclusion policy, the test-file-location rule, and the prohibition on temporary files in tests in the Phase 0 evidence artifact.
- [ ] [P0-T4] Read `.claude/rules/typescript.md` and record the TypeScript toolchain commands and coding standards in the Phase 0 evidence artifact.
- [ ] [P0-T5] Read `.claude/rules/python.md` and record the Python toolchain commands and coding standards in the Phase 0 evidence artifact.
- [ ] [P0-T6] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the exact order `CLAUDE.md` → `.claude/rules/general-code-change.md` → `.claude/rules/general-unit-test.md` → `.claude/rules/typescript.md` → `.claude/rules/python.md`), and the explicit list of files read with the key constraints extracted from each.
- [ ] [P0-T7] Read `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` and confirm in `evidence/baseline/phase0-instructions-read.md` that it carries exactly 12 acceptance criteria (AC-1 through AC-12), all unchecked, and that `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/user-story.md` does not exist.
- [ ] [P0-T8] Run `git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; git status --short` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-git-state.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch name, head SHA, count of modified/untracked entries).
- [ ] [P0-T9] Run `(Get-Content -LiteralPath 'extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts').Count` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-flow-ts-linecount.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the pre-change line count (expected 444) and the 500-line limit headroom.
- [ ] [P0-T10] Run a recursive glob for `quality-tiers.y*ml` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-quality-tiers-absence.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, plus `SearchScope:`, `SearchPatterns:`, and `SearchResult:`, recording that no tier classification source exists and that the uniform gates (line >= 85%, branch >= 75%) still bind while the tier-dependent gates (property-test density, mutation score) have no classification source.
- [ ] [P0-T11] Run a recursive glob for `.dependency-cruiser.cjs` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-depcruise-config-absence.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, plus `SearchScope:`, `SearchPatterns:`, and `SearchResult:`, recording that the architecture-boundary stage named in `spec.md` has no configured tool in this repository. This is a verification outcome, not a skipped command.
- [ ] [P0-T12] Run `npx prettier --check "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-ts-prettier.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail and the count of unformatted files).
- [ ] [P0-T13] Run `npx eslint --no-error-on-unmatched-pattern src test` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-ts-eslint.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error and warning counts).
- [ ] [P0-T14] Run `npx tsc -p ./ --noEmit` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-ts-tsc.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (type-error count).
- [ ] [P0-T15] Run `npm run test:coverage` from `extensions/drm-copilot/` (the repo-sanctioned `npx jest` wrapper `node run-jest.cjs --coverage --coverageReporters=lcov --coverageReporters=text-summary`, which supplies `--config jest.config.cjs` and enforces the issue-#423 prohibited-flag guard; a bare `npx jest` invocation bypasses both and must not be used) and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-ts-jest-coverage.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` carrying the numeric suite counts and the numeric `text-summary` headline percentages for Statements, Branches, Functions, and Lines.
- [ ] [P0-T16] Run `poetry run black --check .` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-py-black.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (count of files that would be reformatted).
- [ ] [P0-T17] Run `poetry run ruff check .` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-py-ruff.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (violation count by rule family).
- [ ] [P0-T18] Run `poetry run pyright` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-py-pyright.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error, warning, and information counts).
- [ ] [P0-T19] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-py-pytest-coverage.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` carrying the numeric passed/failed counts and the numeric `TOTAL` line and branch coverage percentages. Do not substitute a `--cov=<path>.py` form: a file-path `--cov` target measures nothing and produces a gate that cannot fail.
- [ ] [P0-T20] Confirm and record in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/baseline/baseline-py-pytest-coverage.<ISO>.md` that the two defect-codifying assertions are present at their cited anchors before any change: `assert potential_path not in fs.files` at `tests/scripts/dev_tools/test_new_active_feature_folder.py:333` and `assert active_file not in fs.files` at `tests/scripts/dev_tools/test_new_active_feature_folder_part4.py:284`.

### Phase 1 — Fail-Before Regression Capture

- [ ] [P1-T1] [expect-fail] Add the Jest case `retains the promoted potential file and writes issue.md in full mode` to the new sibling file `extensions/drm-copilot/test/lib/new-active-feature-folder/flow.promoted-disposition.test.ts`: seed the potential file under `docs/features/potential/promoted/` in `FakeFolderFileSystem`, run `createActiveFolder` with `workMode: "full-bug"`, and assert that `<target>/issue.md` exists carrying `- Work Mode: full-bug` AND that the promoted path is still present with unchanged content, AND that the emitted line is `Copied potential file to <path>`. The new file is created here rather than appended to `flow.test.ts` because `flow.test.ts` is 499 lines against the 500-line limit in `.claude/rules/general-code-change.md`; the sibling file is matched by `testMatch: ["**/test/**/*.test.ts"]` and by the `new-active-feature-folder` path pattern, so no command in this plan changes.
- [ ] [P1-T2] [expect-fail] Add the Jest case `retains the promoted potential file in minor-audit mode` to `extensions/drm-copilot/test/lib/new-active-feature-folder/flow.promoted-disposition.test.ts` with `workMode: "minor-audit"`, asserting promoted-record retention, `<target>/issue.md` carrying `- Work Mode: minor-audit`, and the emitted `Copied potential file to <path>` line, thereby covering the second placement site at `flow.ts:283`.
- [ ] [P1-T3] [expect-fail] Extend the existing auto-resolve case at `extensions/drm-copilot/test/lib/new-active-feature-folder/flow.test.ts:90-113` with an assertion that the `promoted/` source is still present after `createActiveFolder` returns. Add at most one assertion line so `flow.test.ts` does not exceed 500 lines; if it would, relocate the `resolves the feature name from a valid promoted active file` case into `flow.promoted-disposition.test.ts` instead of extending it in place.
- [ ] [P1-T4] Add the Jest case `takes the move branch for a sibling path that is only a string prefix of the promoted root` to `extensions/drm-copilot/test/lib/new-active-feature-folder/flow.promoted-disposition.test.ts`: seed the source as a FILE directly under `docs/features/potential/` whose basename begins with `promoted`, for example `${WORKSPACE}/docs/features/potential/promoted-notes-feature.md`, so its resolved path is a string prefix of `${WORKSPACE}/docs/features/potential/promoted` but is not contained in it. Assert the source is removed and the emitted line is `Moved potential file to <path>`, proving the containment predicate requires a `<root>/` boundary and is not prefix-only. This case passes both before and after the fix; it is a regression guard against a `startsWith(root)` implementation, not a fail-before case, and is therefore not tagged `[expect-fail]`.
- [ ] [P1-T5] [expect-fail] Create `extensions/drm-copilot/test/lib/promotion-lifecycle-sequence.test.ts` with the case `retains the promoted record across potential_to_issue then new_active_feature_folder`: run `promotePotential` then `createActiveFolder` against one shared in-memory filesystem that implements both `PotentialFileSystem` and `FolderFileSystem`, with an injected `GhClient` fake and an injected `nowProvider`; assert the promoted record is present and byte-identical after both calls and that `<active>/issue.md` carries the work-mode marker. No temporary files, no `gh` invocation, no dependency on the real `docs/features/` tree.
- [ ] [P1-T6] [expect-fail] Add the cases `throws when the reported destination path is absent`, `throws when the reported artifact path is absent`, and `returns the enriched record when every reported path exists` to `extensions/drm-copilot/test/lib/new-active-feature-folder/new-active-feature-folder-service-call.test.ts`, injecting a fake filesystem whose `exists` returns `false` for the reported path and asserting the thrown message names both the tool `new_active_feature_folder` and the absent path.
- [ ] [P1-T7] [expect-fail] Add the cases `throws when the promoted destination is absent` and `returns the enriched record when the destination exists` to `extensions/drm-copilot/test/lib/potential-to-issue/potential-to-issue-service-call.test.ts`, asserting the thrown message names both the tool `potential_to_issue` and the absent path.
- [ ] [P1-T8] [expect-fail] Invert `assert potential_path not in fs.files` at `tests/scripts/dev_tools/test_new_active_feature_folder.py:333` to assert retention of the `promoted/`-seeded source, and rename the test from `test_create_feature_folder_moves_potential_and_updates_files` to a name that states retention.
- [ ] [P1-T9] [expect-fail] Invert `assert active_file not in fs.files` at `tests/scripts/dev_tools/test_new_active_feature_folder_part4.py:284` to assert retention of the `promoted/`-seeded source in `test_create_active_folder_auto_resolve_feature_name_from_promoted_active_file`.
- [ ] [P1-T10] [expect-fail] Run `npm test -- --testPathPatterns "new-active-feature-folder|potential-to-issue|promotion-lifecycle-sequence"` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/fail-before-ts-jest.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (expected non-zero), and `Output Summary:` naming each failing test from P1-T1, P1-T2, P1-T3, P1-T5, P1-T6, and P1-T7 and the assertion text that failed, and confirming that P1-T4 passed (it is a pre-existing-behavior guard, not a fail-before case). `npm test` is `node run-jest.cjs`, so the issue-#423 prohibited-flag guard and `--config jest.config.cjs` still apply; coverage is deliberately omitted because `jest.config.cjs` carries per-file `coverageThreshold` entries for 38 unrelated files that a filtered run cannot satisfy. Coverage is captured only by the full-suite runs in P0-T15 and P7-T5.
- [ ] [P1-T11] [expect-fail] Run `poetry run pytest tests/scripts/dev_tools/test_new_active_feature_folder.py tests/scripts/dev_tools/test_new_active_feature_folder_part4.py --no-cov` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/fail-before-py-pytest.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (expected non-zero), and `Output Summary:` naming the two failing tests from P1-T8 and P1-T9.
- [ ] [P1-T12] If and only if a failing run is structurally impossible for any of P1-T1 through P1-T9, write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/fail-before-exception.<ISO>.md` containing `Timestamp:`, `WhyFailingRunImpossible:` (1-3 sentences), the affected task IDs, and an alternative proof section. Otherwise mark this task complete with the note that both failing runs were captured and no exception dossier is required, and record `SearchScope:`, `SearchPatterns: fail-before-exception.*.md`, and `SearchResult:` in that note.

### Phase 2 — TypeScript Disposition Fix in `flow.ts`

- [ ] [P2-T1] Add a compact disposition helper to `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts` that takes the resolved potential path and the workspace path and returns whether the source lies under `docs/features/potential/promoted`, reusing the existing private `isRelativeTo` helper at `flow.ts:413-420` and the `joinPosix(workspacePath, "docs/features/potential/promoted")` construction already used at `flow.ts:127-130`. If the addition would push `flow.ts` past 500 lines, place the helper in `extensions/drm-copilot/src/lib/new-active-feature-folder/io.ts` (currently 396 lines) and import it, rather than appending to `flow.ts`.
- [ ] [P2-T2] Replace the unconditional `filesystem.move(potentialFile, potentialIssuePath)` at `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts:283` (minor-audit branch) with the disposition decision: `filesystem.copyFile` when the helper returns true, `filesystem.move` otherwise. The read-back and `upsertWorkModeMarker` write at `:284-288` are unchanged.
- [ ] [P2-T3] Replace the unconditional `filesystem.move(potentialFile, potentialIssuePath)` at `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts:346` (full branch) with the same disposition decision. The read-back and `upsertWorkModeMarker` write at `:347-351` are unchanged.
- [ ] [P2-T4] Change the emitted seeding line in `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts` so the copy branch emits `Copied potential file to ${potentialIssuePath}` and the move branch keeps `Moved potential file to ${potentialIssuePath}`, at BOTH emission sites (`:342` for the minor-audit branch and `:352` for the full branch). The minor-audit emission at `:342` runs after the placement at `:283`, so the disposition taken must be carried to it rather than recomputed inconsistently.
- [ ] [P2-T5] Verify that `extensions/drm-copilot/src/lib/new-active-feature-folder/io.ts` was not otherwise modified and that `findPotentialFile` (`io.ts:98-130`) retains its two-directory scan order, its `promoted/` fallback, its `_`-to-`-` normalization, its `.md` filter, its exclusion set, and its name-descending tie-break (INV-1).
- [ ] [P2-T6] Run `npm test -- --testPathPatterns "new-active-feature-folder"` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/post-fix-ts-flow.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (expected 0 for the flow and io suites), and `Output Summary:` recording that P1-T1, P1-T2, and P1-T3 now pass, that P1-T4 still passes (it passed before the fix as well), and that the existing move case at `flow.test.ts:254-286` and the discovery cases at `io.test.ts:26-76` still pass unmodified. `npm test` is `node run-jest.cjs`, so the issue-#423 prohibited-flag guard and `--config jest.config.cjs` still apply; coverage is deliberately omitted because `jest.config.cjs` carries per-file `coverageThreshold` entries for 38 unrelated files that a filtered run cannot satisfy. Coverage is captured only by the full-suite runs in P0-T15 and P7-T5.
- [ ] [P2-T7] Run `(Get-Content -LiteralPath 'extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts').Count` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/flow-ts-linecount-postchange.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the post-change count and confirming it is at or below 500.

### Phase 3 — TypeScript Receipt Post-Conditions

- [ ] [P3-T1] Hoist the filesystem instance constructed inline at `extensions/drm-copilot/src/lib/new-active-feature-folder/new-active-feature-folder-service-call.ts:115` (`input.fileSystem ?? new RealFolderFileSystem()`) into a local `const` and pass that same instance to `createActiveFolder`, so the post-condition observes the filesystem the workflow wrote to.
- [ ] [P3-T2] Add an existence post-condition to `extensions/drm-copilot/src/lib/new-active-feature-folder/new-active-feature-folder-service-call.ts` immediately before the result record is constructed at `:124-133`: assert `result.target` exists and, when `result.potentialIssuePath` is non-null, assert it exists; on failure `throw` with a message naming the tool `new_active_feature_folder` and the absent path so `toFailureToolResult` (`extensions/drm-copilot/src/mcp-tools.ts:110-123`) renders `ok: false`. A warning-only path or a partially populated `ok: true` receipt is prohibited.
- [ ] [P3-T3] Hoist the filesystem instance constructed inline at `extensions/drm-copilot/src/lib/potential-to-issue/potential-to-issue-service-call.ts:164` (`input.fileSystem ?? new RealPotentialFileSystem()`) into a local `const` and pass that same instance to `promotePotential`.
- [ ] [P3-T4] Add an existence post-condition to `extensions/drm-copilot/src/lib/potential-to-issue/potential-to-issue-service-call.ts` immediately before the result record is constructed at `:183-191`: assert `outcome.destination` exists and `throw` on failure with a message naming the tool `potential_to_issue` and the absent path. The `artifacts` entry for this tool is a GitHub issue URL and is deliberately excluded from the existence check.
- [ ] [P3-T5] Verify that `extensions/drm-copilot/src/mcp-tools.ts` is unmodified — `toMcpToolResult` (`:88-108`) and `toFailureToolResult` (`:110-123`) keep their current shape — and that no MCP tool signature, `artifact_type`, or orchestrator-state field changed (INV-7).
- [ ] [P3-T6] Run `npm test -- --testPathPatterns "service-call|promotion-lifecycle-sequence"` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/post-fix-ts-service-calls.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (expected 0), and `Output Summary:` recording that P1-T5, P1-T6, and P1-T7 now pass and that both arms of each new failure branch are exercised. `npm test` is `node run-jest.cjs`, so the issue-#423 prohibited-flag guard and `--config jest.config.cjs` still apply; coverage is deliberately omitted because `jest.config.cjs` carries per-file `coverageThreshold` entries for 38 unrelated files that a filtered run cannot satisfy. Coverage is captured only by the full-suite runs in P0-T15 and P7-T5.

### Phase 4 — Python Parity Mirror and Test Completion

- [ ] [P4-T1] Add a containment predicate to `scripts/dev_tools/new_active_feature_folder_flow.py` that returns whether the resolved potential file lies under `workspace_path / "docs" / "features" / "potential" / "promoted"`, following the existing `Path.relative_to` try/except pattern already used at `new_active_feature_folder_flow.py:76-81`.
- [ ] [P4-T2] Replace `filesystem.move(potential_file, potential_issue_path)` at `scripts/dev_tools/new_active_feature_folder_flow.py:206` (minor-audit branch) with `filesystem.copy_file(...)` when the predicate is true and `filesystem.move(...)` otherwise, using the `copy_file` member already declared on the `FileSystem` protocol at `scripts/dev_tools/new_active_feature_folder_models.py:53`.
- [ ] [P4-T3] Replace `filesystem.move(potential_file, potential_issue_path)` at `scripts/dev_tools/new_active_feature_folder_flow.py:266` (full branch) with the same disposition decision.
- [ ] [P4-T4] Change the emitted lines in `scripts/dev_tools/new_active_feature_folder_flow.py` at `:263` (minor-audit) and `:272` (full) so the copy branch prints `Copied potential file to {potential_issue_path}` and the move branch keeps `Moved potential file to {potential_issue_path}`, byte-matching the TypeScript wording chosen in P2-T4.
- [ ] [P4-T5] Add the pytest case `test_create_feature_folder_moves_unpromoted_potential` to the new file `tests/scripts/dev_tools/test_new_active_feature_folder_part5.py`, following the import and fixture conventions of `tests/scripts/dev_tools/test_new_active_feature_folder_part4.py`: seed the source directly under `docs/features/potential/` and assert it IS removed after the call and that the emitted line is `Moved potential file to ...`, preserving move-branch coverage after the P1-T8 inversion (INV-2). The new file is created rather than appending to `tests/scripts/dev_tools/test_new_active_feature_folder.py`, which is already 533 lines against the 500-line limit; the `_partN` split is the convention already in use in this directory (`_part2` 537, `_part3` 468, `_part4` 472).
- [ ] [P4-T6] Add a pytest case to `tests/scripts/dev_tools/test_new_active_feature_folder_part5.py` asserting the copy branch emits `Copied potential file to ...` for a `promoted/`-seeded source, so the Python emitted-line wording is covered in both arms.
- [ ] [P4-T7] Verify that `scripts/dev_tools/potential_to_issue.py` is unmodified by this change (its 639-line over-limit condition is pre-existing and separately tracked, and must not be touched or worsened), and that `scripts/dev_tools/new_active_feature_folder_io.py` `find_potential_file` (`:35-55`) is unmodified.
- [ ] [P4-T8] Run `poetry run pytest tests/scripts/dev_tools/test_new_active_feature_folder.py tests/scripts/dev_tools/test_new_active_feature_folder_part2.py tests/scripts/dev_tools/test_new_active_feature_folder_part3.py tests/scripts/dev_tools/test_new_active_feature_folder_part4.py tests/scripts/dev_tools/test_new_active_feature_folder_part5.py --no-cov` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/regression-testing/post-fix-py-new-active-feature-folder.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (expected 0), and `Output Summary:` recording that the inverted assertions from P1-T8 and P1-T9 pass and that the unpromoted-seeded suites in `_part2.py` and `_part3.py` are unaffected.

### Phase 5 — Documentation Corrections

- [ ] [P5-T1] Edit `docs/engineering/Feature Playbook.md:14` so it states that the promoted potential is COPIED into the active folder as `issue.md` and that the promoted record under `docs/features/potential/promoted/` is retained.
- [ ] [P5-T2] Edit `docs/features/potential/README.md:6` so step 2 states that a promoted file is copied into `docs/features/active/<feature-name>/` and that the record under `promoted/` is retained.
- [ ] [P5-T3] Append a dated correction note to `docs/research/2026-07-09-potential-entries-duplicate-audit.md` recording that its line-15 and line-28 claim ("no code path deletes or relocates them afterward") was falsified by issue #487 and that its supporting grep at line 24 covered only `extensions/drm-copilot/src/lib/potential-to-issue/` and never examined the `new-active-feature-folder` cluster. Do not rewrite the historical body; append only.
- [ ] [P5-T4] Add the retained-promoted-record expectation to the post-`new_active_feature_folder` integrity checks in `.claude/skills/feature-promotion-lifecycle/SKILL.md` (optional per spec; if declined, record the decline and its reason in the P5-T5 artifact rather than leaving the task ambiguous).
- [ ] [P5-T5] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/documentation-corrections.<ISO>.md` with `Timestamp:` and, for each of P5-T1 through P5-T4, the file path, the before text, the after text, and the disposition (applied or declined-with-reason).

### Phase 6 — Invariant, Parity, and Scope Verification

- [ ] [P6-T1] Perform a side-by-side inspection of `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts` (placement sites and emission sites) against `scripts/dev_tools/new_active_feature_folder_flow.py` (`:206`, `:266`, `:263`, `:272`) and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/ts-python-parity-inspection.<ISO>.md` with `Timestamp:` and a per-site table showing that the disposition rule and the emitted-line wording match in both languages, and recording that the receipt post-condition is deliberately TypeScript-only because the Python CLI emits no receipt (INV-6).
- [ ] [P6-T2] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/invariant-verification.<ISO>.md` with `Timestamp:` and a pass/fail line for each of INV-1 (discovery contract unchanged), INV-2 (unpromoted sources still move), INV-3 (`issue.md` bytes unchanged for the same input), INV-4 (no interface growth), INV-6 (byte parity), INV-7 (MCP surface unchanged), and INV-8 (no temporary files and no `gh` in tests), each citing the file, the location, and the verifying test or command.
- [ ] [P6-T3] Run `git diff --stat` from the workspace root and confirm in `evidence/other/invariant-verification.<ISO>.md` that the changed-file set is exactly the four production files, the eight test files (`flow.test.ts`, `flow.promoted-disposition.test.ts`, `promotion-lifecycle-sequence.test.ts`, `new-active-feature-folder-service-call.test.ts`, `potential-to-issue-service-call.test.ts`, `test_new_active_feature_folder.py`, `test_new_active_feature_folder_part4.py`, `test_new_active_feature_folder_part5.py`), and the documentation files named in Phase 5, with no unexpected file present.
- [ ] [P6-T4] Run a line count over every file modified or created in Phases 1 through 4 and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/file-size-invariant.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming each modified production and test file is at or below 500 lines, and explicitly confirming `extensions/drm-copilot/src/lib/new-active-feature-folder/flow.ts` (INV-5, AC-10). `tests/scripts/dev_tools/test_new_active_feature_folder.py` is 533 lines before this change — a pre-existing over-limit condition analogous to `scripts/dev_tools/potential_to_issue.py`. Record its pre-change and post-change line counts and confirm the count was not increased by P1-T8. Every other modified production and test file must be at or below 500 lines unconditionally.
- [ ] [P6-T5] Confirm and record in `evidence/other/invariant-verification.<ISO>.md` that no hand-repair or backfill of any previously lost promoted record was performed, and specifically that `docs/features/potential/promoted/2026-08-17-promotion-lifecycle-loses-promoted-record.md` remains absent as retained evidence.
- [ ] [P6-T6] Confirm and record in `evidence/other/invariant-verification.<ISO>.md` that no file under `.claude/rules/` or `.github/instructions/` was modified.
- [ ] [P6-T7] Run `git worktree list --porcelain` (read-only) and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/other/worktree-state-report.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the worktrees observed and stating explicitly that no worktree was removed or pruned and that `.claude/worktrees/agent-afc9f4fd25ec235a5/` was not touched. Diagnosis and report only; no removal command may be issued.

### Phase 7 — Final QC Toolchain Loop and Coverage Delta

- [ ] [P7-T1] Run `npx prettier --write "src/**/*.ts" "test/**/*.ts" "*.json" "*.cjs"` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-ts-prettier.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording whether any file was rewritten. If any file was rewritten, restart the TypeScript loop at P7-T1 and record a new iteration artifact.
- [ ] [P7-T2] Run `npx eslint --no-error-on-unmatched-pattern src test` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-ts-eslint.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error and warning counts; both must be 0). On failure or file rewrite, restart the TypeScript loop at P7-T1.
- [ ] [P7-T3] Run `npx tsc -p ./ --noEmit` from `extensions/drm-copilot/` and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-ts-tsc.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (type-error count must be 0). On failure, restart the TypeScript loop at P7-T1.
- [ ] [P7-T4] Record the architecture-boundary stage outcome in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-ts-architecture.<ISO>.md` with `Timestamp:`, `Command:` (the recursive glob for `.dependency-cruiser.cjs`), `EXIT_CODE:`, `Output Summary:`, plus `SearchScope:`, `SearchPatterns:`, and `SearchResult:`, cross-referencing the Phase 0 baseline finding that no dependency-cruiser configuration exists in this repository. This is an executed verification with a real exit code, not a skipped command; `EXIT_CODE: SKIPPED` is not a valid value.
- [ ] [P7-T5] Run `npm run test:coverage` from `extensions/drm-copilot/` (the repo-sanctioned `npx jest` wrapper described in P0-T15) and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-ts-jest-coverage.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (must be 0), and `Output Summary:` carrying the numeric suite counts and the numeric `text-summary` percentages for Statements, Branches, Functions, and Lines. On failure, restart the TypeScript loop at P7-T1.
- [ ] [P7-T6] Run `poetry run black .` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-py-black.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the count of reformatted files. If any file was reformatted, restart the Python loop at P7-T6 and record a new iteration artifact.
- [ ] [P7-T7] Run `poetry run ruff check .` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-py-ruff.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (violation count must be 0). Ruff is configured with `fix = true`, so if it rewrites any file, restart the Python loop at P7-T6.
- [ ] [P7-T8] Run `poetry run pyright` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-py-pyright.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error count must be 0). On failure, restart the Python loop at P7-T6.
- [ ] [P7-T9] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the workspace root and write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-py-pytest-coverage.<ISO>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (must be 0), and `Output Summary:` carrying the numeric passed/failed counts and the numeric `TOTAL` line and branch coverage percentages. Do not use a `--cov=<path>.py` form. On failure, restart the Python loop at P7-T6.
- [ ] [P7-T10] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/final-qc-loop-ledger.<ISO>.md` with `Timestamp:` and one row per loop iteration per language, recording the stage reached, the exit code, and whether files were rewritten, and confirming that the final iteration completed every stage of the language's loop in a single consecutive clean pass. Every recorded stage must carry a numeric exit code; `SKIPPED` is not an acceptable value for any planned command task.
- [ ] [P7-T11] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/coverage-delta.<ISO>.md` with `Timestamp:` and, for each of TypeScript and Python, the baseline line and branch coverage (from the Phase 0 artifacts), the post-change line and branch coverage (from P7-T5 and P7-T9), the delta, and the coverage of the new and changed lines in the four production files (`flow.ts`, `new-active-feature-folder-service-call.ts`, `potential-to-issue-service-call.ts`, `new_active_feature_folder_flow.py`). Compare each against the uniform thresholds line >= 85% and branch >= 75%, assert no regression on changed lines, and record that `quality-tiers.yml` is absent at the repository root so no T1-T4 classification applies while the uniform gates still bind. If any required numeric value is unavailable, the outcome is remediation-required, never PASS. Derive the TypeScript per-file and changed-line figures from the `DA:`/`BRDA:` records in `extensions/drm-copilot/coverage/lcov.info` produced by P7-T5; `text-summary` reports global totals only and is not a sufficient source for the per-file requirement. Derive the Python per-file figures from the `term-missing` per-file rows produced by P7-T9. Record that `jest.config.cjs` carries no `coverageThreshold` entry for any of the four production files, so the 85/75 gate is asserted here manually rather than by the runner.
- [ ] [P7-T12] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/ac-traceability.<ISO>.md` with `Timestamp:` and one row per acceptance criterion AC-1 through AC-12, naming the satisfying task IDs, the verifying test or command, and the evidence artifact path. The AC checkboxes in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` remain unchecked until execution completes; this task records traceability only.
- [ ] [P7-T13] Write `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/qa-gates/evidence-location-audit.<ISO>.md` with `Timestamp:`, `SearchScope:`, `SearchPatterns:`, and `SearchResult:` confirming that every artifact produced by this plan resides under `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/evidence/<kind>/` and that no artifact was written to `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/`, or `artifacts/post-change/`.

---

## Acceptance-Criteria Traceability

Every criterion in `docs/features/active/2026-08-17-promotion-lifecycle-loses-promoted-record-487/spec.md` maps to at least one task. All 12 checkboxes remain UNCHECKED in `spec.md`; this table is the planning-time mapping only.

| AC | Requirement (abbreviated) | Covering tasks |
| --- | --- | --- |
| AC-1 | Promoted record survives `new_active_feature_folder` in full mode | P1-T1, P1-T10, P2-T1, P2-T3, P2-T6 |
| AC-2 | Promoted record survives in minor-audit mode (`flow.ts:283`) | P1-T2, P1-T10, P2-T1, P2-T2, P2-T6 |
| AC-3 | `<targetDir>/issue.md` content unchanged in both branches | P1-T1, P1-T2, P1-T3, P2-T2, P2-T3, P2-T6, P6-T2 |
| AC-4 | An unpromoted source is still moved (INV-2) | P1-T4, P2-T6, P4-T5, P4-T8, P6-T2 |
| AC-5 | `new_active_feature_folder` receipt post-condition holds | P1-T6, P1-T10, P3-T1, P3-T2, P3-T6 |
| AC-6 | `potential_to_issue` receipt post-condition holds | P1-T7, P1-T10, P3-T3, P3-T4, P3-T6 |
| AC-7 | Sequenced lifecycle regression exists and fails before the fix | P1-T5, P1-T10, P1-T12, P3-T6 |
| AC-8 | The two defect-codifying Python assertions are inverted | P1-T8, P1-T9, P1-T11, P4-T8 |
| AC-9 | Python cluster stays in parity with the TypeScript behavior | P4-T1, P4-T2, P4-T3, P4-T4, P4-T6, P4-T8, P6-T1 |
| AC-10 | `flow.ts` remains within the 500-line limit | P0-T9, P2-T1, P2-T7, P6-T4 |
| AC-11 | Full toolchain passes for both languages with coverage recorded | P0-T12 through P0-T19, P7-T1 through P7-T11 |
| AC-12 | Conflicting documentation is corrected | P5-T1, P5-T2, P5-T3, P5-T4, P5-T5 |

## Out-of-Scope Confirmations

- No hand-repair or backfill of previously lost promoted records (verified by P6-T5).
- No change to `findPotentialFile` discovery order or its `promoted/` fallback (verified by P2-T5).
- No change to `potential_to_issue` move mechanics or issue-creation path (verified by P3-T5, P4-T7).
- No creation of `quality-tiers.yml` (recorded by P0-T10, P7-T11).
- No change to `scripts/dev_tools/potential_to_issue.py` (verified by P4-T7).
- No modification of `.claude/rules/**` or `.github/instructions/**` (verified by P6-T6).
- No git worktree removed or pruned (verified by P6-T7).
