# Remediation Plan — legacy-discovery-dotnet-vsto-analyzers (#369), Remediation Cycle 4

- Timestamp: 2026-07-19T04-15
- Issue: #369
- Work Mode: full-feature (per `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/issue.md`)
- Branch: `feature/legacy-discovery-dotnet-vsto-analyzers-369`
- PR: #384 (base `epic/legacy-discovery-and-parity-integration`)
- Sole requirements source: `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-inputs.2026-07-19T04-15.md` (single Blocking finding — the required CI check `Extension Tests (ubuntu-latest)` fails because the jest suite `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts` reports the two bundled discovery-artifact-gate hooks are registered in no pack manifest). No scope beyond that document is authorized.
- Remediation approach: register the two bundled hook paths in the core pack manifest `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` by appending them at the alphabetically-correct positions in its `paths` array, preserving the array's existing ordering convention and changing nothing else. This is the established repository pattern (issue #367 registered the sibling discovery skills in `core.json`; the hooks are core runtime). This plan modifies no analyzer module, no hook logic, no test, and no policy rule file.
- Feature folder (`<FEATURE>`): `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369`
- Evidence locations: canonical `<FEATURE>/evidence/<kind>/` only (`evidence/remediation-baseline/`, `evidence/qa-gates/`, `evidence/regression-testing/`). No `artifacts/`-rooted evidence path is permitted for feature evidence. Prior-cycle evidence (`r1c1-*`, `r2c2-*`, `r3c3-*`) MUST NOT be overwritten. All new artifacts for this cycle use the `r4c4-` filename prefix.
- `<TS>` in evidence filenames below denotes the execution-time ISO-8601 timestamp in `yyyy-MM-ddTHH-mm` format, captured when the task runs.
- Every evidence artifact for a command step MUST include: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `EXIT_CODE: SKIPPED` is not a valid passing outcome for any command-bearing task in this plan.
- All commands run from the repository root unless a task states otherwise. Shell is PowerShell unless a task states otherwise.

## Ground-Truth Contracts (read-only inputs; do not modify)

- The two bundled hook files that must be registered are already present on disk in the bundle (pushed down in cycle 2) and are byte-identical mirrors of the repo-root originals:
  - `.claude/hooks/enforce-discovery-artifact-gate.ps1`
  - `.claude/hooks/validate-discovery-artifact-gate.ps1`
- The extension jest suite `extensions/drm-copilot/test/lib/push-down/claude-pack-manifest-completeness.test.ts` enumerates every bundled `.claude` agent/skill/hook file under `extensions/drm-copilot/resources/claude-customizations/.claude/` and asserts each appears in the union of `paths` arrays across `extensions/drm-copilot/resources/claude-customizations/pack-manifests/*.json` (minus three documented pre-existing, out-of-scope exceptions). The current `missing` set is exactly the two hook paths above.
- The only file this plan edits is `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`. The edit appends exactly two string entries to its `paths` array. The `paths` array orders entries alphabetically within each `.claude/` subgroup; the two new entries insert at:
  - `.claude/hooks/enforce-discovery-artifact-gate.ps1` — between `.claude/hooks/enforce-completion-consistency.ps1` and `.claude/hooks/enforce-epic-invocation-origin.ps1`.
  - `.claude/hooks/validate-discovery-artifact-gate.ps1` — between `.claude/hooks/validate-bash.ps1` and `.claude/hooks/validate-executor-output.ps1`.
- The Python bundle contract test `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts` currently passes (cycle 2) and must remain green. This plan adds no Python production code and expects no Python coverage change.
- No TypeScript production source is modified; the change is a JSON manifest data edit. The jest run is a completeness/pass verification of the manifest data, not a TypeScript logic change.

### Phase 0 — Policy Read and Baseline Capture

- [x] [P0-T1] Read policy files in the required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/typescript.md`, `.claude/rules/typescript-suppressions.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md` — and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated and the list includes `.claude/rules/typescript.md` and `.claude/rules/python.md`.
- [x] [P0-T2] [expect-fail] Capture the fail-before baseline of the failing extension suite: run `npm --prefix extensions/drm-copilot run test -- test/lib/push-down/claude-pack-manifest-completeness.test.ts` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-extension-suite-fail-before.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the failing assertion and the exact `Received` missing-path array. Acceptance: the recorded `EXIT_CODE` is non-zero and the `Output Summary:` records the `missing` set as exactly `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1`. This task is expected to fail before the fix; a passing result here means the finding did not reproduce and the plan must stop for re-scoping.
- [x] [P0-T3] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-black-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T4] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-ruff-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T5] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-pyright-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the artifact exists with all four fields and the recorded exit code matches the actual run.
- [x] [P0-T6] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-phase0-pytest-baseline.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric baseline line-coverage and branch-coverage percentages, the pass/fail/skip counts, and explicit confirmation that `test_bundled_claude_payload_contains_all_repo_runtime_contracts` passes. Acceptance: the artifact exists with numeric coverage values (placeholders such as `UNVERIFIED` are invalid) and records the bundle contract test as passing.

### Phase 1 — Register Bundle Hooks in the Core Pack Manifest

- [x] [P1-T1] Edit `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, adding exactly two string entries to its `paths` array at the alphabetically-correct positions: insert `.claude/hooks/enforce-discovery-artifact-gate.ps1` immediately after `.claude/hooks/enforce-completion-consistency.ps1` and before `.claude/hooks/enforce-epic-invocation-origin.ps1`; insert `.claude/hooks/validate-discovery-artifact-gate.ps1` immediately after `.claude/hooks/validate-bash.ps1` and before `.claude/hooks/validate-executor-output.ps1`. Change nothing else in the file (no reordering of existing entries, no whitespace or key changes outside the two inserted lines). Acceptance: `Select-String -Pattern 'enforce-discovery-artifact-gate\.ps1|validate-discovery-artifact-gate\.ps1' extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` returns exactly two matches at the specified positions.
- [x] [P1-T2] Confirm the edit is well-formed JSON and minimal: run `node -e "JSON.parse(require('fs').readFileSync('extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json','utf8'))"` to validate parse, then run `git diff -- extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-manifest-edit-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the parse result and the diff. Acceptance: the JSON parse exits 0 and the diff adds exactly two lines (the two hook paths) with no other change.
- [x] [P1-T3] Re-run the failing suite to confirm it now passes: run `npm --prefix extensions/drm-copilot run test -- test/lib/push-down/claude-pack-manifest-completeness.test.ts` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/regression-testing/r4c4-pass-after-manifest-completeness.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the pass count and that the `missing` set is now empty. If the suite still fails, correct the manifest edit and re-run until it passes; `EXIT_CODE: SKIPPED` is not a valid outcome. Acceptance: exit code 0 and the `claude-pack-manifest-completeness` suite passes.

### Phase 2 — Verification (Extension Suite Green, Python Toolchain Green, No Scope Creep)

- [x] [P2-T1] Run the full extension test suite: `npm --prefix extensions/drm-copilot run test` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-extension-suite.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the total suite/test pass counts. If the run fails, fix within the authorized manifest scope and re-run; `EXIT_CODE: SKIPPED` is not a valid outcome. Acceptance: exit code 0 with all jest suites passing, including `claude-pack-manifest-completeness`.
- [x] [P2-T2] Run `poetry run black --check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or reformats files, fix, then restart the loop from P2-T2. Acceptance: exit 0 with no files changed, recorded in the artifact.
- [x] [P2-T3] Run `poetry run ruff check .` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails or auto-fixes files, fix, then restart the loop from P2-T2. Acceptance: exit 0 recorded in the artifact.
- [x] [P2-T4] Run `poetry run pyright` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If this stage fails, fix, then restart the loop from P2-T2. Acceptance: exit 0 with 0 errors recorded in the artifact.
- [x] [P2-T5] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the numeric post-change line-coverage and branch-coverage percentages, the pass/fail/skip counts, and explicit confirmation that `test_bundled_claude_payload_contains_all_repo_runtime_contracts` remains green. If this stage fails, fix, then restart the loop from P2-T2. Acceptance: exit 0 with numeric coverage values recorded (placeholders such as `UNVERIFIED` are invalid) and the bundle contract test recorded as passing.
- [x] [P2-T6] Write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-python-coverage-delta.<TS>.md` comparing the Phase 0 baseline Python coverage (from `evidence/remediation-baseline/r4c4-phase0-pytest-baseline.<TS>.md`) against the P2-T5 post-change coverage, reporting: baseline line/branch %, post-change line/branch %, and confirmation that line coverage >= 85% and branch coverage >= 75% hold with no regression. Acceptance: the artifact contains all numeric values and both threshold checks pass. (This cycle adds no Python production code, so no coverage regression is expected.)
- [x] [P2-T7] Verify no out-of-scope file was modified: run `git diff --name-only origin/epic/legacy-discovery-and-parity-integration...HEAD` plus the uncommitted `git diff --name-only` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-scope-verification.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the changed-file set. Acceptance: the only non-documentation change introduced by this cycle is `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`; no file under `.claude/rules/`, `.github/instructions/`, `extensions/drm-copilot/src/`, `extensions/drm-copilot/test/`, or any analyzer/hook logic path is modified by this cycle.

### Phase 3 — Commit and Push

- [x] [P3-T1] Stage and commit the manifest change together with this cycle's plan-checklist updates and all Phase 0–2 evidence artifacts (`extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/remediation-plan.2026-07-19T04-15.md`, and every new file under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/remediation-baseline/r4c4-*`, `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/regression-testing/r4c4-*`, and `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-*`) in a single commit with a professional, factual message referencing issue #369 remediation cycle 4 and the pack-manifest registration of the two discovery-artifact-gate bundle hooks. Acceptance: `git log -1 --name-only` lists the `core.json` manifest, the plan, and only `r4c4-*` evidence files, and no other source, test, or policy path.
- [x] [P3-T2] Confirm the working tree is clean of untracked cycle evidence and unstaged changes: run `git status --porcelain` and confirm no uncommitted `r4c4-*` evidence or manifest change remains. Acceptance: the command produces no output.
- [x] [P3-T3] Push the updated branch: `git push origin feature/legacy-discovery-dotnet-vsto-analyzers-369`. Acceptance: the command exits 0 and the remote branch HEAD matches the local branch HEAD (verify with `git rev-parse HEAD` and `git rev-parse origin/feature/legacy-discovery-dotnet-vsto-analyzers-369` after the push).
- [x] [P3-T4] Confirm PR #384 remains mergeable after the push: run `gh pr view 384 --json mergeable,mergeStateStatus` and write `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/qa-gates/r4c4-pr-mergeable-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: the recorded `mergeable` value is `MERGEABLE` (not `CONFLICTING` or `UNKNOWN`).

## Exit Condition

All Phase 0–3 tasks complete with their acceptance criteria satisfied; the two bundled hook paths `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1` are registered in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` at their alphabetically-correct positions with no other manifest change; the extension jest suite `claude-pack-manifest-completeness` and the full extension test suite pass; the full Python toolchain (Black, Ruff, Pyright, Pytest with coverage) passes with line coverage >= 85% and branch coverage >= 75% and no regression, and `test_bundled_claude_payload_contains_all_repo_runtime_contracts` remains green; no analyzer module, hook logic, test, or policy rule file is modified; fresh evidence exists under `docs/features/active/2026-07-17-legacy-discovery-dotnet-vsto-analyzers-369/evidence/` with the `r4c4-` prefix without overwriting prior-cycle evidence; the branch is pushed; and `gh pr view 384 --json mergeable` reports `MERGEABLE`.
