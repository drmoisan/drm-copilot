# 2026-08-07-parallel-orchestrator-surface - Plan

- **Issue:** #441
- **Parent (optional):** Epic `parallel-orchestration` (`docs/features/epics/parallel-orchestration/epic.md`), wave 3, feature F5
- **Owner:** drmoisan
- **Last Updated:** 2026-08-07
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-feature

## Required References

- `CLAUDE.md` (standing instructions)
- `.claude/rules/general-code-change.md` (cross-language code change policy)
- `.claude/rules/general-unit-test.md` (cross-language unit test policy)
- `.claude/rules/python.md`, `.claude/rules/python-suppressions.md` (Python toolchain and standards)
- Requirements: `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/spec.md` (22 acceptance criteria, authoritative; contains three settled `## Adjudicated Decisions` that must not be re-opened) and `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/user-story.md` (11 acceptance criteria)
- Research: `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/research/2026-08-07T12-30-parallel-orchestrator-surface-research.md`
- Design authority: `docs/research/2026-08-07-parallel-orchestration-design-research.md` (§ references)
- Epic constraints: `docs/features/epics/parallel-orchestration/epic.md` (non-goals, Wave-4 Contention Note)

**All work must comply with these policies; do not duplicate their content here.**

## Scope Summary

Deliverables (all new files; additive only):

1. `.claude/agents/parallel-orchestrator.md` — agent persona (spec R1).
2. `.claude/skills/parallel-orchestrate/SKILL.md` — procedure skill (spec R2, sixteen-through-eighteen section layout per R2.1).
3. `.claude/skills/parallel-run/SKILL.md` — user-invocable entry point (spec R3).
4. `docs/features/templates/parallel/parallel-status.md` — generated-projection template (spec Deliverable 4).
5. `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` — contract tests (spec R4).
6. Bundled mirrors of deliverables 1-3 at `extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md`, `.../.claude/skills/parallel-orchestrate/SKILL.md`, and `.../.claude/skills/parallel-run/SKILL.md`, plus registration of those three `.claude`-relative paths in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`. This is a repository bundle-parity requirement enforced by `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and `tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py`, not new feature surface. It does not violate Decision 2: no file under `.claude/hooks/` and no `.claude/settings.json` change is involved.

Hard constraints (non-negotiable; from spec `## Constraints` and Adjudicated Decisions 1-3):

- `.claude/agents/epic-orchestrator.md`, `.claude/skills/epic-orchestrate/SKILL.md`, and `.claude/skills/orchestrate/SKILL.md` are NOT modified.
- No file under `.claude/hooks/` and no `.claude/settings.json` change (Decision 2).
- No schema definition or alteration; F3 owns manifest (§11) and checkpoint (§12) schemas including the `merge_status` enum (Decision 3).
- The parent (`parallel-orchestrator`) performs `gh pr merge --merge` per item after durably confirming CI green; the child runs unmodified with `epic_mode` false/absent and is never instructed to merge its own PR (Decision 1).
- The literal child kickoff marker is `Parallel mode: true`.
- No integration branch, no final integration PR, no fan-in merge-conflict path anywhere in the delivered surface.
- The three reserved wave-4 sections `## Mutation Protocol (F6)`, `## Enforcement Hooks (F7)`, `## Radius Drift Detection (F8)` are authored by F5 as the final three top-level headings of `parallel-orchestrate/SKILL.md`, each with a one-line reserved body.

Languages in scope: Markdown (surface files) and Python (contract tests). The Python toolchain loop (Black -> Ruff -> Pyright -> Pytest with coverage) applies; coverage policy is mandatory (line >= 85%, branch >= 75%). A single scoped TypeScript test invocation is required to confirm the TS twin of the pack-manifest completeness check remains green. No `.ts` source file is modified, so the full TypeScript toolchain loop does not apply.

Evidence root (canonical, non-overridable): `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/` with kinds `baseline/`, `regression-testing/`, `qa-gates/`, `other/`. No evidence path may resolve under `artifacts/`.

## F7 Coordination Note (carry forward to the F7 planner)

Per spec `## Cross-Feature Dependencies` and research E.4, the F5-delivered surface is not executable end-to-end until F7 lands. The F7 planner must scope both existing epic hooks for the parallel case, in addition to the §9 F7 deliverables:

1. `.claude/hooks/enforce-epic-merge-gate.ps1` (registered at `.claude/settings.json:112`) denies any `gh pr merge --merge` without an epic-shaped checkpoint (`EPIC_MERGE_GATE_BLOCKED`). The parallel parent's per-item merge (spec R2.8 step 3) is denied until F7 extends or scopes the gate's allow conditions.
2. `.claude/hooks/enforce-epic-worktree-removal-gate.ps1` (registered at `.claude/settings.json:116`) denies any `git worktree remove` without a matching epic checkpoint record (`EPIC_WORKTREE_REMOVAL_BLOCKED`). Because `PreToolUse` denials are conjunctive, F7's new `enforce-parallel-worktree-removal-gate.ps1` alone cannot override this deny; F7 must also coordinate the epic gate's conditions.

F5 modifies neither hook nor `.claude/settings.json`. The delivered skill text states this dependency, naming both hooks and both block reasons (P2-T8, P2-T10, verified in P2-T15).

## Execution-Time Re-Verification (spec `## Assumptions`)

F3 and F4 had not landed when the spec and research were authored. Before Phase 1, the executor performs the re-verifications listed in P0-T7 against the current branch state and records the outcome. Where F3/F4 artifacts have landed, their shipped values (exact `merge_status` enum, `artifact_type` string, kickoff-artifact location) supersede the design-document stand-ins; where they have not landed, the design-document contract (§11, §12, §6) is used exactly as specified in the spec. This does not re-open any Adjudicated Decision.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Policy Compliance and Baseline Capture

- [ ] [P0-T1] Read the policy files in the order defined by `policy-compliance-order`: (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) `.claude/rules/python.md`, (5) `.claude/rules/python-suppressions.md`; then write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the numbered order above), and the explicit list of files read.
  - Acceptance: The artifact exists at the stated path and contains all three required fields with the five files listed.
- [ ] [P0-T2] Capture the Python formatting baseline by running `poetry run black --check .` from the repository root and writing `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-black.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: The artifact exists and records the actual exit code and a concise result summary.
- [ ] [P0-T3] Capture the Python lint baseline by running `poetry run ruff check .` from the repository root and writing `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-ruff.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: The artifact exists and records the actual exit code and a concise result summary.
- [ ] [P0-T4] Capture the Python type-check baseline by running `poetry run pyright` from the repository root and writing `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-pyright.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: The artifact exists and records the actual exit code and the error/warning counts.
- [ ] [P0-T5] Capture the Python test-and-coverage baseline by running `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root and writing `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-pytest-coverage.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that includes the numeric pass/fail counts and the numeric baseline line-coverage and branch-coverage percentages.
  - Acceptance: The artifact exists and `Output Summary:` contains numeric coverage headline values (no placeholders).
- [ ] [P0-T6] Record baseline SHA-256 content hashes of `.claude/agents/epic-orchestrator.md`, `.claude/skills/epic-orchestrate/SKILL.md`, and `.claude/skills/orchestrate/SKILL.md` (for example via `Get-FileHash -Algorithm SHA256`) into `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-frozen-surface-hashes.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` listing the three path-to-hash pairs.
  - Acceptance: The artifact exists and lists exactly three hashes; these hashes are the pin source for P4-T6 and the byte-identity checks in P5-T1.
- [ ] [P0-T7] Verify the upstream-landing state on the current branch and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-upstream-contracts.<yyyy-MM-ddTHH-mm>.md` recording, with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`: (a) whether `scripts/dev_tools/validate_parallel_orchestrator_state.py` and `.claude/rules/parallel-orchestration.md` exist (F3 landed or not) and, if landed, the exact `merge_status` value set and `artifact_type` string shipped; (b) whether a `parallel` route exists in `config/orchestration-routing.json`; (c) whether F4's kickoff-artifact convention (`docs/features/parallel/<slug>/parallel-kickoff.md`) is confirmed by landed F4 artifacts. Record which contract source (landed artifact vs design-document stand-in per spec `## Assumptions`) governs each of the three items for the remainder of execution.
  - Acceptance: The artifact exists and states a governing contract source for each of the three items; no spec Adjudicated Decision is altered.

### Phase 1 — Agent Persona: `.claude/agents/parallel-orchestrator.md`

- [ ] [P1-T1] Create `.claude/agents/parallel-orchestrator.md` with YAML frontmatter adapted from `.claude/agents/epic-orchestrator.md` per spec R1: `name: parallel-orchestrator` (exact string), `model: opus`, a `description`, a `tools` allowlist that excludes `Agent(pr-author)` and scopes `Write`/`Edit` to `docs/features/parallel/**` and `artifacts/orchestration/**`, `skills` including `parallel-orchestrate`, `memory: project`, and a `SubagentStop` hook invoking `.claude/hooks/validate-orchestrator-output.ps1` with `-CheckpointPath artifacts/orchestration/parallel-orchestrator-state.json` and `-ArtifactType parallel-orchestrator-state` (use the artifact-type string confirmed in P0-T7 if F3 landed a different one).
  - Acceptance: The file exists; frontmatter parses; `name:` is exactly `parallel-orchestrator`; `tools` contains no `Agent(pr-author)` entry; the `SubagentStop` hook line carries both stated parameters.
- [ ] [P1-T2] Author the persona intro and `## Skill` section in `.claude/agents/parallel-orchestrator.md`: cohort and per-item-merge framing; claim authorization to delegate `Agent(orchestrator)` without claiming exclusivity (research A.1); bind `## Skill` to `parallel-orchestrate`.
  - Acceptance: The intro contains no integration-branch or final-PR framing; `## Skill` names `parallel-orchestrate`.
- [ ] [P1-T3] Author `## Startup Protocol` in `.claude/agents/parallel-orchestrator.md`: read `CLAUDE.md` and rules; read `artifacts/orchestration/parallel-orchestrator-state.json`; on a `parallel_slug` match, resume via durable ground truth re-derived from `git worktree list --porcelain`, `git branch`, and `gh pr view --json state,mergedAt,headRefOid`; otherwise begin at manifest parsing from `docs/features/parallel/<slug>/parallel.md`.
  - Acceptance: The section names the checkpoint path, all three re-derivation commands, and the manifest path.
- [ ] [P1-T4] Author `## Invocation Origin` in `.claude/agents/parallel-orchestrator.md`: entry points `/parallel-orchestrate` and `/parallel-run`; a prohibition on invoking `Agent(parallel-orchestrator)` from within an `orchestrator` run; a note that hook coverage is delivered by F7's extension of `.claude/hooks/enforce-epic-invocation-origin.ps1` (§9) and is not shipped by this feature.
  - Acceptance: The section names both entry points and contains the prohibition sentence.
- [ ] [P1-T5] Author `## Prepared-Run Execution` in `.claude/agents/parallel-orchestrator.md`: handoff from `parallel-planner`; read F4's committed kickoff artifact; each item resumes at atomic execution from its committed `plan-path` rather than re-running promotion, research, or planning; no integration-ref discovery mechanics.
  - Acceptance: The section contains the resume-at-plan-path statement and no `git cat-file`/`git show` integration-ref fallback.
- [ ] [P1-T6] Author `## Delegation Model` in `.claude/agents/parallel-orchestrator.md`: a single channel `Agent(orchestrator)` per item with `isolation: "worktree"`, `run_in_background: true`, branch base `origin/main`, and `model` bound to the routing receipt; no `Agent(pr-author)` channel; no upstream-context citation lines (no `depends_on`).
  - Acceptance: The section defines exactly one delegation channel with all four stated spawn parameters.
- [ ] [P1-T7] Author `## Cohort Scheduling` in `.claude/agents/parallel-orchestrator.md`: consumes `cohorts[]` seeded by F4; enforces the cohort barrier and `max_concurrency` slot filling in ascending item-key (`issue_num`) order; never computes or recolors cohorts.
  - Acceptance: The section states the consume-only rule and the slot-filling order.
- [ ] [P1-T8] Author `## Checkpoint Persistence` in `.claude/agents/parallel-orchestrator.md`: the §12 field list F5 writes to `artifacts/orchestration/parallel-orchestrator-state.json`, cross-referencing the skill's `## Parallel-Level Checkpoint` section for the full enumeration; state that F3 owns the schema.
  - Acceptance: The section names the checkpoint path and the F3 schema-ownership statement.
- [ ] [P1-T9] Author `## Documentation Maintenance` in `.claude/agents/parallel-orchestrator.md`: `docs/features/parallel/<slug>/parallel-status.md` is a generated projection, regenerated at the boundaries defined in the skill's `## Documentation Maintenance Boundaries`, never hand-authored.
  - Acceptance: The section contains the generated-never-hand-authored statement.
- [ ] [P1-T10] Author `## Completion Requirements` in `.claude/agents/parallel-orchestrator.md`: mode-dependent per §8.7 (`closed`: every non-withdrawn item `merged` or `worktree_removed`, final `parallel-status.md` regeneration, passing `require_complete` checkpoint validation; `open`: terminates only via `/parallel-close`, named as F6-owned); no integration-PR condition.
  - Acceptance: The section defines both modes and contains no integration-PR condition.
- [ ] [P1-T11] Verify `.claude/agents/parallel-orchestrator.md` end-state: the body contains exactly the nine required headings `## Skill`, `## Startup Protocol`, `## Invocation Origin`, `## Prepared-Run Execution`, `## Delegation Model`, `## Cohort Scheduling`, `## Checkpoint Persistence`, `## Documentation Maintenance`, `## Completion Requirements`; the file contains none of the literals `Epic mode: true`, `--base epic/`, `integration-to-main`.
  - Acceptance: A grep-based check confirms all nine headings present and all three forbidden literals absent; result noted in the task completion output.

### Phase 2 — Procedure Skill: `.claude/skills/parallel-orchestrate/SKILL.md`

- [ ] [P2-T1] Create `.claude/skills/parallel-orchestrate/SKILL.md` with YAML frontmatter (`context: fork`, `agent: parallel-orchestrator`, argument hint accepting the parallel manifest path or slug), the `# Parallel Orchestrate Skill` intro heading, and the `## Prerequisites` section (CLAUDE.md, rules, compliance reading order), matching R2.1 layout items 1-3.
  - Acceptance: The file exists; frontmatter declares `context: fork`, `agent: parallel-orchestrator`, and an argument hint accepting the parallel manifest path or slug; the intro heading and `## Prerequisites` are present in that order.
- [ ] [P2-T2] Author `## Parallel Manifest Consumption` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.2: reference F3's schema authority (`.claude/rules/parallel-orchestration.md` and the F3 validators); `issue_num` is the primary key; F5 reads `mode`, `max_concurrency`, and per-item identity and state; F5 never writes the manifest; a malformed manifest is rejected as a synthetic Blocking finding before any kickoff; the section is explicitly not a schema authority.
  - Acceptance: The section contains the F3 schema-authority reference and the malformed-manifest rejection rule.
- [ ] [P2-T3] Author `## Cohort Consumption and Ordering` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.3: cohorts (`cohorts[] { index, generation, item_keys[] }`) are an F4-seeded input; F5 consumes and never computes or recolors (recoloring is F6/F8 scope); items within a cohort are non-conflicting by construction, may branch from the same `main` tip, and may merge in any order.
  - Acceptance: The section contains the consume-only statement and the same-tip/any-order statement.
- [ ] [P2-T4] Author `## Cohort Barrier and Max-Concurrency Slot Filling` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.4: cohort `N+1` branches from `main` only after every cohort-`N` item is `merged` or `worktree_removed`; `current_cohort` increments only on durable `git`/`gh` confirmation, never from in-memory notifications; a blocked item holds the barrier; `max_concurrency` caps fan-out independently of cohort size with slots filled and refilled in `ascending item-key order`; name (without shipping) the F7 deliverables `enforce-parallel-cohort-barrier.ps1`, `PARALLEL_COHORT_BARRIER_BLOCKED`, and `PARALLEL_COHORT_BARRIER_VIOLATION`.
  - Acceptance: The section contains the barrier predicate, the token `max_concurrency`, the exact phrase `ascending item-key order`, and the three F7 names.
- [ ] [P2-T5] Author `## Per-Item Branch and Worktree Lifecycle` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.5: one `git fetch origin main` before each cohort launch; each item worktree is created by the `Agent(orchestrator, isolation: "worktree", run_in_background: true)` spawn branched from `origin/main`; PR base is `main`; no integration branch is created or fetched; no final PR; no fan-in path.
  - Acceptance: The section states `origin/main` branching and PR base `main` and contains the no-integration-branch statement.
- [ ] [P2-T6] Author `## Parallel-Mode Kickoff Parameter` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.6 as amended by Decision 1, containing: (1) the literal marker line `Parallel mode: true. parallel_slug: <slug>. parallel_checkpoint_path: artifacts/orchestration/parallel-orchestrator-state.json. cohort_index: <n>. PR base branch MUST be main; pass --base main to gh pr create.`; (2) the item's active feature folder path written literally as `docs/features/active/<basename>`; (3) the canonical issue number line; (4) the committed `plan-path` plus the resume-at-atomic-execution instruction; (5) the model-budget marker line `model_budget.fable_policy: <disabled|available|preferred>.`; (6) spawn parameters listed as non-prompt parameters (`isolation: "worktree"`, `run_in_background: true`, branch base `origin/main`, `model` per routing receipt); plus the negative obligations that the kickoff prompt never carries `Preparation mode: true` or `Epic mode: true` and does not instruct the child to merge its own PR, and the exclusion list (blast radius, `max_concurrency`, `mode` are parent-side concerns).
  - Acceptance: The section contains the literal strings `Parallel mode: true` and `PR base branch MUST be main`, states both never-carries markers, and contains no occurrence of `gh pr merge` anywhere within the section.
- [ ] [P2-T7] Author `## Model Selection` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.7: adapted from the epic skill with name substitution only — the `model_budget.fable_policy` kickoff marker, per-delegation resolution via `ModelRouting.psm1`, the rule that `route` is never a model input, and the delegation-channel sentence narrowed to `Agent(orchestrator)` only.
  - Acceptance: The section names `ModelRouting.psm1`, states the route-is-never-a-model-input rule, and names no `Agent(pr-author)` channel.
- [ ] [P2-T8] Author `## Per-Item Merge to Main (Merge-on-Green)` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.8 (Decision 1, parent-side): the child runs unmodified (`epic_mode` false/absent) and finishes at its own DONE with PR open and CI green (child PR Creation Gate condition 6); the parent durably confirms via `gh pr view --json state,mergedAt,headRefOid` (and `gh pr checks` as needed) and records `merge_status: ci_green`; the parent executes `gh pr merge --merge <PR>` (base `main`); on success records `merge_commit_sha`, `merged_at`, `merge_status: merged`, and regenerates `parallel-status.md`; on merge conflict follows `## Per-Item Merge-Conflict Handling`. State explicitly that `.claude/skills/orchestrate/SKILL.md` is not modified by this feature and that no `parallel_merge` object or additional PR Creation Gate condition exists in the child contract. Name `EPIC_MERGE_GATE_BLOCKED` (from `.claude/hooks/enforce-epic-merge-gate.ps1`) as the F7-dependency block condition that denies the parent merge until F7 lands.
  - Acceptance: The section states the parent executes `gh pr merge --merge` after durably confirming CI green, contains the orchestrate-SKILL-not-modified statement, and names `EPIC_MERGE_GATE_BLOCKED`.
- [ ] [P2-T9] Author `## Per-Item Merge-Conflict Handling` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.9: on a conflicted `gh pr merge --merge`, the parent writes a synthetic Blocking finding to the item's own `remediation-inputs.<timestamp>.md` in the item's active feature folder, instructing conflict resolution against `origin/main` (`git fetch origin main`; `git merge --no-commit origin/main`; capture `git diff --name-only --diff-filter=U` plus conflict markers); the parent re-delegates the child, which processes the finding through its unmodified R1-R5 loop with the shared `remediation_pass` cap of 3; each remediated pass ends at child DONE with CI green and the parent retries the merge; during remediation the item legitimately remains `pr_open` or `ci_green` (Decision 3); on loop exhaustion the parent records the terminal `merge_status: blocked_ci_loop_limit` and the blocked item holds the cohort barrier; include the one-sentence §13.1 boundary naming F8 for drift recording, quiesce, recompute, and requeue.
  - Acceptance: The section maps loop exhaustion to `blocked_ci_loop_limit`, states the cap of 3, and contains the F8 hand-off sentence.
- [ ] [P2-T10] Author `## Worktree Cleanup` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.10: after an item reaches `merge_status: merged` (durably confirmed), the parent runs `git worktree remove <path>` from the main checkout and records `merge_status: worktree_removed` plus `worktree_removed_at`; note that mechanical gating for parallel worktrees is F7 scope and name `EPIC_WORKTREE_REMOVAL_BLOCKED` (from `.claude/hooks/enforce-epic-worktree-removal-gate.ps1`) as the F7-dependency block condition that denies removal until F7 lands.
  - Acceptance: The section contains the removal sequence with both state recordings and names `EPIC_WORKTREE_REMOVAL_BLOCKED`.
- [ ] [P2-T11] Author `## Documentation Maintenance Boundaries` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.11: `docs/features/parallel/<slug>/parallel-status.md` is a generated projection of `artifacts/orchestration/parallel-orchestrator-state.json`, never hand-authored, never the source of the cohort table; required header fields `parallel_slug`, `mode`, `max_concurrency`, `current_cohort`, `recolor_generation`, `last_updated`; item table (one row per `items[]` entry with `issue_num`, `feature_folder`, cohort index, lifecycle `state`, `merge_status`, `pr_url`, `merge_commit_sha`, lifecycle timestamps; cohort column replaces the wave column); cohort table projecting `cohorts[] { index, generation, item_keys[] }`; read-only projections `## Conflict Edges`, `## Mutations`, `## Drift Events` (empty arrays render empty sections); regeneration boundaries: run kickoff, every item `state`/`merge_status` transition, every cohort transition, every `recolor_generation` increment, every append to `mutations[]` or `drift_events[]`, and run completion (`closed`) or close (`open`).
  - Acceptance: The section contains the generated-never-hand-authored statement, the never-the-source-of-the-cohort-table statement, all six header fields, and the full regeneration-boundary list.
- [ ] [P2-T12] Author `## Parallel-Level Checkpoint` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.12: enumerate the §12 fields F5 writes to `artifacts/orchestration/parallel-orchestrator-state.json` (consumption documentation only; F3 owns the schema); enumerate all eight `merge_status` values `not_started`, `worktree_created`, `pr_open`, `ci_green`, `merged`, `worktree_removed`, `blocked_drift`, `blocked_ci_loop_limit` with the F5-written transition chain; state that F5 never writes `blocked_drift`, `conflict_edges[]`, `mutations[]`, or `drift_events[]`; state that every field is re-derivable on resume from `git worktree list --porcelain`, `git branch`, and `gh pr view --json state,mergedAt,headRefOid` (the checkpoint is a cache of durable state); validation via the F3 validator with `artifact_type: "parallel-orchestrator-state"`.
  - Acceptance: The section lists all eight enum values and the four never-writes items.
- [ ] [P2-T13] Author `## Completion Requirements` in `.claude/skills/parallel-orchestrate/SKILL.md` per R2.13: `closed` (default) fires the completion gate when every non-withdrawn item is `merged` or `worktree_removed`, plus the final `parallel-status.md` regeneration and a passing `require_complete` checkpoint validation; `open` has no automatic completion and terminates only via `/parallel-close`, named as F6-owned and not specified here; no integration-PR condition exists.
  - Acceptance: The section defines both modes exactly as stated and contains no integration-PR condition.
- [ ] [P2-T14] Append to `.claude/skills/parallel-orchestrate/SKILL.md`, after `## Completion Requirements`, the three reserved placeholder sections as the final three top-level headings, exactly `## Mutation Protocol (F6)`, then `## Enforcement Hooks (F7)`, then `## Radius Drift Detection (F8)`, each with a single one-line body of the form "Reserved for F<N>; content is appended by that feature and must not be relocated."
  - Acceptance: The three headings are the last three `##` headings in the file, in the stated order, each appearing exactly once, each followed by exactly the one-line reserved body.
- [ ] [P2-T15] Verify `.claude/skills/parallel-orchestrate/SKILL.md` end-state with a grep/heading scan: (a) the first thirteen `##` headings, in document order, are exactly R2.1 items 3-15 (`## Prerequisites`, `## Parallel Manifest Consumption`, `## Cohort Consumption and Ordering`, `## Cohort Barrier and Max-Concurrency Slot Filling`, `## Per-Item Branch and Worktree Lifecycle`, `## Parallel-Mode Kickoff Parameter`, `## Model Selection`, `## Per-Item Merge to Main (Merge-on-Green)`, `## Per-Item Merge-Conflict Handling`, `## Worktree Cleanup`, `## Documentation Maintenance Boundaries`, `## Parallel-Level Checkpoint`, `## Completion Requirements`), and the file has exactly sixteen `##` headings in total; (b) the three reserved wave-4 headings exist, are uniquely named (each appears exactly once), and are the final three headings; (c) the file names both `EPIC_MERGE_GATE_BLOCKED` and `EPIC_WORKTREE_REMOVAL_BLOCKED`; (d) the file contains none of the literals `Epic mode: true`, `--base epic/`, `integration-to-main`; (e) the `## Parallel-Mode Kickoff Parameter` section contains no occurrence of `gh pr merge`.
  - Acceptance: All five checks pass; the check method and result are noted in the task completion output.

### Phase 3 — Entry Point and Status Template

- [ ] [P3-T1] Create `.claude/skills/parallel-run/SKILL.md` per spec R3: YAML frontmatter (`context: fork`, `agent: parallel-orchestrator`, argument hint `[parallel-slug]`); intro; `## Procedure` with (1) resolve `docs/features/parallel/<slug>/` from the slug or path argument, (2) kickoff-artifact discovery at the local path only (`docs/features/parallel/<slug>/parallel-kickoff.md`, or the F4-landed location confirmed in P0-T7) with a STOP path when absent whose text instructs running `/parallel-plan` first (or invoking `/parallel-orchestrate <manifest-path>` directly), (3) execute the kickoff artifact's `## Invocation Prompt` with items resuming at atomic execution from their committed `plan-path` rather than re-running promotion, research, or planning, (4) honor existing checkpoint state and resume instead of restart; and a `## Scope` statement that the skill adds only kickoff resolution, everything else is governed by `parallel-orchestrate/SKILL.md`, and no final integration PR exists.
  - Acceptance: The file exists; frontmatter declares `context: fork` and `agent: parallel-orchestrator`; the STOP path names `/parallel-plan`; the resume-at-plan-path statement is present; no `git cat-file`/`git show` integration-ref fallback appears.
- [ ] [P3-T2] Create `docs/features/templates/parallel/parallel-status.md` mirroring `docs/features/templates/epic/epic-status.md`: begin with an HTML-comment generated-file banner stating the file is generated and must not be hand-authored; include a title, a restated generated-only paragraph, the header bullet block (`parallel_slug`, `mode`, `max_concurrency`, `current_cohort`, `recolor_generation`, `last_updated`), the item table skeleton (columns per P2-T11 with a cohort column), the cohort table skeleton (`index`, `generation`, `item_keys[]`), and the `## Conflict Edges`, `## Mutations`, and `## Drift Events` section skeletons.
  - Acceptance: The file exists and its first bytes are an HTML comment (`<!--`) containing a generated-file, do-not-hand-author statement; all listed sections are present.
- [ ] [P3-T3] Verify the Phase 3 files end-state with a grep scan: `.claude/skills/parallel-run/SKILL.md` contains none of the literals `Epic mode: true`, `--base epic/`, `integration-to-main`; `docs/features/templates/parallel/parallel-status.md` carries the banner and the six header fields.
  - Acceptance: Both checks pass; the check method and result are noted in the task completion output.
- [ ] [P3-T4] Mirror the three delivered `.claude` runtime files byte-identically into the bundled payload: copy `.claude/agents/parallel-orchestrator.md` to `extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md`; `.claude/skills/parallel-orchestrate/SKILL.md` to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md`; `.claude/skills/parallel-run/SKILL.md` to `extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-run/SKILL.md`. Required by `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts`, which asserts every repo `.claude/**` file (excluding `.claude/settings.local.json` and `.claude/agent-memory/**`) exists in the bundle with identical content.
  - Acceptance: The three bundled files exist and each is byte-identical to its repo counterpart, verified by SHA-256 comparison of all three pairs.
- [ ] [P3-T5] Register the three new bundled paths in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` by adding `.claude/agents/parallel-orchestrator.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, and `.claude/skills/parallel-run/SKILL.md` to its `paths` array, following the existing ordering convention and the epic precedent (`.claude/agents/epic-orchestrator.md`, `.claude/skills/epic-orchestrate/SKILL.md`, `.claude/skills/epic-run/SKILL.md`). No other pack manifest is modified.
  - Acceptance: `core.json` parses as valid JSON; each of the three new entries appears exactly once; no pre-existing entry is removed or reordered incorrectly.
- [ ] [P3-T6] Run the two bundle-parity suites and record evidence: (a) `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_push_down_claude_pack_manifest_completeness.py -v` from the repository root; (b) `node run-jest.cjs test/lib/push-down/claude-pack-manifest-completeness.test.ts test/lib/push-down/claude-customizations.test.ts` from `extensions/drm-copilot`. Write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/regression-testing/bundle-parity.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:` (both commands), `EXIT_CODE:` (one per command), and an `Output Summary:` containing numeric passed/failed counts for each.
  - Acceptance: Both commands exit 0 with zero failures; the artifact records all four fields for both commands.

### Phase 4 — Contract Tests: `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`

- [ ] [P4-T1] Create `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` following the pattern of `tests/scripts/dev_tools/test_epic_run_kickoff_discovery_contract.py`, containing module docstring, typed repo-root/file-loading helpers, and existence-plus-frontmatter tests: `.claude/agents/parallel-orchestrator.md` frontmatter declares `name: parallel-orchestrator` (exact), a `model`, a `tools` list excluding `Agent(pr-author)`, a `skills` list containing `parallel-orchestrate`, and a `SubagentStop` hook line carrying `-CheckpointPath artifacts/orchestration/parallel-orchestrator-state.json` and `-ArtifactType parallel-orchestrator-state`; `.claude/skills/parallel-orchestrate/SKILL.md` and `.claude/skills/parallel-run/SKILL.md` frontmatter declare `context: fork` and `agent: parallel-orchestrator`; both skill frontmatters additionally declare an argument hint (`parallel-orchestrate`: accepting the parallel manifest path or slug; `parallel-run`: `[parallel-slug]`); `docs/features/templates/parallel/parallel-status.md` begins with the generated-file HTML-comment banner.
  - Acceptance: The file exists; the listed tests are implemented with full type hints and Arrange-Act-Assert structure; no runtime temp files or external processes are used.
- [ ] [P4-T2] Add tests to `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` asserting the agent-body heading set (the nine headings of P1-T11); that the first `#` heading of `.claude/skills/parallel-orchestrate/SKILL.md` is exactly `# Parallel Orchestrate Skill` and precedes `## Prerequisites` (R2.1 item 2); that the first thirteen `##` headings, in document order, are exactly R2.1 items 3-15; and that the final three top-level headings are exactly `## Mutation Protocol (F6)`, `## Enforcement Hooks (F7)`, `## Radius Drift Detection (F8)`, in that order, each appearing exactly once, each followed by a one-line reserved body.
  - Acceptance: The ordered-heading and reserved-section tests are implemented and deterministic.
- [ ] [P4-T3] Add tests to `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` asserting kickoff-contract content of the `## Parallel-Mode Kickoff Parameter` section: contains the literal `Parallel mode: true` and the literal `PR base branch MUST be main`; states the kickoff prompt never carries `Preparation mode: true` or `Epic mode: true`; contains no occurrence of `gh pr merge` within that section; and requires the `docs/features/active/<basename>` folder path and the canonical issue number in the kickoff prompt; and requires the section to state that the child resumes at atomic execution from its committed `plan-path` rather than re-running promotion, research, or planning.
  - Acceptance: The section-scoped assertions extract the section text by heading boundaries before matching (no whole-file matching for section-scoped criteria).
- [ ] [P4-T4] Add tests to `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` asserting behavioral skill text: the cohort-barrier statement (cohort `N+1` only after every cohort-`N` item is `merged` or `worktree_removed`), the `max_concurrency` token and the phrase `ascending item-key order`; the merge-on-green statements (parent executes `gh pr merge --merge` after durable CI-green confirmation; `.claude/skills/orchestrate/SKILL.md` not modified); the conflict-handling mapping (`blocked_ci_loop_limit`, cap of 3, F8 hand-off sentence); the documentation boundaries (generated/never hand-authored, never the source of the cohort table, all six header fields `parallel_slug`, `mode`, `max_concurrency`, `current_cohort`, `recolor_generation`, `last_updated`, an item table with a cohort column, a cohort table carrying `generation`, and a boundary list including `recolor_generation` increments and `mutations[]`/`drift_events[]` appends); the checkpoint enumeration (all eight `merge_status` values and the four F5-never-writes items); the mode-dependent completion text; the presence of `EPIC_MERGE_GATE_BLOCKED` and `EPIC_WORKTREE_REMOVAL_BLOCKED` in the skill text; and the `parallel-run` STOP path naming `/parallel-plan` plus its resume-at-plan-path statement.
  - Acceptance: Each spec AC in this group has at least one dedicated assertion with an actionable failure message.
- [ ] [P4-T5] Add negative-literal tests to `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`: none of the three delivered runtime files (`.claude/agents/parallel-orchestrator.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/skills/parallel-run/SKILL.md`) contains any of the prescriptive literals `Epic mode: true`, `--base epic/`, or `integration-to-main` (scoped to the delivered files only; the test module holds the literals as data).
  - Acceptance: The negative tests scan exactly the three runtime files and no others.
- [ ] [P4-T6] Add epic-surface immutability tests to `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`: SHA-256 content-hash pinning of `.claude/agents/epic-orchestrator.md` and `.claude/skills/epic-orchestrate/SKILL.md` against the baseline hashes recorded in P0-T6 (hashes embedded as constants; no git dependency in-test).
  - Acceptance: The pinned hashes equal the P0-T6 baseline values and the tests pass against the current files.
- [ ] [P4-T7] Verify `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` does not exceed 500 lines (`(Get-Content <path>).Count` or equivalent); if it exceeds the limit, split assertions into helper modules under `tests/scripts/dev_tools/` until every file is within the limit.
  - Acceptance: Every test file created by this feature is <= 500 lines; the measured count is noted in the task completion output.
- [ ] [P4-T8] Run `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -v` and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/regression-testing/contract-tests-pass.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` containing the numeric passed/failed counts.
  - Acceptance: `EXIT_CODE: 0` with zero failures; the artifact exists at the stated path with all four fields.

### Phase 5 — Immutability Verification and Acceptance-Criteria Reconciliation

- [ ] [P5-T1] Verify frozen-surface byte-identity by running `git diff <merge-base> -- .claude/agents/epic-orchestrator.md .claude/skills/epic-orchestrate/SKILL.md .claude/skills/orchestrate/SKILL.md`, where `<merge-base>` is the merge base resolved by `git merge-base HEAD epic/parallel-orchestration-integration` (empty output required) and re-hashing the three files against the P0-T6 baseline; write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/other/frozen-surface-verification.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: The diff is empty for all three paths and all three hashes match baseline; the artifact records both results.
- [ ] [P5-T2] Verify the feature-branch diff contains no changes under `.claude/hooks/` and no change to `.claude/settings.json` by running `git diff <merge-base> --name-only`, where `<merge-base>` is the merge base resolved by `git merge-base HEAD epic/parallel-orchestration-integration`, and inspecting the path list; write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/other/no-hook-or-settings-change.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the full changed-path list).
  - Acceptance: Interpreting each listed path as repo-root-relative, no path begins with `.claude/hooks/` and no path equals `.claude/settings.json`. Changed paths under `extensions/drm-copilot/resources/claude-customizations/` are expected (P3-T4, P3-T5) and do not violate this criterion; the artifact states this explicitly.
- [ ] [P5-T3] Mirror the plan-level `## F7 Coordination Note (carry forward to the F7 planner)` into `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/other/f7-coordination-note.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:` and the full note text covering both epic-hook interactions (`enforce-epic-merge-gate.ps1` / `EPIC_MERGE_GATE_BLOCKED` and `enforce-epic-worktree-removal-gate.ps1` / `EPIC_WORKTREE_REMOVAL_BLOCKED`).
  - Acceptance: The artifact exists and names both hooks and both block reasons.
- [ ] [P5-T4] Check off each satisfied acceptance criterion in `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/spec.md` (`## Acceptance Criteria`, 22 items) per the `acceptance-criteria-tracking` skill: verify each criterion individually against the delivered files and passing tests, change only `- [ ]` to `- [x]` for verified items, leave unmet items unchecked and document any gap.
  - Acceptance: Every checked item has a named verifying artifact or passing test; criterion text is unmodified; no phantom criteria added.
- [ ] [P5-T5] Check off each satisfied acceptance criterion in `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/user-story.md` (`## Acceptance Criteria`, 11 items) per the `acceptance-criteria-tracking` skill, tracked independently of `spec.md`, with the same one-at-a-time verification rules.
  - Acceptance: Every checked item has a named verifying artifact or passing test; criterion text is unmodified.
- [ ] [P5-T6] Write the AC status summary to `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/other/ac-status-summary.<yyyy-MM-ddTHH-mm>.md` in the `acceptance-criteria-tracking` format, covering both sources (`spec.md`: total 22, checked, remaining; `user-story.md`: total 11, checked, remaining; list of any unchecked criterion texts).
  - Acceptance: The artifact exists and reports both source files with numeric totals.
- [ ] [P5-T7] Re-verify bundle parity at end state: recompute SHA-256 for each repo/bundle pair from P3-T4 and confirm equality, and confirm the three `.claude`-relative paths remain present in `pack-manifests/core.json`. Write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/other/bundle-parity-verification.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` listing the three path pairs with their matching hashes and the manifest-entry confirmation.
  - Acceptance: All three hash pairs match and all three manifest entries are present.

### Phase 6 — Final QC Loop

Loop rule: run P6-T1 through P6-T4 in order. If any step fails or changes files, restart from P6-T1 and repeat until all four steps pass in a single clean pass. The recorded artifacts must reflect the final clean pass. If any step of the loop modifies `.claude/agents/parallel-orchestrator.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, or `.claude/skills/parallel-run/SKILL.md`, re-run P3-T4 to re-sync the bundled mirrors before restarting the loop at P6-T1; a stale mirror fails `test_push_down_claude_resource_contracts.py` at P6-T4. These command tasks are unconditional; `SKIPPED` is not a valid completion outcome for any of them.

- [ ] [P6-T1] Run `poetry run black .` from the repository root and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/final-qc-black.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (files reformatted count; a clean final pass reformats zero files).
  - Acceptance: The artifact records the final clean pass with `EXIT_CODE: 0` and zero files changed.
- [ ] [P6-T2] Run `poetry run ruff check .` from the repository root and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/final-qc-ruff.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: The artifact records `EXIT_CODE: 0` with zero lint errors on the final pass.
- [ ] [P6-T3] Run `poetry run pyright` from the repository root and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/final-qc-pyright.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error and warning counts).
  - Acceptance: The artifact records `EXIT_CODE: 0` with zero type errors on the final pass.
- [ ] [P6-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` from the repository root and write `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/final-qc-pytest-coverage.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` containing the numeric pass/fail counts and the numeric post-change line-coverage and branch-coverage percentages.
  - Acceptance: The artifact records `EXIT_CODE: 0` with all tests passing and numeric post-change coverage values (no placeholders).
- [ ] [P6-T5] Write the coverage delta and threshold verification to `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/coverage-delta.<yyyy-MM-ddTHH-mm>.md` with `Timestamp:` and a comparison table reporting: baseline line and branch coverage (from P0-T5), post-change line and branch coverage (from P6-T4), and new/changed-code coverage for this feature's diff (the diff's executable-code content is the test file, which is excluded from the coverage denominator per policy; record this explicitly and confirm no production Python file changed, so changed-line coverage regression is structurally zero). Verify post-change line coverage >= 85%, branch coverage >= 75%, and no regression versus baseline; if any required numeric value is unavailable or any threshold fails, record the outcome as remediation-required, not PASS.
  - Acceptance: The artifact contains all numeric values, the explicit new/changed-code statement, and an explicit PASS or remediation-required verdict.

## Test Plan

- Unit/contract: `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py` (Phases 4) asserts all structural spec criteria — existence and frontmatter of deliverables 1-4, ordered headings, reserved wave-4 sections, kickoff-marker literals with section scoping, behavioral text assertions, prescriptive-literal negatives, and content-hash pinning of the frozen epic files. Deterministic; no network, no temp files, <= 500 lines per file.
- Integration: none required; the deliverables are Markdown surface files (spec `## Testability Note`).
- Manual/CLI: git-diff frozen-surface and hooks/settings verification (P5-T1, P5-T2).
- Coverage evidence: baseline `evidence/baseline/baseline-pytest-coverage.<ts>.md` (P0-T5); post-change `evidence/qa-gates/final-qc-pytest-coverage.<ts>.md` (P6-T4); comparison `evidence/qa-gates/coverage-delta.<ts>.md` (P6-T5). All paths relative to `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/`.

## Open Questions / Notes

- None open. The three cross-feature questions raised by research are settled by spec Adjudicated Decisions 1-3 and are not re-opened by this plan. Upstream stand-in contracts (F3 enum/artifact-type, F4 kickoff location) are re-verified at execution time by P0-T7 without altering any decision.
