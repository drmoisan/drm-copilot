# Epic Status: claude-runtime-portability

Generated projection of `artifacts/orchestration/epic-orchestrator-state.json`. Do not hand-edit;
this file is regenerated by `epic-orchestrator` at each wave and `merge_status` transition. The
checkpoint JSON is the machine-authoritative source; `epic.md` is the human-authored manifest.

- Integration branch: `epic/claude-runtime-portability-integration`
- Current wave: 2 (blocked)
- Last updated: 2026-08-30T21:20:00Z
- Model budget: `fable_policy: disabled`

## Feature Status

| issue_num | feature_folder | wave | merge_status | pr_url | merge_commit_sha | worktree_created_at | pr_opened_at | merge_confirmed_at | worktree_removed_at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 596 | docs/features/active/2026-08-29-batch-budget-state-portability-596 | 0 | merged | https://github.com/drmoisan/drm-copilot/pull/604 | 4ec38c4c | 2026-08-30T00:24:10Z | 2026-08-30T07:08:00Z | 2026-08-30T07:11:44Z | - |
| 598 | docs/features/active/2026-08-29-blast-radius-powershell-calling-convention-598 | 0 | merged | https://github.com/drmoisan/drm-copilot/pull/603 | f134119c | 2026-08-30T00:24:10Z | 2026-08-30T06:52:39Z | 2026-08-30T06:57:24Z | - |
| 597 | docs/features/active/2026-08-29-caller-site-invocation-correctness-597 | 1 | merged | https://github.com/drmoisan/drm-copilot/pull/605 | 8b94217e | 2026-08-30T07:03:29Z | 2026-08-30T07:32:00Z | 2026-08-30T07:35:41Z | - |
| 599 | docs/features/active/2026-08-29-remove-remaining-python-invocations-599 | 2 | worktree_created | - | - | 2026-08-30T10:05:53Z | - | - | - |

Three of four features are merged into the integration branch. Waves 0 and 1 are complete.

## Wave Schedule

Computed by longest-path layering over the `depends_on` DAG per the `epic-orchestrate` skill.

| wave | features | gating dependency | state |
| --- | --- | --- | --- |
| 0 | 596, 598 | none (empty `depends_on`) | complete |
| 1 | 597 | 598 merged | complete |
| 2 | 599 | 597 merged | blocked at child S6 |

Waves 1 and 2 are serialized because 597 and 599 both rewrite
`.claude/skills/parallel-plan/SKILL.md`. This is a genuine file-contention edge.

## Wave 2 Block — feature 599

Feature 599 is **implementation-complete but unreviewed**, and therefore unmerged. No PR is open.

Delivered and pushed:

- Plan closed at **80/80 tasks** across Phases 0-6.
- Ten commits above the integration base `8b94217e`, head `352214e3`, worktree clean and in sync
  with `origin`.
- All four language QA gates green: bash 92.3% lines over 290 cases (per-file 0.989 and 0.949);
  Python `black`/`ruff`/`pyright` clean with 79 passed and the reference module at 100%;
  TypeScript prettier/lint/typecheck exit 0 with coverage identical to the Phase 0 baseline;
  PowerShell 27 passed, 0 failed.
- All 33 acceptance criteria checked off in `spec.md` (20) and `user-story.md` (13).

**Halt cause.** The child run stopped at `S6_feature_review`: `Agent(feature-review)` is not
launchable in the executing session, whose registry exposes only `orchestrator` and `pr-author`.
This is not a missing definition — `.claude/agents/feature-review.md` is present and the
orchestrator's `tools:` grant enumerates it, but that grant is a permission ceiling rather than a
registry.

S6 is a required delegated step that the route contract forbids performing locally, and the
PR-creation readiness gate independently refuses while `step6_status` is `blocked`. Substituting a
self-review or routing around the gap via a nested `Agent(orchestrator)` was rejected: the nested
session inherits the same registry and could re-plan, re-execute, or create a fresh worktree,
which is the outcome the adopt-in-place constraint exists to prevent.

Nothing is at risk. All work is committed and pushed.

**To resume:** re-enter at `S6_feature_review` from a session whose registry includes
`feature-review`. The PR context artifacts are current against `352214e3` and need refreshing only
if the branch moves.

### Open items for the reviewer

1. **PG-1**, referred from Phase 3 and still unruled: `parallel_lane_assertion.py` rejects an
   `--edges` value beginning with a hyphen (argparse consumes the token, exit 2) while the bash
   port accepts it and exits 0. This is an argument-parsing-boundary divergence that none of the
   five divergence classes declared in `spec.md` covers, because all five describe output produced
   after the diagnostic runs. The port is the more permissive side. The decision is whether
   `spec.md` should declare a sixth divergence class. No acceptance criterion depends on it.
2. The four `## Definition of Done` items in `spec.md` remain unchecked by design; they are the
   reviewer's and epic owner's call, not the executor's.
3. **Known Residual**, for the epic owner: manifest line 14 is not fully satisfied on delivery,
   because non-goals 1 and 2 (the drift-detection CLI and the mutation-abandon CLI) remain by
   deliberate decision — both are gate-coupled and cannot be replaced without co-designing their
   hooks. The corrective action is a manifest rewording, recorded in
   `evidence/other/known-residual.2026-08-30T20-45.md`. Manifest line 15 is fully satisfied.

## Integration PR

Not yet opened. Opened after every feature reaches `merged` or `worktree_removed`; feature 599 has
not.

| field | value |
| --- | --- |
| pr_number | - |
| ci_status | - |
| merge_commit_sha | - |
| merged_at | - |

## Execution Preconditions

Both are carried from the kickoff artifact and are not discoverable from the plans themselves.

1. Install node dependencies in every child worktree before any TypeScript task runs.
   `node_modules` is gitignored, so `git worktree add` never populates it, and `npx` silently
   resolves a missing local binary to an unrelated registry package rather than failing.
2. Move each child checkpoint out of preparation mode before its Phase 0 baseline runs, or
   `enforce-orchestration-preimplementation-gate.ps1` blocks `[P0-T1]`.

## Known Validator Defect

`_validate_wave_barrier_ordering` in `scripts/dev_tools/validate_epic_orchestrator_state.py`
runs unconditionally and applies no `not_started` filter, so every dependent feature emits
`EPIC_WAVE_BARRIER_VIOLATION` from epic kickoff until its dependency merges. One such error
remains expected for this epic (599) and clears once that feature reaches a merged status.
