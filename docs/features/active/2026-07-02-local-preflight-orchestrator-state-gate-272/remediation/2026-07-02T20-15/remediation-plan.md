# local-preflight-orchestrator-state-gate (Remediation Plan)

- **Issue:** #272
- **Cycle:** 1 (entry)
- **Timestamp:** 2026-07-02T20-15
- **Triggering findings:** `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/remediation/2026-07-02T20-15/remediation-inputs.md`
- **Work Mode:** `full-bug`. AC source: `spec.md` `## Acceptance Criteria`.
- **Status:** Draft

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the outcome of this remediation cycle must be reported BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Evidence Location Invariant:** All evidence for this remediation cycle is written under `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/<kind>/` (canonical sub-paths used in this plan: `remediation-baseline/`, `qa-gates/`, `other/`) per `evidence-and-timestamp-conventions`. No task in this plan writes to `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or any other non-canonical path.

## Do Not Do (carried forward from remediation/2026-07-02T20-15/remediation-inputs.md)

- Do not weaken or remove the existing five receipt checks, the Case A/B/C precedence, or the hook's `exit 0`/JSON-`permissionDecision` contract while remediating.
- Do not fix the coverage gap by adding `enforce-pr-author-skill.ps1` to an `ExcludedPath`/exclude list or by lowering the coverage threshold — the fix must make the existing allowlist entry actually take effect in the canonical artifact.
- Do not remove the `pr_author_preflight` documentation already present in `orchestrate/SKILL.md`/`orchestrator.md`/`pr-author.md`.
- Do not delete or rename the real, live `artifacts/orchestration/orchestrator-state.json` checkpoint at any point in this remediation cycle.
- Do not check AC #11 back to `[x]` in `spec.md` until the regenerated coverage artifact corroborates the claimed numbers; do not check AC #7.

---

### Phase 0 — Remediation Baseline Capture (Cycle 1)

- [x] [P0-T1] Read `CLAUDE.md` (repository tone, policy-compliance order, architecture).
- [x] [P0-T2] Read `.claude/rules/general-code-change.md` (cross-language code change policy).
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` (cross-language unit test policy, coverage thresholds).
- [x] [P0-T4] Read `.claude/rules/powershell.md` (PoshQC toolchain commands, coverage floor).
- [x] [P0-T5] Read `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/remediation/2026-07-02T20-15/remediation-inputs.md` in full (this cycle's authoritative findings and Do Not Do constraints).
- [x] [P0-T6] Read `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/audit/2026-07-02T20-15/policy-audit.md` Section 5 ("Test Coverage Detail") and Section 8 ("Gaps and Exceptions") for the coverage-artifact gap detail.
- [x] [P0-T7] Read `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/audit/2026-07-02T20-15/code-review.md` Findings Table rows 1-4.
- [x] [P0-T8] Write the Phase 0 policy-read evidence artifact to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read in P0-T1 through P0-T7.
- [x] [P0-T9] Record the current branch/commit state (`git rev-parse HEAD`, `git branch --show-current`, `git status --porcelain`) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/branch-commit-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T10] Re-parse the current `artifacts/pester/powershell-coverage.xml` and confirm it still shows 9 `<class>` entries with `covered="0"` and no entry for `.claude/hooks/enforce-pr-author-skill.ps1`, confirming the Blocking finding's starting state prior to this cycle's remediation; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/stale-coverage-artifact-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T11] Edit `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/spec.md`'s Acceptance Criteria bullet #11 ("Full PowerShell toolchain pass completed...") checkbox from `[x]` to `[ ]`, adding an inline note that it was unchecked pending corroborated coverage evidence per this remediation cycle's Blocking finding #1. Do not modify any other Acceptance Criteria checkbox (AC #7 remains unchecked and untouched).

### Phase 1 — Blocking: Diagnose and Regenerate Canonical PowerShell Coverage Artifact

- [x] [P1-T1] Search candidate non-repo directories for installed copies of `pester.runsettings.psd1` using `Get-ChildItem -Path @("$env:USERPROFILE\.vscode-server-insiders", "$env:USERPROFILE\.vscode-insiders", "$env:APPDATA\Code - Insiders") -Recurse -Filter 'pester.runsettings.psd1' -ErrorAction SilentlyContinue`; record every discovered non-repo path (or an explicit "none found in these candidate directories" result plus the exact directories searched) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/mcp-settings-discovery.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T2] For each non-repo `pester.runsettings.psd1` path discovered in P1-T1 (or, if none were found in the initial candidate directories, expand the search to `$env:LOCALAPPDATA` and `$env:USERPROFILE` recursively and repeat the search before concluding none exists), read its `CodeCoverage.Path` list and confirm whether it omits `.claude/hooks/enforce-pr-author-skill.ps1`, corroborating (or refuting) the root cause stated in `remediation/2026-07-02T20-15/remediation-inputs.md` item 1; record the finding to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/mcp-settings-root-cause-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T3] Read `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` directly and confirm its `CodeCoverage.Path` list already includes `.claude/hooks/enforce-pr-author-skill.ps1` (the repo-tracked copy is expected to already be correct, per this feature's own prior edit); record confirmation to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/repo-tracked-settings-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T4] From the repository root, run `Import-Module ./scripts/powershell/PoshQC -Force; (Get-Module PoshQC).Path` and confirm the resolved module path is under the repository tree (`scripts/powershell/PoshQC/...`), establishing that `$script:PesterSettings` (and therefore the settings file `Invoke-PoshQCTest` will consume) resolves to the repo-tracked `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, not the non-repo-tracked copy found in (or searched for in) P1-T1/P1-T2; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/poshqc-module-resolution-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T5] Record the selected remediation approach: regenerate the canonical coverage artifact by importing the repo-tracked PoshQC module directly (`Import-Module ./scripts/powershell/PoshQC -Force`) and invoking `Invoke-PoshQCTest -Root '.' -ScanFolders @('tests/scripts/claude-hooks')` — bypassing the `mcp__drm-copilot__run_poshqc_test` MCP wrapper's stale bundled settings entirely, per remediation-inputs item 1(b)'s third option — rather than attempting to locate and edit the external extension's installed settings copy (non-reproducible and outside repository version control); record the decision and rationale to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/remediation-baseline/coverage-fix-approach-decision.md`.
- [x] [P1-T6] From the repository root, run `Import-Module ./scripts/powershell/PoshQC -Force; Invoke-PoshQCTest -Root '.' -ScanFolders @('tests/scripts/claude-hooks')`, writing the regenerated `pester-junit.xml` and `powershell-coverage.xml`/`powershell-coverage.koverage.xml` to their canonical `artifacts/pester/` paths per the repo-tracked settings file's `TestResult.OutputPath`/`CodeCoverage.OutputPath`; record the run to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/coverage-artifact-regeneration.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (must include total tests passed/failed and the command-level coverage percentage reported for `.claude/hooks/enforce-pr-author-skill.ps1`).
- [x] [P1-T7] Parse the regenerated `artifacts/pester/powershell-coverage.xml` and confirm it contains a `<class name=".../.claude/hooks/enforce-pr-author-skill.ps1">` entry with `LINE` counter values implying line coverage >= 85%; record the parsed class name, covered count, missed count, and computed percentage to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/coverage-artifact-class-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T8] Compare the P1-T7 regenerated coverage percentage for `.claude/hooks/enforce-pr-author-skill.ps1` against the pre-feature baseline (90.99%, 101/111 commands, per `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/baseline/poshqc-test-baseline.md`) and confirm no regression on the file's changed lines (the `Invoke-OrchestratorStatePreflight` function and its call site in `Get-PrAuthorBypassReason`); record baseline %, regenerated %, and changed-lines % to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/coverage-regeneration-delta.md`.
- [x] [P1-T9] Confirm the regenerated `artifacts/pester/pester-junit.xml` reports zero test failures and a test count of at least 53 (the two `enforce-pr-author-skill.*.Tests.ps1` files' combined prior count), confirming the regeneration run introduced no new failures; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/coverage-regeneration-test-pass-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P1-T10] Having confirmed P1-T7, P1-T8, and P1-T9 all corroborate the claimed coverage numbers, edit `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/spec.md`'s Acceptance Criteria bullet #11 checkbox from `[ ]` (set in P0-T11) back to `[x]`, with an inline note referencing the `evidence/qa-gates/coverage-artifact-class-verification.md` and `evidence/qa-gates/coverage-regeneration-delta.md` artifacts as the corroborating evidence. Do not check AC #7.

### Phase 2 — Major: Correct Stale Documentation References

- [x] [P2-T1] Edit `README.md` to remove the `validate-orchestrator-state.yml` bullet from the "## CI and release workflows" list (line 390).
- [x] [P2-T2] Run `grep -n "validate-orchestrator-state" README.md` and confirm zero matches remain; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/readme-stale-reference-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P2-T3] Read `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/orchestrate/SKILL.md`'s `## PR Creation Gate` section in full and determine whether its PR-creation-gate model is legitimately distinct from (CI-only for) the `.agents`/Codex ecosystem, or whether it should describe the same local-hook enforcement mechanism now documented in `.claude/skills/orchestrate/SKILL.md`.
- [x] [P2-T4] Edit `extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/orchestrate/SKILL.md`'s `## PR Creation Gate` section (line 144) to replace the stale "The repository CI gate `Orchestrator State Gate` runs the same validator..." claim with an accurate description consistent with the P2-T3 finding — either describing the actual current enforcement mechanism for this ecosystem, or explicitly noting that no CI gate performs this validation — without deleting any part of the section's guidance that is itself still accurate.
- [x] [P2-T5] Run `grep -n "Orchestrator State Gate" extensions/drm-copilot/resources/codex-and-agents-customizations/.agents/skills/orchestrate/SKILL.md` and confirm either zero matches remain or the remaining text no longer claims CI enforcement; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/agents-skill-stale-reference-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

### Phase 3 — Minor (Optional): Harden End-to-End Pester Test Against Mutable Checkpoint State

- [x] [P3-T1] Delegate to `powershell-typed-engineer` (direct mode, single test file) to modify the `'script entrypoint (end-to-end)'` context in `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (lines 93-127) so the test points `Invoke-OrchestratorStatePreflight`'s `-CheckpointPath` parameter at a deliberately-nonexistent, non-temp-file sibling path (e.g. `artifacts/orchestration/orchestrator-state.nonexistent-fixture.json`, following the file's existing "real seam, stand-in existing file"/deliberately-absent-path pattern) instead of the real, mutable `artifacts/orchestration/orchestrator-state.json` checkpoint, without changing the test's asserted `permissionDecision`/`permissionDecisionReason` expectations. (No `powershell-typed-engineer` subagent-delegation tool was available in this execution session's toolset; the single-file, direct-mode-scoped edit was performed directly by the executor as a mechanically-necessary micro-action within this task's stated scope, consistent with the task's "direct mode, single test file" constraint.)
- [x] [P3-T2] Verify `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` remains under 500 lines after P3-T1 (`(Get-Content tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1).Count -lt 500`); if the cap would be exceeded, extract a shared fixture constant before finalizing.
- [x] [P3-T3] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and confirm the modified end-to-end test still passes deterministically (same assertions: `$LASTEXITCODE -eq 0`, `permissionDecision -eq 'deny'`, reason contains `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/test-hardening-verification.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

### Phase 4 — Final QA Loop and Remediation Cycle Closeout

- [x] [P4-T1] Run `mcp__drm-copilot__run_poshqc_format` against `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and record a zero-diff pass to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-remediation-poshqc-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T2] Run `mcp__drm-copilot__run_poshqc_analyze` against `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and record a zero-error pass to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-remediation-poshqc-analyze.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T3] From the repository root, run `Import-Module ./scripts/powershell/PoshQC -Force; Invoke-PoshQCTest -Root '.' -ScanFolders @('tests/scripts/claude-hooks')` a final time and confirm (a) all tests pass with zero failures and (b) the canonical `artifacts/pester/powershell-coverage.xml` still contains the `.claude/hooks/enforce-pr-author-skill.ps1` class entry at >= 85% line coverage with no regression versus the P1-T8 delta; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-remediation-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric coverage percentage.
- [x] [P4-T4] Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` and confirm `test_bundled_claude_payload_contains_all_repo_runtime_contracts` still passes, confirming this remediation cycle did not disturb the `.claude`/bundled-mirror byte-identity invariant (no hook files are touched by this remediation plan); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-remediation-pytest-mirror-parity.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T5] Run `git diff --stat -- .claude/hooks/enforce-pr-author-skill.ps1 extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-pr-author-skill.ps1 extensions/drm-copilot/resources/codex-and-agents-customizations/.codex/hooks/enforce-pr-author-skill.ps1` and confirm zero output (no changes to any of the three hook copies), preserving the hook's `exit 0`/JSON-`permissionDecision` contract and the existing Case A/B/C/receipt-check precedence unmodified by this remediation cycle; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/hook-invariants-unchanged-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T6] Confirm `artifacts/orchestration/orchestrator-state.json` was neither deleted nor renamed during this remediation cycle (`Test-Path artifacts/orchestration/orchestrator-state.json` returns `True`); record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/live-checkpoint-preserved-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T7] Run `grep -rln "pr_author_preflight" .claude/skills/orchestrate/SKILL.md .claude/agents/orchestrator.md .claude/agents/pr-author.md` and confirm all three files still match, confirming the documentation added by the original feature remains unmodified by this remediation cycle; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/pr-author-preflight-docs-unchanged-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T8] Read `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/spec.md`'s Acceptance Criteria section directly and confirm AC #11 is checked `[x]` with the P1-T10 corroboration note, AC #7 remains unchecked `[ ]`, and no other checkbox state was altered by this remediation cycle; record to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/qa-gates/final-remediation-ac-checklist-confirmation.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P4-T9] Record the remediation cycle 1 closeout summary (disposition of the Blocking finding, the Major finding, and the Minor finding, plus links to every evidence artifact produced in Phases 0-4) to `docs/features/active/2026-07-02-local-preflight-orchestrator-state-gate-272/evidence/other/remediation-cycle-1-summary.md`.
