# Policy Compliance Audit: parallel-orchestrator-surface (Issue #441)

**Audit Date:** 2026-08-08
**Auditor:** `feature-review` agent
**Feature Folder:** `docs/features/active/2026-08-07-parallel-orchestrator-surface-441`
**Base Branch:** `epic/parallel-orchestration-integration`
**Merge Base:** `ee0626e838109fe8d3fe3904fb4631c71879baa3`
**Head:** `feature/parallel-orchestrator-surface-441` @ `41633ad5e867070853e3e4501c3457b6641d1efc`
**Work Mode:** `full-feature` (marker `- Work Mode: full-feature` at `issue.md:10`)
**Code Under Test:** 33 changed paths across the full branch diff. Runtime deliverables:
`.claude/agents/parallel-orchestrator.md`, `.claude/skills/parallel-orchestrate/SKILL.md`,
`.claude/skills/parallel-run/SKILL.md`, `docs/features/templates/parallel/parallel-status.md`;
their bundled mirrors under
`extensions/drm-copilot/resources/claude-customizations/.claude/**`;
`extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`; three Python
test-tree files under `tests/scripts/dev_tools/`; feature documents and evidence artifacts.

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|---|---|---|---|---|---|---|
| Python | 3 files (all in `tests/`) | 3004 tests (36 added) | ✅ 3004 pass, 0 fail | 91.82% lines, 83.80% branches | 91.82% lines, 83.80% branches | N/A — zero changed production Python files |
| Markdown | 26 files | N/A (no coverage instrument) | ✅ 36 contract tests assert the delivered Markdown structure | N/A (no coverage instrument) | N/A (no coverage instrument) | N/A (no coverage instrument) |
| JSON | 1 file (`pack-manifests/core.json`) | N/A (config file) | ✅ `format_json --check` and `validate_json` clean | N/A (config file) | N/A (config file) | N/A (config file) |
| TypeScript | 0 files | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) |
| PowerShell | 0 files | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) |
| C# | 0 files | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) | N/A (zero changed files) |

**Explicit per-language coverage verdicts** (required to be `PASS` or `FAIL` for every language with
changed files on the branch; `N/A` is used only for languages with zero changed files):

- Python — **PASS** (1 language with changed source files; thresholds met, no regression)
- Markdown — **PASS** (no coverage instrument exists for Markdown; structural verification is the
  36-test contract suite, which passes)
- JSON — **PASS** (no coverage instrument exists for a config file; `format_json --check` and
  `validate_json` both exit 0)
- TypeScript — **N/A** (zero changed files on the branch)
- PowerShell — **N/A** (zero changed files on the branch)
- C# — **N/A** (zero changed files on the branch)

Zero-changed-file determination command:
`git diff --name-only ee0626e8..41633ad5 -- '*.ps1' '*.psm1' '*.psd1' '*.ts' '*.cs' '*.yml' '*.yaml'`
→ empty output.

### Coverage Evidence Checklist

- Python baseline coverage artifact: `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/baseline/baseline-pytest-coverage.2026-08-08T16-47.md`
- Python post-change coverage artifact: `artifacts/python/lcov.info` (present; regenerated by this audit's own `pytest --cov` run)
- Python post-change QA artifact: `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/final-qc-pytest-coverage.2026-08-08T17-57.md`
- TypeScript baseline coverage artifact: `N/A - zero changed TypeScript files on the branch`
- TypeScript post-change coverage artifact: `N/A - zero changed TypeScript files on the branch`
- PowerShell baseline coverage artifact: `N/A - zero changed PowerShell files on the branch`
- PowerShell post-change coverage artifact: `N/A - zero changed PowerShell files on the branch`
- C# baseline coverage artifact: `N/A - zero changed C# files on the branch`
- C# post-change coverage artifact: `N/A - zero changed C# files on the branch`
- Per-language comparison summary: `docs/features/active/2026-08-07-parallel-orchestrator-surface-441/evidence/qa-gates/coverage-delta.2026-08-08T17-58.md`, plus section `### 1.2.1 Per-Language Coverage Comparison` of this audit (independently re-measured)

**Non-negotiable verdict rule check:** numeric baseline and post-change coverage metrics are
present for the one language with changed files (Python). Changed/new-code coverage is reported as
structurally not applicable, with the reason stated, not omitted.

---

## Rejected Scope Narrowing

No scope narrowing was attempted by the caller. The delegating prompt stated
`Determine scope yourself per your scope invariant.` and the audit was performed against the full
branch diff `ee0626e838109fe8d3fe3904fb4631c71879baa3..41633ad5e867070853e3e4501c3457b6641d1efc`.

For transparency, the caller supplied one environment note that borders on coverage-relevant
assertion. It is recorded verbatim, with the disposition that it was treated as evidence to verify
rather than as an instruction to skip a check:

> Environment note, offered as fact rather than as a scope instruction: the Pester suites
> enforce-pr-author-skill.Tests.ps1 and codex-pretooluse-integration.Tests.ps1 read the real
> gitignored artifacts/orchestration/orchestrator-state.json rather than a mocked seam, so they
> fail whenever an orchestrated run is in progress and they fail identically at the merge-base.
> Evaluate them as you judge appropriate; if you reach a finding about them, state it.

Disposition: the note is not a narrowing, because it does not mark any language out of scope and
does not instruct a check to be skipped. It was independently verified rather than accepted. Both
suites were executed (Section 7). One test fails; the mechanism matches the note; the branch diff
changes zero PowerShell files, so the failure cannot be attributed to this branch. The claim about
`codex-pretooluse-integration.Tests.ps1` did not reproduce: that suite reported zero failures in
this environment. See Section 8, Gap G1.

---

## Evidence Location Compliance

Scan command: `poetry run python -m scripts.dev_tools.validate_evidence_locations --root .`
Result: `EVIDENCE_EXIT=0`, no output. **PASS.**

Branch-diff scan for forbidden evidence roots (`artifacts/baselines/`, `artifacts/baseline/`,
`artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`,
`artifacts/regression-testing/`, `artifacts/post-change/`) over
`git diff --name-status ee0626e8..41633ad5`: **zero matches.**

Every evidence artifact this branch adds is under the canonical
`<FEATURE>/evidence/<kind>/` scheme:

| Canonical sub-path | Artifacts added |
|---|---|
| `evidence/baseline/` | 7 |
| `evidence/other/` | 5 |
| `evidence/qa-gates/` | 5 |
| `evidence/regression-testing/` | 2 |

`EVIDENCE_LOCATION_OVERRIDE_REJECTED`: none. No caller instruction supplied a non-canonical
evidence path.

---

## Executive Summary

This branch delivers the execution half of the `parallel` orchestration surface (epic feature F5)
as four Markdown runtime/template files, their three bundled mirrors, one `pack-manifests/core.json`
registration, and one Python contract-test module split across three test-tree files. No production
Python, TypeScript, PowerShell, or C# source changed. No GitHub Actions workflow, benchmark script,
or composite action changed, so the `modified-workflow-needs-green-run` policy rule does not fire.

The full Python toolchain passes cleanly in a single pass, independently re-run by this audit:
Black clean over 372 files, Ruff clean, Pyright 0 errors / 0 warnings, Pytest 3004 passed / 0
failed. Repo-wide Python line coverage is 91.82% (>= 85% required) and branch coverage 83.80%
(>= 75% required), with no regression versus the recorded baseline. The 36 new contract tests all
pass and assert every structural acceptance criterion, including content-hash pinning that proves
the frozen epic surface is byte-identical.

Two policy-relevant gaps are recorded, neither of which is a coverage or toolchain failure. The
material one (G2) is an internal contradiction within this feature's own deliverables: the
`## Per-Item Merge-Conflict Handling` procedure assigns the parent the write of an item's
`remediation-inputs.<timestamp>.md` under `docs/features/active/`, which the delivered persona's
`tools` allowlist does not permit; the same allowlist also has no permitted mechanism for the
manifest-validation library call the skill prescribes. G1 records a pre-existing, out-of-diff Pester
test-isolation defect that this branch neither introduced nor can affect.

**Policy documents evaluated:**
- ✅ `.claude/rules/general-code-change.md`
- ✅ `.claude/rules/general-unit-test.md`
- ✅ `.claude/rules/quality-tiers.md`
- ✅ `.claude/rules/tonality.md`
- ✅ `.claude/rules/parallel-orchestration.md` (schema authority the delivered surface consumes)
- ✅ `.claude/rules/orchestrator-state.md`
- ✅ `CLAUDE.md`

**Language-specific policies evaluated:**
- ✅ `.claude/rules/python.md` + `.claude/rules/python-suppressions.md` (3 changed test files)
- ✅ `.claude/rules/self-explanatory-code-commenting.md`
- N/A `.claude/rules/powershell.md` — zero changed PowerShell files
- N/A `.claude/rules/typescript.md` — zero changed TypeScript files
- N/A `.claude/rules/csharp.md` — zero changed C# files
- N/A `.github/instructions/github-actions.instructions.md` — zero changed workflow files

**Temporary artifacts cleanup:**
- ✅ No temporary or one-time script is present in the branch diff. All three added Python files are
  permanent test-tree modules.
- Two gitignored tool byproducts are produced by the toolchain (`.coverage`,
  `artifacts/python/lcov.info`) and are not committed. This audit additionally produced
  `artifacts/cov-review.json` as a read-only extraction of the existing `.coverage` data file; it is
  a review byproduct under the allowed `artifacts/` root and is not evidence output.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|---|---|---|
| **Independence** — Tests run in any order | ✅ PASS | All 36 tests are module-level functions with no shared mutable state, no fixtures with wider-than-function scope, and no ordering dependency. Each reads committed files fresh through `read_repo_text` (`parallel_orchestrator_surface_test_support.py:73-89`). Verified by full-suite run: `poetry run pytest --cov --cov-branch --cov-report=term-missing` → 3004 passed. |
| **Isolation** — Each test targets single behavior | ✅ PASS | One acceptance obligation per test. Section-scoped criteria are parametrized as 11 named cases via `SECTION_OBLIGATION_CASES` (`parallel_orchestrator_surface_expectations.py:202-246`), so each criterion reports as its own test id rather than being bundled. |
| **Fast execution** — Tests complete quickly | ✅ PASS | `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q` → `36 passed in 0.07s` (≈2 ms/test). Full suite 10.61s for 3004 tests. |
| **Determinism** — Consistent results | ✅ PASS | No clock read, no RNG, no `sleep`, no network, no subprocess, no temporary file. Only committed-file reads. Module docstring states the invariant explicitly (`test_parallel_orchestrator_surface_contracts.py:26-27`). Confirmed by two independent runs producing identical results. |
| **Readability & Maintainability** — Clear structure | ✅ PASS | Descriptive `test_*` names, one-line imperative docstring per test, explicit `# Arrange / Act` and `# Assert` markers, and per-fragment assertion messages naming the missing obligation (`assert_fragments`, support:312-335). |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|---|---|---|
| **Baseline Coverage Documented** | ✅ PASS | **Baseline:** 91.82% statements (12432/13539), 83.80% branches (4190/5000).<br>**Command:** `poetry run pytest --cov --cov-branch --cov-report=term-missing`<br>**Artifact:** `evidence/baseline/baseline-pytest-coverage.2026-08-08T16-47.md`<br>**Timestamp:** 2026-08-08 16:47 |
| **No Coverage Regression** | ✅ PASS | **Post-change (independently re-measured by this audit):** 91.82362065145136% statements, 83.80% branches, 13539 statements, 5000 branch destinations.<br>**Change:** 0.00 pp lines, 0.00 pp branches.<br>**Command:** `poetry run coverage json -o artifacts/cov-review.json --quiet`<br>**Status:** No regression on any measured metric. |
| **New Code Coverage** | ✅ PASS (not applicable by construction) | **New/modified Python files:** `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`, `parallel_orchestrator_surface_test_support.py`, `parallel_orchestrator_surface_expectations.py` — all three in the test tree.<br>All three are outside `[tool.coverage.run] source = ["src", "scripts/dev_tools"]` and are additionally matched by the `omit` patterns `tests/*` / `*/tests/*`, which `general-unit-test.md` **requires** ("Configure coverage tooling to exclude test files (e.g., `tests/`)").<br>**Zero production Python files changed**, so the set of changed executable lines inside the coverage denominator is empty and changed-line regression is structurally zero. Confirmed arithmetically: numerator (12432), denominator (13539), and missing count (1107) are identical on both sides. |
| **Comprehensive Coverage** | ✅ PASS | All 22 `spec.md` and 11 `user-story.md` acceptance criteria are covered by at least one of the 36 tests or by a direct git/diff verification recorded in `feature-audit.2026-08-08T18-12.md`. Full mapping in Appendix A and in the feature audit's evaluation table. |
| **Positive Flows** — Valid inputs | ✅ PASS | 30 of 36 tests assert required presence: frontmatter identity fields, ordered heading layouts, section-scoped obligation fragments, template banner, the three producer/consumer seam bindings, and the two F7 block-reason names. |
| **Negative Flows** — Invalid/absent inputs | ✅ PASS | 4 negative-assertion tests: `test_agent_tools_allowlist_excludes_pr_author_channel`; `test_kickoff_section_carries_no_child_merge_instruction`; `test_delivered_runtime_files_carry_no_prescriptive_epic_literal` (3 parametrized cases × 3 literals). The parsers additionally fail closed: `split_frontmatter` raises on a missing or unterminated fence (support:151-159) and `extract_section` raises on an absent heading (support:297-301), so a section-scoped assertion can never degrade into a match against an empty string. |
| **Edge Cases** — Boundary conditions | ✅ PASS | Heading-boundary extraction is tested implicitly by every section-scoped case; `top_level_headings` matches only `"## "`-prefixed lines so a `###` heading or a heading name quoted inside prose is not mistaken for a section (support:256-275). Uniqueness of the three reserved headings is asserted separately from their position (`test_orchestrate_skill_reserved_wave_four_sections_close_the_file`). Exact heading-count boundary is asserted (`== 16`). |
| **Error Handling** — Error paths | ⚠️ PARTIAL | The three fail-closed `raise ValueError` paths in `split_frontmatter` (two branches) and `extract_section` (one branch) are defensive guards with no dedicated negative test. Because these modules sit in the test tree they are outside the coverage denominator, so the gap is not visible in any coverage metric. Impact is low: a regression in a guard would surface as a confusing failure message rather than as a false pass, since the guards raise rather than return empty. Recorded as Gap G3. |
| **Concurrency** | N/A | The units under test are pure file readers and text parsers. No concurrency surface. |
| **State Transitions** | N/A | No stateful component is introduced. The `merge_status` transition chain the surface documents is procedure text asserted as text, not executable state machinery. |

### 1.2.1 Per-Language Coverage Comparison

- Python: Baseline: 91.82% statements and 83.80% branch destinations. Post-change: 91.82% statements and 83.80% branch destinations. Change: 0.00 pp statements, 0.00 pp branches. New/changed-code coverage: N/A - zero changed production Python files, so the changed-executable-line set inside the denominator is empty and the denominator is unchanged at 13539 statements / 5000 branch destinations. Disposition: PASS. Evidence: `artifacts/python/lcov.info`; `evidence/baseline/baseline-pytest-coverage.2026-08-08T16-47.md`; `evidence/qa-gates/final-qc-pytest-coverage.2026-08-08T17-57.md`; `evidence/qa-gates/coverage-delta.2026-08-08T17-58.md`; this audit's independent `coverage json` extraction.
- Markdown: Baseline: N/A - no coverage instrument exists for Markdown. Post-change: N/A - no coverage instrument exists for Markdown. Change: N/A. New/changed-code coverage: N/A. Disposition: PASS. Evidence: 36 structural contract tests in `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`, all passing.
- JSON: Baseline: N/A - config file, no coverage instrument. Post-change: N/A - config file, no coverage instrument. Change: N/A. New/changed-code coverage: N/A. Disposition: PASS. Evidence: `format_json --check` exit 0; `validate_json` exit 0; pack-manifest-completeness and resource-contract suites passing.
- TypeScript: Baseline: N/A - zero changed files on the branch. Post-change: N/A - zero changed files on the branch. Change: N/A. New/changed-code coverage: N/A. Disposition: N/A. Evidence: `git diff --name-only ee0626e8..41633ad5 -- '*.ts'` returns empty output.
- PowerShell: Baseline: N/A - zero changed files on the branch. Post-change: N/A - zero changed files on the branch. Change: N/A. New/changed-code coverage: N/A. Disposition: N/A. Evidence: `git diff --name-only ee0626e8..41633ad5 -- '*.ps1' '*.psm1' '*.psd1'` returns empty output.
- C#: Baseline: N/A - zero changed files on the branch. Post-change: N/A - zero changed files on the branch. Change: N/A. New/changed-code coverage: N/A. Disposition: N/A. Evidence: `git diff --name-only ee0626e8..41633ad5 -- '*.cs'` returns empty output.

**Reproducibility note on the recorded branch delta.** `evidence/qa-gates/coverage-delta.2026-08-08T17-58.md` records post-change branch coverage as 83.82% (`covered_branches` 4191, `num_partial_branches` 555), a +1 destination improvement over baseline, and attributes it by inference to the newly added `.claude` files and `core.json` entries changing inputs traversed by existing bundle-parity helpers. This audit's independent re-run at the same HEAD produced 83.80% (`covered_branches` 4190, `num_partial_branches` 556) — the baseline value. The +1 destination is therefore environment-dependent, not a reproducible property of the branch content. This does not change any verdict: both values clear the 75% floor by a wide margin and neither is a regression. It does mean the artifact's causal attribution for that single destination is unverified. Recorded as Gap G4.

### 1.3 Test Structure and Diagnostics

| Requirement | Status | Evidence |
|---|---|---|
| **Clear Failure Messages** | ✅ PASS | `assert_fragments` asserts each obligation separately and names the missing fragment, deliberately avoiding an opaque whole-section mismatch (support:332-335). The seam tests print the parsed producer prescription plus the observed consumer state (`test_seam_status_template_realises_cohort_columns_prescribed_by_skill:344-349). The hash test prints both expected and actual digests. |
| **Arrange-Act-Assert Pattern** | ✅ PASS | Every test carries explicit `# Arrange`, `# Arrange / Act`, and `# Assert` comment markers delimiting the three phases. |
| **Document Intent** | ✅ PASS | Module docstring states Purpose, Flow, and Invariants/constraints. Each test has a one-line imperative docstring; the parametrized obligation test carries an extended docstring explaining that section extraction precedes matching so a drifted fragment fails the case that owns it. |

### 1.4 External Dependencies and Environment

| Requirement | Status | Evidence |
|---|---|---|
| **Avoid External Dependencies** | ✅ PASS | No database, network, remote API, or external process. Filesystem access is limited to reading files committed to the checkout, declared in the support-module docstring's Side effects section (support:22-25). |
| **Use Mocks/Stubs** | ✅ PASS (none required) | Nothing is mocked. The units under test are the committed Markdown artifacts themselves, so mocking would defeat the contract. This is the correct choice for a text-contract suite. |
| **Environment Stability** | ✅ PASS | No mutable global state, no environment variable read, and **no temporary file creation** (explicitly asserted in both module docstrings). `REPO_ROOT` is derived from `Path(__file__).resolve().parents[3]` (support:38) rather than from the process working directory, so results do not depend on invocation directory. |

### 1.5 Policy Audit Requirement

| Requirement | Status | Evidence |
|---|---|---|
| **Pre-submission Review** | ✅ PASS | This artifact plus `code-review.2026-08-08T18-12.md` and `feature-audit.2026-08-08T18-12.md` constitute the required pre-submission review. Outstanding items are enumerated in Section 8 and routed through `remediation-inputs.2026-08-08T18-12.md`. |

---

## 2. General Code Change Policy Compliance

### 2.1 Before Making Changes

| Requirement | Status | Evidence |
|---|---|---|
| **Clarify the objective** | ✅ PASS | Objective is documented in `issue.md` (issue #441), `spec.md` (38 KB, 22 acceptance criteria, R1–R4 requirements, 6 adjudicated decisions), and `user-story.md` (4 story statements, 5 scenarios, 11 acceptance criteria). |
| **Read existing change plans** | ✅ PASS | `evidence/baseline/phase0-instructions-read.md` records the Phase 0 policy reads. `plan.2026-08-07T11-11.md` is the executed atomic plan. |
| **Document the plan** | ✅ PASS | `plan.2026-08-07T11-11.md`: 59 of 59 tasks checked, 0 unchecked (`grep -c "^- \[ \]"` → 0; `grep -c "^- \[x\]"` → 59). |

### 2.2 Design Principles

| Requirement | Status | Evidence |
|---|---|---|
| **Simplicity first** | ✅ PASS | Deliverables are plain Markdown with no generated content and no indirection. The test module holds all assertions; parsing helpers and pinned data are separated into two support modules with no behavior of their own. |
| **Reusability** | ✅ PASS | `assert_fragments`, `extract_section`, `top_level_headings`, `split_frontmatter`, `parse_frontmatter`, and `markdown_table_header_cells` are generic Markdown/frontmatter helpers reused across all 36 tests. Naming follows the established `*_test_support` precedent (`epic_planner_launch_evidence_test_support`). |
| **Extensibility** | ✅ PASS | The skill file is sectioned so that F6, F7, and F8 each append inside a uniquely named reserved `##` section without reflowing F5 content. Cross-references name sections by exact heading text, never by number or position (`parallel-orchestrate/SKILL.md:30-35`), so a section can be added without renumbering. One residual friction is recorded as Gap G5 (the hard `== 16` heading-count assertion). |
| **Separation of concerns** | ✅ PASS | Three clean layers: pure parsers (`*_test_support.py`), inert pinned data (`*_expectations.py`), assertions (`test_*.py`). The parser module performs the only filesystem I/O and declares it. |

### 2.3 Module & File Structure

| Requirement | Status | Evidence |
|---|---|---|
| **Cohesive modules** | ✅ PASS | Each module has one stated purpose; no grab-bag utility appears. `parallel_orchestrator_surface_expectations.py` is inert data only, as its docstring asserts. |
| **Under 500 lines** | ✅ PASS | `parallel_orchestrator_surface_test_support.py` 465; `test_parallel_orchestrator_surface_contracts.py` 457; `parallel_orchestrator_surface_expectations.py` 253. All under the 500-line limit. Markdown deliverables (436 / 225 / 78 / 56 lines) are exempt per `general-code-change.md`, and none exceeds 500 in any case. Two files are within 8% of the limit — recorded as Gap G6 (advisory, not a violation). |
| **Public vs internal** | ✅ PASS | Regex constants in the support module are `_`-prefixed internals (`_WHITESPACE_RUN`, `_BACKTICKED`, `_HEADER_FIELDS`, `_COHORT_PROJECTION`, `_PROJECTION_SECTION`, `_DELIMITER_ROW`); the public surface is the named parser functions and path constants. |
| **No circular dependencies** | ✅ PASS | Dependency direction is strictly `test_* → expectations → test_support`. `expectations` imports only two heading constants from `test_support` (expectations:28-31); `test_support` imports nothing from the other two. |

### 2.4 Naming, Docs, and Comments

| Requirement | Status | Evidence |
|---|---|---|
| **Descriptive names** | ✅ PASS | `prescribed_cohort_table_columns`, `subagent_stop_hook_commands`, `markdown_table_header_cells`, `KICKOFF_NEVER_CARRIES_FRAGMENTS`. Module constants are `CONSTANT_CASE`, functions `snake_case`, per PEP 8. |
| **Docs/docstrings** | ✅ PASS | Every module carries a Purpose/Usage/Invariants docstring; every one of the 14 public functions in the support module carries a Google-style docstring with `Args:`, `Returns:`, `Raises:`, and `Side Effects:`. Every test carries a docstring. Satisfies `self-explanatory-code-commenting.md`. |
| **Comment why, not what** | ✅ PASS | Comments explain rationale, not mechanics. Examples: `# ``parents[3]`` walks tests/scripts/dev_tools/<file> back to the repo root.` (support:37); `# Uniqueness matters because a duplicated placeholder would give a wave-4 feature two candidate append points.` (test:239-240); `# Pair each candidate header row with the delimiter row that confirms it, so an ordinary pipe-bearing prose line is not treated as a table header.` (support:458-459). Every loop and comprehension carries an intent comment as required. No numbered `NOTE n:` tags appear. |

### 2.5 After Making Changes — Toolchain Execution

| Requirement | Status | Evidence |
|---|---|---|
| **1. Formatting** | ✅ PASS | **Command:** `poetry run black --check .`<br>**Result:** `All done! 372 files would be left unchanged.` Zero files reformatted. |
| **2. Linting** | ✅ PASS | **Command:** `poetry run ruff check .`<br>**Result:** `All checks passed!` Zero findings. Zero `# noqa` suppressions added by this branch. |
| **3. Type checking** | ✅ PASS | **Command:** `poetry run pyright`<br>**Result:** `0 errors, 0 warnings, 0 informations` |
| **4. Architecture boundaries** | N/A | No `dependency-cruiser` or `NetArchTest` surface changed; zero TypeScript and zero C# files in the diff. |
| **5. Testing** | ✅ PASS | **Command:** `poetry run pytest --cov --cov-branch --cov-report=term-missing`<br>**Result:** `3004 passed in 10.61s`, 0 failed, 0 skipped, 0 errors. |
| **6. Contract / schema compatibility** | ✅ PASS | The 36 new tests **are** the contract check for the delivered surface. Existing bundle-parity and pack-manifest-completeness suites (`test_push_down_claude_pack_manifest_completeness.py`, `test_push_down_claude_resource_contracts.py`, `test_push_down_claude_pack_end_to_end.py`) pass within the 3004, confirming the three new `core.json` entries and the three bundled mirrors are consistent. |
| **7. Integration tests** | ✅ PASS (within available scope) | JSON governance: `format_json --check` → exit 0, all governed files already formatted; `validate_json` → exit 0. Bundle parity independently verified by SHA-256 (Section 7). End-to-end live execution of the parallel surface is documented as not possible before F7 lands (`spec.md` Cross-Feature Dependencies; skill `## Per-Item Merge to Main` F7 dependency block), which is an accepted spec-level limitation, not an unrun check. |
| **Full toolchain loop** | ✅ PASS | All applicable stages passed in a single pass with zero files modified, so no restart was required. Independently reproduced by this audit; consistent with `evidence/qa-gates/final-qc-*.2026-08-08T17-5*.md`. |
| **Explicit reporting** | ✅ PASS | Exact commands and verbatim results are recorded in Section 7 and Appendix B. |

### 2.6 Summarize and Document

| Requirement | Status | Evidence |
|---|---|---|
| **Summarize changes** | ✅ PASS | Single commit `41633ad5 feat(parallel-orchestrator): add agent, skills, and surface contracts`. Section 9 enumerates all 33 changed paths by disposition. |
| **Design choices explained** | ✅ PASS | `spec.md` `## Adjudicated Decisions` records 6 decisions with rationale, including Decision 1 (parent-side merge rather than child-side) and Decision 3 (`blocked_ci_loop_limit` as the mapped terminal for loop exhaustion). The delivered files restate each delta from the epic precedent inline. |
| **Update supporting documents** | ✅ PASS | `pack-manifests/core.json` registers all three new runtime files; three bundled mirrors added; `docs/features/templates/parallel/parallel-status.md` template added; `spec.md`, `user-story.md`, and `plan.2026-08-07T11-11.md` updated with delivery status. |
| **Provide next steps** | ✅ PASS | `spec.md` `## Cross-Feature Dependencies` names F6/F7/F8 follow-on scope. Section 8 and `remediation-inputs.2026-08-08T18-12.md` carry this audit's next steps. |

---

## 3. Language-Specific Code Change Policy Compliance

### Section 3A: Python Code Change Policy Compliance

#### 3A.1 Tooling & Baseline

| Requirement | Status | Evidence |
|---|---|---|
| **Formatting with Black** | ✅ PASS | **Command:** `poetry run black --check .`<br>**Result:** `All done! 372 files would be left unchanged.` |
| **Linting with Ruff** | ✅ PASS | **Command:** `poetry run ruff check .`<br>**Result:** `All checks passed!` |
| **Type checking with Pyright** | ✅ PASS | **Command:** `poetry run pyright`<br>**Result:** `0 errors, 0 warnings, 0 informations` |
| **Testing with Pytest** | ✅ PASS | **Command:** `poetry run pytest --cov --cov-branch --cov-report=term-missing`<br>**Result:** `3004 passed in 10.61s` |

#### 3A.2 Python Design & Typing

| Requirement | Status | Evidence |
|---|---|---|
| **Strong typing** | ✅ PASS | Every function in all three modules is fully annotated on parameters and return type. All module constants carry explicit annotations (`DELIVERED_RUNTIME_FILES: tuple[Path, ...]`, `SECTION_OBLIGATION_CASES: tuple[tuple[str, str, tuple[str, ...]], ...]`). `from __future__ import annotations` in all three. No bare `Any`. |
| **`Any` isolation** | ✅ PASS | The untyped `yaml.safe_load` boundary is isolated in exactly one place, `parse_frontmatter`, which narrows to `dict[str, object]` and raises when the result is not a mapping (support:162-186). Downstream code uses `isinstance` narrowing plus `cast("...")` at each nesting level rather than propagating `Any` (`subagent_stop_hook_commands`, support:213-253). Zero `# type: ignore` added by this branch. |
| **Dataclasses for value objects** | N/A | No value object is introduced. Pinned expectation data is expressed as module-level immutable tuples, which is the simpler correct choice here. |
| **Protocols/ABCs for interfaces** | N/A | No multiple-implementation surface. `general-code-change.md` calls for protocols only when multiple implementations are likely; none is. |
| **Avoid utility classes** | ✅ PASS | No class is defined. All helpers are module-level functions, which is the policy-preferred form for pure stateless transformations. |

#### 3A.3 Python Error Handling

| Requirement | Status | Evidence |
|---|---|---|
| **Specific exceptions** | ✅ PASS | Fails fast with `ValueError` carrying a specific message at three guard points: missing frontmatter fence, unterminated fence, non-mapping frontmatter (support:151-186), and absent section heading (support:297-301). `extract_section` chains context with `raise ... from exc`. No broad `except:` or `except Exception:` anywhere. |
| **Logging over print** | N/A | No logging or output surface. Test modules communicate through assertion messages, which is the correct channel. |
| **Invariants at construction** | ✅ PASS | Guards run at parse time, before any assertion can consume a malformed document, so a malformed input cannot silently satisfy a presence check. |

### Section 3D: JSON Configuration Policy Compliance

#### 3D.1 JSON Tooling

| Requirement | Status | Evidence |
|---|---|---|
| **Formatting with sorted keys** | ✅ PASS | **Command:** `poetry run python -m scripts.dev_tools.format_json --check`<br>**Result:** exit 0; every governed file reported `already formatted`. |
| **Schema validation** | ✅ PASS | **Command:** `poetry run python -m scripts.dev_tools.validate_json`<br>**Result:** exit 0. Two `missing $schema` advisories were emitted, both against pre-existing evidence JSON belonging to unrelated features (`...-435/evidence/other/mixed-complete-checkpoint.2026-08-04T10-46.json`, `...-413/evidence/other/completion-passing-checkpoint.2026-07-25T17-19.json`). Neither is in this branch's diff. |
| **Required `$schema`** | ✅ PASS | `pack-manifests/core.json` retains its existing `$schema` declaration; the diff adds three array entries only and declares no new governed file. |

#### 3D.2 JSON Structure

| Requirement | Status | Evidence |
|---|---|---|
| **Strict JSON only** | ✅ PASS | Diff adds three plain string array entries. No comment, trailing comma, or other JSON5 construct. |
| **Deterministic key order** | ✅ PASS | The three entries are inserted in sorted position within their arrays (`.claude/agents/parallel-orchestrator.md` between `orchestrator.md` and `parallel-planner.md`; `parallel-orchestrate/SKILL.md` before `parallel-plan/SKILL.md`; `parallel-run/SKILL.md` after it). Confirmed by `format_json --check` reporting no change needed. |

### Sections 3B (PowerShell) and 3C (Bash)

N/A — zero PowerShell and zero Bash files changed on the branch. Verified by
`git diff --name-only ee0626e8..41633ad5 -- '*.ps1' '*.psm1' '*.psd1' '*.sh'` → empty.

---

## 4. Language-Specific Unit Test Policy Compliance

### Section 4A: Python Unit Test Policy Compliance

#### 4A.1 Framework and Scope

| Requirement | Status | Evidence |
|---|---|---|
| **Use Pytest** | ✅ PASS | `import pytest`; `@pytest.mark.parametrize` used for the 11 section-obligation cases, the 2 skill-frontmatter cases, the 3 prescriptive-literal cases, and the 2 frozen-hash cases. No alternative runner or `unittest` construct. |
| **Coverage expectation** | ✅ PASS | Repo-wide 91.82% statements (>= 85%) and 83.80% branches (>= 75%). New-code coverage is not applicable because all three new files are test-tree files that policy requires be excluded, and zero production Python files changed. |

#### 4A.2 Test Style and Structure

| Requirement | Status | Evidence |
|---|---|---|
| **Focused unit tests** | ✅ PASS | 36 tests, one obligation each. The heaviest test asserts a single ordered heading tuple. |
| **Mocking sparingly** | ✅ PASS | Zero mocks. The contract subject is the committed artifact; mocking it would invalidate the test. |
| **Organization** | ✅ PASS | `tests/scripts/dev_tools/` matches the directory the spec names (`spec.md` R4) and follows the sibling precedent `test_epic_run_kickoff_discovery_contract.py`. Helper modules use the established `*_test_support` / `*_expectations` naming. See Gap G7 for the mirroring nuance (the subjects live under `.claude/`, not `scripts/dev_tools/`). |

#### 4A.3 Naming and Readability

| Requirement | Status | Evidence |
|---|---|---|
| **Naming conventions** | ✅ PASS | All test names are descriptive `test_<subject>_<expected_behavior>` sentences, e.g. `test_orchestrate_skill_reserved_wave_four_sections_close_the_file`, `test_seam_status_template_realises_header_fields_prescribed_by_skill`. Parametrized ids are explicit kebab-case labels rather than positional indices for the 11 obligation cases. |
| **Docstrings/comments** | ✅ PASS | Module-level Purpose/Flow/Invariants docstring plus a per-test imperative docstring. Two parametrized tests carry extended docstrings explaining the extraction-before-matching invariant and the producer-parsing rationale. |

#### 4A.4 Running the Toolchain

| Requirement | Status | Evidence |
|---|---|---|
| **Use Pytest** | ✅ PASS | **Command:** `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q`<br>**Result:** `36 passed in 0.07s` |
| **No Alternative Test Runners** | ✅ PASS | Pytest only. |

### Section 4B: PowerShell Unit Test Policy Compliance

N/A — zero PowerShell files changed. Two pre-existing Pester suites were nonetheless executed as
part of verifying the caller's environment note; see Section 7 and Gap G1.

---

## 5. Test Coverage Detail

### Delivered surface contract suite (36 tests)

| Test Name | Scenario Type | Subject Asserted | Status |
|---|---|---|---|
| `test_agent_frontmatter_declares_parallel_orchestrator_identity` | Positive | `name`, `model`, `tools`, `skills ∋ parallel-orchestrate` | ✅ |
| `test_agent_tools_allowlist_excludes_pr_author_channel` | Negative | no `pr-author` entry in `tools` | ✅ |
| `test_agent_subagent_stop_hook_targets_parallel_checkpoint` | Positive | hook script + `-CheckpointPath` + `-ArtifactType` | ✅ |
| `test_skill_frontmatter_forks_the_parallel_orchestrator_agent[×2]` | Positive | `context: fork`, `agent: parallel-orchestrator` on both skills | ✅ |
| `test_orchestrate_skill_argument_hint_accepts_manifest_path_or_slug` | Positive | argument hint accepts path or slug | ✅ |
| `test_run_skill_argument_hint_is_the_parallel_slug` | Positive | argument hint is exactly `[parallel-slug]` | ✅ |
| `test_status_template_begins_with_generated_file_banner` | Positive | opens `<!--`; banner names GENERATED FILE / DO NOT HAND-AUTHOR | ✅ |
| `test_agent_body_contains_exactly_the_nine_required_headings` | Edge (exact set) | agent `##` headings equal the required nine | ✅ |
| `test_orchestrate_skill_intro_heading_precedes_prerequisites` | Positive | `# Parallel Orchestrate Skill` precedes `## Prerequisites` | ✅ |
| `test_orchestrate_skill_first_thirteen_headings_match_required_layout` | Edge (exact count + order) | 16 `##` headings; first 13 match R2.1 order | ✅ |
| `test_orchestrate_skill_reserved_wave_four_sections_close_the_file` | Edge (position + uniqueness) | final three headings are F6/F7/F8, each once | ✅ |
| `test_orchestrate_skill_reserved_sections_carry_one_line_reserved_body` | Edge (exact body) | each reserved body is exactly its one reserved sentence | ✅ |
| `test_orchestrate_skill_section_states_its_required_obligations[×11]` | Positive (section-scoped) | 11 named acceptance criteria, each matched inside its owning section | ✅ |
| `test_kickoff_section_carries_no_child_merge_instruction` | Negative | no `gh pr merge` inside the kickoff section | ✅ |
| `test_seam_status_template_realises_header_fields_prescribed_by_skill` | Seam (producer→consumer) | 6 parsed header fields all realised in the template | ✅ |
| `test_seam_status_template_realises_cohort_columns_prescribed_by_skill` | Seam (producer→consumer) | parsed cohort columns all present in one template header row | ✅ |
| `test_seam_status_template_realises_projections_prescribed_by_skill` | Seam (producer→consumer) | 3 parsed projection section names all present as template headings | ✅ |
| `test_checkpoint_section_states_the_four_arrays_never_written` | Positive (scoped split) | never-written list names all four F6/F8-owned arrays | ✅ |
| `test_skill_names_both_f7_dependency_block_reasons` | Positive | `EPIC_MERGE_GATE_BLOCKED`, `EPIC_WORKTREE_REMOVAL_BLOCKED` | ✅ |
| `test_run_skill_stop_path_names_parallel_plan_and_plan_path_resume` | Positive (section-scoped) | STOP branch, `/parallel-plan` guidance, plan-path resume | ✅ |
| `test_delivered_runtime_files_carry_no_prescriptive_epic_literal[×3]` | Negative | none of 3 files carries any of 3 forbidden literals | ✅ |
| `test_frozen_epic_surface_matches_pinned_baseline_digest[×2]` | Regression pin | SHA-256 of both frozen epic files equals pinned baseline | ✅ |

**Coverage of the delivered surface:** 100% of the 22 `spec.md` and 11 `user-story.md` acceptance
criteria are asserted either by one of the 36 tests or by a direct git verification recorded in
`feature-audit.2026-08-08T18-12.md`. The three criteria not expressible as an in-process text
assertion (frozen-file `git diff` emptiness for the two epic files and `orchestrate/SKILL.md`; no
change under `.claude/hooks/` or to `.claude/settings.json`) are verified by git commands in the
feature audit; the two frozen epic files carry an additional in-process content-hash pin.

**Not covered:** the three defensive `raise ValueError` guard branches in `split_frontmatter` and
`extract_section` have no dedicated negative test. Gap G3.

### Repo-wide Python coverage (denominator context)

**Coverage:** 91.82% statements (12432 / 13539 covered, 1107 missing, 387 excluded);
83.80% branch destinations (4190 / 5000 covered, 810 missing, 556 partial).
Combined coverage.py headline figure: 89.66%, displayed as `90%`.
Source: `poetry run coverage json -o artifacts/cov-review.json --quiet` over the `.coverage` data
file produced by this audit's own pytest run.

---

## 6. Test Execution Metrics

| Metric | Value | Status |
|---|---|---|
| Total Tests (repo-wide) | 3004 | ✅ |
| Tests Passed | 3004 (100%) | ✅ |
| Tests Failed | 0 | ✅ |
| Tests Skipped / xfailed / errors | 0 / 0 / 0 | ✅ |
| Tests Added by This Branch | 36 | ✅ (2968 baseline + 36 = 3004, reconciles exactly) |
| Execution Time (repo-wide) | 10.61 s | ✅ Fast |
| Execution Time (new suite) | 0.07 s | ✅ Fast |
| Average Time per Test (new suite) | ≈ 2 ms | ✅ Fast |
| Collection Time (new suite) | 0.05 s | ✅ |
| Acceptance Criteria Asserted | 33 / 33 (22 spec + 11 user story) | ✅ |
| Test File Sizes | 457 / 465 / 253 lines | ✅ All under 500 |
| Code Coverage (repo-wide Python) | 91.82% statements, 83.80% branches | ✅ |
| Coverage Delta vs Baseline | 0.00 pp statements, 0.00 pp branches | ✅ No regression |

---

## 7. Code Quality Checks

**For Python:**

| Check | Command | Result | Status |
|---|---|---|---|
| Black Formatting | `poetry run black --check .` | `All done! 372 files would be left unchanged.` | ✅ |
| Ruff Linting | `poetry run ruff check .` | `All checks passed!` | ✅ |
| Pyright Type Checking | `poetry run pyright` | `0 errors, 0 warnings, 0 informations` | ✅ |
| Pytest (repo-wide, with coverage) | `poetry run pytest --cov --cov-branch --cov-report=term-missing` | `3004 passed in 10.61s`; `Coverage LCOV written to file artifacts/python/lcov.info` | ✅ |
| Pytest (new suite) | `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q` | `36 passed in 0.07s` | ✅ |

**For JSON:**

| Check | Command | Result | Status |
|---|---|---|---|
| Sorted-key formatting | `poetry run python -m scripts.dev_tools.format_json --check` | exit 0; all governed files `already formatted` | ✅ |
| Schema validation | `poetry run python -m scripts.dev_tools.validate_json` | exit 0; 2 pre-existing out-of-diff `missing $schema` advisories | ✅ |

**Repository-invariant checks:**

| Check | Command | Result | Status |
|---|---|---|---|
| Evidence locations canonical | `poetry run python -m scripts.dev_tools.validate_evidence_locations --root .` | exit 0, no output | ✅ |
| Frozen epic surface unmodified | `git diff --stat ee0626e8..41633ad5 -- .claude/agents/epic-orchestrator.md .claude/skills/epic-orchestrate/SKILL.md .claude/skills/orchestrate/SKILL.md` | empty output | ✅ |
| No hook or settings change | `git diff --stat ee0626e8..41633ad5 -- .claude/hooks/ .claude/settings.json` | empty output | ✅ |
| Bundle parity (source vs bundled mirror) | `sha256sum` on all three pairs | `94f5f08b…` / `592d0054…` / `9fc7fe3a…` identical on both sides of each pair | ✅ |
| No forbidden epic literal in delivered files | `grep -n "Epic mode: true\|--base epic/\|integration-to-main" <3 files>` | no match | ✅ |
| `modified-workflow-needs-green-run` trigger paths | `git diff --name-only ee0626e8..41633ad5 -- .github/workflows/ .github/actions/ scripts/benchmarks/` | empty — rule does not fire | ✅ N/A |
| Cross-references named in delivered text resolve | existence checks on `compute_concurrency_batches`, `validate_parallel_manifest_text`, `ModelRouting.psm1`, 4 epic hook scripts, `parallel-planner.md`, `parallel-plan/SKILL.md`; `git log` corroboration of the `#452` / PR `#453` citation (commit `b086cf69`) | all resolve | ✅ |

**For PowerShell (executed to verify the caller's environment note; zero PowerShell files changed):**

| Check | Command | Result | Status |
|---|---|---|---|
| Named Pester suites | `Invoke-Pester` over `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` and `tests/scripts/codex-hooks/codex-pretooluse-integration.Tests.ps1` (Pester 5.6.1) | `TOTAL=52 PASSED=51 FAILED=1`. Sole failure: `enforce-pr-author-skill.ps1.allowed commands.allows gh pr create --body-file artifacts/pr_body_12.md when context exists`, message `Expected: 'allow' / But was: 'deny'`. | ⚠️ Pre-existing, out of diff — Gap G1 |

**Notes on pre-existing failures unrelated to this work.** The single Pester failure is caused by
the hook under test resolving the real, gitignored `artifacts/orchestration/orchestrator-state.json`
(present in this worktree, mid-orchestrated-run) instead of a test-supplied checkpoint. The suite
invokes the hook without overriding its checkpoint path, so the assertion outcome depends on
mutable state outside the repository. This branch changes zero `.ps1`, `.psm1`, or `.psd1` files, so
it cannot have introduced the failure. `git log -1 -- tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1`
resolves to `72360a22`, a commit that predates the merge base. The caller's additional claim that
`codex-pretooluse-integration.Tests.ps1` fails did **not** reproduce: that suite reported zero
failures in this run.

---

## 8. Gaps and Exceptions

### Identified Gaps

- **G1 — Pre-existing Pester test-isolation defect (out of diff; not remediation-required for this branch).**
  `tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1` has one failing test because the
  hook under test reads the real gitignored `artifacts/orchestration/orchestrator-state.json` rather
  than a mocked seam, making the outcome dependent on whether an orchestrated run is in progress.
  This violates `general-unit-test.md` ("Tests must not rely on mutable global state or external
  configuration that can change between runs") and the determinism principle. **It is not
  attributable to this branch:** zero PowerShell files changed, and the suite's last modification
  predates the merge base. Recommended action: open a separate potential-bug entry to inject the
  checkpoint path as a test parameter. Blocking for this branch: **no**.

- **G2 — The delivered persona's `tools` allowlist does not permit two actions the delivered skill prescribes (remediation-required).**
  Three concrete instances, all internal to this feature's own four deliverables:
  1. `.claude/skills/parallel-orchestrate/SKILL.md:277-283` (`## Per-Item Merge-Conflict Handling`
     step 1) assigns **the parent** the write of `remediation-inputs.<timestamp>.md` into the item's
     active feature folder under `docs/features/active/`. The persona's `Write`/`Edit` scopes are
     `docs/features/parallel/**` and `artifacts/orchestration/**` only
     (`.claude/agents/parallel-orchestrator.md:10-13`), so the prescribed write is denied.
     This is a divergence this feature introduced: the frozen epic precedent assigns the equivalent
     capture and write to the **child's** `atomic-executor`
     (`.claude/skills/epic-orchestrate/SKILL.md:187-194`), not to the parent.
  2. `.claude/skills/parallel-orchestrate/SKILL.md:75-77` instructs the persona to
     "Validate by calling `validate_parallel_manifest_text` from
     `scripts/dev_tools/parallel_manifest_contract.py`". Manifest validation is deliberately a
     library call and not an MCP `artifact_type` (`.claude/rules/parallel-orchestration.md`), and the
     persona's Bash grants are `Bash(git *)` and `Bash(gh *)` only, so it has **no** permitted
     mechanism to run that validation.
  3. `.claude/skills/parallel-orchestrate/SKILL.md:397-399` offers the CLI fallback
     `python -m scripts.dev_tools.validate_orchestration_artifacts parallel-orchestrator-state <path>`,
     which the same Bash grants do not permit. This instance is an **inherited** pattern —
     `.claude/skills/epic-orchestrate/SKILL.md:279` carries the identical unexecutable fallback under
     the identical allowlist — so it is advisory rather than a new defect.
  Severity: Major. Blocking for this branch: recorded as **remediation-required** because instances 1
  and 2 are internal contradictions within this feature's own deliverables rather than the accepted,
  spec-documented F7 dependency. Routed through `remediation-inputs.2026-08-08T18-12.md`.

- **G3 — Three defensive guard branches have no dedicated negative test.**
  `split_frontmatter` (missing fence, unterminated fence) and `extract_section` (absent heading) each
  raise `ValueError` with no test exercising the raise. Because the modules are test-tree files they
  are outside the coverage denominator, so the gap is invisible to every coverage metric. Impact is
  low — the guards raise rather than return empty, so a regression degrades a message rather than
  producing a false pass. Severity: Minor. Blocking: **no**.

- **G4 — One recorded coverage figure is not reproducible.**
  `evidence/qa-gates/coverage-delta.2026-08-08T17-58.md` records post-change branch coverage as
  83.82% (`covered_branches` 4191, partial 555) and attributes the +1 destination to this branch's
  new `.claude` files and `core.json` entries. This audit's independent re-run at the same HEAD
  produced 83.80% (`covered_branches` 4190, partial 556), the baseline value. The +1 destination is
  environment-dependent, so the artifact's causal attribution for it is unverified. No verdict
  changes: both values clear the 75% floor and neither is a regression. Severity: Minor
  (documentation accuracy). Blocking: **no**.

- **G5 — The `== 16` heading-count assertion couples wave-4 features to this test module.**
  `test_orchestrate_skill_first_thirteen_headings_match_required_layout:216` asserts the skill
  carries exactly sixteen `##` headings. F6, F7, and F8 can append `###` subsections inside their
  reserved sections without touching this test, which is the designed path; but any wave-4 feature
  that needs a new `##` section must edit this test module. That is arguably the intended guard, and
  it is stated as such in the test's assertion message. Recorded so a future maintainer reads the
  coupling as deliberate rather than accidental. Severity: Nit. Blocking: **no**.

- **G6 — Two added files sit within 8% of the 500-line limit.**
  `parallel_orchestrator_surface_test_support.py` at 465 lines and
  `test_parallel_orchestrator_surface_contracts.py` at 457 lines. Both comply. The split into three
  modules was made specifically to comply, and the module docstrings say so. Future additions to the
  contract will need a further split. Severity: Nit. Blocking: **no**.

- **G7 — Test location mirrors the spec-named directory rather than the subjects' directory.**
  `general-unit-test.md` requires tests to mirror the production source structure; the subjects here
  live under `.claude/agents/` and `.claude/skills/`, while the tests live at
  `tests/scripts/dev_tools/`. The path is the one `spec.md` R4 names and matches the established
  sibling precedent `tests/scripts/dev_tools/test_epic_run_kickoff_discovery_contract.py`. No
  `tests/.claude/` convention exists in this repository. Treated as conforming to established repo
  convention. Severity: Info. Blocking: **no**.

### Approved Exceptions

- **The parallel surface is not executable end-to-end until F7 lands.** `spec.md`
  `## Cross-Feature Dependencies` and `user-story.md` `## Non-Goals` explicitly scope enforcement
  hooks and `.claude/settings.json` changes to F7, and the delivered skill text names
  `EPIC_MERGE_GATE_BLOCKED` and `EPIC_WORKTREE_REMOVAL_BLOCKED` as the resulting block conditions.
  This is a documented, accepted spec-level limitation, not an audit gap. It is distinct from G2,
  which concerns capability the delivered persona itself was supposed to grant.
- **All three added Python files are excluded from coverage measurement.** They are test-tree files,
  which `general-unit-test.md` *requires* be excluded. The coverage-exclusion prohibition in that
  rule applies to production files; none of the three is production code. Not an exception to any
  threshold.

### Removed/Skipped Tests

**None.** No pre-existing test was deleted, skipped, weakened, or renamed. Test count reconciles
exactly: 2968 baseline + 36 added = 3004 observed
(`evidence/baseline/baseline-pytest-coverage.2026-08-08T16-47.md` versus this audit's run).

---

## 9. Summary of Changes

### Commits in This Branch

1. **`41633ad5`** — `feat(parallel-orchestrator): add agent, skills, and surface contracts`

Range audited: `ee0626e838109fe8d3fe3904fb4631c71879baa3..41633ad5e867070853e3e4501c3457b6641d1efc`
(33 files changed, 3976 insertions, 92 deletions).

### Files Modified

**Runtime deliverables (4 files, all NEW):**

1. **`.claude/agents/parallel-orchestrator.md`** (NEW, 225 lines)
   - Agent persona for the parallel execution surface. Frontmatter declares
     `name: parallel-orchestrator`, `model: opus`, a `tools` allowlist without any `pr-author`
     channel, `skills` including `parallel-orchestrate`, `memory: project`, and a `SubagentStop`
     hook invoking `validate-orchestrator-output.ps1` with the parallel checkpoint path and
     artifact type.
   - Body carries exactly the nine required `##` sections.
   - Gap G2 instance 1 originates in the `Write`/`Edit` scope list here.

2. **`.claude/skills/parallel-orchestrate/SKILL.md`** (NEW, 436 lines)
   - Procedure skill. `context: fork`, `agent: parallel-orchestrator`, argument hint accepting a
     manifest path or slug. Thirteen F5-authored `##` sections in the R2.1 order, followed by three
     reserved wave-4 placeholder sections each carrying its single reserved sentence.
   - Cross-references name sections by exact heading text so wave-4 appends need no reflow.

3. **`.claude/skills/parallel-run/SKILL.md`** (NEW, 56 lines)
   - User-invocable entry point. `context: fork`, `agent: parallel-orchestrator`, argument hint
     `[parallel-slug]`. Kickoff discovery collapses to a single local path lookup with an explicit
     STOP branch naming `/parallel-plan`.

4. **`docs/features/templates/parallel/parallel-status.md`** (NEW, 78 lines)
   - Generated-projection template opening with an HTML-comment do-not-hand-author banner. Realises
     the six header fields, the item table with a `cohort_index` column, the cohort table, and the
     three read-only projection sections the skill prescribes.

**Bundled mirrors and registration (4 files):**

5. **`extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md`** (NEW) — byte-identical mirror, SHA-256 `94f5f08b…`.
6. **`.../claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md`** (NEW) — byte-identical mirror, SHA-256 `592d0054…`.
7. **`.../claude-customizations/.claude/skills/parallel-run/SKILL.md`** (NEW) — byte-identical mirror, SHA-256 `9fc7fe3a…`.
8. **`.../claude-customizations/pack-manifests/core.json`** (MODIFIED, +3 lines) — registers the three new runtime paths in sorted position.

**Tests (3 files, all NEW):**

9. **`tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`** (NEW, 457 lines) — 36 contract tests.
10. **`tests/scripts/dev_tools/parallel_orchestrator_surface_test_support.py`** (NEW, 465 lines) — path constants, loaders, Markdown/frontmatter parsers, producer-prescription parsers.
11. **`tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`** (NEW, 253 lines) — inert pinned heading sequences, fragment tuples, and the two frozen-surface SHA-256 digests.

**Feature documents (3 files, MODIFIED):**

12. **`docs/features/active/2026-08-07-parallel-orchestrator-surface-441/spec.md`** (MODIFIED, +44/-…) — 22 acceptance criteria now all checked.
13. **`.../user-story.md`** (MODIFIED, +22/-…) — 11 acceptance criteria now all checked.
14. **`.../plan.2026-08-07T11-11.md`** (MODIFIED, +118/-…) — 59 of 59 tasks checked.

**Evidence artifacts (19 files, all NEW, all under canonical `evidence/<kind>/`):** 7 baseline,
5 other, 5 qa-gates, 2 regression-testing.

---

## 10. Compliance Verdict

### Overall Status: ⚠️ PARTIALLY COMPLIANT

All toolchain gates, all coverage thresholds, all acceptance criteria, and every repository
invariant check pass with verified evidence. One Major gap (G2) is an internal contradiction within
this feature's own deliverables: the delivered persona's `tools` allowlist does not permit two
actions the delivered skill text prescribes — the parent-side write of an item's
`remediation-inputs.<timestamp>.md` under `docs/features/active/`, and the manifest-validation
library call. That gap is the sole reason this verdict is not FULLY COMPLIANT. It is routed through
`remediation-inputs.2026-08-08T18-12.md`. Five further gaps (G3–G7) are Minor, Nit, or Info and are
recorded without remediation. G1 is a pre-existing, out-of-diff PowerShell test-isolation defect
that this branch cannot have caused.

**Fail-closed check:** no required baseline artifact, QA artifact, coverage metric, or
coverage-comparison artifact is missing. Every coverage figure reported here is numeric and was
independently re-measured by this audit rather than copied from the executor's evidence. No section
is marked UNVERIFIED.

---

### Policy-by-Policy Summary

#### General Code Change Policy (Section 2)
- ✅ Before Making Changes: objective, plan reads, and plan document all present; 59/59 plan tasks complete.
- ✅ Design Principles: simple Markdown deliverables; generic reusable parsers; wave-4 extensibility by named sections; clean three-layer separation.
- ✅ Module & File Structure: all files under 500 lines; internals `_`-prefixed; dependency direction acyclic.
- ✅ Naming, Docs, Comments: descriptive names; full Google-style docstrings; rationale-bearing comments; no numbered notes.
- ✅ Toolchain Execution: format, lint, type-check, test, contract, and JSON governance all pass in a single pass with zero file modifications.
- ✅ Summarize & Document: single conventional commit; manifest, mirrors, template, and feature documents all updated.

#### Language-Specific Code Change Policy (Section 3)

**For Python:**
- ✅ Tooling & Baseline: Black, Ruff, Pyright, Pytest all clean.
- ✅ Design & Typing: fully annotated; single isolated `yaml.safe_load` boundary; zero `Any`, zero `# type: ignore`, zero `# noqa`.
- ✅ Error Handling: specific `ValueError` fail-fast guards; no broad catch.

**For JSON:**
- ✅ Tooling: `format_json --check` and `validate_json` both exit 0.
- ✅ Structure: strict JSON; sorted insertion positions preserved.

**For PowerShell / TypeScript / C# / Bash / GitHub Actions:** N/A — zero changed files each.

#### General Unit Test Policy (Section 1)
- ✅ Core Principles: independent, isolated, fast (0.07 s / 36 tests), deterministic (no clock, RNG, network, subprocess, or temp file), readable.
- ✅ Coverage & Scenarios: 91.82% lines / 83.80% branches repo-wide, no regression; 30 positive and 4 negative assertions; ⚠️ one PARTIAL on defensive-guard error paths (G3).
- ✅ Test Structure: per-fragment failure messages; explicit AAA markers; documented intent.
- ✅ External Dependencies: none; no mocks needed; no temporary files.
- ✅ Policy Audit: this artifact plus the code review and feature audit.

#### Language-Specific Unit Test Policy (Section 4)

**For Python:**
- ✅ Framework & Scope: Pytest with parametrize; thresholds met.
- ✅ Test Style & Structure: one obligation per test; zero mocks; spec-named directory with sibling precedent (G7 Info).
- ✅ Naming & Readability: descriptive names and explicit kebab-case parametrize ids.
- ✅ Toolchain: Pytest only.

**For PowerShell:** N/A for this diff; the two suites executed for the environment note are covered in Section 7 and Gap G1.

---

### Metrics Summary

- ✅ 3004 / 3004 tests passing (100%), 36 added, count reconciles exactly against baseline.
- ✅ 33 / 33 acceptance criteria asserted by test or by direct git verification.
- ✅ 91.82% Python line coverage (>= 85% required, margin +6.82 pp).
- ✅ 83.80% Python branch coverage (>= 75% required, margin +8.80 pp).
- ✅ 0.00 pp coverage change versus baseline; no regression on any measured metric.
- ✅ 0 Black reformats, 0 Ruff findings, 0 Pyright errors, 0 Pyright warnings.
- ✅ 0 new suppressions (`# noqa`, `# type: ignore`).
- ✅ 3 / 3 bundled mirrors byte-identical to source by SHA-256.
- ✅ Frozen epic surface byte-identical: empty `git diff` for all three frozen paths, plus 2 content-hash pins.
- ✅ 0 changes under `.claude/hooks/`; 0 changes to `.claude/settings.json`.
- ✅ 19 / 19 evidence artifacts under canonical `<FEATURE>/evidence/<kind>/`; validator exit 0.
- ✅ Test execution time 10.61 s repo-wide, 0.07 s for the new suite (fast).
- ⚠️ 1 Major gap (G2) routed to remediation; 1 pre-existing out-of-diff PowerShell failure (G1).

---

### Recommendation

**Needs revision (narrow) — Conditional Go otherwise.**

Every gate that this branch is responsible for passes. The single item to address before merge is
G2: reconcile the delivered persona's `tools` allowlist with the two actions the delivered skill
text prescribes. The preferred fix for instance 1 is to reassign the `remediation-inputs` write to
the child's chain, matching the frozen epic precedent, rather than to widen the parent's write
scope — this preserves both the tighter allowlist and the precedent's actor model. Instance 2
requires either a permitted invocation mechanism for `validate_parallel_manifest_text` or a
restatement of the obligation in terms the persona can execute. Instance 3 is inherited from the
epic precedent and may be deferred with the rest of that pattern.

Recommended non-blocking follow-ups: open a potential-bug entry for G1 (Pester checkpoint-path test
isolation), add three negative tests for the guard branches in G3, and correct the branch-delta
attribution sentence in the coverage-delta artifact per G4.

---

## Appendix A: Test Inventory

### Complete Test List — `tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`

Collected via `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q --collect-only` (36 items).

1. `test_agent_frontmatter_declares_parallel_orchestrator_identity`
2. `test_agent_tools_allowlist_excludes_pr_author_channel`
3. `test_agent_subagent_stop_hook_targets_parallel_checkpoint`
4. `test_skill_frontmatter_forks_the_parallel_orchestrator_agent[relative_path0]`
5. `test_skill_frontmatter_forks_the_parallel_orchestrator_agent[relative_path1]`
6. `test_orchestrate_skill_argument_hint_accepts_manifest_path_or_slug`
7. `test_run_skill_argument_hint_is_the_parallel_slug`
8. `test_status_template_begins_with_generated_file_banner`
9. `test_agent_body_contains_exactly_the_nine_required_headings`
10. `test_orchestrate_skill_intro_heading_precedes_prerequisites`
11. `test_orchestrate_skill_first_thirteen_headings_match_required_layout`
12. `test_orchestrate_skill_reserved_wave_four_sections_close_the_file`
13. `test_orchestrate_skill_reserved_sections_carry_one_line_reserved_body`
14. `test_orchestrate_skill_section_states_its_required_obligations[kickoff-marker-and-main-base]`
15. `test_orchestrate_skill_section_states_its_required_obligations[kickoff-never-carries-foreign-markers]`
16. `test_orchestrate_skill_section_states_its_required_obligations[kickoff-feature-folder-and-issue-number]`
17. `test_orchestrate_skill_section_states_its_required_obligations[kickoff-resume-at-plan-path]`
18. `test_orchestrate_skill_section_states_its_required_obligations[cohort-barrier-and-slot-filling-order]`
19. `test_orchestrate_skill_section_states_its_required_obligations[merge-on-green-parent-executes-merge]`
20. `test_orchestrate_skill_section_states_its_required_obligations[merge-conflict-exhaustion-and-f8-handoff]`
21. `test_orchestrate_skill_section_states_its_required_obligations[boundaries-generated-projection-rules]`
22. `test_orchestrate_skill_section_states_its_required_obligations[boundaries-regeneration-list]`
23. `test_orchestrate_skill_section_states_its_required_obligations[checkpoint-eight-merge-status-values]`
24. `test_orchestrate_skill_section_states_its_required_obligations[completion-both-run-modes]`
25. `test_kickoff_section_carries_no_child_merge_instruction`
26. `test_seam_status_template_realises_header_fields_prescribed_by_skill`
27. `test_seam_status_template_realises_cohort_columns_prescribed_by_skill`
28. `test_seam_status_template_realises_projections_prescribed_by_skill`
29. `test_checkpoint_section_states_the_four_arrays_never_written`
30. `test_skill_names_both_f7_dependency_block_reasons`
31. `test_run_skill_stop_path_names_parallel_plan_and_plan_path_resume`
32. `test_delivered_runtime_files_carry_no_prescriptive_epic_literal[relative_path0]`
33. `test_delivered_runtime_files_carry_no_prescriptive_epic_literal[relative_path1]`
34. `test_delivered_runtime_files_carry_no_prescriptive_epic_literal[relative_path2]`
35. `test_frozen_epic_surface_matches_pinned_baseline_digest[.claude/agents/epic-orchestrator.md-f4e3589ab53e6a61791f2d31e7506e7e6003ec63fe651f3cec323023d923f250]`
36. `test_frozen_epic_surface_matches_pinned_baseline_digest[.claude/skills/epic-orchestrate/SKILL.md-3c2e38bd5bdc5e2b7312437d47dc27aa282f2ff24fbaf01590b51e853e788d68]`

### Supporting modules (no tests; parsers and inert data)

- `tests/scripts/dev_tools/parallel_orchestrator_surface_test_support.py` — 14 public functions:
  `read_repo_text`, `file_sha256`, `collapse_whitespace`, `split_frontmatter`, `parse_frontmatter`,
  `string_sequence`, `subagent_stop_hook_commands`, `top_level_headings`, `extract_section`,
  `assert_fragments`, `orchestrate_skill_section`, `prescribed_header_fields`,
  `prescribed_cohort_table_columns`, `prescribed_projection_sections`,
  `markdown_table_header_cells`.
- `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py` — inert pinned data only.

---

## Appendix B: Toolchain Commands Reference

**PR context refresh (artifacts were absent at audit start; regenerated with the resolved base):**
```bash
poetry run python -m scripts.dev_tools.pr_context.collector \
  --base epic/parallel-orchestration-integration --head HEAD --repo-root .
# → artifacts/pr_context.summary.txt, artifacts/pr_context.appendix.txt
```

**Python toolchain (in policy order):**
```bash
# 1. Formatting
poetry run black --check .

# 2. Linting
poetry run ruff check .

# 3. Type checking
poetry run pyright

# 5. Tests + coverage
poetry run pytest --cov --cov-branch --cov-report=term-missing
poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q
poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py -q --collect-only

# Coverage extraction (reads the existing .coverage data file; does not re-run tests)
poetry run coverage json -o artifacts/cov-review.json --quiet
```

**JSON governance:**
```bash
poetry run python -m scripts.dev_tools.format_json --check
poetry run python -m scripts.dev_tools.validate_json
```

**Repository invariants:**
```bash
poetry run python -m scripts.dev_tools.validate_evidence_locations --root .

git diff --name-status ee0626e838109fe8d3fe3904fb4631c71879baa3..41633ad5
git diff --stat ee0626e8..41633ad5 -- .claude/agents/epic-orchestrator.md \
  .claude/skills/epic-orchestrate/SKILL.md .claude/skills/orchestrate/SKILL.md
git diff --stat ee0626e8..41633ad5 -- .claude/hooks/ .claude/settings.json
git diff --name-only ee0626e8..41633ad5 -- '*.ps1' '*.psm1' '*.psd1' '*.ts' '*.cs' '*.yml' '*.yaml'
git diff --name-only ee0626e8..41633ad5 -- .github/workflows/ .github/actions/ scripts/benchmarks/

sha256sum .claude/agents/parallel-orchestrator.md \
  .claude/skills/parallel-orchestrate/SKILL.md \
  .claude/skills/parallel-run/SKILL.md \
  extensions/drm-copilot/resources/claude-customizations/.claude/agents/parallel-orchestrator.md \
  extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-orchestrate/SKILL.md \
  extensions/drm-copilot/resources/claude-customizations/.claude/skills/parallel-run/SKILL.md

grep -n "Epic mode: true\|--base epic/\|integration-to-main" \
  .claude/agents/parallel-orchestrator.md \
  .claude/skills/parallel-orchestrate/SKILL.md \
  .claude/skills/parallel-run/SKILL.md
```

**PowerShell (environment-note verification only; zero PowerShell files changed):**
```powershell
Import-Module Pester -MinimumVersion 5.0
$c = New-PesterConfiguration
$c.Run.Path = @(
  'tests/scripts/claude-hooks/enforce-pr-author-skill.Tests.ps1',
  'tests/scripts/codex-hooks/codex-pretooluse-integration.Tests.ps1'
)
$c.Run.PassThru = $true
$c.Output.Verbosity = 'None'
$r = Invoke-Pester -Configuration $c
```

**Template resolution note.** No MCP tool surface (`mcp__drm-copilot__*`) was available in this
agent's tool allowlist, so `resolve_policy_audit_template_asset` and
`validate_orchestration_artifacts` could not be invoked. The bundled template assets were resolved
by their canonical bundled paths — the same paths that tool returns, per
`extensions/drm-copilot/src/policy-audit-template-assets.ts`:
`extensions/drm-copilot/resources/templates/policy_audit/policy-audit.yyyy-MM-ddTHH-mm.md`,
`.../code-review.yyyy-MM-ddTHH-mm.md`, `.../feature-audit.yyyy-MM-ddTHH-mm.md`. All canonical major
headings and the Appendix B command reference are preserved and the template instruction block is
removed. MCP-based artifact validation could not be run; this is recorded as an assumption rather
than a pass.

---

**Audit Completed By:** `feature-review` agent
**Audit Date:** 2026-08-08
**Policy Version:** Current as of 2026-08-08
