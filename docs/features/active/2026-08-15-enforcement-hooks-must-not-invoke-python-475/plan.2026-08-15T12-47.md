# Atomic Plan — enforcement-hooks-must-not-invoke-python (Issue #475) — Full Scope (revision 9)

- Issue: #475
- Feature folder: `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475`
- Plan path (canonical, updated in place): `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/plan.2026-08-15T12-47.md`
- Work Mode: full-feature (AC sources: `spec.md` version 2.1 AC-1..AC-29 and `user-story.md` US-1..US-12; every criterion is covered by named tasks in the AC and US Coverage Matrix below — nothing is deferred, scoped out, or recorded as a follow-up)
- Research basis: `research/2026-08-15T09-00-python-free-enforcement-hooks-research.md` (hook-surface migration) and `research/2026-08-15T15-30-full-parity-check-inventory-and-bash-json-research.md` (the authoritative 85-check parity inventory; the row-by-row checklist for the completion-validator port)
- Binding scope state per `artifacts/orchestration/orchestrator-state.json`: the HI-1 component hold is RELEASED (2026-08-15T15:45Z). The orchestrator-state completion validator is ported to PowerShell at COMPLETE PARITY, measured row by row against the 85-check inventory (U family 57, C family 25, M family 3; 6 check rows already mirrored — `U1.1`, `U1.2` with the documented message divergence, `U2.1`, `U3.1`, `U4.1`, `M1.1` — plus the two pre-existing formulas `Get-ComplexityFloor`/`Resolve-DelegationModel` backing `U6.C5`/`U6.M4` without check wiring; 79 rows to port; where this recount differs from the research prose total of "7 of 85", the row-by-row parity artifact is authoritative). No check family is deferred. The existing tested mirror (`OrchestratorState.psm1`, `OrchestratorStateCompletion.psm1`, `ModelRouting.psm1`) is EXTENDED, not discarded. SD-2 (`$ArtifactType` dispatch fix) is in scope. SD-3 (structural Python-absence criterion) and D-2 avoidance (empty-success-output contract) remain binding. The three deliberate deviations PD-1/PD-2/PD-3 are implemented exactly as the spec records them (AC-21/22/23); they are not re-derived.
- Languages in scope: PowerShell (production hooks/libraries, Pester tests; toolchain: PoshQC format -> PoshQC analyze -> Pester with coverage) and Python (only existing pytest contract suites affected by the bundle mirror; toolchain: Black -> Ruff -> Pyright -> Pytest with coverage). No new Python production code; `scripts/dev_tools/*.py` is not modified.
- Stop condition (binding): run through feature-review with zero blocking findings, then STOP. No pull request is created from this run and `pr-author` is not delegated to.

## Evidence Location Invariant (non-overridable)

All evidence artifacts produced by this plan are written under `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/<kind>/` (`baseline/`, `regression-testing/`, `qa-gates/`, `other/`). No evidence is written under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or any other `artifacts/` sub-path. `artifacts/orchestration/` is used only for the orchestrator checkpoint, which is not evidence. `<TS>` in artifact names below denotes an ISO-8601 `yyyy-MM-ddTHH-mm` timestamp captured at execution time.

## Module Decomposition (500-line cap; each file named, each with its own test file)

Eleven NEW production modules, every one under 500 lines, registered as a coverage target, mirrored byte-identically into the bundle, and registered in `core.json`:

| # | New module | Content (inventory rows) | Test file |
| --- | --- | --- | --- |
| 1 | `.claude/lib/discovery-validation/DiscoveryValidation.psm1` | shared discovery-artifact validation (profile + 7 schema types) | `tests/scripts/claude-lib/discovery-validation/DiscoveryValidation.Tests.ps1` |
| 2 | `.claude/lib/orchestrator-state/OrchestratorStateReceipts.psm1` | U5.1-U5.8, U6.R1-U6.R4, U6.H1-U6.H5 (17 checks) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateReceipts.Tests.ps1` |
| 3 | `.claude/lib/orchestrator-state/OrchestratorStateModelReceipts.psm1` | U6.C1-U6.C7, U6.M1-U6.M6 (13 checks; wired to `Get-ComplexityFloor` / `Resolve-DelegationModel`) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateModelReceipts.Tests.ps1` |
| 4 | `.claude/lib/codex-routing/CodexDeployment.psm1` | port of `resolve_codex_deployment.py` (254 Python lines) | `tests/scripts/claude-lib/codex-routing/CodexDeployment.Parity.Tests.ps1` |
| 5 | `.claude/lib/codex-routing/CodexTopology.psm1` | port of `resolve_codex_topology.py` (291 Python lines) | `tests/scripts/claude-lib/codex-routing/CodexTopology.Parity.Tests.ps1` |
| 6 | `.claude/lib/orchestrator-state/OrchestratorStateCodexModelReceipts.psm1` | U6.X1-U6.X11 (11 checks; consumes CodexDeployment) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCodexModelReceipts.Tests.ps1` |
| 7 | `.claude/lib/orchestrator-state/OrchestratorStateCodexTopologyReceipts.psm1` | U6.T1-U6.T11 (11 checks; consumes CodexTopology) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCodexTopologyReceipts.Tests.ps1` |
| 8 | `.claude/lib/orchestrator-state/OrchestratorStateRoutingMatrix.psm1` | PD-1 pinned routing-matrix constants + accessors (`requires_pr_gate`, `requires_ci_gate`, `required_agents`, `required_skills`, `required_mcp_tools` per route) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateRoutingMatrix.Tests.ps1` (includes the static config-parity test) |
| 9 | `.claude/lib/orchestrator-state/OrchestratorStateCompletionChecks.psm1` | C1.1, C2.1, C3.1-C3.2, C4.1-C4.4, C5.1, C7.1-C7.2 (11 checks) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCompletionChecks.Tests.ps1` |
| 10 | `.claude/lib/orchestrator-state/OrchestratorStateRoutingContract.psm1` | C6.1-C6.14 (14 checks, including the bug-promotion tool substitution) | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateRoutingContract.Tests.ps1` |
| 11 | `.claude/lib/orchestrator-state/OrchestratorStateUnconditional.psm1` | U-family aggregator: composes the existing U1-U4 base checks (`OrchestratorState.psm1`) with modules 2, 3, 6, 7 into one `Get-OrchestratorStateUnconditionalError` entry point | `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateUnconditional.Tests.ps1` |

Extended (not discarded): `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` (completion entry point wires unconditional + C family + M family with PD-2 single emission), `.claude/lib/orchestrator-state/OrchestratorState.psm1` (preflight collapse, probe deletion). Modified hooks: `enforce-discovery-artifact-gate.ps1`, `validate-discovery-artifact-gate.ps1`, `validate-orchestrator-output.ps1`. If any module above approaches the 500-line cap during implementation, the pre-authorized split is a sibling helper `.psm1` in the same folder with its own sibling test file; no other production split shape is authorized. Independently, a test file that approaches the cap may be split into a sibling `<Module>.<Family>.Tests.ps1` in the same folder (house pattern: `BlastRadius.Conflict.Tests.ps1`, `BlastRadius.Validation.Tests.ps1`); no other test split shape is authorized. Every such split stays within the phase's 3-test-file cap (Phases 4, 6, 7 have 2 test files and Phases 2 and 8 have 1, so one split each fits — for Phase 2 the authorized shape is `DiscoveryValidation.VersionFloor.Tests.ps1` if the version-floor `It`s of P2-T6 push `DiscoveryValidation.Tests.ps1` toward the cap; Phase 10 is already at 3 and admits no split). Every new parity-bearing test file's header states that the suite is intended as the behavioral oracle for the eventual bash migration of the hook surface (AC-24).

## Sequencing Rationale (self-gating change)

These hooks gate the run that modifies them (`validate-orchestrator-output.ps1` is the orchestrator's own completion gate; `OrchestratorState.psm1` backs the PR-author gate). Ordering rules enforced by the phase structure below: (1) the guard lands first with its fixture `It`s green from the outset and an EMPTY allowlist — its two repository-scan `It`s are legitimately red until the last Python removal lands (Phase 11), so no phase runs them before Phase 14 and every intermediate verification task passes `scan_folders` narrowed to the specific test folder(s) under verification (a default scan would pull the red scan `It`s in); (2) all new leaf modules (Phases 2, 4-8) are additive and land with their tests before any consumer changes; (3) the aggregation/wiring phase (Phase 9) extends the completion module before the completion hook is switched to it (Phase 10); (4) `validate-orchestrator-output.ps1` stops calling `Test-PythonOrchestratorValidatorAvailable` in Phase 10, strictly BEFORE the probe's deletion in Phase 11, so no phase boundary leaves a hook referencing a deleted command; (5) mirrors and manifests land in Phase 12, after which the guard's repository scan (Phase 14) verifies zero findings in the guarded tree and zero findings in the mirror tree; (6) each modified hook is verified green immediately after modification, including a direct-invocation sanity check (`pwsh -NoProfile -File ...` with a `CLAUDE_HOOK_INPUT` payload) where applicable, before the next phase begins (AC-17); (7) the Python bundle-parity test `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` (`test_bundled_claude_payload_contains_all_repo_runtime_contracts`) enumerates every repo `.claude/**` file and asserts bundle presence plus byte identity, so it is expectedly red from the first new `.claude/lib/**` module creation in P2-T2 until the bundle mirror and manifest registration complete in Phase 12 (through P12-T8) — no planned task runs pytest inside that window (P12-T10 is the first), and an opportunistic pytest run inside the window must record this known-red contract test as the in-progress mirror state, not as a regression. No full-suite Pester run is scheduled before Phase 15.

## Change-Budget Accounting

`.claude/rules/powershell.md` caps each batch at 3 production files and 3 test files. Per-phase batches: Phase 1 (0 prod / 2 test — the guard test file plus the dot-sourced test-support helper `.ps1`, which is not mirrored, not registered in `core.json`, and not a coverage target); Phase 2 (1 prod + the coverage-settings file / 1 test, or 2 test files only if the authorized `DiscoveryValidation.VersionFloor.Tests.ps1` sibling split is used); Phase 3 (2 prod / 2 test); Phase 4 (2 prod + settings / 2 test); Phase 5 (2 prod + settings / 2 test); Phase 6 (2 prod + settings / 2 test); Phase 7 (2 prod + settings / 2 test); Phase 8 (1 prod + settings / 1 test); Phase 9 (2 prod + settings / 2 test); Phase 10 (1 prod / 3 test); Phase 11 (1 prod / 2 test); Phase 12 (six mirror-copy batches of at most 3 byte-identical resource copies each, plus 1 manifest resource file and one batch of 3 manifest test files); Phases 13-15 (no production or test file changes; evidence, documentation, and AC checkoffs only); Phase 16 (0 production / 2 test files — the two new sibling validator-dispatch test suites of P16-T2/P16-T3; up to 2 production files, the two discovery hooks, only if the authorized fallback path seam is exercised, in which case a mirror batch of at most 2 byte-identical copies follows under its own P16-T5 internal reset). No batch exceeds the cap. The batch-budget counter enforced by `.claude/hooks/enforce-powershell-batch-budget.ps1` is session-scoped, not phase-scoped: each of Phases 1-12 and Phase 16 therefore begins with a T1 reset task that deletes the accumulated state file, marking the phase boundary as a batch boundary so the per-phase accounting above holds at execution time (the Phase 16 reset is additionally recorded as `local_execution_overrides[LEO-1]`; it is a reset, not a cap override).

## Hard Constraints (restated from the delegation, binding on every task)

- No bash port; no task moves hook logic into `.claude/lib/bash/`.
- Complete parity is measured row by row against the 85-check inventory; no check family may be deferred, scoped out, or recorded as a follow-up. PD-1/PD-2/PD-3 are implemented as the spec records them; they are deviations with stated resolutions, not deferrals.
- No test mutates `$env:PATH`, saves/restores PATH, probes a live `python`, or defines a shadow `function python`. The Python-absence criterion is satisfied structurally (SD-3).
- `Invoke-DiscoveryValidatorExe` keeps its name and `@{ ExitCode; Output }` return shape; all 26 existing test references depend on both (verified at HEAD `b1a86fd3`: 15 `Mock` registrations — 9 in `enforce-discovery-artifact-gate.Tests.ps1` at `:26,38,51,67,87,96,107,118,137` and 6 in `validate-discovery-artifact-gate.Tests.ps1` at `:25,38,51,63,102,120` — plus 11 `Should -Invoke` assertions). The `[scriptblock] $Invoker` seams on `Invoke-OrchestratorStatePreflight` and `Invoke-RoutingContractValidation` are retained; only default bodies change.
- 500-line cap on every production and test file. No temporary files in tests.
- Every changed `.claude/**` file is mirrored byte-identically into `extensions/drm-copilot/resources/claude-customizations/.claude/**`; every new `.claude/lib/**` module is registered in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` (existing `.claude/lib` entries at lines 107-114).
- Verdict message prefixes preserved exactly: `DISCOVERY_ARTIFACT_GATE_BLOCKED:`, `ROUTING_CONTRACT_BLOCKED:`, `MODEL_ROUTING_BLOCKED:`, `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`. The `MODEL_ROUTING_BLOCKED` routing depends only on the literal token `model_routing_receipts|complexity_assessments` in error text (`validate-orchestrator-output.ps1:330`). `.claude/settings.json` hook wiring strings unchanged (`tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py:119-123` pins the command fragments).
- Error strings in the ported checks follow the inventory's exact templates, including the Python `str()`/`repr()` interpolation distinctions the inventory marks (C7, U6.X11, U6.T11). Documented divergences are limited to the U1 load-error messages and PD-1/PD-2/PD-3.
- Coverage evidence rule (binding on every coverage-bearing task): `CoveragePercentTarget = 0` in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1:148`, so PoshQC does NOT fail on coverage. Every coverage task must read numeric line/branch values out of `artifacts/pester/powershell-coverage.xml`; a zero exit code is not coverage evidence.
- Removing the probe and the Python leg from `OrchestratorState.psm1` removes currently-covered lines, so that file's coverage percentage may tick down slightly; do not plan for or require an increase there — the floors (85% line / 75% branch) still apply.

### Phase 0 — Policy Reads and Toolchain Baselines

- [x] [P0-T1] Read the policy files in the exact order given by `CLAUDE.md`: (1) `.github/copilot-instructions.md`, (2) `.github/instructions/general-code-change.instructions.md`, (3) `.github/instructions/general-unit-test.instructions.md`, (4) `.github/instructions/powershell-code-change.instructions.md`, (5) `.github/instructions/powershell-unit-test.instructions.md`, (6) `.github/instructions/python-code-change.instructions.md`, (7) `.github/instructions/python-unit-test.instructions.md`; also confirm the auto-loaded rules `.claude/rules/powershell.md` and `.claude/rules/python.md`. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read.
  - Acceptance: the artifact exists with all three required fields and lists all files above.
- [x] [P0-T2] Capture the PowerShell formatting baseline: run `mcp__drm-copilot__run_poshqc_format` on the repository and confirm zero files are modified on the clean pre-change tree. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-poshqc-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: artifact exists with all four fields; `Output Summary:` states the number of files changed (expected 0).
- [x] [P0-T3] Capture the PowerShell lint baseline: run `mcp__drm-copilot__run_poshqc_analyze` with repo settings. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-poshqc-analyze.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding count).
  - Acceptance: artifact exists with all four fields.
- [x] [P0-T4] Capture the PowerShell test-and-coverage baseline: run `mcp__drm-copilot__run_poshqc_test` using `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-poshqc-test.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass/fail counts and numeric baseline line-coverage and branch-coverage percentages read from `artifacts/pester/powershell-coverage.xml` (coverage is mandatory policy for PowerShell; a zero exit code is not coverage evidence).
  - Acceptance: artifact exists with all four fields and numeric coverage headline values read from the coverage XML.
- [x] [P0-T5] Capture the Python formatting baseline: run `poetry run black --check .` (non-mutating check mode). Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-black-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: artifact exists with all four fields.
- [x] [P0-T6] Capture the Python lint baseline: run `poetry run ruff check .`. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: artifact exists with all four fields.
- [x] [P0-T7] Capture the Python type-check baseline: run `poetry run pyright`. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: artifact exists with all four fields.
- [x] [P0-T8] Capture the Python test-and-coverage baseline: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/baseline/baseline-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including pass counts and numeric baseline line-coverage and branch-coverage percentages (coverage is mandatory policy for Python).
  - Acceptance: artifact exists with all four fields and numeric coverage headline values.

### Phase 1 — AST Guard (empty allowlist, two-sided design)

- [x] [P1-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P1-T2] Author the test-support helper `tests/scripts/claude-runtime/EnforcementHooksNoPythonInvocation.Helpers.ps1` (under 500 lines) containing (a) the AST detection helper that classifies every `CommandAst`, and (b) the allowlist table, authored EMPTY — zero entries, because the HI-1 hold is released and every Python invocation site is removed within this run. Detection classes: (1) bare or `&`/`.`-invoked constant command whose `GetCommandName()` is exactly `python`, `python3`, `py`, or `poetry` (case-insensitive, including quoted constants); (2) `Start-Process` with a matching `-FilePath` or first-positional constant; (3) dynamic invocation fail-closed with two carve-outs: (a) a `VariableExpressionAst` bound to a `[scriptblock]`-typed parameter in the same file is not a finding — the `& $Invoker` / `& $ProfileReader` / `& $RequiredArtifactReader` / `& $FileExistsCheck` pattern, 25 verified sites in the guarded tree (`enforce-completion-consistency.ps1:298`; `enforce-completion-helpers.ps1:103,150`; `enforce-discovery-artifact-gate.ps1:122,176`; `enforce-powershell-batch-budget.ps1:189,190,196,198,208`; `enforce-python-batch-budget.ps1:186,187,193,195,205`; `persist-session-id.ps1:103,108,110,133`; `validate-discovery-artifact-gate.ps1:125,208`; `validate-orchestrator-output.ps1:143,218`; `validate-task-researcher-output.ps1:146`; `OrchestratorState.psm1:467`); (b) a dot-source (`.`) invocation of a variable that is not a `[scriptblock]` parameter is not a finding (the sibling-script helper-load pattern; the two occurrences are `enforce-completion-consistency.ps1:47` and `enforce-parallel-drift-gate.ps1:67`, both assigned from `Join-Path $PSScriptRoot '<sibling>.ps1'` in the same file); `&`-invocation of a non-scriptblock variable remains a fail-closed finding; both carve-outs and the residual gap are documented in the helper header; the spec-sanctioned fallback (flag dynamic invocations only when the variable name matches `(?i)python|poetry|py\b`) remains the recorded alternative if AST binding proves unreliable; (4) any `Invoke-Expression`. The helper header documents: the allowlist is empty and must remain empty; entries may only be added by an owner decision, never by an implementer working around a failure. The helper is test support only: it is NOT mirrored into the bundle, NOT registered in `core.json`, and NOT added to the coverage target list.
  - Satisfies: AC-8 (helper leg), AC-9 (detection-logic leg).
- [x] [P1-T3] Author `tests/scripts/claude-runtime/enforcement-hooks-no-python-invocation.Tests.ps1` (under 500 lines; add a second `*.Tests.ps1` in the same folder only if the fixture `It`s alone still exceed the cap — never duplicate the detection helper across files), modeled structurally on `tests/scripts/claude-runtime/test-name-uniqueness.Tests.ps1`: the repository root is resolved in `BeforeAll` by walking up from `$PSScriptRoot` (CWD-independent), the helper is dot-sourced from `BeforeAll`, and the file carries the in-memory fixture `It`s plus both repository-scan `It`s. Scan roots are anchored at the resolved repository root and are exactly two: `<repoRoot>/.claude/hooks` and `<repoRoot>/.claude/lib`, enumerated with `Get-ChildItem -Recurse` under those two roots only (files `*.ps1` and `*.psm1`, excluding `.claude/lib/bash/**`); a repo-wide recursive glob MUST NOT be used. The bundled mirror tree `extensions/drm-copilot/resources/claude-customizations/.claude/**` is a byte-identical second copy holding the same invocation sites; it is OUT of scan scope and deliberately NOT allowlisted, because allowlist keys are repo-root-relative paths. Include an assertion that the enumerated file set contains no path under `extensions/`. Two repository-scan `It`s: Assertion A (no violations beyond the allowlist) — the scan's findings, after subtracting matches against the (empty) allowlist, are empty, with the residual finding list in `-Because`; Assertion B (no allowlist rot) — every allowlist entry corresponds to at least one real detected invocation in its named file and enclosing function (vacuously green over the empty allowlist, retained so any future entry that goes stale fails the suite). Non-detection fixtures: `python`/`poetry run` in string literals and comments, `Invoke-Python*` function-name invocations, scriptblock-parameter `& $Invoker` calls, and the dot-source-of-path-variable form. No PATH mutation or live-executable probing anywhere in the file. Fixture `It` names must not differ from one another only by letter case (`test-name-uniqueness.Tests.ps1` enforces case-folded uniqueness of sibling names within the same parent scope).
  - Acceptance: file exists with both scan `It`s, the two-root anchored enumeration, and the no-`extensions/` assertion; the enumerated set is exactly the files under those two roots minus `.claude/lib/bash/**`, and contains no `extensions/` path. Satisfies: AC-8 (structure leg; shape deviation from the spec's single scan `It` is recorded in the matrix row), AC-9, AC-10 (fixture legs), US-5 (guard leg).
- [x] [P1-T4] Run the guard's fixture `It`s only (Pester filtered to the fixture contexts, or `scan_folders` narrowed with the scan `It`s excluded by tag/filter) and confirm every detection fixture (classes 1-4) reports a finding and every non-detection fixture reports zero findings — the fixtures must pass from the outset. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/regression-testing/guard-fixtures.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording the fixture-proven detection results and the empty allowlist state.
  - Acceptance: all fixture `It`s pass; artifact exists with all four fields. Satisfies: AC-9, AC-10 (fixture legs).
- [x] [P1-T5] [expect-fail] Run the guard's repository-scan Assertion A against the current tree and record the failure as fail-before evidence: the scan must report exactly the known pre-change invocation sites (`enforce-discovery-artifact-gate.ps1:50`, `validate-discovery-artifact-gate.ps1:53`, `validate-orchestrator-output.ps1:196`, `OrchestratorState.psm1:367` and `:451`) and no others — zero findings in the six incidental hooks and zero findings for the two dot-source helper-load sites. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/regression-testing/guard-scan-fail-before.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero), `Output Summary:` listing the detected sites. The scan `It`s are then not run again until Phase 14; every intermediate verification task narrows its scan scope.
  - Acceptance: Assertion A fails with exactly the five listed findings; artifact exists with all four fields. Satisfies: AC-10 (no-false-positive leg), US-5 (detection-proof leg).

### Phase 2 — Shared Discovery-Validation Module

- [x] [P2-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P2-T2] Create `.claude/lib/discovery-validation/DiscoveryValidation.psm1` (new module, under 500 lines) implementing the per-type validation used by both discovery hooks: `profile` -> placeholder-contract check mirroring `validate_profile_text` (empty/whitespace document, YAML-parse failure handling, non-mapping root, presence of the single placeholder field `legacy_source_path`; a documented top-level-key check against `TODO(#9001)`, no full YAML parser); the seven schema types (`feature-contract`, `coverage-ledger`, `runtime-scenario`, `parity-matrix`, `unspecified-behavior`, `product-decision`, `evidence-reference`) -> JSON parse, object-root check, `$schema` extraction (absent/non-string/empty reported in the `schema resolution failed (...)` error family), then `Test-Json -SchemaFile` (JSON Schema Draft 2020-12) against the locally resolved `file://` schema under `schemas/discovery/v1/`; non-`file` schemes reported as `schema resolution failed (...)`; no `http(s)://` fetch. Result contract (D-2 avoidance): success returns `ExitCode = 0` with empty `Output`; failure returns `ExitCode = 1` with one error string per line. Module header documents the PowerShell 7.4+ requirement for `Test-Json -SchemaFile` Draft 2020-12 support, its verified presence in PowerShell 7.6.3 in this environment, and the destination-repository risk on PowerShell 7.0-7.3. Verification note: confirm the schema type-to-file mapping against `scripts/dev_tools/validate_discovery_artifacts.py` during implementation — the type names `runtime-scenario`, `product-decision`, and `unspecified-behavior` correspond to `runtime-characterization-scenario.schema.json`, `product-decision-record.schema.json`, and `unspecified-behavior-record.schema.json` respectively.
  - Satisfies: AC-7 (production leg), AC-6 (contract leg), AC-18, US-8 (module-header leg).
- [x] [P2-T3] Create `tests/scripts/claude-lib/discovery-validation/DiscoveryValidation.Tests.ps1` (under 500 lines) covering, with in-memory document strings and no temporary files: valid and invalid JSON; non-object root; missing, non-string, and empty `$schema`; non-`file` scheme; schema pass and schema fail against the read-only repository fixtures in `schemas/discovery/v1/`; profile cases (empty document, YAML-failure handling, non-mapping root, missing `legacy_source_path`); and an explicit assertion that a passing validation returns `ExitCode = 0` with empty `Output`. No PATH mutation, no live executables.
  - Satisfies: AC-7 (test leg), AC-6 (unit-test leg).
- [x] [P2-T4] Add the destination-version-floor runtime check to `.claude/lib/discovery-validation/DiscoveryValidation.psm1` (same file as P2-T2; stays under 500 lines; Phase 2 remains 1 production file plus the coverage-settings file): the module performs a runtime PowerShell version check through an injectable version seam (an optional parameter or module-scoped seam defaulting to `$PSVersionTable.PSVersion`; the seam is read, never written) and FAILS CLOSED on PowerShell < 7.4 with a single explicit, actionable error message that (a) names the required version (PowerShell 7.4+), (b) names `Test-Json -SchemaFile` JSON Schema Draft 2020-12 support as the reason, and (c) identifies issue #475. The check runs before any schema validation is attempted; on an unsupported version the module does not fail obscurely (no raw `Test-Json` parameter error) and does not silently degrade (no partial validation, no fail-open). Background recorded in the spec's "Destination Version Floor — PowerShell 7.4+" section: all seven schemas under `schemas/discovery/v1/*.json` declare Draft 2020-12 (verified 7 of 7); the repo standard "PowerShell 7+" in `.claude/rules/powershell.md` is below the floor and `.claude/rules/**` is not modified; Draft-2020-12-free alternatives were assessed and rejected in the spec.
  - Acceptance: on an injected version below 7.4 every validation entry point returns the fail-closed error containing all three message elements; on 7.4+ behavior is unchanged. Satisfies: AC-26, US-11 (enforcement leg).
- [x] [P2-T5] Add comment-based help to `.claude/lib/discovery-validation/DiscoveryValidation.psm1` stating the PowerShell 7.4+ version floor and its reason (`Test-Json -SchemaFile` Draft 2020-12 support). The help text is the destination-visible surface: the module ships to destinations inside the `.claude/**` payload, the pushed-down pack contains only `config/` and `pack-manifests/` alongside `.claude/**` (no pack README exists to carry the statement), and `.claude/rules/**` is not modified. File stays under 500 lines.
  - Acceptance: the module's comment-based help states the 7.4+ floor and the `Test-Json -SchemaFile` Draft 2020-12 reason. Satisfies: AC-27, US-11 (help-text leg).
- [x] [P2-T6] Extend `tests/scripts/claude-lib/discovery-validation/DiscoveryValidation.Tests.ps1` with deterministic version-floor tests driven ONLY through the injectable version seam: an injected version below 7.4 (for example 7.3.x) yields the fail-closed error, with assertions on all three message elements (PowerShell 7.4+, `Test-Json -SchemaFile` Draft 2020-12 reason, issue #475); an injected version at and above the floor (7.4.0 and a higher version) proceeds to normal validation; and no silent-degradation path exists (the below-floor case never returns `ExitCode = 0`). The tests MUST NOT mutate `$PSVersionTable` and MUST NOT depend on ambient host state, per `.claude/rules/powershell.md`. If these `It`s push the file toward the 500-line cap, use the authorized sibling split `DiscoveryValidation.VersionFloor.Tests.ps1` in the same folder (Phase 2 stays within the 3-test-file cap either way).
  - Acceptance: all version-floor tests pass deterministically through the seam with zero `$PSVersionTable` mutation. Satisfies: AC-28, US-11 (seam-test leg).
- [x] [P2-T7] Register `.claude/lib/discovery-validation/DiscoveryValidation.psm1` as a coverage target in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, following the existing entries at lines 48, 70, 91-92.
  - Satisfies: AC-16 (coverage-target leg).
- [x] [P2-T8] Verify the new module immediately: run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/discovery-validation`; suite green (including the P2-T6 version-floor `It`s); read the module's numeric line and branch coverage out of `artifacts/pester/powershell-coverage.xml` and confirm the 85%/75% floors. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/discovery-module-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric coverage values. Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/discovery-validation/DiscoveryValidation.psm1` (plus any authorized sibling helper `.psm1` created under the pre-authorized split) — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suite green, numeric coverage recorded from the XML, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16 (floor leg), AC-17 (per-site leg).

### Phase 3 — Discovery Hooks

- [x] [P3-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P3-T2] Replace the body of `Invoke-DiscoveryValidatorExe` in `.claude/hooks/enforce-discovery-artifact-gate.ps1` (invocation at line 50; stale doc-comment at line 39) so it imports and calls the shared `.claude/lib/discovery-validation/DiscoveryValidation.psm1` implementation. Preserve the seam function name, the `-ValidatorArgs <string[]>` parameter, and the `@{ ExitCode; Output }` return shape. Leave the fail-open `$ProfileReader = { $null }` default and the deny-on-non-empty-output logic (lines 183-184) unchanged. Preserve the `DISCOVERY_ARTIFACT_GATE_BLOCKED:` prefix.
  - Satisfies: AC-5 (hook 1 leg), US-4 (production leg), US-1 (hook 1 leg).
- [x] [P3-T3] Apply the same replacement to `Invoke-DiscoveryValidatorExe` in `.claude/hooks/validate-discovery-artifact-gate.ps1` (invocation at line 53; stale doc-comment at line 42), with the same preserved contracts (seam name, return shape, deny-on-output logic at lines 217-218, prefix).
  - Satisfies: AC-5 (hook 2 leg), US-4 (production leg), US-1 (hook 2 leg).
- [x] [P3-T4] Add hook-level allow-verdict tests to `tests/scripts/claude-hooks/enforce-discovery-artifact-gate.Tests.ps1` and `tests/scripts/claude-hooks/validate-discovery-artifact-gate.Tests.ps1` asserting that a passing validation (empty seam output, `ExitCode = 0`) yields an allow verdict through the existing deny-on-non-empty-output logic (D-2 avoidance proof). Optionally update the cosmetic `ModuleNotFoundError` fixture messages at `enforce-discovery-artifact-gate.Tests.ps1:107` and `validate-discovery-artifact-gate.Tests.ps1:102`. Leave all 26 existing `Invoke-DiscoveryValidatorExe` test references unmodified (15 `Mock` registrations plus 11 `Should -Invoke` assertions, per-file breakdown in the Hard Constraints).
  - Satisfies: AC-6 (hook-level test leg), AC-5 (mock-site survival), US-4.
- [x] [P3-T5] Verify both discovery hooks immediately: run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-hooks` (or the two suites via `Invoke-Pester -Path`); both suites green with all 26 seam references unmodified. Direct-invocation sanity check: invoke `pwsh -NoProfile -File .claude/hooks/enforce-discovery-artifact-gate.ps1` and `pwsh -NoProfile -File .claude/hooks/validate-discovery-artifact-gate.ps1` once each with a `CLAUDE_HOOK_INPUT` payload, confirming each process exits without a `CommandNotFoundException` and returns the expected verdict (allow, given the gate's current fail-open declaration default) with the `DISCOVERY_ARTIFACT_GATE_BLOCKED:` prefix path intact; record both invocations and exit codes. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/discovery-hooks-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: both suites green and both direct invocations recorded before Phase 4 begins. Satisfies: AC-5, AC-17 (per-site leg).

### Phase 4 — Parity Port: Receipt and Optional-Key Families (U5, U6.R, U6.H, U6.C, U6.M)

- [x] [P4-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P4-T2] Create `.claude/lib/orchestrator-state/OrchestratorStateReceipts.psm1` (under 500 lines) implementing inventory rows U5.1-U5.8 (`delegation_receipts` list form, object/namespaced form, promotion sub-keys, type error), U6.R1-U6.R4 (`remediation_loop` cycle invariants, including the zero-error tolerance for a non-object `remediation_loop` or non-list `cycles`), and U6.H1-U6.H5 (`human_interaction` shape), with error strings matching the inventory templates exactly. The module header records the U6.H reconciliation (spec Parity Contract): these library checks are additive to the stricter hook-internal `Test-HumanInteractionShape`, which is retained unchanged; strictness never decreases.
  - Satisfies: AC-19 (U5/U6.R/U6.H rows), AC-25 (library-port leg), US-6.
- [x] [P4-T3] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateReceipts.Tests.ps1` (under 500 lines) with at least one failing fixture per row (17 rows) asserting the exact inventory error string, plus at least one passing fixture per family (U5, U6.R, U6.H), driven through in-memory JSON strings; file header states the bash-migration behavioral-oracle intent.
  - Satisfies: AC-19 (fixture leg), AC-24 (oracle-header leg).
- [x] [P4-T4] Create `.claude/lib/orchestrator-state/OrchestratorStateModelReceipts.psm1` (under 500 lines) implementing rows U6.C1-U6.C7 (`complexity_assessments` per-entry, with U6.C5 wired to the existing `Get-ComplexityFloor` in `.claude/lib/model-routing/ModelRouting.psm1` — not a re-implementation) and U6.M1-U6.M6 (`model_routing_receipts` per-entry, with U6.M4 wired to the existing `Resolve-DelegationModel` — not a re-implementation), error strings matching the inventory templates exactly.
  - Satisfies: AC-19 (U6.C/U6.M rows and formula-reuse clause), US-6.
- [x] [P4-T5] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateModelReceipts.Tests.ps1` (under 500 lines) with at least one failing fixture per row (13 rows) asserting the exact string, one passing fixture per family, and explicit assertions that U6.C5/U6.M4 invoke the single `ModelRouting.psm1` formulas (for example via `Mock -ModuleName` observation or exported-command identity); oracle-intent header.
  - Satisfies: AC-19 (fixture and reuse-assertion legs), AC-24 (oracle-header leg).
- [x] [P4-T6] Register both new modules as coverage targets in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/orchestrator-state`; suites green; read both modules' numeric line/branch coverage from `artifacts/pester/powershell-coverage.xml` and confirm the floors. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/parity-receipts-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage per module). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/orchestrator-state/OrchestratorStateReceipts.psm1` and `.claude/lib/orchestrator-state/OrchestratorStateModelReceipts.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suites green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 5 — Parity Port: Codex Resolver Ports

- [x] [P5-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P5-T2] Create `.claude/lib/codex-routing/CodexDeployment.psm1` (under 500 lines): a PowerShell port of `scripts/dev_tools/resolve_codex_deployment.py` (254 lines), exposing the resolver the U6.X checks consume (including the `ValueError`-equivalent throw surface U6.X5 reports and the nine resolved keys U6.X11 compares). Module header follows the `ModelRouting.psm1` pattern for any pinned constants and states the single-implementation rule: U6.X checks must call this port, never a re-implementation.
  - Satisfies: AC-20 (deployment-port leg), US-6.
- [x] [P5-T3] Create `tests/scripts/claude-lib/codex-routing/CodexDeployment.Parity.Tests.ps1` (under 500 lines) following the `ModelRouting.Parity.Tests.ps1` house pattern: parity fixtures asserting the port's outputs against the Python source's documented behavior for representative and boundary inputs, including invalid-input throw cases; oracle-intent header.
  - Satisfies: AC-20 (parity-test leg), AC-24 (oracle-header leg).
- [x] [P5-T4] Create `.claude/lib/codex-routing/CodexTopology.psm1` (under 500 lines): a PowerShell port of `scripts/dev_tools/resolve_codex_topology.py` (291 lines), exposing the resolver the U6.T checks consume (invalid-input throw surface U6.T10; the twelve resolved keys U6.T11 compares; `FORCED_ROOT_PERSONAS` for U6.T9). Same single-implementation rule in the header.
  - Satisfies: AC-20 (topology-port leg), US-6.
- [x] [P5-T5] Create `tests/scripts/claude-lib/codex-routing/CodexTopology.Parity.Tests.ps1` (under 500 lines) following the same house pattern; oracle-intent header.
  - Satisfies: AC-20 (parity-test leg), AC-24 (oracle-header leg).
- [x] [P5-T6] Register both codex-routing modules as coverage targets in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/codex-routing`; suites green; read numeric coverage from `artifacts/pester/powershell-coverage.xml`; floors met. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/codex-resolvers-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage per module). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/codex-routing/CodexDeployment.psm1` and `.claude/lib/codex-routing/CodexTopology.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suites green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 6 — Parity Port: Codex Receipt Validators (U6.X, U6.T)

- [x] [P6-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P6-T2] Create `.claude/lib/orchestrator-state/OrchestratorStateCodexModelReceipts.psm1` (under 500 lines) implementing rows U6.X1-U6.X11 (`codex_model_routing_receipts` per-entry: shape, 10 required keys, phase string, resolver-invalid-inputs, ceiling monotonicity, ceiling-transition rules, resolved-key comparison with Python `repr()`-style interpolation per U6.X11), consuming `CodexDeployment.psm1` as the single resolver implementation.
  - Satisfies: AC-19 (U6.X rows), AC-20 (single-resolver consumption leg), US-6.
- [x] [P6-T3] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCodexModelReceipts.Tests.ps1` (under 500 lines): one failing fixture per row (11 rows) asserting the exact string including the `{expected!r}`/`{actual!r}` rendering, one passing fixture for the family, and an assertion that the checks invoke the `CodexDeployment.psm1` resolver rather than a re-implementation; oracle-intent header.
  - Satisfies: AC-19, AC-20, AC-24 (oracle-header leg).
- [x] [P6-T4] Create `.claude/lib/orchestrator-state/OrchestratorStateCodexTopologyReceipts.psm1` (under 500 lines) implementing rows U6.T1-U6.T11 (`codex_topology_receipts` per-entry: shape, 13 required keys, phase, languages list, integer-not-boolean file counts per U6.T6, `cross_cutting` boolean, `execution_context` string, `root_persona` null-or-enum, resolver-invalid-inputs, resolved-key comparison per U6.T11), consuming `CodexTopology.psm1` as the single resolver implementation.
  - Satisfies: AC-19 (U6.T rows), AC-20 (single-resolver consumption leg), US-6.
- [x] [P6-T5] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCodexTopologyReceipts.Tests.ps1` (under 500 lines): one failing fixture per row (11 rows) asserting the exact string, one passing fixture, resolver-reuse assertion, and explicit boolean-rejected-where-integer-required fixtures for U6.T6; oracle-intent header.
  - Satisfies: AC-19, AC-20, AC-24 (oracle-header leg).
- [x] [P6-T6] Register both modules as coverage targets in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/orchestrator-state`; suites green; numeric coverage from `artifacts/pester/powershell-coverage.xml`; floors met. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/codex-receipts-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage per module). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/orchestrator-state/OrchestratorStateCodexModelReceipts.psm1` and `.claude/lib/orchestrator-state/OrchestratorStateCodexTopologyReceipts.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suites green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 7 — Parity Port: Routing-Matrix Constants (PD-1) and Completion Checks (C1-C5, C7)

- [x] [P7-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P7-T2] Create `.claude/lib/orchestrator-state/OrchestratorStateRoutingMatrix.psm1` (under 500 lines): the PD-1 resolution exactly as the spec records it — the routing-matrix subset the checks consume (per-route `requires_pr_gate`, `requires_ci_gate`, `required_agents`, `required_skills`, `required_mcp_tools`) embedded as pinned constants following the `ModelRouting.psm1:33-39` pattern, with accessor functions for the C3/C4/C6 gates. The module performs NO disk read of `config/orchestration-routing.json` at validation time and documents why pinned constants were chosen over fail-closed-on-missing-config (a missing-config crash or blanket block is the portability failure this feature removes; destinations do not receive the config).
  - Satisfies: AC-21 (constants and no-disk-read legs), US-9.
- [x] [P7-T3] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateRoutingMatrix.Tests.ps1` (under 500 lines) including the static Pester config-parity test pinning every constant against `config/orchestration-routing.json` in drm-copilot (read at TEST time only, as the parity oracle), plus accessor unit tests; oracle-intent header.
  - Satisfies: AC-21 (config-parity-test leg), US-9, AC-24 (oracle-header leg).
- [x] [P7-T4] Create `.claude/lib/orchestrator-state/OrchestratorStateCompletionChecks.psm1` (under 500 lines) implementing rows C1.1 (completion-blocking step statuses, full five-value blocking set), C2.1 (`blocked_reason` must be absent/`none`, backtick-quoted message per the inventory), C3.1-C3.2 (pr_gate object and required fields, route-gated via `OrchestratorStateRoutingMatrix.psm1`), C4.1-C4.4 (ci_gate object, required fields, `conclusion == success`, `head_sha` match, route-gated with absent-flag-keeps-gate-on semantics), C5.1 (mandatory route phases, static map — `small` and `preparation` only), and C7.1-C7.2 (preparation-terminal contract with Python `repr()`-style interpolation), error strings matching the inventory templates exactly.
  - Satisfies: AC-19 (C1/C2/C3/C4/C5/C7 rows), AC-21 (route-gate consumption leg), US-6.
- [x] [P7-T5] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCompletionChecks.Tests.ps1` (under 500 lines): one failing fixture per row (11 rows) asserting the exact string (including the C7 `!r` rendering), one passing fixture per family, and route-gate on/off fixtures proving C3/C4 fire only for routes whose pinned constants require them; oracle-intent header.
  - Satisfies: AC-19, AC-24 (oracle-header leg).
- [x] [P7-T6] Register both modules as coverage targets in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/orchestrator-state`; suites green; numeric coverage from `artifacts/pester/powershell-coverage.xml`; floors met. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/completion-checks-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage per module). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/orchestrator-state/OrchestratorStateRoutingMatrix.psm1` and `.claude/lib/orchestrator-state/OrchestratorStateCompletionChecks.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suites green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 8 — Parity Port: Routing Contract (C6)

- [x] [P8-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P8-T2] Create `.claude/lib/orchestrator-state/OrchestratorStateRoutingContract.psm1` (under 500 lines) implementing rows C6.1-C6.14: matrix routes-object presence, route selection, route membership, `required_agents`/`required_skills`/`required_mcp_tools` equality against the pinned matrix (with the bug-promotion tool substitution `new_potential_entry` -> `new_potential_bug_entry` only when `promotion-type == "bug"`), per-agent delegation-receipt presence, per-skill receipt presence (`required is True` and non-empty `evidence`), per-tool successful-MCP-receipt presence (`ok is True` and non-empty `evidence`), `local_execution_overrides` and `delegation_bypasses` empty-list rules (both message variants each; present-key semantics per the Python C6.10/C6.11, which require the key to exist as a list), and `lifecycle_operations` list/object/MCP-surface rules. Consumes `OrchestratorStateRoutingMatrix.psm1`; error strings match the inventory templates exactly.
  - Satisfies: AC-19 (C6 rows), AC-21 (route-gate consumption leg), US-6.
- [x] [P8-T3] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateRoutingContract.Tests.ps1` (under 500 lines): one failing fixture per row (14 rows, covering both message variants of C6.10/C6.11) asserting the exact string, one passing fixture, and a bug-promotion substitution fixture; oracle-intent header.
  - Satisfies: AC-19, AC-24 (oracle-header leg).
- [x] [P8-T4] Register the module as a coverage target in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/orchestrator-state`; suite green; numeric coverage from `artifacts/pester/powershell-coverage.xml`; floors met. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/routing-contract-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/orchestrator-state/OrchestratorStateRoutingContract.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suite green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 9 — Aggregation and Completion Wiring (U-family entry point, M2, M3 reuse, PD-2)

- [x] [P9-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P9-T2] Create `.claude/lib/orchestrator-state/OrchestratorStateUnconditional.psm1` (under 500 lines) exporting `Get-OrchestratorStateUnconditionalError`: the single U-family entry point that composes the existing U1-U4 base checks (`OrchestratorState.psm1` loader and base-presence functions, whose documented path-prefixed U1 load-error message divergence is the recorded parity divergence) with `OrchestratorStateReceipts.psm1`, `OrchestratorStateModelReceipts.psm1`, `OrchestratorStateCodexModelReceipts.psm1`, and `OrchestratorStateCodexTopologyReceipts.psm1`, preserving the Python key-gated semantics (each optional-key family contributes zero errors when its key is absent).
  - Satisfies: AC-19 (U-family aggregation leg), AC-1 (unconditional-block leg, consumed in Phase 11), US-6.
- [x] [P9-T3] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateUnconditional.Tests.ps1` (under 500 lines): aggregation fixtures proving each family's errors surface through the entry point, key-gated zero-error fixtures, and a fully-valid-checkpoint zero-error fixture (the live checkpoint shape is a realistic fixture source, supplied as in-memory strings); oracle-intent header.
  - Satisfies: AC-19 (aggregation-test leg), AC-24 (oracle-header leg).
- [x] [P9-T4] Extend `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` (currently 243 lines; stays under 500): `Test-OrchestratorStateCompletionReadiness` becomes the complete-parity completion validation for the hook's call surface — it runs `Get-OrchestratorStateUnconditionalError`, the C-family checks (`OrchestratorStateCompletionChecks.psm1`, `OrchestratorStateRoutingContract.psm1`), and the M-family gate (existing M1.1 retained; new M2.1 assessment-per-matched-phase with the inventory's exact error string; M3 satisfied by invoking the same U6.C/U6.M per-entry validator implementation from `OrchestratorStateModelReceipts.psm1` — never a re-implementation). PD-2 resolution implemented as the spec records it: each error string is emitted exactly once under the completion call (single emission, a declared divergence from Python's duplicate emission); the gate does not re-emit the unconditional block's U6.C/U6.M output. Existing exported names and the `MODEL_ROUTING_BLOCKED` token guarantee (`model_routing_receipts|complexity_assessments` in error text) preserved.
  - Satisfies: AC-19 (M2/M3 rows and wiring), AC-22 (single-emission and reuse legs), US-2 (completion-gate leg), US-6.
- [x] [P9-T5] Extend `tests/scripts/claude-lib/orchestrator-state/OrchestratorStateCompletion.Tests.ps1` (currently 184 lines; stays under 500): M2 failing/passing fixtures asserting the exact inventory string; the PD-2 single-emission test (a malformed `complexity_assessments` or `model_routing_receipts` entry produces each error string exactly once under the completion call); a reuse assertion that the M3 leg invokes the `OrchestratorStateModelReceipts.psm1` implementation; all existing tests remain green unmodified; oracle-intent note added to the header.
  - Satisfies: AC-22 (test leg), AC-19, AC-24 (oracle-header leg).
- [x] [P9-T6] Register `OrchestratorStateUnconditional.psm1` as a coverage target in `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`; run `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-lib/orchestrator-state`; all suites green; numeric coverage from `artifacts/pester/powershell-coverage.xml` for the new module and the extended `OrchestratorStateCompletion.psm1`; floors met. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/completion-wiring-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric coverage per file). Additionally record in this evidence artifact a `Gate Hashes:` block containing `Get-FileHash -Algorithm SHA256` output for every production module verified by this gate — for this gate: `.claude/lib/orchestrator-state/OrchestratorStateUnconditional.psm1` and the extended `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1` — one `<repo-relative-path> = <sha256>` line per module. These hashes are the baseline reference points P15-T10 compares against.
  - Acceptance: suites green, numeric coverage recorded, floors met, and the artifact carries a `Gate Hashes:` block with one SHA-256 line per module verified by this gate. Satisfies: AC-16, AC-17.

### Phase 10 — Completion Hook: `$ArtifactType` Dispatch (SD-2 / D-1 / PD-3), Probe Consultation Removed

- [x] [P10-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P10-T2] Modify `.claude/hooks/validate-orchestrator-output.ps1`: replace the default invoker body of `Invoke-RoutingContractValidation` (lines 185-216) — remove the `Test-PythonOrchestratorValidatorAvailable` probe consultation at `:195` and the `& python -m scripts.dev_tools.validate_orchestration_artifacts` leg at `:196-197`; the portable PowerShell path becomes the only path, dispatching on `$ArtifactType` exactly as the spec defines: `orchestrator-state` -> the complete-parity `Test-OrchestratorStateCompletionReadiness -CheckpointPath $Path`; `epic-orchestrator-state` and `parallel-orchestrator-state` -> the type-scoped structural check (file exists, parses as JSON, object root; failure yields `ExitCode = 1` with the load error as `Output`, fail-closed; standard-checkpoint `REQUIRED_STATE_KEYS` NOT applied; model-routing gate NOT applied); any other value -> fail closed with an error naming the unsupported type. The dispatch implementation's comments record PD-3: this is defined fail-closed behavior in a region where Python parity is undefined (argparse exit 2, zero checks run), a design decision and not a deferral. Retain the `[scriptblock] $Invoker` seam and the guarded `Import-Module` of `OrchestratorStateCompletion.psm1` (lines 206-208). `Test-HumanInteractionShape` (lines 68-150) is byte-unchanged and continues to run for all artifact types. All four verdict prefixes preserved. After this task the hook makes zero references to `Test-PythonOrchestratorValidatorAvailable` (the probe itself is deleted in Phase 11).
  - Satisfies: AC-2, AC-3, AC-23 (comment leg), US-1 (hook 3 leg), US-3, US-2 (completion-gate leg).
- [x] [P10-T3] Update `tests/scripts/claude-hooks/validate-orchestrator-output.Tests.ps1` (currently 486 lines; must stay under 500 — this edit removes more than it adds): remove the probe mock at `:468`; replace the source-text assertion at `:476-484` (which pins the literal `python -m scripts\.dev_tools\.validate_orchestration_artifacts` string) with its inverse — a negative-invocation AST assertion that the default invoker contains no `python`/`python3`/`py`/`poetry` `CommandAst`.
  - Satisfies: AC-11 (negative-AST leg), AC-12 (item 2).
- [x] [P10-T4] Update `tests/scripts/claude-hooks/validate-orchestrator-output.model-routing.Tests.ps1`: delete the probe mocks at `:127` and `:143` (a Pester `Mock` of a removed command fails at mock-registration time, so they must not survive Phase 11) and simplify the import-order `BeforeAll` at `:97-109`; all verdict assertions unchanged.
  - Satisfies: AC-12 (item 3).
- [x] [P10-T5] Create `tests/scripts/claude-hooks/validate-orchestrator-output.artifact-type-dispatch.Tests.ps1` (new file, under 500 lines — the existing suite is at 486 lines and cannot absorb this): dispatch tests for all three wired `$ArtifactType` values plus one unsupported value; the D-1 regression assertion — a fixture epic or parallel checkpoint that lacks the standard-checkpoint keys but is well-formed JSON with an object root passes the epic/parallel dispatch leg (no false `ROUTING_CONTRACT_BLOCKED`), and a missing or JSON-invalid checkpoint of those types is denied; and the AC-25 both-layers test — for a checkpoint carrying `human_interaction`, both the hook-internal `Test-HumanInteractionShape` and the library U6.H checks demonstrably execute (strictness is additive). In-memory/mocked content seams only; no temporary files.
  - Satisfies: AC-3 (dispatch-test leg), AC-4, AC-25 (both-layers-test leg), US-3.
- [x] [P10-T6] Verify the completion hook immediately: run the four suites `validate-orchestrator-output.Tests.ps1`, `validate-orchestrator-output.model-routing.Tests.ps1`, `validate-orchestrator-output.human-interaction.Tests.ps1`, and `validate-orchestrator-output.artifact-type-dispatch.Tests.ps1` (narrowed `scan_folders` or `Invoke-Pester -Path`; do not run the guard suite); all green. Direct-invocation sanity check once per wired `-ArtifactType`: invoke `pwsh -NoProfile -File .claude/hooks/validate-orchestrator-output.ps1` three times with a `CLAUDE_HOOK_INPUT` payload — default (`orchestrator-state`), `-ArtifactType epic-orchestrator-state`, and `-ArtifactType parallel-orchestrator-state` — confirming each process exits without a `CommandNotFoundException` and returns the expected fail-closed or pass verdict for the on-disk checkpoint state with prefixes intact; record all three invocations and exit codes. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/completion-hook-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: all four suites green and all three direct invocations recorded before Phase 11 begins. Self-gating invariant (AC-29), stated verbatim here and quoted verbatim in the evidence artifact this task writes: **The reconciliation branch corrects the checkpoint, never the check.** If a newly enforced check fails against real checkpoint state, that is the gate working correctly, not a defect in the check. The permitted response is to correct `artifacts/orchestration/orchestrator-state.json` and record the failing check IDs in the evidence artifact before the phase closes. Adjusting a check to accommodate the checkpoint, weakening a row to make this run pass, or relaxing a threshold is PROHIBITED. If the failure cannot be resolved by correcting the checkpoint, execution halts and reports it as a finding — the executor must not continue past this phase. Satisfies: AC-17 (per-site leg), AC-29 (Phase 10 reconciliation and halt legs), US-2, US-3, US-12.

### Phase 11 — OrchestratorState.psm1: Preflight Collapse and Probe Deletion

- [x] [P11-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`.
- [x] [P11-T2] Modify `.claude/lib/orchestrator-state/OrchestratorState.psm1` (currently 497 lines; the deletions keep it under 500): (a) collapse the default `$Invoker` body of `Invoke-OrchestratorStatePreflight` (lines 443-465) to the portable validation path — the ported unconditional-block checks via `Get-OrchestratorStateUnconditionalError` (guarded lazy `Import-Module` of `OrchestratorStateUnconditional.psm1`, house pattern of `validate-orchestrator-output.ps1:206-208`, avoiding load-order circularity) followed by `Test-OrchestratorStatePrCreationReadiness -CheckpointPath $Path` — matching the Python plain-call-plus-`--require-pr-creation-ready` surface with no Python invocation; (b) DELETE `Test-PythonOrchestratorValidatorAvailable` (lines 346-372, containing the `& python -c 'import ...'` probe at `:367`) and remove its export (line 496) — both former callers are now portable (the hook stopped calling it in Phase 10), so nothing references the deleted command at this phase boundary; (c) retain the `[scriptblock] $Invoker` parameter, the `{ExitCode, Output}` -> `{HasErrors, ErrorText}` translation, and the strict-mode property guard (lines 467-481); (d) preserve the fail-closed direction for missing/invalid checkpoints and the `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` prefix. Note: this removes currently-covered lines, so this file's coverage may tick down slightly; the floors still apply but no increase is planned. Also remove or rewrite the remaining prose references to the deleted symbol in the same file so no reference survives: the module doc-comment at `:26`, the comment at `:487`, and the comment at `:425` describing the `python -m` default invoker. These are comments and produce no `CommandAst`, so the guard is unaffected; the requirement comes from P15-T9 clause (e), which asserts the name is referenced nowhere in the tree.
  - Acceptance: no reference to the deleted symbol survives in `OrchestratorState.psm1` (definition, export, or prose), and a repository-wide search for `Test-PythonOrchestratorValidatorAvailable` returns no match in production or test sources. Satisfies: AC-1 (both legs — preflight collapse and probe deletion), US-1 (site 4 leg), US-2 (preflight leg).
- [x] [P11-T3] Update `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, context `capability detection (portable-path routing)` at lines 279-329: remove all probe mocks (`:295`, `:309`, `:321` — mocks of a deleted command fail at registration time); delete or rewrite the `$true`-branch test that asserted Python-CLI selection; convert the `$false`-branch tests to default-behavior tests asserting the portable path (unconditional block + readiness) is the default; the four direct seam-injection tests at lines 250-273 pass unmodified; add a default-path test asserting a checkpoint with a U-family violation (for example a malformed `delegation_receipts` entry) now fails the preflight verdict.
  - Satisfies: AC-1 (seam-tests leg), AC-12 (item 1).
- [x] [P11-T4] Update the comment at `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` lines 72-75 (it describes the preflight default invoker deferring to a Python validator subprocess, which is no longer true); leave every verdict assertion unchanged.
  - Satisfies: AC-12 (item 4).
- [x] [P11-T5] Verify the preflight change immediately: run the Pester suites `OrchestratorState.Tests.ps1`, `OrchestratorStateCompletion.Tests.ps1`, `OrchestratorStateUnconditional.Tests.ps1`, `enforce-pr-author-skill.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.Tests.ps1`, and `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (narrowed; do not run the guard suite); all green, confirming the PR-author preflight returns the same fail-closed verdicts (missing, unreadable, structurally invalid checkpoints) with the `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` prefix unchanged. Direct-invocation sanity check via the consumer hook: invoke `pwsh -NoProfile -File .claude/hooks/enforce-pr-author-skill.ps1` once with a `CLAUDE_HOOK_INPUT` payload that reaches the preflight path, confirming the process exits without a `CommandNotFoundException` and returns the expected verdict with its prefix intact; record the invocation and exit code. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/preflight-orchestratorstate-verify.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: all listed suites green and the direct invocation recorded before Phase 12 begins. Self-gating invariant (AC-29), stated verbatim here and quoted verbatim in the evidence artifact this task writes: **The reconciliation branch corrects the checkpoint, never the check.** If a newly enforced check fails against real checkpoint state, that is the gate working correctly, not a defect in the check. The permitted response is to correct `artifacts/orchestration/orchestrator-state.json` and record the failing check IDs in the evidence artifact before the phase closes. Adjusting a check to accommodate the checkpoint, weakening a row to make this run pass, or relaxing a threshold is PROHIBITED. If the failure cannot be resolved by correcting the checkpoint, execution halts and reports it as a finding — the executor must not continue past this phase. Satisfies: AC-17 (per-site leg), AC-29 (Phase 11 reconciliation and halt legs), US-2 (preflight leg), US-12.

### Phase 12 — Bundle Mirror and Manifest Registration

- [x] [P12-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. No approved cap override is used; each of this phase's internal batches (the six mirror-copy batches, the manifest registration, and the manifest-test batch, per the Change-Budget Accounting) remains within the 3-production / 3-test per-batch cap on its own, and this same deletion is repeated at each internal batch boundary within the phase.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit` and before each subsequent internal batch's first `Write`/`Edit`.
- [x] [P12-T2] Mirror batch 1 (byte-identical copies, at most 3 files): copy `.claude/hooks/enforce-discovery-artifact-gate.ps1`, `.claude/hooks/validate-discovery-artifact-gate.ps1`, and `.claude/hooks/validate-orchestrator-output.ps1` to their paths under `extensions/drm-copilot/resources/claude-customizations/.claude/`.
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T3] Mirror batch 2 (at most 3 files): copy `.claude/lib/orchestrator-state/OrchestratorState.psm1`, `.claude/lib/orchestrator-state/OrchestratorStateCompletion.psm1`, and `.claude/lib/discovery-validation/DiscoveryValidation.psm1` to their bundle paths.
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T4] Mirror batch 3 (at most 3 files): copy `.claude/lib/orchestrator-state/OrchestratorStateReceipts.psm1`, `OrchestratorStateModelReceipts.psm1`, and `OrchestratorStateUnconditional.psm1` to their bundle paths.
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T5] Mirror batch 4 (at most 3 files): copy `.claude/lib/orchestrator-state/OrchestratorStateCodexModelReceipts.psm1`, `OrchestratorStateCodexTopologyReceipts.psm1`, and `OrchestratorStateRoutingMatrix.psm1` to their bundle paths.
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T6] Mirror batch 5 (at most 3 files): copy `.claude/lib/orchestrator-state/OrchestratorStateCompletionChecks.psm1`, `OrchestratorStateRoutingContract.psm1`, and `.claude/lib/codex-routing/CodexDeployment.psm1` to their bundle paths.
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T7] Mirror batch 6 (1 file): copy `.claude/lib/codex-routing/CodexTopology.psm1` to its bundle path. (Execution note: this batch also mirrored `.claude/lib/orchestrator-state/OrchestratorStateCheckpointValue.psm1`, the sibling helper created under the Module Decomposition pre-authorized split clause, as required by the Hard Constraint that every changed `.claude/**` file be mirrored byte-identically. Batch size 2, within the 3-file cap.)
  - Satisfies: AC-13 (mirror leg), US-7.
- [x] [P12-T8] Register all eleven new `.claude/lib/**` module paths in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, following the existing `.claude/lib` entries at lines 107-114. (Execution note: twelve paths registered — the eleven named modules plus `OrchestratorStateCheckpointValue.psm1`, the pre-authorized sibling-split helper, per the Hard Constraint that every new `.claude/lib/**` module be registered in `core.json`.)
  - Satisfies: AC-13 (manifest leg), US-7.
- [x] [P12-T9] Manifest-pinning tests (one batch, 3 test files, house pattern of `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Manifest.Tests.ps1`): extend `OrchestratorState.Manifest.Tests.ps1` to pin the seven new orchestrator-state modules; create `tests/scripts/claude-lib/discovery-validation/DiscoveryValidation.Manifest.Tests.ps1`; create `tests/scripts/claude-lib/codex-routing/CodexRouting.Manifest.Tests.ps1` pinning both codex-routing modules; each asserts presence in `core.json` and byte-identical bundle mirroring; run all three green (narrowed).
  - Satisfies: AC-16 (manifest-test leg), US-7.
- [x] [P12-T10] Run the Python contract suites that pin the bundle and wiring: `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py`; both must pass (byte-identity of every changed `.claude/**` file; `.claude/settings.json` command fragments unchanged). Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/bundle-mirror-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: both contract surfaces pass. Satisfies: AC-13 (verification leg), AC-15 (wiring leg), US-7.

### Phase 13 — Parity-Coverage Artifact and Potential Entry

- [x] [P13-T1] Produce the row-by-row parity-coverage artifact: `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/other/parity-coverage.<TS>.md` mapping EVERY one of the 85 inventory rows (U family 57, C family 25, M family 3) to (a) its implementing PowerShell module and function and (b) the test (file and `It`/fixture name) that asserts its exact error string or documented divergence. Mark the 6 pre-existing check rows (`U1.1`, `U1.2` with the documented message divergence, `U2.1`, `U3.1`, `U4.1`, `M1.1`) plus the two pre-existing formulas (`Get-ComplexityFloor`, `Resolve-DelegationModel`) that back `U6.C5`/`U6.M4` without check wiring, and the 79 ported rows; where this recount differs from the research prose total of "7 of 85", the row-by-row artifact is authoritative and the discrepancy is recorded on the artifact — it is not an unmapped row and not a deferral; record the documented divergences (U1 load-error messages, PD-1, PD-2, PD-3) inline on their rows; confirm zero unmapped rows. Include `Timestamp:`, `Command:` (the inventory source path and the mapping method), `EXIT_CODE:`, `Output Summary:` (85/85 mapped; 0 deferred).
  - Acceptance: all 85 rows mapped to implementation and test; zero rows deferred or unmapped; if any row cannot be mapped, the plan outcome is remediation-required, not PASS. Satisfies: AC-19 (measurement leg), US-6.
- [x] [P13-T2] Create the rescoped potential entry under `docs/features/potential/` (AC-14): it records (a) defect D-2's avoided-not-redesigned disposition and (b) the PowerShell 7.4+ `Test-Json -SchemaFile` constraint for destination repositories on 7.0-7.3, and states explicitly that NO completion-validator check family is deferred. No artifact of this feature may describe the epic/parallel structural check as a deferral (PD-3 is defined behavior in an undefined-parity region).
  - Acceptance: the potential entry exists with both recorded items and the explicit no-deferral statement. Satisfies: AC-14, US-8 (potential-entry leg), AC-23 (no-deferral-language leg).

### Phase 14 — Guard Repository-Scan Verification (zero findings, empty allowlist)

- [x] [P14-T1] Run the guard's two repository-scan `It`s from `tests/scripts/claude-runtime/enforcement-hooks-no-python-invocation.Tests.ps1` and confirm both now PASS: Assertion A reports ZERO findings over the guarded tree (empty allowlist; all five pre-change invocation sites removed; zero findings in the six incidental hooks — `check-python-test-purity.ps1`, `enforce-evidence-locations.ps1`, `enforce-orchestration-preimplementation-gate.ps1`, `enforce-python-batch-budget.ps1`, `validate-executor-output.ps1`, `validate-feature-review-coverage.ps1` — and zero findings for the two dot-source helper-load sites); Assertion B passes over the empty allowlist. Verify via `git status`/`git diff` that the six incidental hooks are byte-unchanged. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/regression-testing/guard-scan-zero-findings.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (0), `Output Summary:` (zero findings; allowlist empty; six incidental hooks unmodified).
  - Acceptance: both scan `It`s pass with zero findings; artifact exists with all four fields. Satisfies: AC-10 (repository-scan leg), AC-8 (zero-allowlist leg), US-1 (structural-verification leg), US-5 (zero-findings leg).
- [x] [P14-T2] Verify zero findings under the bundled mirror tree as well (it is outside the scan roots by design): dot-source `tests/scripts/claude-runtime/EnforcementHooksNoPythonInvocation.Helpers.ps1` in a one-off `pwsh -NoProfile` command, enumerate `extensions/drm-copilot/resources/claude-customizations/.claude/hooks` and `.../.claude/lib` (`*.ps1`, `*.psm1`, excluding `lib/bash`), run the same AST detection helper over that set, and confirm zero findings. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/regression-testing/guard-scan-mirror-tree.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (zero findings in the mirror tree).
  - Acceptance: zero findings over the mirror tree; artifact exists with all four fields. Satisfies: AC-10 (mirror-tree leg), US-7 (identical-behavior leg).

### Phase 15 — Final QA Loop, Coverage Delta, and AC Reconciliation

- [x] [P15-T1] PowerShell QA step 1 — formatting: run `mcp__drm-copilot__run_poshqc_format`; if any file changes, restart the PowerShell loop from this step. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-poshqc-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Satisfies: AC-17 (final-loop leg).
- [x] [P15-T2] PowerShell QA step 2 — linting: run `mcp__drm-copilot__run_poshqc_analyze`; zero errors required; restart the loop from P15-T1 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-poshqc-analyze.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Satisfies: AC-17 (final-loop leg).
- [x] [P15-T3] PowerShell QA step 3 — tests with coverage: run `mcp__drm-copilot__run_poshqc_test` with `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` over the FULL suite (first full-suite run of this plan; includes the guard's repository-scan `It`s, now green with zero findings); one clean pass required; restart the loop from P15-T1 on failure. Read numeric post-change line and branch coverage out of `artifacts/pester/powershell-coverage.xml` for the suite AND for each modified/new coverage target: `OrchestratorState.psm1`, `OrchestratorStateCompletion.psm1`, `enforce-discovery-artifact-gate.ps1`, `validate-discovery-artifact-gate.ps1`, `validate-orchestrator-output.ps1`, and the eleven new modules. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-poshqc-test.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including all numeric per-file values (a zero exit code is not coverage evidence). `SKIPPED` is not a permitted outcome.
  - Satisfies: AC-16 (floor leg), AC-17 (final-loop leg), US-5 (CI-suite leg).
- [x] [P15-T4] Python QA step 1 — formatting: run `poetry run black .`; if any file changes, restart the Python loop from this step. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Satisfies: full-QA obligation for the Python surface touched by the bundle mirror.
- [x] [P15-T5] Python QA step 2 — linting: run `poetry run ruff check .`; zero errors required; restart the Python loop from P15-T4 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Satisfies: full-QA obligation for the Python surface.
- [x] [P15-T6] Python QA step 3 — type checking: run `poetry run pyright`; zero errors required; restart the Python loop from P15-T4 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Satisfies: full-QA obligation for the Python surface.
- [x] [P15-T7] Python QA step 4 — tests with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`; full suite green in one pass; restart the Python loop from P15-T4 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/final-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change line-coverage and branch-coverage percentages. `SKIPPED` is not a permitted outcome.
  - Satisfies: full-QA obligation for the Python surface; coverage evidence contract.
- [x] [P15-T8] Coverage delta verification: compare the Phase 0 baseline artifacts against the Phase 15 final artifacts for both languages and record baseline coverage, post-change coverage, and changed/new-file coverage (the sixteen PowerShell coverage targets named in P15-T3), all values read from the coverage XML/reports, not inferred from exit codes. Confirm no regression on changed lines and that the 85% line / 75% branch floors hold for every modified and new file (note: `OrchestratorState.psm1` may show a small decrease from the probe deletion — floors still apply, no increase required); if any required numeric value is unavailable or any floor fails, the plan outcome is remediation-required, not PASS. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/coverage-delta.<TS>.md` with `Timestamp:`, `Command:` (comparison inputs), `EXIT_CODE:`, `Output Summary:` (baseline vs post-change vs changed-code numbers).
  - Satisfies: AC-16 (delta leg), coverage evidence contract.
- [x] [P15-T9] Cross-cutting constraint sweep: verify by inspection of the change diff that (a) no test in the change mutates `$env:PATH`, saves/restores PATH, probes a live `python`, or defines a shadow `python` function (SD-3); (b) no production or test file in the change exceeds 500 lines; (c) no hook logic moved into `.claude/lib/bash/`; (d) all four verdict prefixes (`DISCOVERY_ARTIFACT_GATE_BLOCKED:`, `ROUTING_CONTRACT_BLOCKED:`, `MODEL_ROUTING_BLOCKED:`, `ORCHESTRATOR_STATE_PREFLIGHT_FAILED`) appear unchanged in the modified files and `.claude/settings.json` is untouched; (e) `Test-PythonOrchestratorValidatorAvailable` is absent from `OrchestratorState.psm1` (definition and export) and referenced nowhere in the tree; (f) the six incidental hooks are byte-unchanged; (g) no artifact of this feature describes the epic/parallel structural check or any check family as deferred, and the spec's "Decision Record — Language Selection" section is preserved intact (AC-23/AC-24); (h) all evidence artifacts reside under `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/`; (i) `scripts/dev_tools/*.py` and the MCP TypeScript validator surface are unmodified. Record the sweep in `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/other/final-constraint-sweep.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Satisfies: AC-11 (no-PATH leg), AC-15 (prefix leg), AC-23 (no-deferral-language leg), AC-24 (decision-record-preservation leg), US-10.
- [x] [P15-T10] Self-gating audit (AC-29 audit leg): verify by diff-based assertion that no check row, error-string template, threshold, or validator module was modified during Phases 10-15 to accommodate the live checkpoint. Method: using the in-flight `Gate Hashes:` baselines recorded by the phase-verification evidence artifacts and the pre-run HEAD `b1a86fd3` as the git baseline, assert that (a) all twelve files (the eleven new parity modules and the extended `OrchestratorStateCompletion.psm1`) are byte-identical between their green verification gates (P2-T8, P4-T6, P5-T6, P6-T6, P7-T6, P8-T4, P9-T6) and the final tree, proven by recomputing `Get-FileHash -Algorithm SHA256` over each file in the final tree and comparing it against the `Gate Hashes:` block recorded in that phase's verification evidence artifact — any mismatch is an accommodation change, since no planned task after those gates edits these repo-root files (the Phase 12 mirror copies create bundle copies only) — a hash delta attributable to the P15-T1 formatting pass, as named in that task's evidence artifact, is a formatting normalization and not an accommodation change, and must be recorded as such, with the attribution shown (the P15-T1 changed-file list naming the module) alongside the differing hashes; a hash delta with no such attribution remains an accommodation change and triggers the halt path; (b) the Phase 10-11 edits to `.claude/hooks/validate-orchestrator-output.ps1` and `.claude/lib/orchestrator-state/OrchestratorState.psm1` are exactly the planned changes of P10-T2 and P11-T2, with no check removed, no error string weakened, and no threshold relaxed relative to the 85-row inventory mapping recorded in the P13-T1 parity-coverage artifact; and (c) any reconciliation performed under P10-T6/P11-T5 modified only `artifacts/orchestration/orchestrator-state.json`, with the failing check IDs recorded in those tasks' evidence artifacts. Clauses (b) and (c) use the pre-run HEAD `b1a86fd3` as the single git baseline (`git diff b1a86fd3 -- <paths>`); this run creates no intermediate commits, so `git log -p` carries no in-run history and must not be relied on. The evidence artifact quotes the invariant verbatim: **The reconciliation branch corrects the checkpoint, never the check.** Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/self-gating-audit.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
  - Acceptance: zero unplanned modifications to any parity module, check row, or threshold across Phases 10-15; any reconciliation touched only the checkpoint and its failing check IDs are recorded; artifact exists with all four fields and the verbatim invariant. If any accommodation change is found, the plan outcome is remediation-required, not PASS. Satisfies: AC-29 (audit leg), US-12 (audit leg).
- [x] [P15-T11] AC reconciliation: in `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/spec.md` and `user-story.md`, check off EVERY criterion AC-1..AC-29 and US-1..US-12, each with a concrete evidence reference (test path or evidence artifact path) per the AC and US Coverage Matrix below. Check spec AC-19's already-present/ported phrasing off against the recount recorded on the parity-coverage artifact (6 pre-existing check rows plus the two pre-existing formulas backing `U6.C5`/`U6.M4`, and 79 ported rows) rather than the research prose total of "7 already-present / 78 ported" — the row-by-row artifact is authoritative, and the recorded discrepancy is not a deferral. On the AC-8 evidence pointer, record the shape deviation inline: the spec describes one repository-scan `It` and the shipped guard carries two (Assertion A no-violations, Assertion B no-allowlist-rot), a deliberate strengthening per this plan's AC-8 matrix row — the check-off must not be a silent mismatch. Nothing is deferred in this run — if any criterion cannot be checked with evidence, STOP and report the run remediation-required naming the unmet items; do not mark anything deferred and do not delete or reword any AC row or AC text. Per the recorded stop condition, do not create a pull request and do not delegate to pr-author.
  - Acceptance: every AC-1..AC-29 and US-1..US-12 is checked with a concrete evidence pointer, or the run is reported remediation-required naming the unmet items; no criterion is silently dropped or marked deferred.

### Phase 16 — Discovery-Hook Coverage Remediation

Remediation context: P15-T8 failed its own acceptance because the two modified discovery hooks fell below the 85% line-coverage floor after Phase 3 (`enforce-discovery-artifact-gate.ps1` 87.27% baseline, 48/55 -> 82.76% post-change, 48/58; `validate-discovery-artifact-gate.ps1` 87.93% baseline, 51/58 -> 83.61% post-change, 51/61). Covered-line counts are unchanged; each file gained exactly 3 uncovered lines — the replacement `Invoke-DiscoveryValidatorExe` body (`enforce-discovery-artifact-gate.ps1:64-71`, `validate-discovery-artifact-gate.ps1:67-74`: the `Join-Path` module-path resolution, the `Test-Path` guard with its module-not-found early return, and the `Import-Module`/delegation path), which is never executed in tests because that function is the universally mocked seam. The remedy is ADDITIVE TESTS ONLY: new tests invoke the real, unmocked seam so its body executes. Prohibited in this phase: relaxing the 85% floor; editing `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` to drop either hook from the coverage-target list; modifying any existing test, mock registration, or `Should -Invoke` assertion; weakening or deleting any check, error-string template, or threshold; adding a Python invocation of any kind; any temporary file.

- [x] [P16-T1] Reset the PowerShell per-batch budget before this phase's writes: delete `.claude/state/powershell-batch-budget.$env:CLAUDE_SESSION_ID.json` (and the `powershell-batch-budget.default.json` sibling when `CLAUDE_SESSION_ID` is unset), per the reset path named by `.claude/hooks/enforce-powershell-batch-budget.ps1:137`. This post-Phase-15 reset is the approved override recorded as `local_execution_overrides[LEO-1]`; no cap override is used — this phase remains within the 3-production / 3-test per-batch cap on its own.
  - Acceptance: the state file is absent before the phase's first `Write`/`Edit`. Satisfies: AC-16 (remediation-enablement leg).
- [x] [P16-T2] Create `tests/scripts/claude-hooks/enforce-discovery-artifact-gate.ValidatorDispatch.Tests.ps1` (NEW sibling test file per the authorized `<Module>.<Family>.Tests.ps1` split shape; under 500 lines) exercising the REAL, unmocked `Invoke-DiscoveryValidatorExe` in `.claude/hooks/enforce-discovery-artifact-gate.ps1` so its replacement body (lines 64-71) actually executes. `Mock Invoke-DiscoveryValidatorExe` MUST NOT appear anywhere in this file. Minimum `It`s: (1) module-not-found early return — drive the `Test-Path` guard false with a Context-scoped `Mock Test-Path { $false }` carrying a `-ParameterFilter` that matches only the `DiscoveryValidation.psm1` literal path (a filesystem-boundary cmdlet mock; the repository's mocking prohibition targets external executables, and no temporary file is involved); assert `ExitCode = 1` and `Output` matching the template `Discovery-validation module not found: <path>`. Only if this mechanism proves unreliable is the recorded fallback authorized: a minimal injectable module-path seam on the hook (an optional parameter whose safe default is the current `Join-Path` expression, per the `.claude/rules/powershell.md` design-seam guidance) — a production change that activates P16-T5's mirror branch; a new production parameter is NOT added if the mock mechanism suffices. (2) success path (D-2 contract) — the real dispatch executes resolution, guard, and import; assert `ExitCode = 0` with EMPTY `Output`; permitted mechanisms: a committed repository artifact that validates cleanly, or a Context-scoped mock of the module-exported `Invoke-DiscoveryArtifactValidation` returning `@{ ExitCode = 0; Output = '' }` (the hook seam itself remains unmocked and fully executed). (3) failure path — real invocation with a deterministic failing input (for example a nonexistent artifact path, or a committed read-only `schemas/discovery/v1/*.json` file passed under a mismatching artifact-type token); assert a non-zero `ExitCode` and non-empty `Output`. No temporary files, no `$env:PATH` mutation. `tests/scripts/claude-hooks/enforce-discovery-artifact-gate.Tests.ps1` is byte-unchanged: its 9 `Mock Invoke-DiscoveryValidatorExe` registrations (lines 26, 38, 51, 67, 87, 96, 107, 118, 137) and its `Should -Invoke` assertions are not modified, and the new file lives in a separate Pester container so no mock in either file can shadow the other.
  - Acceptance: the new file exists with at least the three `It`s above and zero occurrences of `Mock Invoke-DiscoveryValidatorExe`; the existing suite file is byte-identical to its pre-phase state. Satisfies: AC-16 (hook 1 coverage leg), AC-5 (seam-survival leg).
- [x] [P16-T3] Create `tests/scripts/claude-hooks/validate-discovery-artifact-gate.ValidatorDispatch.Tests.ps1` (NEW sibling test file, same shape and constraints as P16-T2; under 500 lines) exercising the REAL, unmocked `Invoke-DiscoveryValidatorExe` in `.claude/hooks/validate-discovery-artifact-gate.ps1` (replacement body at lines 67-74) with the same three minimum `It`s: module-not-found early return (`ExitCode = 1`, `Output` matching `Discovery-validation module not found: <path>`), success path (`ExitCode = 0`, EMPTY `Output` — the D-2 contract), and failure path (non-zero `ExitCode`, non-empty `Output`), using the same mechanisms and the same fallback-seam authorization as P16-T2. `Mock Invoke-DiscoveryValidatorExe` MUST NOT appear anywhere in this file. No temporary files, no `$env:PATH` mutation. `tests/scripts/claude-hooks/validate-discovery-artifact-gate.Tests.ps1` is byte-unchanged: its 6 `Mock Invoke-DiscoveryValidatorExe` registrations (lines 25, 38, 51, 63, 102, 120) and its `Should -Invoke` assertions are not modified.
  - Acceptance: the new file exists with at least the three `It`s above and zero occurrences of `Mock Invoke-DiscoveryValidatorExe`; the existing suite file is byte-identical to its pre-phase state. Satisfies: AC-16 (hook 2 coverage leg), AC-5 (seam-survival leg).
- [x] [P16-T4] Verify the remediation with a targeted run: `mcp__drm-copilot__run_poshqc_test` with `scan_folders` narrowed to `tests/scripts/claude-hooks` (this executes both new sibling suites AND every existing hook suite, proving all 26 existing `Invoke-DiscoveryValidatorExe` references — 15 `Mock` registrations plus 11 `Should -Invoke` assertions — still pass alongside the real-dispatch tests); all suites green. Read numeric line and branch coverage for `.claude/hooks/enforce-discovery-artifact-gate.ps1` and `.claude/hooks/validate-discovery-artifact-gate.ps1` out of `artifacts/pester/powershell-coverage.xml` (`CoveragePercentTarget = 0`; a zero exit code is not coverage evidence) and confirm both hooks are at or above 85% line and 75% branch coverage. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/discovery-hook-coverage-remediation.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (both hooks' numeric values against the failing 82.76%/83.61% state). The artifact MUST additionally carry a `Gate Hashes:` block so P15-T10's audit remains satisfiable: if this phase modified any production file (the fallback seam), one `<repo-relative-path> = <sha256>` line per modified production file plus an attribution note recording the Phase 16 task ID and reason for the hash delta — recorded and attributed exactly as the P15-T1 formatting carve-out is attributed in P15-T10 — so the audit remains truthful; if the phase touched NO production file, the block states exactly `Gate Hashes: no production file modified in Phase 16`.
  - Acceptance: hook-folder run green; both hooks' numeric line coverage >= 85% and branch coverage >= 75%, read from the coverage XML; artifact exists with all four fields and the `Gate Hashes:` block (hashes with attribution, or the explicit no-production-change statement). Satisfies: AC-16 (floor leg), AC-17 (per-site leg).
- [x] [P16-T5] Conditional bundle mirror (explicit skip branch authorized by this task text): IF P16-T2/P16-T3 exercised the fallback path seam and modified a production `.claude/**` file, first repeat the P16-T1 budget-state deletion at this internal batch boundary, then copy each modified production file byte-identically to its path under `extensions/drm-copilot/resources/claude-customizations/.claude/` (at most 2 copies; `tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py` requires byte identity). IF no production `.claude/**` file was modified, make no copy. Either way write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/other/phase16-mirror-disposition.<TS>.md` with `Timestamp:`, `Command:` (the copy commands, or `none`), `EXIT_CODE:`, `Output Summary:` stating either the mirrored file list or `NO_PRODUCTION_CHANGE — mirror not required`. Test files under `tests/**` are outside the bundle-parity scope of `test_push_down_claude_resource_contracts.py` and are never mirrored.
  - Acceptance: the disposition artifact exists with all four fields, and every production `.claude/**` file modified in this phase (zero or two) has a byte-identical bundle copy. Satisfies: AC-13 (conditional mirror leg).
- [x] [P16-T6] PowerShell QA step 1 — formatting (the full per-language QA loop re-runs because Phase 16 changes files after Phase 15's loop completed): run `mcp__drm-copilot__run_poshqc_format`; if any file changes, restart the PowerShell loop from this step (and if a mirrored production file changed, re-run the P16-T5 copy before restarting). Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-poshqc-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Acceptance: artifact exists with all four fields; clean pass recorded. Satisfies: AC-17 (final-loop leg).
- [x] [P16-T7] PowerShell QA step 2 — linting: run `mcp__drm-copilot__run_poshqc_analyze`; zero errors required; restart the PowerShell loop from P16-T6 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-poshqc-analyze.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Acceptance: artifact exists with all four fields; zero errors recorded. Satisfies: AC-17 (final-loop leg).
- [x] [P16-T8] PowerShell QA step 3 — tests with coverage: run `mcp__drm-copilot__run_poshqc_test` with `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` over the FULL suite (includes the guard's repository-scan `It`s and all 26 seam references); one clean pass required; restart the loop from P16-T6 on failure. Read numeric post-change line and branch coverage out of `artifacts/pester/powershell-coverage.xml` for the suite, for both discovery hooks, and for every coverage target named in P15-T3; confirm every file meets the 85%/75% floors and that no file's coverage is below the value recorded in P15-T3's final artifact (no regression on any other file). Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-poshqc-test.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including all numeric per-file values (a zero exit code is not coverage evidence). `SKIPPED` is not a permitted outcome.
  - Acceptance: full suite green in one clean pass; both hooks and every P15-T3 coverage target at or above the floors with no per-file regression versus P15-T3; artifact exists with all four fields and numeric per-file values. Satisfies: AC-16 (floor leg), AC-17 (final-loop leg).
- [x] [P16-T9] Python QA step 1 — formatting: run `poetry run black .`; if any file changes, restart the Python loop from this step. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-black.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Acceptance: artifact exists with all four fields; clean pass recorded. Satisfies: full-QA obligation for the Python surface.
- [x] [P16-T10] Python QA step 2 — linting: run `poetry run ruff check .`; zero errors required; restart the Python loop from P16-T9 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-ruff.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Acceptance: artifact exists with all four fields; zero errors recorded. Satisfies: full-QA obligation for the Python surface.
- [x] [P16-T11] Python QA step 3 — type checking: run `poetry run pyright`; zero errors required; restart the Python loop from P16-T9 on failure. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-pyright.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `SKIPPED` is not a permitted outcome.
  - Acceptance: artifact exists with all four fields; zero errors recorded. Satisfies: full-QA obligation for the Python surface.
- [x] [P16-T12] Python QA step 4 — tests with coverage: run `poetry run pytest --cov --cov-branch --cov-report=term-missing`; full suite green in one pass; restart the Python loop from P16-T9 on failure. This run re-proves bundle byte-identity through `test_push_down_claude_resource_contracts.py`, which fails if the P16-T5 mirror branch was required but not performed. Write `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/phase16-final-pytest.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change line-coverage and branch-coverage percentages. `SKIPPED` is not a permitted outcome.
  - Acceptance: full suite green including the bundle-parity contract test; artifact exists with all four fields and numeric coverage values. Satisfies: full-QA obligation for the Python surface; coverage evidence contract.
- [x] [P16-T13] Coverage-delta reconciliation and check-off: produce `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/evidence/qa-gates/coverage-delta.<TS>.md` satisfying P15-T8's full requirements — baseline coverage, post-change coverage, and changed/new-file coverage for both languages, covering all sixteen PowerShell coverage targets named in P15-T3 (including both discovery hooks), every value read from the coverage XML/reports, not inferred from exit codes — with `Timestamp:`, `Command:` (comparison inputs), `EXIT_CODE:`, `Output Summary:` (baseline vs post-change vs changed-code numbers, explicitly recording both hooks' recovery from 82.76%/83.61% to at-or-above 85%). THEN, only when both hooks meet the 85% line / 75% branch floors and no other file regressed: mark `[P15-T8]` as `[x]` in this plan file, and check off spec `AC-16` in `docs/features/active/2026-08-15-enforcement-hooks-must-not-invoke-python-475/spec.md` with a concrete evidence pointer to the delta artifact. If any required numeric value is unavailable or any floor still fails, the plan outcome is remediation-required, not PASS, and neither checkbox is marked.
  - Acceptance: delta artifact complete with all numeric values; `[P15-T8]` and spec AC-16 are checked if and only if both hooks are at or above the floors with no regression on any other file. Satisfies: AC-16 (delta and check-off legs).

## AC and US Coverage Matrix

Every criterion is covered; there is no deferral disposition in this revision.

| Criterion | Covering tasks |
| --- | --- |
| AC-1 | P9-T2 (unconditional block), P11-T2, P11-T3, P11-T5 (probe deleted; portable preflight default; seam tests unmodified). |
| AC-2 | P10-T2, P10-T3 (no Python `CommandAst` in the default invoker; seam and guarded import retained). |
| AC-3 | P10-T2, P10-T5, P10-T6 (dispatch on all three wired types plus unsupported-type fail-closed). |
| AC-4 | P10-T5 (D-1 regression fixtures: well-formed epic/parallel passes; missing/invalid denied). |
| AC-5 | P3-T2, P3-T3, P3-T4, P3-T5 (26 seam references pass unmodified). |
| AC-6 | P2-T2, P2-T3, P3-T4. |
| AC-7 | P2-T2, P2-T3. |
| AC-8 | P1-T2, P1-T3, P14-T1 (zero allowlist entries). Recorded shape deviation: the spec describes ONE repository-scan `It`; this plan ships TWO (Assertion A no-violations and Assertion B no-allowlist-rot). This is a deliberate strengthening, not an unremarked mismatch — Assertion B exists so any future allowlist entry that goes stale fails the suite. P15-T11 records this deviation inline on the AC-8 evidence pointer at check-off. |
| AC-9 | P1-T2, P1-T3, P1-T4. |
| AC-10 | P1-T3, P1-T4, P1-T5 (non-detection and fail-before legs), P14-T1 (zero-findings repository scan; incidental hooks byte-unchanged), P14-T2 (mirror tree). |
| AC-11 | P10-T3 (negative-invocation AST assertion replacing `:476-484`), P15-T9 (no PATH mutation anywhere in the change). |
| AC-12 | P11-T3 (item 1), P10-T3 (item 2), P10-T4 (item 3), P11-T4 (item 4). |
| AC-13 | P12-T2..P12-T8, P12-T10. |
| AC-14 | P13-T2 (rescoped potential entry; explicit no-deferral statement). |
| AC-15 | P12-T10, P15-T9. |
| AC-16 | P2-T7, P4-T6, P5-T6, P6-T6, P7-T6, P8-T4, P9-T6, P12-T9, P15-T3, P15-T8, P16-T1..P16-T4, P16-T8, P16-T13 (Phase 16 discovery-hook coverage remediation: additive real-dispatch tests restore `enforce-discovery-artifact-gate.ps1` and `validate-discovery-artifact-gate.ps1` to the 85%/75% floors, enabling the P15-T8 check-off). |
| AC-17 | P2-T8, P3-T5, P4-T6, P5-T6, P6-T6, P7-T6, P8-T4, P9-T6, P10-T6, P11-T5, P15-T1..P15-T3. |
| AC-18 | P2-T2. |
| AC-19 | P4-T2..P4-T5, P5-T2..P5-T5, P6-T2..P6-T5, P7-T4, P7-T5, P8-T2, P8-T3, P9-T2..P9-T5 (the 79 ported rows with exact-string fixtures), P13-T1 (row-by-row measurement artifact, 85/85; 6 pre-existing check rows plus the two pre-existing formulas backing `U6.C5`/`U6.M4`), P15-T11 (check-off against the recount, not the research prose total). |
| AC-20 | P5-T2..P5-T5 (resolver ports and parity tests), P6-T2..P6-T5 (single-resolver consumption). |
| AC-21 | P7-T2, P7-T3 (pinned constants, config-parity test, no validation-time disk read, documented rationale), P7-T4, P8-T2 (consuming gates). |
| AC-22 | P9-T4, P9-T5 (single emission; M3 reuse of the same per-entry implementation). |
| AC-23 | P10-T2 (dispatch comments record PD-3 as defined behavior), P13-T2 and P15-T9 (no artifact uses deferral language). |
| AC-24 | P4-T3, P4-T5, P5-T3, P5-T5, P6-T3, P6-T5, P7-T3, P7-T5, P8-T3, P9-T3, P9-T5 (oracle-intent headers), P15-T9 (spec decision record preserved). |
| AC-25 | P4-T2 (five U6.H checks ported with inventory strings; additive reconciliation documented), P10-T2 (`Test-HumanInteractionShape` unchanged), P10-T5 (both-layers test). |
| AC-26 | P2-T4 (fail-closed runtime version check on PowerShell < 7.4 with all three required message elements: 7.4+, `Test-Json -SchemaFile` Draft 2020-12 reason, issue #475; no obscure failure, no silent degradation), P2-T6 (deterministic proof through the injectable seam), P2-T8 (suite green). |
| AC-27 | P2-T5 (comment-based help states the 7.4+ floor and its reason; the destination-visible surface — no pack README exists and `.claude/rules/**` is not modified). |
| AC-28 | P2-T6 (injectable version seam; no `$PSVersionTable` mutation; no reliance on ambient host state; below-floor and at/above-floor fixtures). |
| AC-29 | P10-T6 and P11-T5 (verbatim invariant in acceptance and in the evidence artifacts; correct-the-checkpoint-only reconciliation with failing check IDs recorded; halt-and-report path when the checkpoint cannot be corrected), P15-T10 (diff-based audit that no check row, threshold, or validator module was modified during Phases 10-15 to accommodate the live checkpoint), P15-T11 (check-off with evidence), plus the Executor Notes standing rule. |
| US-1 | P3-T2, P3-T3, P10-T2, P11-T2, P14-T1, P14-T2. |
| US-2 | P9-T4, P10-T6, P11-T5 (same fail-closed verdicts; prefixes preserved). |
| US-3 | P10-T2, P10-T5, P10-T6. |
| US-4 | P3-T2, P3-T3, P3-T4. |
| US-5 | P1-T3, P1-T4, P1-T5 (guard detects introductions; zero false positives), P14-T1 (zero findings on the final tree), P15-T3 (guard runs in the full CI-equivalent suite). |
| US-6 | P4-T2..P9-T6 (all 85 checks enforced), P13-T1 (no deferral recorded; divergences limited to PD-1/PD-2/PD-3 and the U1 note). |
| US-7 | P12-T2..P12-T10, P14-T2. |
| US-8 | P2-T2 (module header), P13-T2 (rescoped potential entry). |
| US-9 | P7-T2, P7-T3 (pinned constants with config-parity test; no crash or blanket block on missing config). |
| US-10 | P15-T9 (decision record preserved through review), plus the oracle-intent headers of P4-T3, P4-T5, P5-T3, P5-T5, P6-T3, P6-T5, P7-T3, P7-T5, P8-T3, P9-T3, P9-T5. |
| US-11 | P2-T4 (fail-closed enforcement visible at the point of failure), P2-T5 (help text states the same floor), P2-T6 (deterministic seam test without `$PSVersionTable` mutation). |
| US-12 | P10-T6, P11-T5 (checkpoint-only reconciliation with recorded failing check IDs; halt-and-report path), P15-T10 (audit that no check, row, or threshold was adjusted), plus the Executor Notes standing rule. |

## Executor Notes

- The executor delegates PowerShell implementation batches to `powershell-typed-engineer`; each phase above is a compliant batch under the 3-production / 3-test cap (mirror-copy batches are byte-identical resource copies).
- The batch-budget counter is session-scoped, not phase-scoped; the per-phase reset task (deleting `.claude/state/powershell-batch-budget.<session_id>.json`, the reset remedy named at `.claude/hooks/enforce-powershell-batch-budget.ps1:137`; state-path construction per the module header at `:14`) is the mechanism that makes the Change-Budget Accounting section true. Each phase must still independently respect the 3-production / 3-test cap — the reset marks a batch boundary, it does not authorize exceeding a batch.
- Phase ordering is mandatory: guard fixtures and the fail-before scan evidence (P1-T4, P1-T5) exist before any invocation site is removed; every leaf parity module is green before its consumer wiring lands (Phases 4-8 before 9, 9 before 10-11); `validate-orchestrator-output.ps1` stops calling `Test-PythonOrchestratorValidatorAvailable` in Phase 10, strictly before the probe's deletion in Phase 11, so no phase boundary leaves a hook referencing a deleted command; the guard's repository-scan verification (Phase 14) runs only after the last removal (Phase 11) and after the mirror lands (Phase 12); no full-suite Pester run occurs before P15-T3.
- Intermediate verification runs MUST narrow `scan_folders` (or `Invoke-Pester -Path`) to the specific test folder(s) named by the task; a default scan pulls in the guard's repository-scan `It`s, which are legitimately red until the Phase 11 removal lands.
- Every coverage claim must cite numeric values read from `artifacts/pester/powershell-coverage.xml` (PowerShell) or the pytest coverage report (Python); `CoveragePercentTarget = 0` means a passing PoshQC exit code carries no coverage information.
- Complete parity is non-negotiable: if any inventory row cannot be implemented or mapped, stop and report remediation-required; do not record a deferral, a follow-up, or a scope-out anywhere.
- Standing self-gating rule (AC-29/US-12, binding from Phase 10 onward): **The reconciliation branch corrects the checkpoint, never the check.** If a newly enforced check fails against real checkpoint state, that is the gate working correctly, not a defect in the check. The only permitted response is to correct `artifacts/orchestration/orchestrator-state.json` and record the failing check IDs in the phase's evidence artifact. Adjusting a check to accommodate the checkpoint, weakening a row to make this run pass, or relaxing a threshold is PROHIBITED. If the failure cannot be resolved by correcting the checkpoint, execution halts and reports it as a finding — the executor must not continue past the phase. Rationale: a validator that a run edits to let itself through is worse than no validator — it certifies compliance while enforcing nothing. P15-T10 audits compliance with this rule by diff.
- If any preflight revision is required, this same file is updated in place; no timestamped sibling plan files are created.
