# Remediation Plan: planner-hook-em-dash-mismatch-357 (Issue #357) — Cycle 1

**Plan Timestamp:** 2026-07-17T14-38
**Feature Folder:** `docs/features/active/planner-hook-em-dash-mismatch-357`
**Triggering Input:** `docs/features/active/planner-hook-em-dash-mismatch-357/remediation-inputs.2026-07-17T14-38.md`
**Supporting Audits:** `policy-audit.2026-07-17T14-38.md`, `code-review.2026-07-17T14-38.md`, `feature-audit.2026-07-17T14-38.md`

**Scope of this cycle:** Close the coverage-measurement and coverage-threshold gap identified for `.claude/hooks/validate-planner-output.ps1` (69.87% line coverage, below the uniform 85% requirement in `.claude/rules/quality-tiers.md` and `.claude/rules/powershell.md`; absent from `artifacts/pester/powershell-coverage.xml`). Implements Enumerated Fix List items 1–3 (required) and item 4 (optional, included).

**Change budget for this cycle:** `.claude/hooks/validate-planner-output.ps1` (in scope only if a covering test requires a change; no change is currently expected), `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`.

**Explicitly out of scope for this cycle:** `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` (vendored copy; Informational finding, tracked separately). No lowering of the 85% line / 75% branch coverage threshold. No coverage-exclusion of `.claude/hooks/validate-planner-output.ps1`.

---

### Phase 0 — Remediation Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/powershell.md` in that policy-read order, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record the baseline formatting result in `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-format-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T3] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record the baseline analyzer result in `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-analyze-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T4] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` using the pre-remediation `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` (allowlist not yet modified), and record the pre-remediation baseline test/coverage result in `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/poshqc-test-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` restating the numeric baseline line coverage for `.claude/hooks/validate-planner-output.ps1` (69.87%, 109/156 commands, per the prior cycle's `evidence/baseline/poshqc-test-baseline.md`).
- [x] [P0-T5] Directly inspect `artifacts/pester/powershell-coverage.xml` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/remediation-baseline/coverage-xml-baseline-check.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming that no `<class>`/`<sourcefile>` entry for `validate-planner-output.ps1` exists in this canonical artifact prior to the `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` allowlist change.

---

### Phase 1 — Coverage-Measurement and Coverage-Gap Fixes

- [x] [P1-T1] In `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, add `'.claude/hooks/validate-planner-output.ps1'` as a new entry in the `CodeCoverage.Path` array, with a one-line comment referencing issue #357 following the existing per-issue comment convention already used in that array (e.g., the comments for issues #272, #305).
- [x] [P1-T2] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, add a new `It` test case under the `plan structure validation` `Context` block that supplies a plan whose `### Phase` heading is malformed (e.g., `### Phase 1 Implement` with no separator), asserting the returned validation error matches `phase heading must match`, so `Get-PlanStructureValidationReport`'s `-not $phaseMatch.Success` branch in `.claude/hooks/validate-planner-output.ps1` (lines 136–139) executes and is asserted on.
- [x] [P1-T3] In `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, remove the duplicate `It 'allows termination when phase headings use the canonical em dash (U+2014)'` test case (lines 107–128), since it is now byte-identical to the retained `It 'allows termination when the plan satisfies the structural contract'` test case (lines 84–105) and adds no distinct test signal.

---

### Phase 2 — Final QA and Coverage Verification

- [x] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-format-remediation1-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero or the run modifies any file, restart the full toolchain loop from P2-T1.
- [x] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` scoped to `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-analyze-remediation1-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `EXIT_CODE` is non-zero, remediate the reported finding(s) and restart the full toolchain loop from P2-T1.
- [x] [P2-T3] Run `mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1` using the updated `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/poshqc-test-remediation1-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including the pass/fail test count and the numeric post-change line coverage percentage for `.claude/hooks/validate-planner-output.ps1` as reported in `artifacts/pester/powershell-coverage.xml`. If any test fails or the run modifies files, restart the full toolchain loop from P2-T1.
- [ ] [P2-T4] Directly inspect `artifacts/pester/powershell-coverage.xml` and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/coverage-xml-post-check-remediation1.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming a `<class>`/`<sourcefile>` entry for `validate-planner-output.ps1` now exists with a `line-rate`/`LINE` counter.
- [x] [P2-T5] Record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/coverage-delta-remediation1.md` comparing the baseline line coverage (69.87%, from `evidence/remediation-baseline/poshqc-test-baseline.md`) against the post-change line coverage (from `evidence/qa-gates/poshqc-test-remediation1-final.md`), stating the numeric delta, the coverage status of the newly-exercised changed lines (`.claude/hooks/validate-planner-output.ps1` lines 136–139), and an explicit pass/fail statement against the 85% uniform line-coverage threshold in `.claude/rules/quality-tiers.md`.
- [x] [P2-T6] Run `git diff --stat` against the merge-base for `.claude/hooks/validate-planner-output.ps1`, `tests/scripts/claude-hooks/validate-planner-output.Tests.ps1`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, and this remediation cycle's `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/` additions, and record `docs/features/active/planner-hook-em-dash-mismatch-357/evidence/qa-gates/change-scope-check-remediation1.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` confirming no file outside the declared change budget — in particular `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/validate-planner-output.ps1` — was modified.
