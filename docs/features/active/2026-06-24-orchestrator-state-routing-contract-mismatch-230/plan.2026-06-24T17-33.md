# orchestrator-state-routing-contract-mismatch (Plan)

- **Issue:** #230
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-06-24T17-33
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** full-bug

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Scope lock:** This plan implements exactly the design in `spec.md` (Proposed Fix) and the research target values (§6). The only files changed are: `config/orchestration-routing.json`, `extensions/drm-copilot/resources/config/orchestration-routing.json`, `.claude/skills/orchestrate/SKILL.md`, `tests/scripts/dev_tools/test_validate_orchestrator_state_routing_contract.py` (verification/assertion-literal updates only), and one new test module under `tests/scripts/dev_tools/`. The validator logic in `scripts/dev_tools/_orchestrator_state_routing.py` and `scripts/dev_tools/validate_orchestrator_state.py` (and their bundled mirrors) MUST NOT change. No agents, skills, or MCP tools are created. No Codex payload files are edited.

**Evidence root (non-overridable):** All evidence artifacts MUST be written under `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/<kind>/`. Canonical sub-kinds used by this plan: `baseline/`, `qa-gates/`, `other/`. The `artifacts/` tree MUST NOT be used for evidence output.

---

### Phase 0 — Policy Reading & Baseline Capture

- [x] [P0-T1] Read repository policy files in the order defined by `policy-compliance-order`, scoped to Python (the only language with code/test changes): `CLAUDE.md`; `.claude/rules/general-code-change.md`; `.claude/rules/general-unit-test.md`; `.claude/rules/python.md`; `.claude/rules/python-suppressions.md`; `.claude/rules/self-explanatory-code-commenting.md`; `.claude/rules/quality-tiers.md`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the ordered list), and the explicit list of files read. Acceptance: the artifact exists and lists every file above.
- [x] [P0-T2] Capture the branch/commit baseline. Run `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/baseline-git.md` with `Timestamp:`, `Command:` (both commands), `EXIT_CODE:`, and `Output Summary:` (branch name and full commit SHA). Acceptance: artifact records the current branch and HEAD SHA.
- [x] [P0-T3] Capture the baseline format state. Run `poetry run black --check .`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/baseline-black.md` with `Timestamp:`, `Command: poetry run black --check .`, `EXIT_CODE:`, and `Output Summary:` (pass/fail and count of files that would be reformatted). Acceptance: artifact records the exit code and summary.
- [x] [P0-T4] Capture the baseline lint state. Run `poetry run ruff check .`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/baseline-ruff.md` with `Timestamp:`, `Command: poetry run ruff check .`, `EXIT_CODE:`, and `Output Summary:` (pass/fail and error count). Acceptance: artifact records the exit code and summary.
- [x] [P0-T5] Capture the baseline type-check state. Run `poetry run pyright`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/baseline-pyright.md` with `Timestamp:`, `Command: poetry run pyright`, `EXIT_CODE:`, and `Output Summary:` (error and warning counts). Acceptance: artifact records the exit code and summary.
- [x] [P0-T6] Capture the baseline test + coverage state. Run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/baseline/baseline-pytest.md` with `Timestamp:`, `Command: poetry run pytest --cov --cov-branch --cov-report=term-missing`, `EXIT_CODE:`, and `Output Summary:` including the numeric pass/fail counts, the total line-coverage percent, and the total branch-coverage percent. Acceptance: artifact records numeric line and branch coverage headline values (not placeholders).

### Phase 1 — Canonical Routing Matrix Correction

- [x] [P1-T1] Edit `config/orchestration-routing.json`, `small` route. Set `required_agents` to exactly `["atomic-planner", "atomic-executor", "feature-review"]` (rename `feature-reviewer` -> `feature-review`; remove `commit-steward`). Acceptance: the `small.required_agents` array equals that list in order; the file contains no `feature-reviewer` and no `commit-steward` token. Spec AC: routing-matrix agent-name correction (spec.md AC line 205).
- [x] [P1-T2] Edit `config/orchestration-routing.json`, `small` route. Set `required_skills` to exactly `["orchestrate", "feature-promotion-lifecycle", "atomic-plan-contract", "acceptance-criteria-tracking", "pr-context-artifacts", "pr-base-branch-merge-base"]` (remove `orchestrator-workflow` and `repo-automation-adapter`). Acceptance: the `small.required_skills` array equals that list in order; no `orchestrator-workflow` and no `repo-automation-adapter` token. Spec AC: routing-matrix skill-name correction (spec.md AC line 205).
- [x] [P1-T3] Edit `config/orchestration-routing.json`, `small` route. Set `required_mcp_tools` to exactly `["new_potential_entry", "potential_to_issue", "new_active_feature_folder", "collect_pr_context", "validate_orchestration_artifacts"]` (remove `collect_commit_context`). Acceptance: the `small.required_mcp_tools` array equals that list in order; no `collect_commit_context` token. Spec AC: routing-matrix MCP-tool correction (spec.md AC line 205).
- [x] [P1-T4] Edit `config/orchestration-routing.json`, `large` route. Set `required_agents` to exactly `["task-researcher", "prd-feature", "atomic-planner", "atomic-executor", "feature-review"]` (rename `feature-reviewer` -> `feature-review`; remove `commit-steward`; retain `task-researcher` and `prd-feature`). Acceptance: the `large.required_agents` array equals that list in order; no `feature-reviewer` and no `commit-steward` token. Spec AC: routing-matrix agent-name correction (spec.md AC line 205).
- [x] [P1-T5] Edit `config/orchestration-routing.json`, `large` route. Set `required_skills` to exactly `["orchestrate", "feature-promotion-lifecycle", "atomic-plan-contract", "acceptance-criteria-tracking", "pr-context-artifacts", "pr-base-branch-merge-base"]` (remove `orchestrator-workflow` and `repo-automation-adapter`). Acceptance: the `large.required_skills` array equals that list in order; no removed tokens present. Spec AC: routing-matrix skill-name correction (spec.md AC line 205).
- [x] [P1-T6] Edit `config/orchestration-routing.json`, `large` route. Set `required_mcp_tools` to exactly `["new_potential_entry", "potential_to_issue", "new_active_feature_folder", "collect_pr_context", "validate_orchestration_artifacts"]` (remove `collect_commit_context`). Acceptance: the `large.required_mcp_tools` array equals that list in order; no `collect_commit_context` token. Spec AC: routing-matrix MCP-tool correction (spec.md AC line 205).
- [x] [P1-T7] Edit `config/orchestration-routing.json`, `remediation` route. Set `required_agents` to exactly `["atomic-planner", "atomic-executor", "feature-review"]` (rename `feature-reviewer` -> `feature-review`; remove `commit-steward`). Acceptance: the `remediation.required_agents` array equals that list in order; no `feature-reviewer` and no `commit-steward` token. Spec AC: routing-matrix agent-name correction (spec.md AC line 205).
- [x] [P1-T8] Edit `config/orchestration-routing.json`, `remediation` route. Set `required_skills` to exactly `["orchestrate", "atomic-plan-contract", "acceptance-criteria-tracking", "pr-context-artifacts"]` (remove `orchestrator-workflow` and `repo-automation-adapter`; do NOT add `feature-promotion-lifecycle` or `pr-base-branch-merge-base`). Acceptance: the `remediation.required_skills` array equals that list in order; no removed tokens present and no newly added tokens. Spec AC: routing-matrix skill-name correction (spec.md AC line 205); research §6 remediation note.
- [x] [P1-T9] Edit `config/orchestration-routing.json`, `remediation` route. Set `required_mcp_tools` to exactly `["collect_pr_context", "validate_orchestration_artifacts"]` (remove `collect_commit_context`). Acceptance: the `remediation.required_mcp_tools` array equals that list in order; no `collect_commit_context` token. Spec AC: routing-matrix MCP-tool correction (spec.md AC line 205).
- [x] [P1-T10] Verify `config/orchestration-routing.json` is valid JSON and the top-level structure (`$schema`, `version`, `routes`, per-route `description`) is unchanged. Run `poetry run python -c "import json,pathlib; json.loads(pathlib.Path('config/orchestration-routing.json').read_text(encoding='utf-8'))"`. Acceptance: command exits 0; only the three per-route required lists changed; no keys added or removed. Spec AC: structure unchanged (spec.md line 138).
- [x] [P1-T11] Confirm the entire `config/orchestration-routing.json` file contains none of the removed tokens. Run `poetry run python -c "import pathlib; t=pathlib.Path('config/orchestration-routing.json').read_text(encoding='utf-8'); assert all(s not in t for s in ['feature-reviewer','commit-steward','orchestrator-workflow','repo-automation-adapter','collect_commit_context']), 'stale token present'"`. Acceptance: command exits 0 with no assertion failure. Spec AC: spec.md AC line 205.

### Phase 2 — Bundled Mirror Lockstep

- [x] [P2-T1] Edit `extensions/drm-copilot/resources/config/orchestration-routing.json` so its byte content is identical to the corrected `config/orchestration-routing.json`. Acceptance: the two files are byte-for-byte identical. Spec AC: byte-identical mirror (spec.md AC line 206).
- [x] [P2-T2] Verify byte identity. Run `poetry run python -c "import pathlib; a=pathlib.Path('config/orchestration-routing.json').read_bytes(); b=pathlib.Path('extensions/drm-copilot/resources/config/orchestration-routing.json').read_bytes(); assert a==b, 'config copies differ'"`. Acceptance: command exits 0 with no assertion failure. Spec AC: spec.md AC line 206.

### Phase 3 — Orchestrate Skill Receipt-Emission Instructions

- [x] [P3-T1] Edit `.claude/skills/orchestrate/SKILL.md`. Append a new section titled `## Routing-Contract Receipt Emission` that instructs the orchestrator to write three receipt arrays into `artifacts/orchestration/orchestrator-state.json` for the retained required names of the selected route. The section MUST specify: (a) for each required agent in the route's `required_agents`, a `delegation_receipts[]` object carrying `agent_name` (non-empty string equal to the required agent name) recorded after that delegation returns; (b) a `skill_receipts[]` array where each entry is an object with `skill` (non-empty string equal to a `required_skills` entry), `required: true`, and `evidence` (non-empty string, for example `"read:.claude/skills/orchestrate/SKILL.md"`), written after each required skill is read; (c) an `mcp_call_receipts[]` array where each entry is an object with `tool` (non-empty string equal to a `required_mcp_tools` entry), `ok: true`, and `evidence` (non-empty string such as the MCP response summary or artifact path), appended after each successful required MCP call. The section MUST state that these shapes are read by `_receipt_agents`, `_receipt_skills`, and `_mcp_tools` in `scripts/dev_tools/_orchestrator_state_routing.py` and that the receipt arrays make `require_complete: true` satisfiable. Do not modify the validator. Acceptance: SKILL.md contains the new section with the three named arrays and the exact field requirements (`agent_name`; `skill`/`required: true`/`evidence`; `tool`/`ok: true`/`evidence`). Spec AC: receipt-emission contract documented (spec.md AC line 208).
- [x] [P3-T2] Verify `.claude/skills/orchestrate/SKILL.md` does not exceed 500 lines. Run `poetry run python -c "import pathlib; n=len(pathlib.Path('.claude/skills/orchestrate/SKILL.md').read_text(encoding='utf-8').splitlines()); assert n<=500, n"`. Acceptance: command exits 0. General policy: 500-line file limit.

### Phase 4 — Tests

- [x] [P4-T1] Verify the existing `tests/scripts/dev_tools/test_validate_orchestrator_state_routing_contract.py` builds its fixtures from `load_routing_matrix()` and references no stale names. Run `poetry run python -c "import pathlib; t=pathlib.Path('tests/scripts/dev_tools/test_validate_orchestrator_state_routing_contract.py').read_text(encoding='utf-8'); assert all(s not in t for s in ['feature-reviewer','commit-steward','orchestrator-workflow','repo-automation-adapter','collect_commit_context']), 'stale name referenced'"`. If any stale token is found, update only the offending assertion literal to the corrected name; do not change fixture-construction logic. Acceptance: command exits 0; the hardcoded assertion literals `task-researcher`, `orchestrate`, and `new_potential_entry` remain present and valid. Spec AC: regression test tracks corrected matrix (spec.md AC line 209).
- [x] [P4-T2] Confirm the positive regression in `test_validate_orchestrator_state_routing_contract.py` (`test_complete_state_accepts_full_routing_contract_evidence`) passes against the corrected matrix. Run `poetry run pytest tests/scripts/dev_tools/test_validate_orchestrator_state_routing_contract.py::test_complete_state_accepts_full_routing_contract_evidence -q`. Acceptance: the test passes (exit 0), proving a truthful completed-large checkpoint with corrected names returns zero routing-contract errors. Spec AC: spec.md AC line 209.
- [x] [P4-T3] Confirm the negative assertions still produce clear messages under the corrected matrix. Run `poetry run pytest tests/scripts/dev_tools/test_validate_orchestrator_state_routing_contract.py -q`. Acceptance: all tests in the module pass (exit 0), including `test_complete_state_rejects_missing_required_agent_receipt` (asserts `Checkpoint missing required agent receipt: task-researcher.`), `test_complete_state_rejects_missing_required_skill_receipt` (asserts `Checkpoint missing required skill receipt: orchestrate.`), and `test_complete_state_rejects_missing_successful_mcp_receipt` (asserts `Checkpoint missing successful MCP receipt: new_potential_entry.`). Spec AC: negative tests with clear messages (spec.md AC line 210).
- [x] [P4-T4] Create `tests/scripts/dev_tools/test_orchestration_routing_config_parity.py` (new module; mirrors `config/orchestration-routing.json` location under the test tree per the test-location rule). The module must contain a single deterministic Arrange-Act-Assert test, `test_canonical_and_bundled_routing_config_are_byte_identical`, that reads both `config/orchestration-routing.json` and `extensions/drm-copilot/resources/config/orchestration-routing.json` via `Path(__file__).resolve().parents[N]` repo-root resolution (no temp files, no network), and asserts the two byte strings are equal with an actionable failure message. The module and test must carry docstrings per `self-explanatory-code-commenting`. Acceptance: the file exists, defines exactly that test, and uses no temporary files or external dependencies. Spec AC: guard test asserts identical copies (spec.md AC line 207).
- [x] [P4-T5] Run the new parity test. Run `poetry run pytest tests/scripts/dev_tools/test_orchestration_routing_config_parity.py -q`. Acceptance: the test passes (exit 0). Spec AC: spec.md AC line 207.
- [x] [P4-T6] Verify no new test file exceeds 500 lines. Run `poetry run python -c "import pathlib; p=pathlib.Path('tests/scripts/dev_tools/test_orchestration_routing_config_parity.py'); n=len(p.read_text(encoding='utf-8').splitlines()); assert n<=500, n"`. Acceptance: command exits 0. General policy: 500-line file limit.
- [x] [P4-T7] Confirm the validator source files are unchanged. Run `git diff --name-only -- scripts/dev_tools/_orchestrator_state_routing.py scripts/dev_tools/validate_orchestrator_state.py extensions/drm-copilot/resources/scripts/dev_tools/_orchestrator_state_routing.py extensions/drm-copilot/resources/scripts/dev_tools/validate_orchestrator_state.py`. Acceptance: command produces no output (no validator-source modifications). Spec AC: no validator logic change (spec.md AC line 211).

### Phase 5 — Final QA Loop

- [x] [P5-T1] Run formatting. Run `poetry run black .`. If files are reformatted, re-run until no files change, then restart this phase from P5-T1. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/qa-gates/qa-black.md` with `Timestamp:`, `Command: poetry run black .`, `EXIT_CODE:`, and `Output Summary:` (files reformatted count, final clean status). Acceptance: a clean black pass is recorded.
- [x] [P5-T2] Run linting. Run `poetry run ruff check .`. If it fails or auto-fixes files, correct and restart from P5-T1. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/qa-gates/qa-ruff.md` with `Timestamp:`, `Command: poetry run ruff check .`, `EXIT_CODE:`, and `Output Summary:` (error count). Acceptance: zero ruff errors recorded.
- [x] [P5-T3] Run type checking. Run `poetry run pyright`. If it fails, correct and restart from P5-T1. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/qa-gates/qa-pyright.md` with `Timestamp:`, `Command: poetry run pyright`, `EXIT_CODE:`, and `Output Summary:` (error and warning counts). Acceptance: zero pyright errors recorded.
- [x] [P5-T4] Run tests with coverage. Run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. If it fails or any prior step changed files, correct and restart from P5-T1. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/qa-gates/qa-pytest.md` with `Timestamp:`, `Command: poetry run pytest --cov --cov-branch --cov-report=term-missing`, `EXIT_CODE:`, and `Output Summary:` including numeric pass/fail counts, total line-coverage percent, and total branch-coverage percent. Acceptance: all tests pass; line coverage >= 85% and branch coverage >= 75%, recorded as numeric values.
- [x] [P5-T5] Record the coverage delta and changed-line non-regression. Write `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/qa-gates/coverage-comparison.md` with `Timestamp:`, baseline line/branch coverage (from P0-T6), post-change line/branch coverage (from P5-T4), and a statement of changed-line coverage. Because the only changed source is JSON config and Markdown (not coverage-measured) plus added/updated tests, note that no production Python line changed and the `_orchestrator_state_routing.py` coverage must not regress versus baseline. Acceptance: artifact records baseline, post-change, and no-regression confirmation; if any value is unavailable, mark the outcome remediation-required (never PASS). Spec AC: toolchain clean pass with coverage thresholds (spec.md AC line 212).

### Phase 6 — Documentation & Status

- [x] [P6-T1] Update the feature `issue.md` and/or `spec.md` status fields and check off the satisfied acceptance criteria in `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/spec.md` (lines 205-213) with evidence pointers. Mirror any GitHub issue update to `docs/features/active/2026-06-24-orchestrator-state-routing-contract-mismatch-230/evidence/issue-updates/issue-230.<timestamp>.md` per `evidence-and-timestamp-conventions`. Acceptance: each addressed AC is checked with a pointer to the corresponding evidence artifact; the issue-update mirror exists if a GitHub update was posted. Spec AC: spec.md acceptance-criteria section.
