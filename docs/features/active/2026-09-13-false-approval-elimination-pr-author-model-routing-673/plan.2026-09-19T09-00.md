# Atomic Implementation Plan (Revision 4) — False-Approval Elimination, Issues #673 and #672

- Feature folder: `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/`
- Issue: #673. Work Mode: `full-bug` (`issue.md`). `spec.md` is the acceptance-criteria source. [P1-T2] amends it to add AC-34 through AC-39, so this plan's inventory is AC-1 through AC-39.
- Second issue closed by the same pull request: #672, whose feature folder is `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/`. Phase 9 migrates the gate that issue delivered and [P11-T9] closes the two criteria still unchecked in its spec.
- Plan authored: 2026-09-19T09-00
- **Supersedes `plan.2026-09-18T16-00.md`** in the same folder, which superseded `plan.2026-09-18T13-30.md`, which superseded `plan.2026-09-13T20-48.md`. No earlier plan is edited or deleted. No task of `plan.2026-09-18T16-00.md` or `plan.2026-09-18T13-30.md` was executed; the checked tasks of `plan.2026-09-13T20-48.md` produced the reproduction evidence this plan reuses read-only.
- **Why it is superseded.** Revision 3 is correct as far as it goes and its design is carried forward unchanged. It left `.claude/hooks/enforce-prd-feature-before-planner.ps1` on F1's folder-first `Resolve-WorktreeCallTarget`, pinned that with AC-37, and recorded the migration as a follow-up. The user decided on 2026-09-19 to do the migration here instead, so one pull request closes both #673 and #672. This revision adds design C (§2.7), Phase 9, the replacement AC-37, the new AC-39, and the regression case of §2.7.5; it renumbers the former Phases 9 and 10 to 10 and 11.
- Branch: `bug/false-approval-elimination-673`, tip `c50f82c2` when this plan was authored.
- Topology (user ruling, 2026-09-18): standalone pull request to `main`. Not an epic child. No push occurs inside this plan; the full Pester run of [P11-T4] precedes any push the caller makes.
- Anchor for every diff and log: `F5_BASE_SHA`, the merge base of `HEAD` and `origin/main`, recorded once by [P0-T3].

## Evidence and host-data rules (binding on every task)

1. Every artifact is written under `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/<kind>/` with `<kind>` one of `baseline`, `regression-testing`, `qa-gates`, `other`. New artifacts carry an `r3-` prefix so no earlier artifact is overwritten; artifacts introduced by this revision carry an `r4-` prefix. Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. `EXIT_CODE: SKIPPED` is never a passing outcome.
2. **No host data.** No artifact, script, test, fixture, or document this plan writes may contain an absolute filesystem path, a drive letter, a user-profile path, or the executing user's account name. Refer to files by repository-relative path. Where a command prints such a value, the artifact records it with the value replaced: the executing worktree root becomes `<WORKSPACE_ROOT>`, the session scratchpad directory becomes `<SCRATCHPAD>`, the user profile directory becomes `<HOME>`. The executor computes these values at run time and never writes them.
3. Runner scripts for fresh PowerShell processes live in the session scratchpad and contain only repository-relative paths. They obtain the root at run time with `(Get-Location).Path` and are launched with the executing worktree as the working directory. The `Command:` field records the scratchpad directory as `<SCRATCHPAD>`, and the artifact quotes the script body.
4. Artifacts record Pester test names and pass or fail status, never failure-message text, and never decision JSON that carries a path.
5. **A clean tree is never asserted as an empty porcelain, and a commit task observes the tree before it appends.** This rule applies wherever an acceptance condition reads `git status --porcelain`, and it exists because every task in this plan writes an evidence artifact under the feature folder. Three consequences bind:
   - **Scope, not emptiness.** The satisfiable form is the one [P10-T2] already uses: *lists no path outside the feature folder*. `is empty` is never a valid acceptance condition after Phase 0, because the task's own artifact is uncommitted when the observation is taken. [P10-T5], [P10-T6], and [P10-T9] use the scoped form for this reason; the phases they verify ([P10-T1] through [P10-T4], [P11-T1] through [P11-T4]) write artifacts with no intervening commit.
   - **Observation order inside a commit task.** Each of [P1-T6], [P4-T7], [P7-T8], [P8-T9], [P9-T13], and [P11-T10] commits, then appends to `evidence/other/r3-commits.md`. The porcelain observation is taken **immediately after the commit and before that append**, and the artifact records it with the label `Porcelain (post-commit, pre-append):`. Taken after the append it would report the append itself and could never be clean at any scope.
   - **The last append must reach the branch.** [P11-T10] is the final task, so an append left uncommitted there never reaches the branch at all. It therefore takes its observation post-commit and pre-append as above, and then makes one further commit whose only content is that append.
6. **An offending literal is named by location, never reproduced.** Rule 2 admits no quoting exemption, and the scan applies to this plan and to every artifact under the feature folder, both of which are modified files. So where this plan or an artifact must refer to a token the [P0-T16] matchers would catch — a drive-letter path being removed, a host path being redacted — it identifies the token by file and line and by the role it plays on that line, and leaves the executor to read the spelling from the file. This binds the redaction log of [P1-T5], the removal task [P9-T6], and the scan reports of [P0-T16] and [P10-T9], none of which may quote a matched token.
7. Synthetic roots inside the prd-feature suites follow a **form**, not a closed list: each is a bare string literal beginning `/synthetic-worktrees/`, optionally composed with a loop index at run time. Any value of that form names no host and satisfies both the host-data rule and the determinism statement issue #672's spec requires of that suite. The members already in the tree or required by this plan are examples of the form rather than an exhaustive set: `/synthetic-worktrees/coordinating-session` and `/synthetic-worktrees/item-worktree` (`enforce-prd-feature-before-planner.TargetResolution.Tests.ps1:32-33`), and `/synthetic-worktrees/session-root`, which is already the blanket-mock root at `enforce-prd-feature-before-planner.Tests.ps1:13` and `.FolderResolution.Tests.ps1:28` and which [P9-T5] step 2, [P9-T6], and [P9-T7] all require. Stated as a closed pair, this rule would have contradicted three of its own tasks.

## 1. Why this revision exists

### 1.1 Established facts (empirical probe, 2026-09-18; treated as established)

1. A PreToolUse hook runs in the directory of the process that made the call. A call made from inside an item's own worktree already reads that worktree's checkpoint through the relative path.
2. An isolated subagent (`Agent` with `isolation: worktree`) gets a worktree built from `origin/main`'s tip, and its hooks run inside that worktree using `main`'s copy of the hook scripts. A hook change on this branch therefore cannot be exercised end to end by isolated subagents before merge. **All pre-merge verification in this plan is in-process or child-process Pester and the Python suite. No task delegates to an isolated subagent to verify behaviour.**
3. The hook payload's `cwd` field is, for the calls that produce false approvals, the coordinator's directory, not the item's. It is never used to identify the item.
4. False approvals arise only from calls made by a process whose directory is not the item's worktree: a coordinator delegating gated work on an item's behalf, or a process acting on an item through `cd` or `git -C`. The incident was green because a stale per-feature checkpoint for issue 838 sat at the coordinator's root.
5. `Resolve-WorktreeCallTarget` reads signals in the order FeatureFolderPath, FilePath, Branch (`.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:276-280`) and treats any signal with other than exactly one candidate as `Ambiguous` (`:289-293`). A feature folder merged to `main` exists in every checkout branched from `main`, so a relative folder token is almost always `Ambiguous`, and it overrides a branch signal that is unique. Verified: `branch: <item branch>` alone resolves uniquely; the same text plus the relative folder path resolves `Ambiguous`. The folder identifies what the work is, not where it lives.
6. Verified 2026-09-18: a coordinating session delegating `atomic-planner` for an item whose feature folder has merged, and therefore also exists in other checkouts, matched twelve candidate worktrees and was denied `TARGET_WORKTREE_AMBIGUOUS` by `.claude/hooks/enforce-prd-feature-before-planner.ps1`. That is the ordinary state of a folder after merge, so the gate denies the ordinary case. §2.7.5 makes it a regression case.

### 1.2 Delivered by #687 on `main` (re-derived against the tree; not re-implemented)

| Fact | Location |
| --- | --- |
| No-target reason code literal; accessor `Get-WorktreeResolutionNoTargetReasonCode` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:59`; `:475`, exported `:500` |
| Ambiguity reason code literal; accessor `Get-WorktreeResolutionAmbiguityReasonCode` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:55`; `:463`, exported `:499` |
| `WorktreeResolution.psm1` is at the 500-line cap | last line `:500` |
| The result constructor `New-WorktreeResolutionTargetResult` sets `ReasonCode` from the two accessors | `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:66`, `:128-132` |
| Branch reader `Find-WorktreeResolutionBranchSignal` (`--head`, `--branch`, `branch:`) | `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:164`, pattern `:61` |
| Worktree enumeration `Get-WorktreeResolutionWorktreeRoot` (optional `-Branch` filter) | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:334` |
| Worktree marker test `Test-WorktreeResolutionRootMarker`; ascent `Find-WorktreeResolutionRoot` | `.claude/lib/worktree-resolution/WorktreeResolution.psm1:228`, `:258` |
| pr-author seam `Resolve-PrAuthorWorktreeTarget`; mapping `Get-PrAuthorTargetCheckpointResolution` | `.claude/hooks/enforce-pr-author-skill-helpers.ps1:41-62`, `:64-107` |
| pr-author resolution runs after Case C and before the preflight (NoTarget and Ambiguous deny) | `.claude/hooks/enforce-pr-author-skill-helpers.ps1:314-320` |

### 1.3 Not delivered (this plan's scope)

1. **Binding 1, AC-4 form.** `.claude/hooks/enforce-pr-author-skill.ps1:49` still assigns the relative literal; `SessionRoot` returns it unchanged (`helpers:96`).
2. **Binding 2.** `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1:35` defaults `-CheckpointPath`; `:78` passes no argument, so check 6 reads the session root's checkpoint even when binding 1 resolved another worktree.
3. **Binding 3.** `.claude/hooks/enforce-model-routing-receipt.ps1:46` defaults `-CheckpointPath`; `:157` passes no argument. No resolution step exists.
4. **Design A and B** (checkpoint hygiene; identity resolution), which no earlier revision planned.
5. **Design C, the #672 gate migration.** `.claude/hooks/enforce-prd-feature-before-planner.ps1:278-280` still calls `Resolve-WorktreeCallTarget`; `:272-275` still reads the envelope `cwd` as the session root; `:353` still reads the calling session's checkpoint through `Get-PrdFeatureCheckpointFolder`'s default path (`:160`); `Select-PrdFeatureFolderByTarget` still disambiguates cited folders from `$Target.SignalValue` (`helpers:237-241`). Revision 3 pinned all of this as unchanged; this revision replaces it.

## 2. Design

### 2.1 Identity resolution module (design B)

New module `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, beside F1's two modules. It imports `WorktreeResolution.psm1` and `WorktreeTargetResolution.psm1` with `-ErrorAction Stop` and re-uses F1's enumeration, marker test, ascent, branch reader, path normaliser, path join, result constructor, and reason-code accessors. It re-implements none of them. `WorktreeResolution.psm1` (500 lines) and `WorktreeTargetResolution.psm1` are **not modified**, so no extraction is needed; the coupling guard in [P10-T5] asserts both are absent from the diff.

Exported functions (all `[CmdletBinding()]`, each with `[OutputType()]` declared to match what it returns, including `[string[]], [object[]]` where it returns `, [string[]]`):

| Function | Contract |
| --- | --- |
| `Get-WorktreeItemCheckpointRelativePath` | Returns the one definition of the literal `artifacts/orchestration/orchestrator-state.json`. |
| `Get-WorktreeItemCheckpointPath -WorktreeRoot` | Returns `Join-WorktreeResolutionPath -WorktreeRoot <root> -RepoRelativePath (Get-WorktreeItemCheckpointRelativePath)`, an absolute forward-slash path. |
| `Find-WorktreeItemIssueSignal -Text` | Returns, as `, [string[]]`, the distinct normalised issue numbers named by lines matching `Canonical issue number for this feature is <digits>` (case-sensitive, optional `#` before the digits), in order of first appearance. |
| `ConvertTo-WorktreeItemIssueNumber -Value` | Pure. Returns the positive-integer issue number as a digit string without leading zeros for an integer value or a string of the form optional `#` plus digits (whitespace trimmed); `$null` for anything else, including zero, `none`, and an empty value. |
| `Get-WorktreeItemCheckpointText -Path` | The module's only filesystem read (seam): raw text of a file, or `$null` when absent or unreadable. |
| `Get-WorktreeItemCheckpointIssue -WorktreeRoot` | Reads the checkpoint at `Get-WorktreeItemCheckpointPath` through the seam, parses it with `ConvertFrom-Json` inside `try`, and returns `ConvertTo-WorktreeItemIssueNumber` of its `issue-num` property; `$null` when the file is absent, empty, unparseable, not a JSON object, or lacks the key. |
| `Get-WorktreeItemLiveRoot -SessionRoot [-Branch]` | Seam. Ascends from `SessionRoot` with `Find-WorktreeResolutionRoot`; when no worktree root is found returns an empty array. Otherwise returns `Get-WorktreeResolutionWorktreeRoot -SessionRoot <ascended root> [-Branch <b>]` filtered to roots for which `Test-WorktreeResolutionRootMarker` is true. |
| `Resolve-WorktreeItemTarget -Text [-SessionRoot]` | Always returns an object built by `New-WorktreeResolutionTargetResult`. Algorithm in §2.2. `SessionRoot` defaults to `(Get-Location).ProviderPath`. |

**Definition of live.** A worktree root `W` is live when (L1) it is registered in the administrative layout of the repository containing the calling process's directory, which is the main checkout plus every `worktrees/<name>/gitdir` registration (`Get-WorktreeResolutionWorktreeRoot`), and (L2) `Test-WorktreeResolutionRootMarker -Path W` is true, meaning the directory exists and its `.git` entry is a directory or a well-formed `gitdir:` file. A registration whose directory was deleted (git's "prunable" state) fails L2 and is excluded. A worktree records issue `N` when `Get-WorktreeItemCheckpointIssue -WorktreeRoot W` equals `N`. Only the canonical checkpoint path is read, so a checkpoint archived under `artifacts/orchestration/handoff/` is never matched.

**Stale attempts at the same issue.** A worktree from an earlier attempt is excluded when its directory is gone (L2) or its checkpoint was archived or removed. When it is still live and still records issue `N`, the issue alone places the call in two worktrees, which is `Ambiguous`. The only tie-breaker is a branch signal in the same call: git checks a branch out in at most one worktree, so the branch names exactly one of them. No timestamp, `last_updated`, lifecycle step, or path proximity is ever read to break the tie, because a stale checkpoint carries those values too; choosing by them is the defect class this feature removes. The ambiguity detail names the remedy: archive the stale checkpoint (design A) or add a `branch:` label.

### 2.2 Resolution algorithm (`Resolve-WorktreeItemTarget`)

Let `I` be the issue numbers from `Find-WorktreeItemIssueSignal`, `B` the branch from `Find-WorktreeResolutionBranchSignal` (first match, F1's reader unchanged), and `S` the normalised session path. Decided in this order:

| # | Condition | Status | Detail (no absolute path is ever included) |
| --- | --- | --- | --- |
| 1 | `I` has more than one member | Ambiguous | `the call names <k> different issue numbers (<list>), and an item has exactly one` |
| 2 | `I` empty and `B` absent | NoTarget, returned before any enumeration or file read | `the call carries neither a canonical issue number line nor a branch signal (--head, --branch, or branch:), so the item it acts on cannot be identified` |
| 3 | `B` present: `MB = Get-WorktreeItemLiveRoot -SessionRoot S -Branch B` has 0 members | NoTarget | `branch '<B>' is checked out in no live worktree` |
| 4 | `B` present: `MB` has more than one member | Ambiguous | `branch '<B>' is checked out in <k> live worktrees` |
| 5 | `B` present, `MB = {W}`, `I` empty | resolved `W` | `branch '<B>' resolves to the live worktree that has it checked out` |
| 6 | `B` present, `MB = {W}`, `I = {N}`, `W` records `N` | resolved `W` (this breaks a stale-attempt tie) | `branch '<B>' and issue <N> resolve to the same live worktree` |
| 7 | `B` present, `MB = {W}`, `I = {N}`, `W` records a different issue `M` | Ambiguous | `branch '<B>' places the call in a worktree whose checkpoint records issue <M>, not issue <N>` |
| 8 | `B` present, `MB = {W}`, `I = {N}`, `W`'s issue unknown, and some other live worktree records `N` | Ambiguous | `branch '<B>' and issue <N> place the call in different worktrees` |
| 9 | `B` present, `MB = {W}`, `I = {N}`, `W`'s issue unknown, no live worktree records `N` | resolved `W` | as row 5 |
| 10 | `B` absent, `I = {N}`: `MI` = live worktrees recording `N` has 0 members | NoTarget | `issue <N> is recorded in the orchestrator checkpoint of no live worktree` |
| 11 | `B` absent, `MI = {W}` | resolved `W` | `issue <N> resolves to the live worktree recording it` |
| 12 | `B` absent, `MI` has more than one member | Ambiguous | `issue <N> is recorded in the orchestrator checkpoints of <k> live worktrees; move the stale checkpoint to artifacts/orchestration/handoff/ or add a branch: label naming the item's branch` |

A resolved result has `Status` `SessionRoot` when `W` equals `S` or equals `Find-WorktreeResolutionRoot -Path S` (the latter evaluated only when the former is false), and `OtherWorktree` otherwise; `Signal` is `Branch` with `SignalValue` `B` when a branch was used, and empty otherwise; `Candidate` is `@(W)`. Ambiguous results carry the candidate roots in `Candidates` (an object field, never rendered into a reason). `ReasonCode` is supplied by F1's constructor: `NoTarget` carries the no-target code and `Ambiguous` the ambiguity code (user mapping, 2026-09-18: zero live matches or no identity deny with the no-target code; several, or disagreeing signals, deny with the ambiguity code).

The calling process's directory is used for exactly two things: to find the repository whose worktrees are enumerated, and to label a resolved result `SessionRoot`. It is never the checkpoint location unless identity resolution selects it. The payload `cwd` field is not read.

### 2.3 Signal precedence decision (point 5)

**Decision: a feature folder is used only to find documents and never to choose the worktree.** `Resolve-WorktreeItemTarget` reads no path signal at all: an absolute or relative feature-folder path, and an absolute file path, are ignored for worktree selection. Ranking unique signals above location-ambiguous ones was rejected, because an absolute path is still host-dependent, and a ranked resolver would keep a path as a fallback selector.

The rule applies to all four gates this plan touches. The three bindings of §2.6 locate no documents at all, so they read no folder. The prd-feature gate does locate documents, and §2.7 gives it the folder in exactly that role: identity picks the worktree, then the folder names which documents to check beneath it.

### 2.4 Checkpoint hygiene (design A) and where it is enforced

**Rule.** A coordinating session never holds a per-feature `artifacts/orchestration/orchestrator-state.json` at its own root when it hands work off. It moves such a file to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json`, following the existing convention (`artifacts/orchestration/handoff/` already holds `orchestrator-state.epic-678-integration-pr-686.2026-09-18T10-37.json` in this worktree).

**Enforcement decision.**

1. **Skill contract (normative).** [P8-T2] to [P8-T4] add the rule to `.claude/skills/orchestrate/SKILL.md` `## Checkpoint Handling` (archive a checkpoint belonging to a different issue instead of overwriting or leaving it; archive on handing an item to another session or worktree), `.claude/skills/parallel-orchestrate/SKILL.md` `## Per-Item Branch and Worktree Lifecycle`, and `.claude/skills/epic-orchestrate/SKILL.md` `## Epic-Level Checkpoint`. A Pester contract test ([P8-T6]) asserts each statement is present, so the rule cannot be removed silently.
2. **Mechanical check inside the four gates.** Under identity resolution a stale root checkpoint can affect a verdict only when it records the same issue as the call. In that case the root is a second live worktree recording the issue, and §2.2 row 12 denies `Ambiguous` with the archive remedy in the reason. A stale checkpoint for a different issue is never read by these gates. The gates therefore enforce hygiene exactly where it could change their verdict, with no new deny path.
3. **No separate hook or validator check is added.** A check that denied on the mere presence of a foreign checkpoint would add a deny path to gates for a condition that is not itself a gated action, and a Python validator check would require a TypeScript parity port. Neither is needed for the correctness of the four gates. The gates that still read cwd-relative checkpoints are listed in §8 as the places where hygiene continues to matter; they are tracked under issue #690 and are out of scope here.

**Delegation identity (skill contract).** `.claude/skills/orchestrate/SKILL.md:258` currently requires the canonical issue line only on delegations to `atomic-planner`, `atomic-executor`, and `feature-review`. [P8-T2] extends it to all six receipt-gated types (`atomic-planner`, `atomic-executor`, `feature-review`, `task-researcher`, `prd-feature`, `pr-author`, matching `Get-ModelRoutingGatedAgent` at `.claude/hooks/enforce-model-routing-receipt.ps1:73-80`) and adds a `branch:` label naming the item's branch, written as the first `branch:` occurrence in the prompt. Before promotion assigns an issue number, the branch label alone is required. The same paragraph names both identity-resolving gates, `enforce-model-routing-receipt.ps1` and `enforce-prd-feature-before-planner.ps1`, because after Phase 9 an `atomic-planner` delegation is read for identity by both. The contract test pins the skill's agent list to the gate's list.

### 2.5 Hook outcomes (all four gates)

| Resolution state | Action | Reason text begins with |
| --- | --- | --- |
| `SessionRoot` or `OtherWorktree` | proceed against the resolved worktree root; all existing decision logic runs unchanged | existing reasons only |
| resolved; checkpoint or document state is genuinely bad | existing deny, unchanged | `ORCHESTRATOR_STATE_PREFLIGHT_FAILED` (pr-author), `MODEL_ROUTING_RECEIPT_BLOCKED` (model-routing), `PRD_FEATURE_BLOCKED: resolved feature folder ... is missing:` or the work-mode reason (prd-feature) |
| `NoTarget` | deny | `$target.ReasonCode` (the no-target code) |
| `Ambiguous` | deny | `$target.ReasonCode` (the ambiguity code) |

There is no working-directory fallback on any branch. A target-resolution deny never contains a document-family or checkpoint-family reason, and a document-family or checkpoint-family deny never contains either target-resolution code. §2.7.4 states the one place where the prd gate emits a resolution code on a non-identity path and why it belongs to the resolution family.

### 2.6 Hook changes (bindings 1, 2, and 3)

- **DD-1 — pr-author seam body.** `Resolve-PrAuthorWorktreeTarget` keeps its name and `-CommandText` parameter; its body becomes `Resolve-WorktreeItemTarget -Text $CommandText -SessionRoot (Get-Location).Path`. `Get-PrAuthorTargetCheckpointResolution` merges `SessionRoot` and `OtherWorktree` into one branch returning `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot`; its deny branch and reason text are unchanged (#687 behaviour, including the `--head` remedy). `enforce-pr-author-skill-helpers.ps1` adds `Import-Module (Join-Path $PSScriptRoot '../lib/worktree-resolution/WorktreeItemResolution.psm1') -Force -ErrorAction Stop` after its two F1 imports (`:38-39`), which stay.
- **DD-2 — resolve once, pass explicitly (bindings 1 and 2).** `enforce-pr-author-skill.ps1:49` becomes `$script:OrchestratorStateCheckpointPath = $null` (the spec's required form). In `Get-PrAuthorBypassReason`, after the deny check, `$script:OrchestratorStateCheckpointPath` is assigned the resolved absolute path and passed to `Invoke-OrchestratorStatePreflight -CheckpointPath`, to `Test-PrAuthorReceiptVerification -CheckpointPath` (new mandatory parameter), and on to `Test-EpicBaseBranchOverride -CheckpointPath` (new mandatory parameter), which passes it to `Get-PrAuthorCheckpointContent -CheckpointPath` (now mandatory, default removed).
- **DD-3 — model-routing (binding 3).** `enforce-model-routing-receipt.ps1` imports `WorktreeItemResolution.psm1` at script scope with `-Force -ErrorAction Stop` after the `HookPayload.psm1` import at `:36`. New seam `Resolve-ModelRoutingWorktreeTarget -PromptText` calls `Resolve-WorktreeItemTarget -Text $PromptText -SessionRoot (Get-Location).Path`. New `Get-ModelRoutingTargetCheckpointResolution -PromptText` maps §2.5 and returns an ordered dictionary with `CheckpointPath` and `Reason`. The reason for an unresolved target is `"$($target.ReasonCode): $($target.Detail) The model-routing gate will not check this delegation against a checkpoint that may belong to a different item. Put the line 'Canonical issue number for this feature is <N>.' and a 'branch: <item branch>' label in the delegation prompt so the gate can identify the item."`. The deny sits between the scope filter (`:153-155`) and the checkpoint read (`:157`), which becomes `Get-ModelRoutingCheckpoint -CheckpointPath $resolution.CheckpointPath`; `Get-ModelRoutingCheckpoint`'s parameter becomes mandatory with no default. Only the `prompt` field is scanned.

### 2.7 prd-feature gate migration (design C, issue #672)

The gate's job is unchanged: block an `atomic-planner` delegation whose feature folder does not yet hold the prerequisite documents its persisted work mode requires. What changes is how it decides *where* to look.

#### 2.7.1 Identity picks the worktree

- **DD-4 — derivation seam.** `Get-PrdFeatureCallTarget` loses its `-Envelope` parameter and keeps `-ToolInput`. It assembles the prompt and description text exactly as today (`.claude/hooks/enforce-prd-feature-before-planner.ps1:265-267`) and returns `Resolve-PrdFeatureWorktreeTarget -Text $text`. That new one-line seam calls `Resolve-WorktreeItemTarget -Text $Text -SessionRoot (Get-Location).Path`. The envelope `cwd` read at `:272-275` is deleted with the parameter, so the gate has no syntactic route to it. The file's `Import-Module` of `WorktreeTargetResolution.psm1` (`:108`) **stays**, and an import of `WorktreeItemResolution.psm1` is added immediately after it, unguarded for the same stated reason — the two-import form of `.claude/hooks/enforce-pr-author-skill-helpers.ps1:38-39`.

  **Why both imports, not a swap (orchestrator ruling, 2026-09-19).** `Join-WorktreeResolutionPath`, which the probe composition at `:381` calls, is defined at `WorktreeTargetResolution.psm1:311` and appears only in that file's explicit `Export-ModuleMember -Function` list at `:341-347`. `WorktreeResolution.psm1`'s list at `:490-500` does not carry it. A module that imports a sibling does not re-export the sibling's functions to its own importer when its exports are pinned by an explicit list, so importing `WorktreeItemResolution.psm1` alone would not put `Join-WorktreeResolutionPath` in the gate's scope. Swapping the import would therefore make `:381` an unresolvable command on every `OtherWorktree` result — the coordinator case this feature exists to fix — and [P9-T9] row 1 could not pass. Keeping both imports is the minimal change, follows the precedent already in the tree for this exact reason, leaves the new module's export surface at the eight functions §2.1 specifies, and keeps DD-8's claim that `:379-382` is unchanged.

  **Scope sweep (recorded so it is not re-derived).** The same rule was checked against every other consumer in this plan and no second instance exists. `enforce-pr-author-skill-helpers.ps1` already imports both F1 modules explicitly and DD-1 adds the third. `enforce-model-routing-receipt.ps1` calls no F1 function — it reads `ReasonCode` and `Detail` as properties — so DD-3's single import suffices. `WorktreeItemResolution.psm1` imports both F1 modules itself and calls them from inside its own session state. The prd gate reaches `Get-WorktreeResolutionAmbiguityReasonCode` and the no-target accessor through the dot-sourced helpers' import of `WorktreeResolution.psm1` (`helpers:27`), because an `Import-Module` inside a dot-sourced file lands in the caller's session state; that is why the helpers' import is load-bearing for the parent and must not be removed. Every suite this plan authors imports all three modules explicitly per §4; the four pre-existing suites it edits import none or two and resolve through the hook and helpers they dot-source, which §4 records as sound and which no task changes.
- `Invoke-PrdFeatureBeforePlannerDecision` keeps its `-ResolvedTarget` injection seam and its `$PSBoundParameters.ContainsKey` binding rule unchanged.
- **DD-5 — unresolved identity denies first.** The Ambiguous-only check at `:337-339` becomes a check for `NoTarget`, for `Ambiguous`, and for a `$null` target from either source. It denies through `Get-PrdFeatureTargetDecision -ReasonCode <code> -Detail <detail>`, which replaces `Get-PrdFeatureAmbiguityDecision` (`:203-231`) and takes the code as a parameter instead of calling one accessor. `$null` has two origins and both mean the same thing: the `-ResolvedTarget` seam explicitly bound to `$null`, and `Get-PrdFeatureCallTarget`'s existing early return for empty text (`:268-270`), which DD-4 keeps so that a call carrying no text still performs no enumeration and no file read. For either, the code is `Get-WorktreeResolutionNoTargetReasonCode` and the detail is `the call target was not resolved`. The early return is load-bearing for a pinned assertion: `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1:186` asserts the resolver is invoked zero times for an empty-text call. The reason keeps the `PRD_FEATURE_BLOCKED: <code> - <detail>. ` prefix shape and replaces the remedy sentence, which currently instructs the caller to cite an absolute path (`:227-228`) and after this change would name an action that no longer selects anything. The new remedy is: `Put the line 'Canonical issue number for this feature is <N>.' and a 'branch: <item branch>' label in the delegation prompt so the gate can identify the item.` The deny still runs before any document probe.

#### 2.7.2 The folder locates documents beneath the resolved worktree

- **DD-6 — checkpoint fallback anchored to the resolved worktree.** `Get-PrdFeatureCheckpointFolder`'s `-CheckpointPath` parameter becomes `[Parameter(Mandatory)]` with no default (`:160`). The single production call site passes `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot`. The calling session's checkpoint is therefore never read unless identity resolved to the calling session's own worktree.
- **DD-7 — folder selection.** The parent computes `$candidates = @(Find-PrdFeatureFolderCandidate -Prompt $prompt)` as today (`:341`) and reads the checkpoint only when it needs it:
  - exactly one candidate: that candidate is the folder, and no checkpoint read occurs;
  - zero or more than one candidate: the checkpoint folder is read once, through DD-6.

  `Find-PrdFeatureFolderFromPrompt` replaces its `-Target` parameter with an optional `-CheckpointFolder`, and `Select-PrdFeatureFolderByTarget` (`helpers:211-244`) becomes `Select-PrdFeatureFolderByCheckpoint -Candidate -CheckpointFolder`, which normalises the checkpoint value through `ConvertTo-PrdFeatureFolderToken` and returns it when it is one of the candidates. The disambiguator is still not prompt position and is still not a foreign session's record: the checkpoint now consulted belongs to the worktree identity selected, which is the item's own. When it names none of the candidates the tie is unresolved and the gate denies with the ambiguity code, as today (`:346-348`), with the detail changed to name the checkpoint rather than a derived target. The replacement detail must retain the clause `cites $($candidates.Count) feature folders ($($candidates -join ', '))` verbatim, because `TargetResolution.Tests.ps1:417` asserts `*cites 2 feature folders*` and [P9-T5] step 5 keeps that assertion while [P9-T12] gates on the row.
  - When the prompt named no folder and the checkpoint supplied none, the decision logic of the existing `must reference a feature folder` deny at `:363-371` is unchanged. Its reason string at `:368` is reworded by [P9-T4], which is required rather than optional: that string is the file's second occurrence of the checkpoint filename literal, and three acceptance conditions require the literal to be gone from the file.
- **DD-8 — probe anchoring unchanged.** The probe folder is composed with `Join-WorktreeResolutionPath -WorktreeRoot $target.WorktreeRoot -RepoRelativePath $folderNormalized` when the resolved status is `OtherWorktree`, and is the bare repo-relative spelling when it is `SessionRoot` (`:379-382`, unchanged, including the call at `:381`). That call resolves only because DD-4 keeps the `WorktreeTargetResolution.psm1` import alongside the new one; the reasoning is recorded under DD-4 and is the single condition on which this bullet's "unchanged" claim depends. The `SessionRoot` case is correct because the gate is then executing inside the resolved worktree. The residual case — a process whose directory is a subdirectory of the resolved worktree — is recorded in §8; it is pre-existing, it can only produce a false denial, and closing it would rewrite the probe-path assertions of two suites that sit within fifty lines of the 500-line cap.
- **DD-8a — the folder-absent guard keeps its `$probeFolder -ne $folderNormalized` conjunct (`:393`).** Revision 4 proposed dropping it on the reasoning that composition is always absolute; DD-8 makes that false for a `SessionRoot` result, and dropping it would make `if ($null -eq $issueContent)` fire ahead of the work-mode branch on every session-root call whose `issue.md` is unreadable. `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1:338-349` (`It 'denies with the indeterminate-marker reason when issue.md is unreadable'`) pins exactly that case and is a row [P9-T7] forbids editing. The conjunct is therefore kept byte-unchanged, which is also what §2.7.3 and §2.7.4 require.
- **DD-9 — dead branch removal.** `Test-PrdFeatureSessionRootTarget` (`:181-201`) and the deny at `:359-361` are deleted. Both exist to stop a session's own checkpoint standing in for another worktree's call; after DD-6 the checkpoint read is anchored by construction, so the guard is unreachable and would be uncovered code.

#### 2.7.3 Work-mode contract preserved exactly

`Resolve-PrdFeatureWorkMode`'s regex, its legacy `full` normalisation, and `Get-PrdFeatureRequiredFile`'s mapping and `default` arm (`helpers:52`, `:58-61`, `:90-95`) are not edited. `full-feature` requires `spec.md` and `user-story.md`; `full-bug` requires `spec.md`; `minor-audit` requires neither; an absent or unrecognised marker denies on its own branch (`:406-418`), naming no prerequisite set and running no required-file probe.

#### 2.7.4 Genuine denies preserved

A prerequisite document that is truly absent under the resolved folder still denies with the existing reason `PRD_FEATURE_BLOCKED: resolved feature folder '<folder>' is missing: <list> (work mode: <mode>). ...` (`:434-436`), which contains neither resolution code. The work-mode deny likewise contains neither.

One non-identity path keeps a resolution code: the resolved folder does not exist beneath a resolved worktree that is not the session root (`:393-395`, guarded by the DD-8a conjunct). That is a resolution failure and not a document failure — the gate cannot confirm it is looking at the item it resolved — so it stays in the resolution family and keeps the ambiguity code. What changes at that site is the decision helper and one phrase of the detail: the `return` on `:394` moves from the deleted `Get-PrdFeatureAmbiguityDecision` to `Get-PrdFeatureTargetDecision` with the ambiguity accessor passed explicitly, so the emitted code is identical and the detail differs only in reading `resolved target worktree` where it read `derived target worktree`. `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1:481-497` (`It 'denies with the ambiguity reason when the folder is absent from the target root'`, asserting the code at `:495`) is a surviving row that requires exactly this, and it is not among [P9-T5]'s edits. When the resolved worktree is the session root the conjunct is false and an unreadable `issue.md` falls through to the work-mode branch, exactly as it does today. The gate therefore emits a resolution code on exactly three paths: unresolved identity, unresolved multi-candidate tie, and folder-absent-under-another-worktree. A named test asserts the two families never intersect.

#### 2.7.5 Regression case

A coordinating session delegates `atomic-planner` for an item whose feature folder also exists in other worktrees, which is the normal state once a folder has merged. Before this change the gate resolves folder-first, matches several worktrees, and denies `TARGET_WORKTREE_AMBIGUOUS` — observed 2026-09-18 with twelve candidate worktrees. After this change the prompt's canonical issue line resolves to the one live worktree whose checkpoint records that issue, the prerequisites are probed beneath it, and the call is allowed. [P9-T2] records the pre-change verdict deterministically and [P9-T9] row 1 asserts the post-change verdict.

#### 2.7.6 Files expected in the diff for #672

`.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, their two mirrors under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/`, the three existing suites `enforce-prd-feature-before-planner.Tests.ps1`, `enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`, `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`, the new suite `enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`, and `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`. `.claude/lib/worktree-resolution/WorktreeResolution.psm1` and `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` stay absent from the diff. No manifest edit is needed for #672: the two gate files are already registered in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json` at `:48-49`, and the two F1 module paths at `:169-170`, which is the pair RS-9 cites and after which [P2-T4] inserts the new module.

## 3. Reconciliation record (every restated acceptance criterion)

[P1-T2] copies this section into `spec.md` verbatim.

| ID | AC | Original wording assumed | Restatement |
| --- | --- | --- | --- |
| RS-1 | AC-9, AC-10, AC-13, AC-15, AC-17 | one "ambiguity reason code" | Two target-resolution codes exist: the no-target code (no identity, or an identity that places in no live worktree) and the ambiguity code (several live worktrees, or disagreeing identities). Tests obtain both only from `Get-WorktreeResolutionNoTargetReasonCode` and `Get-WorktreeResolutionAmbiguityReasonCode`. AC-15 treats "the ambiguity reason code" as the set of both. AC-17 asserts one case per unresolved state. |
| RS-2 | AC-13 rows R8, R9 | R8 "target resolves outside any worktree"; R9 "a command naming two worktrees" | Paths no longer place a call. R8 becomes "an identity that places in no live worktree" (no-target code). R9 becomes "identities that place in different worktrees": two issue numbers, an issue and a branch that disagree, or an issue recorded by two live worktrees (ambiguity code); never first-match. |
| RS-3 | AC-9, AC-10 row R2 | the sibling-only case denies with the ambiguity code | R2's payload carries no identity, so it denies with the no-target code. A second model-routing R2 row uses the verbatim prompt of `evidence/baseline/repro-3-4-control-pair.md:28`, whose only path token is a repository-relative file path beneath a feature folder, and also denies with the no-target code, because neither a folder nor a file path is an identity. |
| RS-4 | AC-11 row R4 | own and sibling both present, verdict from own (mechanism unstated) | Proven through identity: the prompt or command names the own item by issue or branch, both fixture checkpoints are live, and the verdict comes from the own checkpoint. A further row proves a branch label breaks a stale-attempt tie. |
| RS-5 | AC-12 | fail-before observable for both families | pr-author R2 already denies on this tree (#687). Satisfied by the archived pre-#687 `allow` (`evidence/baseline/repro-3-2-control-pair.md`, AC-1, AC-2), the dossier `evidence/regression-testing/fail-before-exception.2026-09-19T09-00.md`, and the substitute binding-2 row `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present`, which allows before the fix and denies after it. The model-routing half is observed directly. |
| RS-6 | AC-14 row R7 | empty or unparseable checkpoint at a resolved target | Asserted on targets resolved by branch (§2.2 row 9) or by the hook seam. Under issue-only identity an unreadable checkpoint cannot be matched to its issue, so that call denies with the no-target code; that is still a deny and is stated, not hidden. |
| RS-7 | AC-8 | no fallback to the process working directory | The working directory only locates the repository and labels `SessionRoot` (§2.2). It is never the checkpoint location unless identity resolution selects that worktree. For the prd gate the one remaining use of the process directory is DD-8's `SessionRoot` probe spelling, which names the resolved worktree itself. |
| RS-8 | AC-19 | protected rows at spec line numbers, edit forms (a) and (b) | #687 moved rows and renamed the end-to-end row (audited by [P0-T6]). This plan edits: form (a) at `enforce-pr-author-skill.epic-base-branch.Tests.ps1:34` and `:40`, which are the two calls inside AC-19's protected span `:22-42`, plus `:48`, `:58`, `:68`, `:78`, `:84`, which are outside it, and `enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1:38`, `:52`; form (c), one Describe-level `BeforeEach` in `enforce-model-routing-receipt.Tests.ps1` mocking `Resolve-ModelRoutingWorktreeTarget` to `SessionRoot` at `(Get-Location).Path`, the form #687 used on `main`; no edit to `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` (its end-to-end row resolves NoTarget before the pin at `:97` is read). |
| RS-9 | AC-21, AC-22 | F1 merged into an epic integration branch | F1 (#669) is on `main`. AC-21: both F1 module paths appear once in `core.json` `paths[]` (`:169-170`), `WorktreeResolution.Manifest.Tests.ps1` passes, and the commit adding `WorktreeTargetResolution.psm1` is an ancestor of `origin/main`. AC-22: the binding table is refreshed to current lines and gains the no-target rows and the identity-derivation row. |
| RS-10 | AC-23 | search the whole repository | Evaluated over files added or modified outside the feature folder, for both literals. Two #687 files carrying the literals are edited and cleaned: the doc comment at `helpers:74` and `:78`, and `enforce-pr-author-skill.TargetResolution.Tests.ps1`. Skill edits and every file Phase 9 touches must not carry either literal; the prd suites already obtain the code from the accessor. |
| RS-11 | AC-24 | F5 defines no target derivation; every use calls F1 | The identity derivation is a new resolution mode placed in the worktree-resolution library beside F1, re-using F1's enumeration, marker test, ascent, branch reader, join, constructor, and accessors. No hook file defines any derivation, normalisation, or reason code. |
| RS-12 | AC-25 | each hook imports F1's module | Each entry hook imports the library module it calls (`WorktreeItemResolution.psm1`) unconditionally at script scope, in the form its own file already uses: the pr-author helpers and the model-routing gate use `-Force -ErrorAction Stop`, and the prd-feature gate keeps the bare `-Force` form of `.claude/hooks/enforce-prd-feature-before-planner.ps1:108`, whose unguarded import is already documented as the fail-closed choice at `:104-107`. Both forms are unguarded, which is what AC-25 requires; neither is wrapped in `try` or softened with `SilentlyContinue`. That module imports F1's two modules with `-ErrorAction Stop`. `enforce-pr-author-skill.epic-base-branch.ps1` calls no library function (it receives the path) and gains no import. `enforce-prd-feature-before-planner.ps1` keeps its `WorktreeTargetResolution.psm1` import at `:108` and gains a second import of `WorktreeItemResolution.psm1`, because `Join-WorktreeResolutionPath` is exported only by the former (DD-4's scope note); `enforce-prd-feature-before-planner-helpers.ps1` keeps its `WorktreeResolution.psm1` import, which supplies both the normalisation `ConvertTo-PrdFeatureFolderToken` calls and, through the dot-source, the two reason-code accessors the parent calls. |
| RS-13 | AC-28 | diff against the epic integration branch | Diff against `F5_BASE_SHA`. |
| RS-14 | AC-30 | no file exceeds 500 lines | `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1` is 509 lines on `main`; [P6-T2] moves its value-contract block into a sibling file. The prd files are 456, 320, 499, 454, and 446 lines before Phase 9; DD-9's deletions give the gate file headroom, and the new identity rows go into a new suite rather than into the 499-line file. |
| RS-15 | AC-31 | four hooks | Coverage floor also applies to `WorktreeItemResolution.psm1` and to the two prd gate files. |
| RS-16 | AC-32, spec test construction | out-of-process fixture spawns | Fixture rows run in process with both `Set-Location` and `[Environment]::CurrentDirectory` set and restored in `finally` (spec trap 1 permits this), so their lines are coverage-instrumented. The prd suites are exempt from this mechanism entirely: they change no directory and touch no file (RS-19). |
| RS-17 | AC-1, AC-2 | verbatim decision JSON | [P1-T5] replaces host tokens in the archived runs with placeholders under the binding host-data rule; permission decisions and reason codes are unchanged, and a redaction log records per-file counts. |
| RS-18 | AC-33 | seven-stage loop for PowerShell | A Python test file changes ([P8-T5]), so format, lint, and type stages also run for it. |
| RS-19 | issue #672 spec, "The new suite creates no temporary file or directory..." | asserted of `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1` as delivered | Re-asserted over that suite as Phase 9 leaves it and over the new `enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`. Both keep bare-literal synthetic roots, supply the modelled directory as data on an injected result, create nothing, and change no directory. [P10-T7] is the checking task and [P11-T9] records the closure. |
| RS-20 | issue #672 spec, multi-candidate disambiguator | "re-specified against the new disambiguator, not deleted" | Re-specified a second time, against DD-7's resolved-worktree checkpoint. The row is edited, never deleted, and it still proves selection is not earliest-occurrence. |

## 4. Test construction

- **Committed fixtures cannot be real worktrees** (git does not track a `.git` component). Rows whose verdict depends on topology inject it: identity rows mock `Get-WorktreeItemLiveRoot` inside module `WorktreeItemResolution` (`Mock -ModuleName WorktreeItemResolution`), declaring fixture directories as live worktrees, while the identity module's checkpoint reads and the hook's checkpoint reader run unmocked against committed fixture files. Rows needing a state the hook reaches only through the hook seam mock `Resolve-PrAuthorWorktreeTarget`, `Resolve-ModelRoutingWorktreeTarget`, or `Resolve-PrdFeatureWorktreeTarget` with results built by `New-WorktreeResolutionTargetResult`.
- **Every hook-family matrix row names its working directory, and the default is a fixture root that carries the three artifact files.** `.gitignore:6` ignores `/artifacts`, which is root-anchored, so the repository-root `artifacts/` tree is untracked and absent on a clean checkout, while `tests/fixtures/worktree-resolution/<root>/artifacts/...` is tracked normally. The pr-author gate reads three paths relative to the process directory: `artifacts/pr_body_<N>.receipt.json` (check 2, `.claude/hooks/enforce-pr-author-skill-helpers.ps1:154`), `artifacts/pr_body_<N>.md` (check 4, `:177`), and the last-write time of `artifacts/pr_context.summary.txt` (check 5, `:205`, resolved through `enforce-pr-author-skill.ps1:137-141`). A row that leaves the working directory to the executing process therefore passes in a development worktree that happens to hold `/artifacts` and fails on a clean checkout, before ever reaching the behaviour it asserts.

  The rule: **every** pr-author and model-routing matrix row runs inside `Invoke-WorktreeResolutionFixtureCall` with an explicit `-WorkingDirectory`. Where the §7 row text names one, that is the value. Where it does not, the default is `tests/fixtures/worktree-resolution/pr-author/session-root` for a pr-author row and `tests/fixtures/worktree-resolution/model-routing/session-root` for a model-routing row. Both defaults exist in §4.1, and the pr-author default carries all three artifact files, so checks 2, 4, and 5 read committed bytes and `Get-PrContextArtifactExistence` (`enforce-pr-author-skill.ps1:53-64`) finds the context file for real. No row mocks `Get-PrContextArtifactExistence`, `Get-PrAuthorReceiptContent`, `Get-PrBodyFileBytes`, or `Get-PrContextSummaryLastWriteUtc`: the committed fixture removes the need, which is why a fixture root was chosen over a permitted mock. The `-ContextExists` parameter of `Get-PrAuthorBypassReason` (`helpers:246`) is likewise never supplied directly by a row; every row enters through the same entrypoint production uses, so the Case C branch (`helpers:307-310`) is exercised rather than bypassed.
- **Rows with no identity** (R2, R5, the relative-folder row) mock nothing: §2.2 row 2 returns before any enumeration or file read, so they are deterministic. "No mock" in those rows means no mock of the resolver, the checkpoint readers, or the artifact seams; it has never meant no fixture, and the working-directory rule above still applies to them.
- **AC-9 and AC-10.** Both R2 rows run with the working directory set to a committed session-root fixture holding a ready sibling checkpoint and mock neither the resolver nor any checkpoint reader.
- **In-process location control.** `Invoke-WorktreeResolutionFixtureCall` saves `(Get-Location).Path` and `[System.Environment]::CurrentDirectory`, sets both to the fixture directory, invokes the script block, and restores both in `finally`. Both must be set because `Get-PrBodyFileBytes` reads with `[System.IO.File]::ReadAllBytes`.
- **Import order in test files.** `BeforeAll` dot-sources the hook first, then imports `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `WorktreeResolution.psm1`, and `WorktreeTargetResolution.psm1` without `-Force`, so the test binds to the module instance the hook loaded when the hook loads one, and still loads it before the fix, when the hook does not. This rule binds the suites **this plan authors** — [P2-T3], [P4-T3], [P4-T4], [P8-T6], and [P9-T9] — each of which imports all three modules explicitly rather than relying on a sibling module's exports, for the reason recorded under DD-4: an explicit `Export-ModuleMember` list does not re-export a sibling module's functions.

  The four pre-existing suites this plan edits are a different case and no task adds an import to any of them. `enforce-prd-feature-before-planner.Tests.ps1` and `enforce-prd-feature-before-planner.FolderResolution.Tests.ps1` carry no `Import-Module` line at all yet call `New-WorktreeResolutionTargetResult` (`:13` and `:28`) and, in the second, `Get-WorktreeResolutionAmbiguityReasonCode` (`:126`, `:145`); `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1` imports two of the three at `:109-110` and, after [P9-T5] step 4, mocks `Resolve-WorktreeItemTarget` without importing its module; `enforce-pr-author-skill.TargetResolution.Tests.ps1` imports nothing and calls both accessors after [P5-T5]. All four resolve those commands through the hook and helpers they dot-source, which is sound for the same reason the fixture helper is: an `Import-Module` inside a dot-sourced file lands in the caller's session state, so the command is present without the suite importing it. Adding imports would also contradict [P9-T6]'s changed-line condition and [P9-T7]'s touched-region condition, so the plan deliberately does not.

  `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` is the same case: it calls `New-WorktreeResolutionTargetResult` and imports nothing, resolving through the importing suite's session state because it is dot-sourced rather than imported.
- **Synthetic roots** (unit and path-capture rows) are composed at run time as the path root of `$PSScriptRoot` joined with `f5-seam-root/<name>`; no test file contains a drive letter or absolute host path.
- **The prd-feature suites are the one exception to the preceding bullet, and deliberately so.** Issue #672's spec requires that suite to derive no absolute path from the environment, the current directory, the script file location, or a source-control query, and to model the current directory as data on an injected result. Its synthetic roots stay the bare literals `/synthetic-worktrees/coordinating-session` and `/synthetic-worktrees/item-worktree`, which name no host and satisfy the host-data rule. The new identity suite follows the same convention; where a row needs a family of roots it composes them from a bare literal and a loop index, for example `'/synthetic-worktrees/item-worktree-{0}' -f $i`, which is still a literal-derived value and reads nothing from the host. Neither suite sets a location, and `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` is not used by either.
- **No temporary files.** All fixture content is committed under `tests/fixtures/worktree-resolution/`. No test creates a file or directory, and none uses `TestDrive`, `New-TemporaryFile`, or a temp path.
- **Receipts.** Fixture receipt `created_at` is `2099-01-01T00:00:00Z`, strictly newer than any checkout time; `.gitattributes:1` (`* text=auto eol=lf`) keeps body bytes LF, so the recorded SHA-256 holds.
- **Schema coupling.** pr-author fixture checkpoints derive from the checkpoint at `evidence/baseline/repro-fixture-manifest.md:53-100`, observed to pass `Invoke-OrchestratorStatePreflight`. `tests/fixtures/worktree-resolution/README.md` and the head comment of the pr-author matrix file name `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`.

### 4.1 Fixture map (roots relative to `tests/fixtures/worktree-resolution/`)

| Root | Checkpoint `issue-num` | Content |
| --- | --- | --- |
| `pr-author/session-root` | `838` | base JSON; `artifacts/pr_context.summary.txt`; `artifacts/pr_body_1.md`; `artifacts/pr_body_1.receipt.json` |
| `pr-author/item-own-ready` | `901` | base JSON with `objective` `Own item B fixture.`; the same three artifact files |
| `pr-author/item-own-not-ready` | `901` | base JSON with `step5_status` `pending` |
| `pr-author/item-own-epic-mode` | `901` | base JSON plus `"epic_mode": true` and `"epic_context": {"integration_branch": "epic/f5-fixture-integration"}` |
| `model-routing/session-root` | `838` | `{"issue-num":"838","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `model-routing/item-own-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `model-routing/item-own-no-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"feature-review"}]}` |
| `model-routing/item-stale-receipt` | `901` | `{"issue-num":"901","model_routing_receipts":[{"agent":"atomic-planner"}]}` |
| `shared/item-own-invalid-json` | unknown | checkpoint `{ this is not valid json` |
| `shared/item-own-empty` | unknown | checkpoint of zero bytes |
| `shared/item-no-checkpoint` | none | `artifacts/pr_context.summary.txt` only |

Fixture branch names used only inside mocks: `f5-fixture-own`, `f5-fixture-sibling`, `f5-fixture-missing`. Issue `902` is recorded by no fixture. The prd-feature suites consume no fixture root: every seam they need is a function mock.

## 5. Derivation of asserted values (fixed; not left to the executor)

- **MCP PoshQC tools** return a summary composed before the child runs and no child output. No acceptance condition reads a value from them; every count comes from invoking the module directly.
- **Fresh process.** Every PowerShell toolchain run is a fresh `pwsh -NoProfile -File <SCRATCHPAD>/<script>.ps1` process (route `a`), or, where a direct `pwsh` command is refused, a bare `sh <SCRATCHPAD>/<script>.sh` whose body is `pwsh -NoProfile -File "$(dirname "$0")/<script>.ps1"` (route `a-prime`), per [P0-T7]. The script body begins `$ErrorActionPreference = 'Stop'`, `$root = (Get-Location).Path`, `Import-Module ./scripts/powershell/PoshQC/PoshQC.psd1 -Force`, then the task's command with `-Root $root`. A reused interactive session is not a valid route.
- **Formatter.** `Invoke-PoshQCFormat` logs `Formatted: <path>` per rewrite and `Already formatted: <path>` otherwise (`scripts/powershell/PoshQC/PoshQC.Analyzer.psm1:60-65`). Observation: the count of lines beginning `Formatted: `, the count beginning `Already formatted: `, and `git status --porcelain` before and after.
- **Analyzer.** `Invoke-PoshQCAnalyze -Root $root` runs with Error, Warning, and Information severities (`PoshQC.Analyzer.psm1:101`) and prints `PSScriptAnalyzer passed: no findings under <root>` on success (`:185`); otherwise it prints a table and throws `PSScriptAnalyzer reported <N> issue(s).` (`:181-183`). `Invoke-ScriptAnalyzer` with a severity filter is never used.
- **Pester.** `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` writes JUnit to `artifacts/pester/pester-junit.xml` (`pester.runsettings.psd1:15`) and CoverageGutters coverage to `artifacts/pester/powershell-coverage.xml` (`:21-22`); `CoveragePercentTarget = 0` (`:298`), so the 85% floor is an explicit comparison. A `testsuite` is matched by a `name` attribute ending with the test file's base filename; a `testcase` by a `name` attribute ending with the quoted `It` name; zero matching `testcase` elements is a failure. Testsuite passed count = `tests - failures - skipped - disabled`. Root failures come from the `testsuites` element's `failures` and `errors`. Every task reading a report records its last-write time and requires it at or after the task's `Timestamp:`.
- **Coverage.** Per file: the `sourcefile` whose `name` equals the base filename inside the `package` whose `name` ends with `.claude/hooks` or `.claude/lib/worktree-resolution`; percent = `covered / (covered + missed) x 100` from `counter type="LINE"`, raw counters recorded. Changed-line coverage: an added line number carrying a `line` element counts as covered when its `ci` attribute is greater than 0.
- **Baseline failures.** If [P0-T10] records failing testcases, every root `failures 0` condition before Phase 11 excludes exactly those names, which the artifact lists. [P11-T4] applies no exclusion: a baseline failure still present stops execution and is reported, because CI fails on it.
- **Python.** `poetry run pytest -q` final summary line (`<N> passed`, plus `failed` or `error` counts when present). `poetry run black --check <file>` prints `would be left unchanged` on success; `poetry run ruff check <file>` prints `All checks passed!`; `poetry run pyright <file>` prints `0 errors`. No Python production file changes, so no Python coverage obligation exists.

## 6. PowerShell batch-budget schedule

`.claude/hooks/enforce-powershell-batch-budget.ps1` denies a fourth distinct production or test PowerShell file written through Write or Edit in one session (`:10-11`) and counts `.psd1` as production (`:34`). A reset deletes every `.claude/state/powershell-batch-budget.*.json` in the executing worktree and appends an entry to `evidence/other/r3-batch-budget-resets.md` (file names deleted, or `none present`); a refused deletion is recorded verbatim and execution stops. Mirror copies are made with `Copy-Item -LiteralPath <source> -Destination <mirror> -Force` through a shell, so they consume no slot and are byte copies.

| Batch | Production files | Test files |
| --- | --- | --- |
| B1 ([P2]) | `WorktreeItemResolution.psm1`, `pester.runsettings.psd1` | `WorktreeItemResolution.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1` |
| B2 ([P4]) | none | `WorktreeResolutionFixture.Helpers.ps1`, `enforce-pr-author-skill.WorktreeResolution.Tests.ps1`, `enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` |
| B3 ([P5]) | `enforce-pr-author-skill.ps1`, `enforce-pr-author-skill-helpers.ps1`, `enforce-pr-author-skill.epic-base-branch.ps1` | `enforce-pr-author-skill.TargetResolution.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1` |
| B4 ([P6]) | none | `OrchestratorState.Tests.ps1`, `OrchestratorState.ValueContract.Tests.ps1` |
| B5 ([P7]) | `enforce-model-routing-receipt.ps1` | `enforce-model-routing-receipt.Tests.ps1`, `enforcement-hooks-checkpoint-path-explicit.Tests.ps1` |
| B6 ([P8]) | none | `checkpoint-hygiene-skill-contract.Tests.ps1` |
| B7 ([P9]) | `enforce-prd-feature-before-planner.ps1`, `enforce-prd-feature-before-planner-helpers.ps1` | `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`, `enforce-prd-feature-before-planner.Tests.ps1`, `enforce-prd-feature-before-planner.FolderResolution.Tests.ps1` |
| B8 ([P9]) | none | `enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`, `enforcement-hooks-checkpoint-path-explicit.Tests.ps1` |

## 7. Named Pester tests (exact `It` names)

`tests/scripts/claude-runtime/test-name-uniqueness.Tests.ps1` requires distinct names per parent scope, including expanded `-ForEach` rows.

`tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1` (in process; topology through mocks of `Get-WorktreeItemLiveRoot`, `Get-WorktreeItemCheckpointText`, `Find-WorktreeResolutionRoot`, `Get-WorktreeResolutionWorktreeRoot`, and `Test-WorktreeResolutionRootMarker` inside module `WorktreeItemResolution`):
1. `returns the repository-relative orchestrator checkpoint path`
2. `matches the default checkpoint path of Invoke-OrchestratorStatePreflight` (parses `.claude/lib/orchestrator-state/OrchestratorState.psm1` with `[System.Management.Automation.Language.Parser]::ParseFile` and compares the `CheckpointPath` parameter default of `Invoke-OrchestratorStatePreflight`)
3. `composes the absolute checkpoint path under a worktree root`
4. `reads every distinct issue number from canonical issue lines`
5. `returns no issue number for text without a canonical issue line`
6. `normalises string and integer issue-num values`
7. `rejects zero, none, empty, and non-numeric issue-num values`
8. `reads the issue number from a worktree checkpoint through the checkpoint text seam`
9. `returns no issue number for an absent, empty, or unparseable checkpoint`
10. `keeps only registered worktrees that still carry a root marker`
11. `returns no live worktree when the session path is inside no repository`
12. `resolves NoTarget without enumerating worktrees when the text carries no identity`
13. `resolves the single live worktree whose checkpoint records the issue`
14. `resolves NoTarget when no live worktree records the issue`
15. `resolves Ambiguous when two live worktrees record the issue`
16. `breaks a stale-attempt tie with the branch signal`
17. `resolves Ambiguous when the branch worktree records a different issue`
18. `resolves Ambiguous when the branch and the issue name different worktrees`
19. `resolves the branch worktree when its checkpoint records no issue and no other worktree records it`
20. `resolves Ambiguous when the text names two different issue numbers`
21. `resolves the branch worktree when only a branch signal is present`
22. `resolves NoTarget when the branch is checked out in no live worktree`
23. `ignores a feature-folder path and resolves NoTarget when it is the only signal`
24. `resolves by branch when a relative feature folder and a unique branch are both present`
25. `labels the result SessionRoot when the resolved worktree is the session root`
26. `carries the no-target and ambiguity codes supplied by the worktree-resolution accessors`
27. `names no absolute path in the Detail of any result`

`tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.Tests.ps1` (all in process; "identity" = real `Resolve-WorktreeItemTarget` with mocked `Get-WorktreeItemLiveRoot`; "seam" = mocked `Resolve-PrAuthorWorktreeTarget`):
1. `pr-author R1 allows when the resolved own checkpoint is ready and the working directory is the sibling session root` (seam OtherWorktree `pr-author/item-own-ready`; cwd `pr-author/session-root`)
2. `pr-author R1 allows when the branch locates the own ready checkpoint from the sibling session root` (identity `--head f5-fixture-own`)
3. `pr-author R2 denies with the no-target code when only the sibling session-root checkpoint is present` (no mock; cwd `pr-author/session-root`; command `gh pr create --title "B" --body-file artifacts/pr_body_1.md`)
4. `pr-author R3 denies with the preflight reason when the resolved own checkpoint is not ready` (identity to `pr-author/item-own-not-ready`)
5. `pr-author R3 denies with the preflight reason when the checkpoint is absent at the resolved target` (identity to `shared/item-no-checkpoint`)
6. `pr-author R4 takes the preflight verdict from the own checkpoint located by branch when the sibling checkpoint is ready` (identity; live roots `pr-author/session-root` and `pr-author/item-own-not-ready`)
7. `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present` (seam OtherWorktree `pr-author/item-own-epic-mode`; **cwd `pr-author/session-root`**; command `gh pr create --head f5-fixture-own --base main --title "B" --body-file artifacts/pr_body_1.md`; substitute fail-before). The working directory is stated explicitly here rather than defaulted, because this row is the only direct fail-before/pass-after evidence for AC-12's binding-2 half (RS-5) and it is the one row that must traverse checks 1 to 5 to reach check 6 (`Test-EpicBaseBranchOverride`, `helpers:211`). `pr-author/item-own-epic-mode` carries no artifact files, so resolving the target there while running in `pr-author/session-root` is what lets the row read the committed receipt, body, and context-summary bytes from the working directory and take the epic verdict from the resolved checkpoint.
8. `pr-author R5 denies with the no-target code when the command names no target` (no mock; cwd `shared/item-no-checkpoint`; command `gh pr create --title "B" --body-file artifacts/pr_body_1.md` with no `--head` and no identity in the text). The working directory is stated rather than defaulted so this row stays distinct from row 3: `shared/item-no-checkpoint` holds `artifacts/pr_context.summary.txt` and no checkpoint at all, so Case C is satisfied and the deny cannot come from a checkpoint the row did not intend to consult. §2.2 row 2 returns before any enumeration or file read, so checks 2 to 5 are never reached and the root's lack of a body file and receipt is immaterial.
9. `pr-author R6 allows when the working directory is the item worktree and its own checkpoint is ready` (seam SessionRoot `pr-author/item-own-ready`; cwd `pr-author/item-own-ready`)
10. `pr-author R7 denies with the preflight reason when the resolved checkpoint is unparseable` (identity to `shared/item-own-invalid-json`)
11. `pr-author R7 denies with the preflight reason when the resolved checkpoint is empty` (identity to `shared/item-own-empty`)
12. `pr-author R8 denies with the no-target code when the branch is checked out in no live worktree` (identity `--head f5-fixture-missing`)
13. `pr-author R9 denies with the ambiguity code when the branch is checked out in two live worktrees` (identity; the mock returns two roots for the branch)
14. `pr-author R10 allows a command that is not a gated gh pr invocation when the target is unresolvable`
15. `pr-author genuine-absence and target-resolution reason codes never appear in each other's decisions`
16. `pr-author denies an unresolved <Status> target without reaching the orchestrator-state preflight`, `-ForEach` over `NoTarget` and `Ambiguous` (seam; `Invoke-OrchestratorStatePreflight` mocked to throw)
17. `pr-author passes one resolved checkpoint path to both the preflight and the epic base-branch check` (seam with a synthetic root; both readers mocked to capture `-CheckpointPath`; expected value is the normalised synthetic root followed by `/artifacts/orchestration/orchestrator-state.json`)

`tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` (prompt `P901` = `Plan the change for the item.` followed by a line `Canonical issue number for this feature is 901. All artifact content, file paths, and cross-references must use this number.`):
1. `model-routing R1 allows when the resolved own checkpoint records the receipt` (identity `P901`; live `model-routing/session-root`, `model-routing/item-own-receipt`; cwd `model-routing/session-root`)
2. `model-routing R2 denies with the no-target code when only the sibling session-root checkpoint is present` (no mock; cwd `model-routing/session-root`; prompt `Plan the change for the item.`)
3. `model-routing R2 denies with the no-target code when the only signal is a repository-relative path` (no mock; prompt = the prompt at `evidence/baseline/repro-3-4-control-pair.md:28`, whose only path token is a repository-relative file path to a `spec.md` beneath a feature folder, not a folder token; the verdict is the same under §2.2 row 2 either way, and the name is worded to match the pinned prompt rather than to describe a folder it does not carry)
4. `model-routing R3 denies with the blocked reason when the resolved own checkpoint records no receipt` (identity `P901` to `model-routing/item-own-no-receipt`)
5. `model-routing R3 denies with the blocked reason when the checkpoint is absent at the resolved target` (identity `branch: f5-fixture-own` to `shared/item-no-checkpoint`)
6. `model-routing R4 takes the verdict from the own checkpoint located by issue number when the sibling checkpoint records the receipt` (identity `P901`; live `model-routing/session-root`, `model-routing/item-own-no-receipt`; cwd `model-routing/session-root`)
7. `model-routing R4 selects the branch-named worktree when a stale attempt records the same issue` (identity `P901` plus `branch: f5-fixture-own`; live `model-routing/session-root`, `model-routing/item-stale-receipt`, `model-routing/item-own-no-receipt`; branch maps to `item-own-no-receipt`; expects the blocked reason)
8. `model-routing R5 denies with the no-target code when the prompt names no target`
9. `model-routing R6 allows when the working directory is the item worktree and its own receipt is present` (identity `P901`; live `model-routing/item-own-receipt`; cwd the same)
10. `model-routing R7 denies with the blocked reason when the resolved checkpoint is unparseable` (identity branch to `shared/item-own-invalid-json`)
11. `model-routing R7 denies with the blocked reason when the resolved checkpoint is empty` (identity branch to `shared/item-own-empty`)
12. `model-routing R8 denies with the no-target code when the issue is recorded in no live worktree` (prompt names issue 902)
13. `model-routing R9 denies with the ambiguity code when the issue and the branch name different worktrees` (`P901` plus `branch: f5-fixture-sibling` mapped to `model-routing/session-root`)
14. `model-routing R9 denies with the ambiguity code when two live worktrees record the issue` (`P901`; live `item-stale-receipt` and `item-own-no-receipt`)
15. `model-routing R10 allows a subagent outside the gated set when the target is unresolvable`
16. `model-routing genuine-absence and target-resolution reason codes never appear in each other's decisions`
17. `model-routing denies an unresolved <Status> target without reaching the checkpoint read`, `-ForEach` over `NoTarget` and `Ambiguous` (seam; `Get-ModelRoutingCheckpoint` mocked to throw)
18. `model-routing reads the checkpoint at the resolved absolute path for a <Status> target`, `-ForEach` over `SessionRoot` and `OtherWorktree` (seam with a synthetic root; reader mocked to capture)

`tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1`. The list below carries eight names. [P7-T4] creates the file with the seven that are not the `Get-PrdFeatureCheckpointFolder` row; [P9-T10] adds that row as the eighth and widens the literal test's file set from four to five. No row of this file is ever authored before the production change it asserts, so no interval exists in which the file is expected to fail.
- `every production call to Invoke-OrchestratorStatePreflight supplies CheckpointPath explicitly`
- `every production call to Get-PrAuthorCheckpointContent supplies CheckpointPath explicitly`
- `every production call to Get-ModelRoutingCheckpoint supplies CheckpointPath explicitly`
- `every production call to Test-EpicBaseBranchOverride supplies CheckpointPath explicitly`
- `every production call to Test-PrAuthorReceiptVerification supplies CheckpointPath explicitly`
- `the in-scope hook files carry the checkpoint path literal in no string expression`
- `every production call to Get-PrdFeatureCheckpointFolder supplies CheckpointPath explicitly` (added by [P9-T10])
- `the orchestrator-state module keeps its four hundred ninety-nine line count`

`tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1` (reads skill text with whitespace collapsed, section extracted by heading):
- `orchestrate skill archives a foreign per-feature checkpoint to the handoff folder`
- `parallel-orchestrate skill keeps the coordinator root free of a per-feature checkpoint`
- `epic-orchestrate skill keeps the coordinator root free of a per-feature checkpoint`
- `orchestrate skill requires the canonical issue line on every receipt-gated delegation`
- `orchestrate skill requires a branch label on every receipt-gated delegation`
- `orchestrate skill names every subagent type the model-routing gate receipt-gates` (dot-sources `.claude/hooks/enforce-model-routing-receipt.ps1` and compares `Get-ModelRoutingGatedAgent` with the names in the `## Issue Number Consistency` rule)
- `orchestrate skill names both gates that identify the item from the delegation prompt`

`tests/scripts/claude-hooks/enforce-pr-author-skill.TargetResolution.Tests.ps1` renames ([P5-T5]): `denies with TARGET_WORKTREE_NOT_DERIVABLE when the call names no target` becomes `denies with the no-target code when the call names no target`; `denies with TARGET_WORKTREE_AMBIGUOUS when signals disagree` becomes `denies with the ambiguity code when signals disagree`; `uses the session-root path unchanged when the target resolves to the session root` becomes `uses the absolute session-root checkpoint path when the target resolves to the session root`.

`tests/scripts/claude-hooks/enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1` (new in [P9-T9]; synthetic roots are the two bare literals of §4; "identity" = real `Resolve-WorktreeItemTarget` with `Get-WorktreeItemLiveRoot` and `Get-WorktreeItemCheckpointText` mocked inside module `WorktreeItemResolution`; "seam" = mocked `Resolve-PrdFeatureWorktreeTarget`):
1. `prd R1 allows a coordinating-session delegation whose item is identified by issue number while the folder exists in twelve worktrees` (identity; twelve live roots, one recording the issue; prerequisite present beneath it)
2. `prd R2 denies with the no-target code when the prompt carries neither an issue number nor a branch`
3. `prd R3 denies with the ambiguity code when two live worktrees record the issue`
4. `prd R4 resolves by branch when a stale attempt records the same issue`
5. `prd R5 reads the feature folder from the checkpoint of the resolved worktree when the prompt names none` (seam OtherWorktree; `Get-PrdFeatureCheckpointFolder` mocked to capture `-CheckpointPath`)
6. `prd R5 never composes a checkpoint path under the coordinating session root` (same capture; asserts the captured path begins with the item worktree root)
7. `prd R6 denies with the missing-document reason when a prerequisite is absent beneath the resolved worktree`
8. `prd R7 ignores a feature-folder path when choosing the worktree` (identity; the prompt's only path token is a relative folder and the issue line names the item)
9. `prd R8 selects among two cited folders with the checkpoint of the resolved worktree`
10. `prd R8 denies with the ambiguity code when the resolved checkpoint names neither cited folder`
11. `prd R9 denies with the work-mode reason when the marker is absent from the resolved folder`
12. `prd resolution-family and document-family reasons never appear in each other's decisions`
13. `prd lets no feature-folder path and no file path select the worktree` (the prompt carries a relative folder token, an absolute file path, and one canonical issue line; `Get-WorktreeItemLiveRoot` is mocked so the folder token would place in several roots and the issue in one; asserts the resolved root is the issue's, that the resolver's `Signal` is not `FeatureFolderPath` or `FilePath`, and that `Get-PrdFeatureCallTarget` declares no `Envelope` parameter). The resolver does receive the whole assembled text, including any path token in it, because DD-4 hands it the prompt and description unchanged; what this row pins is §2.3's guarantee that no path signal is ever read as a worktree selector.

`tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1` after [P9-T5]. Removed: `treats a call with no derived target as resolving at the session root`, `treats a NoTarget and a SessionRoot result as resolving at the session root`, `treats another worktree as not the session root` (their subject is deleted by DD-9); `supplies the session root from the envelope cwd when the runtime sets one` and `omits the session root when the envelope carries no cwd field` (their subject is deleted by DD-4). Renamed: `derives nothing from a call whose text is empty` becomes `resolves no target from a call whose text is empty`; `hands a repo-relative citation to the derivation` becomes `hands the assembled prompt and description text to the identity resolver`; `hands a repo-relative citation carrying a branch signal to the derivation` becomes `hands a branch label to the identity resolver`; `denies with the ambiguity reason when a repo-relative citation places in no worktree` becomes `denies with the no-target code when the identity places in no live worktree`; `denies rather than validating against a sibling session checkpoint` becomes `reads the checkpoint of the resolved worktree rather than the session root's`. Added: `supplies the process location as the session root and reads no envelope cwd`. Every other `It` name in the file is unchanged.

---

### Phase 0 — Policy Reading, Current-Tree Verification, and Baseline Capture

- [x] [P0-T1] Read, in order, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/powershell.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/tonality.md`, `.claude/rules/orchestrator-state.md`, `.claude/rules/plan-acceptance-gates.md`, and test whether `quality-tiers.yml` exists at the repository root. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-instructions-read.md` with `Timestamp:`, `Policy Order:` (the ten paths in order read), `Files Read:` (the same ten), and one line `QUALITY_TIERS_YML: present` or `QUALITY_TIERS_YML: absent`. Acceptance: three labels present, ten entries under `Files Read:`, exactly one `QUALITY_TIERS_YML:` line.
- [x] [P0-T2] Read `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`, `.../issue.md`, `.../research/2026-09-13T22-10-false-approval-elimination-research.md`, `.../plan.2026-09-13T20-48.md`, `.../plan.2026-09-18T13-30.md`, `.../plan.2026-09-18T16-00.md`, this plan, and `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-feature-documents-read.md` with `Timestamp:`, `Files Read:` (eight paths), `AC Inventory:` listing the identifiers under the #673 `spec.md` `## Acceptance Criteria`, and `Issue 672 Unchecked:` giving the line number and full text of every `- [ ]` line in the #672 spec that sits below its `## Acceptance Criteria` heading. Acceptance: exactly 33 identifiers, `AC-1` through `AC-33`; exactly two entries under `Issue 672 Unchecked:`, one beginning `The new suite creates no temporary file or directory` and one beginning `The PowerShell toolchain`.
- [x] [P0-T3] Record base and scope. Run `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, `git merge-base HEAD origin/main`, then `git diff --name-only <F5_BASE_SHA> HEAD` with the merge-base value substituted, then `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-base-ref.md` with the four schema fields, lines `F5_BRANCH:`, `F5_HEAD_SHA:`, `F5_BASE_SHA:`, and `Pre-Change Diff:` and `Porcelain:` sections holding both outputs verbatim. No toplevel path is recorded. Acceptance: `F5_BRANCH:` reads `bug/false-approval-elimination-673`; `F5_BASE_SHA:` is 40 hexadecimal characters; every path in both sections begins with `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/`; `Porcelain:` lists `plan.2026-09-19T09-00.md`.
- [x] [P0-T4] Verify AC-1 to AC-3 on the branch. Confirm `evidence/baseline/repro-3-2-control-pair.md`, `evidence/baseline/repro-3-4-control-pair.md`, `evidence/baseline/repro-fixture-manifest.md`, and `evidence/baseline/repro-verdict.md` exist under the feature folder; count lines of `repro-verdict.md` equal to `REPRODUCTION: CONFIRMED`; run `git log --format=%H --diff-filter=A <F5_BASE_SHA>..HEAD -- docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/repro-verdict.md` and `git log --format=%H <F5_BASE_SHA>..HEAD -- .claude/hooks`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-reproduction-evidence-audit.md` with the four schema fields, the four existence results, the token count, `EVIDENCE_COMMIT:` (first log output), and `HOOK_COMMITS_ON_BRANCH:` (second log output, or `none`). Acceptance: four files exist; token count 1; `EVIDENCE_COMMIT:` is one 40-hexadecimal SHA; `HOOK_COMMITS_ON_BRANCH:` reads `none`.
- [x] [P0-T5] Re-verify the cited current-tree facts with `Get-Content` line indexing and write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-current-tree-facts.md` (four schema fields) quoting each line. Lines and required contained tokens, in order: `.claude/hooks/enforce-pr-author-skill.ps1:49` `$script:OrchestratorStateCheckpointPath =`; `.claude/hooks/enforce-pr-author-skill-helpers.ps1:38` `WorktreeResolution.psm1`; `:39` `WorktreeTargetResolution.psm1`; `:61` `Resolve-WorktreeCallTarget`; `:96` `CheckpointPath = $script:OrchestratorStateCheckpointPath`; `:99` `-RepoRelativePath $script:OrchestratorStateCheckpointPath`; `:211` `Test-EpicBaseBranchOverride -CommandText $CommandText`; `:315` `Get-PrAuthorTargetCheckpointResolution`; `:320` `Invoke-OrchestratorStatePreflight -CheckpointPath`; `:333` `Test-PrAuthorReceiptVerification -CommandText $CommandText`; `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1:35` `$CheckpointPath = 'artifacts`; `:78` `$checkpointRaw = Get-PrAuthorCheckpointContent`; `.claude/hooks/enforce-model-routing-receipt.ps1:36` `HookPayload.psm1`; `:46` `$CheckpointPath = 'artifacts`; `:157` `$checkpoint = Get-ModelRoutingCheckpoint`; `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1:103` `'Branch', 'SessionRoot'`; `:164` `function Find-WorktreeResolutionBranchSignal`; `.claude/lib/worktree-resolution/WorktreeResolution.psm1:228` `function Test-WorktreeResolutionRootMarker`; `:334` `function Get-WorktreeResolutionWorktreeRoot`; `.claude/hooks/enforce-prd-feature-before-planner.ps1:108` `WorktreeTargetResolution.psm1`; `:160` `$CheckpointPath = 'artifacts`; `:181` `function Test-PrdFeatureSessionRootTarget`; `:278` `Resolve-WorktreeCallTarget`; `:337` `$target.Status -eq 'Ambiguous'`; `:353` `$folder = Get-PrdFeatureCheckpointFolder`; `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1:27` `WorktreeResolution.psm1`; `:237` `$Target.SignalValue`; `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.Tests.ps1:13` `Resolve-WorktreeCallTarget`; `:208` `Get-PrdFeatureCheckpointFolder -CheckpointPath`; `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1:28` `Resolve-WorktreeCallTarget`; `:105` `-Target $target`; `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1:169` `Test-PrdFeatureSessionRootTarget`; `.claude/skills/orchestrate/SKILL.md:258` `Every delegation prompt to`; `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1:312` `$script:OrchestratorStateCheckpointPath = 'artifacts`; `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1:97` `$script:OrchestratorStateCheckpointPath = 'artifacts`; `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py:129` `.claude/skills/epic-orchestrate/SKILL.md`. Also record `@(Get-Content).Count` of `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`, `.claude/hooks/enforce-model-routing-receipt.ps1`, `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`, `.claude/lib/orchestrator-state/OrchestratorState.psm1`, `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.Tests.ps1`. Acceptance: every quoted line contains its token; the counts are 312, 340, 115, 180, 500, 347, 499, 509, 456, 320, 499, 454, 446 in that order. Any mismatch stops execution and is reported as a plan-revision requirement.
- [x] [P0-T6] Audit the seven AC-19 rows against #687. Run `git log --format=%H -S Resolve-PrAuthorWorktreeTarget <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`, take the last SHA printed as the #687 commit, and for `tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` and `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1` compare `git show <that commit>^:<file>` with `git show <that commit>:<file>`; for `tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1` run `git log --format=%H <F5_BASE_SHA> -1 -- tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1` and record that it differs from the #687 commit. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-ac19-row-audit.md` (four schema fields) with one row per protected row: spec location, current location, every current `It` name in the span, and `unchanged by #687` or the verbatim before and after text. [P11-T4] reads the `It` names from this artifact. Acceptance: seven rows; the end-to-end row records a pre-#687 name ending `(exit 0, deny, ORCHESTRATOR_STATE_PREFLIGHT_FAILED)` and a current name ending `(exit 0, deny, TARGET_WORKTREE_NOT_DERIVABLE)`; both #687 `BeforeEach` blocks quoted verbatim.
- [x] [P0-T7] Establish the fresh-process route. Write `<SCRATCHPAD>/r3-probe.ps1` printing `PSVersion: <$PSVersionTable.PSVersion>` and `LocationIsWorktreeRoot: <bool>`, where the bool compares `(Get-Location).Path` with the output of `git rev-parse --show-toplevel` after normalising both to forward slashes (neither value is printed). Attempt route `a` (`pwsh -NoProfile -File <SCRATCHPAD>/r3-probe.ps1`); if refused, record the refusal with host tokens replaced and attempt route `a-prime` (`sh <SCRATCHPAD>/r3-probe.sh`, body per §5). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-execution-route.md` (four schema fields) with exactly one `ROUTE_SELECTED: a` or `ROUTE_SELECTED: a-prime` line and the probe output. Acceptance: one `ROUTE_SELECTED:` line; the probe prints `LocationIsWorktreeRoot: True`. If both routes are refused, record `ROUTE_SELECTED: none`, stop, and report the blocked state.
- [x] [P0-T8] Formatter baseline. Record `git status --porcelain`; in a fresh process run `Invoke-PoshQCFormat -Root $root` per §5 with `scripts/powershell/PoshQC/PoshQC.psd1` imported; record `git status --porcelain` again. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-poshqc-format.md` (four schema fields) with the count of lines beginning `Formatted: `, the count beginning `Already formatted: `, and a `Tree Delta:` section with both porcelain outputs. If any tracked file was rewritten, revert each with `git checkout -- <path>`, list the repository-relative paths, and record a third porcelain output. Acceptance: both counts are numbers; the `Already formatted: ` count is greater than 0; the final porcelain output equals the first.
- [x] [P0-T9] Analyzer baseline. In a fresh process run `Invoke-PoshQCAnalyze -Root $root` per §5 with `scripts/powershell/PoshQC/PoshQC.psd1` imported. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-poshqc-analyze.md` (four schema fields) with either the line beginning `PSScriptAnalyzer passed: no findings under` (root replaced by `<WORKSPACE_ROOT>`) or the `PSScriptAnalyzer reported <N> issue(s).` line and the findings table with host tokens replaced. Acceptance: exactly one outcome recorded, `N` numeric when findings exist, and the output does not contain `No PowerShell files found under`.
- [x] [P0-T10] Full Pester baseline with coverage. In a fresh process run `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` with `scripts/powershell/PoshQC/PoshQC.psd1` imported. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pester-coverage.md` (four schema fields) with, per §5: report last-write time; root `tests`, `failures`, `errors`, `disabled`; the name of every `testcase` carrying a `failure` child (or `none`); `tests`, `failures`, `skipped`, `disabled`, and passed count for the testsuites ending `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, `enforcement-hooks-no-python-invocation.Tests.ps1`, `OrchestratorState.Tests.ps1`, `enforce-model-routing-receipt.Tests.ps1`, `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1`, `enforce-pr-author-skill.epic-base-branch.Tests.ps1`, `enforce-prd-feature-before-planner.Tests.ps1`, `enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`, `enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`; overall line coverage; and per-file line coverage (percent, `covered`, `missed`) and `line`-element count for the four in-scope hooks of §2.6 and for `.claude/hooks/enforce-prd-feature-before-planner.ps1` and `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`. Acceptance: last-write time at or after `Timestamp:`; all ten testsuites present; six per-file percentages, one overall percentage, twelve raw counters, and six `line`-element counts recorded as numbers. A non-zero failure count is recorded, not waived.
- [x] [P0-T11] Python suite baseline. Run `poetry run pytest -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pytest.md` (four schema fields) with the final summary line verbatim and the node ID of every failed or errored test (or `none`). Acceptance: the summary line is recorded with a numeric passed count.
- [x] [P0-T12] Python format baseline for the one Python file this plan edits. Run `poetry run black --check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-black.md` (four schema fields) with the output verbatim. Acceptance: `EXIT_CODE: 0` and the output contains `would be left unchanged`.
- [x] [P0-T13] Python lint baseline. Run `poetry run ruff check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-ruff.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `All checks passed!`.
- [x] [P0-T14] Python type baseline. Run `poetry run pyright tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-pyright.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `0 errors`.
- [x] [P0-T15] Mirror baseline. Compute `Get-FileHash -Algorithm SHA256` for `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`, `.claude/hooks/enforce-model-routing-receipt.ps1`, `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, `.claude/skills/orchestrate/SKILL.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, `.claude/skills/epic-orchestrate/SKILL.md` and each counterpart under `extensions/drm-copilot/resources/claude-customizations/`, and for `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` against `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-phase0-mirror-sha.md` (four schema fields), one row per pair with repository-relative paths. Acceptance: ten rows, twenty 64-hexadecimal hashes, each pair marked equal or unequal.
- [x] [P0-T16] Host-token inventory. In a fresh process, compute at run time the set of worktree roots from `git worktree list --porcelain`, the user profile directory, and the account name, and for every file under `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/` count lines matching any of: the regex `(?<![A-Za-z])[A-Za-z]:[\\/]`, the regex `(?<![A-Za-z0-9._-])/[A-Za-z]/[A-Za-z]`, the regex `[\\/](Users|home)[\\/]`, or a case-insensitive simple match of the account name. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-host-token-inventory.md` (four schema fields) listing each file with a non-zero count and its count, with no matched text. Acceptance: every listed path is repository-relative and each count is a number; `plan.2026-09-19T09-00.md` has count 0.

### Phase 1 — Spec Amendment, Binding Refresh, Redaction, and Fixture-Collision Check

- [x] [P1-T1] Refresh `##### BINDING TABLE` in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: update each existing row's `file:line` to the current tree. Five rows have stated replacements — derivation `WorktreeTargetResolution.psm1:233`, exported `:346`; sibling import `:27`; normalisation `WorktreeResolution.psm1:421`, exported `:498`; ambiguity accessor `:463`, exported `:499`; ambiguity literal `:55`, returned `:472`. The sixth existing row, the F1 module path, is re-derived by the executor from the tracked path of `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` and its `core.json` entry rather than being supplied here, because [P1-T3] verifies that entry independently. Then append `<NO_TARGET_REASON_CODE> accessor function name` = `Get-WorktreeResolutionNoTargetReasonCode` (`WorktreeResolution.psm1:475`, exported `:500`) and `<NO_TARGET_REASON_CODE> literal value returned by that accessor` = `TARGET_WORKTREE_NOT_DERIVABLE` (`:59`, returned `:487`). Archive the eight-row table with each cited source line quoted in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-f1-binding-table.md` (four schema fields). Acceptance: the spec table has eight rows, none containing `_unbound_`; the artifact quotes one source line per citation in the table — sixteen lines, since each row carries a definition citation and an export or return citation — each containing the identifier its row names.
- [x] [P1-T2] Amend `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: set `**Version:** 0.5` and `**Last Updated:** 2026-09-19T09-00`; insert immediately before `## Acceptance Criteria` a section headed `## Revision 0.5 — Identity-Based Target Resolution` containing §1.1, §2.1 to §2.7, and the §3 table of this plan verbatim; and append after AC-33, as a new subsection `### Identity resolution, checkpoint hygiene, prd-feature migration, and host data (Revision 0.5)`, the six criteria quoted in §9 of this plan verbatim. Acceptance: `spec.md` contains the heading `## Revision 0.5 — Identity-Based Target Resolution` once, and its `## Acceptance Criteria` section now lists exactly 39 identifiers `AC-1` through `AC-39`.
- [x] [P1-T3] Verify restated AC-21. Run `git log --format=%H --diff-filter=A origin/main -- .claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`, then `git merge-base --is-ancestor <that SHA> origin/main` and record its exit code; count occurrences of `.claude/lib/worktree-resolution/WorktreeResolution.psm1` and `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` in `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`; copy the `WorktreeResolution.Manifest.Tests.ps1` values from [P0-T10]. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-f1-on-main.md` (four schema fields). Acceptance: ancestry exit code 0; each count exactly 1; the testsuite shows `failures` 0 and a passed count greater than 0.
- [x] [P1-T4] Fixture-collision check. Search `.claude/hooks`, `.claude/lib`, `.claude/rules`, `scripts/dev_tools`, and `scripts/bash` recursively with `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` and classify each matching file as root-anchored, suffix-matching, or documentation, and whether it enumerates `tests/fixtures/`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-fixture-collision-check.md` (four schema fields), one row per matching file with file and line. Acceptance: every matching file has a row; the row for `.claude/hooks/enforce-checkpoint-monotonic.ps1` cites `:196` as `suffix-matching`; a final line reads `COLLISION_BLOCKS_FIXTURES: NO` or `COLLISION_BLOCKS_FIXTURES: YES`. On `YES`, stop and report.
- [x] [P1-T5] Redact host tokens from every file listed by [P0-T16] except `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-13T20-48.md`, `.../plan.2026-09-18T13-30.md`, and `.../plan.2026-09-18T16-00.md`, which are superseded and are not edited. In a fresh process, with values computed at run time and never written, replace in this order and in both separator spellings: each worktree root with `<WORKTREE_ROOT>`; the user profile directory (including its `/<drive>/...` shell spelling) with `<HOME>`; a program-files prefix with `<PROGRAM_FILES>`; remaining occurrences of the account name with `<USER>`; then any remaining match of the [P0-T16] path regexes, up to the next whitespace, quote, or backtick, with `<HOST_PATH>`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-host-token-redaction.md` (four schema fields) with, per file, the repository-relative path and the replacement count per placeholder. Acceptance: re-running the [P0-T16] count over the redacted files returns 0 for each; every `permissionDecision` value and every reason-code token in `evidence/baseline/repro-3-2-control-pair.md` and `evidence/baseline/repro-3-4-control-pair.md` is unchanged, compared with `git show HEAD:<path>`.
- [x] [P1-T6] Commit Phases 0 and 1. Stage `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673` with `git add` and a repository-relative pathspec; commit with the message read from `-F <SCRATCHPAD>/r3-msg-p1.txt`. If a PreToolUse gate denies, record its reason verbatim with host tokens replaced and report rather than routing around it. Take the porcelain observation post-commit and pre-append under rule 5, then append the command lines, the gate response, and that observation to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md` (four schema fields). Acceptance: `git log -1 --name-only` lists `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md` and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-19T09-00.md`, and no path outside the feature folder; the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all.

### Phase 2 — Identity Resolution Library Module (batch B1)

- [x] [P2-T1] Reset the batch budget per §6 and append entry `B1` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B1` entry lists deleted file names or `none present`.
- [x] [P2-T2] Create `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1` implementing §2.1 and §2.2 exactly: comment-based help containing the line `CONVENTION: this module fails fast at module scope and imports its siblings with -ErrorAction Stop.`; `Set-StrictMode -Version Latest` followed immediately by `$ErrorActionPreference = 'Stop'`; column-0 `Import-Module (Join-Path $PSScriptRoot 'WorktreeResolution.psm1') -ErrorAction Stop` and the same for `WorktreeTargetResolution.psm1`; the eight functions of §2.1 with comment-based help, exported by one `Export-ModuleMember -Function` statement; an intent comment above every loop and a decision comment above every multi-branch conditional. The module contains neither reason-code literal and no `SilentlyContinue` outside `Get-WorktreeItemCheckpointText`. Acceptance: the file exists, is at most 400 lines, contains the convention sentence verbatim, contains `$ErrorActionPreference = 'Stop'` on the line after `Set-StrictMode -Version Latest`, and a `Select-String -SimpleMatch` for `TARGET_WORKTREE_` returns no match.
- [x] [P2-T3] Create `tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1` with the 27 `It` names of §7 for this file, Arrange-Act-Assert structure, expected codes obtained only from the two F1 accessors, synthetic roots composed per §4, and mocks registered inside module `WorktreeItemResolution`. Acceptance: the file exists, is at most 500 lines, contains the 27 names verbatim, and contains no drive-letter path, `TestDrive`, `New-TemporaryFile`, or either reason-code literal.
- [x] [P2-T4] Register the module: insert `".claude/lib/worktree-resolution/WorktreeItemResolution.psm1",` after `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json:170`; add `'.claude/lib/worktree-resolution/WorktreeItemResolution.psm1'` to `$script:ExpectedPaths` and to each of the three `-ForEach` lists in `tests/scripts/claude-lib/worktree-resolution/WorktreeResolution.Manifest.Tests.ps1`, whose existing path entries are the pairs of literal lines `:28-29`, `:35-36`, `:46-47`, and `:73-74`; the lines `:27`, `:34`, `:45`, and `:72` are the `@(` openers and the ranges `:27-30`, `:34-37`, `:45-48`, `:72-75` are the enclosing blocks, so the new entry is appended after the second literal of each pair; add the same path after `scripts/powershell/PoshQC/settings/pester.runsettings.psd1:295` with a one-line issue #673 comment. Acceptance: `Select-String -SimpleMatch` counts of the path are 1 in `core.json`, 4 in the manifest test, and 1 in the runsettings file; `core.json` still parses with `ConvertFrom-Json`.
- [x] [P2-T5] Mirror by byte copy per §6: `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1` to `extensions/drm-copilot/resources/claude-customizations/.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1` to `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`. Record both commands (repository-relative) and SHA-256 pairs in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md` (four schema fields; appended by every later mirror task). Acceptance: both pairs equal.
- [x] [P2-T6] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p2-scoped-pester.md` (four schema fields) with, per §5, report last-write time, root `failures` and `errors`, the testsuites ending `WorktreeItemResolution.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, and `ClaudeLibModuleConvention.Tests.ps1`, and the line coverage of `WorktreeItemResolution.psm1`. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0; the first testsuite has 27 passed; the other three have `failures` 0; module line coverage at least 85%.
- [x] [P2-T7] Append a ninth row to the spec binding table in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/spec.md`: `<TARGET_DERIVATION> identity form (Revision 0.5)` = `Resolve-WorktreeItemTarget` with the `file:line` of its definition and export in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`; append both quoted lines to `evidence/other/r3-f1-binding-table.md`. Acceptance: the table has nine rows and each quoted line contains `Resolve-WorktreeItemTarget`.

### Phase 3 — Committed Fixture Roots

- [x] [P3-T1] Create the committed fixtures of §4.1 under `tests/fixtures/worktree-resolution/`. Every checkpoint is at `artifacts/orchestration/orchestrator-state.json` under its root. Base JSON is the checkpoint at `evidence/baseline/repro-fixture-manifest.md:53-100` verbatim (its `issue-num` is `838`), with the per-root changes of §4.1; the `901` roots change only `objective` where stated and `issue-num`. The pr-author artifact files: `artifacts/pr_context.summary.txt` one line of text; `artifacts/pr_body_1.md` exactly `Sibling item A pull request body.` followed by one LF; `artifacts/pr_body_1.receipt.json` `{"number": 1, "sha256": "<SHA-256 of that body file>", "created_at": "2099-01-01T00:00:00Z"}`. Add `tests/fixtures/worktree-resolution/README.md` stating purpose, the §4.1 map, and the schema dependency on `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`. Record any PreToolUse interception and its resolution in `evidence/other/r3-fixture-collision-check.md`. Acceptance: `git status --porcelain --untracked-files=all -- tests/fixtures/worktree-resolution` lists every file of §4.1 plus `README.md`; `shared/item-own-empty/artifacts/orchestration/orchestrator-state.json` has length 0.
- [x] [P3-T2] Fixture sanity check. In a fresh process import `.claude/lib/orchestrator-state/OrchestratorState.psm1` and `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`; run `Invoke-OrchestratorStatePreflight -CheckpointPath <path>` (absolute path composed at run time from `$root`) on the checkpoints of `pr-author/session-root`, `pr-author/item-own-ready`, `pr-author/item-own-epic-mode`, `pr-author/item-own-not-ready`, `shared/item-own-invalid-json`, `shared/item-own-empty`, and the absent `shared/item-no-checkpoint` path; run `Get-WorktreeItemCheckpointIssue` on every root of §4.1; recompute SHA-256 of both `pr_body_1.md` files and count their CR bytes. If `item-own-epic-mode` reports errors, add exactly the fields its error text names, re-run, and record both runs. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-committed-fixture-manifest.md` (four schema fields) with repository-relative fixture paths, each checkpoint's content, the §7 rows consuming it, and the results. Acceptance: `HasErrors` is `False` for the first three and `True` for the other four; the issue results equal the §4.1 column (`unknown` and `none` recorded as empty); both body hashes equal the receipts' `sha256`; both CR counts are 0.

### Phase 4 — Matrix Tests Authored Against the Unmodified Hooks (batch B2)

- [x] [P4-T1] Reset the batch budget per §6; append entry `B2` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B2` entry exists.
- [x] [P4-T2] Create `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` defining `Get-WorktreeResolutionFixturePath -RelativePath` (normalised absolute path under `tests/fixtures/worktree-resolution/`, computed from `$PSScriptRoot`), `Invoke-WorktreeResolutionFixtureCall -WorkingDirectory -ScriptBlock` (per §4, both locations restored in `finally`), and `New-WorktreeResolutionFixtureTarget -Status -WorktreeRoot [-Candidate]` (wraps `New-WorktreeResolutionTargetResult`). Acceptance: the file exists, is at most 200 lines, defines the three functions with `[CmdletBinding()]` and `[OutputType()]`, and contains none of `New-TemporaryFile`, `GetTempPath`, `GetTempFileName`, `TestDrive`.
- [x] [P4-T3] Create `tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.Tests.ps1` with the 17 `It` definitions of §7 (18 expanded names), the §4 import order, a head comment naming `REQUIRED_STATE_KEYS` and `OrchestratorStateUnconditional.psm1`, and expected codes only from the two accessors. Every row supplies `-WorkingDirectory` to `Invoke-WorktreeResolutionFixtureCall` under the §4 working-directory rule, defaulting to `pr-author/session-root` where §7 names none. Rows 3 and 8 register no mock, in the narrowed sense §4 states. Acceptance: the file exists, is at most 500 lines, contains the 18 expanded names verbatim, contains neither reason-code literal and no drive-letter path; the count of `Invoke-WorktreeResolutionFixtureCall` occurrences with a `-WorkingDirectory` argument equals the count of `It` blocks; and the file contains no `-ContextExists`, no `Mock -CommandName Get-PrContextArtifactExistence`, no `Mock -CommandName Get-PrAuthorReceiptContent`, no `Mock -CommandName Get-PrBodyFileBytes`, and no `Mock -CommandName Get-PrContextSummaryLastWriteUtc`. If it would exceed 500 lines, move **rows 8 to 17** unchanged into `tests/scripts/claude-hooks/enforce-pr-author-skill.WorktreeResolution.InProcess.Tests.ps1` after an additional §6 reset recorded as `B2b`; rows 1 to 7 stay in the named file. The movable set is named by row number because §4's working-directory rule leaves no row that uses no fixture, so the earlier "rows that use no fixture" formulation named an empty set; a moved row keeps its own `-WorkingDirectory` value and its §7 text unchanged.
- [x] [P4-T4] Create `tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` with the 18 `It` definitions of §7 (20 expanded names) under the rules of [P4-T3], its working-directory default being `model-routing/session-root`. Rows 17 and 18 mock `Resolve-ModelRoutingWorktreeTarget` inside their own `It` only, because that function does not exist before [P7-T2]. Acceptance: the file exists, is at most 500 lines, contains the 20 expanded names verbatim, and contains neither reason-code literal and no drive-letter path; the split rule of [P4-T3] applies with rows 1 to 7 kept in the named file and **rows 8 to 18** movable, each keeping its own `-WorkingDirectory` value.
- [x] [P4-T5] [expect-fail] Run the new suites against the unmodified hooks. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-fail-before-matrix.md` (four schema fields) with the report last-write time and the name and status of every `testcase` under the two `WorktreeResolution` testsuites. Acceptance: last-write time at or after `Timestamp:`; both testsuites present; Failed: `model-routing R2 denies with the no-target code when only the sibling session-root checkpoint is present`, `model-routing R2 denies with the no-target code when the only signal is a repository-relative path`, `model-routing R4 takes the verdict from the own checkpoint located by issue number when the sibling checkpoint records the receipt`, `pr-author R4 takes the epic base-branch verdict from the own checkpoint when own and sibling checkpoints are both present`; Passed: `pr-author R2 denies with the no-target code when only the sibling session-root checkpoint is present`, `pr-author R1 allows when the resolved own checkpoint is ready and the working directory is the sibling session root`, `pr-author R6 allows when the working directory is the item worktree and its own checkpoint is ready`, `model-routing R1 allows when the resolved own checkpoint records the receipt`, `model-routing R6 allows when the working directory is the item worktree and its own receipt is present`; every pre-existing testsuite under `tests/scripts/claude-hooks` has `failures` 0 apart from [P0-T10] exclusions.
- [x] [P4-T6] Write the AC-18 pre-fix record `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/baseline/r3-prefix-r1-r6.md` (four schema fields; `Command:` names the [P4-T5] command) quoting the four R1 and R6 `testcase` entries named in [P4-T5], and the dossier `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/fail-before-exception.2026-09-19T09-00.md` with `Timestamp:`, `WhyFailingRunImpossible:` (pr-author R2 already denies on this tree because #687 added the no-target deny; commit from [P0-T6]), and an alternative-proof section citing `evidence/baseline/repro-3-2-control-pair.md`, `evidence/baseline/repro-verdict.md`, and the Failed status of the pr-author R4 epic row. Acceptance: four Passed entries; the dossier carries both fields and the three citations.
- [x] [P4-T7] Commit Phases 2 to 4. Stage exactly `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `extensions/drm-copilot/resources/claude-customizations/.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `extensions/drm-copilot/resources/claude-customizations/pack-manifests/core.json`, `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, `extensions/drm-copilot/resources/powershell/PoshQC/settings/pester.runsettings.psd1`, the two B1 test files, `tests/fixtures/worktree-resolution`, the B2 test files, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with `git add` and repository-relative pathspecs; commit with `-F <SCRATCHPAD>/r3-msg-p4.txt`. `tests/fixtures/` is not in the issue #539 exempt set, so the pre-implementation gate reads the executing worktree's checkpoint; if it denies, record the `PREIMPLEMENTATION_GATE_BLOCKED` reason verbatim with host tokens replaced and report rather than routing around it. Take the porcelain observation post-commit and pre-append under rule 5, then append it with the command lines to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `tests/fixtures/worktree-resolution/README.md` and `tests/scripts/claude-hooks/enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` and no path beginning `.claude/hooks/`; the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all.

### Phase 5 — pr-author Family Implementation (batch B3)

- [x] [P5-T1] Reset the batch budget per §6; append entry `B3` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B3` entry exists.
- [x] [P5-T2] In `.claude/hooks/enforce-pr-author-skill.ps1`, replace `:49` with `$script:OrchestratorStateCheckpointPath = $null` and a comment of at most two lines stating that the resolution step assigns the absolute path. Acceptance: `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` over the file matches no non-comment line, and the file declares `$script:OrchestratorStateCheckpointPath = $null` exactly once.
- [x] [P5-T3] In `.claude/hooks/enforce-pr-author-skill-helpers.ps1` apply DD-1 and DD-2: add the `WorktreeItemResolution.psm1` import after `:39`; change the body of `Resolve-PrAuthorWorktreeTarget` (`:61`); merge the `SessionRoot` and `OtherWorktree` branches of `Get-PrAuthorTargetCheckpointResolution` into one returning `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot`, deny branch unchanged; rewrite the doc comment at `:69-81` and the header at `:18-21` to name the two accessor functions and describe identity resolution, containing neither reason-code literal; add `[Parameter(Mandatory)] [string] $CheckpointPath` to `Test-PrAuthorReceiptVerification` and pass it at `:211` as `Test-EpicBaseBranchOverride -CommandText $CommandText -CheckpointPath $CheckpointPath`; in `Get-PrAuthorBypassReason` assign `$script:OrchestratorStateCheckpointPath = $resolution.CheckpointPath` after the deny check and pass it to `Invoke-OrchestratorStatePreflight -CheckpointPath` and `Test-PrAuthorReceiptVerification -CheckpointPath`. Acceptance: the file is at most 500 lines; it contains neither reason-code literal and no `SilentlyContinue`; `Resolve-WorktreeCallTarget` no longer occurs; `Get-WorktreeItemCheckpointPath` occurs exactly once.
- [x] [P5-T4] In `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1`: make `Get-PrAuthorCheckpointContent`'s parameter `[Parameter(Mandatory)] [string] $CheckpointPath` with no default; add `[Parameter(Mandatory)] [string] $CheckpointPath` to `Test-EpicBaseBranchOverride`; change `:78` to `$checkpointRaw = Get-PrAuthorCheckpointContent -CheckpointPath $CheckpointPath`; update the doc comments at `:23` and `:26-28` to describe the resolved absolute path. Acceptance: the file is at most 500 lines and no line assigns a default to `$CheckpointPath`.
- [x] [P5-T5] In `tests/scripts/claude-hooks/enforce-pr-author-skill.TargetResolution.Tests.ps1`: rename the three tests per §7; replace every reason-code literal with the matching accessor call (`ReasonCode = (Get-WorktreeResolutionNoTargetReasonCode)`; `Should -BeLike ("{0}*" -f (Get-WorktreeResolutionNoTargetReasonCode))`, and the ambiguity equivalent); replace the synthetic drive-letter roots at `:26`, `:37`, `:43` with roots composed per §4; change the assertion at `:54` to expect the normalised composed session root followed by `/artifacts/orchestration/orchestrator-state.json`. Acceptance: the file contains neither reason-code literal, no drive-letter path, and the three new names verbatim.
- [x] [P5-T6] Apply AC-19 form (a): append ` -CheckpointPath (Join-Path $PSScriptRoot 'no-such-checkpoint.json')` to each `Test-EpicBaseBranchOverride` call at `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1:34`, `:40`, `:48`, `:58`, `:68`, `:78`, `:84` and `tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.TriggerScoping.Tests.ps1:38`, `:52`, changing nothing else. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md` (four schema fields) quoting each edited line before and after, marking the two inside AC-19's protected span `:22-42`. Acceptance: nine before/after pairs, each after-line differing from its before-line only by the appended argument.
- [x] [P5-T7] Mirror `.claude/hooks/enforce-pr-author-skill.ps1`, `.claude/hooks/enforce-pr-author-skill-helpers.ps1`, and `.claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1` by byte copy to the same relative paths under `extensions/drm-copilot/resources/claude-customizations/`; append commands and SHA-256 pairs to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: three equal pairs.
- [x] [P5-T8] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p5-scoped-pester.md` (four schema fields) with the report last-write time, the `tests` and `failures` of every testsuite whose name ends with a file named `enforce-pr-author-skill*.Tests.ps1`, `enforce-prd-feature-before-planner*.Tests.ps1`, or `enforce-model-routing-receipt.Tests.ps1`, and every `testcase` status of `enforce-model-routing-receipt.WorktreeResolution.Tests.ps1` (still pre-fix and exempt). Acceptance: last-write time at or after `Timestamp:`; every listed pr-author testsuite, including `enforce-pr-author-skill.WorktreeResolution.Tests.ps1`, the three prd-feature testsuites, and `enforce-model-routing-receipt.Tests.ps1` have `failures` 0.

### Phase 6 — OrchestratorState.Tests.ps1 Pin Replacement and Size Reduction (batch B4)

- [x] [P6-T1] Reset the batch budget per §6; append entry `B4` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B4` entry exists.
- [x] [P6-T2] Create `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.ValueContract.Tests.ps1` holding the `Describe 'Get-OrchestratorStateCheckpoint value contract'` block moved verbatim from `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1:485-508`, preceded by a `BeforeAll` reproducing `:23-28` (module import) and the `New-ReadyCheckpoint` and `Set-CheckpointFixture` helpers at `:34-68`. Copy exactly that range: `Set-CheckpointFixture` closes at `:68`, and `:69` closes the source file's own `BeforeAll`, so including `:69` would carry an extra closing brace into the new `BeforeAll` and the new file would not parse. Then delete `:485-508` and the blank line before them from the source file. Acceptance: both `It` names of that block appear in the new file and in no other file under `tests/scripts/claude-lib/orchestrator-state/`.
- [x] [P6-T3] In `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, inside `It 'blocks a not-ready checkpoint with ORCHESTRATOR_STATE_PREFLIGHT_FAILED through the default path'`, replace the pin at `:312` with `$script:MissingWorktreeRoot = Join-Path $PSScriptRoot 'no-such-worktree-fixture'` and a `Mock -CommandName Resolve-PrAuthorWorktreeTarget` returning `Status = 'OtherWorktree'`, `WorktreeRoot = $script:MissingWorktreeRoot`, `ReasonCode = $null`, `Detail = 'absent fixture worktree'`, leaving every assertion line unchanged. Append the before and after text to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md`, labelled as outside AC-19's rows. Acceptance: the file is at most 500 lines and no longer contains `nonexistent-fixture.json`.
- [x] [P6-T4] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p6-scoped-pester.md` (four schema fields) with the report last-write time, root `failures` and `errors`, the testsuites ending `OrchestratorState.Tests.ps1` and `OrchestratorState.ValueContract.Tests.ps1`, and both files' line counts. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0; the value-contract testsuite has 2 passed; both line counts at most 500.

### Phase 7 — model-routing Family Implementation (batch B5)

- [ ] [P7-T1] Reset the batch budget per §6; append entry `B5` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B5` entry exists.
- [ ] [P7-T2] In `.claude/hooks/enforce-model-routing-receipt.ps1` apply DD-3: the import after `:36`; `Get-ModelRoutingCheckpoint` parameter mandatory with no default; `Resolve-ModelRoutingWorktreeTarget` (`[CmdletBinding()]`, `[OutputType([pscustomobject])]`, `[Parameter(Mandatory)] [AllowEmptyString()] [string] $PromptText`); `Get-ModelRoutingTargetCheckpointResolution` (`[OutputType([System.Collections.Specialized.OrderedDictionary])]`, reason text exactly as DD-3); in `Invoke-ModelRoutingReceiptDecision`, between the scope filter and the read, obtain `prompt` with `Get-ClaudeHookToolInputString` (an absent prompt is passed as an empty string), resolve, return a deny carrying `$resolution.Reason` when set, otherwise call `Get-ModelRoutingCheckpoint -CheckpointPath $resolution.CheckpointPath`; update the header (`:7-9`, `:26-27`) to describe identity resolution and the delegation-identity contract. Acceptance: the file is at most 500 lines; it contains no `SilentlyContinue`, no reason-code literal, and no non-comment occurrence of `orchestrator-state.json`; the six decision branches of spec `### Boundaries and invariants to preserve` remain, with the new deny between branches 3 and 4.
- [ ] [P7-T3] In `tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1`, add one Describe-level `BeforeEach` directly after the `BeforeAll` ending at `:17`, mocking `Resolve-ModelRoutingWorktreeTarget` to return `[pscustomobject]@{ Status = 'SessionRoot'; WorktreeRoot = (Get-Location).Path; ReasonCode = $null; Detail = 'session root' }`, with a two-line comment citing issue #673 (RS-8 form (c)). No other line changes. Append the inserted block to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-protected-row-edits.md`. Acceptance: `git diff <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1`, with the recorded SHA substituted, shows added lines only.
- [ ] [P7-T4] Create `tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1` with the seven `It` names §7 assigns to this task, which is every name in its §7 list except the `Get-PrdFeatureCheckpointFolder` row. The five call-site tests parse every `.ps1` and `.psm1` under `.claude/hooks` and `.claude/lib` with `[System.Management.Automation.Language.Parser]::ParseFile`, collect each `CommandAst` whose command name is the target function, assert the collection is non-empty, and assert each carries a `CommandParameterAst` named `CheckpointPath`. The literal test asserts that no `StringConstantExpressionAst` or `ExpandableStringExpressionAst` in the four in-scope hook files of §2.6 contains `orchestrator-state.json`; its file list is a single array literal at the top of that `It` so [P9-T10] can widen it in one line. The line-count test asserts `@(Get-Content -LiteralPath .claude/lib/orchestrator-state/OrchestratorState.psm1).Count` equals 499, with the path resolved from `$PSScriptRoot`. The `Get-PrdFeatureCheckpointFolder` row is deliberately not authored here, because the production change it asserts does not land until [P9-T4] and a row authored now could only fail. Acceptance: the file exists with the seven names verbatim, does not contain `Get-PrdFeatureCheckpointFolder`, and creates no file.
- [ ] [P7-T5] Mirror `.claude/hooks/enforce-model-routing-receipt.ps1` by byte copy to `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-model-routing-receipt.ps1`; append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: one equal pair.
- [ ] [P7-T6] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks,tests/scripts/claude-runtime,tests/scripts/claude-lib -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p7-scoped-pester.md` (four schema fields) with the report last-write time, root `failures` and `errors`, and every `testcase` name and status under the two `WorktreeResolution` hook testsuites, `WorktreeItemResolution.Tests.ps1`, and `enforcement-hooks-checkpoint-path-explicit.Tests.ps1`. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0 apart from [P0-T10] exclusions; every §7 name for those four files, other than the `Get-PrdFeatureCheckpointFolder` row that [P9-T10] adds later, appears exactly once with status Passed.
- [ ] [P7-T7] Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-fail-before-pass-after.md` (four schema fields) pairing [P4-T5]'s Failed status with [P7-T6]'s Passed status for the four rows [P4-T5] requires Failed, and citing `fail-before-exception.2026-09-19T09-00.md` for the pr-author R2 row. Acceptance: four Failed-then-Passed pairs and the dossier citation.
- [ ] [P7-T8] Commit Phases 5 to 7 with the method and gate handling of [P4-T7], staging the four hook files, their four mirrors under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/`, every test file modified or created in B3 to B5, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with commit message `-F <SCRATCHPAD>/r3-msg-p7.txt`. Take the porcelain observation post-commit and pre-append under rule 5, then append it to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `.claude/hooks/enforce-model-routing-receipt.ps1` and `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-model-routing-receipt.ps1`; the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all.

### Phase 8 — Skill Contracts: Checkpoint Hygiene and Delegation Identity (batch B6)

- [ ] [P8-T1] Reset the batch budget per §6; append entry `B6` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B6` entry exists.
- [ ] [P8-T2] Edit `.claude/skills/orchestrate/SKILL.md`. In `## Checkpoint Handling` append step 4 beginning `4. **Checkpoint hygiene (issue #673).**` stating: when the checkpoint at `artifacts/orchestration/orchestrator-state.json` records an `issue-num` other than the item this invocation is about, move it to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json` before writing a new checkpoint, rather than overwriting it or leaving it; apply the same move when this session hands an item whose checkpoint it holds to another session or worktree; and give the reason in one factual sentence (a checkpoint left at a session root is read by gates that resolve against that root). In `## Issue Number Consistency` replace the sentence at `:258` with one requiring the line on every delegation prompt to a receipt-gated subagent type, naming `atomic-planner`, `atomic-executor`, `feature-review`, `task-researcher`, `prd-feature`, and `pr-author`; after the quoted line add a paragraph beginning `Each such prompt also carries a` requiring a `branch: <name>` label naming the branch checked out in the item's worktree, written as the first `branch:` occurrence in the prompt, required alone before promotion assigns an issue number, and stating that `enforce-model-routing-receipt.ps1` and `enforce-prd-feature-before-planner.ps1` identify the item from these two lines, deny a gated delegation they cannot identify, and never use a feature-folder path to choose the worktree. Neither edit adds a `##` heading or a reason-code literal. Acceptance: the file contains `Checkpoint hygiene (issue #673)` once, `orchestrator-state.issue-` once, `Each such prompt also carries a` once, and `enforce-prd-feature-before-planner.ps1` at least once; its `##` heading count is unchanged from `F5_BASE_SHA`.
- [ ] [P8-T3] Edit `.claude/skills/parallel-orchestrate/SKILL.md`: append to the end of `## Per-Item Branch and Worktree Lifecycle` (after the paragraph ending at `:235`) a paragraph beginning `**Checkpoint hygiene (issue #673).**` stating that the coordinating session never holds a per-feature checkpoint at its own root; before the first item delegation of a run it moves any `artifacts/orchestration/orchestrator-state.json` at its root to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json` and writes none there for the rest of the run, because each item's checkpoint lives in that item's worktree; and that a gated call the coordinator issues on an item's behalf is resolved by the item's issue number and branch, never by the coordinator root. The paragraph adds no `##` heading and contains none of `Epic mode: true`, `--base epic/`, `integration-to-main`. Acceptance: the file contains `Checkpoint hygiene (issue #673)` once and `orchestrator-state.issue-` once; its `##` heading count is 16.
- [ ] [P8-T4] Edit `.claude/skills/epic-orchestrate/SKILL.md`: append to the end of `## Epic-Level Checkpoint`, which begins at `:278` and whose final paragraph runs `:295-299` with the next `## ` heading at `:301` (so the insertion point is after the paragraph ending at `:299`, not after `:293`, which is mid-section), the paragraph of [P8-T3], with "item delegation" read as "child delegation". Acceptance: the file contains `Checkpoint hygiene (issue #673)` once and `orchestrator-state.issue-` once; its `##` heading count is unchanged from `F5_BASE_SHA`.
- [ ] [P8-T5] Re-baseline the frozen-surface pin in `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`. Identify both digests by the path string recorded above each, not by line number, because the two edits in this task are order-dependent and inserting the comment paragraph shifts every digest line below it: replace the digest entry whose path string is `.claude/skills/epic-orchestrate/SKILL.md` with the lowercase SHA-256 of that file after [P8-T4], and add to the file's existing comment block a paragraph beginning `RE-BASELINED by issue #673.` stating that the checkpoint-hygiene paragraph was added to `## Epic-Level Checkpoint` and that the pin stays live for the entry whose path string is `.claude/agents/epic-orchestrator.md`, whose digest is unchanged. Perform the digest replacement first and the comment insertion second, and record both the pre-edit and post-edit line numbers of each digest entry. Record the old and new digests in `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-frozen-pin-rebaseline.md` (four schema fields). Acceptance: the new digest equals `Get-FileHash -Algorithm SHA256` of the edited skill (lowercased); the digest recorded against `.claude/agents/epic-orchestrator.md` is byte-identical to its value at `F5_BASE_SHA`; the file contains `RE-BASELINED by issue #673.` exactly once.
- [ ] [P8-T6] Create `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1` with the seven `It` names of §7. Each test extracts its section by heading from the skill file (resolved from `$PSScriptRoot`), collapses whitespace, and asserts the tokens that [P8-T2] to [P8-T4] require; the sixth dot-sources `.claude/hooks/enforce-model-routing-receipt.ps1` and asserts every name returned by `Get-ModelRoutingGatedAgent` appears in the `## Issue Number Consistency` section; the seventh asserts that section names both `enforce-model-routing-receipt.ps1` and `enforce-prd-feature-before-planner.ps1`. Acceptance: the file exists, is at most 200 lines, contains the seven names verbatim, and creates no file.
- [ ] [P8-T7] Mirror `.claude/skills/orchestrate/SKILL.md`, `.claude/skills/parallel-orchestrate/SKILL.md`, and `.claude/skills/epic-orchestrate/SKILL.md` by byte copy to the same relative paths under `extensions/drm-copilot/resources/claude-customizations/`; append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: three equal pairs.
- [ ] [P8-T8] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-runtime -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, then run `poetry run pytest tests/scripts/dev_tools/test_parallel_orchestrator_surface_contracts.py tests/scripts/dev_tools/test_epic_bounded_child_return_contract.py tests/scripts/dev_tools/test_claude_rules_frontmatter.py tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-p8-scoped.md` (four schema fields) with the Pester report last-write time, the `checkpoint-hygiene-skill-contract.Tests.ps1` testsuite values, and the pytest summary line. Acceptance: last-write time at or after `Timestamp:`; that testsuite has 7 passed and `failures` 0; the pytest summary carries no `failed` or `error` count. A failing pytest case stops execution and is reported with its node ID.
- [ ] [P8-T9] Commit Phase 8 with the method of [P4-T7], staging the three skills, their three mirrors, `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1`, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with `-F <SCRATCHPAD>/r3-msg-p8.txt`. Take the porcelain observation post-commit and pre-append under rule 5, then append it to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `.claude/skills/epic-orchestrate/SKILL.md` and `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`; the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all.

### Phase 9 — prd-feature Gate Migration, Issue #672 (batches B7 and B8)

- [ ] [P9-T1] Reset the batch budget per §6; append entry `B7` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B7` entry exists.
- [ ] [P9-T2] Record the pre-change verdict of §2.7.5 against the still-unmodified gate. In a fresh process, dot-source `.claude/hooks/enforce-prd-feature-before-planner.ps1`, import `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` and `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, and run two controls. Control 1 (deterministic, the acceptance-bearing one): build `$target = New-WorktreeResolutionTargetResult -Status 'Ambiguous' -SessionRoot '/synthetic-worktrees/coordinating-session' -Signal 'FeatureFolderPath' -SignalValue 'docs/features/active/2026-09-13-synthetic-target-672' -Candidate @() -Detail 'modelled multi-worktree placement of a merged feature folder'`, and call `Invoke-PrdFeatureBeforePlannerDecision -ToolInputRaw <an Agent payload whose subagent_type is atomic-planner and whose prompt cites that folder and carries a canonical issue-number line naming the synthetic issue 901> -ResolvedTarget $target`. The synthetic number is used rather than a real one so that neither this plan nor the artifact it writes carries an identity signal for a live item. Control 2 (observational only): call the same entrypoint with no `-ResolvedTarget`, letting the shipped folder-first derivation run, and record only `permissionDecision`, whether the reason contains the value of `Get-WorktreeResolutionAmbiguityReasonCode`, and the integer candidate count reported in the reason if one is present. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r4-prd-pre-change-control.md` (four schema fields) with both controls, the field observation of §1.1 fact 6 (date, twelve candidate worktrees, ambiguity code), and no path other than the synthetic literals and repository-relative paths. Acceptance: control 1 records `permissionDecision` `deny` and a reason containing the value returned by `Get-WorktreeResolutionAmbiguityReasonCode` and not containing `is missing:`; control 2 records a `permissionDecision` value and a boolean, and is explicitly labelled `OBSERVATION ONLY, NOT AN ACCEPTANCE CONDITION` because its outcome depends on how many checkouts of this repository exist on the running host.
- [ ] [P9-T3] In `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1` apply DD-7: rename `Select-PrdFeatureFolderByTarget` to `Select-PrdFeatureFolderByCheckpoint`, replacing its `[AllowNull()] [object] $Target` parameter with `[AllowNull()] [string] $CheckpointFolder` and its body's `[string] $Target.SignalValue` read (`:237`) with a `ConvertTo-PrdFeatureFolderToken -Path $CheckpointFolder` normalisation, returning that value when `$Candidate -contains` it and `$null` otherwise; replace `Find-PrdFeatureFolderFromPrompt`'s `-Target` parameter with an optional `[AllowNull()] [string] $CheckpointFolder` passed through to the renamed selector; rewrite both comment-based-help `.DESCRIPTION` blocks (`:217-221`, `:254-265`) to state that the disambiguator is the checkpoint of the worktree identity resolved, which is the item's own, and that neither prompt position nor a foreign session's record takes that role. Rewrite four further comment regions that the same change falsifies and that no acceptance condition detects today: the `.SYNOPSIS` blocks at `:213-215` and `:248-253`, both of which name the derived target as the disambiguator, and the body comments at `:284` and `:289-291`, which describe consulting the derived target. None of the rewritten text may contain the phrase `derived target`. `Resolve-PrdFeatureWorkMode`, `Get-PrdFeatureRequiredFile`, `ConvertTo-PrdFeatureFolderToken`, `Find-PrdFeatureFolderCandidate`, `Get-PrdFeatureMissingFile`, and the file's `WorktreeResolution.psm1` import at `:27` are not edited. Acceptance: the file is at most 500 lines; `Select-PrdFeatureFolderByTarget` occurs nowhere in it; `SignalValue` occurs nowhere in it; the phrase `derived target` occurs nowhere in it, which is the one condition that can fail if a stale comment is left behind; `Select-PrdFeatureFolderByCheckpoint` is declared exactly once; `git diff <F5_BASE_SHA> -- .claude/hooks/enforce-prd-feature-before-planner-helpers.ps1` with the recorded SHA substituted shows no change inside the `Resolve-PrdFeatureWorkMode`, `Get-PrdFeatureRequiredFile`, or `ConvertTo-PrdFeatureFolderToken` function bodies.
- [ ] [P9-T4] In `.claude/hooks/enforce-prd-feature-before-planner.ps1` apply DD-4, DD-5, DD-6, DD-7, and DD-9: **keep** the `:108` import of `WorktreeTargetResolution.psm1` and add a second unguarded import of `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1` on the line after it, mirroring `.claude/hooks/enforce-pr-author-skill-helpers.ps1:38-39`, and extend the justification comment at `:104-107` by one sentence naming why both are needed (`Join-WorktreeResolutionPath` is exported only by the first, per DD-4's scope note); add `Resolve-PrdFeatureWorktreeTarget -Text` whose body is `Resolve-WorktreeItemTarget -Text $Text -SessionRoot (Get-Location).Path`; drop `Get-PrdFeatureCallTarget`'s `-Envelope` parameter and the envelope-cwd read at `:272-275`, so its body assembles the prompt and description text, keeps the existing empty-text early return at `:268-270`, and otherwise returns `Resolve-PrdFeatureWorktreeTarget -Text $text`; widen the `:337` check per DD-5 to `NoTarget`, `Ambiguous`, and a `$null` target from either source; make `Get-PrdFeatureCheckpointFolder`'s `-CheckpointPath` `[Parameter(Mandatory)]` with no default (`:160`); replace `Get-PrdFeatureAmbiguityDecision` with `Get-PrdFeatureTargetDecision -ReasonCode -Detail` carrying DD-5's remedy sentence; implement DD-7's candidate-count branch, passing `Get-WorktreeItemCheckpointPath -WorktreeRoot $target.WorktreeRoot` to the checkpoint reader; delete `Test-PrdFeatureSessionRootTarget` (`:181-201`) and the deny at `:359-361`; and rewrite the `.DESCRIPTION` resolution-order block (`:14-59`) to describe identity-first resolution in the same numbered form.

  **Reword the checkpoint-filename deny at `:368`.** That line is the second of the file's two occurrences of the checkpoint filename literal (the first is the `:160` default DD-6 removes), and it sits inside the double-quoted `permissionDecisionReason` of the `must reference a feature folder` deny. Three acceptance conditions — this task's literal condition, [P9-T10]'s widened array, and [P10-T1]'s zero-code-match requirement — cannot pass while it stands, so DD-7's "unchanged" claim for `:363-371` is narrowed to the decision logic and does not extend to this reason string. Replace the reason value with exactly: `PRD_FEATURE_BLOCKED: atomic-planner delegation must reference a feature folder, either in the prompt or in the orchestrator checkpoint of the worktree the call resolves to, so spec.md and user-story.md prerequisites can be verified.` The replacement names the checkpoint by role instead of by filename and is also the more accurate statement after DD-6, since the checkpoint consulted is the resolved worktree's and no longer the calling session's.

  **Correct the doc comments the change falsifies.** These carry no acceptance condition of their own today, which is why they survived three review rounds; each asserts behaviour this task removes. Rewrite: `:104`, which credits issue #669 with call-target derivation, to name issue #673's identity resolution alongside it; `:238-253`, `Get-PrdFeatureCallTarget`'s `.DESCRIPTION`, which states that the function supplies the session root from the envelope's own `cwd` field and that the derivation keeps only the worktrees under which a repo-relative path exists — both false after DD-4; and the comment at `:325-326`, which says the envelope root is read for its `cwd`. The rewritten text must not contain the phrase `derived target`, which the acceptance condition below counts.

  **The seven `derived target` occurrences, each assigned to an edit.** The condition is reachable only because every one falls inside a region this task already rewrites, and they are enumerated here so that is verifiable rather than assumed: `:40` and `:45` sit in the `:14-59` header block; `:344` and `:347` are the comment and detail of the multi-candidate tie branch, which DD-7's candidate-count branch replaces; `:351` is the comment above the checkpoint fallback, replaced by the same DD-7 rewrite; `:360` is inside the branch DD-9 deletes; and `:394` is the detail handled above. No occurrence lies in a region this task declares unedited, so no instruction here forbids what another requires. `Get-PrdFeatureAmbiguityDecision` has four call sites and DD-5 deletes its definition, so all four must be accounted for: `:338` and `:347` are rewritten by DD-5 and DD-7; `:360` is deleted with the branch above; and `:394` — the `return` inside the `:393` guard — becomes `Get-PrdFeatureTargetDecision -ReasonCode (Get-WorktreeResolutionAmbiguityReasonCode) -Detail (<the existing detail expression at `:394`, copied with `derived target worktree` replaced by `resolved target worktree` and nothing else changed>)`, which preserves the emitted code and changes the detail only in that phrase. The substitution is required rather than cosmetic: `:394` is the file's last surviving occurrence of `derived target` once the other rewrites land, and this task's acceptance forbids that phrase. It is safe because no test asserts this detail text — the only other repository match is a comment at `TargetResolution.Tests.ps1:482`, and the surviving row at `:481-497` asserts only the deny, the ambiguity code, and the absence of the work-mode phrase. What is unedited at that guard is its `if` condition on `:393` alone, including the `-and $probeFolder -ne $folderNormalized` conjunct (DD-8a); the `return` on the next line is edited. The `:379-382` probe-anchoring conditional, the work-mode block (`:397-418`), and the missing-file block (`:420-444`) are not edited. Acceptance: the file is at most 500 lines; `Resolve-WorktreeCallTarget`, `Test-PrdFeatureSessionRootTarget`, `Get-PrdFeatureAmbiguityDecision`, and `-Envelope` occur nowhere in it; the file still contains `-and $probeFolder -ne $folderNormalized` exactly once; `Get-PrdFeatureTargetDecision` occurs four times (one definition and three call sites); `Get-ClaudeHookEnvelopeValue` and `Test-ClaudeHookEnvelopeHasKey` occur nowhere in it; the phrase `derived target` occurs nowhere in it, which is the one condition that can fail if a stale doc comment is left behind; the file carries exactly two `Import-Module` lines under `../lib/worktree-resolution/`, naming `WorktreeTargetResolution.psm1` and `WorktreeItemResolution.psm1`; `Join-WorktreeResolutionPath` still occurs exactly once; `Get-WorktreeItemCheckpointPath` occurs exactly once; `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` matches no line; the file still contains `PRD_FEATURE_BLOCKED: resolved feature folder '$probeFolder' is missing: ` and `the ''- Work Mode:'' marker is absent, unreadable, or unrecognized` verbatim.
- [ ] [P9-T5] Edit `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1` per the §7 entry for this file. Work in this order, because the deletions shift every later line number and the executor re-reads the file between steps rather than applying the numbers below to an already-edited file.
  1. Delete whole rows: `:168-179` (the three `Test-PrdFeatureSessionRootTarget` rows, whose subject DD-9 removes) and `:211-236` (`supplies the session root from the envelope cwd when the runtime sets one` and `omits the session root when the envelope carries no cwd field`, whose subject DD-4 removes). Both ranges are **pre-edit coordinates within this one step**, so delete the higher range `:211-236` first and `:168-179` second; taking them in the other order shifts the upper range by twelve lines before it is read. These deletions remove the `Resolve-WorktreeCallTarget` mocks at `:212` and `:226` and the `Should -Invoke` assertions at `:222` and `:235`.
  2. Repoint the seven surviving `Mock -CommandName Resolve-WorktreeCallTarget` occurrences — `:182`, `:190`, `:201`, `:264`, `:279`, `:291`, `:407` — to `Mock -CommandName Resolve-PrdFeatureWorktreeTarget`, and the three surviving `Should -Invoke -CommandName Resolve-WorktreeCallTarget` assertions — `:186`, `:197`, `:208` — to the same new name, keeping each one's `-Times` value. Statuses: `:182` stays `NoTarget`; `:190` stays `NoTarget`; `:201` stays `OtherWorktree`; `:264` stays `SessionRoot`; `:279` stays `OtherWorktree`; `:291` becomes `NoTarget` (a text carrying no identity is §2.2 row 2, not an ambiguity); `:407` becomes `SessionRoot` with `WorktreeRoot` `/synthetic-worktrees/session-root`, per step 5.
  3. Remove the `-Envelope $null` argument from the three `Get-PrdFeatureCallTarget` calls at `:184`, `:194`, `:205`, which DD-4's parameter removal would otherwise turn into binding errors.
  4. Apply the §7 renames and add `supplies the process location as the session root and reads no envelope cwd`. Its mechanism matters, because the naive form is both unsatisfiable and a host-data violation. Per DD-4, `Get-PrdFeatureCallTarget` passes only `-Text`; it is `Resolve-PrdFeatureWorktreeTarget` that supplies `-SessionRoot` to `Resolve-WorktreeItemTarget`. So the row must not mock the seam to capture a `-SessionRoot` the seam never receives, and it must not compare a captured value against a live location read, which would derive a path from the current directory — the very thing issue #672's test-hygiene criterion, checked off by [P11-T9], forbids. Use two assertions instead, neither of which reads a location and neither of which names the seam a second time, so the ten-occurrence count in this task's acceptance is unaffected:
     - **Structural.** Parse the hook with `[System.Management.Automation.Language.Parser]::ParseFile`, locate the `CommandAst` whose command name is `Resolve-WorktreeItemTarget`, and assert it carries a `CommandParameterAst` named `SessionRoot`; then assert the `ParameterAst` set of `Get-PrdFeatureCallTarget` contains no parameter named `Envelope`.
     - **Behavioural.** Mock `Resolve-WorktreeItemTarget` **in the test scope with no `-ModuleName`**, capture its `-SessionRoot` argument, call `Get-PrdFeatureCallTarget` with a text-bearing tool input, and assert the captured value is a non-empty string for which `[System.IO.Path]::IsPathRooted` returns true. That is a pure predicate over the captured value: it fails for an empty, null, or relative value and reads nothing from the host.

       This one mock is deliberately scope-different from the module-scoped `Get-WorktreeItemLiveRoot` mocks §4 mandates elsewhere, and the difference is not a slip. `-ModuleName` intercepts only calls originating inside the named module. After DD-4 the call to `Resolve-WorktreeItemTarget` sits in `Resolve-PrdFeatureWorktreeTarget`, which the suite dot-sources into its own `BeforeAll` scope at `:101-102`, so a module-scoped mock would not intercept it: the capture would stay unset, the `IsPathRooted` assertion would fail, and the real resolver would run and enumerate the host's live worktrees — making the row host-dependent in a way [P10-T7]'s token counts cannot see, because the enumeration is reached indirectly through the library. The suite's three working seam mocks at `:182`, `:190`, and `:201` use no `-ModuleName` for exactly this reason; §4's mocks are module-scoped because those functions are called from inside `Resolve-WorktreeItemTarget`.

     The row therefore contains no `Get-Location`, no `Set-Location`, and no `CurrentDirectory`, which [P10-T7] counts.
  5. In `denies rather than selecting the earliest candidate on an unresolved tie` (row at `:404`), the injected status must be resolved, not `NoTarget`: DD-5 denies on `NoTarget` before `Find-PrdFeatureFolderCandidate` runs, so a `NoTarget` result makes DD-7's tie branch unreachable and the row would assert a branch it never enters. Use `SessionRoot` with a synthetic `WorktreeRoot`, keep `Get-PrdFeatureCheckpointFolder` mocked to `$null`, and keep the row's name and its `cites 2 feature folders` assertion. Also rewrite the row's comment at `:405-406`, which reads that a `NoTarget` result carries a null `SignalValue` so the tie stays unresolved and the deny comes from the tie branch rather than the derivation's ambiguity branch: both halves are false after this step and DD-7, and like the comments in step 8 no acceptance condition would otherwise detect it.
  6. Change `New-ModelledTarget` to build results with `-Signal 'Branch'` and a branch-name `SignalValue`, and change its call sites to pass a branch name in place of a folder or file path. There are eleven call sites — `:250`, `:316`, `:332`, `:346`, `:358`, `:375`, `:393`, `:430`, `:450`, `:472`, `:488`, each naming the call's first line with its `-SignalValue` argument on the following line — and **`:488` is exempt (value on the following line, `:489`)**: it lies inside the surviving row `denies with the ambiguity reason when the folder is absent from the target root` (`:481-497`), which this task's acceptance and §2.7.4 both require to be unedited. Leaving its composed folder-shaped value in place is harmless, because after DD-7 the gate reads no `SignalValue` at all, so that argument is inert. Change the other ten.
  7. In the renamed `reads the checkpoint of the resolved worktree rather than the session root's` row (`:384-402`), **replace the four residual assertions at `:398-401`** with the allow assertion and the captured-path assertion: mock `Get-PrdFeatureCheckpointFolder` to capture `-CheckpointPath`, assert the captured value begins with `/synthetic-worktrees/item-worktree`, and assert the decision is `allow` when the item's checkpoint names a folder whose prerequisite is present beneath the item worktree. All four old assertions are incompatible with the new verdict and none may be retained: `:398` asserts `deny`, `:399` asserts the ambiguity code, `:400` asserts the other folder is absent from the reason, and `:401` asserts `Should -Invoke -CommandName Get-PrdFeatureFileExistence -Times 0 -Exactly`, which an `allow` verdict necessarily contradicts because the allow path reaches the prerequisite probe. Replace `:401` with a positive invocation count matching the row's work mode.
  8. Update the determinism statement (`:17-25`) to say the modelled directory is carried on the injected result's `SessionRoot` member and that the suite still creates nothing and changes no directory; and update the `Context 'call-target derivation seam'` comment at `:165-166`, which describes "the two parent-side functions the decision path calls" and is left stale by step 1's deletion of the `Test-PrdFeatureSessionRootTarget` rows — after that deletion only one parent-side function remains in the context.
  9. Add `$script:NoTargetCode = Get-WorktreeResolutionNoTargetReasonCode` to the `BeforeAll`, beside the existing `$script:AmbiguityCode` assignment at `:119`, and change the assertion at `:300` to read `$script:NoTargetCode`. That assertion belongs to the row step 2 flips to `NoTarget` at `:291`; `$script:AmbiguityCode` is the file's only reason-code variable today, so without this step the row denies with the correct code and still fails its own assertion.

  Work the nine steps in order. Acceptance: the file is at most 500 lines; `Resolve-WorktreeCallTarget`, `Test-PrdFeatureSessionRootTarget`, and `-Envelope` occur nowhere in it; `Resolve-PrdFeatureWorktreeTarget` occurs exactly ten times (seven mocks and three invocation assertions), the step-4 row adding none because it mocks `Resolve-WorktreeItemTarget` instead; the file defines exactly two reason-code variables, `$script:AmbiguityCode` and `$script:NoTargetCode`, each obtained only from its accessor; it contains no drive-letter path, no `TestDrive`, no `New-TemporaryFile`, no `Set-Location`, no `CurrentDirectory`, and no `Get-Location`; the surviving row `denies with the ambiguity reason when the folder is absent from the target root` (`:481-497`) is unedited, including its `New-ModelledTarget` call at `:488`; every name listed as renamed or added in the §7 entry appears verbatim exactly once, and no `It` name outside that list changed.
- [ ] [P9-T6] Edit `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.Tests.ps1`: change the blanket mock at `:13` from `Resolve-WorktreeCallTarget` to `Resolve-PrdFeatureWorktreeTarget` returning `New-WorktreeResolutionTargetResult -Status 'SessionRoot' -SessionRoot '/synthetic-worktrees/session-root' -WorktreeRoot '/synthetic-worktrees/session-root' -Signal 'Branch' -SignalValue 'f5-fixture-own' -Candidate @('/synthetic-worktrees/session-root') -Detail 'modelled session-root target for the delivered cases'`, so every row keeps the bare repo-relative probe spelling DD-8 preserves for a `SessionRoot` result; and replace all three of the file's drive-letter literals, not one. They are identified here by location and role rather than by spelling, under the host-data rule: the nonexistent-path literal at `:204`, which is the sole argument of `Get-PrdFeatureFileExistence -Path`, becomes `/synthetic-worktrees/session-root/docs/features/active/2026-09-13-no-such-folder/spec.md`; the nonexistent-checkpoint literal at `:208`, the sole argument of `Get-PrdFeatureCheckpointFolder -CheckpointPath`, becomes `/synthetic-worktrees/session-root/artifacts/orchestration/no-such-checkpoint.json`; and the nonexistent-feature-folder literal at `:293`, the sole argument of `Get-PrdFeatureIssueContent -FeatureFolder`, becomes `/synthetic-worktrees/session-root/docs/features/active/2026-09-13-no-such-folder`. The executor reads the three existing spellings from the file itself. Each replacement keeps its row's assertion unchanged, because every one of the three asserts the absent-path return value and none of the three paths exists under either spelling. No other line changes. Acceptance: `git diff <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-prd-feature-before-planner.Tests.ps1` with the recorded SHA substituted touches exactly four lines — the blanket mock and the three literals — and deletes no `It` line. The condition is stated in changed lines rather than in hunks because two of the three literals are four lines apart and would merge into one hunk under the default context width, making any hunk count an artefact of that width. The file contains no drive-letter path and no `Resolve-WorktreeCallTarget`; the file is at most 500 lines.
- [ ] [P9-T7] Edit `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`: change the blanket mock at `:28` exactly as [P9-T6] changes `:13`; and in the multi-candidate disambiguator row `It 'prefers the derived target when it occurs later in the prompt'` (`:95-107`), replace `-Target $target` at `:105` with `-CheckpointFolder 'docs/features/active/2026-08-23-second-2'` and **delete the now-unused `$target` hashtable assignment at `:98-102`**, which sits in the same hunk. The deletion is required, not cosmetic: `Invoke-PoshQCAnalyze` runs at Information severity as well as Error and Warning (`scripts/powershell/PoshQC/PoshQC.Analyzer.psm1:101`), so an assigned-and-never-read `$target` raises `PSUseDeclaredVarsMoreThanAssignments` and fails [P11-T2]. Update the row's comment at `:96-97` to state the disambiguator is now the resolved worktree's checkpoint, keeping the row's `It` name and its assertion that selection is not earliest-occurrence. The `Context` blocks pinned by issue #672's spec for the #518 truncation behaviour and for preserved gate behaviour are not otherwise edited, and `It 'denies with the indeterminate-marker reason when issue.md is unreadable'` (`:338-349`) is left byte-unchanged, which DD-8a is what makes possible. Acceptance: `git diff <F5_BASE_SHA> -- tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1` with the recorded SHA substituted touches only the blanket-mock line and lines inside the `prefers the derived target when it occurs later in the prompt` row, and deletes no `It` line; the file contains no `Resolve-WorktreeCallTarget`, no `-Target `, and no drive-letter path; the file is at most 500 lines. `-Target ` occurs exactly once in the file today, at `:105`, so that condition is satisfiable by this task alone.
- [ ] [P9-T8] Reset the batch budget per §6; append entry `B8` to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-batch-budget-resets.md`. Acceptance: the `B8` entry exists.
- [ ] [P9-T9] Create `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1` with the 13 `It` names of §7 for this file. The suite dot-sources the gate and its helpers, imports `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `WorktreeResolution.psm1`, and `WorktreeTargetResolution.psm1`, obtains both reason codes only from the two accessors, declares a head comment carrying the same determinism statement the sibling matrix suite carries, and uses only synthetic roots that are bare string literals or values composed from them per §4. Row 1 mocks `Get-WorktreeItemLiveRoot` and `Get-WorktreeItemCheckpointText` inside module `WorktreeItemResolution` so that twelve roots are live and exactly one records issue `901`, leaves `Resolve-PrdFeatureWorktreeTarget` unmocked, and asserts `allow`. Acceptance: the file exists, is at most 500 lines, contains the 13 names verbatim, contains neither reason-code literal, no drive-letter path, no `TestDrive`, no `New-TemporaryFile`, no `Set-Location`, no `CurrentDirectory`, no `Get-Location`, no `Resolve-Path` whose argument is not one of the two dot-sourced hook paths or one of the three imported module paths, and no `git` in a command position. The last three prohibitions match [P10-T7]'s five counted tokens exactly: without them a conforming file could still fail that gate, and no task between the two could repair it.
- [ ] [P9-T10] In `tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1`, add the `It` named `every production call to Get-PrdFeatureCheckpointFolder supplies CheckpointPath explicitly`, built on the same AST walk as the five sibling call-site rows, and add `.claude/hooks/enforce-prd-feature-before-planner.ps1` to the single array literal the literal test reads. No other line changes. Acceptance: the file contains the eight §7 names verbatim; it contains `.claude/hooks/enforce-prd-feature-before-planner.ps1` exactly once and `Get-PrdFeatureCheckpointFolder` exactly twice (the `It` name and the AST target name); its `It` count is 8; the file is at most 500 lines.
- [ ] [P9-T11] Mirror `.claude/hooks/enforce-prd-feature-before-planner.ps1` and `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1` by byte copy to the same relative paths under `extensions/drm-copilot/resources/claude-customizations/`; append the two commands (repository-relative) and their SHA-256 pairs to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-mirror-log.md`. Acceptance: two equal pairs.
- [ ] [P9-T12] Scoped verification. In a fresh process run `Invoke-PoshQCTest -Root $root -ScanFolders tests/scripts/claude-hooks,tests/scripts/claude-runtime -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r4-p9-scoped-pester.md` (four schema fields) with the report last-write time, root `failures` and `errors`, every `testcase` name and status under the four `enforce-prd-feature-before-planner*` testsuites and under `enforcement-hooks-checkpoint-path-explicit.Tests.ps1`, and the line coverage with raw counters for `.claude/hooks/enforce-prd-feature-before-planner.ps1` and `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`. Acceptance: last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0 apart from [P0-T10] exclusions; the four prd testsuites are all present; all 13 §7 names for the identity suite appear exactly once with status Passed; `every production call to Get-PrdFeatureCheckpointFolder supplies CheckpointPath explicitly` is Passed; both files' line coverage is at least 85%.
- [ ] [P9-T13] Commit Phase 9 with the method and gate handling of [P4-T7], staging `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, their two mirrors under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/`, the four `tests/scripts/claude-hooks/enforce-prd-feature-before-planner*.Tests.ps1` files, `tests/scripts/claude-runtime/enforcement-hooks-checkpoint-path-explicit.Tests.ps1`, and `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673`, with `-F <SCRATCHPAD>/r4-msg-p9.txt`. Take the porcelain observation post-commit and pre-append under rule 5, then append it to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`. Acceptance: `git log -1 --name-only` lists `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/enforce-prd-feature-before-planner.ps1`, and `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`; the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all.

### Phase 10 — Structural, Coupling, and Constraint Verification

- [ ] [P10-T1] AC-7 and AC-8. Run `Select-String -SimpleMatch -Pattern 'orchestrator-state.json'` over the five in-scope hook files (the four of §2.6 plus `.claude/hooks/enforce-prd-feature-before-planner.ps1`) and classify each match as code or comment; read every branch of `Get-PrAuthorTargetCheckpointResolution`, `Get-ModelRoutingTargetCheckpointResolution`, the DD-7 branch in `Invoke-PrdFeatureBeforePlannerDecision`, and `Resolve-WorktreeItemTarget` in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-binding-removal.md` (four schema fields). Acceptance: zero code matches; each comment match listed with file and line; one line per branch stating its outcome, with no branch yielding a checkpoint path other than through `Get-WorktreeItemCheckpointPath` of a resolved `WorktreeRoot`.
- [ ] [P10-T2] AC-23. Run `git diff --name-only <F5_BASE_SHA> HEAD` and `git status --porcelain`; drop paths under either feature folder; run `Select-String -SimpleMatch` for `TARGET_WORKTREE_AMBIGUOUS` and for `TARGET_WORKTREE_NOT_DERIVABLE` over the remaining files. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-reason-code-literal-search.md` (four schema fields) with the file list and both counts. Acceptance: `git status --porcelain` lists no path outside the two feature folders; both counts are 0.
- [ ] [P10-T3] AC-24 and AC-25. Record every `Import-Module` line and every call to a worktree-resolution library function in the five in-scope hook files and in `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, every `SilentlyContinue` in them, and every function defined in them. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-library-consumption.md` (four schema fields). Acceptance: `enforce-model-routing-receipt.ps1`, `enforce-pr-author-skill-helpers.ps1`, and `enforce-prd-feature-before-planner.ps1` each import `WorktreeItemResolution.psm1` at column 0 with `-Force` or `-ErrorAction Stop` as their file's existing convention requires; `enforce-prd-feature-before-planner.ps1` additionally imports `WorktreeTargetResolution.psm1` because it calls `Join-WorktreeResolutionPath`, which that module alone exports; `enforce-pr-author-skill-helpers.ps1` retains both F1 imports for a different and explicitly different reason — after Phase 5 neither serves a call in that file, and what depends on them is the suites that dot-source it, which resolve the two reason-code accessors and `New-WorktreeResolutionTargetResult` through the resulting session state; `enforce-prd-feature-before-planner-helpers.ps1` still imports `WorktreeResolution.psm1`; the artifact records, for every worktree-resolution function called in these files, which of three resolution categories applies — (i) exported by a module the same file imports, (ii) exported by a module imported by a file dot-sourced into the same session state, naming that file and its import line, or (iii) reached only through a sibling module whose exports are pinned by an explicit `Export-ModuleMember` list — and no call falls in category (iii), which is the module-nesting case this condition exists to catch and the one that does not resolve. Category (ii) is sound and is the route the prd gate's two accessor calls take through `enforce-prd-feature-before-planner-helpers.ps1:27`; zero `SilentlyContinue`; no function defined in a hook file derives a target, normalises a path, or returns a reason code; every such use is a call into `.claude/lib/worktree-resolution/` or a read of the result's `ReasonCode`.
- [ ] [P10-T4] AC-27. Run `Select-String -SimpleMatch` for `python`, `poetry`, `py -3`, and `Start-Process` over the five in-scope hook files, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, and `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, classifying each match as code or comment. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-python-free.md` (four schema fields), naming `tests/scripts/claude-runtime/enforcement-hooks-no-python-invocation.Tests.ps1` as the #475 guard with scan roots at `:40-41`, enumerated at `:60`. Acceptance: zero code matches; the guard path and both citations recorded (its pass result is asserted in [P11-T4]).
- [ ] [P10-T5] AC-28 and AC-37. Run `git diff --name-only <F5_BASE_SHA> HEAD` and `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-coupling-guard.md` (four schema fields) with two sections, `Absent:` and `Present:`. Acceptance: the diff lists none of `.claude/hooks/hook-command-scanner.ps1`, `.claude/hooks/hook-command-invocation.ps1`, `.claude/hooks/enforce-epic-merge-gate.ps1`, `.claude/lib/orchestrator-state/OrchestratorState.psm1`, `.codex/hooks/enforce-codex-model-routing.ps1`, `.claude/lib/worktree-resolution/WorktreeResolution.psm1`, `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1`; the diff lists the **eight** §2.7.6 paths other than `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`, which [P11-T9] has not yet modified at this point and whose presence is therefore asserted there instead; the porcelain output lists no path outside the two feature folders (rule 5).
- [ ] [P10-T6] AC-30. Record `@(Get-Content).Count` for every `.ps1`, `.psm1`, `.psd1`, and `.py` path in `git diff --name-only <F5_BASE_SHA> HEAD`, and run `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-line-counts.md` (four schema fields). Acceptance: `git status --porcelain` lists no path outside the two feature folders (rule 5); every count is at most 500; `tests/scripts/claude-lib/orchestrator-state/OrchestratorState.Tests.ps1`, `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, the four `tests/scripts/claude-hooks/enforce-prd-feature-before-planner*.Tests.ps1` files, and all six `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/` mirrors are listed.
- [ ] [P10-T7] AC-32 and RS-19. Search every test file and helper added or modified by this plan for `New-TemporaryFile`, `GetTempPath`, `GetTempFileName`, `TestDrive`, and `$env:TEMP`; list every assignment to `$env:CLAUDE_TOOL_INPUT` and every `Set-Location` or `CurrentDirectory` assignment with its enclosing construct; and, separately for `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1` and `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`, record the count of `Set-Location` occurrences, the count of `CurrentDirectory` occurrences, the count of `Get-Location` occurrences, the count of `Resolve-Path` occurrences whose argument is not one of the two dot-sourced hook paths or one of the three imported module paths, and the count of occurrences of `git ` inside a command position. `Get-Location` is counted because it is the one remaining way a row in these two suites could derive a path from the current directory, and the other four tokens would not catch it. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-no-temp-files.md` (four schema fields). Acceptance: zero token matches; every `CLAUDE_TOOL_INPUT` assignment sits in a `try` whose `finally` restores it; every `Set-Location` or `CurrentDirectory` assignment sits in `tests/scripts/claude-hooks/WorktreeResolutionFixture.Helpers.ps1` inside a `try` whose `finally` restores both, or inside a child-process script string; for the two prd suites all five recorded counts are 0.
- [ ] [P10-T8] AC-19 and AC-20. Run `git diff <F5_BASE_SHA> HEAD -- tests/scripts/claude-hooks/enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1 tests/scripts/claude-hooks/enforce-model-routing-receipt.Tests.ps1 tests/scripts/claude-hooks/enforce-pr-author-skill.epic-base-branch.Tests.ps1`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/regression-testing/r3-ac19-byte-identity.md` (four schema fields) with the diff and one verdict per protected row. Acceptance: the diff for `enforce-pr-author-skill.OrchestratorStatePreflight.Tests.ps1` is empty; the diff for `enforce-model-routing-receipt.Tests.ps1` contains only the [P7-T3] insertion; the diff for `enforce-pr-author-skill.epic-base-branch.Tests.ps1` contains only the seven [P5-T6] argument additions; no `It` name and no line containing `Should` in a protected row changes.
- [ ] [P10-T9] AC-38. In a fresh process, with values computed at run time and never written, apply the four [P0-T16] matchers to every file in `git diff --name-only <F5_BASE_SHA> HEAD` except `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/plan.2026-09-13T20-48.md`, `.../plan.2026-09-18T13-30.md`, and `.../plan.2026-09-18T16-00.md`, and run `git status --porcelain`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-host-token-scan.md` (four schema fields) with the number of files scanned and the per-matcher match counts, no matched text (rule 6 forbids quoting a matched token). Acceptance: `git status --porcelain` lists no path outside the two feature folders (rule 5); the files-scanned count is greater than 0; all four match counts are 0.

### Phase 11 — Final QC Loop (fresh process, CI-equivalent)

Run [P11-T1] to [P11-T10] in order. If any step fails or changes a file, fix the cause and restart from [P11-T1]; artifacts of an abandoned pass are kept and labelled with their pass number. The gate is one pass in which every step succeeds and changes nothing.

- [ ] [P11-T1] Format. Record `git status --porcelain`; in a fresh process run `Invoke-PoshQCFormat -Root $root` (the command at `.github/workflows/_poshqc.yml:26`); record `git status --porcelain` again. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-poshqc-format.md` (four schema fields) with the pass number, the counts of lines beginning `Formatted: ` and `Already formatted: `, and a `Tree Delta:` section. Acceptance: `Formatted: ` count 0, `Already formatted: ` count greater than 0, the two porcelain outputs identical.
- [ ] [P11-T2] Analyze. In a fresh process run `Invoke-PoshQCAnalyze -Root $root` from `scripts/powershell/PoshQC/PoshQC.psd1` (`_poshqc.yml:36`). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-poshqc-analyze.md` (four schema fields). Acceptance: `EXIT_CODE: 0` and the output contains `PSScriptAnalyzer passed: no findings under`.
- [ ] [P11-T3] Python format, lint, and type for `tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`: run `poetry run black --check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, `poetry run ruff check tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, and `poetry run pyright tests/scripts/dev_tools/parallel_orchestrator_surface_expectations.py`, one artifact each: `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-black.md`, `.../r3-final-ruff.md`, `.../r3-final-pyright.md` (four schema fields each). Acceptance: all three `EXIT_CODE: 0`; the outputs contain `would be left unchanged`, `All checks passed!`, and `0 errors` respectively.
- [ ] [P11-T4] Full Pester with coverage over the whole configured `Run.Path` (`pester.runsettings.psd1:3`). In a fresh `pwsh -NoProfile -File` process (route per [P0-T7]) run `Invoke-PoshQCTest -Root $root -SettingsPath scripts/powershell/PoshQC/settings/pester.runsettings.psd1` (`_poshqc.yml:42`). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-pester-coverage.md` (four schema fields), per §5: report last-write time; root `tests`, `failures`, `errors`, `disabled`; the status of every §7 name and every AC-19 `It` name recorded in [P0-T6]; the testsuites ending `enforcement-hooks-no-python-invocation.Tests.ps1`, `WorktreeResolution.Manifest.Tests.ps1`, `WorktreeTargetResolution.Tests.ps1`, `OrchestratorState.Tests.ps1`, `test-name-uniqueness.Tests.ps1`, `ClaudeLibModuleConvention.Tests.ps1`, and the four `enforce-prd-feature-before-planner*.Tests.ps1` suites; and line coverage with raw counters for the four in-scope hooks of §2.6, `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, `.claude/hooks/enforce-prd-feature-before-planner.ps1`, and `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`. Acceptance: `EXIT_CODE: 0`; last-write time at or after `Timestamp:`; root `failures` 0 and `errors` 0 with no exclusion; every §7 name and every AC-19 name appears exactly once with status Passed; the ten named testsuites have `failures` 0; each of the seven files has line coverage of at least 85%.
- [ ] [P11-T5] Coverage delta. From [P0-T10] and [P11-T4], and from `git diff -U0 <F5_BASE_SHA> HEAD -- .claude/hooks/enforce-pr-author-skill.ps1 .claude/hooks/enforce-pr-author-skill-helpers.ps1 .claude/hooks/enforce-pr-author-skill.epic-base-branch.ps1 .claude/hooks/enforce-model-routing-receipt.ps1 .claude/hooks/enforce-prd-feature-before-planner.ps1 .claude/hooks/enforce-prd-feature-before-planner-helpers.ps1 .claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, compute per file the baseline percent, post percent, and changed-line coverage per §5. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-coverage-delta.md` (four schema fields). Acceptance: twenty-one values recorded (the new module's baseline as `n/a (new file)`); every post percent at least 85%; every hook's post percent at least its baseline; every changed-line figure at least 85%. If the report carries no `line` elements, record that and mark the task remediation-required.
- [ ] [P11-T6] Full Python suite. Run `poetry run pytest -q`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-pytest.md` (four schema fields) with the summary line verbatim. Acceptance: `EXIT_CODE: 0` and the summary line carries no `failed` or `error` count.
- [ ] [P11-T7] Mirror gate. Run `poetry run pytest tests/scripts/dev_tools/test_push_down_claude_resource_contracts.py::test_bundled_claude_payload_contains_all_repo_runtime_contracts -q` and compute SHA-256 for the pairs: the four in-scope hooks of §2.6, the two prd gate files, `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`, the three skills of [P8-T7], and `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`, each against its counterpart under `extensions/drm-copilot/resources/`. Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-final-mirror-gate.md` (four schema fields). Acceptance: `EXIT_CODE: 0`; the summary line reads `1 passed`; eleven pairs with equal hashes.
- [ ] [P11-T8] Seven-stage record (AC-33). Write `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/qa-gates/r3-seven-stage-loop.md` (four schema fields) mapping the stages to the clean pass: formatting [P11-T1] and the black step of [P11-T3]; linting [P11-T2] and the ruff step; type checking the pyright step (PowerShell not applicable per `.claude/rules/powershell.md`); architecture-boundary tests not applicable (record `Select-String -SimpleMatch -Pattern 'dependency-cruiser','NetArchTest'` over `tests/scripts` with its count); unit tests [P11-T4] and [P11-T6]; contract and schema checks [P11-T7] plus the `WorktreeResolution.Manifest.Tests.ps1` and frozen-pin results; integration tests the fixture rows and the end-to-end child-process row inside [P11-T4]. Also record, in a section headed `Single-Pass Statement:`, that [P11-T1], [P11-T2], and [P11-T4] completed in one pass with no file rewritten and no failure, and name the pass number. This task text authorizes the one not-applicable architecture entry and no other. Acceptance: seven stage rows, each naming its task or its reason, the pass number, and the `Single-Pass Statement:` section.
- [ ] [P11-T9] Close the two criteria still unchecked in issue #672's spec. In `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`, change the leading `- [ ] ` to `- [x] ` on exactly the two lines [P0-T2] recorded under `Issue 672 Unchecked:`, changing no other character of either line; then append at the end of the file a subsection headed `### Closure of the two outstanding criteria (issue #673 change set, 2026-09-19)` naming each criterion by its opening clause and citing, for the test-hygiene criterion, `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-no-temp-files.md` ([P10-T7], which records zero `Set-Location`, zero `CurrentDirectory`, zero environment-derived `Resolve-Path`, and zero source-control invocations in both prd suites) and `.../evidence/qa-gates/r4-p9-scoped-pester.md`, and, for the PowerShell-toolchain criterion, `.../evidence/qa-gates/r3-final-poshqc-format.md`, `.../r3-final-poshqc-analyze.md`, `.../r3-final-pester-coverage.md`, and `.../r3-seven-stage-loop.md` with its `Single-Pass Statement:` section. Write the same text to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r4-issue-672-closure.md` (four schema fields). Acceptance: `Select-String -SimpleMatch -Pattern '- [ ] '` over the #672 spec returns exactly the three priority-checkbox lines at `:24-26` and nothing below its `## Acceptance Criteria` heading; the spec contains the closure heading exactly once; each of the six cited artifact paths exists; and `git diff --name-only <F5_BASE_SHA> HEAD` together with `git status --porcelain` now accounts for `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`, the ninth §2.7.6 path, which [P10-T5] could not yet see. The two commands are paired because the file is tracked and modified rather than created, so the anchored diff lists it once [P11-T10] commits and the porcelain lists it before then; one of the two must show it.
- [ ] [P11-T10] Final ordering and commit. Commit every file changed during Phases 10 and 11, including the #672 spec edited by [P11-T9], with the method of [P4-T7] and `-F <SCRATCHPAD>/r3-msg-p10.txt`. Take the porcelain observation post-commit and pre-append under rule 5. Run `git rev-list --reverse <F5_BASE_SHA>..HEAD` and `git log --reverse --format=%H <F5_BASE_SHA>..HEAD -- .claude/hooks`, then append to `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md` the positions of `EVIDENCE_COMMIT` ([P0-T4]) and of the first `.claude/hooks` commit, together with that observation. Because this is the plan's last task, make **one further commit** whose only content is that append, with the message read from `-F <SCRATCHPAD>/r3-msg-p11.txt`; without it the final append never reaches the branch. Acceptance: `EVIDENCE_COMMIT`'s position is lower than the first hooks commit's (AC-3); the recorded `Porcelain (post-commit, pre-append):` observation lists no path at all; after the further commit, `git log -1 --name-only` lists exactly `docs/features/active/2026-09-13-false-approval-elimination-pr-author-model-routing-673/evidence/other/r3-commits.md`, and `git status --porcelain` is then genuinely empty, this being the one point in the plan where nothing is left uncommitted.

## 8. Residual risk and follow-up (not tasks)

- **Other gates that read a cwd-relative checkpoint** are where hygiene (design A) still matters: `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1:27`, `.claude/hooks/enforce-completion-consistency.ps1:300`, and `.claude/hooks/enforce-epic-merge-gate.ps1:63` (F3). None is changed here; all three are tracked under issue #690 and are out of scope for this plan.
- **prd-feature probe spelling for a `SessionRoot` result (DD-8).** When identity resolves to the worktree the gate is running in, the probe keeps the bare repo-relative spelling. A process whose directory is a *subdirectory* of that worktree would then probe a path that does not resolve, producing a false denial. The failure mode is a visible stall, never an approval, and the behaviour is unchanged from before this plan. Closing it means composing the absolute path unconditionally, which would rewrite the probe-path assertions of two suites that sit within fifty lines of the 500-line cap; it is recorded here rather than bundled in.
- **First-match branch reading.** F1's reader returns the first `--head`, `--branch`, or `branch:` match. A prompt with an unrelated `branch:` phrase earlier than the item's label is misread. With the issue line present, a misread branch disagrees with the issue and denies (§2.2 rows 7 and 8); without it, a misread branch whose worktree holds a stale checkpoint with a matching receipt could approve, which hygiene (no stale checkpoint at coordinator roots) and the skill rule (label first; issue line always once assigned) are there to prevent.
- **Post-merge confirmation.** Isolated subagents run `main`'s hooks, so end-to-end behaviour is observable only after merge. The spec's rollout item (confirm no gate allows on a sibling's checkpoint in the next parallel run) is the confirmation, and it now also covers the prd-feature gate.
- **Pre-promotion delegations** (for example research before S3) carry no issue number; they are identified by the `branch:` label alone and deny until prompts carry it. This now includes `atomic-planner` delegations made before promotion, which the prd-feature gate also reads.
- `gh pr edit --body-file` still carries no `--head` and still denies with the no-target code (#687 behaviour, unchanged).
- The three superseded plans retain host tokens; the instruction not to edit them takes precedence, and [P10-T9] excludes them by name.
- `.claude/skills/orchestrate/SKILL.md:256` says to record `issue_num`, while the validator requires `issue-num` (`scripts/dev_tools/validate_orchestrator_state.py:57`); the resolver reads `issue-num`. The Codex twin `.agents/skills/orchestrate/SKILL.md` is not updated (Codex surface excluded by the spec). `.codex/hooks/` carries no mirror of the prd-feature gate, so #672 adds no Codex parity work.

## 9. Criteria added to `spec.md` by [P1-T2] (verbatim)

[P1-T2] writes each criterion below into `spec.md` as an unchecked checkbox item: the characters `- [ ] ` followed by the criterion text exactly as quoted. They are shown here without the checkbox prefix so that the plan validator does not parse them as task lines.

- AC-34 `.claude/skills/orchestrate/SKILL.md` (`## Checkpoint Handling`), `.claude/skills/parallel-orchestrate/SKILL.md` (`## Per-Item Branch and Worktree Lifecycle`), and `.claude/skills/epic-orchestrate/SKILL.md` (`## Epic-Level Checkpoint`) each state the checkpoint-hygiene rule: a per-feature `artifacts/orchestration/orchestrator-state.json` that does not belong to the session's own item is moved to `artifacts/orchestration/handoff/orchestrator-state.issue-<issue-num>.<yyyy-MM-ddTHH-mm>.json`, and a coordinating session writes no per-feature checkpoint at its own root. Verified by the three hygiene tests in `tests/scripts/claude-runtime/checkpoint-hygiene-skill-contract.Tests.ps1`.
- AC-35 `## Issue Number Consistency` in `.claude/skills/orchestrate/SKILL.md` requires the canonical issue number line and a `branch:` label on every delegation to each receipt-gated subagent type returned by `Get-ModelRoutingGatedAgent`, and names both gates that identify the item from the delegation prompt. Verified by the four delegation-identity tests in the same file.
- AC-36 The three bindings select the target worktree by portable identity only, through `Resolve-WorktreeItemTarget` in `.claude/lib/worktree-resolution/WorktreeItemResolution.psm1`: the canonical issue number, matched against the `issue-num` of each live worktree's checkpoint, and a branch signal (`--head`, `--branch`, `branch:`). A worktree is live when it is registered in the repository's worktree administrative layout and its root still carries a worktree marker. A feature-folder path or file path never selects the worktree. No identity, or an identity matching no live worktree, denies with the no-target code; several live matches or disagreeing identities deny with the ambiguity code; a branch signal breaks a tie among live worktrees recording the same issue. Verified by the named tests in `tests/scripts/claude-lib/worktree-resolution/WorktreeItemResolution.Tests.ps1`.
- AC-37 The prd-feature gate (issue #672) selects the worktree by the same portable identity and uses the feature folder only to locate prerequisite documents. `.claude/hooks/enforce-prd-feature-before-planner.ps1` calls `Resolve-WorktreeItemTarget` through its own one-line seam, contains no occurrence of `Resolve-WorktreeCallTarget`, and declares no `Envelope` parameter and reads no `cwd` field from the hook envelope. No feature-folder path and no file path selects the worktree: the resolver receives the assembled prompt and description text unchanged and reads no path signal from it, deciding only on the canonical issue number and a branch signal. After identity resolves, the gate resolves the feature folder beneath the resolved worktree and probes its prerequisites there; when the prompt names no folder it reads the `feature-folder` field from the checkpoint of the resolved worktree, never from the calling session's root; when the prompt names more than one folder that checkpoint is the disambiguator. The work-mode contract is unchanged: `full-feature` requires `spec.md` and `user-story.md`, `full-bug` requires `spec.md`, `minor-audit` requires neither, and an absent or unrecognised marker denies on its own path naming no prerequisite set. A prerequisite document that is genuinely absent still denies with the gate's existing missing-document reason, which carries neither target-resolution code. A coordinating-session `atomic-planner` delegation for an item whose feature folder also exists in other worktrees resolves by identity and is allowed, where it previously denied with the ambiguity code. Expected in the diff for this criterion: `.claude/hooks/enforce-prd-feature-before-planner.ps1`, `.claude/hooks/enforce-prd-feature-before-planner-helpers.ps1`, their two counterparts under `extensions/drm-copilot/resources/claude-customizations/.claude/hooks/`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.Tests.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.FolderResolution.Tests.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.TargetResolution.Tests.ps1`, `tests/scripts/claude-hooks/enforce-prd-feature-before-planner.IdentityResolution.Tests.ps1`, and `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md`. `.claude/lib/worktree-resolution/WorktreeResolution.psm1` and `.claude/lib/worktree-resolution/WorktreeTargetResolution.psm1` remain absent from the diff.
- AC-38 No file added or modified by this change, other than `plan.2026-09-13T20-48.md`, `plan.2026-09-18T13-30.md`, and `plan.2026-09-18T16-00.md`, contains a drive-letter path, a user-profile path, or the executing user's account name. Feature-folder artifacts that carried such tokens are redacted to placeholders, with per-file counts logged under `evidence/other/`.
- AC-39 The two acceptance criteria left unchecked in `docs/features/active/2026-09-13-prd-feature-gate-target-resolution-672/spec.md` — the test-hygiene criterion for the target-resolution suite and the PowerShell-toolchain single-pass criterion — are checked in that file, and a closure subsection in the same file cites the evidence artifact satisfying each.

## 10. Design requirement traceability (user-approved design, 2026-09-18 and 2026-09-19)

| Item | Where planned | Verified by |
| --- | --- | --- |
| A. Checkpoint hygiene | §2.4; [P8-T2] to [P8-T5] | AC-34; [P8-T8]; [P11-T4] |
| B. Portable identity, liveness, stale-attempt rule | §2.1, §2.2; [P2-T2] to [P2-T6]; [P5-T3]; [P7-T2] | AC-35, AC-36; [P2-T6]; [P7-T6]; [P11-T4] |
| C. Deny unidentifiable targets only after the scope filter | §2.5, DD-3 | AC-16, AC-17; [P11-T4] |
| D. Standalone PR to `main` | header; RS-9, RS-13 | [P10-T5] |
| Signal precedence (point 5) | §2.3 | AC-36, AC-37; [P10-T5] |
| prd-feature gate on identity resolution (#672 requirement 1) | §2.7.1, DD-4, DD-5; [P9-T4] | AC-37; [P9-T12]; [P11-T4] |
| Folder used only to locate documents (#672 requirement 2) | §2.7.2, DD-7, DD-8; [P9-T3], [P9-T4] | AC-37; [P9-T9] rows 1, 7, 8; [P9-T12] |
| Checkpoint fallback anchored to the resolved worktree (#672 requirement 3) | §2.7.2, DD-6; [P9-T4] | AC-37; [P9-T9] rows 5 and 6; [P9-T10] call-site test |
| Work-mode contract preserved (#672 requirement 2) | §2.7.3 | AC-37; the four work-mode rows retained in [P9-T5]; [P9-T9] row 11 |
| Genuine denies preserved (#672 requirement 4) | §2.7.4 | AC-37; [P9-T9] rows 7 and 12 |
| AC-37 replacement (#672 requirement 5) | §9 | [P10-T5] `Present:` section for eight of the nine §2.7.6 paths; [P11-T9] for the ninth, the #672 spec, which no phase before it modifies |
| #672's two outstanding criteria closed (#672 requirement 6) | [P11-T9] | AC-39; [P10-T7]; [P11-T8] |
| Coordinator regression case (#672 requirement 7) | §2.7.5; [P9-T2], [P9-T9] row 1 | AC-37; [P9-T12]; [P11-T4] |
| Byte-identical mirrors (#672 requirement 8) | [P9-T11] | [P11-T7] |
| No host data | Evidence rules; [P1-T5] | AC-38; [P10-T9] |
