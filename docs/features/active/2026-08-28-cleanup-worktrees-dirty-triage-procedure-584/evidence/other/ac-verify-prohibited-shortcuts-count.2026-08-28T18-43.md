Timestamp: 2026-08-28T18-43 (re-verified against the revised plan's corrected acceptance text)
Command: sed -n '/^## Prohibited Shortcuts/,/^## /p' .claude/skills/cleanup-merged-worktrees/SKILL.md | sed '1d;$d' | grep -c "^- "
EXIT_CODE: 0
Output Summary: `6`. This is the plan's corrected sed form (`sed '1d;$d'` drops the range's heading line and terminating heading line rather than relying on the naive awk range, which self-terminated on the heading and undercounted to 0 in the original run). Matches the revised plan's expected post-cherry-pick count of `6` exactly (P0-T5 baseline of 5, +1).

Reconciliation: the net top-level bullet-count growth is `+1`, not the originally-assumed `+2`, because the cherry-picked commit substantially expands one pre-existing bullet ("Never act on `NOT_MERGED`, `HAS_UNIQUE_RESIDUALS`, or `PROTECTED_CURRENT` candidates...", grown from 2 lines to 7 lines) to additionally cover the new triage verdict, rather than adding it as a fully separate second new bullet, while also adding exactly one wholly new bullet ("Never delete an origin branch, or run plain filesystem removal on an orphaned worktree-tracking directory..."). This was independently confirmed twice by direct inspection of the section text (line-by-line bullet enumeration) before the plan revision, and is the correct, already-authored content of the cherry-picked commit — not a defect to fix in `.claude/skills/cleanup-merged-worktrees/SKILL.md`. PASS.
